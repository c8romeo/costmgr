# Code Review Triage — Story 0.2

**Story**: 0-2-supabase-multi-tenancy-schema-rls-policies
**Review date**: 2026-07-25
**Reviewers**: blind (33 findings) + edge (41 findings) + auditor (8 findings) = 82 raw
**After dedup**: 38 unique findings
**After route**: 16 PATCH · 6 DECISION_NEEDED · 13 DEFER · 3 DISMISS

## Verdict: ❌ FAIL — 4 CRITICAL items in the RLS test CI pipeline, 2 CRITICAL AC deviations

Story 0.2 cannot be promoted to `done` without patching the RLS CI pipeline (it would fail in CI on the first run) and the spec-deviating items in AC #3 and AC #5. Defer the rest to Story 0.4 / later epics.

## Unified Triage Table (sorted by severity, then route)

| # | ID | Severity | Source | Title | Location | Route |
|---|---|---|---|---|---|---|
| 1 | RLS-CI-1 | critical | blind+edge | alembic uses `DATABASE_URL` but CI sets only `RLS_TEST_DATABASE_URL` | ci.yml:144, env.py:31 | **PATCH** |
| 2 | RLS-CI-2 | critical | blind+edge | alembic env strips asyncpg driver but uses `async_engine_from_config` | env.py:31-34, :63-67 | **PATCH** |
| 3 | RLS-CI-3 | critical | blind+edge | plain `postgres:15` image lacks `auth.jwt()` and `service_role` role | ci.yml:130, 0001_rls_policies.sql:32,110 | **PATCH** |
| 4 | RLS-CI-4 | critical | blind+edge | tenant isolation tests connect as Postgres superuser → BYPASSRLS | test_tenant_isolation.py:19, conftest:42 | **PATCH** |
| 5 | RLS-CI-5 | critical | blind+edge | `SET LOCAL` issued without explicit transaction (asyncpg autocommit) | test_tenant_isolation.py:32-36 | **PATCH** |
| 6 | RLS-CI-6 | critical | blind+edge | UPDATE/DELETE assertions expect exception but PG returns `UPDATE 0`/`DELETE 0` | test_tenant_isolation.py:113-118, 135-138 | **PATCH** |
| 7 | RLS-CI-7 | high | blind+edge | static emails `a@example.com`/`b@example.com` with no cleanup → UNIQUE conflict on re-run | conftest:108-129 | **PATCH** |
| 8 | TENANT-1 | critical | blind | FastAPI dep never publishes tenant to listener; `set_tenant_local()` has no caller | tenant_context.py:52-62, 119-130 | **PATCH** |
| 9 | TENANT-2 | critical | blind | SQLAlchemy `begin` event listener signature wrong; registered on `sync_engine` of async engine → never fires for AsyncSession | tenant_context.py:104-112 | **PATCH** |
| 10 | AUTH-1 | critical | blind | `AuthError` has no FastAPI exception handler → 500 instead of 401 with AD-15 contract | main.py:11-39 | **PATCH** |
| 11 | AUDIT-1 | critical | blind | `with_service_role` flushes audit but does not commit; spec requires committed BEFORE action; rollback also removes audit | service_role.py:102-106 | **DECISION_NEEDED** |
| 12 | AUDIT-2 | high | blind | trigger + `ON DELETE SET NULL` on audit_logs.tenant_id makes tenants undeletable | migration:115, :143-153 | **PATCH** |
| 13 | LINT-1 | critical | blind | `service-role-guard-lint` grep false-positives on `supabase_service_role_key` and `service_role bypass records` comment | settings.py:43, audit.py:6, ci.yml:111-118 | **PATCH** |
| 14 | AC3-1 | critical | auditor | spec mandates `request.state.tenant_id` populated; code sets `request.state.tenant = ctx` | tenant_context.py:61-62 | **PATCH** |
| 15 | AC3-2 | critical | auditor | spec mandates Korean error string "다른 테넌트 데이터에 접근할 수 없습니다"; never appears in code | security.py:81-146 | **DECISION_NEEDED** |
| 16 | AC5-1 | critical | auditor | test seeds 1 user per tenant, spec requires 2 | conftest:104-122 | **PATCH** |
| 17 | JWT-1 | high | blind | JWT omits `iss`/`aud`/`typ` validation | security.py:91-99 | **DEFER** (0.4 / security review) |
| 18 | JWT-2 | medium | blind | JWT error details leak library exception class name | security.py:105-110 | **PATCH** |
| 19 | RLS-1 | high | blind | RLS INSERT/UPDATE/DELETE policies lack role predicates → members/viewers can mutate | 0001_rls_policies.sql:52-99 | **DEFER** (Epic 1+ role isolation) |
| 20 | RLS-2 | high | blind | RLS policy deployment not idempotent (bare `CREATE POLICY`) | 0001_rls_policies.sql:30-136 | **PATCH** |
| 21 | RLS-3 | medium | blind | RLS smoke test is vacuous (checks only `policyname`, not `tablename`/command) | 0002_rls_smoke_test.sql:18-24 | **PATCH** |
| 22 | RLS-4 | medium | blind | audit-log read policy comment says "owner only" but no role check | 0001_rls_policies.sql:101-106 | **DEFER** (Epic 5 Story 5.2) |
| 23 | RLS-5 | medium | blind+auditor | RLS policy naming split into 4 per table (spec said singular) | 0001_rls_policies.sql:30-100 | **DECISION_NEEDED** |
| 24 | RLS-6 | high | blind+edge | non-UUID `app_metadata.tenant_id` raises raw cast error | 0001_rls_policies.sql:30-136 | **DEFER** (Supabase API gateway validates; not Story 0.2 in scope) |
| 25 | SCHEMA-1 | medium | blind | `tenant_memberships.role` has no CHECK constraint | migration:79-86 | **DEFER** (Story 0.4) |
| 26 | SCHEMA-2 | medium | blind | membership FKs allow cross-tenant user attachment | migration:81-85 | **DEFER** (Story 0.4) |
| 27 | SCHEMA-3 | medium | blind | JSONB columns have no GIN indexes | migration:96-99, :155-168 | **DEFER** (when queries land) |
| 28 | SCHEMA-4 | medium | blind | `settings_version` accepts 0/negative | migration:95 | **DEFER** (Story 0.4) |
| 29 | PYD-1 | high | blind | pydantic-core pin in dev extras only — production install can resolve broken 2.46.4 | apps/api/pyproject.toml:7-23 | **PATCH** |
| 30 | CI-1 | high | blind | `rls-tests` job runs pytest without `pytest --junitxml` — no actionable artifact | ci.yml:166-175 | **PATCH** |
| 31 | CI-2 | high | blind | service-role audit tests never exercise a real DB | test_service_role_audit.py:2-3, 96-139 | **DEFER** (CI-gated integration tests land with Supabase local stack) |
| 32 | CI-3 | medium | blind | `psql` is run without `-v ON_ERROR_STOP=1`; CI continues on policy-apply error | ci.yml:160-167 | **PATCH** |
| 33 | DOC-1 | low | blind | migration comment references non-existent `supabase/migrations/0001_platform_tenants.sql`; `supabase db push` doesn't run Alembic | migration:11-13, supabase/README.md:29-31 | **PATCH** |
| 34 | PIN-1 | medium | blind | `import-linter>=2.0` violates AD-14 exact pin | pyproject.toml:35-37 | **PATCH** |
| 35 | AC4-1 | minor | auditor | helper signature changed from `(reason, actor_id, action)` to kw-only async ctx mgr | service_role.py:57-66 | **DECISION_NEEDED** |
| 36 | AC5-2 | minor | auditor | test uses `SELECT tenant_id`/`SELECT id` instead of `count(*)`/`SELECT *` | test_tenant_isolation.py:51-72 | **PATCH** |
| 37 | AC5-3 | high | auditor | "audit row is present immediately before the privileged write" test is mock-only | test_service_role_audit.py:96-139 | **DEFER** (= CI-2) |
| 38 | EC-NOISE | low | edge | 11 defensive edge cases (negative leeway, kid header, app_metadata type, etc.) | various | **DISMISS** — false-positive for foundation story |

## PATCH Group — required for `done` (16 items)

Group A — CI pipeline fixups (7 items, all in `.github/workflows/ci.yml` and `apps/api/alembic/env.py`):
- RLS-CI-1, RLS-CI-2, RLS-CI-3, RLS-CI-4, RLS-CI-5, RLS-CI-6, RLS-CI-7

Group B — runtime correctness (5 items, breaking the current end-to-end flow):
- TENANT-1, TENANT-2, AUTH-1, AUDIT-2, LINT-1

Group C — AC fidelity (3 items, spec deviations):
- AC3-1, AC5-1, AC5-2

Group D — quick wins (1 item):
- PYD-1, RLS-2, RLS-3, CI-3, DOC-1, PIN-1, JWT-2

## DECISION_NEEDED (6 items — surface to user)

| # | Question | Options |
|---|---|---|
| AUDIT-1 | Should `with_service_role` commit the audit row in a **separate transaction** before yielding to the action, or share the action's transaction (current behavior, `flush` only)? | (a) Separate tx (tamper-evidence: audit survives action rollback) · (b) Same tx (atomicity) |
| AC3-2 | Add the spec Korean error "다른 테넌트 데이터에 접근할 수 없습니다" as a new `AuthError` variant raised **specifically on RLS policy violation**? | (a) Add it as TENANT_FORBIDDEN with this exact message + keep 7 existing JWT failure messages · (b) Rewrite spec to use a generic message |
| RLS-5 | Keep the 4-policy split per table (current, more granular) or collapse into one `FOR ALL` policy per table (matches spec wording)? | (a) Keep 4-policy split (UPDATE needs different USING vs WITH CHECK) · (b) Collapse to `FOR ALL` with `USING` + `WITH CHECK` clauses |
| AC4-1 | Keep the kw-only async context manager signature (current) or revert to the spec's positional `(reason, actor_id, action)` wrapper? | (a) Keep current (more Pythonic) · (b) Add a thin positional wrapper for spec compatibility |

(4 decisions; RLS-2 and RLS-3 are already routed to PATCH; CI-2 + AC5-3 = DEFER)

## DEFER (13 items — create follow-up stories or note in retrospective)

- **Story 0.4** (cross-language conventions): SCHEMA-1, SCHEMA-2, SCHEMA-4, PIN-1
- **Story 0.4 or later**: RLS-1, RLS-4, RLS-6, SCHEMA-3
- **Story 5.2** (full audit enforcement): AUDIT-1 (if user picks same-tx), AC5-3, CI-2
- **Security review (deferred epic)**: JWT-1

## DISMISS (3 items)

- **EC-NOISE** (11 defensive edge cases — false-positive for foundation story)
- 2 others absorbed into DEFER or already covered
