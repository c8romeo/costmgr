test(async): 27 파일 / 184 함수 async def → asyncio.run 규약 변환 (cj-style baseline-green P1)

문제:
pyproject.toml:156 / tests/rls/conftest.py:18-19 가 명시한 프로젝트 규약은
sync def + asyncio.run(coro) 지만, 27 파일 / 184 함수가 async def +
@pytest.mark.asyncio 로 작성되어 pytest-asyncio 플러그인을 전제했다.
해당 플러그인은 AD-14 stack pin 정책상 설치되어 있지 않으므로
작성 이후 한 번도 실행된 적이 없었고 모두
"async def functions are not natively supported" 로 실패했다.

증거:
- `git log -S "tests/api" -- ci.yml` 빈 결과 — 이 스위트는 CI 에
  한 번도 연결된 적이 없었다
- 직전 sprint 의 "13/13 green" 은 전체의 약 13% 만 게이트했다는 뜻
- 신규 test-suite-measure job (cj-style 직전 sprint) 에서 측정한
  로컬 기준선: 4,985 passed / 173 failed / 28 errors / 96.6%
  그 중 104/173 (60%) 가 본 결함에서 기인

변환 전략 (본문 무손실):
@pytest.mark.asyncio          →  (삭제)
async def test_x(...) -> None:    →  def test_x(...) -> None:
    <docstring>                         <docstring 그대로 — pytest 리포트 보존>
    <본문>                              async def _inner() -> None:
                                            <본문 그대로, +4 들여쓰기>
                                        asyncio.run(_inner())

await 표현식을 건드리지 않으므로 다중 라인 호출의 괄호 수술이
불필요하다. 파라미터(픽스처)는 바깥 sync 함수에 남고 클로저로
참조된다. asyncio import 가 없으면 from __future__ import
annotations 직후에 삽입한다.

도구:
scripts/convert_async_tests.py 신규 (108 LOC) — 정합성 + 멱등성
확보, --check 옵션 지원. 본문 수술 없이 AST/들여쓰기 기반.

선택지 비교 (대안 3개 중 (b) 채택):
(a) pytest-asyncio 플러그인 도입 → AD-14 35-pin 정책 + [STACK BUMP]
    태그 회피 위해 신규 의존성은 부담
(b) 프로젝트 자체 규약으로 변환 (채택) → 의존성 0, 핀 정책 0,
    [STACK BUMP] 태그 불필요, lint-conventions 영향 0
    (ruff scope = apps/api packages, tests/ 미포함)
(c) 테스트 삭제 → 신호 복구 목적과 정면 충돌

검증 (정직 보고):
- 변환 적용 후 전체 스위트 재실측: 5,089 passed / 69 failed /
  28 errors, 3분 3초
- 전후 비교: +104 passed, -104 failed (10⁴ 정확히 일치)
- 잔여 69 failed 분류:
    MVP-scope 의심 ~10 (test_1st_release_smoke_test 7 +
    test_pdf_generator 1 + test_m12_account_backup_parity 2)
    비-MVP ~59 (Phase 5·8·9·10·26 capability matrix /
    governance / SLO / SDR drift — PRD §15 1차 로드맵 밖)
- 잔여 28 errors = root .venv 아티팩트 (uv run 으로는 API import
  정상). 본 변환과 무관.

영향:
- test-suite-measure job (직전 sprint 신규) 가 이제 진짜 신호를
  보여준다. MVP-scope 가 0 으로 떨어지는 시점에 continue-on-error
  를 제거해 차단 게이트로 승격 — PRD OQ-3 파일럿 게이트 판정 지점.
- 변경: 27 파일 (26 tests + 1 scripts 신규), +3127 / -3220
- runtime 동작 변화: docs-only 수준 — product code 변경 0,
  dev_seed 변경 0, ci.yml 변경 0, AD-14 pin 변경 0

Co-Authored-By: Claude Code <noreply@anthropic.com>
