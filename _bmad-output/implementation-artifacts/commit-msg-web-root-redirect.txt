fix(web): root / 가 Story 0.1 stub 그대로 — polished landing 으로 redirect (cj-style root-route fix)

문제:
apps/web/app/page.tsx 가 Story 0.1 단계의 아키텍처 요약 stub
(bizup/costmgr / 원가 관리 SaaS — 모놀리스 + 헥사고날 코어 골격 /
AD-1 / AD-5 / AD-11) 그대로. cj-style 64번째 (commit be0cf97, 1st
release launch wire) 에서 app/[locale]/(public)/landing/page.tsx
에 폴리시드 landing (LandingHero + LandingFeatures + LandingPricing
+ LandingCTA + ko-KR SSOT) 을 작성했지만 root / 의 page.tsx 는
그대로 남아 있어 사용자가 http://localhost:3000/ 으로 접속 시
스토리 0.1 placeholder 만 보임.

영향:
- 282 cj-style sprint 누적 + 1st release launch 결정 wire 후에도
  초기 진입 화면이 텍스트 placeholder 라는 사용자-면 user-facing
  회계 결함
- AD-29 의 "심플한 가격" / "월 1만원" / "14일 무료 체험" / 마케팅
  랜딩 hero/features/pricing/CTA 가 닿지 않음
- vercel.json 의 /landing public route EXTENSION 이 무력화 (직접
  /ko/landing 입력해야 보임)

근본 원인:
next-intl 의 localePrefix: "as-needed" + app/[locale]/* 라우팅
패턴에서 app/page.tsx (locale segment 밖) 는 middleware 의 intl
처리 영향권 밖. next-intl 문서상 bare root 가 필요한 경우 별도
redirect 명시가 필요.

픽스:
app/page.tsx 를 server component 로 교체하여 redirect('/ko-KR/landing')
호출. Next.js redirect() throws NEXT_REDIRECT 으로 클라이언트가
/kr-KR/landing 으로 이동.

검증:
- curl -sIL http://localhost:3000/ → 307 Temporary Redirect
- curl -sL http://localhost:3000/ → 200 OK + LandingHero +
  LandingFeature + 월 1만원 + 14일 무료 + 원가 관리 모두 노출
- AD-29 verbatim pricing/cooy SSOT 가 닿음 확인

runtime 동작 변화:
- 단일 파일 15 줄 → 16 줄 교체
- product code 변경 0 / dev_seed 변경 0 / ci.yml 변경 0
- AD-14 pin 변경 0 / [STACK BUMP] 태그 불필요
- 스크린샷 (스크린샷 2026-09-06 002621.png) 의 흰바탕+텍스트만
  화면 → 마케팅 hero + 6 feature cards + pricing + CTA 로 변경

스크립트 노트:
fix 적용 시 기존 dev server (PID 25252, 직전 세션) 가 핫리로드 안
해서 stale Story 0.1 응답 유지. 사용자가 옵션 (a) 결정 — taskkill
+ .next 캐시 삭제 + 새 dev server 재시작 (포트 3000).

Co-Authored-By: Claude Code <noreply@anthropic.com>
