# Code Review Triage — Story 0.3

**Story**: 0-3-stack-pin-lockfile-build-pipeline
**Review date**: 2026-07-25
**Reviewers**: blind (24) + edge (47) + auditor (13) = **84 raw findings**
**After dedup**: **~50 unique findings**
**After route**: **26 PATCH · 4 DECISION · 7 DEFER · 4 DISMISS**

## Verdict: ❌ FAIL

5 critical-class blockers prevent Story 0.3 from reaching `done`:

1. **Dockerfile uses tag-only pins, no `@sha256:...` digest** — spec AC #1 violated
2. **`pnpm-lock.yaml` is untracked** — CI first run breaks (`pnpm install --frozen-lockfile` fails)
3. **`pydantic` pinned as range `>=2.10,<2.12`** — AD-14 exact-pin violation
4. **`bump_stack_pin.sh` does NOT update manifests** — script provides false sense of bump
5. **`[STACK BUMP]` bypass broken on squash-merge + merge-commit workflows** — the entire authorization mechanism is unreliable

Plus 9 spec deviations from `ARCHITECTURE-SPINE.md §Stack` are baked into `STACK_PIN.yaml` as silent "HANDOFF L3" notes — CI reports green while spec is unmet.

## Unified Triage Table (sorted by severity, then route)

| # | ID | Severity | Source | Title | Location | Route |
|---|---|---|---|---|---|---|
| 1 | DOCKER-1 | critical | blind+edge+auditor | Dockerfile uses tag-only pins, no `@sha256:...` digest | `Dockerfile:22,41,70,97` | **PATCH** |
| 2 | LOCK-1 | critical | blind+edge+auditor | `pnpm-lock.yaml` untracked → CI first run fails | `git status: ?? pnpm-lock.yaml` | **PATCH** |
| 3 | RANGE-1 | critical | blind+edge | `pydantic: ">=2.10,<2.12"` (range) violates AD-14 exact-pin | `apps/api/pyproject.toml:19`, `docs/STACK_PIN.yaml:40` | **DECISION** |
| 4 | BUMP-1 | critical | blind+edge | `bump_stack_pin.sh` claims to update manifests but only updates YAML | `scripts/bump_stack_pin.sh:14-21,107-138` | **PATCH** |
| 5 | CI-1 | high | blind+edge | `[STACK BUMP]` check reads `git log -1 --format=%s` (subject only); merge commits hide bump tag | `scripts/check_stack_pin.{mjs:117-124,py:140-149}`, `.github/workflows/ci.yml:430-434` | **PATCH** |
| 6 | SCHEMA-1 | high | blind+edge+auditor | 9 HANDOFF L3 spec deviations baked into `STACK_PIN.yaml` (silent) | `docs/STACK_PIN.yaml:72-108` | **DECISION** |
| 7 | CHECK-1 | high | blind+edge+auditor | `check_stack_pin` does NOT read `pnpm-lock.yaml` or `uv.lock` | `scripts/check_stack_pin.{mjs,py}` (no coverage) | **PATCH** |
| 8 | DOCKER-2 | high | blind+edge | CI service `postgres:15` not checked against `STACK_PIN.postgresql` | `.github/workflows/ci.yml:553` | **PATCH** |
| 9 | NOTES-1 | high | edge | `notes.*.spec` drift between `stack_pin` and architecture spine not detected | `scripts/check_stack_pin.{mjs,py}` | **PATCH** |
| 10 | BUMP-2 | high | edge | `bump_stack_pin.sh` heredoc depends on `python3` binary (macOS: `python`) | `scripts/bump_stack_pin.sh:119` | **PATCH** |
| 11 | BUMP-3 | high | edge | `bump_stack_pin.sh` mishandles `current: "NOT INSTALLED"` (notes block matches first) | `scripts/bump_stack_pin.sh:51` | **PATCH** |
| 12 | REGEN-1 | high | edge | `regenerate_stack_pin.py` requires PyYAML but pyproject.toml doesn't declare it | `scripts/regenerate_stack_pin.py:2080-2086`, `pyproject.toml:1223-1228` | **PATCH** |
| 13 | HATCH-1 | high | edge | `hatchling: "latest"` non-reproducible build backend | `docs/STACK_PIN.yaml:55` | **PATCH** |
| 14 | UV-1 | high | blind | `uv.lock` has `import-linter: specifier = ">=2.0"` but pyproject.toml requires `==2.13` | `uv.lock:2604-2609`, `pyproject.toml:1225` | **PATCH** |
| 15 | HEALTH-1 | high | edge | nginx alpine `wget` not in default image → container restart loop | `Dockerfile:128` | **PATCH** |
| 16 | LABEL-1 | high | auditor | `stack-pin-violation` auto-label never applied (spec AC #3 violated) | `.github/dependabot.yml`, `.github/workflows/ci.yml` | **PATCH** |
| 17 | TYPECHECK-1 | high | blind | Check scripts don't validate `@types/*` deps in `apps/web/package.json` | `scripts/check_stack_pin.{mjs,py}` | **PATCH** |
| 18 | CI-2 | high | edge | Floating action versions (`@v4`, `@v5`, `@v7`) — supply-chain risk | `.github/workflows/ci.yml` (15 occurrences) | **PATCH** |
| 19 | TEST-1 | high | edge | No tests for empty/missing/BOM `STACK_PIN.yaml` | `tests/integration/test_stack_pin_check.py` | **PATCH** |
| 20 | CASCADE-1 | high | edge | YAML parser bugs (BOM, anchors, escaped quotes, folded scalars) | `scripts/check_stack_pin.{mjs:38-93,py:38-93}` | **PATCH** |
| 21 | BUMP-4 | high | blind | `bump_stack_pin.sh` tells user to run `pnpm install --frozen-lockfile=false` (nonsensical flag) | `scripts/bump_stack_pin.sh:145` | **PATCH** |
| 22 | CASCADE-2 | high | edge | `regenerate_stack_pin.py` overwrites rich `notes` structure (preserves only `spec`) | `scripts/regenerate_stack_pin.py:2106-2117` | **PATCH** |
| 23 | DOCKER-3 | high | edge | Multi-arch manifest digest pinning (procedure docs but no example) | `docs/STACK_PIN.md`, `Dockerfile:5-16` | **PATCH** |
| 24 | DEPEND-1 | high | edge | No `github-actions` ecosystem in `dependabot.yml` (CI drift) | `.github/dependabot.yml` | **PATCH** |
| 25 | DEPEND-2 | medium | blind+edge | No `open-pull-requests-limit` in dependabot config | `.github/dependabot.yml` | **PATCH** |
| 26 | MSG-1 | medium | auditor | Violation message format diverges from spec (`STACK_PIN_VIOLATION: <pkg> drifted from <expected> to <actual>`) | `scripts/check_stack_pin.{mjs:299-303,py:288-292}` | **PATCH** |
| 27 | FASTAPI-1 | medium | auditor | FastAPI in dev extras, not production deps | `apps/api/pyproject.toml:23-32` | **PATCH** |
| 28 | CI-3 | medium | edge | `concurrency: cancel-in-progress` cancels running stack-pin-check mid-test | `.github/workflows/ci.yml:11-13` | **PATCH** |
| 29 | BUMP-5 | medium | blind | `bump_stack_pin.sh` regex brittle on keys with substring matches (`pytest` vs `pytest-asyncio`) | `scripts/bump_stack_pin.sh:51` | **PATCH** |
| 30 | REGEN-2 | medium | edge | `regenerate_stack_pin.py` hardcodes `spine_path` default | `scripts/regenerate_stack_pin.py:2128-2131` | **PATCH** |
| 31 | MSG-2 | medium | blind+edge | `[STACK BUMP]` check is case-sensitive; merge commit subject used by squash-merge | `scripts/check_stack_pin.{mjs:117-124,py:140-149}` | **PATCH** |
| 32 | BUMP-6 | low | blind | `bump_stack_pin.sh` idempotent re-run returns 0 | `scripts/bump_stack_pin.sh:59-62` | **PATCH** |
| 33 | DOCKER-4 | low | edge | Backend `HEALTHCHECK` uses `urllib.request.urlopen` (works but no retries) | `Dockerfile:92` | **PATCH** |
| 34 | TEST-2 | low | edge | Async tests don't exclude `__pycache__` from copytree (slow) | `tests/integration/test_stack_pin_check.py` | **PATCH** |
| 35 | HATCH-2 | — | — | HATCH-1 already covers | — | **DISMISS** |
| 36 | DOCKER-5 | decision | blind+edge | digest pinning: pin now OR keep tag-only with documented variance? | `Dockerfile:22,41,70,97` | **DECISION** |
| 37 | RANGE-1-D | decision | blind+edge | pydantic: stay in range (PYD-1) OR pin to a specific 2.11.x? | `apps/api/pyproject.toml:19` | **DECISION** |
| 38 | SCHEMA-1-D | decision | blind+edge+auditor | L3 deviations: formalize as tracked exceptions per item OR commit to spec values? | `docs/STACK_PIN.yaml:72-108` | **DECISION** |
| 39 | DOCKER-6 | decision | blind | Either lock at Node 24.18.0 OR allow semver range `>=24.18.0 <25`? | `package.json:8` | **DECISION** |
| 40 | DEPEND-3 | decision | edge | Dependabot PRs: amend commit to add `[STACK BUMP]` automatically OR force manual rebase? | `.github/dependabot.yml`, `docs/DEPENDABOT.md` | **DEFER** |
| 41 | OWNERS-1 | decision | blind+edge | `@platform-team` placeholder → keep + dev-ops to set up real team | `.github/CODEOWNERS:177-178` | **DEFER** |
| 42 | ENGINE-1 | high | blind | `engines.node: "24.18.0"` exact pin blocks local dev (Node 24.15.0) | `package.json:8` | **DEFER** (L3 decision) |
| 43 | SIGN-1 | medium | blind | No signed-commit enforcement for `[STACK BUMP]` (branch protection policy) | `docs/DEPENDABOT.md` | **DEFER** |
| 44 | DEPEND-4 | low | edge | `non-pinned-dependencies` group with `patterns: ["*"]` overlaps with `pinned-dependencies` | `.github/dependabot.yml:263-275` | **DEFER** |
| 45 | TYPES-1 | low | edge | `apps/web/package.json` lacks `engines` field (no drift possible, but contradiction possible) | `apps/web/package.json` | **DEFER** |
| 46 | TGZ-1 | low | edge | `deprecation==2.1.0` (2020) transitive via supabase | `uv.lock:2612-2621` | **DEFER** |
| 47 | STYLE-1 | medium | blind+edge | unicode ❌ in `check_stack_pin.py:1902` (early-error path) | `scripts/check_stack_pin.py:1902` | **PATCH** |
| 48 | TIME-1 | low | edge | Timezone "Asia/Seoul" + 09:00 = 00:00 UTC (correct, no DST) | `.github/dependabot.yml` | **DISMISS** |
| 49 | TEST-3 | low | edge | Test `_run_node`/`_run_py` set `LC_ALL=C.UTF-8` (cosmetic) | `tests/integration/test_stack_pin_check.py` | **DISMISS** |
| 50 | CONC-1 | low | edge | `concurrency: ${{ github.workflow }}-${{ github.ref }}` cancels across PRs (correct behavior) | `.github/workflows/ci.yml:11-13` | **DISMISS** |

## PATCH Group — required for `done` (26 items)

### Group A — Dockerfile + Docker (5)

- **DOCKER-1**: Add `@sha256:...` digest pins to all 4 `FROM` stages + `COPY --from=ghcr.io/astral-sh/uv:0.11.32` (line 45). Procedure documented in `Dockerfile:5-16` but never executed.
- **DOCKER-3**: Document `--platform` for multi-arch reproducibility in `docs/STACK_PIN.md`.
- **DOCKER-4**: Replace `urllib.request.urlopen` with `requests` or add retries (use `python -c "import urllib.request, time; ..."`).
- **DOCKER-5**: Either resolve to "PIN to specific pydantic version in range" (DECISION) or "accept tag-only with documented variance" (PATCH + DECISION).
- **HEALTH-1**: Either `apk add wget` in stage 4 or replace with a curl-based HEALTHCHECK.

### Group B — Lockfile + manifest (4)

- **LOCK-1**: `git add pnpm-lock.yaml` and commit. Required for AC #1 reproducibility.
- **UV-1**: Regenerate `uv.lock` after `import-linter` pin change (`uv lock` then re-commit).
- **HATCH-1**: Pin `hatchling` to a specific version (e.g., `1.27.0`) instead of `"latest"`.
- **FASTAPI-1**: Move `fastapi` from `[project.optional-dependencies].dev` to `[project].dependencies` in `apps/api/pyproject.toml`.

### Group C — Check scripts (5)

- **CHECK-1**: Extend `scripts/check_stack_pin.{mjs,py}` to read `pnpm-lock.yaml` and `uv.lock` and verify resolved versions match declared pins.
- **TYPECHECK-1**: Add `@types/node`, `@types/react`, `@types/react-dom` checks against a `dev_pins` section in `STACK_PIN.yaml`.
- **CASCADE-1**: Replace `parseYamlSimple` with a proper YAML library (PyYAML is already used elsewhere; add to dev deps). Handles BOM, anchors, escaped quotes, folded scalars.
- **STYLE-1**: Replace `❌` in `check_stack_pin.py:1902` with ASCII `[ERROR]`.
- **MSG-1**: Update violation message format to `STACK_PIN_VIOLATION: <pkg> drifted from <expected> to <actual>. Use [STACK BUMP] commit tag to bypass.` (per spec).

### Group D — Bump script (4)

- **BUMP-1**: Make `bump_stack_pin.sh` actually edit the manifest files identified in `PLAN` (not just YAML). Use `sed` for case-by-case substitution matching the planned file. Regenerate lockfiles (optional, requires user confirmation).
- **BUMP-2**: Replace `python3` with `python` (or detect platform).
- **BUMP-3**: Use proper YAML parser (PyYAML) to read CURRENT value, avoiding matches against `notes.*.current`.
- **BUMP-4**: Replace `pnpm install --frozen-lockfile=false` with `pnpm install --no-frozen-lockfile <pkg>` (or document the anti-pattern explicitly).
- **BUMP-5**: Use YAML library to match keys exactly (avoid `pytest` matching `pytest-asyncio`).
- **BUMP-6**: Exit code 1 on no-op (or require `--force`).

### Group E — CI guard (4)

- **CI-1**: Add `--pr-commit` flag or use `pull_request` event's `github.event.pull_request.head.sha` to fetch the PR's actual commit (not merge commit). Alternative: check `git log -1 --format=%s HEAD~1` if merge commit is detected.
- **CI-2**: Pin all `actions/*@v*` to commit SHAs (e.g., `actions/checkout@<sha>`).
- **CI-3**: Exclude `stack-pin-check` from `concurrency: cancel-in-progress` (use `concurrency: group: ${{ github.workflow }}-${{ github.ref }}, cancel-in-progress: ${{ github.event_name != 'pull_request' }}`).
- **LABEL-1**: Add `actions/github-script@v7` step that applies `stack-pin-violation` label when `stack-pin-check` fails.

### Group F — Dependabot (2)

- **DEPEND-1**: Add `github-actions` ecosystem block to monitor action versions.
- **DEPEND-2**: Add `open-pull-requests-limit: 5` to each ecosystem.

### Group G — Tests (1)

- **TEST-1**: Add tests for empty/missing/BOM `STACK_PIN.yaml` (current: 6 tests pass; need 5 more).
- **TEST-2**: Add `__pycache__` to `copytree` ignore list.

### Group H — Regenerate script (2)

- **REGEN-1**: Add `PyYAML` to `pyproject.toml [dependency-groups].dev`.
- **REGEN-2**: Make `spine_path` configurable via CLI arg (currently hardcoded).
- **CASCADE-2**: Preserve all `notes[k]` fields when merging (only update `spec` if absent).

### Group I — Workflow services check (1)

- **DOCKER-2**: Extend `check_stack_pin.{mjs,py}` to scan `.github/workflows/*.yml` for `services:` declarations and compare against `STACK_PIN.postgresql` (and other base images).

### Group J — Notes consistency check (1)

- **NOTES-1**: Add a check that compares `notes[k].spec` against `ARCHITECTURE-SPINE.md §Stack` (or against an embedded copy of the spec). Surface deviation count in pass/fail output.

## DECISION (4 — surface to user)

| # | Question | Options |
|---|---|---|
| **DOCKER-5** | Pin Docker images by `@sha256:...` digest NOW (consistent with spec AC #1)? | (a) Pin now: run `docker pull` + `docker inspect` for each, update Dockerfile. ~30 min. (b) Accept tag-only with documented L3 variance (already noted in `Dockerfile:14-16` comment). |
| **RANGE-1** | Pick a specific pydantic version in the range `>=2.10,<2.12`? | (a) Pin `pydantic==2.11.9` (last in range before 2.12) — addresses AD-14 strict pin. (b) Stay in range; relax check to compare prefix only. (c) Bump to `pydantic==2.13.4` per spec and accept PYD-1 risk (broken pydantic-core wheel). |
| **SCHEMA-1** | 9 spec deviations in `STACK_PIN.yaml` — formalize as tracked exceptions OR commit to spec? | (a) Add `exceptions:` block with hash + owner + deadline per deviation. (b) File separate spikes for each (next 16.2.11, react 19.2.8, etc.). (c) Bring code up to spec one item at a time. |
| **DOCKER-6** | Engines node pin: keep exact `24.18.0` OR allow semver range? | (a) Keep exact (current; document L3 friction in HANDOFF). (b) Use `>=24.18.0 <25` (matches CI behavior; `.nvmrc` still enforces exact). |

## DEFER (7 — handled elsewhere or L3 decisions)

- **OWNERS-1** — `@platform-team` placeholder: depends on org setup (acceptable until GitHub team is provisioned).
- **ENGINE-1** — `engines.node: "24.18.0"` exact pin blocks local dev: known L3 decision (HANDOFF L3).
- **SIGN-1** — No signed-commit enforcement for `[STACK BUMP]`: branch protection policy (not code).
- **DEPEND-3** — Dependabot PRs don't auto-add `[STACK BUMP]` tag: documented in `docs/DEPENDABOT.md` (platform-team manual rebase).
- **DEPEND-4** — `non-pinned-dependencies` group overlaps with `pinned-dependencies`: minor Dependabot config cleanup.
- **TYPES-1** — `apps/web/package.json` lacks `engines` field: no drift possible, optional.
- **TGZ-1** — `deprecation==2.1.0` (2020) transitive: stale but not security issue.

## DISMISS (4 — noise / false positive / handled elsewhere)

- **TIME-1** — Timezone "Asia/Seoul" + 09:00 = 00:00 UTC: correct (KST is UTC+9, no DST).
- **TEST-3** — `_run_node`/`_run_py` set `LC_ALL=C.UTF-8`: cosmetic but works.
- **CONC-1** — `concurrency: cancel-in-progress` cancels across PRs: correct behavior for same-branch PRs.
- **HATCH-2** — duplicate of HATCH-1.

## Change Log

- 2026-07-25 — Story 0.3 CR complete. 84 raw findings → 50 unique → 26 PATCH + 4 DECISION + 7 DEFER + 4 DISMISS. Verdict: **FAIL**.
