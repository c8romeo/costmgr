feat(finops): Phase 21~26 Layer 2 P1 + Layer 3 P2 carry-over (cj-style 189번째)

Phase 21~26 FinOps territory 의 router drift pytest backfill + docs backfill.
cj-style 188 Phase 17~20 carry-over `836a8d4` 의 next-옵션 (b) verbatim 진입 —
cj-style 188 A764 에서 honestly DEFER 로 남긴 Phase 21~26 carry-over 항목의 정직 회복.

apps/api source 변경 0건 (test + docs only sprint).

## Layer 2 P1 — pytest router drift backfill (6 NEW, 46 cases)

- tests/api/modules/finops/test_phase_21_reserved_capacity_router.py (8 cases)
- tests/api/modules/finops/test_phase_22_chargeback_settlement_router.py (8 cases)
- tests/api/modules/finops/test_phase_23_unit_economics_router.py (8 cases)
- tests/api/modules/finops/test_phase_24_budget_planning_router.py (8 cases)
- tests/api/modules/finops/test_phase_25_vendor_management_router.py (8 cases)
- tests/api/modules/finops/test_phase_21_26_router_include.py (6 cases)

CR 11-4 P-015 verbatim — NO pytest fixtures, pure sync, constants at module top.
Phase 17~20 router drift 패턴 (test_phase_NN_*_router.py + include smoke) verbatim 미러.

Router prefix / route-set / HTTP method / request-model contract / serializer
threshold invariant 를 각각 pin. Include smoke 는 5 router 의 main.py import +
include_router 호출 + multi_cloud(Phase 20) 이후 배치 + 21→22→23→24→25 의존 순서를 pin.

## Layer 3 P2 — docs backfill (6 NEW)

- docs/finops-reserved-capacity-router.md (Phase 21)
- docs/finops-chargeback-settlement-router.md (Phase 22)
- docs/finops-unit-economics-router.md (Phase 23)
- docs/finops-budget-planning-router.md (Phase 24)
- docs/finops-vendor-management-router.md (Phase 25)
- docs/finops-cost-anomaly-ml-prediction-no-router.md (Phase 26 — no-router 명시)

Phase 19 pricing docs 패턴 미러: §1 Introduction / §2 Capability Gate /
§3 Endpoint Inventory / §4 Request Model Contract / §5 Invariants /
§6 Audit Action Layer / §7 Router Include / §8 Cross-References.

## honest findings 4종 (수정하지 않고 실제 상태를 pin — CR 11-3 정직성 discipline)

1. Phase 26 에는 FastAPI router 가 없다. cost_anomaly_ml_prediction/ 패키지 전체에
   APIRouter 부재 + main.py 미참조. → router 문서 대신 no-router 문서를 작성하고,
   include smoke Test 6 이 그 부재를 pin. 나중에 router 를 붙이면 이 테스트가
   의도적으로 실패하여 명시적 reviewed change 를 강제한다.

2. Phase 25 request models 는 extra="forbid" 를 선언하지 않는다. Phase 21 은 5개
   모델 전부 ConfigDict(extra="forbid") 인데 Phase 25 의 7개 모델은 pydantic 기본
   ignore. Test 6 은 실제 상태(model_config.get("extra") is None)를 assert 하여
   정직성을 유지 — forbid 로 조이는 것은 별도 source sprint.

3. router prefix 가 territory 마다 불일치. Phase 21 /api/v1/admin/finops/... ,
   Phase 22·23 /api/v1/finops/... , Phase 24 /finops/... , Phase 25 /api/finops/... .
   정규화는 별도 routing sprint, 현 상태를 docs §3 에 명시 pin.

4. serializer 에 validate_* 함수가 없어 Phase 20 패턴의 Test 8
   (validate_* raises on missing fields) 을 그대로 미러 불가. 각 territory 의
   threshold/cap 상수 invariant pin Test 8 로 대체.

## pre-existing 발견 (본 sprint 회귀 아님)

sprint-status.yaml 은 HEAD 시점부터 존재하는 mojibake(#x0080, char ~160859)로
yaml.safe_load 전체 파싱이 불가하다. 신규 append 블록 단독 파싱은 OK (7 items).

## 3중 게이트

- ruff scoped: All checks passed! (신규 6 파일 + tests/api/modules/finops/ 전체)
- pytest: 46/46 NEW PASS, dir 전체 82/82 PASS in 1.19s (regression 0건)
- vitest: N/A (backend test + docs only sprint)
- tsc: N/A (backend test + docs only sprint)

## A19 cohesion 9 surface

Surface 2 testing + Surface 8 docs EXTENSION 만. 나머지 7 surface NO 변경.
Capability matrix v1.36 → v1.52 EXTENSION chain PRESERVED (18 steps, orthogonal).

## Scope

16 files = 14 NEW + 2 MODIFIED atomic single sprint.
sprint-status v3.96 → v3.97 EXTENSION (A766~A770) + MEMORY.md hook EXTENSION.

Co-Authored-By: Claude <noreply@anthropic.com>
