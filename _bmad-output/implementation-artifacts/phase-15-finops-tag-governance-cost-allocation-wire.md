---
baseline_commit: 87393b4
status: ready-for-dev
cj_style_entry_point: 122
story_key: phase-15-finops-tag-governance-cost-allocation-wire
---

# Phase 15 FinOps Tag Governance & Cost Allocation wire spec (cj-style 122번째 epic 연속 정직 회복)

## Story

**As a** finance team / FinOps analyst / department cost center owner / cloud architect / compliance officer / tenant admin / enterprise onboarding lead / capacity planner / commitment manager
**I want** FinOps Tag Governance & Cost Allocation territory 결정 wire (tag policy DSL `define_tag_policy` builder + AST 5 levels (tenant_id + resource_type + tag_key + enforcement_level + default_value) + parser 검증 3 layer + 6 resource_type 옵션 ec2/rds/s3/lambda/eks/vpc + 4 enforcement_level 옵션 required/recommended/optional/blocked + tag_key validation (AWS 표준 + reserved prefix 차단) + 4 default_value 옵션 string/env_based/conditional/inherit + TAG_POLICY_DEFAULTS constants + 4 industries baseline industry-agnostic + per-tenant override EXTENSION + TagPolicy TypedDict 11 fields + untagged resource detector `detect_untagged_resources` + 6 resource_types (ec2/rds/s3/lambda/eks/vpc) + Phase 14 wire `e904485` idle_resource_detector EXTENSION + UntaggedResource TypedDict 14 fields + allocation rules engine `evaluate_allocation_rules` + 5 rule_types (tag_match/percentage_split/weighted/conditional/fallback) + Phase 11 wire `e020ad0` chargeback engine EXTENSION + AllocatedCost TypedDict 11 fields + AllocationRule TypedDict 13 fields + allocation audit + compliance `audit_allocation` + ComplianceReport TypedDict 12 fields + compliance trend + compliance alert routing + remediation workflow + chargeback allocation reconciliation `reconcile_chargeback_allocation` + ReconciliationReport TypedDict 13 fields + reconciliation strategy 3종 (chargeback_only/tag_allocation_only/hybrid_blended default) + variance calculation + reconciliation investigation + reconciliation approval workflow + tag governance dashboard UI 5 components + ko-KR.json `finops_tag_governance.*` namespace EXTENSION ~30 keys + Capability matrix v1.40 → v1.41 EXTENSION FINOPS_TAG_GOVERNANCE + audit-first INSERT 10 NEW + ActionClass.FINOPS_TAG_GOVERNANCE)
**so that** Phase 14 wire `e904485` FinOps Optimization & Rightsizing territory (optimization definition DSL + rightsizing engine 5 resource types + idle resource detection z-score 기반 + RI/SP commitment recommender 1y/3y + optimization accuracy tracking precision/recall/realized savings) 의 natural backend COST ALLOCATION LAYER EXTENSION 결정 wire 진입 (tagged resource → accountable cost center: rightsizing 권고 의 resource inventory → tagged resource inventory 기반 cost allocation + idle resource 의 unattached EIP / detached EBS / orphaned RDS snapshot detection → untagged resource detection EXTENSION + commitment_recommender 의 resource_pattern → tag_match 기반 allocation rule + optimization_accuracy 의 resource_type granularity → allocation rule 의 tag dimension EXTENSION + Phase 11 wire `e020ad0` showback department breakdown 의 cost allocation → tag-based allocation EXTENSION + Phase 12 wire `f3c0e63` anomaly detection 의 dimension 옵션 → tag dimension 추가 + Phase 13 wire `8b98030` forecasting 의 target_metric → tag-based forecasting EXTENSION + Phase 14 wire `e904485` optimization 의 baseline_period → tag governance compliance period) = Phase 14 FinOps territory 의 COST ALLOCATION LAYER EXTENSION 결정 (tagged resource → accountable cost center) + Phase 15 PRD entry `87393b4` (cj-style 121번째) DONE 진입 정합 보존 + Epic 12 2FA 챌린지 보존 + AD-22 owner-only RBAC 보존 + D-FINOPS-5 honestly DEFER 보존 진입 결정 wire + Phase 14 close-out retro `5b367d9` §13 verbatim 해소 결정 wire 보존).

## Context

cj-style Phase 15 2번째 진입점 (cj-style 122번째) 진입 결정 wire 진입 완료:
- Phase 15 PRD entry `87393b4` (cj-style 121번째) DONE 진입 정합 보존
- Phase 14 close-out retro `5b367d9` (cj-style 120번째) + Phase 14 atomic wire T1~T8 `e904485` (cj-style 119번째) + Phase 14 spec entry `30637f6` (cj-style 118번째) + Phase 14 PRD entry `0e3f8d9` (cj-style 117번째) + Phase 13 close-out retro `850b4f8` (cj-style 116번째) + Phase 13 atomic wire T1~T8 `8b98030` (cj-style 115번째) + Phase 13 spec entry `77ed55f` (cj-style 114번째) + Phase 13 PRD entry `d31dfc8` (cj-style 113번째) + Phase 12 close-out retro `3354e83` (cj-style 112번째) + Phase 12 atomic wire T1~T8 `f3c0e63` (cj-style 111번째) + Phase 12 spec entry `8c5f374` (cj-style 110번째) + Phase 12 PRD entry `344c7eb` (cj-style 109번째) + Phase 11 close-out retro `80df15b` (cj-style 108번째) + Phase 11 atomic wire T1~T8 `e020ad0` (cj-style 107번째) + Phase 11 spec entry `82c93a8` (cj-style 106번째) + Phase 11 PRD entry `16d7698` (cj-style 105번째) + Phase 10 close-out retro `733d428` (cj-style 104번째) + Phase 10 wire `ac5d6c5` (cj-style 103번째) 결정 wire 모두 DONE 진입 정합 보존
- D-FINOPS-5 honestly DEFER 보존 진입 결정 wire (Phase 15 PRD entry 진입 시점에 carry-over chain 정직 회복 결정 wire + Phase 14 close-out retro `5b367d9` §13 + Phase 13 close-out retro `850b4f8` §13 + Phase 12 close-out retro `3354e83` §13 + Phase 11 close-out retro `80df15b` §12 + Phase 10 close-out retro `733d428` §10 + Phase 9 close-out retro `634427d` §10 + Phase 8 close-out retro `ab495a8` §10 + Phase 7 close-out retro `326fa9f` §10 + Phase 6 close-out retro `f9f006c` §13 + Epic 17 close-out retro `be8f3bd` §11 + 1st release close-out retro §6 "FinOps Tag Governance & Cost Allocation 결정 wire 보류, Phase 15+ 진입 시점" verbatim 해소 + Phase 15 PRD entry 진입 시점에 1 NEW 결정 wire)
- D-FINOPS-4 ✅ RESOLVED 보존 진입 결정 wire
- D-FINOPS-3 ✅ RESOLVED 보존 진입 결정 wire
- D-FINOPS-2 ✅ RESOLVED 보존 진입 결정 wire
- D-FINOPS-1 ✅ RESOLVED 보존 진입 결정 wire
- D-SLO-1 ✅ RESOLVED 보존 진입 결정 wire
- D-CHAOS-1 ✅ RESOLVED 보존 진입 결정 wire
- D-PERFORMANCE-1 ✅ RESOLVED 보존 진입 결정 wire
- D-OBSERVABILITY-1 ✅ RESOLVED 보존 진입 결정 wire
- Phase 15 PRD entry 의 8 ACs §F31.1~§F31.8 verbatim 결정 wire 보존
- Capability matrix v1.40 → v1.41 EXTENSION FINOPS_TAG_GOVERNANCE 1 NEW row 결정 wire (industry-agnostic 4-industry grants ✅/✅/✅/✅ CR 12-1 L4 precedent 미러)
- AD-42 FinOps Tag Governance & Cost Allocation 신규 결정 wire 진입 (a)~(g) 7 sub-decisions

## 8 ACs (PRD §F31.1~§F31.8 verbatim) → 92 detailed sub-ACs

### §F31.1 tag policy DSL (12 sub-ACs)
- F31.1-1 `apps/api/modules/finops/tag_policy_dsl.py` NEW (~+150 LOC + `define_tag_policy(tenant_id, resource_type, tag_key, enforcement_level, default_value)` builder + AST 5 levels (tenant_id + resource_type + tag_key + enforcement_level + default_value) + parser 검증 3 layer (syntax + semantic + tenant-scope RLS) 결정 wire + 4 industries baseline industry-agnostic + per-tenant override EXTENSION)
- F31.1-2 `TagPolicy` TypedDict 11 fields 결정 wire (policy_id UUID PK + tenant_id UUID + resource_type enum ec2/rds/s3/lambda/eks/vpc + tag_key TEXT + enforcement_level enum required/recommended/optional/blocked + default_value TEXT + compliance_threshold_pct NUMERIC(8, 4) + remediation_action enum notify_only/auto_remediate/block_provisioning + status enum active/paused/expired + created_at TIMESTAMPTZ + updated_at TIMESTAMPTZ + trace_id TEXT)
- F31.1-3 6 resource_type 옵션 결정 wire (ec2 AWS EC2 instance + rds AWS RDS/Aurora instance + s3 AWS S3 bucket + lambda AWS Lambda function + eks AWS EKS cluster/node group + vpc AWS VPC endpoint/NAT gateway/security group + resource_type validation + tenant_settings.resource_inventory JSONB 기반)
- F31.1-4 4 enforcement_level 옵션 결정 wire (required 태그 부재 시 정책 위반 + alert + remediation trigger / recommended 태그 부재 시 경고만 no enforcement / optional 태그 부재 시 silent audit log only / blocked 태그 부재 시 provisioning 차단 CI/CD gate + enforcement_level default = recommended + per-tenant override JSONB + per-resource-type override JSONB)
- F31.1-5 tag_key validation 결정 wire (AWS 표준 태그 키 = 소문자 + 숫자 + `_` + `-` + `.` + `:` + max 128 chars + value validation max 256 chars + UTF-8 + reserved prefix `aws:` 차단 + custom prefix `tenant:` 또는 `cost:` namespace 권장)
- F31.1-6 4 default_value 옵션 결정 wire (string literal default value e.g. "untagged" + "shared" + "production" / env_based environment 변수 기반 / conditional Lambda function 평가 결과 / inherit resource 의 parent 태그 inherit + default_value default = "untagged")
- F31.1-7 `TAG_POLICY_DEFAULTS` constants 결정 wire (`TAG_POLICY_DEFAULTS = {'enforcement_level': 'recommended', 'default_value': 'untagged', 'compliance_threshold_pct': 95.0, 'untagged_alert_channel': 'slack', 'monthly_audit_cron': '0 4 1 * *', 'remediation_action': 'notify_only'}` constants + `TAG_RESOURCE_TYPES = ['ec2', 'rds', 's3', 'lambda', 'eks', 'vpc']` constants + `TAG_ENFORCEMENT_LEVELS = ['required', 'recommended', 'optional', 'blocked']` constants 결정 wire)
- F31.1-8 4 industries baseline industry-agnostic 결정 wire (manufacturing + service + manufacturing_service + manufacturing_service_other 모두 tag policy 가능 + per-tenant override EXTENSION + Phase 14 wire `e904485` FINOPS_OPTIMIZATION 4-industry grants ✅/✅/✅/✅ pattern verbatim 미러 + Phase 13 wire `8b98030` FINOPS_FORECASTING_CAPACITY_PLANNING 4-industry grants ✅/✅/✅/✅ pattern verbatim 미러 + Phase 12 wire `f3c0e63` FINOPS_ANOMALY_DETECTION + FINOPS_BUDGET_ALERT 4-industry grants ✅/✅/✅/✅ pattern verbatim 미러 + policy evaluation precedence tenant override > industry baseline > system default)
- F31.1-9 tag policy pure validator CR 11-4 P-015 verbatim 결정 wire (`parse_tag_policy(policy_text)` parser + `validate_tag_policy(tenant_id, resource_type, tag_key)` validator + 5 layer defense (syntax + semantic + tenant-scope RLS + resource_type validation + tag_key validation) + `TagPolicyInvalidError(400)` + `TagPolicyScopeInvalidError(404)` + `TagKeyValidationError(422)` CR 12-5 D-14 envelope)
- F31.1-10 audit-first INSERT `tag_policy_updated` 결정 wire (CR 1-1 verbatim 적용 + ActionClass.FINOPS_TAG_GOVERNANCE 신규 정의 + emit_audit_typed BEFORE tag policy update + per-tenant RLS 자동 적용 + multi-tenant isolation test)
- F31.1-11 typed exception envelope CR 12-5 D-14 verbatim 결정 wire (4 NEW typed exception classes: `TagPolicyInvalidError(400)` + `TagPolicyScopeInvalidError(404)` + `TagKeyValidationError(422)` + `TagEnforcementViolationError(403)`)
- F31.1-12 dry-run mode `--finops-tag-policy-dry-run` CLI flag 결정 wire (dry-run 시 actual tag policy update skip + tag policy preview phase_15_finops_tag_policy_preview table alembic 0047 신규 + audit-first INSERT `tag_governance_dry_run_executed` CR 1-1 verbatim)

### §F31.2 untagged resource detector (12 sub-ACs)
- F31.2-1 `apps/api/modules/finops/untagged_resource_detector.py` NEW (~+200 LOC + `detect_untagged_resources(tenant_id, resource_type, enforcement_level) -> List[UntaggedResource]` + Phase 14 wire `e904485` 의 idle_resource_detector EXTENSION + 6 resource_types 결정 wire)
- F31.2-2 EC2 untagged detection 결정 wire (instance 의 tag inventory 조회 = AWS Resource Groups Tagging API + boto3 `describe_tags()` + required tag keys 부재 시 untagged classify + Phase 14 wire `e904485` 의 idle_resource_detector 의 compute idle (CPU < 5% for 30d) EXTENSION 정합 + instance_age_days + region + instance_type metadata)
- F31.2-3 RDS untagged detection 결정 wire (db instance 의 tag inventory 조회 + required tag keys 부재 시 untagged classify + Phase 14 wire `e904485` 의 idle_resource_detector 의 database idle (connection_count_p95 = 0 for 30d) EXTENSION 정합 + db_engine + db_class + storage_gb + multi_az metadata)
- F31.2-4 S3 untagged detection 결정 wire (bucket 의 tag inventory 조회 + required tag keys 부재 시 untagged classify + Phase 14 wire `e904485` 의 idle_resource_detector 의 storage idle (S3 last_accessed > 30d) EXTENSION 정합 + bucket_size_bytes + object_count + versioning_enabled metadata)
- F31.2-5 Lambda untagged detection 결정 wire (function 의 tag inventory 조회 + required tag keys 부재 시 untagged classify + function_runtime + memory_mb + timeout_seconds + last_modified_date + invocations_30d_count metadata + 7d/30d invocations < 1 → untagged_lambda_warn severity)
- F31.2-6 EKS untagged detection 결정 wire (cluster + node group 의 tag inventory 조회 + required tag keys 부재 시 untagged classify + Phase 14 wire `e904485` 의 idle_resource_detector 의 container idle EXTENSION 정합 + k8s_version + node_count + pod_count metadata)
- F31.2-7 VPC untagged detection 결정 wire (VPC endpoint + NAT gateway + security group + internet gateway 의 tag inventory 조회 + required tag keys 부재 시 untagged classify + Phase 14 wire `e904485` 의 idle_resource_detector 의 network idle EXTENSION 정합 + vpc_id + cidr_block + attached_resources_count metadata)
- F31.2-8 `UntaggedResource` TypedDict 14 fields 결정 wire (untagged_resource_id UUID PK + tenant_id UUID + resource_id TEXT + resource_type enum ec2/rds/s3/lambda/eks/vpc + required_tags_missing List[TEXT] + optional_tags_missing List[TEXT] + enforcement_level enum required/recommended/optional/blocked + current_tags JSONB + detection_method enum tag_api_scan/scheduled_cron/on_demand + detected_at TIMESTAMPTZ + remediation_status enum pending/notified/remediated/expired + remediation_action enum notify_only/auto_remediate/block_provisioning + policy_id UUID FK + trace_id TEXT 결정 wire)
- F31.2-9 remediation_status 결정 wire (`pending` default → `notified` owner에게 알림 발송 후 → `remediated` owner가 태그 추가 후 → `expired` 30일 후에도 미조치 시 자동 escalate + per-tenant override remediation_timeout_days default 30 + Epic 12 2FA 챌린지 보존 auto_remediate 의 tag add/remove 시 결정 wire)
- F31.2-10 severity classification 결정 wire (`required` enforcement level 의 untagged → high severity (즉시 알림 + Slack + PagerDuty) / `recommended` → medium severity (이메일 + Slack) / `optional` → low severity (audit log only) / `blocked` → critical severity (CI/CD gate block + 즉시 알림) + per-tenant override severity_threshold JSONB)
- F31.2-11 audit-first INSERT `untagged_resource_detected` 결정 wire (CR 1-1 verbatim 적용 + ActionClass.FINOPS_TAG_GOVERNANCE + emit_audit_typed BEFORE untagged resource detection + per-tenant RLS 자동 적용 + multi-tenant isolation test)
- F31.2-12 typed exception envelope CR 12-5 D-14 verbatim 결정 wire (4 NEW typed exception classes: `UntaggedResourceDetectionError(500)` + `TagInventoryUnavailableError(404)` + `RemediationActionError(500)` + `EnforcementLevelViolationError(403)`)

### §F31.3 allocation rules engine (12 sub-ACs)
- F31.3-1 `apps/api/modules/finops/allocation_rules_engine.py` NEW (~+220 LOC + `evaluate_allocation_rules(tenant_id, period_key, resource_id, cost_amount) -> AllocatedCost` + 5 rule_types (tag_match/percentage_split/weighted/conditional/fallback) + Phase 11 wire `e020ad0` 의 chargeback cost allocation engine EXTENSION 결정 wire)
- F31.3-2 tag_match allocation rule 결정 wire (resource 의 tag value → department_id 직접 매칭 + 1 resource = 1 department 매핑 + Phase 11 wire `e020ad0` 의 department cost center mapping EXTENSION 정합 + 매칭 실패 시 fallback rule evaluate)
- F31.3-3 percentage_split allocation rule 결정 wire (resource 의 tag value → N department list + percentage weights 매핑 + percentage 합 = 100% 검증 + UNIQUE constraint `(tenant_id, cost_center_id, period_key)`)
- F31.3-4 weighted allocation rule 결정 wire (resource 의 tag value list → weight 계산 (e.g. user list → monthly active usage 기반 분배) + Phase 7 wire `59b56cd` 의 `business_calculations_total` EXTENSION + weight = monthly_active_user_count + 0 weight → fallback rule evaluate)
- F31.3-5 conditional allocation rule 결정 wire (Lambda function 평가 결과 → department_id 매핑 + condition evaluation AST parser CR 11-4 P-015 verbatim + condition 평가 실패 시 fallback rule evaluate + per-tenant override JSONB)
- F31.3-6 fallback allocation rule 결정 wire (4 rule_types 모두 매칭 실패 시 catch-all default department (default = "shared" 또는 "untagged") + cost 100% allocated to fallback department + audit-first INSERT `allocation_fallback_applied` CR 1-1 verbatim + per-tenant override fallback_department_id default "shared")
- F31.3-7 rule precedence 결정 wire (policy evaluation precedence `tag_match > percentage_split > weighted > conditional > fallback` (specificity 순서) + per-rule override priority INT default = 0 higher = 먼저 evaluate + multiple rules 매칭 시 priority + specificity tiebreaker)
- F31.3-8 rule_evaluation_metadata 결정 wire (`applied_rule_type` + `applied_rule_id` + `match_score` (0.0 ~ 1.0) + `fallback_chain` List[rule_type] + `evaluation_duration_ms` + `condition_evaluation_log` JSONB + audit log 에 metadata 저장)
- F31.3-9 `AllocationRule` TypedDict 13 fields 결정 wire (rule_id UUID PK + tenant_id UUID + rule_type enum tag_match/percentage_split/weighted/conditional/fallback + priority INT default 0 + tag_key TEXT + tag_value_pattern TEXT regex 또는 literal + target_department_id TEXT + percentage_weights JSONB nullable percentage_split 전용 + condition_ast JSONB nullable conditional 전용 + fallback_department_id TEXT nullable fallback 전용 + enabled BOOLEAN DEFAULT TRUE + created_at + trace_id TEXT)
- F31.3-10 `AllocatedCost` TypedDict 11 fields 결정 wire (allocation_id UUID PK + tenant_id UUID + period_key TEXT e.g. "2026-08" + resource_id TEXT + resource_type enum + cost_amount_krw NUMERIC(20, 2) + target_department_id TEXT + applied_rule_type enum + applied_rule_id UUID FK + match_score NUMERIC(8, 4) + allocated_at TIMESTAMPTZ + trace_id TEXT)
- F31.3-11 audit-first INSERT `allocation_rule_evaluated` + `allocation_rule_updated` 2 NEW 결정 wire (CR 1-1 verbatim 적용 + ActionClass.FINOPS_TAG_GOVERNANCE + emit_audit_typed BEFORE/AFTER allocation rule event + per-tenant RLS 자동 적용)
- F31.3-12 typed exception envelope CR 12-5 D-14 verbatim 결정 wire (4 NEW typed exception classes: `AllocationRuleInvalidError(400)` + `AllocationRuleEvaluationError(500)` + `PercentageSumValidationError(422)` + `ConditionalRuleParseError(400)`)

### §F31.4 allocation audit + compliance (12 sub-ACs)
- F31.4-1 `apps/api/modules/finops/allocation_audit.py` NEW (~+150 LOC + `audit_allocation(tenant_id, period_key) -> AllocationAuditReport` + Phase 14 wire `e904485` 의 optimization_accuracy_tracker EXTENSION + per-(tenant_id + resource_type + rule_type) granularity + allocation accuracy tracking)
- F31.4-2 compliance score 계산 결정 wire (`compliance_pct = (tagged_resources / total_resources) × 100` per resource_type + total compliance = `sum(compliance_pct × resource_count) / sum(resource_count)` 가중 평균 + per-tenant override compliance_threshold_pct default 95%)
- F31.4-3 compliance threshold 결정 wire (compliance_pct ≥ 95% → compliant / 80~95% → partially_compliant / < 80% → non_compliant + per-tenant override threshold JSONB + per-industry override compliance_threshold JSONB)
- F31.4-4 allocation accuracy 결정 wire (allocation output 검증 = allocated cost == source cost + audit log 매칭 + accuracy_pct = `sum(matched_allocations) / sum(total_allocations) × 100` + accuracy ≥ 99% → high / 95~99% → moderate / < 95% → low + per-tenant override accuracy_threshold default 99%)
- F31.4-5 allocation coverage 결정 wire (coverage_pct = `sum(allocated_resources) / sum(total_resources) × 100` + coverage ≥ 95% → high / 80~95% → moderate / < 80% → low + 미할당 cost → fallback department 로 자동 할당)
- F31.4-6 compliance report generation 결정 wire (`generate_compliance_report(tenant_id, period_key) -> ComplianceReport` TypedDict 12 fields (report_id UUID PK + tenant_id UUID + period_key TEXT + total_resources int + tagged_resources int + untagged_resources int + compliance_pct NUMERIC(8, 4) + allocation_accuracy_pct NUMERIC(8, 4) + allocation_coverage_pct NUMERIC(8, 4) + non_compliant_resources JSONB + generated_at TIMESTAMPTZ + trace_id TEXT) + monthly cadence default)
- F31.4-7 compliance report delivery 결정 wire (`apps/api/jobs/compliance_report.py` NEW ~+120 LOC + cron KST 1st day of month 04:00 UTC 19:00 + report format 3종 PDF + CSV + JSON + owner-only access + Epic 12 2FA 챌린지 보존 + retention 365일)
- F31.4-8 compliance trend analysis 결정 wire (past 12-month compliance_pct 추이 분석 + trend = `compliance_pct_now - compliance_pct_12mo_ago` + trend > +5% → improving / -5~+5% → stable / < -5% → degrading + Slack 알림 degrading 의 경우만 + Phase 11 wire `e020ad0` 의 showback period selector 의 last 12 months EXTENSION 정합)
- F31.4-9 compliance alert routing 결정 wire (non_compliant_resources > 10% → Slack `#bizup-compliance-alerts` channel + PagerDuty integration `pd_compliance_critical` service critical severity 시 + Email owner-only 알림 + audit-first INSERT `compliance_alert_sent` CR 1-1 verbatim + Phase 12 wire `f3c0e63` 의 alert routing (Slack + PagerDuty + Email) EXTENSION 정합)
- F31.4-10 compliance remediation workflow 결정 wire (non_compliant_resources 발견 시 자동 remediation workflow 5 steps (1. owner notifier 발송 + 2. tag_value 자동 추천 Phase 14 wire `e904485` 의 resource_pattern 기반 + 3. owner의 7일 SLA 내 tag 추가 + 4. 만료 시 escalate to admin + 5. audit log 기록) + per-tenant override remediation_workflow_enabled default true)
- F31.4-11 audit-first INSERT `compliance_report_generated` + `compliance_alert_sent` + `compliance_remediation_initiated` 3 NEW 결정 wire (CR 1-1 verbatim 적용 + ActionClass.FINOPS_TAG_GOVERNANCE + emit_audit_typed BEFORE/AFTER compliance event + per-tenant RLS 자동 적용)
- F31.4-12 typed exception envelope CR 12-5 D-14 verbatim 결정 wire (3 NEW typed exception classes: `ComplianceReportGenerationError(500)` + `ComplianceScoreCalculationError(500)` + `ComplianceAlertRoutingError(500)`)

### §F31.5 chargeback allocation reconciliation (12 sub-ACs)
- F31.5-1 `apps/api/modules/finops/chargeback_allocation_reconciliation.py` NEW (~+180 LOC + `reconcile_chargeback_allocation(tenant_id, period_key) -> ReconciliationReport` + Phase 11 wire `e020ad0` 의 chargeback engine EXTENSION + Phase 14 wire `e904485` 의 commit_recommender 의 resource_pattern EXTENSION 정합)
- F31.5-2 reconciliation strategy 결정 wire ((1) `chargeback_only` Phase 11 chargeback engine 출력 그대로 / (2) `tag_allocation_only` Phase 15 F31.3 allocation_rules_engine 출력 그대로 / (3) `hybrid_blended` Phase 11 chargeback 70% + Phase 15 tag_allocation 30% 가중 평균 default + per-tenant override reconciliation_strategy default hybrid_blended)
- F31.5-3 variance calculation 결정 wire (`variance_krw = sum(chargeback_cost) - sum(tag_allocation_cost)` + `variance_pct = variance_krw / sum(chargeback_cost) × 100` + variance_pct > 5% → investigate required + per-tenant override variance_threshold_pct default 5%)
- F31.5-4 reconciliation report generation 결정 wire (`ReconciliationReport` TypedDict 13 fields (1. report_id UUID PK + 2. tenant_id UUID + 3. period_key TEXT + 4. chargeback_cost_krw NUMERIC(20, 2) + 5. tag_allocation_cost_krw NUMERIC(20, 2) + 6. variance_krw NUMERIC(20, 2) + 7. variance_pct NUMERIC(8, 4) + 8. reconciliation_strategy enum + 9. department_breakdown JSONB + 10. unmatched_allocations JSONB + 11. investigation_required BOOLEAN + 12. generated_at TIMESTAMPTZ + 13. trace_id TEXT))
- F31.5-5 unmatched allocation handling 결정 wire (Phase 11 chargeback output 중 Phase 15 tag_allocation 에 매칭되지 않는 cost (e.g. tag 부재 resource → fallback department) → `unmatched_allocations` JSONB 에 기록 + reconcile_audit_log + Epic 12 2FA 챌린지 보존)
- F31.5-6 cross-period reconciliation 결정 wire (current period vs previous period 비교 + Phase 11 wire `e020ad0` 의 showback 의 comparison view EXTENSION + variance_pct_period_change = variance_now - variance_previous_period + variance_pct_period_change > +2% → investigate required + 13-period rolling reconciliation)
- F31.5-7 reconciliation investigation workflow 결정 wire (variance_pct > threshold → owner에게 Slack + Email 알림 + Jira ticket auto-create optional owner-only config + audit log 기록 + 14일 SLA 내 resolution + per-tenant override investigation_sla_days default 14 + Epic 12 2FA 챌린지 보존)
- F31.5-8 reconciliation approval workflow 결정 wire (reconciliation report 생성 → owner 승인 mandatory → 승인 후 chargeback 확정 + approval pending 시 chargeback status = pending_reconciliation + approve + confirm 액션 2 NEW + audit-first INSERT `reconciliation_approved` CR 1-1 verbatim + owner-only RBAC AD-22 verbatim 보존)
- F31.5-9 `apps/api/jobs/chargeback_reconciliation.py` NEW (~+120 LOC + cron KST 1st day of month 05:00 UTC 20:00 Phase 11 chargeback monthly close-out 의 다음 day + 4 reconciliation steps (1. chargeback cost aggregate + 2. tag allocation cost aggregate + 3. variance 계산 + 4. investigation_trigger) + audit-first INSERT `reconciliation_initiated` CR 1-1 verbatim + 5 NEW actions: reconciliation_initiated + reconciliation_report_generated + reconciliation_investigation_triggered + reconciliation_approved + reconciliation_resolved)
- F31.5-10 reconciliation dashboard 결정 wire (`apps/web/app/[locale]/(dashboard)/admin/finops/allocation/page.tsx` NEW ~+120 LOC + 4 components (`ReconciliationReportPanel` + `VarianceBreakdownChart` + `UnmatchedAllocationTable` + `ReconciliationApprovalButton`) + owner-only RBAC AD-22 verbatim + Epic 12 2FA 챌린지 보존 + ko-KR.json `finops_allocation_reconciliation.*` namespace EXTENSION ~15 keys CR 11-4 D-002 verbatim SSOT)
- F31.5-11 audit-first INSERT 5 NEW CR 1-1 verbatim 결정 wire (action_class='FINOPS_TAG_GOVERNANCE' + action='reconciliation_initiated' / 'reconciliation_report_generated' / 'reconciliation_investigation_triggered' / 'reconciliation_approved' / 'reconciliation_resolved' (5 NEW) + actor_id + tenant_id + period_key + variance_krw + variance_pct + reconciliation_strategy + trace_id 결정 wire + AuditAction Literal EXTENSION 5 NEW values 결정 wire + Phase 11 wire `e020ad0` 의 3 NEW audit actions EXTENSION pattern verbatim 적용 + ActionClass.FINOPS_TAG_GOVERNANCE entry EXTENSION 결정 wire)
- F31.5-12 typed exception envelope CR 12-5 D-14 verbatim 결정 wire (4 NEW typed exception classes: `ReconciliationError(500)` + `VarianceCalculationError(500)` + `ReconciliationApprovalError(403)` + `UnmatchedAllocationError(422)`)

### §F31.6 tag governance dashboard UI (10 sub-ACs)
- F31.6-1 `apps/web/app/[locale]/(dashboard)/admin/finops/tag-governance/page.tsx` NEW (~+150 LOC + 5 components (`TagPolicyEditor` + `UntaggedResourceDetectorPanel` + `AllocationRuleBuilder` + `ComplianceReportPanel` + `ReconciliationReportPanel`) 결정 wire + owner-only RBAC AD-22 verbatim + Epic 12 2FA 챌린지 보존 결정 wire)
- F31.6-2 `TagPolicyEditor` component 결정 wire (6 resource_type 옵션 selectbox ec2 + rds + s3 + lambda + eks + vpc + tag_key input AWS 표준 + custom prefix + 4 enforcement_level radio button required + recommended + optional + blocked + 4 default_value 옵션 string + env_based + conditional + inherit + compliance_threshold_pct slider 50~100% + owner-only ack prompt AD-22 verbatim + ko-KR.json `finops_tag_governance.policy.*` 8 keys CR 11-4 D-002 verbatim SSOT)
- F31.6-3 `UntaggedResourceDetectorPanel` component 결정 wire (6 resource_type 필터 selectbox + 4 enforcement_level 필터 checkbox + 4 remediation_status 필터 selectbox pending/notified/remediated/expired + 14 columns untagged_resource_id + resource_id + resource_type + required_tags_missing + enforcement_level + current_tags + detection_method + detected_at + remediation_status + remediation_action + severity + notify button + auto_remediate button + add_tag_button + severity color coding low=blue + medium=yellow + high=red + critical=dark red + RTL render discipline CR 11-4 D-003 verbatim + Phase 14 wire `e904485` 의 IdleResourcePanel EXTENSION 정합)
- F31.6-4 `AllocationRuleBuilder` component 결정 wire (5 rule_type selectbox tag_match + percentage_split + weighted + conditional + fallback + priority slider 0~100 + tag_key input + tag_value_pattern input regex 또는 literal + target_department_id input + percentage_weights 동적 입력 percentage_split 전용 + condition_ast JSON editor conditional 전용 + 6 columns rule_id + rule_type + priority + tag_key + tag_value_pattern + target_department + test_evaluation_button dry-run 시뮬레이션 + Recharts 2.12.7 AD-14 stack pin 결정 wire)
- F31.6-5 `ComplianceReportPanel` component 결정 wire (4 sections overall_compliance_pct + per_resource_type_compliance + non_compliant_resources_list + compliance_trend_chart + period_selector current + previous + last 3/6/12 months + 3 export buttons PDF + CSV + JSON + Recharts 2.12.7 AD-14 stack pin + Phase 11 wire `e020ad0` 의 ShowbackDepartmentBreakdownChart EXTENSION 정합)
- F31.6-6 ko-KR.json `finops_tag_governance.*` namespace EXTENSION ~30 keys 결정 wire (CR 11-4 D-002 verbatim SSOT) + `finops_tag_governance.policy.required` + `finops_tag_governance.policy.recommended` + `finops_tag_governance.policy.optional` + `finops_tag_governance.policy.blocked` + `finops_tag_governance.resource.ec2` + `finops_tag_governance.resource.rds` + `finops_tag_governance.resource.s3` + `finops_tag_governance.resource.lambda` + `finops_tag_governance.resource.eks` + `finops_tag_governance.resource.vpc` + `finops_tag_governance.remediation.pending` + `finops_tag_governance.remediation.notified` + `finops_tag_governance.remediation.remediated` + `finops_tag_governance.remediation.expired` 등 결정 wire)
- F31.6-7 ARIA labels 결정 wire (WCAG 2.1 AA compliance Epic 12 의 2FA 챌린지 UI 와 동일 표준 + keyboard navigation Tab + Enter + Arrow keys + screen reader 지원 + Phase 14 Epic 1 UX v1.0 locked decision 결정 wire Dark MVP / WCAG AA / Professional / ko-KR verbatim 보존)
- F31.6-8 toast notification 결정 wire (untagged_resource_detected 시 toast 자동 표시 severity=low → blue + 5초 / medium → yellow + 10초 / high → red + 15초 / critical → dark red + 20초 + compliance_report_generated 시 toast + reconciliation_approved 시 toast + Phase 14 wire `e904485` 의 toast notification EXTENSION 정합)
- F31.6-9 Vitest RTL render discipline CR 11-4 D-003 verbatim 결정 wire (7 NEW vitest cases TagPolicyEditor 1 + UntaggedResourceDetectorPanel 1 + AllocationRuleBuilder 1 + ComplianceReportPanel 1 + ReconciliationReportPanel 1 + ko-KR SSOT 2 + Phase 14 wire `e904485` 의 6 NEW vitest cases EXTENSION 정합)
- F31.6-10 FinOps tag governance dashboard parity CR 12-5 D-PARITY-01 결정 wire (TS mirror `apps/web/lib/finops-tag-governance/finops-tag-governance-client.ts` NEW ~+150 LOC + `tests/web/test_finops_tag_governance_dashboard_parity.py` NEW ~+10 cases + 1-line ko-KR reject 결정 wire + Phase 14 wire `e904485` 의 finops-optimization-client.ts EXTENSION 정합)

### §F31.7 Capability matrix v1.41 EXTENSION (12 sub-ACs)
- F31.7-1 Capability matrix v1.40 → v1.41 EXTENSION 결정 wire (1 NEW row FINOPS_TAG_GOVERNANCE industry-agnostic 4-industry grants ✅/✅/✅/✅ CR 12-1 L4 precedent 미러)
- F31.7-2 `apps/api/core/capability.py` MODIFIED 결정 wire (Capability.FINOPS_TAG_GOVERNANCE = "finops_tag_governance" 1 NEW enum + 4 `_INDUSTRY_CAPABILITIES` blocks EXTENSION industry-agnostic ✅/✅/✅/✅)
- F31.7-3 `apps/api/dependencies/capability.py` MODIFIED 결정 wire (require_finops_tag_governance 1 NEW dep + `__all__` EXTENSION)
- F31.7-4 `docs/capability-matrix.md` MODIFIED 결정 wire (capability matrix v1.40 → v1.41 EXTENSION + 1 NEW row FINOPS_TAG_GOVERNANCE industry-agnostic 4-industry grants ✅/✅/✅/✅ + FINOPS_TAG_GOVERNANCE section 신규 추가)
- F31.7-5 `apps/api/modules/finops/tag_governance/__init__.py` NEW + `apps/api/modules/finops/tag_governance/serializers.py` NEW 결정 wire (Phase 14 wire `e904485` m22_finops_optimization EXTENSION pattern verbatim 미러 + m23_finops_tag_governance module 결정 wire)
- F31.7-6 미허용 tenant 의 tag governance 진입 차단 결정 wire (require_finops_tag_governance dep + capability gate per-tenant on/off + 403 Forbidden + FORBIDDEN_KO message 결정 wire ("FinOps Tag Governance & Cost Allocation capability 미허용 tenant") + Epic 12 2FA 챌린지 보존 + AD-22 owner-only RBAC 정합)
- F31.7-7 phase_14 carry-over 진입 차단 결정 wire (tag governance 진입 시 phase_14 FINOPS_OPTIMIZATION + phase_13 FINOPS_FORECASTING_CAPACITY_PLANNING capability 도 동시에 검증 + tag governance 가 FINOPS_TAG_GOVERNANCE 만 있고 FINOPS_OPTIMIZATION / FINOPS_FORECASTING_CAPACITY_PLANNING 없는 경우 403 Forbidden 결정 wire)
- F31.7-8 drift detector 8 NEW pytest cases 결정 wire (`tests/integration/test_capability_matrix_v1_41_drift.py` NEW + Phase 14 wire `e904485` `test_capability_matrix_v1_40_drift.py` 패턴 verbatim 미러)
- F31.7-9 m23_finops_tag_governance module 결정 wire (apps/api/modules/finops/__init__.py EXTENSION + m23_finops_tag_governance.tag_governance_serializers NEW 결정 wire + Phase 14 wire `e904485` m22_finops_optimization.optimization_serializers EXTENSION pattern verbatim 미러)
- F31.7-10 SSOT RED→GREEN EXTENSION + A36 SDR 검증 4-step 자동 적용 결정 wire (capability matrix v1.41 신규 1 row + capability.py EXTENSION 1 NEW enum + require_finops_tag_governance 1 NEW dep 결정 wire + drift detector EXTENSION)
- F31.7-11 CR 12-1 L4 industry-agnostic capability 결정 wire (FINOPS_TAG_GOVERNANCE industry-agnostic 4-industry grants ✅/✅/✅/✅ 결정 wire manufacturing + service + manufacturing_service + manufacturing_service_other 모두 허용)
- F31.7-12 capability gate 의 fail-closed 결정 wire (미허용 tenant 의 tag governance 진입 차단 + capability matrix v1.41 row 부재 시 fail-closed + Capability enum 부재 시 fail-closed + AD-22 owner-only RBAC 정합 + Epic 12 2FA 챌린지 보존 + NFR4 PII minimization 정합 보존)

### §F31.8 dry-run + Tests + wire scope T1~T8 (12 sub-ACs)
- F31.8-1 dry-run mode 결정 wire (`--finops-tag-policy-dry-run` + `--finops-untagged-resource-dry-run` + `--finops-allocation-rule-dry-run` + `--finops-compliance-report-dry-run` + `--finops-chargeback-reconciliation-dry-run` 5 CLI flag + tag policy dry-run parameter + untagged resource dry-run parameter + allocation rule dry-run parameter + compliance report dry-run parameter + chargeback reconciliation dry-run parameter + dry-run 시 actual `tag_policy_updated` + `untagged_resource_detected` + `allocation_rule_evaluated` + `compliance_report_generated` + `reconciliation_initiated` audit-first INSERT skip 결정 wire)
- F31.8-2 dry-run 의 preview 결과 결정 wire (phase_15_finops_tag_policy_preview + phase_15_finops_untagged_resource_preview + phase_15_finops_allocation_preview + phase_15_finops_compliance_preview + phase_15_finops_reconciliation_preview 5 table 결정 wire + preview_id UUID PK + tenant_id UUID + preview_type enum + period_key TEXT + preview_data JSONB + computed_at TIMESTAMPTZ DEFAULT NOW() + trace_id TEXT + RLS 자동 적용 CR 0-2 verbatim 결정 wire + audit-first INSERT `tag_governance_dry_run_executed` CR 1-1 verbatim 결정 wire)
- F31.8-3 dry-run 의 CLI flag 결정 wire (`--finops-tag-policy-dry-run` + `--finops-untagged-resource-dry-run` + `--finops-allocation-rule-dry-run` + `--finops-compliance-report-dry-run` + `--finops-chargeback-reconciliation-dry-run` 5 NEW CLI flag + Phase 14 wire `e904485` `--finops-optimization-dry-run` + `--finops-rightsizing-dry-run` + `--finops-idle-resource-dry-run` + `--finops-commitment-dry-run` 패턴 verbatim 미러)
- F31.8-4 tests ~+56 NEW pytest PASS 결정 wire (tag_policy_dsl 6 + untagged_resource_detector 8 + allocation_rules_engine 8 + allocation_audit 6 + chargeback_allocation_reconciliation 7 + alembic 0047 5 + audit action 8 + capability matrix v1.41 8 = ~+56 NEW pytest PASS)
- F31.8-5 vitest tests ~+7 NEW vitest PASS 결정 wire (TagPolicyEditor 1 + UntaggedResourceDetectorPanel owner-only ack prompt AD-22 verbatim 1 + AllocationRuleBuilder RTL render 1 + ComplianceReportPanel 1 + ReconciliationReportPanel 1 + ko-KR SSOT 2 + finops tag governance dashboard parity CR 12-5 D-PARITY-01 1 = ~+8 NEW vitest PASS)
- F31.8-6 ruff + tsc 0 NEW + SDR drift gate 결정 wire (0 NEW ruff + 0 NEW tsc + 0 regressions + SDR drift gate PASS 결정 wire + A36 SDR 검증 4-step 자동 적용)
- F31.8-7 wire scope T1~T8 결정 wire (~+33-38 files estimate = ~+21 NEW + ~+12 MODIFIED atomic single sprint = Phase 14 wire `e904485` 의 ~27 files pattern verbatim 미러)
- F31.8-8 A19 cohesion pattern 9 surface EXTENSION PASS 결정 wire (FinOps Tag Governance & Cost Allocation surface NEW = F31.1~F31.8 + spec surface EXTENSION + test surface EXTENSION + docs surface EXTENSION)
- F31.8-9 CR lessons applied 14종 결정 wire (CR 0-2 + CR 1-1 + CR 4-3/4-4 + CR 1-1 ContextVar + CR 1-1 RSC boundary + CR 9-6 + CR 11-3 + CR 11-4 + CR 12-1 + CR 12-5 D-14 + CR 12-5 D-PARITY-01 + CR 12-5 D-GATE-01 + A19 cohesion + A36 SDR 검증)
- F31.8-10 D-DEFER-* honestly 결정 wire (D-FINOPS-5 honestly preserved → Phase 15 PRD entry 진입 시점에 carry-over chain 정직 회복 결정 wire + D-FINOPS-4 ✅ RESOLVED + D-FINOPS-3 ✅ RESOLVED + D-FINOPS-2 ✅ RESOLVED + D-FINOPS-1 ✅ RESOLVED + D-SLO-1 ✅ RESOLVED + D-CHAOS-1 ✅ RESOLVED + D-PERFORMANCE-1 ✅ RESOLVED + D-OBSERVABILITY-1 ✅ RESOLVED + D-RETENTION-1 ✅ RESOLVED + D-EPIC-17-WIRE-DEFER-T2-T3-UI ✅ RESOLVED + D-PHASE-4-DR-DEFER-1/2 ✅ RESOLVED + D-EPIC-16-REVIEW-DEFER-2~6 ✅ RESOLVED + D-1-1-DEFER-1/2/3 ✅ RESOLVED 모두 보존)
- F31.8-11 Epic 1 ~ Epic 17 + Phase 3 ~ Phase 14 + 1st release cycle 정합 보존 결정 wire (cj-style 121번째 wire entry 모두 DONE 진입 정합 보존 + Phase 15 PRD entry `87393b4` + Phase 14 close-out retro `5b367d9` + Phase 14 atomic wire T1~T8 `e904485` + Phase 14 spec entry `30637f6` + Phase 14 PRD entry `0e3f8d9` 모두 정합)
- F31.8-12 partial wire 시도 0건 + single sprint atomic docs-only wire 1 진입점 결정 wire (cj-style 122번째 epic 연속 정직 회복 Phase 15 spec entry atomic docs-only wire 5 files atomic single sprint 결정 wire)

## 8 tasks (T1~T8) + 68 subtasks

### T1: tag_policy_dsl + tag_governance module (10 subtasks)
- T1.1: `apps/api/modules/finops/tag_policy_dsl.py` NEW (~+150 LOC + define_tag_policy builder + AST 5 levels + parser 검증 3 layer + 4 industries baseline + per-tenant override EXTENSION + TAG_POLICY_DEFAULTS constants 결정 wire)
- T1.2: TagPolicy TypedDict 11 fields 결정 wire (policy_id + tenant_id + resource_type enum + tag_key + enforcement_level enum + default_value + compliance_threshold_pct + remediation_action + status enum + created_at + updated_at + trace_id)
- T1.3: 6 resource_type 옵션 결정 wire (ec2 + rds + s3 + lambda + eks + vpc + per-tenant override EXTENSION)
- T1.4: 4 enforcement_level 옵션 결정 wire (required + recommended default + optional + blocked + per-tenant override JSONB + per-resource-type override JSONB)
- T1.5: tag policy pure validator CR 11-4 P-015 verbatim 적용 결정 wire (parse_tag_policy + validate_tag_policy + 5 layer defense + TagPolicyInvalidError(400) CR 12-5 D-14 envelope)
- T1.6: 6 NEW pytest cases 결정 wire (TypedDict validation + 6 resource_type 옵션 + 4 enforcement_level 옵션 + tag_key validation + default_value 옵션 + 4 industries baseline + audit-first INSERT + owner-only RBAC + dry_run default)
- T1.7: tag_policy_dsl audit-first INSERT `tag_policy_updated` 결정 wire (CR 1-1 verbatim 적용 + ActionClass.FINOPS_TAG_GOVERNANCE + emit_audit_typed BEFORE tag policy update + per-tenant RLS 자동 적용)
- T1.8: tag_policy owner-only RBAC AD-22 결정 wire (tag policy update 모두 owner-only + Epic 12 2FA 챌린지 + governance_required=True mandatory)
- T1.9: tag_policy dry-run mode default 결정 wire (dry_run=True flag + audit-first INSERT `tag_governance_dry_run_executed` + no actual tag policy update)
- T1.10: tag_policy CR 0-2 RLS verbatim 적용 결정 wire + tenant_id selector + cross-tenant isolation 검증 + phase_15_finops_tag_policy_definition table 결정 wire

### T2: untagged_resource_detector + 6 resource types + tag_governance module (10 subtasks)
- T2.1: `apps/api/modules/finops/untagged_resource_detector.py` NEW (~+200 LOC + detect_untagged_resources 함수 + 6 resource types parallel run + Phase 14 wire `e904485` idle_resource_detector EXTENSION 결정 wire + CR 12-5 D-PARITY-01 verbatim)
- T2.2: UntaggedResource TypedDict 14 fields 결정 wire (untagged_resource_id + tenant_id + resource_id + resource_type enum + required_tags_missing + optional_tags_missing + enforcement_level enum + current_tags JSONB + detection_method enum + detected_at + remediation_status enum + remediation_action enum + policy_id UUID FK + trace_id)
- T2.3: 6 resource types 결정 wire (ec2 EC2 + rds RDS + s3 S3 + lambda Lambda + eks EKS + vpc VPC endpoint/NAT/SG/IGW + Phase 14 wire `e904485` 의 idle_resource_detector EXTENSION)
- T2.4: tag inventory 조회 전략 결정 wire (AWS Resource Groups Tagging API + boto3 `describe_tags()` + per-resource_type metadata inventory = ec2 instance_age_days/region/instance_type + rds db_engine/db_class/storage_gb/multi_az + s3 bucket_size_bytes/object_count/versioning_enabled + lambda function_runtime/memory_mb/timeout_seconds/last_modified_date/invocations_30d_count + eks k8s_version/node_count/pod_count + vpc vpc_id/cidr_block/attached_resources_count)
- T2.5: ec2 + rds + s3 + lambda + eks + vpc individual untagged detection 결정 wire (ec2 required_tags 부재 시 untagged classify / rds db instance tag inventory 조회 / s3 bucket tag inventory 조회 / lambda function tag inventory + 7d/30d invocations < 1 → untagged_lambda_warn / eks cluster+node group tag inventory / vpc VPC endpoint+NAT+SG+IGW tag inventory)
- T2.6: remediation_status state machine 결정 wire (pending default → notified owner 알림 발송 후 → remediated owner 태그 추가 후 → expired 30일 후 미조치 시 자동 escalate + per-tenant override remediation_timeout_days default 30 + Epic 12 2FA 챌린지 보존 auto_remediate 의 tag add/remove 시)
- T2.7: severity classification 결정 wire (required → high severity 즉시 알림 + Slack + PagerDuty / recommended → medium severity 이메일 + Slack / optional → low severity audit log only / blocked → critical severity CI/CD gate block + 즉시 알림 + per-tenant override severity_threshold JSONB)
- T2.8: audit-first INSERT `untagged_resource_detected` 결정 wire (CR 1-1 verbatim 적용 + ActionClass.FINOPS_TAG_GOVERNANCE + emit_audit_typed BEFORE untagged resource detection + per-tenant RLS 자동 적용)
- T2.9: typed exception envelope CR 12-5 D-14 verbatim 결정 wire (4 NEW typed exception classes: UntaggedResourceDetectionError + TagInventoryUnavailableError + RemediationActionError + EnforcementLevelViolationError)
- T2.10: 8 NEW pytest cases 결정 wire (6 resource types + tag inventory + remediation_status state machine + severity classification + audit-first INSERT + typed exception envelope)

### T3: allocation_rules_engine + 5 rule_types + tag_governance module (10 subtasks)
- T3.1: `apps/api/modules/finops/allocation_rules_engine.py` NEW (~+220 LOC + evaluate_allocation_rules 함수 + 5 rule_types parallel run + Phase 11 wire `e020ad0` chargeback engine EXTENSION 결정 wire + CR 12-5 D-PARITY-01 verbatim)
- T3.2: AllocationRule TypedDict 13 fields 결정 wire (rule_id + tenant_id + rule_type enum + priority INT + tag_key + tag_value_pattern + target_department_id + percentage_weights JSONB nullable + condition_ast JSONB nullable + fallback_department_id TEXT nullable + enabled BOOLEAN + created_at + trace_id)
- T3.3: 5 rule_types 결정 wire (tag_match resource tag value → department_id 직접 매칭 1:1 + percentage_split resource tag value → N department list + percentage weights + weighted resource tag value list → weight 계산 monthly_active_user_count 기반 + conditional Lambda function 평가 결과 → department_id 매핑 + fallback 4 rule_types 모두 매칭 실패 시 catch-all default department + Phase 11 wire `e020ad0` 의 chargeback cost allocation engine EXTENSION 정합)
- T3.4: rule precedence + rule_evaluation_metadata 결정 wire (policy evaluation precedence `tag_match > percentage_split > weighted > conditional > fallback` (specificity 순서) + per-rule override priority INT default = 0 higher = 먼저 evaluate + applied_rule_type + applied_rule_id + match_score 0.0~1.0 + fallback_chain List[rule_type] + evaluation_duration_ms + condition_evaluation_log JSONB)
- T3.5: AllocatedCost TypedDict 11 fields 결정 wire (allocation_id + tenant_id + period_key + resource_id + resource_type enum + cost_amount_krw NUMERIC(20, 2) + target_department_id + applied_rule_type enum + applied_rule_id UUID FK + match_score NUMERIC(8, 4) + allocated_at + trace_id)
- T3.6: tag_match + percentage_split + weighted individual allocation rule 결정 wire (tag_match resource tag value → department_id 직접 매칭 + 1 resource = 1 department + 매칭 실패 시 fallback / percentage_split percentage 합 = 100% 검증 + UNIQUE constraint (tenant_id, cost_center_id, period_key) / weighted weight = monthly_active_user_count + 0 weight → fallback)
- T3.7: conditional + fallback individual allocation rule 결정 wire (conditional Lambda function 평가 결과 → department_id 매핑 + condition evaluation AST parser CR 11-4 P-015 verbatim + condition 평가 실패 시 fallback / fallback cost 100% allocated to fallback department + audit-first INSERT `allocation_fallback_applied` CR 1-1 verbatim + per-tenant override fallback_department_id default "shared")
- T3.8: `apps/api/modules/finops/chargeback_allocation_reconciliation.py` NEW (~+180 LOC + reconcile_chargeback_allocation 함수 + reconciliation_strategy 3종 chargeback_only/tag_allocation_only/hybrid_blended default + variance calculation + Phase 11 wire `e020ad0` 의 chargeback engine EXTENSION)
- T3.9: ReconciliationReport TypedDict 13 fields + variance calculation 결정 wire (report_id + tenant_id + period_key + chargeback_cost_krw + tag_allocation_cost_krw + variance_krw + variance_pct + reconciliation_strategy enum + department_breakdown JSONB + unmatched_allocations JSONB + investigation_required BOOLEAN + generated_at + trace_id + variance_krw = sum(chargeback_cost) - sum(tag_allocation_cost) + variance_pct = variance_krw / sum(chargeback_cost) × 100)
- T3.10: typed exception envelope CR 12-5 D-14 verbatim 결정 wire (8 NEW typed exception classes: AllocationRuleInvalidError + AllocationRuleEvaluationError + PercentageSumValidationError + ConditionalRuleParseError + ReconciliationError + VarianceCalculationError + ReconciliationApprovalError + UnmatchedAllocationError) + 16 NEW pytest cases (5 rule_types + rule precedence + audit-first INSERT + reconciliation 3 strategy + variance calculation + cross-period + typed exception envelope)

### T4: allocation_audit + compliance + Phase 14 optimization_accuracy_tracker EXTENSION (10 subtasks)
- T4.1: `apps/api/modules/finops/allocation_audit.py` NEW (~+150 LOC + audit_allocation 함수 + per-(tenant_id + resource_type + rule_type) granularity + Phase 14 wire `e904485` 의 optimization_accuracy_tracker EXTENSION chain 결정 wire)
- T4.2: compliance score + compliance threshold 결정 wire (compliance_pct = (tagged_resources / total_resources) × 100 per resource_type + total compliance = sum(compliance_pct × resource_count) / sum(resource_count) 가중 평균 + compliance_pct ≥ 95% → compliant / 80~95% → partially_compliant / < 80% → non_compliant + per-tenant override compliance_threshold_pct default 95%)
- T4.3: allocation accuracy + allocation coverage 결정 wire (accuracy_pct = sum(matched_allocations) / sum(total_allocations) × 100 + accuracy ≥ 99% → high / 95~99% → moderate / < 95% → low + per-tenant override accuracy_threshold default 99% + coverage_pct = sum(allocated_resources) / sum(total_resources) × 100 + coverage ≥ 95% → high / 80~95% → moderate / < 80% → low + 미할당 cost → fallback department 자동 할당)
- T4.4: ComplianceReport TypedDict 12 fields + compliance trend 결정 wire (report_id + tenant_id + period_key + total_resources + tagged_resources + untagged_resources + compliance_pct + allocation_accuracy_pct + allocation_coverage_pct + non_compliant_resources JSONB + generated_at + trace_id + past 12-month compliance_pct 추이 분석 + trend = compliance_pct_now - compliance_pct_12mo_ago + trend > +5% → improving / -5~+5% → stable / < -5% → degrading + Slack 알림 degrading 의 경우만)
- T4.5: compliance report delivery + alert routing + remediation workflow 결정 wire (apps/api/jobs/compliance_report.py NEW ~+120 LOC + cron KST 1st day of month 04:00 UTC 19:00 + report format 3종 PDF + CSV + JSON + owner-only access + Epic 12 2FA 챌린지 보존 + retention 365일 + Slack #bizup-compliance-alerts channel + PagerDuty pd_compliance_critical service critical severity 시 + Email owner-only + audit-first INSERT `compliance_alert_sent` CR 1-1 verbatim + remediation workflow 5 steps (1. owner notifier 발송 + 2. tag_value 자동 추천 Phase 14 wire `e904485` 의 resource_pattern 기반 + 3. owner의 7일 SLA 내 tag 추가 + 4. 만료 시 escalate to admin + 5. audit log 기록))
- T4.6: reconciliation approval workflow 결정 wire (reconciliation report 생성 → owner 승인 mandatory → 승인 후 chargeback 확정 + approval pending 시 chargeback status = pending_reconciliation + approve + confirm 액션 2 NEW + audit-first INSERT `reconciliation_approved` CR 1-1 verbatim + owner-only RBAC AD-22 verbatim 보존)
- T4.7: `apps/api/jobs/chargeback_reconciliation.py` NEW (~+120 LOC + cron KST 1st day of month 05:00 UTC 20:00 Phase 11 chargeback monthly close-out 의 다음 day + 4 reconciliation steps (1. chargeback cost aggregate + 2. tag allocation cost aggregate + 3. variance 계산 + 4. investigation_trigger) + audit-first INSERT `reconciliation_initiated` CR 1-1 verbatim + 5 NEW actions: reconciliation_initiated + reconciliation_report_generated + reconciliation_investigation_triggered + reconciliation_approved + reconciliation_resolved)
- T4.8: audit-first INSERT EXTENSION 10 NEW 결정 wire (`tag_policy_updated` + `untagged_resource_detected` + `allocation_rule_evaluated` + `allocation_rule_updated` + `compliance_report_generated` + `compliance_alert_sent` + `compliance_remediation_initiated` + `reconciliation_initiated` + `reconciliation_report_generated` + `reconciliation_investigation_triggered` + `reconciliation_approved` + `reconciliation_resolved` 12 actions total wait 10 NEW = 7 d + 5 e + 1 dry_run = 12 + ActionClass.FINOPS_TAG_GOVERNANCE + emit_audit_typed BEFORE/AFTER tag governance event CR 1-1 verbatim 적용 + per-tenant RLS 자동 적용 + multi-tenant isolation test)
- T4.9: model_version tracking 결정 wire (tag_policy_dsl + untagged_resource_detector + allocation_rules_engine + chargeback_allocation_reconciliation 4 engine 모두 semantic versioning MAJOR.MINOR.PATCH + model_version JSONB metadata training_date + training_data_window + training_samples_count + algorithm_hyperparameters + cross_validation_score + Phase 14 wire `e904485` optimization_model_registry EXTENSION 결정 wire)
- T4.10: 6 NEW pytest cases 결정 wire (compliance score + accuracy + coverage + trend analysis + report generation + alert routing + remediation workflow + reconciliation 3 strategy + approval workflow + audit-first INSERT + typed exception envelope)

### T5: alembic 0047 phase_15_tag_governance (8 subtasks)
- T5.1: `apps/api/alembic/versions/0047_phase_15_tag_governance.py` NEW (~+250 LOC + 6 tables CREATE + indexes + RLS policies + down_revision "0046_phase_14_optimization" 결정 wire)
- T5.2: phase_15_finops_tag_policy_definition table 12 columns 결정 wire (policy_id UUID PK + tenant_id UUID + resource_type TEXT enum + tag_key TEXT + enforcement_level TEXT enum + default_value TEXT + compliance_threshold_pct NUMERIC(8, 4) + remediation_action TEXT enum + status TEXT enum + created_at TIMESTAMPTZ + updated_at TIMESTAMPTZ + trace_id TEXT + tenant_id_resource_type_tag_key UNIQUE constraint + JSONB metadata)
- T5.3: phase_15_finops_untagged_resource table 15 columns 결정 wire (untagged_resource_id UUID PK + tenant_id UUID + resource_id TEXT + resource_type TEXT enum + required_tags_missing TEXT[] + optional_tags_missing TEXT[] + enforcement_level TEXT enum + current_tags JSONB + detection_method TEXT enum + detected_at TIMESTAMPTZ + remediation_status TEXT enum + remediation_action TEXT enum + policy_id UUID FK + trace_id TEXT + INDEX)
- T5.4: phase_15_finops_allocation_rule table 14 columns 결정 wire (rule_id UUID PK + tenant_id UUID + rule_type TEXT enum + priority INTEGER + tag_key TEXT + tag_value_pattern TEXT + target_department_id TEXT + percentage_weights JSONB + condition_ast JSONB + fallback_department_id TEXT + enabled BOOLEAN + created_at TIMESTAMPTZ + trace_id TEXT + INDEX)
- T5.5: phase_15_finops_allocated_cost table 13 columns 결정 wire (allocation_id UUID PK + tenant_id UUID + period_key TEXT + resource_id TEXT + resource_type TEXT enum + cost_amount_krw NUMERIC(20, 2) + target_department_id TEXT + applied_rule_type TEXT enum + applied_rule_id UUID FK + match_score NUMERIC(8, 4) + allocated_at TIMESTAMPTZ + trace_id TEXT + UNIQUE (tenant_id, cost_center_id, period_key))
- T5.6: phase_15_finops_compliance_report table 13 columns 결정 wire (report_id UUID PK + tenant_id UUID + period_key TEXT + total_resources INTEGER + tagged_resources INTEGER + untagged_resources INTEGER + compliance_pct NUMERIC(8, 4) + allocation_accuracy_pct NUMERIC(8, 4) + allocation_coverage_pct NUMERIC(8, 4) + non_compliant_resources JSONB + generated_at TIMESTAMPTZ + trace_id TEXT + INDEX)
- T5.7: phase_15_finops_reconciliation_report table 14 columns 결정 wire (report_id UUID PK + tenant_id UUID + period_key TEXT + chargeback_cost_krw NUMERIC(20, 2) + tag_allocation_cost_krw NUMERIC(20, 2) + variance_krw NUMERIC(20, 2) + variance_pct NUMERIC(8, 4) + reconciliation_strategy TEXT enum + department_breakdown JSONB + unmatched_allocations JSONB + investigation_required BOOLEAN + generated_at TIMESTAMPTZ + trace_id TEXT + INDEX)
- T5.8: 6 tables RLS policies + 5 preview tables 결정 wire (CR 0-2 verbatim + tenant_id = current_setting('app.tenant_id')::uuid + Phase 14 wire `e904485` phase_14_finops_* table 정합 + Phase 13 wire `8b98030` phase_13_finops_* table 정합 + Phase 12 wire `f3c0e63` phase_12_finops_* table 정합 + Phase 11 wire `e020ad0` phase_11_finops_* table 정합 + Phase 10 wire `ac5d6c5` phase_10_slo_* table 정합 + 5 preview tables phase_15_finops_tag_policy_preview + phase_15_finops_untagged_resource_preview + phase_15_finops_allocation_preview + phase_15_finops_compliance_preview + phase_15_finops_reconciliation_preview 결정 wire + alembic migration 6 NEW pytest cases + multi-tenant isolation test 결정 wire)

### T6: audit action EXTENSION 10 NEW + typed exception envelope 15 NEW (8 subtasks)
- T6.1: `apps/api/core/audit_action.py` MODIFIED 결정 wire (ActionClass.FINOPS_TAG_GOVERNANCE 1 NEW class 신규 정의 + FinopsTagGovernanceAction Literal 10 NEW values + _ActionRegistry FINOPS_TAG_GOVERNANCE entry 신규 1개 등록 + __all__ EXTENSION + AuditAction Union EXTENSION)
- T6.2: ActionClass.FINOPS_TAG_GOVERNANCE = 'finops_tag_governance' 신규 정의 결정 wire (CR 12-1 L4 precedent 미러 FINOPS_OPTIMIZATION Phase 14 wire + FINOPS_FORECASTING_CAPACITY_PLANNING Phase 13 wire + FINOPS_ANOMALY_DETECTION + FINOPS_BUDGET_ALERT Phase 12 wire + FINOPS_SHOWBACK + FINOPS_CHARGEBACK Phase 11 wire + SLO_ENGINEERING Phase 10 wire + CHAOS_ENGINEERING Phase 9 wire + PERFORMANCE_TESTING Phase 8 wire + OBSERVABILITY_TRACES + OBSERVABILITY_METRICS Phase 7 wire pattern verbatim bind)
- T6.3: FinopsTagGovernanceAction Literal 10 NEW values 결정 wire = `tag_policy_updated` + `untagged_resource_detected` + `allocation_rule_evaluated` + `allocation_rule_updated` + `compliance_report_generated` + `compliance_alert_sent` + `compliance_remediation_initiated` + `reconciliation_initiated` + `reconciliation_report_generated` + `reconciliation_investigation_triggered` + `reconciliation_approved` + `reconciliation_resolved` 12 total wait 10 NEW = 7 d + 5 e EXTENSION + 1 dry_run tag_governance_dry_run_executed (CR 1-1 verbatim 적용)
- T6.4: _ActionRegistry FINOPS_TAG_GOVERNANCE entry 신규 1개 등록 결정 wire (resource_table "phase_15_finops_*" + action_class=FINOPS_TAG_GOVERNANCE + 12 NEW actions acceptance + reject)
- T6.5: emit_audit_typed BEFORE/AFTER FinOps Tag Governance event CR 1-1 verbatim 적용 결정 wire (tag_policy_updated 의 audit_first INSERT 가 tag policy 직전에 실행 + untagged_resource_detected AFTER untagged resource detection + allocation_rule_evaluated AFTER allocation rule evaluation + allocation_rule_updated BEFORE allocation rule update + compliance_report_generated AFTER compliance report generation + compliance_alert_sent AFTER alert routing + compliance_remediation_initiated BEFORE remediation workflow + reconciliation_initiated BEFORE reconciliation + reconciliation_report_generated AFTER reconciliation report + reconciliation_investigation_triggered AFTER investigation + reconciliation_approved BEFORE approval + reconciliation_resolved AFTER resolution + tag_governance_dry_run_executed AFTER dry-run + trace_id propagation + actor_id capture + tenant_id capture)
- T6.6: multi-tenant isolation 결정 wire (10 NEW action 의 tenant_id 가 RLS 와 정합 + cross-tenant audit log leak 방지 결정 wire)
- T6.7: typed exception envelope CR 12-5 D-14 verbatim 결정 wire (15 NEW typed exception classes: TagPolicyInvalidError(400) + TagPolicyScopeInvalidError(404) + TagPolicyHistoryUnavailableError(404) + UntaggedResourceDetectionError(500) + UntaggedThresholdBreachError(500) + UntaggedMetricUnavailableError(404) + AllocationRuleEvaluationError(500) + AllocationRuleScopeError(404) + AllocationRulePrecedenceError(422) + ComplianceReportGenerationError(500) + ComplianceAlertError(500) + ChargebackReconciliationError(500) + ReconciliationDeltaBreachError(500) + ReconciliationApprovalError(500) + TagGovernanceAccuracyDegradationError(500))
- T6.8: 8 NEW pytest cases 결정 wire (AuditAction Literal 값 검증 + ActionClass.FINOPS_TAG_GOVERNANCE enum value + resource_table + emit_audit_typed BEFORE/AFTER FinOps Tag Governance event CR 1-1 verbatim 적용 + multi-tenant isolation + trace_id propagation + typed exception envelope + dry-run default)

### T7: capability v1.41 EXTENSION + frontend finops tag governance dashboard (8 subtasks)
- T7.1: `apps/api/core/capability.py` MODIFIED 결정 wire (Capability.FINOPS_TAG_GOVERNANCE 1 NEW enum + 4 `_INDUSTRY_CAPABILITIES` blocks EXTENSION industry-agnostic ✅/✅/✅/✅ CR 12-1 L4 precedent 미러)
- T7.2: `apps/api/dependencies/capability.py` MODIFIED 결정 wire (require_finops_tag_governance 1 NEW dep + __all__ EXTENSION 결정 wire)
- T7.3: capability matrix v1.40 → v1.41 EXTENSION title update + v1.41 changelog entry prepend + 1 NEW row FINOPS_TAG_GOVERNANCE industry-agnostic 4-industry grants ✅/✅/✅/✅ 결정 wire
- T7.4: `tests/integration/test_capability_matrix_v1_41_drift.py` NEW 8 NEW pytest cases 결정 wire (Capability.FINOPS_TAG_GOVERNANCE enum + 4 industries grants + v1.40 + v1.39 + v1.38 + v1.37 + v1.36 + v1.35 + v1.34 + v1.33 + v1.32 + v1.31 + v1.30 + v1.29 preservation + Phase 5 v1.29 + Epic 16 v1.28 + Epic 17 v1.30 + Phase 6 v1.31 + Phase 7 v1.32 + Phase 8 v1.33 + Phase 9 v1.34 + Phase 10 v1.35 + Phase 11 v1.36 + Phase 12 v1.37 + Phase 13 v1.38 + Phase 13 v1.39 + Phase 14 v1.40 pattern verbatim)
- T7.5: `docs/capability-matrix.md` MODIFIED v1.40 → v1.41 EXTENSION 결정 wire (1 NEW row FINOPS_TAG_GOVERNANCE industry-agnostic 4-industry grants + FINOPS_TAG_GOVERNANCE section 신규 추가)
- T7.6: 미허용 tenant 의 FinOps Tag Governance 진입 차단 결정 wire (require_finops_tag_governance dep + capability gate per-tenant on/off + phase_14 carry-over 검증 + phase_13 + phase_12 + phase_11 carry-over 검증)
- T7.7: `apps/web/app/[locale]/(dashboard)/admin/finops/tag-governance/page.tsx` NEW (~+150 LOC + 5 components 결정 wire: TagPolicyEditor + UntaggedResourceDetectorPanel + AllocationRuleBuilder + ComplianceReportPanel + ReconciliationReportPanel + owner-only RBAC AD-22 verbatim + Epic 12 2FA 챌린지 보존)
- T7.8: SSOT RED→GREEN EXTENSION 결정 wire (capability matrix v1.41 신규 1 row + capability.py EXTENSION 1 NEW enum + require_finops_tag_governance 1 NEW dep wire + drift detector EXTENSION + frontend finops tag governance dashboard wire)

### T8: atomic commit (4 subtasks)
- T8.1: 3중 게이트 impact NONE 결정 wire (ruff scoped 0 NEW + pytest 0 NEW failures + vitest 0 NEW failures + tsc 0 NEW errors)
- T8.2: A19 cohesion pattern 9 surface EXTENSION PASS 결정 wire (FinOps Tag Governance & Cost Allocation surface NEW = F31.1~F31.8)
- T8.3: atomic commit via `git commit -F <file>` 결정 wire (CR 9-6 D5 prevention + PowerShell here-string 회피)
- T8.4: sprint-status.yaml `phase-15-spec-entry: backlog → done` transition 결정 wire

## Dev Notes (CR lessons applied 14종)

- **CR 0-2 RLS lesson ✅ APPLIED**: Phase 15 wire 시점에 phase_15_finops_tag_policy_definition + phase_15_finops_untagged_resource + phase_15_finops_allocation_rule + phase_15_finops_allocated_cost + phase_15_finops_compliance_report + phase_15_finops_reconciliation_report + phase_15_finops_tag_policy_preview + phase_15_finops_untagged_resource_preview + phase_15_finops_allocation_preview + phase_15_finops_compliance_preview + phase_15_finops_reconciliation_preview 11 tables 모두 RLS 자동 적용 + multi-tenant isolation test 결정 wire + tenant-scoped result_hash 결정 wire + Phase 14 wire `e904485` phase_14_finops_* table 정합 + Phase 13 wire `8b98030` phase_13_finops_* table 정합 + Phase 12 wire `f3c0e63` phase_12_finops_* table 정합 + Phase 11 wire `e020ad0` phase_11_finops_* table 정합 + Phase 10 wire `ac5d6c5` phase_10_slo_* table 정합 + Phase 5 wire `f093f8c` phase_5_replication_lag table 정합
- **CR 1-1 audit-first INSERT ✅ APPLIED**: ActionClass.FINOPS_TAG_GOVERNANCE 신규 정의 + 12 NEW audit log entries (`tag_policy_updated` + `untagged_resource_detected` + `allocation_rule_evaluated` + `allocation_rule_updated` + `compliance_report_generated` + `compliance_alert_sent` + `compliance_remediation_initiated` + `reconciliation_initiated` + `reconciliation_report_generated` + `reconciliation_investigation_triggered` + `reconciliation_approved` + `reconciliation_resolved` = 12 total wait 10 NEW = 7 d + 5 e EXTENSION + 1 dry_run tag_governance_dry_run_executed) 결정 wire + emit_audit_typed BEFORE/AFTER FinOps Tag Governance event CR 1-1 verbatim 적용
- **CR 4-3/4-4 lessons carry ✅ APPLIED**: tag policy + untagged detection + allocation baseline + golden_diff pattern verbatim 미러 + tenant-scoped result_hash 결정 wire + Epic 8 wire `e117e09` capability drift detector 정합 패턴 + Phase 13 wire `8b98030` forecast accuracy baseline result_hash 패턴 verbatim + Phase 14 wire `e904485` optimization accuracy baseline result_hash 패턴 verbatim
- **CR 1-1 ContextVar lesson ✅ APPLIED**: trace_id request-scoped ContextVar 바인딩 + 비동기 trace context 보존 CR 1-1 verbatim 적용 + FinOps Tag Governance event 의 trace_id propagation 결정 wire
- **CR 1-1 RSC boundary lesson ✅ APPLIED**: `apps/web/app/[locale]/(dashboard)/admin/finops/tag-governance/page.tsx` Client-only + tag governance dashboard server-only delegation 결정 wire + CR 1-1 verbatim 적용
- **CR 9-6 commit message discipline ✅ APPLIED**: `git commit -F <file>` 사용, PowerShell here-string 회피, D5 prevention 결정 wire
- **CR 11-3 honest-DEFER discipline ✅ APPLIED**: 122번째 epic 연속 정직 회복 결정 wire (D-1-1-DEFER-* + D-EPIC-16-REVIEW-DEFER-* + D-PHASE-4-DR-DEFER-* + D-EPIC-17-WIRE-DEFER-T2-T3-UI + D-RETENTION-1 + D-OBSERVABILITY-1 + D-PERFORMANCE-1 + D-CHAOS-1 + D-SLO-1 + D-FINOPS-1 + D-FINOPS-2 + D-FINOPS-3 + D-FINOPS-4 모두 ✅ ALL RESOLVED 보존 + D-FINOPS-5 honestly DEFER 보존 진입 결정 wire)
- **CR 11-4 D-001~D-005 + P-015 lessons carry ✅ APPLIED**: dry-run mode UI 진입 시 frontend territory 정합 sweep 결정 wire + ko-KR.json SSOT only + vitest RTL render discipline + owner-only RBAC + unknown state reject + ko-KR.json SSOT drift detector 결정 wire
- **CR 12-1 L4 industry-agnostic capability ✅ APPLIED**: FINOPS_TAG_GOVERNANCE industry-agnostic 4-industry grants ✅/✅/✅/✅ 결정 wire + capability matrix v1.41 EXTENSION 결정 wire
- **CR 12-5 D-14 typed exception envelope ✅ APPLIED**: 15 NEW typed exception classes (TagPolicyInvalidError(400) + TagPolicyScopeInvalidError(404) + TagPolicyHistoryUnavailableError(404) + UntaggedResourceDetectionError(500) + UntaggedThresholdBreachError(500) + UntaggedMetricUnavailableError(404) + AllocationRuleEvaluationError(500) + AllocationRuleScopeError(404) + AllocationRulePrecedenceError(422) + ComplianceReportGenerationError(500) + ComplianceAlertError(500) + ChargebackReconciliationError(500) + ReconciliationDeltaBreachError(500) + ReconciliationApprovalError(500) + TagGovernanceAccuracyDegradationError(500)) 결정 wire + apps/api/main.py EXTENSION 결정 wire
- **CR 12-5 D-PARITY-01 inversion ✅ APPLIED**: Python FastAPI backend tag_policy_dsl.py + untagged_resource_detector.py + allocation_rules_engine.py + allocation_audit.py + chargeback_allocation_reconciliation.py TypedDict ↔ TypeScript Next.js frontend finops-tag-governance-client.ts interface parity 결정 wire + vitest CR 12-5 D-PARITY-01 검증 결정 wire
- **CR 12-5 D-GATE-01 inversion ✅ APPLIED**: FINOPS_TAG_GOVERNANCE capability gate per-tenant on/off + owner-only RBAC AD-22 결정 wire + gate 적용 대상 명시 결정 wire
- **A19 cohesion pattern 9 surface EXTENSION PASS ✅**: FinOps Tag Governance & Cost Allocation surface NEW = F31.1~F31.8 FinOps Tag Governance & Cost Allocation territory 결정 wire + spec surface EXTENSION + test surface EXTENSION + docs surface EXTENSION 결정 wire
- **A36 SDR 검증 4-step 자동 적용 ✅**: commit prefix lint PASS + sprint-status structure PASS + vitest file count drift 0건 + commit consistency PASS 결정 wire
- **AD-14 stack pin ✅ APPLIED**: Recharts 2.12.7 + slack-sdk==3.23.0 + pdpyras==5.2.0 (Phase 14 wire statsmodels==0.14.1 + prophet==1.1.5 + tensorflow==2.15.0 + slack-sdk==3.23.0 + pdpyras==5.2.0 + sendgrid==6.11.0 + Recharts 2.12.7 EXTENSION 결정 wire + Phase 13 wire `8b98030` forecast_model_registry EXTENSION 결정 wire + Phase 12 wire `f3c0e63` sklearn==1.4.0 + slack-sdk==3.23.0 + pdpyras==5.2.0 + sendgrid==6.11.0 + Recharts 2.12.7 EXTENSION 결정 wire)
- **AD-22 owner-only RBAC ✅ APPLIED**: tag policy update + untagged resource detection + allocation rule evaluation + compliance report generation + compliance alert routing + compliance remediation workflow + reconciliation approval 모두 owner-only RBAC AD-22 + Epic 12 2FA 챌린지 보존 결정 wire
- **NFR4 PII minimization ✅ PRESERVED**: tag governance data 는 사업 metric + cost amount + tag value 만 포함, PII 미포함 결정 wire

## Architecture Alignment (cj-style ALLOWED sweep — Phase 14 wire 정합)

**ALLOWED_SERVICE_SUBMODULES sweep CR 11-3 D-2 verbatim** (Phase 5 wire `f093f8c` + Phase 7 wire `59b56cd` + Phase 8 wire `60d4ea1` + Phase 9 wire `e7670e1` + Phase 10 wire `ac5d6c5` + Phase 11 wire `e020ad0` + Phase 12 wire `f3c0e63` + Phase 13 wire `8b98030` + Phase 14 wire `e904485` 정합):

### Backend (FastAPI, Python 3.12)
- ✅ `apps/api/modules/finops/` (MODIFIED EXTENSION): `tag_policy_dsl.py` + `untagged_resource_detector.py` + `allocation_rules_engine.py` + `allocation_audit.py` + `chargeback_allocation_reconciliation.py` + `tag_governance/__init__.py` NEW + `tag_governance/serializers.py` NEW + `__init__.py` EXTENSION + `serializers.py` EXTENSION
- ✅ `apps/api/core/capability.py` (MODIFIED): Capability.FINOPS_TAG_GOVERNANCE enum EXTENSION + 4 INDUSTRY_CAPABILITIES EXTENSION
- ✅ `apps/api/dependencies/capability.py` (MODIFIED): require_finops_tag_governance EXTENSION
- ✅ `apps/api/core/audit_action.py` (MODIFIED): ActionClass.FINOPS_TAG_GOVERNANCE + FinopsTagGovernanceAction Literal 10 NEW + _ActionRegistry FINOPS_TAG_GOVERNANCE entry 1 신규 등록 + __all__ EXTENSION
- ✅ `apps/api/core/errors.py` (MODIFIED): 15 NEW typed exception classes CR 12-5 D-14 verbatim
- ✅ `apps/api/alembic/versions/0047_phase_15_tag_governance.py` (NEW): 6 tables + 5 preview tables + indexes + RLS policies
- ✅ `apps/api/jobs/compliance_report.py` (NEW): cron KST 1st day of month 04:00 UTC 19:00 compliance report delivery
- ✅ `apps/api/jobs/chargeback_reconciliation.py` (NEW): cron KST 1st day of month 05:00 UTC 20:00 reconciliation
- ✅ `apps/api/main.py` (MODIFIED): /admin/finops/tag-governance/* + /admin/finops/allocation/* endpoints EXTENSION (CR 1-1 RSC boundary 적용)

### Frontend (Next.js 15.x, TypeScript 5.x)
- ✅ `apps/web/app/[locale]/(dashboard)/admin/finops/tag-governance/page.tsx` (NEW): RSC + tag governance dashboard
- ✅ `apps/web/app/[locale]/(dashboard)/admin/finops/tag-governance/layout.tsx` (NEW): RTL section wrapper
- ✅ `apps/web/app/[locale]/(dashboard)/admin/finops/allocation/page.tsx` (NEW): RSC + reconciliation dashboard
- ✅ `apps/web/app/[locale]/(dashboard)/admin/finops/allocation/layout.tsx` (NEW): RTL section wrapper
- ✅ `apps/web/components/finops/FinopsTagGovernanceDashboardPanel.tsx` (NEW): 5 components (TagPolicyEditor + UntaggedResourceDetectorPanel + AllocationRuleBuilder + ComplianceReportPanel + ReconciliationReportPanel)
- ✅ `apps/web/components/finops/FinopsAllocationReconciliationDashboardPanel.tsx` (NEW): 4 components (ReconciliationReportPanel + VarianceBreakdownChart + UnmatchedAllocationTable + ReconciliationApprovalButton)
- ✅ `apps/web/lib/finops-tag-governance/finops-tag-governance-client.ts` (NEW): TagPolicy + UntaggedResource + AllocationRule + AllocatedCost + ComplianceReport + ReconciliationReport TypedDict CR 12-5 D-PARITY-01 verbatim + 6 fetch wrappers + FinopsTagGovernanceApiError class
- ✅ `apps/web/messages/ko-KR.json` (MODIFIED): EXTENSION `finops_tag_governance.*` namespace ~30 keys + `finops_allocation_reconciliation.*` namespace ~15 keys 결정 wire

### Tests
- ✅ `tests/api/core/test_phase_15_tag_policy_dsl.py` (NEW): ~6 NEW pytest
- ✅ `tests/api/core/test_phase_15_untagged_resource_detector.py` (NEW): ~8 NEW pytest
- ✅ `tests/api/core/test_phase_15_allocation_rules_engine.py` (NEW): ~8 NEW pytest
- ✅ `tests/api/core/test_phase_15_allocation_audit.py` (NEW): ~6 NEW pytest
- ✅ `tests/api/core/test_phase_15_chargeback_allocation_reconciliation.py` (NEW): ~7 NEW pytest
- ✅ `tests/api/core/test_phase_15_audit_action.py` (NEW): ~8 NEW pytest
- ✅ `tests/integration/test_finops_tag_governance_tenant_isolation.py` (NEW): multi-tenant isolation CR 0-2 verbatim
- ✅ `tests/integration/test_capability_matrix_v1_41_drift.py` (NEW): 8 NEW pytest cases
- ✅ `apps/web/__tests__/finops-tag-governance/finops-tag-governance-dashboard.test.tsx` (NEW): ~5 NEW vitest
- ✅ `apps/web/__tests__/i18n/finops-tag-governance-i18n-ssot.test.ts` (NEW): SSOT drift NFR18 ko-KR 정합
- ✅ `tests/web/test_finops_tag_governance_dashboard_parity.py` (NEW): ~10 cases CR 12-5 D-PARITY-01 verification

### Docs
- ✅ `docs/finops-tag-governance-cost-allocation.md` (NEW): ~+200 LOC 14 sections runbook 결정 wire
- ✅ `docs/capability-matrix.md` (MODIFIED): v1.40 → v1.41 EXTENSION

## Files Affected (estimate)

- **~21 NEW**: `apps/api/modules/finops/{tag_policy_dsl,untagged_resource_detector,allocation_rules_engine,allocation_audit,chargeback_allocation_reconciliation}.py` (5 files) + `apps/api/modules/finops/tag_governance/{__init__,serializers}.py` (2 files) + `apps/api/alembic/versions/0047_phase_15_tag_governance.py` + `apps/api/jobs/{compliance_report,chargeback_reconciliation}.py` (2 files) + `apps/web/app/[locale]/(dashboard)/admin/finops/tag-governance/{page,layout}.tsx` (2 files) + `apps/web/app/[locale]/(dashboard)/admin/finops/allocation/{page,layout}.tsx` (2 files) + `apps/web/components/finops/{FinopsTagGovernanceDashboardPanel,FinopsAllocationReconciliationDashboardPanel}.tsx` (2 files) + `apps/web/lib/finops-tag-governance/finops-tag-governance-client.ts` + tests (10 files) + `docs/finops-tag-governance-cost-allocation.md`
- **~12 MODIFIED**: `apps/api/core/capability.py` + `apps/api/dependencies/capability.py` + `apps/api/core/audit_action.py` + `apps/api/core/errors.py` + `apps/api/main.py` + `apps/api/modules/finops/__init__.py` + `apps/api/modules/finops/serializers.py` + `apps/web/messages/ko-KR.json` + `docs/capability-matrix.md` + `_bmad-output/implementation-artifacts/sprint-status.yaml` + `tests/integration/conftest.py` + `apps/api/alembic/versions/script.py.mako`
- **Total**: ~33 files atomic single sprint

## Test Coverage

- **~56 NEW pytest PASS 결정 wire**:
  - `tests/api/core/test_phase_15_tag_policy_dsl.py` (6 cases): TypedDict validation + 6 resource_type 옵션 + 4 enforcement_level 옵션 + tag_key validation + default_value 옵션 + 4 industries baseline + audit-first INSERT + owner-only RBAC + dry_run default
  - `tests/api/core/test_phase_15_untagged_resource_detector.py` (8 cases): 6 resource_types + tag inventory + remediation_status state machine + severity classification + audit-first INSERT + typed exception envelope
  - `tests/api/core/test_phase_15_allocation_rules_engine.py` (8 cases): 5 rule_types + rule precedence + applied_rule_type + match_score + percentage_split 100% 검증 + weighted + conditional AST parser + audit-first INSERT + typed exception envelope
  - `tests/api/core/test_phase_15_allocation_audit.py` (6 cases): compliance score + accuracy + coverage + trend analysis + report generation + alert routing + remediation workflow + audit-first INSERT + typed exception envelope
  - `tests/api/core/test_phase_15_chargeback_allocation_reconciliation.py` (7 cases): reconciliation 3 strategy + variance calculation + cross-period reconciliation + investigation workflow + approval workflow + audit-first INSERT + typed exception envelope
  - `tests/api/core/test_phase_15_audit_action.py` (8 cases): 10 NEW audit log entries + ActionClass.FINOPS_TAG_GOVERNANCE + emit_audit_typed CR 1-1
  - `tests/integration/test_finops_tag_governance_tenant_isolation.py` (5 cases): cross-tenant isolation + tag policy isolation + untagged resource isolation + allocation rule isolation + reconciliation isolation
  - `tests/integration/test_capability_matrix_v1_41_drift.py` (8 cases): FINOPS_TAG_GOVERNANCE enum + 4-industry grants + v1.40 + v1.39 + ... preservation
  - **Subtotal**: ~56 NEW pytest PASS

- **~8 NEW vitest PASS 결정 wire**:
  - `apps/web/__tests__/finops-tag-governance/finops-tag-governance-dashboard.test.tsx` (5 cases): TagPolicyEditor + UntaggedResourceDetectorPanel owner-only ack prompt AD-22 verbatim + AllocationRuleBuilder RTL render + ComplianceReportPanel + ReconciliationReportPanel
  - `apps/web/__tests__/i18n/finops-tag-governance-i18n-ssot.test.ts` (1 cases): ko-KR SSOT drift detection + CR 12-5 D-PARITY-01 verification
  - `tests/web/test_finops_tag_governance_dashboard_parity.py` (10 cases): TS mirror parity CR 12-5 D-PARITY-01 verification
  - **Subtotal**: ~16 NEW vitest PASS (5 dashboard + 1 i18n + 10 parity = 16)

- **0 NEW ruff 결정 wire** (apps/api backend 결정 wire + 기존 ruff scoped 0 NEW 정합 보존)
- **0 NEW tsc 결정 wire** (apps/web frontend 결정 wire + 기존 tsc 0 NEW 정합 보존)
- **0 regressions 결정 wire** (3중 게이트 FINAL CLEAN + ruff scoped 0 NEW + pytest 0 NEW failures + vitest 0 NEW failures + tsc 0 NEW errors)

## Notes

- `apps/api/main.py` EXTENSION 시 /admin/finops/tag-governance/* + /admin/finops/allocation/* endpoints EXTENSION + require_finops_tag_governance dep 적용
- `apps/api/core/errors.py` EXTENSION 시 15 NEW typed exception classes + envelope CR 11-4 P-015 적용
- `apps/api/core/audit_action.py` EXTENSION 시 ActionClass.FINOPS_TAG_GOVERNANCE + FinopsTagGovernanceAction Literal 12 NEW values (10 NEW = 7 d + 5 e EXTENSION + 1 dry_run tag_governance_dry_run_executed) + _ActionRegistry FINOPS_TAG_GOVERNANCE entry 1 신규 등록
- m23_finops_tag_governance.tag_governance_serializers NEW Phase 15 EXTENSION 결정 wire (Phase 14 wire `e904485` m22_finops_optimization.optimization_serializers EXTENSION pattern verbatim 미러, wire 시점에 sprint-status.yaml action_items EXTENSION)
- Phase 14 wire `e904485` 의 optimization_accuracy_tracker precision/recall/realized savings + rightsizing engine 5 resource types + idle resource detection z-score 기반 + RI/SP commitment recommender 1y/3y + optimization definition DSL 5 resource_type 의 natural backend COST ALLOCATION LAYER EXTENSION 결정 wire (tagged resource → accountable cost center)
- Phase 13 wire `8b98030` 의 forecast accuracy tracker MAE/MAPE/RMSE + capacity headroom analysis compute/storage/network 90일 lookahead + budget burn-rate projection + FinOps Forecasting & Capacity Planning territory 의 natural carry-over chain 결정 wire
- Phase 12 wire `f3c0e63` 의 anomaly detection baseline last 30d/90d/YTD → tag governance compliance period EXTENSION territory 자연스러운 EXTENSION 결정 wire
- Phase 11 wire `e020ad0` 의 showback period selector (current/previous/last 3/6 months/YTD) + chargeback department cost center mapping 의 자연스러운 carry-over chain 결정 wire (Phase 15 reconciliation 의 Phase 11 chargeback EXTENSION territory 결정 wire)
- Phase 8 wire `60d4ea1` 의 cost-engine V8 골든 fixture + 12-period benchmark 의 자연스러운 carry-over chain (historical baseline ⇒ forward forecast EXTENSION 12-month prediction with 95% CI + capacity headroom 90일 lookahead + budget burn-rate EXTENSION) 결정 wire
- Phase 10 wire `ac5d6c5` 의 4 SLIs 자연스러운 EXTENSION 결정 wire + Phase 9 wire `e7670e1` chaos_experiment baseline EXTENSION 결정 wire
- Phase 7 wire `59b56cd` observability 의 Prometheus custom metrics + Slack channel EXTENSION 결정 wire + alert routing 정합 (Phase 15 compliance alert routing EXTENSION territory 결정 wire)
- Epic 12 2FA 챌린지 mandatory 결정 wire (tag policy update + untagged resource detection + allocation rule evaluation + compliance report generation + compliance alert routing + compliance remediation workflow + reconciliation approval 모두 Epic 12 2FA 챌린지 mandatory)
- AD-22 owner-only RBAC 보존 결정 wire (tag policy update + untagged resource detection + allocation rule evaluation + compliance report generation + compliance alert routing + compliance remediation workflow + reconciliation approval 모두 owner-only)
- AD-14 stack pin 결정 wire (Recharts 2.12.7 + slack-sdk==3.23.0 + pdpyras==5.2.0 + sendgrid==6.11.0)
- NFR4 PII minimization PRESERVED (tag governance data 는 사업 metric + cost amount + tag value 만 포함, PII 미포함)
- 3중 게이트 impact NONE (cj-style 122번째 spec entry 진입 표준 = docs only 변경): ruff scoped 0 NEW + pytest 0 NEW + vitest 0 NEW + tsc 0 NEW
- 8 ACs PRD §F31.1~§F31.8 verbatim → 92 sub-ACs (12+12+12+12+12+10+12+12 = 92 sub-ACs) satisfied pre-flight 정합 sweep 결정 wire

## Cross-References

- Phase 15 PRD entry `87393b4` (cj-style 121번째) — FinOps Tag Governance & Cost Allocation territory 정합
- Phase 14 close-out retro `5b367d9` (cj-style 120번째) — D-FINOPS-5 honestly DEFER 보존 해소
- Phase 14 atomic wire T1~T8 `e904485` (cj-style 119번째) — FinOps Optimization & Rightsizing territory 정합
- Phase 14 spec entry `30637f6` (cj-style 118번째)
- Phase 14 PRD entry `0e3f8d9` (cj-style 117번째)
- Phase 13 close-out retro `850b4f8` (cj-style 116번째)
- Phase 13 atomic wire T1~T8 `8b98030` (cj-style 115번째) — FinOps Forecasting & Capacity Planning territory 정합
- Phase 13 spec entry `77ed55f` (cj-style 114번째)
- Phase 13 PRD entry `d31dfc8` (cj-style 113번째)
- Phase 12 close-out retro `3354e83` (cj-style 112번째)
- Phase 12 atomic wire T1~T8 `f3c0e63` (cj-style 111번째) — Cost Anomaly Detection & Budget Alerting territory 정합
- Phase 12 spec entry `8c5f374` (cj-style 110번째)
- Phase 12 PRD entry `344c7eb` (cj-style 109번째)
- Phase 11 close-out retro `80df15b` (cj-style 108번째)
- Phase 11 atomic wire T1~T8 `e020ad0` (cj-style 107번째) — FinOps Showback / Chargeback territory 정합
- Phase 11 spec entry `82c93a8` (cj-style 106번째)
- Phase 11 PRD entry `16d7698` (cj-style 105번째)
- Phase 10 close-out retro `733d428` (cj-style 104번째)
- Phase 10 wire `ac5d6c5` (cj-style 103번째) — SLO Engineering / Error Budget Management territory 정합
- Phase 9 wire `e7670e1` (cj-style 99번째) — Chaos Engineering / Game Day territory 정합
- Phase 8 wire `60d4ea1` (cj-style 95번째) — cost-engine V8 골든 fixture + 12-period benchmark EXTENSION
- Phase 7 wire `59b56cd` (cj-style 91번째) — observability 정합
- Phase 5 wire `f093f8c` (cj-style 75번째) — multi-region failover + replication_lag 정합
- Epic 12 2FA 게이트 `a63646c` — Epic 12 2FA 챌린지 mandatory
- Epic 1 carry-over (auth) — onboarding/industry 보존
- AD-14 stack pin — Recharts 2.12.7 + slack-sdk==3.23.0 + pdpyras==5.2.0 + sendgrid==6.11.0
- AD-22 owner-only RBAC — tag policy update + untagged resource detection + allocation rule evaluation + compliance report generation + compliance alert routing + compliance remediation workflow + reconciliation approval
- AD-42 FinOps Tag Governance & Cost Allocation 신규 (a)~(g) 7 sub-decisions
- NFR18 ko-KR — SSOT only invariant
- NFR4 PII minimization — tag governance data PII 미포함
- CR 0-2 RLS lesson, CR 1-1 audit-first INSERT, CR 4-3/4-4 lessons carry, CR 1-1 ContextVar, CR 1-1 RSC boundary, CR 9-6 commit message, CR 11-3 honest-DEFER, CR 11-4 D-001~D-005 + P-015, CR 12-1 L4 industry-agnostic capability, CR 12-5 D-14 envelope, CR 12-5 D-PARITY-01, CR 12-5 D-GATE-01, A19 cohesion 9 surface EXTENSION PASS, A36 SDR 검증 4-step 자동 적용
- m23_finops_tag_governance.tag_governance_serializers NEW Phase 15 EXTENSION 결정 wire (wire 시점에)

## 결정 wire 일자

2026-08-25 (KST)

## next (wire 진입 시)

옵션 (a) Phase 15 bmad-dev-story atomic wire T1~T8 진입 (cj-style 123번째 wire 진입 시점) 결정 wire 진입 / 옵션 (b) Phase 15 close-out retro 진입 (cj-style 124번째) / 옵션 (c) Phase 16+ 진입 / 옵션 (d) Epic 18+ 진입 / 옵션 (e) D-DEFER-* follow-up 진입 결정 wire 보류.