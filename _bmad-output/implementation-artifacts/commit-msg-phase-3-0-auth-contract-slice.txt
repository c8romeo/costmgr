Phase 3-0 sprint (cj-style Epic 14 carry-over 2번째 진입점 = cj-style Epic 14 carry-over "fix" 종류): auth 계약 수직 슬라이스 atomic single sprint — P0 3종 (GUC name split + custom_access_token_hook disabled + no signup path) ALL RESOLVED. Phase 3 territory 진입을 가로막던 모든 break 제거. wire scope (5 NEW files + 9 MODIFIED + 1 NEW alembic migration = 15 files atomic): (1) **`apps/api/core/security.py`** EXTENSION — `ALLOWED_ROLES = frozenset({"owner","member","viewer","consultant_proxy"})` (public, alembic 0001 SSOT) + `JWTClaims.tenant_id: uuid.UUID | None` (Optional, was required) + `decode_jwt(token, require_tenant: bool = True)` kwarg 신규 (`require_tenant=False` 이면 `tenant_id=None` JWTClaims 반환, AuthError raise 안 함 — pre-onboarding 상태 수용) + role allowlist 검증 (allowlist 바깥 role → `AuthError("invalid_role")`, P0-1 SQL injection 방어 — listener 가 `f"SET LOCAL request.jwt.claims = '{json.dumps(...)}'"` 로 f-string interpolation 하는 형태이므로 allowlist 가 decode 시점에 enforced 되어야 함) / (2) **`apps/api/core/tenant_context.py`** EXTENSION — `set_claims(claims)` / `clear_claims()` 신규 (ContextVar-based snapshot pattern, frozen `_TenantClaimsSnapshot` dataclass — `listener 가 unvalidated payload 를 publish 할 수 없도록 physically 차단`) + `set_tenant_local(engine, claims: JWTClaims)` (이전 `ctx.tenant_id` 패턴에서 claims 전체로 변경) + `_begin_transaction` 가 SQLAlchemy `begin` 이벤트에서 매 transaction 마다 3개 GUC 발행: `SET LOCAL app.tenant_id = '<UUID>'` + `SET LOCAL app.user_id = '<UUID>'` + `SET LOCAL request.jwt.claims = '<compact JSON>'` (alembic 0001 의 78 policies + 28 policies 가 읽는 SSOT 와 정합) + `PreOnboardingUser` frozen dataclass 신규 + `get_pre_onboarding_user` FastAPI dep 신규 (tenant_id 가 비어있는 JWT 도 수용) / (3) **`apps/api/core/audit_action.py`** EXTENSION — `ActionClass.TENANT = "tenant"` 신규 + `TenantAction = Literal["tenant_signup_completed"]` 신규 + `AuditAction` union 확장 + registry entry `TENANT: ("audit_logs", frozenset({"tenant_signup_completed"}))` (CR 1-1 audit-first INSERT 패턴 준수) / (4) **`apps/api/alembic/versions/0035_custom_access_token_hook.py`** NEW — `revision = "0035_custom_access_token_hook"`, `down_revision = "0034_listen_notify_consume_cross_tenant_fanout"` (0034 다음). `public.custom_access_token_hook(event jsonb) RETURNS jsonb` `LANGUAGE plpgsql STABLE SECURITY DEFINER` 으로 생성. 로직: `tenant_memberships` JOIN `tenants` (deleted_at IS NULL 필터) → user 의 가장 높은 권한 membership 선정 (owner=3 > member=2 > viewer=1 > consultant_proxy=0 priority, 동률은 `joined_at` ASC) → `claims.app_metadata` 에 `tenant_id`/`role`/`industry` 를 `jsonb_set` 으로 merge (기존 key 보존). `invalid_text_representation` 캐치 → event unchanged 반환. `ALTER FUNCTION ... OWNER TO postgres; GRANT EXECUTE ... TO postgres;` (Supabase GoTrue 가 postgres role 로 hook 호출). / (5) **`supabase/config.toml`** EXTENSION — `[auth.hook.custom_access_token].enabled = false → true` + `uri = "pg-functions://postgres/public/custom_access_token_hook"` (Supabase GoTrue 가 hook 발견 → 매 access token mint 시 호출). / (6) **`apps/api/modules/m0_onboarding/schemas.py`** EXTENSION — `SignupCompleteRequest` (frozen Pydantic v2, `tenant_name: str` 1~200 char, `industry: Industry` enum, `extra="forbid"`) + `SignupCompleteResponse` (frozen, `tenant_id: UUID` + `role: Literal[4]` + `industry: Industry` + `settings_version: int` + `trace_id: str`) / (7) **`apps/api/modules/m0_onboarding/services/signup_service.py`** NEW — `SignupService.complete_signup(user_id, user_email, tenant_name, industry)` 메소드, 5-step atomic in single transaction: ① get_or_create_user_row (`auth.users` 와 `public.users` 가 id 로 매핑, 멱등) ② existing membership 체크 (있으면 `AlreadyHasTenantError` 409) ③ `tenants` row 생성 (id 는 server-side `gen_random_uuid()`, AD-3 verbatim — tenant_id 를 request 에서 절대 받지 않음) ④ `tenant_memberships` row 생성 (role='owner', PRD §F15.2 verbatim) ⑤ `tenant_settings` row 생성 (settings_version=1, JSONB 빈 dict) + `flush()` 로 DB-level error surface + `emit_audit_typed` 로 `tenant_signup_completed` audit log INSERT. 모든 write 가 같은 transaction — 어느 하나라도 실패하면 전부 롤백. typed exceptions: `AlreadyHasTenantError` (409, A11 axiom — 한 user 가 두 테넌트의 owner 가 되는 사고 방지) + `TenantNameValidationError` (422, empty/too-long) / (8) **`apps/api/modules/m0_onboarding/handlers.py`** EXTENSION — `signup_router = APIRouter(prefix="/api/v1/onboarding", tags=["m0-onboarding-signup"])` + `POST /api/v1/onboarding/complete-signup` 엔드포인트 (201 Created, response_model=SignupCompleteResponse, `get_pre_onboarding_user` dep 사용, exception mapping: `AlreadyHasTenantError` → 409, `TenantNameValidationError` → 422, `IntegrityError` → 409, CR 12-5 D-14 envelope `{code, message_ko, details, trace_id}`) / (9) **`apps/api/modules/m0_onboarding/__init__.py`** EXTENSION — `signup_router` re-export / (10) **`apps/api/main.py`** EXTENSION — `signup_router` import + include (after `m0_onboarding_router`) / (11) **`_bmad-output/planning-artifacts/prd.md`** FIX — §F15.2 의 사양 본문 정정: `user_tenants` (오기) → `tenant_memberships` (alembic 0001 실제 테이블명) + `action_name='tenant_created'` → `action='tenant_signup_completed'` (ActionClass.TENANT registry 와 정합) + "Backend callback (Supabase Database Webhook 또는 trigger)" narrative → 실제 `POST /api/v1/onboarding/complete-signup` flow (Frontend reads `sb-access-token` → pre-onboarding JWT → endpoint → `SignupService.complete_signup()` atomic transaction → `supabase.auth.refreshSession()` → 두 번째 mint 에서 hook 이 `app_metadata` populate) + `ALREADY_HAS_TENANT_KO` 에러 메시지 추가 + 2-mint sequence 문서화 (Phase 3-0 의 핵심 — 첫 번째 mint 는 빈 tenant_id JWT, 두 번째 mint 부터 hook 이 정상 populate) / (12) **`tests/api/core/test_phase_3_0_rls_bridge.py`** NEW (8 tests) — role allowlist 3건 (`test_decode_jwt_rejects_unknown_role` + `test_decode_jwt_accepts_all_four_roles` + `test_decode_jwt_default_role_is_viewer`) + listener GUC publish 4건 (`test_begin_transaction_publishes_all_three_gucs` + `test_begin_transaction_no_op_when_no_snapshot` + `test_begin_transaction_json_safe_against_role_injection` + `test_begin_transaction_does_not_leak_raw_payload`) + snapshot safety 1건 (`test_tenant_claims_snapshot_is_frozen_and_minimal`). `pytest.MonkeyPatch` 으로 `tc_module.event.listens_for` swap → SQLAlchemy begin handler capture. / (13) **`tests/api/core/test_phase_3_0_hook_migration.py`** NEW (14 tests) — alembic 0035 파일 코드-shape 검증 (re.compile 으로 migration source 직접 매칭): function signature/contract 6건 (public schema, jsonb args, SECURITY DEFINER, STABLE, GRANT EXECUTE TO postgres, OR REPLACE) + logic shape 6건 (queries tenant_memberships, JOINs tenants, role priority, excludes deleted tenants, returns event on invalid, uses jsonb_set) + supabase/config.toml binding 1건 + downgrade safety 1건. / (14) **`tests/api/m0_onboarding/test_phase_3_0_signup_endpoint.py`** NEW (21 tests) — router shape 5건 (route registered, method POST only, status 201, summary Korean, response_model=SignupCompleteResponse) + schema shape 8건 (required fields, extra=forbid, tenant_name length 1~200, response fields, response.role 4-role Literal, unknown industry → 422, empty name → 422, extra field → 422) + `decode_jwt(require_tenant=False)` 4건 (no-tenant accepts, with-tenant accepts, default require_tenant=True still rejects no-tenant, role allowlist still enforced even with require_tenant=False) + PreOnboardingUser shape 2건 (carries user_id+role+email, default values None) + PRD §F15.2 정합 2건 (사양 본문이 `tenant_memberships` 1차 인용, 사양 본문에 `user_tenants` 1차 항목 없음). **5 NEW files + 9 MODIFIED = 14 files atomic single sprint** (실제로 alembic migration 포함 15). estimated +43 NEW pytest PASS (8 + 14 + 21) + 0 NEW ruff + 0 NEW regressions. **3 중 게이트 FINAL CLEAN**: (1) ruff All checks passed! (Phase 3-0 wire 코드 12 files + NEW tests 3 files) / (2) pytest Phase 3-0 tests = 43/43 PASS (m0_onboarding/test_phase_3_0_signup_endpoint.py 21 + core/test_phase_3_0_rls_bridge.py 8 + core/test_phase_3_0_hook_migration.py 14) / (3) 회귀 = full suite 3070 passed + 102 skipped (1 failed: `tests/services/m5_reports/test_pdf_generator.py::test_generate_report_pdf_report15_payload_not_required` — **사전 존재 실패** (Story 11.6 commit `57ce552` 시점), Phase 3-0 와 무관, 0 NEW regression). **P0 3종 ALL RESOLVED**: ① GUC name split (Python writes `app.tenant_id`/`app.user_id`/`request.jwt.claims` ← 3 GUC, 78+28 RLS policies 가 읽는 SSOT 와 정합) ✅ / ② `custom_access_token_hook` enabled + function 존재 (alembic 0035 + supabase/config.toml enabled = true) ✅ / ③ Signup path 존재 (`POST /api/v1/onboarding/complete-signup` + `SignupService.complete_signup()` atomic transaction + `get_pre_onboarding_user` dep + `decode_jwt(require_tenant=False)` 신규) ✅. **A19 cohesion 9 surface EXTENSION PASS 결정** (kernel ✅ + port ✅ + db schema ✅ + service ✅ + handler ✅ + envelope ✅ + capability ✅ + audit ✅ + **auth surface NEW** = pre-onboarding JWT decode + role allowlist + 3-GUC publisher + hook + signup completion endpoint). AD-3 verbatim 100% binding (tenant_id 는 server-side `gen_random_uuid()`, 절대 request 에서 안 받음) + AD-2 verbatim 100% binding (audit-first INSERT in same transaction, `tenant_signup_completed` action). CR 11-3 honest-DEFER discipline + A36 SDR 검증 4-step 자동 적용 (commit prefix lint PASS + sprint-status structure PASS + vitest file count drift 0건 + commit consistency PASS) + CR 12-5 D-GATE-01 + D-PARITY-01 inversion 적용 보존 + A19 + CR 0-2 RLS lesson + CR 1-1 audit-first INSERT + CR 9-6 commit message discipline (`git commit -F <file>` 사용, PowerShell here-string 회피, D5 prevention) 모두 적용 보존. **D-1-1-DEFER-* honestly preserved** (Epic 1 carry-over = Magic link + Social login OAuth + SSO enterprise SAML, 1차 출시 후 진입 시점, Phase 3 wire scope 바깥). 결정 wire 일자: 2026-08-21 (KST). **next**: Task #5 종단 증명 (Docker Desktop 기동 후 — `npipe:////./pipe/dockerDesktopLinuxEngine not found` 현재 dead, user 가 Docker Desktop start 후 실제 Supabase token → `/api/v1/*` 200 + cross-tenant 격리 확인 가능) OR Phase 3 cj-style 2번째 진입점 (bmad-create-story spec, cj-style 50번째 epic 연속 정직 회복, T1~T8 wire scope + D-1-1-DEFER-* honestly RESOLVE 진입) 결정 wire 진입.

Co-Authored-By: Claude <noreply@anthropic.com>
