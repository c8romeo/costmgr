Story 14.1 sprint (cj-style Epic 14 2번째 진입점 = cj-style 46번째 epic 연속 정직 회복 atomic single sprint T1~T9 wire): LISTEN/NOTIFY Consume Cross-Tenant Fan-Out + Multi-Process Coordination DONE — D-13-1-DEFER-3 ✅ RESOLVED wire 진입. wire scope (PRD §F14 verbatim T1~T9 ALL PASS): T1 alembic 0034 NEW (cross_tenant_fanout NOTIFY trigger EXTENSION on cache_invalidation_log AFTER INSERT, 7-key alphabetical JSON payload via json_object(), channel filter = 'cross_tenant_fanout' ONLY, down_revision='0033_listen_notify_consume_trigger', ADD COLUMN invalidation_id/target_tenant_ids, partial index for cross_tenant_fanout channel) / T2 cache_invalidation_listener.py EXTENSION (~+200 LOC, 5+ channels ALLOWED_CHANNELS EXTENSION + EXPECTED_PAYLOAD_KEYS_CROSS_TENANT frozenset + multi-process coordination Option 1 PostgreSQL LISTEN/NOTIFY only via pg_notify fan-out leader/follower model = LISTEN_FANOUT_LOCK_ID = 0x4C49_5354_4641_4E54 'LISTFANT' ASCII hex + LEADER_HEALTH_CHECK_INTERVAL_SECONDS = 30.0 + LEADER_TAKEOVER_TIMEOUT_SECONDS = 90.0 + 2 NEW typed exceptions LeaderElectionFailedError/LeaderTakeoverFailedError CR 12-5 D-14 envelope + Korean SSOT LEADER_ELECTION_FAILED_KO/LEADER_TAKEOVER_FAILED_KO + LeaderElectionState frozen dataclass + CacheInvalidationPayload dataclass EXTENSION 7-key cross_tenant_fanout variant + _attempt_leader_election NEW + _leader_election_loop NEW + _attempt_takeover NEW + graceful degradation single-pod) / T3 main.py lifespan EXTENSION (parallel on_event start/stop + 2 NEW exception handlers LeaderElectionFailedError 503 LEADER_ELECTION_FAILED + LeaderTakeoverFailedError 503 LEADER_TAKEOVER_FAILED + CR 12-5 D-14 envelope {code, message_ko, details, trace_id} + graceful degradation single-pod startup) / T4 cache_invalidation_listener_adapters.py EXTENSION (~+80 LOC, CrossTenantFanoutAdapter NEW = multi-tenant isolation CR 0-2 RLS lesson + capability gate integration LISTEN_NOTIFY_TENANT_FANOUT + audit-first INSERT 3-row CR 1.1 verbatim + cross-channel contamination 방어 EXTENSION + MultiProcessDispatchAdapter NEW logging stub + _make_adapter EXTENSION cross_tenant_fanout dispatch) / T5 capability.py EXTENSION (2 NEW enum values LISTEN_NOTIFY_TENANT_FANOUT + LISTEN_NOTIFY_MULTIPROCESS, industry-agnostic 4-industry grants ✅/✅/✅/✅, capability matrix v1.22 → v1.23 SSOT RED→GREEN EXTENSION, drift detector test_capability_matrix_v1_23_drift.py NEW) / T6 V8 determinism EXTENSION (~+9 cases, 7-key alphabetical ordering + target_tenant_ids array 결정적 직렬화 + byte-identical across reruns + 5+ channels routing payload shape 결정적, F13.3/F14.3 verbatim) / T7 cross-language drift detector EXTENSION (TS mirror apps/web/lib/cache-invalidation-listener.ts ~+200 LOC = 5+ channels ALLOWED_CHANNELS EXTENSION + PAYLOAD_KEYS_CROSS_TENANT 7-key + CacheInvalidationPayload Discriminated union EXTENSION cross_tenant_fanout variant + serializePayloadForV8 byte-identical EXTENSION + parseCacheInvalidationPayload EXTENSION channel-based key set selection 5 vs 7 + CROSS_TENANT_DRIFT_DETECTED_REJECT_KO constant NEW '크로스 테넌트 LISTEN/NOTIFY 페이로드 형식이 백엔드와 일치하지 않습니다' + MultiTenantIsolationState TS interface NEW + LeaderElectionState TS interface NEW + tests/web/test_cache_invalidation_listener_parity.py EXTENSION ~+12 cases 5+ channels parity + 7-key cross_tenant parity + UUID array byte-identical parity + NEW TS interfaces parity + CROSS_TENANT_DRIFT_DETECTED_REJECT_KO parity) / T8 multi-process coordination tests + cross-tenant fan-out e2e (~+28 cases across 2 NEW test files = tests/api/test_cache_invalidation_multiprocess.py NEW ~+18 cases leader election state invariants + lock ID constants ASCII 'LISTFANT' + 2 NEW exception types + method signatures + graceful degradation + state transitions + tests/integration/test_cross_tenant_fanout_e2e.py NEW ~+10 cases cross_tenant payload handling + capability gate integration + audit-first pattern + cross-channel contamination EXTENSION + Korean reject messages + V8 determinism roundtrip) / T9 3중 게이트 FINAL CLEAN (core wire 코드 ruff check All checks passed! + pytest cache_invalidation 관련 240 PASS + vitest 650 tests PASS frontend 66 files + sprint-status 14-1: ready-for-dev → in-progress → review + handoff memory 신규 wire + MEMORY.md index 신규 wire). 14 NEW files (alembic 0034 + 9 NEW tests files + handoff memory + MEMORY.md index + commit-msg file) + 8 MODIFIED (cache_invalidation_listener.py + cache_invalidation_listener_adapters.py + main.py + capability.py + TS mirror apps/web/lib/cache-invalidation-listener.ts + tests/api/core/test_cache_invalidation_listener.py 5+ channels 정합 업데이트 + tests/web/test_cache_invalidation_listener_parity.py EXTENSION + sprint-status.yaml 14-1 review status) + 1 NEW TS mirror EXTENSION. ~140 NEW pytest PASS + 0 NEW ruff (core wire 코드 All checks passed!) + 0 regressions + 240 cache_invalidation 관련 pytest PASS + 650 vitest tests PASS frontend. A19 cohesion pattern 8 surface EXTENSION PASS 결정 (kernel + port + db schema + service + handler + envelope + capability + audit). AD-25 verbatim 100% binding EXTENSION (4-channel → 5+ channels cross_tenant_fanout channel 추가). CR 11-3 honest-DEFER discipline + A36 SDR 검증 4-step 자동 적용 (commit prefix lint PASS + sprint-status structure PASS + vitest file count drift 0건 + commit consistency PASS) + CR 12-5 D-GATE-01 + D-PARITY-01 inversion 적용 보존 + A19 + CR 0-2 RLS lesson + CR 1-1 audit-first INSERT + CR 9-6 commit message discipline 모두 적용 보존. Multi-process coordination Option 1 결정 wire 진입 (PostgreSQL LISTEN/NOTIFY only via pg_notify fan-out leader/follower model. Option 2 Redis pub/sub rejected rationale: G2 인프라 최소화 정합). supersedes prior 14-1 spec entry commit-msg tip.

Co-Authored-By: Claude <noreply@anthropic.com>
