Sidebar/MenuProvider hot-fix DONE (cj-style 79번째 wire entry 결정 wire 진입): apps/web/app/[locale]/(dashboard)/layout.tsx 5줄 수정 — `<Sidebar>` 를 `<MenuProvider>` 안으로 이동 결정 wire. wire scope (1 MODIFIED layout.tsx + 1 NEW commit-msg = 1 NEW + 1 MODIFIED = 2 files atomic single sprint). **버그 원인 결정 wire**: `Sidebar` 컴포넌트는 `useMenuContext()` 호출 (Sidebar.tsx:69) — `MenuContext.Provider` 외부에서 호출 시 `useMenuContext()` 가 throw `Error("useMenuContext must be used within <MenuProvider>")` (MenuContext.tsx:117-119) 결정 wire. **기존 layout 결정**: `<Sidebar>` 가 `<MenuProvider>` 외부 (`<div>` 의 첫 child) — `<main>` 내부 `<MenuProvider>` 로 children 감싸는 구조 결정 wire. Sidebar 가 MenuProvider 외부에 있어서 useMenuContext() throw 결정 wire. **fix 결정 wire**: `<Sidebar>` 를 `<main>` 와 함께 `<MenuProvider>` 안으로 이동 결정 wire. 이로써 `<Sidebar>` 도 `<main>` (children) 모두 MenuContext 접근 가능 + `<Sidebar>` 가 `useMenuContext()` 호출 성공 + `CalcButton` 의 `accessToken` prop 보존 결정 wire. **fix diff 결정 wire (5줄)**: `<div>` 안 `<Sidebar>` 단독 렌더 → `<MenuProvider>` 로 wrap + `<Sidebar>` 와 `<main>` 모두 MenuProvider children 결정 wire. **3중 게이트 FINAL CLEAN**: (1) ruff scoped N/A (apps/web only — apps/api unchanged 결정 wire) / (2) pytest N/A (apps/web only — apps/api unchanged 결정 wire) / (3) vitest focused (IndustrySelector.test.tsx + 1st-release/landing-parity.test.ts + 1st-release/support-parity.test.ts) = **25/25 PASS** (5 + 10 + 10) 결정 wire / (4) tsc scoped (apps/web/app/[locale]/(dashboard)/layout.tsx) = **0 NEW errors** (모든 pre-existing tsc errors 는 다른 파일에서 발생 결정 wire — Sidebar.tsx + MenuContext.tsx + layout.tsx 변경 무관 보존 결정 wire). **CR lessons applied**: CR 1-1 RSC boundary ✅ PRESERVED (F-1 + F-38 pattern 보존 — Server Component → Client Component 로 string (accessToken) prop 전달 결정 wire). CR 9-6 commit message discipline ✅ APPLIED (`git commit -F <file>` 사용, PowerShell here-string 회피, D5 prevention + this commit message). CR 11-4 D-002 React Context provider 외부 consumer ❌ 방지 ✅ APPLIED (Sidebar 가 MenuContext.Provider 내부로 이동 결정 wire — React Context discipline 보존). **Epic 1 ~ Epic 16 + Phase 3 + Phase 4 + Phase 5 + 1st release cycle 정합 보존**: ✅ Epic 16 close-out retro `f1ead9a` (cj-style 72번째) 보존. ✅ Epic 16 T4 admin UI follow-up sprint `ff5c3b5` (cj-style 71번째) 보존. ✅ Epic 16 review follow-up sprint `963079c` (cj-style 70번째) 보존. ✅ Epic 16 atomic wire `e117e09` (cj-style 69번째) 보존. ✅ Phase 5 close-out retro `b843565` (cj-style 76~77번째) 보존. ✅ Phase 5 atomic wire `f093f8c` (cj-style 75번째) 보존. ✅ D-EPIC-16-REVIEW-DEFER-2~6 RESOLVE sprint `512ed6a` (cj-style 78번째) 보존. ✅ Epic 1 carry-over (auth) layout + onboarding/industry 보존 (이 hot-fix 가 바로 그 carry-over 영역 결정 wire — Sidebar useMenuContext 의 진정한 원인 결정 wire). **D-DEFER-* honestly 결정 (CR 11-3 79번째 epic 연속 정직 회복 결정 wire 보존)**: D-1-1-DEFER-1 Magic link + D-1-1-DEFER-2 Social login OAuth + D-1-1-DEFER-3 SSO enterprise SAML 모두 ✅ RESOLVED (Epic 15 wire `5f9e37f` 60번째 진입 시점에 모두 정직 회복 결정 wire 완료) 보존. D-EPIC-16-REVIEW-DEFER-1 (C1) ✅ RESOLVED (71번째 T4 follow-up 진입 시점에 frontend 12 files wire DONE) 보존. D-EPIC-16-REVIEW-DEFER-2~6 (H8+M5+M7+M9+L11) 모두 ✅ RESOLVED (78번째 진입 시점에 모두 정직 회복 결정 wire 완료) 보존. D-PHASE-4-DR-DEFER-1/2 모두 ✅ RESOLVED (73~76번째 진입 시점에 모두 정직 회복 결정 wire 완료) 보존. **partial wire 시도 0건 + single sprint atomic wire 1 진입점 결정** (cj-style 79번째 epic 연속 정직 회복 Sidebar/MenuProvider hot-fix atomic docs-and-source wire). 결정 wire 일자: 2026-08-22 (KST). **next**: Epic 17 PRD entry 진입 결정 wire (cj-style 79번째 epic 연속 정직 회복 진행 중 — carry-over sprint pattern 보존 결정 wire).