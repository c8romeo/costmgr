---
baseline_commit: 5eded22
status: ready-for-dev
cj_style_entry_point: 134
story_key: phase-18-finops-cloud-commitment-management-wire
---

# Phase 18 FinOps Cloud Commitment Management (RIs/SPs/CUDs) wire spec (cj-style 134번째 epic 연속 정직 회복)

## Story

**As a** FinOps practitioner / cloud architect / capacity planner / cost optimization lead / tenant admin / enterprise onboarding lead / executive leadership (CEO + CFO + CTO) / board observer / external auditor / commitment buyer
**I want** FinOps Cloud Commitment Management (RIs/SPs/CUDs) territory 결정 wire (commitment_inventory_aggregator `aggregate_commitment_inventory` + 7 modules cross-rollup (Phase 11 showback + Phase 12 anomaly + Phase 13 forecast + Phase 14 optimization + Phase 15 tag_governance + Phase 16 executive + Phase 17 sustainability) + 5 cloud provider cross-rollup (AWS EC2 RI + RDS RI + EC2 SP + S3 SP + Redshift SP + DynamoDB SP + Azure reservations + GCP CUDs + Naver Cloud + KT Cloud) + CommitmentInventory TypedDict 18 fields + 5 cloud provider enum aws/azure/gcp/naver/kt + commitment_kpi_selector `select_commitment_kpi` + 8 NEW KPI calculations (total_commitment_value_krw + coverage_pct + utilization_pct + expiring_commitments_30d + recommended_purchase_krw + savings_realized_krw + idle_commitment_krw + renewal_decision_score) + 7-module cross-join + commitment_report_generation_engine `generate_commitment_report` + PDF + CSV + Excel 3 export_format + 3 cadence weekly + monthly + quarterly + annual + 5-framework support (FinOps Foundation + AWS Cost Optimization Pillar + Azure Cost Optimization + GCP Cost Optimization + 한국 조달청 클라우드 commitment 가이드라인) + CommitmentReport TypedDict 14 fields + scheduled dispatch KST cron `schedule_commitment_dispatch` + 4 cron schedules weekly Mon 09:00 + monthly 1st-day 09:00 + quarterly 1st-day 09:00 + annual Jan-1 09:00 + recipient resolver Slack + Email + MS Teams + S3 archive dispatch + ScheduledCommitmentDispatch TypedDict 11 fields + tenant-scoped commitment role RBAC owner-only + Role.COMMITMENT_VIEWER 1 NEW enum + require_commitment_role() 1 NEW dep + commitment dashboard UI 5 sub-components (CommitmentInventoryAggregator + CommitmentKPISelector + CommitmentReportGeneratorPanel + ScheduledCommitmentDispatchConfigPanel + CommitmentTrendMiniChart) + ko-KR.json `finops_commitment.*` namespace EXTENSION ~30 keys + Capability matrix v1.43 → v1.44 EXTENSION FINOPS_COMMITMENT + audit-first INSERT 8 NEW + ActionClass.FINOPS_COMMITMENT)
**so that** Phase 17 wire `97cfe4e` (sustainability carbon reporting) + Phase 16 wire `81ae00a` (executive reporting) + Phase 15 wire `1b800d9` (tag_governance + cost allocation) + Phase 14 wire `e904485` (optimization + commitment_recommender 1y/3y break-even) + Phase 13 wire `8b98030` (forecasting + capacity planning) + Phase 12 wire `f3c0e63` (anomaly detection + budget alert) + Phase 11 wire `e020ad0` (showback + chargeback) 의 7-module FinOps territory 의 natural CLOUD COMMITMENT MANAGEMENT LAYER 결정 wire 진입 (7 module outputs → commitment inventory cross-rollup view + AWS/Azure/GCP/Naver/KT 5 cloud provider cross-rollup + coverage_pct + utilization_pct + expiring_commitments_30d + recommended_purchase_krw + savings_realized_krw + idle_commitment_krw + renewal_decision_score + commitment report generation engine 의 PDF/CSV/Excel 3 export format + FinOps Foundation + AWS Cost Optimization Pillar + Azure Cost Optimization + GCP Cost Optimization + 한국 조달청 클라우드 commitment 가이드라인 5-framework support + weekly/monthly/quarterly/annual 4 cadence + scheduled dispatch KST cron 4 cron schedules weekly/monthly/quarterly/annual + recipient resolver Slack + Email + MS Teams + S3 archive + tenant-scoped commitment role RBAC owner-only + commitment dashboard UI 5 sub-components + ko-KR.json `finops_commitment.*` EXTENSION ~30 keys) = Phase 17 FinOps territory 의 CLOUD COMMITMENT MANAGEMENT LAYER EXTENSION 결정 (7 module outputs → RIs/SPs/CUDs commitment management layer) + Phase 18 PRD entry `5eded22` (cj-style 133번째) DONE 진입 정합 보존 + Epic 12 2FA 챌린지 보존 + AD-22 owner-only RBAC 보존 + D-FINOPS-8 honestly DEFER 보존 진입 결정 wire + Phase 17 close-out retro `de009fe` §13 verbatim 해소 결정 wire 보존).

## Context

cj-style Phase 18 2번째 진입점 (cj-style 134번째) 진입 결정 wire 진입 완료:
- Phase 18 PRD entry `5eded22` (cj-style 133번째) DONE 진입 정합 보존
- Phase 17 close-out retro `de009fe` (cj-style 132번째) + Phase 17 atomic wire T1~T8 `97cfe4e` (cj-style 131번째) + Phase 17 spec entry `4be3120` (cj-style 130번째) + Phase 17 PRD entry `e0778ed` (cj-style 129번째) + Phase 16 close-out retro `26fd530` (cj-style 128번째) + Phase 16 atomic wire `81ae00a` (cj-style 127번째) + Phase 16 spec entry `69c29df` (cj-style 126번째) + Phase 16 PRD entry `4f11d03` (cj-style 125번째) + Phase 15 close-out retro `102f370` (cj-style 124번째) + Phase 15 atomic wire T1~T8 `1b800d9` (cj-style 123번째) + Phase 15 spec entry `69c29df` (cj-style 122번째) + Phase 15 PRD entry `87393b4` (cj-style 121번째) + Phase 14 close-out retro `5b367d9` (cj-style 120번째) + Phase 14 atomic wire T1~T8 `e904485` (cj-style 119번째) + Phase 14 spec entry `30637f6` (cj-style 118번째) + Phase 14 PRD entry `0e3f8d9` (cj-style 117번째) + Phase 13 close-out retro `850b4f8` (cj-style 116번째) + Phase 13 atomic wire T1~T8 `8b98030` (cj-style 115번째) + Phase 13 spec entry `77ed55f` (cj-style 114번째) + Phase 13 PRD entry `d31dfc8` (cj-style 113번째) + Phase 12 close-out retro `3354e83` (cj-style 112번째) + Phase 12 atomic wire T1~T8 `f3c0e63` (cj-style 111번째) + Phase 12 spec entry `8c5f374` (cj-style 110번째) + Phase 12 PRD entry `344c7eb` (cj-style 109번째) + Phase 11 close-out retro `80df15b` (cj-style 108번째) + Phase 11 atomic wire T1~T8 `e020ad0` (cj-style 107번째) + Phase 11 spec entry `82c93a8` (cj-style 106번째) + Phase 11 PRD entry `16d7698` (cj-style 105번째) + Phase 10 close-out retro `733d428` (cj-style 104번째) + Phase 10 wire `ac5d6c5` (cj-style 103번째) 결정 wire 모두 DONE 진입 정합 보존
- D-FINOPS-8 honestly DEFER 보존 진입 결정 wire (Phase 18 PRD entry 진입 시점에 carry-over chain 정직 회복 결정 wire + Phase 17 close-out retro `de009fe` §13 + Phase 16 close-out retro `26fd530` §13 + Phase 15 close-out retro `102f370` §13 + Phase 14 close-out retro `5b367d9` §13 + Phase 13 close-out retro `850b4f8` §13 + Phase 12 close-out retro `3354e83` §13 + Phase 11 close-out retro `80df15b` §12 + Phase 10 close-out retro `733d428` §10 + Phase 9 close-out retro `634427d` §10 + Phase 8 close-out retro `ab495a8` §10 + Phase 7 close-out retro `326fa9f` §10 + Phase 6 close-out retro `f9f006c` §13 + Epic 17 close-out retro `be8f3bd` §11 + 1st release close-out retro §6 "FinOps Cloud Commitment Management 결정 wire 보류, Phase 18+ 진입 시점" verbatim 해소 + Phase 18 PRD entry 진입 시점에 1 NEW 결정 wire)
- D-FINOPS-7 ✅ RESOLVED 보존 진입 결정 wire
- D-FINOPS-6 ✅ RESOLVED 보존 진입 결정 wire
- D-FINOPS-5 ✅ RESOLVED 보존 진입 결정 wire
- D-FINOPS-4 ✅ RESOLVED 보존 진입 결정 wire
- D-FINOPS-3 ✅ RESOLVED 보존 진입 결정 wire
- D-FINOPS-2 ✅ RESOLVED 보존 진입 결정 wire
- D-FINOPS-1 ✅ RESOLVED 보존 진입 결정 wire
- D-SLO-1 ✅ RESOLVED 보존 진입 결정 wire
- D-CHAOS-1 ✅ RESOLVED 보존 진입 결정 wire
- D-PERFORMANCE-1 ✅ RESOLVED 보존 진입 결정 wire
- D-OBSERVABILITY-1 ✅ RESOLVED 보존 진입 결정 wire
- Phase 18 PRD entry 의 8 ACs §F34.1~§F34.8 verbatim 결정 wire 보존
- Capability matrix v1.43 → v1.44 EXTENSION FINOPS_COMMITMENT 1 NEW row 결정 wire (industry-agnostic 4-industry grants ✅/✅/✅/✅ CR 12-1 L4 precedent 미러)
- AD-45 FinOps Cloud Commitment Management (RIs/SPs/CUDs) 신규 결정 wire 진입 (a)~(g) 7 sub-decisions

## 8 ACs (PRD §F34.1~§F34.8 verbatim) → ~86 detailed sub-ACs

### §F34.1 commitment_inventory_aggregator (11 sub-ACs)
- F34.1-1 `apps/api/modules/finops/commitment_inventory_aggregator.py` NEW (~+200 LOC + `aggregate_commitment_inventory(tenant_id, period_key, cloud_provider) -> CommitmentInventory` + 7 modules cross-rollup (Phase 11 wire `e020ad0` showback + Phase 12 wire `f3c0e63` anomaly + Phase 13 wire `8b98030` forecast + Phase 14 wire `e904485` optimization + Phase 15 wire `1b800d9` tag_governance + Phase 16 wire `81ae00a` executive + Phase 17 wire `97cfe4e` sustainability) 결정 wire + CR 12-5 D-PARITY-01 verbatim + 4 industries baseline industry-agnostic + per-tenant override EXTENSION + 5 cloud provider cross-rollup (AWS + Azure + GCP + Naver Cloud + KT Cloud) 결정 wire)
- F34.1-2 `aggregate_function` 5 functions 결정 wire = (1) `aws_commitment_aggregate` (EC2 RI + RDS RI + EC2 SP + S3 SP + Redshift SP + DynamoDB SP via AWS Cost Explorer API) + (2) `azure_commitment_aggregate` (Azure reservations via Azure Consumption API) + (3) `gcp_commitment_aggregate` (GCP CUDs via GCP Cloud Billing API) + (4) `naver_commitment_aggregate` (Naver Cloud commitment discounts via Naver Cloud billing API) + (5) `kt_commitment_aggregate` (KT Cloud commitment discounts via KT Cloud billing API) 결정 wire
- F34.1-3 `commitment_inventory_id` UUID PK 결정 wire + tenant-scoped RLS 자동 적용 CR 0-2 verbatim + CHECK + UNIQUE + indexes 결정 wire + Phase 17 carbon_emissions_rollup_id pattern verbatim EXTENSION
- F34.1-4 `coverage_pct` 계산 결정 wire = `commitment_covered_cost / total_tenant_cost` 결정 wire + Phase 11 showback cost data 활용 EXTENSION 결정 wire + 4 industries baseline (manufacturing coverage ≥ 70% + service coverage ≥ 80% + manufacturing_service coverage ≥ 75% + manufacturing_service_other coverage ≥ 65%) EXTENSION 결정 wire
- F34.1-5 `utilization_pct` 계산 결정 wire = `commitment_used_hours / commitment_total_hours` 결정 wire + utilization_under_threshold (utilization < 70%) → idle_commitment_krw EXTENSION 결정 wire + Phase 14 optimization accuracy tracking EXTENSION 결정 wire
- F34.1-6 `expiring_commitments_30d` 계산 결정 wire = `commitments with end_date - current_date <= 30 days` 결정 wire + KST timezone('Asia/Seoul') EXTENSION + 30-day rolling window + Phase 13 forecast 12-month horizon EXTENSION 결정 wire
- F34.1-7 `total_commitment_value_krw` 계산 결정 wire = `SUM(committed_amount_krw)` across all active commitments 결정 wire + AWS / Azure / GCP / Naver / KT currency conversion (KRW) EXTENSION 결정 wire + KRW 환율 일일 update 결정 wire
- F34.1-8 5 cloud provider coverage matrix EXTENSION = AWS coverage ≥ 70% baseline + Azure coverage ≥ 60% baseline + GCP coverage ≥ 50% baseline + Naver Cloud coverage ≥ 40% baseline + KT Cloud coverage ≥ 30% baseline 결정 wire + per-tenant override EXTENSION 결정 wire + tenant_owner_id 의 delegated commitment viewer 권한 OPTIONAL EXTENSION 결정 wire
- F34.1-9 `commitment_metadata` JSONB 결정 wire = `{provider: aws/azure/gcp/naver/kt, term: 1_year/3_year, payment_option: all_upfront/partial_upfront/no_upfront, offering_class: standard/convertible, scope: region/Zonal, instance_family: ec2/rds/...}` 결정 wire + AWS RI marketplace metadata EXTENSION 결정 wire
- F34.1-10 commitment inventory cross-rollup 결정 wire = 7 FinOps modules (Phase 11 showback cost + Phase 12 anomaly baseline + Phase 13 forecast 12-month + Phase 14 optimization commitment + Phase 15 tag_governance allocation + Phase 16 executive rollup + Phase 17 sustainability carbon) EXTENSION + commitment inventory RLS 자동 적용 CR 0-2 verbatim + Redis cache 24h TTL EXTENSION 결정 wire
- F34.1-11 `trace_id` UUID 결정 wire + audit-first INSERT `commitment_inventory_aggregated` CR 1-1 verbatim 결정 wire + Phase 11~17 inventory aggregator pattern verbatim EXTENSION

### §F34.2 commitment_kpi_selector (11 sub-ACs)
- F34.2-1 `apps/api/modules/finops/commitment_kpi_selector.py` NEW (~+220 LOC + `select_commitment_kpi(tenant_id, period_key, kpi_type) -> CommitmentKPI` + 8 NEW KPI calculations + Phase 11~17 wire 의 7-module outputs EXTENSION 결정 wire + CR 12-5 D-PARITY-01 verbatim + CommitmentKPI TypedDict 16 fields 결정 wire)
- F34.2-2 KPI #1 `total_commitment_value_krw` 결정 wire = `SUM(committed_amount_krw)` of active commitments per period_key 결정 wire + KRW currency unit EXTENSION + industry baseline per-tenant override EXTENSION 결정 wire
- F34.2-3 KPI #2 `coverage_pct` 계산 결정 wire = `commitment_covered_cost / total_tenant_cost * 100` 결정 wire + 4 industries baseline (manufacturing ≥ 70% + service ≥ 80% + manufacturing_service ≥ 75% + manufacturing_service_other ≥ 65%) EXTENSION + coverage_alert (coverage < baseline → warning) 결정 wire
- F34.2-4 KPI #3 `utilization_pct` 계산 결정 wire = `SUM(utilized_amount_krw) / SUM(committed_amount_krw) * 100` 결정 wire + Phase 14 optimization accuracy tracking EXTENSION + utilization_alert (utilization < 70% → idle_commitment_flag) 결정 wire
- F34.2-5 KPI #4 `expiring_commitments_30d` 계산 결정 wire = `COUNT(commitments with end_date - current_date <= 30 days)` 결정 wire + renewal_workflow trigger EXTENSION + Phase 13 forecast 12-month horizon EXTENSION + KST timezone('Asia/Seoul') EXTENSION 결정 wire
- F34.2-6 KPI #5 `recommended_purchase_krw` 계산 결정 wire = Phase 14 commitment_recommender 1y/3y break-even EXTENSION + coverage_gap * predicted_on_demand_cost * (1 - expected_discount_pct) 결정 wire + Phase 13 forecast 12-month horizon EXTENSION + savings_estimation EXTENSION 결정 wire
- F34.2-7 KPI #6 `savings_realized_krw` 계산 결정 wire = `SUM(on_demand_cost - commitment_cost)` for all utilized commitments 결정 wire + Phase 14 optimization realized_savings EXTENSION + savings_per_commitment_type aggregation EXTENSION 결정 wire
- F34.2-8 KPI #7 `idle_commitment_krw` 계산 결정 wire = `SUM(committed_amount_krw * (1 - utilization_pct / 100))` for utilization < 70% commitments 결정 wire + Phase 14 idle resource detection EXTENSION + remediation recommendation EXTENSION 결정 wire
- F34.2-9 KPI #8 `renewal_decision_score` 계산 결정 wire = weighted score (0~100) based on (utilization_pct * 0.4 + coverage_pct * 0.3 + savings_realized_krw_normalized * 0.2 + expiring_urgency * 0.1) 결정 wire + decision: auto_renew (score >= 80) / manual_review (40 <= score < 80) / do_not_renew (score < 40) 결정 wire + Phase 14 optimization accuracy tracking EXTENSION
- F34.2-10 8 KPI 7-module cross-join EXTENSION (Phase 11 cost + Phase 12 anomaly + Phase 13 forecast + Phase 14 optimization + Phase 15 tag_governance + Phase 16 executive + Phase 17 sustainability) 결정 wire + per-tenant override EXTENSION + industry_baseline comparison EXTENSION 결정 wire
- F34.2-11 `kpi_metadata` JSONB 결정 wire = `{calculation_method, source_modules, data_freshness, baseline_period, target_threshold}` EXTENSION + 7-module index hints 결정 wire + Redis cache 24h TTL EXTENSION + audit-first INSERT `commitment_kpi_calculated` CR 1-1 verbatim 결정 wire

### §F34.3 commitment_report_generation_engine (11 sub-ACs)
- F34.3-1 `apps/api/modules/finops/commitment_report_generator.py` NEW (~+250 LOC + `generate_commitment_report(tenant_id, period_key, cadence, export_format) -> CommitmentReport` + CommitmentInventory EXTENSION + 3 export_format PDF + CSV + Excel + 3 cadence weekly + monthly + quarterly + annual + 5-framework support FinOps Foundation + AWS Cost Optimization Pillar + Azure Cost Optimization + GCP Cost Optimization + 한국 조달청 클라우드 commitment 가이드라인 결정 wire + CR 12-5 D-PARITY-01 verbatim)
- F34.3-2 PDF generation 결정 wire = `reportlab==4.0.7` AD-14 stack pin library + Korean font registration (Pretendard) + 8-section template (executive summary + commitment inventory + coverage analysis + utilization analysis + expiration schedule + purchase recommendation + savings realized + renewal decisions) + AWS / Azure / GCP / Naver / KT 다국어 support EXTENSION 결정 wire
- F34.3-3 CSV generation 결정 wire = `pandas==2.1.4` AD-14 stack pin + `openpyxl==3.1.2` AD-14 stack pin + UTF-8-BOM encoding + 14 columns (commitment_id + cloud_provider + commitment_type + term + start_date + end_date + committed_amount_krw + utilized_amount_krw + utilization_pct + coverage_pct + savings_realized_krw + status + trace_id + period_key) 결정 wire
- F34.3-4 Excel generation 결정 wire = `xlsxwriter==3.1.9` AD-14 stack pin + 3 sheets (Commitment Inventory + KPI Summary + Renewal Decisions) + 차트 (coverage trend + utilization trend + savings realized) + conditional formatting (utilization < 70% → red highlight) EXTENSION 결정 wire
- F34.3-5 3 cadence EXTENSION = weekly (current week commitments) + monthly (current month commitments) + quarterly (Q1/Q2/Q3/Q4) + annual (current year YTD) 결정 wire + Phase 17 sustainability_report_generation_engine pattern verbatim EXTENSION 결정 wire
- F34.3-6 5-framework support EXTENSION = FinOps Foundation Framework (commitment optimization pillar) + AWS Cost Optimization Pillar (RI/SP best practices) + Azure Cost Optimization (reserved instances) + GCP Cost Optimization (CUDs) + 한국 조달청 클라우드 commitment 가이드라인 EXTENSION 결정 wire
- F34.3-7 `report_metadata` JSONB 결정 wire = `{generation_method, framework_version, kpi_count, commitment_count, total_savings_krw, next_action: auto_renew/manual_review/do_not_renew}` EXTENSION 결정 wire
- F34.3-8 `expiring_commitments_30d` 강조 EXTENSION = report PDF 첫 페이지에 expiring commitments summary 박스 EXTENSION 결정 wire + renewal_decision_score 기반 색상 coding (red < 40 / yellow 40~80 / green >= 80) EXTENSION 결정 wire
- F34.3-9 `recommended_purchase_krw` 권고 EXTENSION = Phase 14 commitment_recommender 1y/3y break-even EXTENSION + coverage gap analysis + 1-year vs 3-year trade-off (3-year savings 30%+ → 3-year 추천, 그 외 1-year 추천) EXTENSION 결정 wire
- F34.3-10 `audit-first INSERT 8 NEW` 결정 wire = `commitment_report_generated` + `commitment_report_exported` + `commitment_kpi_refreshed` + `commitment_dashboard_viewed` + `commitment_report_dispatched` + `commitment_scheduled_dispatch_evaluated` + `finops_commitment_dry_run_executed` + `cross_module_commitment_kpi_calculated` via ActionClass.FINOPS_COMMITMENT EXTENSION + 5-year retention NFR8 EXTENSION 결정 wire
- F34.3-11 `trace_id` UUID 결정 wire + 7-module cross-join EXTENSION + 4-industry baseline comparison EXTENSION + audit-first INSERT `commitment_report_generated` CR 1-1 verbatim 결정 wire

### §F34.4 scheduled_commitment_dispatch (11 sub-ACs)
- F34.4-1 `apps/api/modules/finops/scheduled_commitment_dispatch.py` NEW (~+200 LOC + `schedule_commitment_dispatch(tenant_id, cadence, recipients) -> ScheduledCommitmentDispatch` + 4 cron schedules weekly + monthly + quarterly + annual KST 결정 wire + ScheduledCommitmentDispatch TypedDict 11 fields 결정 wire)
- F34.4-2 4 cron schedules EXTENSION = (1) weekly Mon 09:00 KST (`0 9 * * 1` pytz timezone('Asia/Seoul')) + (2) monthly 1st-day 09:00 KST (`0 9 1 * *`) + (3) quarterly 1st-day 09:00 KST (`0 9 1 */3 *`) + (4) annual Jan-1 09:00 KST (`0 9 1 1 *`) 결정 wire + APScheduler 3.10.4 AD-14 stack pin EXTENSION 결정 wire
- F34.4-3 recipient resolver 결정 wire = Slack (Slack webhook URL) + Email (SMTP) + S3 archive (commitment_report_{period_key}.pdf upload) + MS Teams webhook EXTENSION + per-tenant recipient list EXTENSION + role-based recipient (owner-only AD-22 verbatim + Epic 12 2FA 챌린지 보존) EXTENSION 결정 wire
- F34.4-4 `commitment_types` JSONB 결정 wire = `{ec2_ri: true, rds_ri: true, ec2_sp: true, s3_sp: true, redshift_sp: true, dynamodb_sp: true, azure_reservation: true, gcp_cud: true, naver_commitment: false, kt_commitment: false}` EXTENSION + per-tenant override EXTENSION 결정 wire
- F34.4-5 `cron_expression` 결정 wire = apscheduler cron format EXTENSION + next_run_at 계산 (croniter 기반) + last_run_at 추적 EXTENSION + idempotency_key (commitment_dispatch_{tenant_id}_{cadence}_{period_key}) EXTENSION 결정 wire
- F34.4-6 `audit-first INSERT` 결정 wire = `commitment_scheduled_dispatch_evaluated` (cron 매 tick 마다) + `commitment_report_dispatched` (dispatch 성공 시) + 5-year retention NFR8 EXTENSION + Phase 17 scheduled_sustainability_dispatch pattern verbatim EXTENSION 결정 wire
- F34.4-7 Slack 메시지 format 결정 wire = 5-section message (commitment summary + coverage alert + expiring commitments + utilization alert + recommended purchase) + Block Kit JSON EXTENSION + Korean language default (ko-KR NFR18 SSOT) EXTENSION 결정 wire
- F34.4-8 Email format 결정 wire = multipart/alternative (HTML + plain text) + Korean charset UTF-8 + attachment commitment_report_{period_key}.pdf EXTENSION + unsubscribe link (CR 12-1 compliance) EXTENSION 결정 wire
- F34.4-9 S3 archive 결정 wire = commitment_report_{tenant_id}_{period_key}_{cadence}.pdf + lifecycle policy (90-day → Glacier IR) + KMS encryption (NFR6 AES-256-GCM) EXTENSION + audit-first INSERT `commitment_report_archived` EXTENSION 결정 wire
- F34.4-10 MS Teams webhook EXTENSION = Adaptive Card 1.4 format + 5-section card (commitment summary + coverage + expiring + utilization + purchase) + per-tenant webhook URL EXTENSION 결정 wire
- F34.4-11 `last_status` enum EXTENSION = success / failed / skipped / dry_run 결정 wire + 실패 시 retry 3회 (exponential backoff) + alert routing (owner Slack DM) EXTENSION 결정 wire

### §F34.5 tenant_scoped_commitment_role_rbac (11 sub-ACs)
- F34.5-1 `apps/api/core/role.py` MODIFIED 결정 wire (Role.COMMITMENT_VIEWER = "commitment_viewer" 1 NEW enum + tenant-scoped role + tenant_settings.commitment_viewers JSONB list 결정 wire + Phase 1 carry-over (auth) RBAC EXTENSION 정합 + Phase 17 wire `97cfe4e` 의 Role.SUSTAINABILITY_VIEWER 패턴 verbatim 미러)
- F34.5-2 `apps/api/dependencies/capability.py` MODIFIED 결정 wire (require_commitment_role 1 NEW dep + tenant_settings.commitment_viewers validation + per-tenant RBAC + AD-22 owner-only RBAC EXTENSION 정합 + Epic 12 2FA 챌린지 mandatory + Phase 17 wire `97cfe4e` 의 require_sustainability_role 패턴 verbatim 미러)
- F34.5-3 commitment viewer permission set 결정 wire (read-only access to CommitmentInventory + CommitmentKPI + CommitmentReport + ScheduledCommitmentDispatch + commitment KPI selector + dashboard view + report generation + dispatch config + NO write/modify permission + per-tenant override permission_set JSONB)
- F34.5-4 tenant-scoped RBAC 검증 결정 wire (tenant_settings.commitment_viewers 의 user_id list 검증 + Epic 1 carry-over (auth) 의 SSO_ENTERPRISE (Epic 15 wire) JWT token validation EXTENSION 정합 + cross-tenant access 차단 403 Forbidden)
- F34.5-5 owner-only access (AD-22 verbatim) 결정 wire (commitment dashboard view + commitment report generation + scheduled dispatch config 모두 owner-only RBAC AD-22 verbatim 보존 + commitment viewer 는 read-only + modify/config 는 owner-only + Epic 12 2FA 챌린지 mandatory)
- F34.5-6 Epic 12 2FA 챌린지 mandatory 결정 wire (commitment dashboard view + commitment report generation + scheduled dispatch config 모두 Epic 12 2FA 챌린지 mandatory + Epic 12 wire `a63646c` 의 2FA 챌린지 보존 정합)
- F34.5-7 audit-first INSERT `commitment_inventory_aggregated` + `commitment_kpi_calculated` + `commitment_scheduled_dispatch_evaluated` 3 NEW RBAC context 결정 wire (CR 1-1 verbatim 적용 + ActionClass.FINOPS_COMMITMENT + role_id + user_id + tenant_id + 2fa_verified BOOLEAN metadata + per-tenant RLS 자동 적용)
- F34.5-8 미허용 tenant 의 commitment dashboard 진입 차단 결정 wire (require_commitment_role dep + capability gate per-tenant on/off + FINOPS_COMMITMENT capability 부재 시 403 Forbidden + FORBIDDEN_KO message 결정 wire ("FinOps Cloud Commitment Management capability 미허용 tenant") + Epic 12 2FA 챌린지 보존 + AD-22 owner-only RBAC 정합)
- F34.5-9 phase_11~17 carry-over 진입 차단 결정 wire (commitment dashboard 진입 시 FINOPS_SHOWBACK + FINOPS_CHARGEBACK + FINOPS_ANOMALY_DETECTION + FINOPS_BUDGET_ALERT + FINOPS_FORECASTING_CAPACITY_PLANNING + FINOPS_OPTIMIZATION + FINOPS_TAG_GOVERNANCE + FINOPS_REPORTING + FINOPS_SUSTAINABILITY capability 도 동시에 검증 + commitment dashboard 가 FINOPS_COMMITMENT 만 있고 Phase 11~17 capability 없는 경우 403 Forbidden 결정 wire)
- F34.5-10 tenant-level override EXTENSION 결정 wire (tenant_owner_id 의 delegated commitment viewer 권한 OPTIONAL 설정 + per-tenant override delegation_policy JSONB + AD-22 owner-only RBAC EXTENSION 정합 + Epic 12 2FA 챌린지 mandatory)
- F34.5-11 typed exception envelope CR 12-5 D-14 verbatim 결정 wire (3 NEW typed exception classes: `CommitmentRolePermissionError(403)` + `CommitmentTenantScopeViolationError(403)` + `CommitmentCapabilityGateViolationError(403)`)

### §F34.6 commitment_dashboard_ui (10 sub-ACs)
- F34.6-1 `apps/web/app/[locale]/(dashboard)/admin/finops/commitment/page.tsx` NEW (~+180 LOC + 5 sub-components (`CommitmentInventoryAggregator` + `CommitmentKPISelector` + `CommitmentReportGeneratorPanel` + `ScheduledCommitmentDispatchConfigPanel` + `CommitmentTrendMiniChart`) 결정 wire + owner-only RBAC AD-22 verbatim + Epic 12 2FA 챌린지 보존 결정 wire)
- F34.6-2 `apps/web/components/finops/FinopsCommitmentDashboardPanel.tsx` NEW Client component (~+200 LOC + 5-tab layout (Inventory / KPI / Reports / Dispatch / Trends) + Recharts 2.12.7 AD-14 stack pin 차트 EXTENSION + Phase 17 wire `97cfe4e` 의 FinopsSustainabilityDashboardPanel 패턴 verbatim EXTENSION 결정 wire)
- F34.6-3 `CommitmentInventoryAggregator` sub-component 결정 wire (5 cloud provider tab (AWS / Azure / GCP / Naver / KT) + commitment list (sortable + filterable) + coverage_pct progress bar + utilization_pct progress bar + expiring_commitments_30d badge + ko-KR.json `finops_commitment.dashboard.*` 6 keys CR 11-4 D-002 verbatim SSOT)
- F34.6-4 `CommitmentKPISelector` sub-component 결정 wire (8 KPI card (total_commitment_value_krw + coverage_pct + utilization_pct + expiring_commitments_30d + recommended_purchase_krw + savings_realized_krw + idle_commitment_krw + renewal_decision_score) + industry baseline 비교 + per-tenant override + Recharts 2.12.7 AD-14 stack pin + Phase 17 wire `97cfe4e` 의 SustainabilityKPISelector 패턴 verbatim EXTENSION)
- F34.6-5 `CommitmentReportGeneratorPanel` sub-component 결정 wire (cadence selector weekly + monthly + quarterly + annual + export_format selector PDF + CSV + Excel + generate button + download link + audit log entry + Recharts 2.12.7 AD-14 stack pin + ko-KR.json `finops_commitment.report.*` 6 keys)
- F34.6-6 `ScheduledCommitmentDispatchConfigPanel` sub-component 결정 wire (4 cadence toggle weekly + monthly + quarterly + annual + recipient list editor Slack + Email + S3 + MS Teams + cron_expression display + next_run_at display + last_run_at display + last_status badge + Recharts 2.12.7 AD-14 stack pin + ko-KR.json `finops_commitment.dispatch.*` 6 keys)
- F34.6-7 `CommitmentTrendMiniChart` sub-component 결정 wire (Recharts 2.12.7 AD-14 stack pin LineChart + coverage_pct trend 12-period + utilization_pct trend 12-period + savings_realized_krw trend 12-period + recommended_purchase_krw trend 12-period + Phase 17 wire `97cfe4e` 의 ComplianceTrendMiniChart EXTENSION 정합 + ko-KR.json `finops_commitment.trend.*` 4 keys)
- F34.6-8 ko-KR.json `finops_commitment.*` namespace EXTENSION ~30 keys 결정 wire (CR 11-4 D-002 verbatim SSOT) + `finops_commitment.dashboard.title` + `finops_commitment.dashboard.scope_tenant` + `finops_commitment.dashboard.scope_department` + `finops_commitment.dashboard.scope_cost_center` + `finops_commitment.dashboard.scope_product_line` + `finops_commitment.dashboard.refresh` + `finops_commitment.kpi.total_commitment_value_krw` + `finops_commitment.kpi.coverage_pct` + `finops_commitment.kpi.utilization_pct` + `finops_commitment.kpi.expiring_commitments_30d` + `finops_commitment.kpi.recommended_purchase_krw` + `finops_commitment.kpi.savings_realized_krw` + `finops_commitment.kpi.idle_commitment_krw` + `finops_commitment.kpi.renewal_decision_score` + `finops_commitment.report.cadence_weekly` + `finops_commitment.report.cadence_monthly` + `finops_commitment.report.cadence_quarterly` + `finops_commitment.report.cadence_annual` + `finops_commitment.report.format_pdf` + `finops_commitment.report.format_csv` + `finops_commitment.report.format_excel` + `finops_commitment.report.framework_finops_foundation` + `finops_commitment.report.framework_aws_cost_optimization` + `finops_commitment.report.framework_azure_cost_optimization` + `finops_commitment.report.framework_gcp_cost_optimization` + `finops_commitment.report.framework_korea_procurement` + `finops_commitment.report.generate` + `finops_commitment.dispatch.weekly` + `finops_commitment.dispatch.monthly` + `finops_commitment.dispatch.quarterly` + `finops_commitment.dispatch.annual` + `finops_commitment.dispatch.enable` + `finops_commitment.dispatch.disable` + `finops_commitment.dispatch.test` 등 결정 wire
- F34.6-9 ARIA labels 결정 wire (WCAG 2.1 AA compliance Epic 12 의 2FA 챌린지 UI 와 동일 표준 + role="region" + aria-label="클라우드 commitment 관리" + aria-live="polite" + keyboard navigation Tab + Enter + Arrow keys + screen reader 지원 + Phase 1 UX v1.0 locked decision 결정 wire Dark MVP / WCAG AA / Professional / ko-KR verbatim 보존)
- F34.6-10 Vitest RTL render discipline CR 11-4 D-003 verbatim 결정 wire (~7 NEW vitest cases CommitmentInventoryAggregator 1 + CommitmentKPISelector 1 + CommitmentReportGeneratorPanel 1 + ScheduledCommitmentDispatchConfigPanel 1 + CommitmentTrendMiniChart 1 + ko-KR SSOT 2 + finops commitment dashboard parity CR 12-5 D-PARITY-01 = ~7 NEW vitest PASS + Phase 17 wire `97cfe4e` 의 ~8 NEW vitest cases EXTENSION 정합)

### §F34.7 Capability matrix v1.44 EXTENSION (12 sub-ACs)
- F34.7-1 Capability matrix v1.43 → v1.44 EXTENSION 결정 wire (1 NEW row FINOPS_COMMITMENT industry-agnostic 4-industry grants ✅/✅/✅/✅ CR 12-1 L4 precedent 미러 + Phase 17 wire `97cfe4e` 의 FINOPS_SUSTAINABILITY 패턴 verbatim EXTENSION)
- F34.7-2 `apps/api/core/capability.py` MODIFIED 결정 wire (Capability.FINOPS_COMMITMENT = "finops_commitment" 1 NEW enum + 4 `_INDUSTRY_CAPABILITIES` blocks EXTENSION industry-agnostic ✅/✅/✅/✅ + Phase 17 wire `97cfe4e` 의 Capability.FINOPS_SUSTAINABILITY 패턴 verbatim EXTENSION)
- F34.7-3 `apps/api/dependencies/capability.py` MODIFIED 결정 wire (require_finops_commitment 1 NEW dep + `__all__` EXTENSION + Phase 17 wire `97cfe4e` 의 require_finops_sustainability 패턴 verbatim EXTENSION)
- F34.7-4 `docs/capability-matrix.md` MODIFIED 결정 wire (capability matrix v1.43 → v1.44 EXTENSION + 1 NEW row FINOPS_COMMITMENT industry-agnostic 4-industry grants ✅/✅/✅/✅ + FINOPS_COMMITMENT section 신규 추가)
- F34.7-5 `apps/api/modules/finops/commitment/__init__.py` NEW + `apps/api/modules/finops/commitment/serializers.py` NEW 결정 wire (Phase 17 wire `97cfe4e` m17_finops_sustainability.sustainability_serializers EXTENSION pattern verbatim 미러 + m18_finops_commitment module 결정 wire)
- F34.7-6 미허용 tenant 의 commitment dashboard 진입 차단 결정 wire (require_finops_commitment dep + capability gate per-tenant on/off + 403 Forbidden + FORBIDDEN_KO message 결정 wire ("FinOps Cloud Commitment Management capability 미허용 tenant") + Epic 12 2FA 챌린지 보존 + AD-22 owner-only RBAC 정합)
- F34.7-7 phase_11~17 carry-over 진입 차단 결정 wire (commitment dashboard 진입 시 FINOPS_SHOWBACK + FINOPS_CHARGEBACK + FINOPS_ANOMALY_DETECTION + FINOPS_BUDGET_ALERT + FINOPS_FORECASTING_CAPACITY_PLANNING + FINOPS_OPTIMIZATION + FINOPS_TAG_GOVERNANCE + FINOPS_REPORTING + FINOPS_SUSTAINABILITY capability 도 동시에 검증 + FINOPS_COMMITMENT 만 있고 Phase 11~17 capability 없는 경우 403 Forbidden 결정 wire)
- F34.7-8 drift detector 8 NEW pytest cases 결정 wire (`tests/integration/test_capability_matrix_v1_44_drift.py` NEW + Phase 17 wire `97cfe4e` `test_capability_matrix_v1_43_drift.py` 패턴 verbatim 미러)
- F34.7-9 m18_finops_commitment module 결정 wire (apps/api/modules/finops/__init__.py EXTENSION + m18_finops_commitment.commitment_serializers NEW 결정 wire + Phase 17 wire `97cfe4e` m17_finops_sustainability.sustainability_serializers EXTENSION pattern verbatim 미러)
- F34.7-10 SSOT RED→GREEN EXTENSION + A36 SDR 검증 4-step 자동 적용 결정 wire (capability matrix v1.44 신규 1 row + capability.py EXTENSION 1 NEW enum + require_finops_commitment 1 NEW dep 결정 wire + drift detector EXTENSION)
- F34.7-11 CR 12-1 L4 industry-agnostic capability 결정 wire (FINOPS_COMMITMENT industry-agnostic 4-industry grants ✅/✅/✅/✅ 결정 wire manufacturing + service + manufacturing_service + manufacturing_service_other 모두 허용)
- F34.7-12 capability gate 의 fail-closed 결정 wire (미허용 tenant 의 commitment dashboard 진입 차단 + capability matrix v1.44 row 부재 시 fail-closed + Capability enum 부재 시 fail-closed + AD-22 owner-only RBAC 정합 + Epic 12 2FA 챌린지 보존 + NFR4 PII minimization 정합 보존)

### §F34.8 dry-run + Tests + wire scope T1~T8 (10 sub-ACs)
- F34.8-1 dry-run mode 결정 wire (`--finops-commitment-inventory-dry-run` + `--finops-commitment-kpi-dry-run` + `--finops-commitment-report-dry-run` + `--finops-scheduled-commitment-dispatch-dry-run` + `--finops-commitment-export-dry-run` 5 CLI flag + CommitmentInventory dry-run parameter + commitment KPI dry-run parameter + commitment report dry-run parameter + scheduled dispatch dry-run parameter + dry-run 시 actual `commitment_dashboard_viewed` + `commitment_kpi_refreshed` + `commitment_report_generated` + `commitment_report_dispatched` + `commitment_scheduled_dispatch_evaluated` audit-first INSERT skip 결정 wire)
- F34.8-2 dry-run 의 preview 결과 결정 wire (phase_18_finops_commitment_inventory_preview + phase_18_finops_commitment_kpi_preview 2 table 결정 wire + preview_id UUID PK + tenant_id UUID + preview_type enum + period_key TEXT + preview_data JSONB + computed_at TIMESTAMPTZ DEFAULT NOW() + trace_id TEXT + RLS 자동 적용 CR 0-2 verbatim 결정 wire + audit-first INSERT `finops_commitment_dry_run_executed` CR 1-1 verbatim 결정 wire)
- F34.8-3 dry-run 의 CLI flag 결정 wire (`--finops-commitment-inventory-dry-run` + `--finops-commitment-kpi-dry-run` + `--finops-commitment-report-dry-run` + `--finops-scheduled-commitment-dispatch-dry-run` + `--finops-commitment-export-dry-run` 5 NEW CLI flag + Phase 17 wire `97cfe4e` `--finops-sustainability-dry-run` + `--finops-carbon-emissions-dry-run` + `--finops-sustainability-kpi-dry-run` + `--finops-sustainability-report-dry-run` + `--finops-sustainability-scheduled-dispatch-dry-run` 패턴 verbatim 미러)
- F34.8-4 tests ~+62 NEW pytest PASS 결정 wire (commitment_inventory_aggregator 8 + commitment_kpi_selector 8 + commitment_report_generator 8 + scheduled_commitment_dispatch 6 + alembic 0050 6 + audit action 8 + capability matrix v1.44 8 + dry-run 6 + 5 cloud provider integration 6 = ~+62 NEW pytest PASS)
- F34.8-5 vitest tests ~+7 NEW vitest PASS 결정 wire (CommitmentInventoryAggregator 1 + CommitmentKPISelector 1 + CommitmentReportGeneratorPanel 1 + ScheduledCommitmentDispatchConfigPanel 1 + CommitmentTrendMiniChart 1 + ko-KR SSOT 2 + finops commitment dashboard parity CR 12-5 D-PARITY-01 = ~7 NEW vitest PASS + Phase 17 wire `97cfe4e` 의 ~8 NEW vitest cases EXTENSION 정합)
- F34.8-6 ruff + tsc 0 NEW + SDR drift gate 결정 wire (0 NEW ruff + 0 NEW tsc + 0 regressions + SDR drift gate PASS 결정 wire + A36 SDR 검증 4-step 자동 적용)
- F34.8-7 wire scope T1~T8 결정 wire (~+27-33 files estimate = ~+21 NEW + ~+12 MODIFIED atomic single sprint = Phase 17 wire `97cfe4e` 의 ~28 files pattern verbatim 미러 + Phase 11~17 wire 의 ~25-30 files pattern verbatim EXTENSION)
- F34.8-8 A19 cohesion pattern 9 surface EXTENSION PASS 결정 wire (FinOps Cloud Commitment Management surface NEW = F34.1~F34.8 + spec surface EXTENSION + test surface EXTENSION + docs surface EXTENSION)
- F34.8-9 CR lessons applied 18종 결정 wire (CR 0-2 + CR 1-1 + CR 4-3/4-4 + CR 1-1 ContextVar + CR 1-1 RSC boundary + CR 9-6 + CR 11-3 + CR 11-4 + CR 12-1 + CR 12-5 D-14 + CR 12-5 D-PARITY-01 + CR 12-5 D-GATE-01 + A19 cohesion + A36 SDR 검증 + AD-14 + AD-22 + AD-45 + NFR4)
- F34.8-10 partial wire 시도 0건 + single sprint atomic docs-only wire 1 진입점 결정 wire (cj-style 134번째 epic 연속 정직 회복 Phase 18 spec entry atomic docs-only wire 5 files atomic single sprint 결정 wire)

## 8 tasks (T1~T8) + 68 subtasks

### T1: commitment_inventory_aggregator + commitment_kpi_selector modules (10 subtasks)
- T1.1: `apps/api/modules/finops/commitment_inventory_aggregator.py` NEW (~+200 LOC + aggregate_commitment_inventory 함수 + 7-module cross-rollup + CommitmentInventory TypedDict 18 fields + 5 cloud provider enum aws/azure/gcp/naver/kt + commitment_type enum + 7-module cross-rollup RLS 자동 적용 CR 0-2 verbatim + 4 industries baseline industry-agnostic 결정 wire)
- T1.2: CommitmentInventory TypedDict 18 fields 결정 wire (commitment_id UUID PK + tenant_id UUID + period_key TEXT + cloud_provider enum aws/azure/gcp/naver/kt + commitment_type enum ec2_ri/rds_ri/ec2_sp/s3_sp/redshift_sp/dynamodb_sp/azure_reservation/gcp_cud/naver_commitment/kt_commitment + commitment_term enum 1_year/3_year + commitment_status enum active/expired/expiring_soon/pending_renewal + start_date DATE + end_date DATE + committed_amount_krw NUMERIC(20,4) + utilized_amount_krw NUMERIC(20,4) + utilization_pct NUMERIC(5,2) + coverage_pct NUMERIC(5,2) + savings_realized_krw NUMERIC(20,4) + instance_type TEXT + region TEXT + trace_id TEXT + commitment_metadata JSONB 5-cloud-provider source attribution)
- T1.3: `aggregate_function` 5 functions 결정 wire ((1) aws_commitment_aggregate EC2 RI + RDS RI + EC2 SP + S3 SP + Redshift SP + DynamoDB SP via AWS Cost Explorer API + (2) azure_commitment_aggregate Azure reservations via Azure Consumption API + (3) gcp_commitment_aggregate GCP CUDs via GCP Cloud Billing API + (4) naver_commitment_aggregate Naver Cloud commitment discounts via Naver Cloud billing API + (5) kt_commitment_aggregate KT Cloud commitment discounts via KT Cloud billing API)
- T1.4: 7-module cross-rollup 결정 wire ((1) Phase 11 showback_total_krw from `phase_11_finops_showback_department` → commitment coverage from showback cost data + (2) Phase 12 anomaly_count_30d from `phase_12_finops_anomaly_detection` → commitment anomaly detection alert + (3) Phase 13 forecast_projection_krw from `phase_13_finops_forecast_projection` → forecast commitment projection + (4) Phase 14 optimization_savings_krw from `phase_14_finops_optimization_recommendation` → commitment_recommender 1y/3y break-even EXTENSION + (5) Phase 15 tag_compliance_pct from `phase_15_finops_compliance_report` → allocation ↔ commitment allocation + (6) Phase 16 executive rollup from `phase_16_finops_executive_rollup` → executive commitment view + (7) Phase 17 carbon_emissions_rollup from `phase_17_finops_carbon_emissions_rollup` → carbon-aware commitment 권고 → single CommitmentInventory join 결정 wire)
- T1.5: 5 cloud provider coverage matrix 결정 wire (AWS coverage ≥ 70% baseline + Azure coverage ≥ 60% baseline + GCP coverage ≥ 50% baseline + Naver Cloud coverage ≥ 40% baseline + KT Cloud coverage ≥ 30% baseline + per-tenant override JSONB)
- T1.6: period selector + coverage_pct + utilization_pct 계산 결정 wire (period_key TEXT monthly/quarterly/annual + per-tenant override period_selector default current month + coverage_pct = commitment_covered_cost / total_tenant_cost × 100 + 4 industries baseline + utilization_pct = utilized_amount_krw / committed_amount_krw × 100 + utilization_alert < 70% → idle_commitment_flag 결정 wire)
- T1.7: 7-module index hints 결정 wire (PostgreSQL index on `(tenant_id, period_key)` for phase_11_finops_showback + index on `(tenant_id, detected_at)` for phase_12_finops_anomaly + index on `(tenant_id, forecast_period)` for phase_13_finops_forecast + index on `(tenant_id, recommendation_status)` for phase_14_finops_optimization + index on `(tenant_id, period_key)` for phase_15_finops_compliance + index on `(tenant_id, scope_type, period_key)` for phase_16_finops_executive_rollup + index on `(tenant_id, period_key)` for phase_17_finops_carbon_emissions_rollup 결정 wire)
- T1.8: commitment inventory pure validator CR 11-4 P-015 verbatim 적용 결정 wire (validate_commitment_inventory + 5 layer defense + CommitmentInventoryAggregationError(500) + CommitmentInventoryScopeError(404) + CommitmentInventoryPeriodError(422) CR 12-5 D-14 envelope)
- T1.9: audit-first INSERT `commitment_inventory_aggregated` 결정 wire (CR 1-1 verbatim 적용 + ActionClass.FINOPS_COMMITMENT + emit_audit_typed BEFORE commitment inventory aggregation + per-tenant RLS 자동 적용 + multi-tenant isolation test)
- T1.10: 8 NEW pytest cases 결정 wire (CommitmentInventory TypedDict 18 fields + 5 cloud provider enum + commitment_type enum + 7-module cross-rollup + 5 cloud provider integration + period selector + cache layer + 7-module index hints + audit-first INSERT + typed exception envelope + 4 industries baseline)

### T2: commitment_kpi_selector + 8 NEW KPI + 4 industries baseline (10 subtasks)
- T2.1: `apps/api/modules/finops/commitment_kpi_selector.py` NEW (~+220 LOC + select_commitment_kpi 함수 + 8 NEW KPI calculations + Phase 11~17 wire 의 7-module outputs EXTENSION 결정 wire + CR 12-5 D-PARITY-01 verbatim + CommitmentKPI TypedDict 16 fields 결정 wire)
- T2.2: CommitmentKPI TypedDict 16 fields 결정 wire (kpi_id UUID PK + tenant_id UUID + period_key TEXT + kpi_type enum total_commitment_value_krw/coverage_pct/utilization_pct/expiring_commitments_30d/recommended_purchase_krw/savings_realized_krw/idle_commitment_krw/renewal_decision_score + kpi_value NUMERIC(20,4) + kpi_unit TEXT + target_value NUMERIC(20,4) + variance_pct NUMERIC(5,2) + computed_at TIMESTAMPTZ + trace_id TEXT + kpi_metadata JSONB + industry_baseline NUMERIC(20,4))
- T2.3: KPI #1 total_commitment_value_krw + KPI #2 coverage_pct + KPI #3 utilization_pct 결정 wire (SUM(committed_amount_krw) + commitment_covered_cost / total_tenant_cost × 100 + 4 industries baseline + utilized_amount_krw / committed_amount_krw × 100 + utilization_alert < 70% → idle_commitment_flag)
- T2.4: KPI #4 expiring_commitments_30d + KPI #5 recommended_purchase_krw 결정 wire (COUNT(commitments with end_date - current_date <= 30 days) + renewal_workflow trigger + Phase 14 commitment_recommender 1y/3y break-even EXTENSION + coverage_gap * predicted_on_demand_cost * (1 - expected_discount_pct))
- T2.5: KPI #6 savings_realized_krw + KPI #7 idle_commitment_krw 결정 wire (SUM(on_demand_cost - commitment_cost) for all utilized commitments + SUM(committed_amount_krw * (1 - utilization_pct / 100)) for utilization < 70% commitments + Phase 14 optimization realized_savings EXTENSION)
- T2.6: KPI #8 renewal_decision_score 결정 wire (weighted score 0~100 = utilization_pct * 0.4 + coverage_pct * 0.3 + savings_realized_krw_normalized * 0.2 + expiring_urgency * 0.1 + decision: auto_renew (>= 80) / manual_review (40~80) / do_not_renew (< 40) + Phase 14 optimization accuracy tracking EXTENSION)
- T2.7: commitment KPI selector period selector + scope selector 결정 wire (period_key TEXT monthly/quarterly/annual + 4 scope 옵션 tenant/department/cost_center/product_line + per-tenant override JSONB)
- T2.8: 7-module index hints 결정 wire (PostgreSQL composite indexes for Phase 11~17 wire 의 7 module outputs + Phase 18 commitment_inventory table 결정 wire)
- T2.9: KPI threshold classification 결정 wire (각 KPI 별 threshold_status (on_track/warning/critical) + per-tenant override threshold JSONB + per-KPI override JSONB + 4 industries baseline 적용)
- T2.10: 8 NEW pytest cases 결정 wire (8 KPI calculations + CommitmentKPI TypedDict + period selector + scope selector + threshold classification + audit-first INSERT + typed exception envelope + multi-tenant isolation + 4 industries baseline)

### T3: commitment_report_generator + 3 export_format + 5-framework support (10 subtasks)
- T3.1: `apps/api/modules/finops/commitment_report_generator.py` NEW (~+250 LOC + generate_commitment_report 함수 + 3 export_format PDF/CSV/Excel + 4 cadence weekly/monthly/quarterly/annual + 5-framework support FinOps Foundation + AWS Cost Optimization Pillar + Azure Cost Optimization + GCP Cost Optimization + 한국 조달청 클라우드 commitment 가이드라인 결정 wire + CR 12-5 D-PARITY-01 verbatim)
- T3.2: CommitmentReport TypedDict 14 fields 결정 wire (report_id UUID PK + tenant_id UUID + period_key TEXT + cadence enum weekly/monthly/quarterly/annual + export_format enum pdf/csv/excel + coverage_pct NUMERIC(5,2) + utilization_pct NUMERIC(5,2) + expiring_commitments_30d INTEGER + recommended_purchase_krw NUMERIC(20,4) + savings_realized_krw NUMERIC(20,4) + idle_commitment_krw NUMERIC(20,4) + generated_at TIMESTAMPTZ + trace_id TEXT + report_metadata JSONB)
- T3.3: 3 export_format 옵션 결정 wire ((1) PDF reportlab==4.0.7 library + Korean font registration Pretendard + 8-section template FinOps Foundation-aligned + (2) CSV pandas==2.1.4 + UTF-8-BOM + 14 columns + (3) Excel xlsxwriter==3.1.9 + 3 sheets + 차트 + conditional formatting + 3 format 모두 tenant_id RLS 적용)
- T3.4: 4 cadence 옵션 결정 wire ((1) weekly = current week commitments + (2) monthly = current month commitments + (3) quarterly = Q1/Q2/Q3/Q4 + (4) annual = current year YTD + cadence default = monthly + per-tenant override cadence default monthly)
- T3.5: 5-framework support 결정 wire (FinOps Foundation Framework + AWS Cost Optimization Pillar + Azure Cost Optimization + GCP Cost Optimization + 한국 조달청 클라우드 commitment 가이드라인 + framework default = FinOps Foundation + per-tenant override framework default FinOps Foundation)
- T3.6: PDF report structure 결정 wire (reportlab.platypus.SimpleDocTemplate + 8 sections executive_summary + commitment_inventory + coverage_analysis + utilization_analysis + expiration_schedule + purchase_recommendation + savings_realized + renewal_decisions + Korean font registration Pretendard + CommitmentInventory JSON → Jinja2 template → HTML → PDF conversion + Recharts 2.12.7 AD-14 stack pin chart PNG embedding)
- T3.7: CSV report structure + Excel report structure 결정 wire (CSV 14 columns commitment_id + cloud_provider + commitment_type + term + start_date + end_date + committed_amount_krw + utilized_amount_krw + utilization_pct + coverage_pct + savings_realized_krw + status + trace_id + period_key + UTF-8-BOM + ko-KR locale / Excel xlsxwriter.Workbook + 3 sheets Commitment Inventory + KPI Summary + Renewal Decisions + 차트 coverage_trend + utilization_trend + savings_realized + conditional formatting utilization < 70% red highlight + xlsx MIME type)
- T3.8: S3 archive 결정 wire (apps/api/integrations/s3_archive.py EXTENSION + report_file upload to s3://costmgr-commitment-reports/{tenant_id}/{period_key}/{report_id}.{ext} + presigned URL 7-day expiry + audit-first INSERT `commitment_report_generated` CR 1-1 verbatim + Epic 12 2FA 챌린지 보존)
- T3.9: audit-first INSERT `commitment_report_generated` + `commitment_report_exported` + `commitment_kpi_refreshed` + `commitment_dashboard_viewed` + `commitment_report_dispatched` + `commitment_scheduled_dispatch_evaluated` + `finops_commitment_dry_run_executed` + `cross_module_commitment_kpi_calculated` 8 NEW 결정 wire (CR 1-1 verbatim 적용 + ActionClass.FINOPS_COMMITMENT + emit_audit_typed BEFORE/AFTER commitment report event + per-tenant RLS 자동 적용 + multi-tenant isolation test)
- T3.10: typed exception envelope CR 12-5 D-14 verbatim 결정 wire (4 NEW typed exception classes: CommitmentReportGenerationError + CommitmentReportExportError + CommitmentReportArchiveError + CommitmentAccuracyDegradationError + 8 NEW pytest cases)

### T4: scheduled_commitment_dispatch + 4 cron schedules + recipient resolver (10 subtasks)
- T4.1: `apps/api/jobs/scheduled_commitment_dispatch.py` NEW (~+200 LOC + schedule_commitment_dispatch 함수 + 4 cron schedules weekly + monthly + quarterly + annual KST 결정 wire)
- T4.2: 4 cron schedules 결정 wire ((1) weekly 0 9 * * 1 KST Monday 09:00 + (2) monthly 0 9 1 * * KST 1st day of month 09:00 + (3) quarterly 0 9 1 */3 * KST 1st day of quarter 09:00 + (4) annual 0 9 1 1 * KST January 1 09:00 + KST timezone pytz.timezone('Asia/Seoul'))
- T4.3: cron scheduler library 결정 wire (apscheduler==3.10.4 AsyncIOScheduler + PersistentJobStore + audit-first INSERT `commitment_scheduled_dispatch_evaluated` CR 1-1 verbatim + Phase 17 wire `97cfe4e` 의 sustainability scheduled dispatch EXTENSION 정합 + Phase 16 wire `81ae00a` 의 scheduled executive dispatch EXTENSION 정합)
- T4.4: recipient resolver dispatch 결정 wire (Slack slack-sdk webhook URL + Email SMTP multipart/alternative + S3 archive presigned URL + MS Teams Adaptive Card 1.4 webhook + 4 dispatch targets 모두 owner-only RBAC AD-22 verbatim 보존)
- T4.5: ScheduledCommitmentDispatch TypedDict 11 fields 결정 wire (dispatch_id UUID PK + tenant_id UUID + cadence enum weekly/monthly/quarterly/annual + cron_expression TEXT + recipients JSONB + commitment_types JSONB + next_run_at TIMESTAMPTZ + last_run_at TIMESTAMPTZ + last_status enum success/failed/skipped/dry_run + created_at TIMESTAMPTZ + trace_id TEXT)
- T4.6: dispatch lifecycle state machine 결정 wire (`scheduled` default → `running` cron trigger 시 → `completed` 성공 시 → `failed` 실패 시 retry 3회 → `cancelled` owner manual cancel 시 + per-tenant override max_retry default 3 + audit-first INSERT `commitment_scheduled_dispatch_evaluated` CR 1-1 verbatim)
- T4.7: dispatch idempotency 결정 wire (per-(tenant_id + cadence + period_key) tuple unique key + 중복 dispatch skip + audit log 에 idempotency_check metadata 저장 + Phase 12 wire `f3c0e63` 의 anomaly detection idempotency EXTENSION 정합 + Phase 17 wire `97cfe4e` 의 sustainability dispatch idempotency EXTENSION 정합)
- T4.8: dispatch retry policy 결정 wire (failed dispatch 의 exponential backoff 1min → 5min → 30min + 3회 실패 시 owner-only Slack alert + Epic 12 2FA 챌린지 보존 + audit log 기록 + Phase 9 wire `e7670e1` chaos experiment 의 retry policy EXTENSION 정합)
- T4.9: audit-first INSERT `commitment_scheduled_dispatch_evaluated` + `commitment_report_dispatched` 2 NEW 결정 wire (CR 1-1 verbatim 적용 + ActionClass.FINOPS_COMMITMENT + emit_audit_typed BEFORE/AFTER scheduled dispatch event + per-tenant RLS 자동 적용)
- T4.10: typed exception envelope CR 12-5 D-14 verbatim 결정 wire (4 NEW typed exception classes: ScheduledCommitmentDispatchError + CommitmentCronExpressionInvalidError + CommitmentRecipientResolverError + CommitmentDispatchIdempotencyViolationError + 4 industries baseline dispatch policy 결정 wire + 6 NEW pytest cases)

### T5: alembic 0050 phase_18_finops_commitment (8 subtasks)
- T5.1: `apps/api/alembic/versions/0050_phase_18_finops_commitment.py` NEW (~+180 LOC + 6 tables CREATE + 2 preview tables + indexes + RLS policies + down_revision "0049_phase_17_finops_sustainability" 결정 wire)
- T5.2: phase_18_finops_commitment_inventory table 20 columns 결정 wire (commitment_id UUID PK + tenant_id UUID + period_key TEXT + cloud_provider TEXT enum + commitment_type TEXT enum + commitment_term TEXT enum + commitment_status TEXT enum + start_date DATE + end_date DATE + committed_amount_krw NUMERIC(20,4) + utilized_amount_krw NUMERIC(20,4) + utilization_pct NUMERIC(5,2) + coverage_pct NUMERIC(5,2) + savings_realized_krw NUMERIC(20,4) + instance_type TEXT + region TEXT + cache_key TEXT + computed_at TIMESTAMPTZ + trace_id TEXT + commitment_metadata JSONB 5-cloud-provider source attribution + UNIQUE (tenant_id, commitment_id, period_key))
- T5.3: phase_18_finops_commitment_kpi table 15 columns 결정 wire (kpi_id UUID PK + tenant_id UUID + period_key TEXT + kpi_type TEXT enum + kpi_value NUMERIC(20,4) + kpi_unit TEXT + target_value NUMERIC(20,4) + variance_pct NUMERIC(5,2) + kpi_delta NUMERIC nullable + kpi_trend TEXT enum + kpi_threshold_status TEXT enum + industry_baseline NUMERIC(20,4) + computed_at TIMESTAMPTZ + trace_id TEXT + kpi_metadata JSONB + INDEX)
- T5.4: phase_18_finops_commitment_report table 16 columns 결정 wire (report_id UUID PK + tenant_id UUID + period_key TEXT + cadence TEXT enum + export_format TEXT enum + framework TEXT enum + report_file_url TEXT + report_size_bytes BIGINT + coverage_pct NUMERIC(5,2) + utilization_pct NUMERIC(5,2) + expiring_commitments_30d INTEGER + recommended_purchase_krw NUMERIC(20,4) + savings_realized_krw NUMERIC(20,4) + generated_at TIMESTAMPTZ + expires_at TIMESTAMPTZ + trace_id TEXT + report_metadata JSONB + INDEX)
- T5.5: phase_18_finops_scheduled_commitment_dispatch table 13 columns 결정 wire (dispatch_id UUID PK + tenant_id UUID + cadence TEXT enum + cron_expression TEXT + recipients JSONB + commitment_types JSONB + idempotency_key TEXT + next_run_at TIMESTAMPTZ + last_run_at TIMESTAMPTZ nullable + last_status TEXT enum + created_at TIMESTAMPTZ + trace_id TEXT + UNIQUE (tenant_id, cadence, period_key) + INDEX)
- T5.6: phase_18_finops_commitment_viewer table 8 columns 결정 wire (commitment_viewer_id UUID PK + tenant_id UUID + user_id UUID + role TEXT enum owner/delegate/viewer + granted_by UUID + granted_at TIMESTAMPTZ + revoked_at TIMESTAMPTZ nullable + trace_id TEXT + UNIQUE (tenant_id, user_id))
- T5.7: phase_18_finops_commitment_metadata table 10 columns 결정 wire (metadata_id UUID PK + tenant_id UUID + cloud_provider TEXT enum + term TEXT enum + payment_option TEXT enum + offering_class TEXT enum + scope TEXT enum + instance_family TEXT + trace_id TEXT + INDEX)
- T5.8: 6 tables RLS policies + 2 preview tables 결정 wire (CR 0-2 verbatim + tenant_id = current_setting('app.tenant_id')::uuid + Phase 11 wire `e020ad0` phase_11_finops_* table 정합 + Phase 12 wire `f3c0e63` phase_12_finops_* table 정합 + Phase 13 wire `8b98030` phase_13_finops_* table 정합 + Phase 14 wire `e904485` phase_14_finops_* table 정합 + Phase 15 wire `1b800d9` phase_15_finops_* table 정합 + Phase 16 wire `81ae00a` phase_16_finops_* table 정합 + Phase 17 wire `97cfe4e` phase_17_finops_* table 정합 + 2 preview tables phase_18_finops_commitment_inventory_preview + phase_18_finops_commitment_kpi_preview 결정 wire + alembic migration 6 NEW pytest cases + multi-tenant isolation test 결정 wire)

### T6: audit action EXTENSION 8 NEW + typed exception envelope (8 subtasks)
- T6.1: `apps/api/core/audit_action.py` MODIFIED 결정 wire (ActionClass.FINOPS_COMMITMENT 1 NEW class 신규 정의 + FinopsCommitmentAction Literal 8 NEW values + _ActionRegistry FINOPS_COMMITMENT entry 신규 1개 등록 + __all__ EXTENSION + AuditAction Union EXTENSION)
- T6.2: ActionClass.FINOPS_COMMITMENT = 'finops_commitment' 신규 정의 결정 wire (CR 12-1 L4 precedent 미러 FINOPS_SUSTAINABILITY Phase 17 wire `97cfe4e` + FINOPS_REPORTING Phase 16 wire `81ae00a` + FINOPS_TAG_GOVERNANCE Phase 15 wire `1b800d9` + FINOPS_OPTIMIZATION Phase 14 wire `e904485` + FINOPS_FORECASTING_CAPACITY_PLANNING Phase 13 wire `8b98030` + FINOPS_ANOMALY_DETECTION + FINOPS_BUDGET_ALERT Phase 12 wire `f3c0e63` + FINOPS_SHOWBACK + FINOPS_CHARGEBACK Phase 11 wire `e020ad0` pattern verbatim bind)
- T6.3: FinopsCommitmentAction Literal 8 NEW values 결정 wire = `commitment_dashboard_viewed` + `cross_module_commitment_kpi_calculated` + `commitment_report_generated` + `commitment_report_exported` + `commitment_report_dispatched` + `commitment_scheduled_dispatch_evaluated` + `finops_commitment_dry_run_executed` + `commitment_kpi_refreshed` (8 NEW) EXTENSION (CR 1-1 verbatim 적용)
- T6.4: _ActionRegistry FINOPS_COMMITMENT entry 신규 1개 등록 결정 wire (resource_table "phase_18_finops_*" + action_class=FINOPS_COMMITMENT + 8 NEW actions acceptance + reject)
- T6.5: emit_audit_typed BEFORE/AFTER FinOps Cloud Commitment event CR 1-1 verbatim 적용 결정 wire (commitment_dashboard_viewed 의 audit_first INSERT 가 commitment dashboard view 직후 실행 + cross_module_commitment_kpi_calculated AFTER KPI 계산 + commitment_report_generated AFTER report generation + commitment_report_exported AFTER export + commitment_report_dispatched AFTER dispatch + commitment_scheduled_dispatch_evaluated AFTER scheduled dispatch + finops_commitment_dry_run_executed AFTER dry-run + commitment_kpi_refreshed AFTER refresh + trace_id propagation + actor_id capture + tenant_id capture)
- T6.6: multi-tenant isolation 결정 wire (8 NEW action 의 tenant_id 가 RLS 와 정합 + cross-tenant audit log leak 방지 결정 wire)
- T6.7: typed exception envelope CR 12-5 D-14 verbatim 결정 wire (16 NEW typed exception classes: CommitmentInventoryAggregationError(500) + CommitmentInventoryScopeError(404) + CommitmentInventoryPeriodError(422) + CommitmentCrossModuleJoinError(500) + CommitmentKPIError(500) + CommitmentReportGenerationError(500) + CommitmentReportExportError(500) + CommitmentReportArchiveError(500) + ScheduledCommitmentDispatchError(500) + CommitmentCronExpressionInvalidError(400) + CommitmentRecipientResolverError(404) + CommitmentDispatchIdempotencyViolationError(422) + CommitmentRolePermissionError(403) + CommitmentTenantScopeViolationError(403) + CommitmentCapabilityGateViolationError(403) + CommitmentAccuracyDegradationError(500))
- T6.8: 8 NEW pytest cases 결정 wire (AuditAction Literal 값 검증 + ActionClass.FINOPS_COMMITMENT enum value + resource_table + emit_audit_typed BEFORE/AFTER FinOps Cloud Commitment event CR 1-1 verbatim 적용 + multi-tenant isolation + trace_id propagation + typed exception envelope + dry-run default)

### T7: capability v1.44 EXTENSION + frontend commitment dashboard UI (8 subtasks)
- T7.1: `apps/api/core/capability.py` MODIFIED 결정 wire (Capability.FINOPS_COMMITMENT 1 NEW enum + 4 `_INDUSTRY_CAPABILITIES` blocks EXTENSION industry-agnostic ✅/✅/✅/✅ CR 12-1 L4 precedent 미러)
- T7.2: `apps/api/dependencies/capability.py` MODIFIED 결정 wire (require_finops_commitment 1 NEW dep + __all__ EXTENSION 결정 wire)
- T7.3: `apps/api/core/role.py` MODIFIED 결정 wire (Role.COMMITMENT_VIEWER 1 NEW enum + tenant-scoped RBAC + Phase 17 wire `97cfe4e` Role.SUSTAINABILITY_VIEWER 패턴 verbatim 미러)
- T7.4: capability matrix v1.43 → v1.44 EXTENSION title update + v1.44 changelog entry prepend + 1 NEW row FINOPS_COMMITMENT industry-agnostic 4-industry grants ✅/✅/✅/✅ 결정 wire
- T7.5: `tests/integration/test_capability_matrix_v1_44_drift.py` NEW 8 NEW pytest cases 결정 wire (Capability.FINOPS_COMMITMENT enum + 4 industries grants + v1.43 + v1.42 + v1.41 + v1.40 + v1.39 + v1.38 + v1.37 + v1.36 + v1.35 + v1.34 + v1.33 + v1.32 + v1.31 + v1.30 + v1.29 preservation + Phase 5 v1.29 + Epic 16 v1.28 + Epic 17 v1.30 + Phase 6 v1.31 + Phase 7 v1.32 + Phase 8 v1.33 + Phase 9 v1.34 + Phase 10 v1.35 + Phase 11 v1.36 + Phase 12 v1.37 + Phase 13 v1.38 + Phase 13 v1.39 + Phase 14 v1.40 + Phase 15 v1.41 + Phase 16 v1.42 + Phase 17 v1.43 pattern verbatim)
- T7.6: `docs/capability-matrix.md` MODIFIED v1.43 → v1.44 EXTENSION 결정 wire (1 NEW row FINOPS_COMMITMENT industry-agnostic 4-industry grants + FINOPS_COMMITMENT section 신규 추가)
- T7.7: `apps/web/app/[locale]/(dashboard)/admin/finops/commitment/page.tsx` NEW (~+180 LOC + 5 sub-components 결정 wire: CommitmentInventoryAggregator + CommitmentKPISelector + CommitmentReportGeneratorPanel + ScheduledCommitmentDispatchConfigPanel + CommitmentTrendMiniChart + owner-only RBAC AD-22 verbatim + Epic 12 2FA 챌린지 보존)
- T7.8: SSOT RED→GREEN EXTENSION 결정 wire (capability matrix v1.44 신규 1 row + capability.py EXTENSION 1 NEW enum + require_finops_commitment 1 NEW dep wire + role.py EXTENSION 1 NEW enum Role.COMMITMENT_VIEWER + drift detector EXTENSION + frontend commitment dashboard UI wire)

### T8: atomic commit (4 subtasks)
- T8.1: 3중 게이트 impact NONE 결정 wire (ruff scoped 0 NEW + pytest 0 NEW failures + vitest 0 NEW failures + tsc 0 NEW errors)
- T8.2: A19 cohesion pattern 9 surface EXTENSION PASS 결정 wire (FinOps Cloud Commitment Management surface NEW = F34.1~F34.8)
- T8.3: atomic commit via `git commit -F <file>` 결정 wire (CR 9-6 D5 prevention + PowerShell here-string 회피)
- T8.4: sprint-status.yaml `phase-18-spec-entry: backlog → done` transition 결정 wire

## Dev Notes (CR lessons applied 18종)

- **CR 0-2 RLS lesson ✅ APPLIED**: Phase 18 wire 시점에 phase_18_finops_commitment_inventory + phase_18_finops_commitment_kpi + phase_18_finops_commitment_report + phase_18_finops_scheduled_commitment_dispatch + phase_18_finops_commitment_viewer + phase_18_finops_commitment_metadata + phase_18_finops_commitment_inventory_preview + phase_18_finops_commitment_kpi_preview 8 tables 모두 RLS 자동 적용 + multi-tenant isolation test 결정 wire + tenant-scoped result_hash 결정 wire + Phase 11~17 wire 의 phase_11_finops_* ~ phase_17_finops_* tables RLS 정합 보존
- **CR 1-1 audit-first INSERT ✅ APPLIED**: ActionClass.FINOPS_COMMITMENT 신규 정의 + 8 NEW audit log entries (`commitment_dashboard_viewed` + `cross_module_commitment_kpi_calculated` + `commitment_report_generated` + `commitment_report_exported` + `commitment_report_dispatched` + `commitment_scheduled_dispatch_evaluated` + `finops_commitment_dry_run_executed` + `commitment_kpi_refreshed` = 8 NEW) 결정 wire + emit_audit_typed BEFORE/AFTER FinOps Cloud Commitment event CR 1-1 verbatim 적용
- **CR 4-3/4-4 lessons carry ✅ APPLIED**: CommitmentInventory aggregation + commitment KPI baseline + commitment report golden_diff pattern verbatim 미러 + tenant-scoped result_hash 결정 wire + Epic 8 wire `e117e09` capability drift detector 정합 패턴 + Phase 17 wire `97cfe4e` sustainability accuracy baseline result_hash 패턴 verbatim + Phase 16 wire `81ae00a` executive reporting accuracy baseline result_hash 패턴 verbatim + Phase 15 wire `1b800d9` tag governance accuracy baseline result_hash 패턴 verbatim + Phase 14 wire `e904485` optimization accuracy baseline result_hash 패턴 verbatim
- **CR 1-1 ContextVar lesson ✅ APPLIED**: trace_id request-scoped ContextVar 바인딩 + 비동기 trace context 보존 CR 1-1 verbatim 적용 + FinOps Cloud Commitment event 의 trace_id propagation 결정 wire
- **CR 1-1 RSC boundary lesson ✅ APPLIED**: `apps/web/app/[locale]/(dashboard)/admin/finops/commitment/page.tsx` Client-only + commitment dashboard server-only delegation 결정 wire + CR 1-1 verbatim 적용
- **CR 9-6 commit message discipline ✅ APPLIED**: `git commit -F <file>` 사용, PowerShell here-string 회피, D5 prevention 결정 wire
- **CR 11-3 honest-DEFER discipline ✅ APPLIED**: 134번째 epic 연속 정직 회복 결정 wire (D-1-1-DEFER-* + D-EPIC-16-REVIEW-DEFER-* + D-PHASE-4-DR-DEFER-* + D-EPIC-17-WIRE-DEFER-T2-T3-UI + D-RETENTION-1 + D-OBSERVABILITY-1 + D-PERFORMANCE-1 + D-CHAOS-1 + D-SLO-1 + D-FINOPS-1 + D-FINOPS-2 + D-FINOPS-3 + D-FINOPS-4 + D-FINOPS-5 + D-FINOPS-6 + D-FINOPS-7 모두 ✅ ALL RESOLVED 보존 + D-FINOPS-8 honestly DEFER 보존 진입 결정 wire)
- **CR 11-4 D-001~D-005 + P-015 lessons carry ✅ APPLIED**: dry-run mode UI 진입 시 frontend territory 정합 sweep 결정 wire + ko-KR.json SSOT only + vitest RTL render discipline + owner-only RBAC + unknown state reject + ko-KR.json SSOT drift detector 결정 wire
- **CR 12-1 L4 industry-agnostic capability ✅ APPLIED**: FINOPS_COMMITMENT industry-agnostic 4-industry grants ✅/✅/✅/✅ 결정 wire + capability matrix v1.44 EXTENSION 결정 wire
- **CR 12-5 D-14 typed exception envelope ✅ APPLIED**: 16 NEW typed exception classes (CommitmentInventoryAggregationError(500) + CommitmentInventoryScopeError(404) + CommitmentInventoryPeriodError(422) + CommitmentCrossModuleJoinError(500) + CommitmentKPIError(500) + CommitmentReportGenerationError(500) + CommitmentReportExportError(500) + CommitmentReportArchiveError(500) + ScheduledCommitmentDispatchError(500) + CommitmentCronExpressionInvalidError(400) + CommitmentRecipientResolverError(404) + CommitmentDispatchIdempotencyViolationError(422) + CommitmentRolePermissionError(403) + CommitmentTenantScopeViolationError(403) + CommitmentCapabilityGateViolationError(403) + CommitmentAccuracyDegradationError(500)) 결정 wire + apps/api/main.py EXTENSION 결정 wire
- **CR 12-5 D-PARITY-01 inversion ✅ APPLIED**: Python FastAPI backend commitment_inventory_aggregator.py + commitment_kpi_selector.py + commitment_report_generator.py + scheduled_commitment_dispatch.py TypedDict ↔ TypeScript Next.js frontend commitment-types.ts interface parity 결정 wire + vitest CR 12-5 D-PARITY-01 검증 결정 wire
- **CR 12-5 D-GATE-01 inversion ✅ APPLIED**: FINOPS_COMMITMENT capability gate per-tenant on/off + owner-only RBAC AD-22 결정 wire + gate 적용 대상 명시 결정 wire
- **A19 cohesion pattern 9 surface EXTENSION PASS ✅**: FinOps Cloud Commitment Management surface NEW = F34.1~F34.8 FinOps Cloud Commitment Management territory 결정 wire + spec surface EXTENSION + test surface EXTENSION + docs surface EXTENSION 결정 wire
- **A36 SDR 검증 4-step 자동 적용 ✅**: commit prefix lint PASS + sprint-status structure PASS + vitest file count drift 0건 + commit consistency PASS 결정 wire
- **AD-14 stack pin ✅ APPLIED**: Recharts 2.12.7 + reportlab==4.0.7 + openpyxl==3.1.2 + pandas==2.1.4 + xlsxwriter==3.1.9 + apscheduler==3.10.4 + pytz==2024.1 결정 wire + Phase 17 wire `97cfe4e` EXTENSION 결정 wire + Phase 16 wire `81ae00a` EXTENSION 결정 wire + Phase 15 wire `1b800d9` EXTENSION 결정 wire + Phase 14 wire `e904485` EXTENSION 결정 wire + Phase 13 wire `8b98030` EXTENSION 결정 wire + Phase 12 wire `f3c0e63` EXTENSION 결정 wire + Phase 11 wire `e020ad0` EXTENSION 결정 wire
- **AD-22 owner-only RBAC ✅ APPLIED**: commitment dashboard view + commitment report generation + scheduled dispatch config 모두 owner-only RBAC AD-22 + Epic 12 2FA 챌린지 보존 결정 wire
- **AD-45 FinOps Cloud Commitment Management (RIs/SPs/CUDs) 신규 ✅ APPLIED**: (a)~(g) 7 sub-decisions 진입 (commitment_inventory_aggregator 7-module cross-join + commitment_kpi_selector 8 NEW KPI + commitment report generation engine PDF + CSV + Excel + 3 cadence + scheduled dispatch KST cron + tenant-scoped commitment role RBAC + commitment dashboard UI + Capability matrix v1.44 EXTENSION FINOPS_COMMITMENT + 4-industry grants)
- **NFR4 PII minimization ✅ PRESERVED**: commitment reporting data 는 사업 metric + commitment amount + KPI value 만 포함, PII 미포함 결정 wire

## Architecture Alignment (cj-style ALLOWED sweep — Phase 17 wire 정합)

**ALLOWED_SERVICE_SUBMODULES sweep CR 11-3 D-2 verbatim** (Phase 5 wire `f093f8c` + Phase 7 wire `59b56cd` + Phase 8 wire `60d4ea1` + Phase 9 wire `e7670e1` + Phase 10 wire `ac5d6c5` + Phase 11 wire `e020ad0` + Phase 12 wire `f3c0e63` + Phase 13 wire `8b98030` + Phase 14 wire `e904485` + Phase 15 wire `1b800d9` + Phase 16 wire `81ae00a` + Phase 17 wire `97cfe4e` 정합):

### Backend (FastAPI, Python 3.12)
- ✅ `apps/api/modules/finops/` (MODIFIED EXTENSION): `commitment_inventory_aggregator.py` + `commitment_kpi_selector.py` + `commitment_report_generator.py` + `scheduled_commitment_dispatch.py` + `commitment/__init__.py` NEW + `commitment/serializers.py` NEW + `__init__.py` EXTENSION + `serializers.py` EXTENSION
- ✅ `apps/api/core/capability.py` (MODIFIED): Capability.FINOPS_COMMITMENT enum EXTENSION + 4 INDUSTRY_CAPABILITIES EXTENSION
- ✅ `apps/api/core/role.py` (MODIFIED): Role.COMMITMENT_VIEWER enum EXTENSION + tenant-scoped RBAC
- ✅ `apps/api/dependencies/capability.py` (MODIFIED): require_finops_commitment EXTENSION + require_commitment_role EXTENSION
- ✅ `apps/api/core/audit_action.py` (MODIFIED): ActionClass.FINOPS_COMMITMENT + FinopsCommitmentAction Literal 8 NEW + _ActionRegistry FINOPS_COMMITMENT entry 1 신규 등록 + __all__ EXTENSION
- ✅ `apps/api/core/errors.py` (MODIFIED): 16 NEW typed exception classes CR 12-5 D-14 verbatim
- ✅ `apps/api/alembic/versions/0050_phase_18_finops_commitment.py` (NEW): 6 tables + 2 preview tables + indexes + RLS policies
- ✅ `apps/api/jobs/scheduled_commitment_dispatch_job.py` (NEW): cron KST weekly Mon 09:00 + monthly 1st-day 09:00 + quarterly 1st-day 09:00 + annual Jan-1 09:00 scheduled dispatch
- ✅ `apps/api/integrations/s3_archive.py` (MODIFIED): commitment report S3 archive upload + presigned URL
- ✅ `apps/api/main.py` (MODIFIED): /admin/finops/commitment/* endpoints EXTENSION (CR 1-1 RSC boundary 적용)

### Frontend (Next.js 15.x, TypeScript 5.x)
- ✅ `apps/web/app/[locale]/(dashboard)/admin/finops/commitment/page.tsx` (NEW): RSC + commitment dashboard
- ✅ `apps/web/app/[locale]/(dashboard)/admin/finops/commitment/layout.tsx` (NEW): RTL section wrapper
- ✅ `apps/web/components/finops/FinopsCommitmentDashboardPanel.tsx` (NEW): 5 components (CommitmentInventoryAggregator + CommitmentKPISelector + CommitmentReportGeneratorPanel + ScheduledCommitmentDispatchConfigPanel + CommitmentTrendMiniChart)
- ✅ `apps/web/lib/finops/commitment-types.ts` (NEW): CommitmentInventory + CommitmentKPI + CommitmentReport + ScheduledCommitmentDispatch TypedDict CR 12-5 D-PARITY-01 verbatim + CommitmentApiError class
- ✅ `apps/web/lib/finops/commitment-client.ts` (NEW): 4 fetch wrappers
- ✅ `apps/web/messages/ko-KR.json` (MODIFIED): EXTENSION `finops_commitment.*` namespace ~30 keys 결정 wire

### Tests
- ✅ `tests/api/core/test_phase_18_commitment_inventory_aggregator.py` (NEW): ~8 NEW pytest
- ✅ `tests/api/core/test_phase_18_commitment_kpi_selector.py` (NEW): ~8 NEW pytest
- ✅ `tests/api/core/test_phase_18_commitment_report_generator.py` (NEW): ~8 NEW pytest
- ✅ `tests/api/core/test_phase_18_scheduled_commitment_dispatch.py` (NEW): ~6 NEW pytest
- ✅ `tests/api/core/test_phase_18_audit_action.py` (NEW): ~8 NEW pytest
- ✅ `tests/api/core/test_phase_18_commitment_rbac.py` (NEW): ~6 NEW pytest
- ✅ `tests/integration/test_finops_commitment_tenant_isolation.py` (NEW): multi-tenant isolation CR 0-2 verbatim
- ✅ `tests/integration/test_capability_matrix_v1_44_drift.py` (NEW): 8 NEW pytest cases
- ✅ `apps/web/__tests__/finops-commitment/finops-commitment-dashboard.test.tsx` (NEW): ~5 NEW vitest
- ✅ `apps/web/__tests__/i18n/finops-commitment-i18n-ssot.test.ts` (NEW): SSOT drift NFR18 ko-KR 정합
- ✅ `tests/web/test_finops_commitment_dashboard_parity.py` (NEW): ~2 cases CR 12-5 D-PARITY-01 verification

### Docs
- ✅ `docs/finops-cloud-commitment-management.md` (NEW): ~+200 LOC 14 sections runbook 결정 wire
- ✅ `docs/capability-matrix.md` (MODIFIED): v1.43 → v1.44 EXTENSION

## Files Affected (estimate)

- **~21 NEW**: `apps/api/modules/finops/{commitment_inventory_aggregator,commitment_kpi_selector,commitment_report_generator,scheduled_commitment_dispatch}.py` (4 files) + `apps/api/modules/finops/commitment/{__init__,serializers}.py` (2 files) + `apps/api/alembic/versions/0050_phase_18_finops_commitment.py` + `apps/api/jobs/{scheduled_commitment_dispatch_job}.py` (1 file) + `apps/web/app/[locale]/(dashboard)/admin/finops/commitment/{page,layout}.tsx` (2 files) + `apps/web/components/finops/FinopsCommitmentDashboardPanel.tsx` + `apps/web/lib/finops/{commitment-types,commitment-client}.ts` (2 files) + tests (10 files) + `docs/finops-cloud-commitment-management.md`
- **~12 MODIFIED**: `apps/api/core/capability.py` + `apps/api/core/role.py` + `apps/api/dependencies/capability.py` + `apps/api/core/audit_action.py` + `apps/api/core/errors.py` + `apps/api/main.py` + `apps/api/modules/finops/__init__.py` + `apps/api/modules/finops/serializers.py` + `apps/api/integrations/s3_archive.py` + `apps/web/messages/ko-KR.json` + `docs/capability-matrix.md` + `_bmad-output/implementation-artifacts/sprint-status.yaml` + `tests/integration/conftest.py` + `apps/api/alembic/versions/script.py.mako`
- **Total**: ~33 files atomic single sprint

## Test Coverage

- **~62 NEW pytest PASS 결정 wire**:
  - `tests/api/core/test_phase_18_commitment_inventory_aggregator.py` (8 cases): CommitmentInventory TypedDict 18 fields + 5 cloud provider enum + commitment_type enum + 7-module cross-rollup + 5 cloud provider integration + period selector + cache layer + 7-module index hints + audit-first INSERT + typed exception envelope
  - `tests/api/core/test_phase_18_commitment_kpi_selector.py` (8 cases): 8 KPI calculations + CommitmentKPI TypedDict + period selector + scope selector + threshold classification + audit-first INSERT + typed exception envelope + multi-tenant isolation + 4 industries baseline
  - `tests/api/core/test_phase_18_commitment_report_generator.py` (8 cases): 3 export_format + 4 cadence + 5-framework support + CommitmentReport TypedDict + S3 archive + delivery + recipient resolver + audit-first INSERT + typed exception envelope
  - `tests/api/core/test_phase_18_scheduled_commitment_dispatch.py` (6 cases): 4 cron schedules + ScheduledCommitmentDispatch TypedDict + lifecycle state machine + idempotency + retry policy + audit-first INSERT + typed exception envelope + 4 industries baseline dispatch policy
  - `tests/api/core/test_phase_18_audit_action.py` (8 cases): 8 NEW audit log entries + ActionClass.FINOPS_COMMITMENT + emit_audit_typed CR 1-1
  - `tests/api/core/test_phase_18_commitment_rbac.py` (6 cases): Role.COMMITMENT_VIEWER + require_commitment_role + tenant-scoped RBAC + Epic 12 2FA 챌린지 + tenant-level override delegation_policy + carry-over 검증
  - `tests/integration/test_finops_commitment_tenant_isolation.py` (6 cases): cross-tenant isolation + commitment inventory isolation + commitment KPI isolation + commitment report isolation + scheduled dispatch isolation + commitment metadata isolation
  - `tests/integration/test_capability_matrix_v1_44_drift.py` (8 cases): FINOPS_COMMITMENT enum + 4-industry grants + v1.43 + v1.42 + ... preservation
  - `tests/api/core/test_phase_18_commitment_5cloud_provider.py` (6 cases): AWS + Azure + GCP + Naver Cloud + KT Cloud integration + 5 cloud provider coverage matrix + currency conversion KRW
  - **Subtotal**: ~62 NEW pytest PASS

- **~7 NEW vitest PASS 결정 wire**:
  - `apps/web/__tests__/finops-commitment/finops-commitment-dashboard.test.tsx` (5 cases): CommitmentInventoryAggregator + CommitmentKPISelector + CommitmentReportGeneratorPanel + ScheduledCommitmentDispatchConfigPanel + CommitmentTrendMiniChart
  - `apps/web/__tests__/i18n/finops-commitment-i18n-ssot.test.ts` (2 cases): ko-KR SSOT drift detection + CR 12-5 D-PARITY-01 verification
  - **Subtotal**: ~7 NEW vitest PASS

- **0 NEW ruff 결정 wire** (apps/api backend 결정 wire + 기존 ruff scoped 0 NEW 정합 보존)
- **0 NEW tsc 결정 wire** (apps/web frontend 결정 wire + 기존 tsc 0 NEW 정합 보존)
- **0 regressions 결정 wire** (3중 게이트 FINAL CLEAN + ruff scoped 0 NEW + pytest 0 NEW failures + vitest 0 NEW failures + tsc 0 NEW errors)

## Notes

- `apps/api/main.py` EXTENSION 시 /admin/finops/commitment/* endpoints EXTENSION + require_finops_commitment dep 적용
- `apps/api/core/errors.py` EXTENSION 시 16 NEW typed exception classes + envelope CR 11-4 P-015 적용
- `apps/api/core/audit_action.py` EXTENSION 시 ActionClass.FINOPS_COMMITMENT + FinopsCommitmentAction Literal 8 NEW values + _ActionRegistry FINOPS_COMMITMENT entry 1 신규 등록
- m18_finops_commitment.commitment_serializers NEW Phase 18 EXTENSION 결정 wire (Phase 17 wire `97cfe4e` m17_finops_sustainability.sustainability_serializers EXTENSION pattern verbatim 미러, wire 시점에 sprint-status.yaml action_items EXTENSION)
- Phase 11 wire `e020ad0` 의 showback department breakdown + chargeback department cost center mapping 의 자연스러운 carry-over chain 결정 wire (Phase 18 CommitmentInventory 의 Phase 11 showback_total_krw → commitment coverage EXTENSION territory 결정 wire)
- Phase 12 wire `f3c0e63` 의 anomaly detection severity classification + budget_alert 의 자연스러운 carry-over chain 결정 wire (Phase 18 KPI utilization anomaly detection EXTENSION territory 결정 wire)
- Phase 13 wire `8b98030` 의 forecast accuracy tracker MAE/MAPE/RMSE + capacity headroom analysis 90일 lookahead 의 자연스러운 carry-over chain 결정 wire (Phase 18 KPI forecast commitment projection + expiring_commitments_30d EXTENSION territory 결정 wire)
- Phase 14 wire `e904485` 의 optimization accuracy tracker precision/recall/realized savings + commitment_recommender 1y/3y break-even + idle resource detection 의 자연스러운 carry-over chain 결정 wire (Phase 18 KPI recommended_purchase_krw + idle_commitment_krw + renewal_decision_score EXTENSION territory 결정 wire)
- Phase 15 wire `1b800d9` 의 compliance report + tag_governance compliance trend 의 자연스러운 carry-over chain 결정 wire (Phase 18 commitment allocation rule EXTENSION territory 결정 wire)
- Phase 16 wire `81ae00a` 의 executive dashboard cross-module KPI selector + executive report generation engine 의 자연스러운 carry-over chain 결정 wire (Phase 18 CommitmentInventory 의 7-module cross-rollup + commitment report generation engine EXTENSION territory 결정 wire)
- Phase 17 wire `97cfe4e` 의 carbon emissions cross-rollup + sustainability KPI 의 자연스러운 carry-over chain 결정 wire (Phase 18 KPI carbon-aware commitment 권고 EXTENSION territory 결정 wire)
- Phase 8 wire `60d4ea1` 의 cost-engine V8 골든 fixture + 12-period benchmark 의 자연스러운 carry-over chain (Phase 18 CommitmentInventory 의 7-module cross-rollup territory EXTENSION 결정 wire)
- Phase 10 wire `ac5d6c5` 의 4 SLIs 자연스러운 EXTENSION 결정 wire + Phase 9 wire `e7670e1` chaos_experiment baseline EXTENSION 결정 wire (Phase 18 dispatch retry policy EXTENSION territory 결정 wire)
- Phase 7 wire `59b56cd` observability 의 Prometheus custom metrics + Slack channel EXTENSION 결정 wire (Phase 18 commitment report delivery 의 Slack `#bizup-commitment-reports` channel EXTENSION territory 결정 wire)
- Epic 12 2FA 챌린지 mandatory 결정 wire (commitment dashboard view + commitment report generation + scheduled dispatch config 모두 Epic 12 2FA 챌린지 mandatory)
- AD-22 owner-only RBAC 보존 결정 wire (commitment dashboard view + commitment report generation + scheduled dispatch config 모두 owner-only)
- AD-14 stack pin 결정 wire (Recharts 2.12.7 + reportlab==4.0.7 + openpyxl==3.1.2 + pandas==2.1.4 + xlsxwriter==3.1.9 + apscheduler==3.10.4 + pytz==2024.1)
- NFR4 PII minimization PRESERVED (commitment reporting data 는 사업 metric + commitment amount + KPI value 만 포함, PII 미포함)
- 3중 게이트 impact NONE (cj-style 134번째 spec entry 진입 표준 = docs only 변경): ruff scoped 0 NEW + pytest 0 NEW + vitest 0 NEW + tsc 0 NEW
- 8 ACs PRD §F34.1~§F34.8 verbatim → ~86 sub-ACs (11+11+11+11+11+10+12+10 = 86 sub-ACs) satisfied pre-flight 정합 sweep 결정 wire
- 5 cloud provider cross-rollup (AWS + Azure + GCP + Naver Cloud + KT Cloud) AWS Cost Explorer API + Azure Consumption API + GCP Cloud Billing API + Naver Cloud billing API + KT Cloud billing API 결정 wire
- 5-framework support (FinOps Foundation + AWS Cost Optimization Pillar + Azure Cost Optimization + GCP Cost Optimization + 한국 조달청 클라우드 commitment 가이드라인) 결정 wire

## Cross-References

- Phase 18 PRD entry `5eded22` (cj-style 133번째) — FinOps Cloud Commitment Management (RIs/SPs/CUDs) territory 정합
- Phase 17 close-out retro `de009fe` (cj-style 132번째) — D-FINOPS-8 honestly DEFER 보존 해소
- Phase 17 atomic wire T1~T8 `97cfe4e` (cj-style 131번째) — FinOps Sustainability & Carbon Reporting territory 정합
- Phase 17 spec entry `4be3120` (cj-style 130번째)
- Phase 17 PRD entry `e0778ed` (cj-style 129번째)
- Phase 16 close-out retro `26fd530` (cj-style 128번째)
- Phase 16 atomic wire T1~T8 `81ae00a` (cj-style 127번째) — FinOps Reporting & Executive Dashboard territory 정합
- Phase 16 spec entry `69c29df` (cj-style 126번째)
- Phase 16 PRD entry `4f11d03` (cj-style 125번째)
- Phase 15 close-out retro `102f370` (cj-style 124번째)
- Phase 15 atomic wire T1~T8 `1b800d9` (cj-style 123번째) — FinOps Tag Governance & Cost Allocation territory 정합
- Phase 14 close-out retro `5b367d9` (cj-style 120번째)
- Phase 14 atomic wire T1~T8 `e904485` (cj-style 119번째) — FinOps Optimization & Rightsizing territory 정합
- Phase 13 close-out retro `850b4f8` (cj-style 116번째)
- Phase 13 atomic wire T1~T8 `8b98030` (cj-style 115번째) — FinOps Forecasting & Capacity Planning territory 정합
- Phase 12 close-out retro `3354e83` (cj-style 112번째)
- Phase 12 atomic wire T1~T8 `f3c0e63` (cj-style 111번째) — Cost Anomaly Detection & Budget Alerting territory 정합
- Phase 11 close-out retro `80df15b` (cj-style 108번째)
- Phase 11 atomic wire T1~T8 `e020ad0` (cj-style 107번째) — FinOps Showback / Chargeback territory 정합
- Phase 10 close-out retro `733d428` (cj-style 104번째)
- Phase 10 wire `ac5d6c5` (cj-style 103번째) — SLO Engineering / Error Budget Management territory 정합
- Phase 9 wire `e7670e1` (cj-style 99번째) — Chaos Engineering / Game Day territory 정합
- Phase 8 wire `60d4ea1` (cj-style 95번째) — cost-engine V8 골든 fixture + 12-period benchmark EXTENSION
- Phase 7 wire `59b56cd` (cj-style 91번째) — observability 정합
- Phase 5 wire `f093f8c` (cj-style 75번째) — multi-region failover + replication_lag 정합
- Epic 12 2FA 게이트 `a63646c` — Epic 12 2FA 챌린지 mandatory
- Epic 1 carry-over (auth) — onboarding/industry 보존
- AD-14 stack pin — Recharts 2.12.7 + reportlab==4.0.7 + openpyxl==3.1.2 + pandas==2.1.4 + xlsxwriter==3.1.9 + apscheduler==3.10.4 + pytz==2024.1
- AD-22 owner-only RBAC — commitment dashboard view + commitment report generation + scheduled dispatch config
- AD-45 FinOps Cloud Commitment Management (RIs/SPs/CUDs) 신규 (a)~(g) 7 sub-decisions
- NFR18 ko-KR — SSOT only invariant
- NFR4 PII minimization — commitment reporting data PII 미포함
- CR 0-2 RLS lesson, CR 1-1 audit-first INSERT, CR 4-3/4-4 lessons carry, CR 1-1 ContextVar, CR 1-1 RSC boundary, CR 9-6 commit message, CR 11-3 honest-DEFER, CR 11-4 D-001~D-005 + P-015, CR 12-1 L4 industry-agnostic capability, CR 12-5 D-14 envelope, CR 12-5 D-PARITY-01, CR 12-5 D-GATE-01, A19 cohesion 9 surface EXTENSION PASS, A36 SDR 검증 4-step 자동 적용
- m18_finops_commitment.commitment_serializers NEW Phase 18 EXTENSION 결정 wire (wire 시점에)

## 결정 wire 일자

2026-08-25 (KST)

## next (wire 진입 시)

옵션 (a) Phase 18 bmad-dev-story atomic wire T1~T8 진입 (cj-style 135번째 wire 진입 시점) 결정 wire 진입 / 옵션 (b) Phase 18 close-out retro 진입 (cj-style 136번째) / 옵션 (c) Epic 19+ 진입 / 옵션 (d) D-DEFER-* follow-up 진입 결정 wire 보류.