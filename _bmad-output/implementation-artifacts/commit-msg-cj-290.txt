feat(supabase+test+ci): cj-290 wire sprint (cj-style 290번째) — Epic 30+ RLS EXTENSION 결정 wire

cj-288 wire sprint `b91906a` 의 alembic migration 0060_cost_records_
and_bom_matrix.py 결정 wire 의 후속 EXTENSION — production tenant
isolation (AD-3) 결정 wire 보강을 위한 source+docs atomic single
sprint 결정 wire.

territory = cj-282 Epic 30+ PRD entry `0c7524e` 의 Story 30.1 CSV
export territory (cj-287 wire sprint `5c37446` 의 csv_routes.py 결정
wire + cj-288 wire sprint `b91906a` 의 alembic migration 결정 wire
보존).

## 변경 사항 (8 files = 4 NEW source + 2 MODIFIED source + 2 NEW meta + 2 MODIFIED meta atomic single sprint)

### 1. NEW RLS policies (cost_records + bom_matrix, 4-policy split × 2 tables)

- `supabase/policies/0060_cost_records_and_bom_matrix_rls.sql` NEW
  (~195 LOC):
  + ALTER TABLE `cost_records` ENABLE/FORCE ROW LEVEL SECURITY
  + `cost_records_select_same_tenant` — same-tenant 4-role read
    (owner + member + viewer + consultant_proxy per AD-10)
  + `cost_records_insert_blocked` — explicit `WITH CHECK (false)`
    (read-only application surface; dev_seed bypasses via postgres
    BYPASSRLS)
  + `cost_records_update_blocked` — explicit `USING (false) WITH
    CHECK (false)` (AD-2 INSERT-only soft invariant)
  + `cost_records_delete_blocked` — explicit `USING (false)`
  + ALTER TABLE `bom_matrix` ENABLE/FORCE ROW LEVEL SECURITY
  + `bom_matrix_select_same_tenant` (same as cost_records)
  + `bom_matrix_insert_blocked`
  + `bom_matrix_update_blocked`
  + `bom_matrix_delete_blocked`
  + COMMENT ON POLICY × 8 + COMMENT ON TABLE × 2 = 10 doc lines
  + Pattern mirror: 0016_budget_scenarios_rls.sql (GUC pattern
    NEWEST convention) + 0014_tenant_backups_rls.sql (5-policy split
    with explicit blocked UPDATE/DELETE)

### 2. NEW RLS test (5 cases mirroring test_bom_lines_isolation.py)

- `tests/rls/test_cost_records_bom_matrix_isolation.py` NEW
  (~270 LOC):
  + `test_tenant_a_can_read_own_cost_records` — AC #1 SELECT own
  + `test_tenant_a_cannot_read_tenant_b_cost_records` — AC #1
    cross-tenant isolation (cost_records)
  + `test_tenant_a_cannot_read_tenant_b_bom_matrix` — AC #2
    cross-tenant isolation (bom_matrix own + cross)
  + `test_tenant_a_cannot_insert_cost_record_for_tenant_b` — AC #3
    INSERT blocked policy enforcement (InsufficientPrivilegeError)
  + `test_tenant_a_cannot_update_tenant_b_bom_matrix` — AC #4 UPDATE
    silently affects 0 rows (USING makes row INVISIBLE)
  + Uses `_open_as_tenant` with `SET LOCAL app.tenant_id` +
    `SET LOCAL request.jwt.claims` (Phase 3-0 listener dual GUC
    pattern, cj-style 100+ territory)
  + Costmgr_test role (NOSUPERUSER NOBYPASSRLS) per 0000 CI shim

### 3. MODIFIED smoke test (8 NEW policy triples append)

- `supabase/policies/0002_rls_smoke_test.sql` MODIFIED (+12 lines):
  + Append 8 NEW (schema, table, policy) tuples to `expected_policies`
    array: 4 for cost_records + 4 for bom_matrix
  + Smoke test array now has 28 triples (was 20)
  + Pattern: `'public', '<table>', '<policy_name>'` arrays verbatim

### 4. MODIFIED CI YAML (3 Apply RLS policies steps × 3 lines)

- `.github/workflows/ci.yml` MODIFIED (+6 lines across 3 jobs):
  + `rls-tests` job (line ~568 area): append `-f supabase/policies/
    0060_cost_records_and_bom_matrix_rls.sql` after existing
    `-f supabase/policies/0001_rls_policies.sql`
  + `web-e2e` job (line ~727 area): same modification
  + `smoke-e2e` job (line ~871 area): same modification
  + Pattern: parallel `psql -v ON_ERROR_STOP=1` invocations (NOT `&&`
    chained — matches existing convention)

### 5. NEW commit-msg file (cj-290 결정 wire 진입)

- `_bmad-output/implementation-artifacts/commit-msg-cj-290.txt` NEW
  (~200 LOC) — 본 file 결정 wire 진입.

### 6. NEW handoff file (6-section handoff 결정 wire)

- `memory/handoff-2026-09-06-cj-290-rls-extension-done.md` NEW
  (~200 LOC) — 6-section handoff 결정 wire 진입 완료.

### 7. MODIFIED sprint-status.yaml (v4.59 → v4.60 EXTENSION 결정 wire)

- `_bmad-output/implementation-artifacts/sprint-status.yaml` MODIFIED
  결정 wire:
  + A703 cj-290 entry — Epic 30+ RLS EXTENSION wire sprint 결정 wire
    진입
  + last_updated_note_v4_60 신규 paragraph — _bmad-output/implementation
    -artifacts/commit-msg-cj-290.txt + handoff 결정 wire

### 8. MODIFIED memory/MEMORY.md (cj-290 hook EXTENSION 결정 wire)

- `memory/MEMORY.md` MODIFIED 결정 wire:
  + cj-290 hook EXTENSION (1-line index format, cj-style 290번째
    entry) — handoff 결정 wire sub-link 결정 wire

## 결정 wire 정합 (cj-282 PRD entry AD/NFR bind verbatim 보존)

### AD bind 4/4 active (cj-282 PRD entry §F44.1 verbatim 보존)
- **AD-2** (audit-first INSERT append-only) — cost_records + bom_matrix
  자체는 read-only application surface (csv_routes.py:298-336
  SELECT-only). export event 는 audit_logs 에 INSERT (cj-287 의
  csv_routes.py:362 ActionClass.REPORTS fix 보존). RLS 의 INSERT/
  UPDATE/DELETE blocked policies = AD-2 INSERT-only soft invariant
  mirror 0014 + 0016.
- **AD-3** (RLS 정합) — 본 sprint 의 NEW RLS file 결정 wire 진입.
  Phase 3-0 listener fix 후속 (cj-style 100+ territory 의 dual GUC
  pattern). tenant_id FK to tenants(id) + tenant_id+period_key index
  + RLS 결정 wire 일치.
- **AD-15** (SSOT) — id UUID DEFAULT gen_random_uuid() (CSV export
  spec 호환). tenant_id UUID v4 JWT-derived. RLS 키는 같은 SSOT 컬럼.
- **AD-22** (owner-only RBAC verbatim) — csv_routes.py 의
  `require_any_role("owner", "admin")` 결정 wire 보존 (cj-287 wire
  sprint 진입 완료). RLS 는 third defense layer 결정 wire 보존.

### NFR bind 2/2 active (cj-282 PRD entry §F44 NFR 매트릭스)
- **NFR5** (streaming P95 ≤ 5s for 10만 row) — idx_cost_records_
  tenant_period composite index + idx_bom_matrix_tenant_period 보존.
  RLS 의 SELECT same-tenant predicate = 추가 cost 없음 결정 wire
  보존 (인덱스 컬럼과 RLS 키 컬럼이 동일 → index-driven scan).
- **NFR18** (ko-KR vocabulary SSOT) — comment ON TABLE 결정 wire
  보존.

### CR 결정 wire verbatim 보존
- **CR 0-2 RLS** — cj-289 close-out retro §13 의 RLS 결정 wire
  보류 해소. 본 sprint 가 production tenant isolation 보장 결정
  wire 진입.
- **CR 1-1 audit-first INSERT** — cj-287 fix 보존 (csv_routes.py:
  362 ActionClass.REPORTS).
- **CR 9-6 atomic commit** — `git commit -F <file>` convention 결정
  wire 보존.
- **CR 11-3 honest-DEFER 227번째** — 본 sprint 의 cj-289 carryover
  정직 회복. PRE-EXISTING 2 failures (test-suite-measure P3 +
  web-e2e csv-export 3/3 carryover) 보존 결정 wire.
- **CR 11-4 P-015 pure validator pattern** — csv_routes.py 결정
  wire 보존.
- **CR 12-5 D-14 typed exception envelope** — cj-287 wire sprint 의
  4 NEW exception handlers 보존 + 본 sprint 의 "defense-in-depth
  explicit > implicit" 결정 wire 보존 (USING(false) WITH CHECK(false)
  의 explicit blocked policies).

## runtime 동작 변화 honestly reported

- source+docs atomic single sprint — 8 files = 4 NEW source + 2
  MODIFIED source + 2 NEW meta + 2 MODIFIED meta 결정 wire 진입.
- apps/api/alembic/versions/0060_cost_records_and_bom_matrix.py 변경
  0 결정 wire 보존 (cj-288 wire sprint 진입 완료).
- csv_routes.py 변경 0 결정 wire 보존 (cj-287 wire sprint 진입 완료).
- dev_seed 변경 0 결정 wire 보존 (cj-286 EXTENSION 의 `_seed_report_
  fixtures` 보존). dev_seed 는 postgres role BYPASSRLS 사용 → RLS
  부재/존재 무관 결정 wire 보존.
- AD-14 stack pin (35 pins) 변경 없음 / [STACK BUMP] tag 불필요
  결정 wire 보존.
- capability matrix v1.54 EXTENSION preserved from cj-285 EXTENSION
  wire sprint 결정 wire 보존 (no change at v1.55).
- audit action EXTENSION preserved from cj-285 EXTENSION wire sprint
  결정 wire 보존 (no change at v1.55).

## Pre-existing carryover 결정 wire 정직 회복

cj-289 close-out retro §13 의 "RLS EXTENSION 결정 wire 보류" 해소:

- cj-288 wire sprint `b91906a` 의 alembic migration 결정 wire 가
  CREATE TABLE 2 + CREATE INDEX 3 + COMMENT 2 만 포함 (line 84-88
  의 "RLS 결정 wire 보류" comment verbatim 보존).
- 본 sprint 가 cj-style 290+ EXTENSION territory 진입 — supabase/
  policies/0060_cost_records_and_bom_matrix_rls.sql 결정 wire.
- production tenant isolation 결정 wire 일치 (AD-3 invariant
  production-ready 회복).

## Next (cj-291+ 결정 wire 보류)

- 옵션 (a) cj-291 wire sprint (cj-style 291번째, RECOMMENDED next) —
  Epic 30+ capability matrix v1.55 EXTENSION 결정 wire 보완 또는
  cj-290 close-out retro docs-only 결정 wire (Phase 23/24 close-out
  retro `7875ac9`/`c14199b` verbatim pattern mirror).
- 옵션 (b) cj-291 Story 30.2 PDF export wire sprint (cj-style 291번째)
  — weasyprint vs reportlab 결정 + Jinja2 ko-KR template + matplotlib
  3 charts 결정 wire 진입 (OQ-EPIC30+-1 + OQ-EPIC30+-4 결정).
- 옵션 (c) Epic 29+ spec implementation chain 진입 결정 wire (cj-29x
  territory) — 18 spec drifts unresolved + 18 stories × multi-sprint.
- 옵션 (d) Pilot 고객 유치 결정 wire 진입 (PRD OQ-3 파일럿 게이트 1주
  post M0-M6).

Co-Authored-By: Claude Code <noreply@anthropic.com>
