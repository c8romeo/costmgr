ci(workflows): test-suite-measure 차단 게이트 승격 (PRD OQ-3 pilot gate decision)

결정 근거:
MVP-scope 실패 = 0 확인됨 (사용자 명시 결정). cj-style baseline-green
effort 4 sprint 누적:
  - 1차 (commit 4ca3355): 비차단 측정 job 신설 (continue-on-error: true)
  - 2차 (commit 349d227): async convention 변환
    4,985 → 5,089 passed, 173 → 69 failed
  - 3차 (commit 98baafa): MVP-scope 10건 triage
    5,089 → 5,098 passed, 69 → 59 failed
  - 4차 (commit f006e6c): Epic 15 SSO test path doubling 픽스
    5,098 → 5,112 passed, 59 → 45 failed

현재 기준선 (2026-09-06, uv run python -m pytest, Windows):
  5,112 passed / 45 failed / 130 skipped / 28 collection errors
  MVP-scope = 0, 잔여 45 = 비-MVP (Phase 5·8·9·10·26 enterprise SRE
  + capability matrix drift + SDR/commit/label drift) — PRD §15
  1차 로드맵 밖, 별도 cj-style sprint 트랙

변경 사항:
1. continue-on-error: true 제거 → job 이 pytest 실패 시 차단 게이트
   역할 수행
2. 스크립트 수정: PYTEST_EXIT 변수로 pytest exit code 캡처 후
   `exit "${PYTEST_EXIT}"` 로 step exit 에 그대로 전파. 기존 `set +e`
   + `${PIPESTATUS[0]}` echo 만으로는 step 이 항상 0 으로 종료되어
   게이트로 기능하지 않음
3. 주석 블록 갱신: "비차단" → "BLOCKING GATE (PRD OQ-3 pilot gate)"
   + 4 sprint 진화 이력 + 새 기준선 + 비-MVP 분리 메모
4. step 이름 갱신: "(measurement only — does not gate)" →
   "(BLOCKING — MVP gate; non-MVP 회귀 표면화)"
5. STEP SUMMARY 텍스트 갱신: "비차단" → "BLOCKING GATE", 기준선
   표기를 (2026-09-05) 와 (2026-09-06) 두 날짜 모두 노출하여
   진화 이력 명시

runtime 동작 변화:
- ci.yml 변경: +28 / -15 (주석 + 스크립트 정정)
- 다른 13 jobs: 변경 0
- 비-MVP 45 실패가 다음 PR 부터 빨간불로 가시화 — 비-MVP 트랙
  진입의 자연스러운 트리거
- product code 변경 0, dev_seed 변경 0, AD-14 pin 변경 0
- [STACK BUMP] 태그 불필요

영향 범위:
- 신규 PR 이 비-MVP 영역 (예: Phase 8 p99_budget 테스트 수정) 을
  건드릴 때 자동으로 빨간불 — 명시적 opt-in 으로 의도 표면화
- MVP-scope 회귀 (예: 새 async-violator 도입, 새 FLOWS 이름
  rename, PRD §9 #15 payload inversion 등) 도 즉시 표면화

Co-Authored-By: Claude Code <noreply@anthropic.com>
