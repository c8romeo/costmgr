---
title: bizup 통합 PRD v7.0
status: final
created: 2026-07-12
updated: 2026-08-26
changelog:
  - v7.0 (2026-08-26): Phase 21 PRD entry 결정 wire DONE (cj-style 149번째 epic 연속 정직 회복 atomic docs-only wire) — 옵션 (a) Phase 21+ 진입 결정 wire (Phase 20.5 close-out retro `8505d98` 148번째 + Phase 20.5 atomic wire T1~T3 `46ddcc5` 147번째 + Phase 20.5 spec entry `e23141d` 146번째 + Phase 20 close-out retro `f361016` 145번째 + Phase 20 atomic wire T1~T8 `52dad7f` 144번째 + Phase 20 spec entry `bdc7997` 143번째 + Phase 20 PRD entry `ff8a797` 142번째 모두 wire DONE 진입 정합 보존 후 다음 territory 진입). 사용자 권장 결정 = 옵션 (a) Phase 21+ 진입 + 옵션 (a) FinOps Reserved Capacity Planning (Recommended) (demand_forecast_aggregator + capacity_planning_aggregator + commitment_recommendation_engine + reserved_capacity_orchestrator 4 NEW backend aggregator + reserved_capacity_dashboard UI 5 sub-components + Capability matrix v1.46 → v1.47 EXTENSION FINOPS_RESERVED_CAPACITY_PLANNING + audit-first INSERT 8 NEW + 16 NEW typed exceptions + dry-run + Tests + wire scope T1~T8) = Phase 21 territory 결정 wire 진입 (cj-style 149번째 epic 연속 정직 회복 진입 시점에 결정). rationale 5종: ① Phase 13 wire `8b98030` FinOps Forecasting & Capacity Planning + Phase 14 wire `e904485` FinOps Optimization & Rightsizing + Phase 18 wire `67059cf` FinOps Cloud Commitment Management + Phase 19 wire `8db3cfc` FinOps Pricing, Rate Card & TCO Modeling + Phase 20 wire `52dad7f` FinOps Multi-Cloud Cost Unified Reconciliation territory 의 natural closure layer "Operate" FinOps Foundation framework 결정 wire 진입 (forecast demand → optimize rightsizing → recommend commitment → execute negotiation → track multi-cloud reconciliation → ACT reserved capacity plan = Phase 13→14→18→19→20 cycle 의 자연스러운 composition layer 신규 — 신규 backend aggregator 작성 없이 기존 Phase 13/14/18 aggregator modules 직접 compose 만으로 구현 가능) ② cj-style discipline 회피 위험 방지 (Phase 11~20 10-phase FinOps chain 완료 직후 즉시 Phase 21 진입 = 1-day atomic sprint discipline = 49~148번째 누적 100-entry-point cycle 후 149번째 진입 정직 회복 discipline 유지) ③ 비즈니스 우선순위 = enterprise 고객 onboarding 시 demand_forecast → capacity_planning → commitment_recommendation → reserved_capacity_orchestrator 통합 decision engine territory 필수 (Phase 13 12-month forecast horizon 의 reserved_capacity_demand_forecast_krw + Phase 14 rightsizing + commitment_recommender 1y/3y break-even 의 capacity_headroom_pct + Phase 18 commitment_inventory 의 expiring_commitments_30d + renewal_decision_score 의 reserved_capacity_commitment_recommendation_krw + Phase 19 pricing 의 tco_1year_commitment_krw + tco_3year_commitment_krw 의 reserved_capacity_tco_optimization_krw + Phase 20 multi_cloud reconciliation 의 cost_variance_total_krw 의 reserved_capacity_multi_cloud_attribution_krw) ④ Phase 20 close-out retro `f361016` 의 D-FINOPS-9 honestly DEFER 의 7개 세부 항목 모두 Phase 20 territory 흡수 완료 + Phase 20.5 close-out retro `f361016` + `8505d98` 의 4 honest deviations (router include ✅ DONE + test backfill DEFER + docs backfill DEFER + scripts backfill DEFER) 모두 carry-over chain 정직 회복 결정 wire 진입 + Phase 21 Reserved Capacity Planning territory 의 composition layer 가 Phase 13/14/18/19/20 5개 territory 의 wired aggregator modules 를 직접 재사용하여 신규 backend 코드 최소화 (estimated ~+25-30 files = ~+18 NEW + ~+12 MODIFIED) + AD-49 FinOps Reserved Capacity Planning 신규 결정 ((a)~(g) 7 sub-decisions 모두 결정 wire 진입) ⑤ 1st release close-out retro + Epic 17 close-out retro + Phase 5~20.5 close-out retro territory verbatim 보존 + AD-22 owner-only RBAC + Epic 12 2FA 챌린지 보존 + AD-14 stack pin + NFR4 PII minimization ✅ PRESERVED + AD-49 FinOps Reserved Capacity Planning 신규 결정 ((a)~(g) 7 sub-decisions 모두 결정 wire 진입). master PRD v6.0 → v7.0 atomic edit (docs only). **§F37 FinOps Reserved Capacity Planning territory 신규** 결정 wire 진입 (F37.1 demand_forecast_aggregator 5 FinOps modules (Phase 13 + Phase 14 + Phase 18 + Phase 19 + Phase 20) cross-join EXTENSION + F37.2 capacity_planning_aggregator 5-source reconciliation + F37.3 commitment_recommendation_engine reserved capacity tier calculation + F37.4 reserved_capacity_orchestrator composition layer + F37.5 reserved_capacity dashboard UI 5 sub-components + F37.6 Capability matrix v1.47 EXTENSION FINOPS_RESERVED_CAPACITY_PLANNING + F37.7 audit action EXTENSION 8 NEW + 16 NEW typed exception classes + F37.8 dry-run + Tests + wire scope T1~T8). §15 로드맵 Phase 21 row status 백로그 → in-progress (PRD entry DONE 진입 wire) 결정. AD-49 신규 결정. capability matrix v1.46 → v1.47 EXTENSION FINOPS_RESERVED_CAPACITY_PLANNING 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅. CR 11-3 honest-DEFER discipline 149번째 epic 연속 정직 회복 검증 ✅ (D-1-1-DEFER-1/2/3 RESOLVED 보존 60~149번째 + D-LAUNCH-1-DEFER-1 honestly preserved 65~149번째 + D-EPIC-16-REVIEW-DEFER-2~6 ✅ RESOLVED 보존 78~149번째 + D-PHASE-4-DR-DEFER-1/2 ✅ RESOLVED 보존 73~149번째 + D-EPIC-17-WIRE-DEFER-T2-T3-UI ✅ RESOLVED 보존 83~149번째 + D-RETENTION-1 ✅ RESOLVED 보존 85~149번째 + D-OBSERVABILITY-1 ✅ RESOLVED 보존 89~149번째 + D-PERFORMANCE-1 ✅ RESOLVED 보존 93~149번째 + D-CHAOS-1 ✅ RESOLVED 보존 100~149번째 + D-SLO-1 ✅ RESOLVED 보존 101~149번째 + D-FINOPS-1 ✅ RESOLVED 보존 105~149번째 + D-FINOPS-2 ✅ RESOLVED 보존 109~149번째 + D-FINOPS-3 ✅ RESOLVED 보존 113~149번째 + D-FINOPS-4 ✅ RESOLVED 보존 117~149번째 + D-FINOPS-5 ✅ RESOLVED 보존 121~149번째 + D-FINOPS-6 ✅ RESOLVED 보존 125~149번째 + D-FINOPS-7 ✅ RESOLVED 보존 129~149번째 + D-FINOPS-8 ✅ RESOLVED 보존 133~149번째 + D-FINOPS-9 ✅ RESOLVED 보존 137~149번째 + **D-FINOPS-10 신규 honestly DEFER 보존 = Phase 21 PRD entry 진입 시점에 carry-over chain 정직 회복 결정 wire 진입: Phase 20.5 close-out retro `8505d98` §13 + Phase 20 close-out retro `f361016` §13 + Phase 20 wire `52dad7f` §13 + Phase 19 close-out retro `18ca1ae` §13 + Phase 19 wire `8db3cfc` §13 + Phase 18 close-out retro `de72f50` §13 + Phase 17 close-out retro `de009fe` §13 + Phase 16 close-out retro `26fd530` §13 + Phase 15 close-out retro `102f370` §13 + Phase 14 close-out retro `5b367d9` §13 + Phase 13 close-out retro `850b4f8` §13 + Phase 12 close-out retro `3354e83` §13 + Phase 11 close-out retro `80df15b` §12 + Phase 10 close-out retro `733d428` §10 + Phase 9 close-out retro `634427d` §10 + Phase 8 close-out retro `ab495a8` §10 + Phase 7 close-out retro `326fa9f` §10 + Phase 6 close-out retro `f9f006c` §13 + Epic 17 close-out retro `be8f3bd` §11 "FinOps Reserved Capacity Planning 결정 wire 보류, Phase 21+ 진입 시점" verbatim 해소**) + A36 SDR 검증 4-step 자동 적용 (commit prefix lint PASS + sprint-status structure PASS + vitest file count drift 0건 + commit consistency PASS) + CR 12-5 D-GATE-01 + D-PARITY-01 inversion 적용 보존 + A19 cohesion 9 surface EXTENSION PASS + CR 0-2 RLS lesson (Phase 21 finops_reserved_capacity tables RLS 자동 적용 + tenant-scoped reserved_capacity policy 결정) + CR 1-1 audit-first INSERT (reserved_capacity_dashboard_viewed + demand_forecast_calculated + capacity_planning_recommended + commitment_recommendation_generated + reserved_capacity_dry_run_executed + reserved_capacity_kpi_refreshed + reserved_capacity_commitment_executed + reserved_capacity_orchestrator_triggered 8 NEW CR 1-1 verbatim 적용) + CR 9-6 commit message discipline (`git commit -F <file>` 사용, PowerShell here-string 회피, D5 prevention) + NFR4 PII minimization ✅ PRESERVED + NFR18 ko-KR SSOT 모두 적용 보존. 결정 wire 일자: 2026-08-26 (KST).
  - v6.0 (2026-08-25): Phase 20 PRD entry 결정 wire DONE (cj-style 142번째 epic 연속 정직 회복 atomic docs-only wire) — 옵션 (a) Phase 20+ 진입 결정 wire (Phase 19 close-out retro `18ca1ae` 140번째 + Phase 19 atomic wire T1~T8 `8db3cfc` 139번째 + Phase 19 spec entry `59d15fb` 138번째 + Phase 19 PRD entry `ff8a797` 137번째 + Phase 19.5 carry-over 결정 wire `b2fb1d8` 141번째 모두 wire DONE 진입 정합 보존 후 다음 territory 진입). 사용자 권장 결정 = 옵션 (a) Phase 20+ 진입 + 옵션 (a) FinOps Multi-Cloud Cost Unified Reconciliation (Recommended) (5 cloud provider unified rate card reconciliation P0 + 5 cloud provider unified cost reconciliation P0 + AWS EDP 자동 negotiation bot P1 + Azure EA consumption commit reconciliation P1 + GCP CUD flexible/fixed tier break-even optimization P1 + Naver/KT public pricing API stability 검증 P2 + blended vs unblended 실시간 차이 추적 P2 + marketplace SaaS pricing 파편화 통합 P2 = Phase 20 territory 결정 wire 진입 (cj-style 142번째 epic 연속 정직 회복 진입 시점에 결정). rationale 5종: ① Phase 19 wire `8db3cfc` FinOps Pricing, Rate Card & TCO Modeling territory (rate_card_aggregator 5 cloud provider cross-rollup + tco_modeling_selector 8 NEW KPI + pricing report generation engine PDF + CSV + Excel + scheduled pricing dispatch KST cron + tenant-scoped pricing role RBAC owner-only) 의 natural backend MULTI-CLOUD COST UNIFIED RECONCILIATION LAYER EXTENSION 결정 wire 진입 (9 FinOps modules 의 data aggregate → multi-cloud unified source of truth view + 5 cloud provider (AWS EDP + Azure EA + GCP CUD Pricing + Naver Cloud Volume Tier + KT Cloud Volume Tier) cross-tenant unified cost reconciliation + auto-negotiation bot + marketplace SaaS pricing 통합) ② cj-style discipline 회피 위험 방지 (Phase 11~19 9-phase FinOps chain 완료 직후 즉시 Phase 20 진입 = 1-day atomic sprint discipline = 49~141번째 누적 93-entry-point cycle 후 142번째 진입 정직 회복 discipline 유지) ③ 비즈니스 우선순위 = enterprise 고객 onboarding 시 multi-cloud unified cost source of truth (5 cloud provider rate card reconciliation + 5 cloud provider cost reconciliation + AWS EDP 자동 negotiation + Azure EA consumption reconciliation + GCP CUD break-even optimization + Naver/KT pricing API stability + blended/unblended 실시간 차이 + marketplace SaaS pricing 통합) territory 필수 (Phase 19 pricing의 5 cloud provider cross-rollup → Phase 20 unified reconciliation EXTENSION + Phase 18 commitment의 RIs/SPs/CUDs → auto-negotiation bot EXTENSION + Phase 17 sustainability의 renewable_energy_pct → carbon-aware pricing reconciliation EXTENSION + Phase 16 reporting의 executive rollup → multi-cloud executive reconciliation EXTENSION + Phase 15 tag_governance의 allocation rule → multi-cloud allocation rule EXTENSION + Phase 14 optimization의 rightsizing + commitment_recommender → multi-cloud optimization EXTENSION + Phase 13 forecast의 12-month horizon → multi-cloud forecast EXTENSION + Phase 12 anomaly의 z-score 기반 → multi-cloud anomaly detection EXTENSION + Phase 11 showback의 cost center breakdown → multi-cloud cost center breakdown EXTENSION) ④ Phase 19 wire `8db3cfc` 의 5 cloud provider rate card cross-rollup (AWS EDP + Azure EA + GCP CUD Pricing + Naver Cloud Volume Tier + KT Cloud Volume Tier) + Phase 18 wire `67059cf` 의 5 cloud provider commitment cross-rollup (AWS EC2 RI + RDS RI + EC2 SP + S3 SP + Redshift SP + DynamoDB SP + Azure reservations + GCP CUDs + Naver Cloud + KT Cloud) + Phase 19 PRD entry `ff8a797` D-FINOPS-9 7개 세부 항목 (5 cloud provider unified rate card reconciliation P0 + AWS EDP 자동 negotiation bot P1 + Azure EA consumption commit reconciliation P1 + GCP CUD flexible/fixed tier break-even optimization P1 + Naver/KT public pricing API stability 검증 P2 + 5 cloud provider unified cost reconciliation P0 + blended vs unblended 실시간 차이 추적 P2 + marketplace SaaS pricing 파편화 통합 P2) priority 매트릭스 (P0 2개 + P1 3개 + P2 3개 = 7 unique 항목) 모두 Phase 20 territory 흡수 + Phase 19.5 carry-over 결정 wire `b2fb1d8` 의 AD-47 D-DEFER-* carry-over 신규 (a)~(g) 7 sub-decisions 결정 wire 진입 시점 verbatim 해소 ⑤ 1st release close-out retro + Epic 17 close-out retro + Phase 5~19 close-out retro territory verbatim 보존 + AD-22 owner-only RBAC + Epic 12 2FA 챌린지 보존 + AD-14 stack pin + NFR4 PII minimization ✅ PRESERVED + AD-47 FinOps Multi-Cloud Cost Unified Reconciliation 신규 결정 ((a)~(g) 7 sub-decisions 모두 결정 wire 진입). master PRD v5.0 → v6.0 atomic edit (docs only). **§F36 FinOps Multi-Cloud Cost Unified Reconciliation territory 신규** 결정 wire 진입 (F36.1 multi_cloud_rate_card_reconciliation_aggregator 5 cloud provider cross-rollup EXTENSION + F36.2 multi_cloud_cost_reconciliation_aggregator unified source of truth + F36.3 negotiation_bot AWS EDP + Azure EA + GCP CUD break-even + F36.4 blended_unblended_tracker + Naver/KT public pricing API stability 검증 + F36.5 marketplace_saas_pricing_integrator + F36.6 multi_cloud dashboard UI 5 sub-components + F36.7 Capability matrix v1.46 EXTENSION FINOPS_MULTI_CLOUD_UNIFIED_RECONCILIATION + F36.8 dry-run + Tests + wire scope T1~T8). §15 로드맵 Phase 20 row status 백로그 → in-progress (PRD entry DONE 진입 wire) 결정. AD-47 신규 결정. capability matrix v1.45 → v1.46 EXTENSION FINOPS_MULTI_CLOUD_UNIFIED_RECONCILIATION 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅. CR 11-3 honest-DEFER discipline 142번째 epic 연속 정직 회복 검증 ✅ (D-1-1-DEFER-1/2/3 RESOLVED 보존 60~142번째 + D-LAUNCH-1-DEFER-1 honestly preserved 65~142번째 + D-EPIC-16-REVIEW-DEFER-2~6 ✅ RESOLVED 보존 78~142번째 + D-PHASE-4-DR-DEFER-1/2 ✅ RESOLVED 보존 73~142번째 + D-EPIC-17-WIRE-DEFER-T2-T3-UI ✅ RESOLVED 보존 83~142번째 + D-RETENTION-1 ✅ RESOLVED 보존 85~142번째 + D-OBSERVABILITY-1 ✅ RESOLVED 보존 89~142번째 + D-PERFORMANCE-1 ✅ RESOLVED 보존 93~142번째 + D-CHAOS-1 ✅ RESOLVED 보존 100~142번째 + D-SLO-1 ✅ RESOLVED 보존 101~142번째 + D-FINOPS-1 ✅ RESOLVED 보존 105~142번째 + D-FINOPS-2 ✅ RESOLVED 보존 109~142번째 + D-FINOPS-3 ✅ RESOLVED 보존 113~142번째 + D-FINOPS-4 ✅ RESOLVED 보존 117~142번째 + D-FINOPS-5 ✅ RESOLVED 보존 121~142번째 + D-FINOPS-6 ✅ RESOLVED 보존 125~142번째 + D-FINOPS-7 ✅ RESOLVED 보존 129~142번째 + D-FINOPS-8 ✅ RESOLVED 보존 133~142번째 + **D-FINOPS-9 honestly DEFER 보존 + Phase 19.5 carry-over 결정 wire `b2fb1d8` 의 AD-47 신규 (a)~(g) 7 sub-decisions 모두 결정 wire 진입 = Phase 20 PRD entry 진입 시점에 carry-over chain 정직 회복 결정 wire 진입: Phase 19 close-out retro `18ca1ae` §13 + Phase 19.5 carry-over 결정 wire `b2fb1d8` §F35.5~§F35.13 + Phase 18 close-out retro `de72f50` §13 + Phase 17 close-out retro `de009fe` §13 + Phase 16 close-out retro `26fd530` §13 + Phase 15 close-out retro `102f370` §13 + Phase 14 close-out retro `5b367d9` §13 + Phase 13 close-out retro `850b4f8` §13 + Phase 12 close-out retro `3354e83` §13 + Phase 11 close-out retro `80df15b` §12 + Phase 10 close-out retro `733d428` §10 + Phase 9 close-out retro `634427d` §10 + Phase 8 close-out retro `ab495a8` §10 + Phase 7 close-out retro `326fa9f` §10 + Phase 6 close-out retro `f9f006c` §13 + Epic 17 close-out retro `be8f3bd` §11 "FinOps Multi-Cloud Cost Unified Reconciliation 결정 wire 보류, Phase 20+ 진입 시점" verbatim 해소**) + A36 SDR 검증 4-step 자동 적용 (commit prefix lint PASS + sprint-status structure PASS + vitest file count drift 0건 + commit consistency PASS) + CR 12-5 D-GATE-01 + D-PARITY-01 inversion 적용 보존 + A19 cohesion 9 surface EXTENSION PASS + CR 0-2 RLS lesson (Phase 20 finops_multi_cloud tables RLS 자동 적용 + tenant-scoped multi-cloud policy 결정) + CR 1-1 audit-first INSERT (multi_cloud_dashboard_viewed + multi_cloud_rate_card_reconciled + multi_cloud_cost_reconciled + negotiation_bot_triggered + marketplace_saas_pricing_integrated + multi_cloud_dry_run_executed + multi_cloud_kpi_refreshed + blended_unblended_tracked 8 NEW CR 1-1 verbatim 적용) + CR 9-6 commit message discipline (`git commit -F <file>` 사용, PowerShell here-string 회피, D5 prevention) + NFR4 PII minimization ✅ PRESERVED + NFR18 ko-KR SSOT 모두 적용 보존. 결정 wire 일자: 2026-08-25 (KST).
  - v5.0 (2026-08-25): Phase 19 PRD entry 결정 wire DONE (cj-style 137번째 epic 연속 정직 회복 atomic docs-only wire) — 옵션 (a) Phase 19+ 진입 결정 wire (Phase 18 close-out retro `de72f50` 136번째 + Phase 18 atomic wire T1~T8 `67059cf` 135번째 + Phase 18 spec entry `bdc7997` 134번째 + Phase 18 PRD entry `5eded22` 133번째 모두 wire DONE 진입 정합 보존 후 다음 territory 진입). 사용자 권장 결정 = 옵션 (a) Phase 19+ 진입 + 옵션 (a) FinOps Pricing, Rate Card & TCO Modeling (Recommended) (rate_card_aggregator 5 cloud provider (AWS EDP + Azure EA + GCP CUD Pricing + Naver Cloud Volume Tier + KT Cloud Volume Tier) cross-rollup + tco_modeling_selector 8 NEW KPI calculations (total_blended_rate_krw_per_hour + effective_discount_pct + tco_1year_commitment_krw + tco_3year_commitment_krw + tco_on_demand_krw + cost_per_user_krw + cost_per_transaction_krw + unit_economics_score) + pricing_report_generation_engine PDF + CSV + Excel + scheduled_pricing_dispatch KST cron + tenant_scoped_pricing_role RBAC owner-only + pricing dashboard UI 5 sub-components + ko-KR.json `finops_pricing.*` namespace EXTENSION ~30 keys + ARIA labels WCAG 2.1 AA + audit-first INSERT 8 NEW via ActionClass.FINOPS_PRICING (`pricing_dashboard_viewed` + `cross_module_pricing_kpi_calculated` + `pricing_report_generated` + `pricing_report_exported` + `pricing_report_dispatched` + `pricing_scheduled_dispatch_evaluated` + `finops_pricing_dry_run_executed` + `pricing_kpi_refreshed`) + Capability FINOPS_PRICING 1 NEW + dry-run mode 5 NEW CLI flags + tests + wire scope T1~T8) = Phase 19 territory 결정 wire 진입 (cj-style 137번째 epic 연속 정직 회복 진입 시점에 결정). rationale 5종: ① Phase 18 wire `67059cf` FinOps Cloud Commitment Management territory (commitment_inventory_aggregator 5 cloud provider cross-rollup + commitment_kpi_selector 8 NEW KPI selector + commitment report generation engine PDF + CSV + Excel + scheduled commitment dispatch KST cron + tenant-scoped commitment role RBAC owner-only) 의 natural backend PRICING & TCO MODELING LAYER EXTENSION 결정 wire 진입 (8 FinOps modules 의 data aggregate → pricing-grade TCO view + cross-module pricing KPI 8개 + scheduled pricing report export PDF+CSV+Excel weekly/monthly/quarterly/annual + rate_card cross-rollup + blended_rate_per_hour + effective_discount_pct + TCO 1y/3y 비교 + unit economics + AWS EDP + Azure EA + GCP CUD Pricing + Naver Cloud + KT Cloud 멀티 cloud pricing 지원) ② cj-style discipline 회피 위험 방지 (Phase 11~18 8-phase FinOps chain 완료 직후 즉시 Phase 19 진입 = 1-day atomic sprint discipline = 49~136번째 누적 88-entry-point cycle 후 137번째 진입 정직 회복 discipline 유지) ③ 비즈니스 우선순위 = enterprise 고객 onboarding 시 pricing 기반 TCO modeling (rate card 관리 + 1y vs 3y vs on-demand vs Savings Plans 비교 + blended/unblended rate 추적 + unit economics (cost per user, cost per transaction) + 한국 공공 조달 가격 가이드라인 정합) territory 필수 (Phase 18 commitment의 RIs/SPs/CUDs 구매 권고 → Phase 19 TCO modeling EXTENSION + Phase 17 sustainability의 renewable_energy_pct + carbon_offset → carbon-aware pricing 권고 EXTENSION + Phase 16 reporting의 executive rollup → pricing KPI rollup EXTENSION + Phase 15 tag_governance의 allocation rule → pricing allocation rule EXTENSION + Phase 14 optimization의 rightsizing + commitment_recommender → TCO break-even EXTENSION + Phase 13 forecast의 12-month horizon → pricing forecast EXTENSION + Phase 12 anomaly의 z-score 기반 → pricing anomaly detection EXTENSION + Phase 11 showback의 cost center breakdown → cost center pricing breakdown EXTENSION + AWS EDP + Azure EA + GCP CUD Pricing + Naver Cloud + KT Cloud 멀티 cloud rate card 지원) ④ Phase 18 wire `67059cf` 의 5 cloud provider commitment cross-rollup (AWS EC2 RI + RDS RI + EC2 SP + S3 SP + Redshift SP + DynamoDB SP + Azure reservations + GCP CUDs + Naver Cloud + KT Cloud) + Phase 14 wire `e904485` 의 commitment_recommender 1y/3y break-even + Phase 13 wire `8b98030` 의 12-month forecast + Phase 11 wire `e020ad0` 의 cost center cost allocation + Phase 12 wire `f3c0e63` 의 anomaly detection baseline 의 자연스러운 carry-over chain = Phase 19 pricing 의 rate_card_inventory (AWS EDP + Azure EA + GCP CUD Pricing + Naver Cloud Volume Tier + KT Cloud Volume Tier + marketplace) + blended_rate_per_hour (effective rate after discount) + effective_discount_pct (1 - blended/on_demand) + TCO 1y vs 3y vs on_demand (Phase 14 break-even EXTENSION) + cost_per_user_krw (unit economics) + cost_per_transaction_krw (unit economics) + unit_economics_score (composite KPI) + renewal_workflow (auto + manual + hold) EXTENSION 정합 ⑤ 1st release close-out retro + Epic 17 close-out retro + Phase 5~18 close-out retro territory verbatim 보존 + AD-22 owner-only RBAC + Epic 12 2FA 챌린지 보존 + AD-14 stack pin + NFR4 PII minimization ✅ PRESERVED + AD-46 FinOps Pricing, Rate Card & TCO Modeling 신규 결정 ((a)~(g) 7 sub-decisions 모두 결정 wire 진입). master PRD v4.9 → v5.0 atomic edit (docs only). **§F35 FinOps Pricing, Rate Card & TCO Modeling territory 신규** 결정 wire 진입 (F35.1 rate_card_aggregator 5 cloud provider cross-rollup EXTENSION + F35.2 tco_modeling_selector 8 NEW KPI selector + F35.3 pricing_report_generation_engine PDF + CSV + Excel + F35.4 scheduled_pricing_dispatch KST cron + F35.5 tenant_scoped_pricing_role RBAC owner-only + F35.6 pricing dashboard UI 5 sub-components + F35.7 Capability matrix v1.45 EXTENSION FINOPS_PRICING + F35.8 dry-run + Tests + wire scope T1~T8). §15 로드맵 Phase 19 row status 백로그 → in-progress (PRD entry DONE 진입 wire) 결정. AD-46 신규 결정. capability matrix v1.44 → v1.45 EXTENSION FINOPS_PRICING 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅. CR 11-3 honest-DEFER discipline 137번째 epic 연속 정직 회복 검증 ✅ (D-1-1-DEFER-1/2/3 RESOLVED 보존 60~137번째 + D-LAUNCH-1-DEFER-1 honestly preserved 65~137번째 + D-EPIC-16-REVIEW-DEFER-2~6 ✅ RESOLVED 보존 78~137번째 + D-PHASE-4-DR-DEFER-1/2 ✅ RESOLVED 보존 73~137번째 + D-EPIC-17-WIRE-DEFER-T2-T3-UI ✅ RESOLVED 보존 83~137번째 + D-RETENTION-1 ✅ RESOLVED 보존 85~137번째 + D-OBSERVABILITY-1 ✅ RESOLVED 보존 89~137번째 + D-PERFORMANCE-1 ✅ RESOLVED 보존 93~137번째 + D-CHAOS-1 ✅ RESOLVED 보존 100~137번째 + D-SLO-1 ✅ RESOLVED 보존 101~137번째 + D-FINOPS-1 ✅ RESOLVED 보존 105~137번째 + D-FINOPS-2 ✅ RESOLVED 보존 109~137번째 + D-FINOPS-3 ✅ RESOLVED 보존 113~137번째 + D-FINOPS-4 ✅ RESOLVED 보존 117~137번째 + D-FINOPS-5 ✅ RESOLVED 보존 121~137번째 + D-FINOPS-6 ✅ RESOLVED 보존 125~137번째 + D-FINOPS-7 ✅ RESOLVED 보존 129~137번째 + D-FINOPS-8 ✅ RESOLVED 보존 133~137번째 + **D-FINOPS-9 신규 honestly DEFER 보존 = Phase 19 PRD entry 진입 시점에 carry-over chain 정직 회복 결정 wire 진입: Phase 18 close-out retro `de72f50` §13 + Phase 18 close-out retro `de72f50` §13 + Phase 17 close-out retro `de009fe` §13 + Phase 16 close-out retro `26fd530` §13 + Phase 15 close-out retro `102f370` §13 + Phase 14 close-out retro `5b367d9` §13 + Phase 13 close-out retro `850b4f8` §13 + Phase 12 close-out retro `3354e83` §13 + Phase 11 close-out retro `80df15b` §12 + Phase 10 close-out retro `733d428` §10 + Phase 9 close-out retro `634427d` §10 + Phase 8 close-out retro `ab495a8` §10 + Phase 7 close-out retro `326fa9f` §10 + Phase 6 close-out retro `f9f006c` §13 + Epic 17 close-out retro `be8f3bd` §11 "FinOps Pricing, Rate Card & TCO Modeling 결정 wire 보류, Phase 19+ 진입 시점" verbatim 해소**) + A36 SDR 검증 4-step 자동 적용 (commit prefix lint PASS + sprint-status structure PASS + vitest file count drift 0건 + commit consistency PASS) + CR 12-5 D-GATE-01 + D-PARITY-01 inversion 적용 보존 + A19 cohesion 9 surface EXTENSION PASS + CR 0-2 RLS lesson (Phase 19 finops_pricing tables RLS 자동 적용 + tenant-scoped pricing policy 결정) + CR 1-1 audit-first INSERT (pricing_dashboard_viewed + cross_module_pricing_kpi_calculated + pricing_report_generated + pricing_report_exported + pricing_report_dispatched + pricing_scheduled_dispatch_evaluated + finops_pricing_dry_run_executed + pricing_kpi_refreshed 8 NEW CR 1-1 verbatim 적용) + CR 9-6 commit message discipline (`git commit -F <file>` 사용, PowerShell here-string 회피, D5 prevention) + NFR4 PII minimization ✅ PRESERVED + NFR18 ko-KR SSOT 모두 적용 보존. 결정 wire 일자: 2026-08-25 (KST).
  - v4.9 (2026-08-25): Phase 18 PRD entry 결정 wire DONE (cj-style 133번째 epic 연속 정직 회복 atomic docs-only wire) — 옵션 (a) Phase 18+ 진입 결정 wire (Phase 17 close-out retro `de009fe` 132번째 + Phase 17 atomic wire T1~T8 `97cfe4e` 131번째 + Phase 17 spec entry `4be3120` 130번째 + Phase 17 PRD entry `e0778ed` 129번째 모두 wire DONE 진입 정합 보존 후 다음 territory 진입). 사용자 권장 결정 = 옵션 (a) Phase 18+ 진입 + 옵션 (a) FinOps Cloud Commitment Management (RIs/SPs/CUDs) (Recommended) (commitment_inventory_aggregator 5 cloud provider (AWS EC2 RI + RDS RI + EC2 SP + S3 SP + Redshift SP + DynamoDB SP + Azure reservations + GCP CUDs + Naver Cloud + KT Cloud) cross-rollup + commitment_kpi_selector 8 NEW KPI calculations (total_commitment_value_krw + coverage_pct + utilization_pct + expiring_commitments_30d + recommended_purchase_krw + savings_realized_krw + idle_commitment_krw + renewal_decision_score) + commitment_report_generation_engine PDF + CSV + Excel + scheduled_commitment_dispatch KST cron + tenant_scoped_commitment_role RBAC owner-only + commitment dashboard UI 5 sub-components + ko-KR.json `finops_commitment.*` namespace EXTENSION ~30 keys + ARIA labels WCAG 2.1 AA + audit-first INSERT 8 NEW via ActionClass.FINOPS_COMMITMENT (`commitment_dashboard_viewed` + `cross_module_commitment_kpi_calculated` + `commitment_report_generated` + `commitment_report_exported` + `commitment_report_dispatched` + `commitment_scheduled_dispatch_evaluated` + `finops_commitment_dry_run_executed` + `commitment_kpi_refreshed`) + Capability FINOPS_COMMITMENT 1 NEW + dry-run mode 5 NEW CLI flags + tests + wire scope T1~T8) = Phase 18 territory 결정 wire 진입 (cj-style 133번째 epic 연속 정직 회복 진입 시점에 결정). rationale 5종: ① Phase 17 wire `97cfe4e` FinOps Sustainability & Carbon Reporting territory (carbon_emissions_aggregator 6-module cross-rollup + sustainability_kpi_selector 8 NEW KPI selector + sustainability report generation engine PDF + CSV + Excel + scheduled sustainability dispatch KST cron + tenant-scoped sustainability role RBAC owner-only) 의 natural backend CLOUD COMMITMENT MANAGEMENT LAYER EXTENSION 결정 wire 진입 (7 FinOps modules 의 data aggregate → commitment-grade cloud cost optimization view + cross-module commitment KPI 8개 + scheduled commitment report export PDF+CSV+Excel weekly/monthly/quarterly/annual + commitment_inventory cross-rollup + coverage_pct + utilization_pct + expiring_commitments_30d + purchase_recommendation + ROI scoring + risk scoring + renewal_workflow) ② cj-style discipline 회피 위험 방지 (Phase 11~17 7-phase FinOps chain 완료 직후 즉시 Phase 18 진입 = 1-day atomic sprint discipline = 49~132번째 누적 84-entry-point cycle 후 133번째 진입 정직 회복 discipline 유지) ③ 비즈니스 우선순위 = enterprise 고객 onboarding 시 commitment 기반 cloud cost optimization (RIs/SPs/CUDs purchase recommendation + coverage_pct + utilization_pct + expiring_commitments_30d + ROI scoring + risk scoring + renewal workflow) territory 필수 (Phase 14 optimization의 RI/SP commitment_recommender 1y/3y break-even → Phase 18 commitment_inventory cross-join + coverage_pct EXTENSION + Phase 17 sustainability의 renewable_energy_pct + carbon_offset → carbon-aware commitment 권고 EXTENSION + Phase 13 forecast의 12-month horizon → expiring_commitments_30d + utilization_forecast EXTENSION + Phase 11 showback의 cost center breakdown → cost center commitment coverage EXTENSION + Phase 12 anomaly의 z-score 기반 → commitment anomaly detection EXTENSION + Phase 15 tag_governance의 allocation rule → commitment allocation rule EXTENSION + AWS / Azure / GCP / Naver Cloud / KT Cloud 멀티 cloud commitment 지원) ④ Phase 17 wire `97cfe4e` 의 6-module cross-rollup (Phase 11 showback + Phase 12 anomaly + Phase 13 forecast + Phase 14 optimization + Phase 15 tag_governance + Phase 16 executive + Phase 17 sustainability) + Phase 14 wire `e904485` 의 commitment_recommender 1y/3y break-even + Phase 13 wire `8b98030` 의 12-month forecast + Phase 11 wire `e020ad0` 의 cost center cost allocation + Phase 12 wire `f3c0e63` 의 anomaly detection baseline 의 자연스러운 carry-over chain = Phase 18 commitment 의 commitment_inventory (EC2 RI + RDS RI + EC2 SP + S3 SP + Redshift SP + DynamoDB SP + Azure reservations + GCP CUDs + Naver Cloud + KT Cloud) + coverage_pct (commitment covered cost / total cost) + utilization_pct (commitment used hours / commitment total hours) + expiring_commitments_30d (next 30-day expiration) + purchase_recommendation (Phase 14 break-even EXTENSION) + ROI scoring (savings / cost) + risk scoring (low + medium + high) + renewal_workflow (auto + manual + hold) EXTENSION 정합 ⑤ 1st release close-out retro + Epic 17 close-out retro + Phase 5~17 close-out retro territory verbatim 보존 + AD-22 owner-only RBAC + Epic 12 2FA 챌린지 보존 + AD-14 stack pin + NFR4 PII minimization ✅ PRESERVED + AD-45 FinOps Cloud Commitment Management 신규 결정 ((a)~(g) 7 sub-decisions 모두 결정 wire 진입). master PRD v4.8 → v4.9 atomic edit (docs only). **§F34 FinOps Cloud Commitment Management territory 신규** 결정 wire 진입 (F34.1 commitment_inventory_aggregator 5 cloud provider cross-rollup EXTENSION + F34.2 commitment_kpi_selector 8 NEW KPI selector + F34.3 commitment_report_generation_engine PDF + CSV + Excel + F34.4 scheduled_commitment_dispatch KST cron + F34.5 tenant_scoped_commitment_role RBAC owner-only + F34.6 commitment dashboard UI 5 sub-components + F34.7 Capability matrix v1.44 EXTENSION FINOPS_COMMITMENT + F34.8 dry-run + Tests + wire scope T1~T8). §15 로드맵 Phase 18 row status 백로그 → in-progress (PRD entry DONE 진입 wire) 결정. AD-45 신규 결정. capability matrix v1.43 → v1.44 EXTENSION FINOPS_COMMITMENT 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅. CR 11-3 honest-DEFER discipline 133번째 epic 연속 정직 회복 검증 ✅ (D-1-1-DEFER-1/2/3 RESOLVED 보존 60~133번째 + D-LAUNCH-1-DEFER-1 honestly preserved 65~133번째 + D-EPIC-16-REVIEW-DEFER-2~6 ✅ RESOLVED 보존 78~133번째 + D-PHASE-4-DR-DEFER-1/2 ✅ RESOLVED 보존 73~133번째 + D-EPIC-17-WIRE-DEFER-T2-T3-UI ✅ RESOLVED 보존 83~133번째 + D-RETENTION-1 ✅ RESOLVED 보존 85~133번째 + D-OBSERVABILITY-1 ✅ RESOLVED 보존 89~133번째 + D-PERFORMANCE-1 ✅ RESOLVED 보존 93~133번째 + D-CHAOS-1 ✅ RESOLVED 보존 100~133번째 + D-SLO-1 ✅ RESOLVED 보존 101~133번째 + D-FINOPS-1 ✅ RESOLVED 보존 105~133번째 + D-FINOPS-2 ✅ RESOLVED 보존 109~133번째 + D-FINOPS-3 ✅ RESOLVED 보존 113~133번째 + D-FINOPS-4 ✅ RESOLVED 보존 117~133번째 + D-FINOPS-5 ✅ RESOLVED 보존 121~133번째 + D-FINOPS-6 ✅ RESOLVED 보존 125~133번째 + D-FINOPS-7 ✅ RESOLVED 보존 129~133번째 + **D-FINOPS-8 신규 honestly DEFER 보존 = Phase 18 PRD entry 진입 시점에 carry-over chain 정직 회복 결정 wire 진입: Phase 17 close-out retro `de009fe` §13 + Phase 16 close-out retro `26fd530` §13 + Phase 15 close-out retro `102f370` §13 + Phase 14 close-out retro `5b367d9` §13 + Phase 13 close-out retro `850b4f8` §13 + Phase 12 close-out retro `3354e83` §13 + Phase 11 close-out retro `80df15b` §12 + Phase 10 close-out retro `733d428` §10 + Phase 9 close-out retro `634427d` §10 + Phase 8 close-out retro `ab495a8` §10 + Phase 7 close-out retro `326fa9f` §10 + Phase 6 close-out retro `f9f006c` §13 + Epic 17 close-out retro `be8f3bd` §11 "FinOps Cloud Commitment Management 결정 wire 보류, Phase 18+ 진입 시점" verbatim 해소**) + A36 SDR 검증 4-step 자동 적용 (commit prefix lint PASS + sprint-status structure PASS + vitest file count drift 0건 + commit consistency PASS) + CR 12-5 D-GATE-01 + D-PARITY-01 inversion 적용 보존 + A19 cohesion 9 surface EXTENSION PASS + CR 0-2 RLS lesson (Phase 18 finops_commitment tables RLS 자동 적용 + tenant-scoped commitment policy 결정) + CR 1-1 audit-first INSERT (commitment_dashboard_viewed + cross_module_commitment_kpi_calculated + commitment_report_generated + commitment_report_exported + commitment_report_dispatched + commitment_scheduled_dispatch_evaluated + finops_commitment_dry_run_executed + commitment_kpi_refreshed 8 NEW CR 1-1 verbatim 적용) + CR 9-6 commit message discipline (`git commit -F <file>` 사용, PowerShell here-string 회피, D5 prevention) + NFR4 PII minimization ✅ PRESERVED + NFR18 ko-KR SSOT 모두 적용 보존. 결정 wire 일자: 2026-08-25 (KST).
  - v4.8 (2026-08-25): Phase 17 PRD entry 결정 wire DONE (cj-style 129번째 epic 연속 정직 회복 atomic docs-only wire) — 옵션 (a) Phase 17+ 진입 결정 wire (Phase 16 close-out retro `26fd530` 128번째 + Phase 16 atomic wire T1~T8 `81ae00a` 127번째 + Phase 16 spec entry `69c29df` 126번째 + Phase 16 PRD entry `4f11d03` 125번째 모두 wire DONE 진입 정합 보존 후 다음 territory 진입). 사용자 권장 결정 = 옵션 (a) Phase 17+ 진입 + 옵션 (a) FinOps Sustainability & Carbon Reporting (Recommended) (carbon_emissions_aggregator 6 FinOps modules cross-join EXTENSION + sustainability_kpi_selector 8 NEW KPI calculations (carbon_intensity_kgco2e_per_krw + cloud_carbon_emissions_kgco2e + data_center_pue + renewable_energy_pct + scope1_emissions_kgco2e + scope2_emissions_kgco2e + scope3_emissions_kgco2e + carbon_offset_kgco2e) + sustainability_report_generation_engine PDF + CSV + Excel monthly/quarterly/annual + scheduled_sustainability_dispatch KST cron 4 cadence + tenant_scoped_sustainability_role RBAC owner-only + Phase 17 wire 5 NEW components (SustainabilityDashboardAggregator + SustainabilityKPISelector + SustainabilityReportGeneratorPanel + ScheduledSustainabilityDispatchConfigPanel + SustainabilityTrendMiniChart) + ko-KR.json `finops_sustainability.*` namespace EXTENSION ~30 keys + ARIA labels WCAG 2.1 AA + audit-first INSERT 8 NEW via ActionClass.FINOPS_SUSTAINABILITY (`sustainability_dashboard_viewed` + `cross_module_sustainability_kpi_calculated` + `sustainability_report_generated` + `sustainability_report_exported` + `sustainability_report_dispatched` + `sustainability_scheduled_dispatch_evaluated` + `finops_sustainability_dry_run_executed` + `sustainability_kpi_refreshed`) + Capability FINOPS_SUSTAINABILITY 1 NEW + dry-run mode 5 NEW CLI flags + tests + wire scope T1~T8) = Phase 17 territory 결정 wire 진입 (cj-style 129번째 epic 연속 정직 회복 진입 시점에 결정). rationale 5종: ① Phase 16 wire `81ae00a` FinOps Reporting & Executive Dashboard territory (executive_dashboard_aggregator 5-module cross-join + 8 NEW cross-module KPI selector + executive report generation engine PDF + CSV + Excel + scheduled dispatch KST cron + tenant-scoped executive role RBAC) 의 natural backend SUSTAINABILITY ROLLUP LAYER EXTENSION 결정 wire 진입 (6 FinOps modules 의 data aggregate → sustainability-grade carbon rollup view + cross-module sustainability KPI 8개 + scheduled sustainability report export PDF+CSV+Excel monthly/quarterly/annual) ② cj-style discipline 회피 위험 방지 (Phase 11~16 6-phase FinOps chain 완료 직후 즉시 Phase 17 진입 = 1-day atomic sprint discipline = 49~128번째 누적 80-entry-point cycle 후 129번째 진입 정직 회복 discipline 유지) ③ 비즈니스 우선순위 = EU CSRD (Corporate Sustainability Reporting Directive, 2024~2027 단계 시행) + SEC Climate Disclosure Rules (2026 강화) + EU Taxonomy Regulation + IFRS S2 Climate-related Disclosures + 한국 KSSB (Korean Sustainability Standards Board, 2026 신설) 규제 driver + enterprise 고객 onboarding 시 sustainability-grade carbon reporting + 6-module cross-rollup KPI dashboard + scheduled sustainability report dispatch territory 필수 (Phase 11 showback department breakdown EXTENSION + Phase 12 anomaly detection baseline EXTENSION + Phase 13 forecast 12-month horizon EXTENSION + Phase 14 optimization accuracy tracking EXTENSION + Phase 15 tag compliance scoring EXTENSION + Phase 16 executive rollup cross-join EXTENSION) ④ Phase 16 wire `81ae00a` 의 5-module cross-join (Phase 11 showback + Phase 12 anomaly + Phase 13 forecast + Phase 14 optimization + Phase 15 tag_governance) + Phase 14 wire `e904485` 의 idle resource detection + commitment recommender + Phase 13 wire `8b98030` 의 capacity headroom + budget burn-rate + Phase 15 wire `1b800d9` 의 tag compliance 의 자연스러운 carry-over chain = Phase 17 sustainability rollup 의 aggregate_function (6 modules cross-join with carbon attribution key from Phase 14 idle_resource + Phase 15 tag_governance + Phase 16 executive rollup) + KPI calculation (Phase 11 cost → carbon intensity + Phase 13 forecast → carbon projection + Phase 14 idle cost → idle carbon + Phase 15 tag compliance → resource efficiency + Phase 16 executive rollup → executive carbon view) + scheduled dispatch (KST cron weekly/monthly/quarterly/annual) + EU CSRD/IFRS S2 compliance report EXTENSION 정합 ⑤ 1st release close-out retro + Epic 17 close-out retro + Phase 5~16 close-out retro territory verbatim 보존 + AD-22 owner-only RBAC + Epic 12 2FA 챌린지 보존 + AD-14 stack pin + NFR4 PII minimization ✅ PRESERVED + AD-44 FinOps Sustainability & Carbon Reporting 신규 결정 ((a)~(g) 7 sub-decisions 모두 결정 wire 진입). master PRD v4.7 → v4.8 atomic edit (docs only). **§F33 FinOps Sustainability & Carbon Reporting territory 신규** 결정 wire 진입 (F33.1 carbon_emissions_aggregator 6 modules cross-join EXTENSION + F33.2 sustainability_kpi_selector 8 NEW KPI selector + F33.3 sustainability_report_generation_engine PDF + CSV + Excel + F33.4 scheduled_sustainability_dispatch KST cron + F33.5 tenant_scoped_sustainability_role RBAC owner-only + F33.6 sustainability_dashboard UI 5 sub-components + F33.7 Capability matrix v1.43 EXTENSION FINOPS_SUSTAINABILITY + F33.8 dry-run + Tests + wire scope T1~T8). §15 로드맵 Phase 17 row status 백로그 → in-progress (PRD entry DONE 진입 wire) 결정. AD-44 신규 결정. capability matrix v1.42 → v1.43 EXTENSION FINOPS_SUSTAINABILITY 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅. CR 11-3 honest-DEFER discipline 129번째 epic 연속 정직 회복 검증 ✅ (D-1-1-DEFER-1/2/3 RESOLVED 보존 60~129번째 + D-LAUNCH-1-DEFER-1 honestly preserved 65~129번째 + D-EPIC-16-REVIEW-DEFER-2~6 ✅ RESOLVED 보존 78~129번째 + D-PHASE-4-DR-DEFER-1/2 ✅ RESOLVED 보존 73~129번째 + D-EPIC-17-WIRE-DEFER-T2-T3-UI ✅ RESOLVED 보존 83~129번째 + D-RETENTION-1 ✅ RESOLVED 보존 85~129번째 + D-OBSERVABILITY-1 ✅ RESOLVED 보존 89~129번째 + D-PERFORMANCE-1 ✅ RESOLVED 보존 93~129번째 + D-CHAOS-1 ✅ RESOLVED 보존 100~129번째 + D-SLO-1 ✅ RESOLVED 보존 101~129번째 + D-FINOPS-1 ✅ RESOLVED 보존 105~129번째 + D-FINOPS-2 ✅ RESOLVED 보존 109~129번째 + D-FINOPS-3 ✅ RESOLVED 보존 113~129번째 + D-FINOPS-4 ✅ RESOLVED 보존 117~129번째 + D-FINOPS-5 ✅ RESOLVED 보존 121~129번째 + D-FINOPS-6 ✅ honestly DEFER 보존 125~129번째). 결정 wire 일자: 2026-08-25 (KST).
  - v4.7 (2026-08-25): Phase 16 PRD entry 결정 wire DONE (cj-style 125번째 epic 연속 정직 회복 atomic docs-only wire) — 옵션 (a) Phase 16+ 진입 결정 wire (Phase 15 close-out retro `102f370` 124번째 + Phase 15 atomic wire T1~T8 `1b800d9` 123번째 + Phase 15 spec entry `69c29df` 122번째 + Phase 15 PRD entry `87393b4` 121번째 모두 wire DONE 진입 정합 보존 후 다음 territory 진입). 사용자 권장 결정 = 옵션 (a) Phase 16+ 진입 + 옵션 (a) FinOps Reporting & Executive Dashboard (Recommended) (5-module cross-rollup aggregator (Phase 11 showback + Phase 12 anomaly + Phase 13 forecast + Phase 14 optimization + Phase 15 tag_governance) + cross-module KPI selector (8 NEW KPI: total_monthly_cost_krw + monthly_cost_growth_pct + cost_per_employee_krw + cost_anomaly_count_30d + forecast_deviation_pct + idle_cost_monthly_krw + tag_compliance_pct + optimization_realized_savings_krw) + executive report generation engine PDF + CSV + Excel monthly/quarterly/annual + scheduled dispatch KST cron + tenant-scoped executive role RBAC owner-only + Phase 16 wire 5 NEW components (ExecutiveDashboardAggregator + CrossModuleKPISelector + ExecutiveReportGeneratorPanel + ScheduledDispatchConfigPanel + ComplianceTrendMiniChart) + ko-KR.json `finops_reporting.*` namespace EXTENSION ~30 keys + ARIA labels WCAG 2.1 AA + audit-first INSERT 8 NEW (`executive_report_generated` + `executive_dashboard_viewed` + `executive_kpi_refreshed` + `executive_report_exported` + `executive_report_dispatched` + `executive_scheduled_dispatch_evaluated` + `finops_reporting_dry_run_executed` + `cross_module_kpi_calculated`) + Capability FINOPS_REPORTING 1 NEW + dry-run mode 5 NEW CLI flags + tests + wire scope T1~T8) = Phase 16 territory 결정 wire 진입 (cj-style 125번째 epic 연속 정직 회복 진입 시점에 결정, AskUserQuestion 결정 verbatim bind). rationale 5종: ① Phase 15 wire `1b800d9` FinOps Tag Governance & Cost Allocation territory 의 natural backend EXECUTIVE ROLLUP LAYER EXTENSION 결정 wire 진입 (5 FinOps modules 의 data aggregate → executive-grade rollup view + cross-module KPI 8개 + scheduled executive report export PDF+CSV+Excel monthly/quarterly/annual) ② cj-style discipline 회피 위험 방지 (Phase 11~15 5-phase FinOps chain 완료 직후 즉시 Phase 16 진입 = 1-day atomic sprint discipline = 49~124번째 누적 76-entry-point cycle 후 125번째 진입 정직 회복 discipline 유지) ③ 비즈니스 우선순위 = 1st release + enterprise 고객 onboarding 후 executive-grade cost reporting + 5-module cross-rollup KPI dashboard + scheduled executive report dispatch territory 필수 (Phase 11 showback department breakdown EXTENSION + Phase 12 anomaly detection baseline EXTENSION + Phase 13 forecast 12-month horizon EXTENSION + Phase 14 optimization accuracy tracking EXTENSION + Phase 15 tag compliance scoring EXTENSION) ④ Phase 15 wire `1b800d9` 의 tag_governance surface + Phase 14 wire `e904485` 의 optimization surface + Phase 13 wire `8b98030` 의 forecast surface + Phase 12 wire `f3c0e63` 의 anomaly surface + Phase 11 wire `e020ad0` 의 showback surface 의 자연스러운 carry-over chain = Phase 16 cross-module rollup 의 aggregate_function (5 modules cross-join with cost allocation key from Phase 15 tag_governance) + KPI calculation (Phase 12 anomaly detection baseline + Phase 13 forecast deviation + Phase 14 optimization realized savings + Phase 15 tag compliance scoring EXTENSION) + scheduled dispatch (KST cron weekly/monthly/quarterly/annual) EXTENSION 정합 ⑤ 1st release close-out retro + Epic 17 close-out retro + Phase 5~15 close-out retro territory verbatim 보존 + AD-22 owner-only RBAC + Epic 12 2FA 챌린지 보존 + AD-14 stack pin + NFR4 PII minimization ✅ PRESERVED + AD-43 FinOps Reporting & Executive Dashboard 신규 결정 ((a)~(g) 7 sub-decisions 모두 결정 wire 진입). master PRD v4.6 → v4.7 atomic edit (docs only). **§F32 FinOps Reporting & Executive Dashboard territory 신규** 결정 wire 진입 (F32.1 cross-FinOps-module rollup aggregator 5 modules cross-join EXTENSION + F32.2 cross-module KPI 8 KPI selector + F32.3 executive report generation engine PDF + CSV + Excel + F32.4 scheduled dispatch KST cron + F32.5 tenant-scoped executive role RBAC owner-only + F32.6 executive dashboard UI 5 sub-components + F32.7 Capability matrix v1.42 EXTENSION FINOPS_REPORTING + F32.8 dry-run + Tests + wire scope T1~T8). §15 로드맵 Phase 16 row status 백로그 → in-progress (PRD entry DONE 진입 wire) 결정. AD-43 신규 결정. capability matrix v1.41 → v1.42 EXTENSION FINOPS_REPORTING 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅. CR 11-3 honest-DEFER discipline 125번째 epic 연속 정직 회복 검증 ✅ (D-1-1-DEFER-1/2/3 RESOLVED 보존 60~125번째 + D-EPIC-16-REVIEW-DEFER-2~6 ✅ RESOLVED 보존 78~125번째 + D-PHASE-4-DR-DEFER-1/2 ✅ RESOLVED 보존 73~125번째 + D-EPIC-17-WIRE-DEFER-T2-T3-UI ✅ RESOLVED 보존 83~125번째 + D-RETENTION-1 ✅ RESOLVED 보존 85~125번째 + D-OBSERVABILITY-1 ✅ RESOLVED 보존 89~125번째 + D-PERFORMANCE-1 ✅ RESOLVED 보존 93~125번째 + D-CHAOS-1 ✅ RESOLVED 보존 100~125번째 + D-SLO-1 ✅ RESOLVED 보존 101~125번째 + D-FINOPS-1 ✅ RESOLVED 보존 105~125번째 + D-FINOPS-2 ✅ RESOLVED 보존 109~125번째 + D-FINOPS-3 ✅ RESOLVED 보존 113~125번째 + D-FINOPS-4 ✅ RESOLVED 보존 117~125번째 + D-FINOPS-5 ✅ honestly DEFER 보존 121~125번째). 결정 wire 일자: 2026-08-25 (KST).
  - v4.6 (2026-08-25): Phase 15 PRD entry 결정 wire DONE (cj-style 121번째 epic 연속 정직 회복 atomic docs-only wire) — 옵션 (a) Phase 15+ 진입 결정 wire (Phase 14 close-out retro `5b367d9` 120번째 + Phase 14 atomic wire T1~T8 `e904485` 119번째 + Phase 14 spec entry `30637f6` 118번째 + Phase 14 PRD entry `0e3f8d9` 117번째 모두 wire DONE 진입 정합 보존 후 다음 territory 진입). 사용자 권장 결정 = 옵션 (a) Phase 15+ 진입 + 옵션 (a) FinOps Tag Governance & Cost Allocation (Recommended) (tag policy DSL 4 enforcement_level required/recommended/optional/blocked + untagged resource detector 6 resource types EC2/RDS/S3/Lambda/EKS/VPC + allocation rules engine 5 rule types tag_match/percentage_split/weighted/conditional/fallback + allocation audit + compliance + chargeback allocation reconciliation hybrid_blended default + tag governance dashboard UI 5 components + audit-first INSERT 8 NEW + Capability FINOPS_TAG_GOVERNANCE 1 NEW + dry-run mode + tests + wire scope T1~T8) = Phase 15 territory 결정 wire 진입 (cj-style 121번째 epic 연속 정직 회복 진입 시점에 결정, AskUserQuestion 결정 verbatim bind). rationale 5종: ① Phase 14 wire `e904485` FinOps Optimization & Rightsizing territory (optimization definition DSL + rightsizing engine 5 resource types + idle resource detection z-score 기반 + RI/SP commitment recommendations 1y/3y + optimization accuracy tracking precision/recall/realized savings) 의 natural backend carry-over chain = Phase 15 = FinOps Tag Governance & Cost Allocation territory 의 natural next (rightsizing 권고 의 resource inventory → tagged resource inventory 기반 cost allocation + idle resource 의 unattached EIP / detached EBS / orphaned RDS snapshot detection → untagged resource detection EXTENSION + commitment_recommender 의 resource_pattern → tag_match 기반 allocation rule + optimization_accuracy 의 resource_type granularity → allocation rule 의 tag dimension EXTENSION + Phase 11 showback department breakdown 의 cost allocation → tag-based allocation EXTENSION + Phase 12 anomaly detection 의 dimension 옵션 → tag dimension 추가 + Phase 13 forecasting 의 target_metric → tag-based forecasting EXTENSION + Phase 14 optimization 의 baseline_period → tag governance compliance period) = Phase 14 FinOps territory 의 COST ALLOCATION LAYER EXTENSION 결정 (tagged resource → accountable cost center) ② cj-style discipline 회피 위험 방지 (49~120번째 누적 72-entry-point cycle + 120번째 Phase 14 close-out retro + 119번째 atomic wire + 118번째 spec entry + 117번째 PRD entry 패턴 모두 wire DONE 진입 정합 보존 후 즉시 Phase 15 진입 = 1-day atomic sprint discipline) ③ 비즈니스 우선순위 = enterprise 고객 onboarding 시 tag policy 정의 + untagged resource detection + tag-based cost allocation + chargeback allocation reconciliation + compliance report territory 필수 (Phase 14 optimization 권고 의 resource inventory tagged 여부 검증 + Phase 11 showback department breakdown 의 department ↔ cost center mapping → tag ↔ cost center mapping EXTENSION + Phase 12 budget_alert 의 budget_scope (department + cost_center) → tag dimension 추가 + Phase 13 forecasting 의 cost_engine 출력 → tag-based forecast EXTENSION) ④ Phase 14 wire `e904485` 의 resource inventory (compute + storage + database + network + container 5 types) + Phase 13 wire `8b98030` 의 capacity_headroom_report dimension EXTENSION + Phase 11 wire `e020ad0` 의 department cost center mapping + Phase 12 wire `f3c0e63` 의 anomaly detection baseline (z-score > 3.0) + Phase 8 wire `60d4ea1` 의 cost-engine 12-period benchmark 의 자연스러운 carry-over chain = Phase 15 tag governance 의 resource_inventory tagged 여부 검증 (EC2/RDS/S3/Lambda/EKS/VPC 6 resource types) + tag_policy DSL (4 enforcement_level: required/recommended/optional/blocked) + untagged resource detection (last 7d/30d/90d baseline) + allocation rules engine (5 rule types: tag_match/percentage_split/weighted/conditional/fallback) + chargeback allocation reconciliation (Phase 11 chargeback engine EXTENSION) + compliance report (monthly cadence) EXTENSION 정합 ⑤ 1st release close-out retro + Epic 17 close-out retro + Phase 5 close-out retro + Phase 6 close-out retro + Phase 7 close-out retro + Phase 8 close-out retro + Phase 9 close-out retro + Phase 10 close-out retro + Phase 11 close-out retro + Phase 12 close-out retro + Phase 13 close-out retro + Phase 14 close-out retro territory verbatim 보존 + AD-22 owner-only RBAC + Epic 12 2FA 챌린지 보존 + AD-14 stack pin + NFR4 PII minimization ✅ PRESERVED + AD-42 FinOps Tag Governance & Cost Allocation 신규 결정 ((a)~(g) 7 sub-decisions 모두 결정 wire 진입). master PRD v4.5 → v4.6 atomic edit (docs only). **§F31 FinOps Tag Governance & Cost Allocation territory 신규** (F31.1 tag policy DSL (`apps/api/modules/finops/tag_policy_dsl.py` NEW ~+150 LOC, syntax: `define_tag_policy(tenant_id, resource_type, tag_key, enforcement_level, default_value)` builder + AST 5 levels + 6 resource_type 옵션 (ec2 + rds + s3 + lambda + eks + vpc) + 4 enforcement_level 옵션 (required + recommended + optional + blocked) + tag_key validation (AWS 표준 + reserved prefix `aws:` 차단) + 4 default_value 옵션 (string + env_based + conditional + inherit) + 4 industries baseline + per-tenant override EXTENSION + policy evaluation precedence tenant override > industry baseline > system default + `TAG_POLICY_DEFAULTS = {'enforcement_level': 'recommended', 'default_value': 'untagged', 'compliance_threshold_pct': 95.0}` constants + audit-first INSERT `tag_policy_updated` CR 1-1 verbatim) / F31.2 untagged resource detector (`apps/api/modules/finops/untagged_resource_detector.py` NEW ~+200 LOC + 6 resource types (EC2 instance + RDS db instance + S3 bucket + Lambda function + EKS cluster + VPC endpoint/NAT/security group) + Phase 14 wire `e904485` 의 idle_resource_detector EXTENSION + 4 remediation_status (pending + notified + remediated + expired) + UntaggedResource TypedDict 14 fields + audit-first INSERT `untagged_resource_detected` CR 1-1 verbatim) / F31.3 allocation rules engine (`apps/api/modules/finops/allocation_rules_engine.py` NEW ~+220 LOC + 5 rule types (tag_match + percentage_split + weighted + conditional + fallback) + rule precedence tag_match > percentage_split > weighted > conditional > fallback + Phase 11 wire `e020ad0` 의 chargeback engine EXTENSION + AllocationRule TypedDict 13 fields + AllocatedCost TypedDict 11 fields + audit-first INSERT `allocation_rule_evaluated` + `allocation_rule_updated` 2 NEW CR 1-1 verbatim) / F31.4 allocation audit + compliance (`apps/api/modules/finops/allocation_audit.py` NEW ~+150 LOC + compliance score calculation + compliance threshold 95% default + allocation accuracy 99% threshold + allocation coverage 95% threshold + ComplianceReport TypedDict 12 fields + monthly compliance_report cron KST 1st day of month 04:00 + 3 export format PDF/CSV/JSON + compliance trend analysis last 12-month + compliance alert routing Slack + PagerDuty + Email + compliance remediation workflow 5 steps + audit-first INSERT `compliance_report_generated` + `compliance_alert_sent` + `compliance_remediation_initiated` 3 NEW CR 1-1 verbatim) / F31.5 chargeback allocation reconciliation (`apps/api/modules/finops/chargeback_allocation_reconciliation.py` NEW ~+180 LOC + 3 reconciliation strategy (chargeback_only + tag_allocation_only + hybrid_blended default) + variance calculation + ReconciliationReport TypedDict 13 fields + unmatched allocation handling + cross-period reconciliation 13-period rolling + reconciliation investigation workflow + reconciliation approval workflow owner-only RBAC AD-22 + Epic 12 2FA 챌린지 보존 + audit-first INSERT 5 NEW `reconciliation_initiated` + `reconciliation_report_generated` + `reconciliation_investigation_triggered` + `reconciliation_approved` + `reconciliation_resolved` CR 1-1 verbatim) / F31.6 tag governance dashboard UI (`apps/web/app/[locale]/(dashboard)/admin/finops/tag-governance/page.tsx` NEW ~+150 LOC + 5 components (TagPolicyEditor + UntaggedResourceDetectorPanel + AllocationRuleBuilder + ComplianceReportPanel + ReconciliationReportPanel) + owner-only RBAC AD-22 verbatim + Epic 12 2FA 챌린지 보존 + ko-KR.json `finops_tag_governance.*` namespace EXTENSION ~30 keys CR 11-4 D-002 verbatim SSOT 결정 wire) / F31.7 Capability matrix v1.41 EXTENSION FINOPS_TAG_GOVERNANCE (capability matrix v1.40 → v1.41 EXTENSION 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅ CR 12-1 L4 precedent 미러, FINOPS_OPTIMIZATION Phase 14 wire + FINOPS_FORECASTING_CAPACITY_PLANNING Phase 13 wire + FINOPS_ANOMALY_DETECTION + FINOPS_BUDGET_ALERT Phase 12 wire + FINOPS_SHOWBACK + FINOPS_CHARGEBACK Phase 11 wire + SLO_ENGINEERING Phase 10 wire + CHAOS_ENGINEERING Phase 9 wire + PERFORMANCE_TESTING Phase 8 wire + OBSERVABILITY_TRACES + OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind + `apps/api/core/capability.py` MODIFIED + Capability.FINOPS_TAG_GOVERNANCE = "finops_tag_governance" 1 NEW enum + `apps/api/dependencies/capability.py` MODIFIED require_finops_tag_governance 1 NEW dep + drift detector `tests/integration/test_capability_matrix_v1_41_drift.py` NEW 8 NEW pytest cases) / F31.8 dry-run + tests + wire scope T1~T8 결정 (~+56 NEW pytest PASS (tag_policy_dsl 6 + untagged_resource_detector 8 + allocation_rules_engine 8 + allocation_audit 6 + chargeback_allocation_reconciliation 7 + alembic 0047 5 + audit action 8 + capability matrix v1.41 8 = ~+56 NEW pytest PASS) + ~+7 NEW vitest PASS (TagPolicyEditor + UntaggedResourceDetectorPanel owner-only ack prompt AD-22 verbatim + AllocationRuleBuilder RTL render + ComplianceReportPanel + ReconciliationReportPanel + ko-KR SSOT 2 + finops tag governance dashboard parity CR 12-5 D-PARITY-01 결정 wire) + 0 NEW ruff + 0 regressions + T1 tag_policy_dsl + 4 enforcement_level + T2 untagged_resource_detector + 6 resource types + T3 allocation_rules_engine + 5 rule types + T4 allocation_audit + compliance report + chargeback_allocation_reconciliation + T5 alembic 0047 phase_15_tag_governance + T6 audit action EXTENSION 8 NEW + T7 capability v1.41 EXTENSION + frontend finops tag governance dashboard 5 NEW components + T8 3중 게이트 FINAL CLEAN atomic commit) + AD-42 FinOps Tag Governance & Cost Allocation 신규 결정 ((a)~(g) 7 sub-decisions 모두 결정 wire 진입). Phase 14 close-out retro `5b367d9` (cj-style 120번째) + Phase 14 atomic wire T1~T8 `e904485` (cj-style 119번째) + Phase 14 spec entry `30637f6` (cj-style 118번째) + Phase 14 PRD entry `0e3f8d9` (cj-style 117번째) 결정 verbatim preserve + EXTENSION (Phase 14 territory DONE 정합). CR 11-3 honest-DEFER discipline 121번째 epic 연속 정직 회복 검증 ✅ (D-1-1-DEFER-1/2/3 RESOLVED 보존 60~121번째 + D-LAUNCH-1-DEFER-1 honestly preserved 65~121번째 + D-EPIC-16-REVIEW-DEFER-2~6 ✅ RESOLVED 보존 78~121번째 + D-PHASE-4-DR-DEFER-1/2 ✅ RESOLVED 보존 73~121번째 + D-EPIC-17-WIRE-DEFER-T2-T3-UI ✅ RESOLVED 보존 83~121번째 + D-RETENTION-1 ✅ RESOLVED 보존 85~121번째 + D-OBSERVABILITY-1 ✅ RESOLVED 보존 89~121번째 + D-PERFORMANCE-1 ✅ RESOLVED 보존 93~121번째 + D-CHAOS-1 ✅ RESOLVED 보존 100~121번째 + D-SLO-1 ✅ RESOLVED 보존 101~121번째 + D-FINOPS-1 ✅ RESOLVED 보존 105~121번째 + D-FINOPS-2 ✅ RESOLVED 보존 109~121번째 + D-FINOPS-3 ✅ RESOLVED 보존 113~121번째 + D-FINOPS-4 ✅ RESOLVED 보존 117~121번째 + **D-FINOPS-5 신규 honestly DEFER 보존 = Phase 15 PRD entry 진입 시점에 carry-over chain 정직 회복 결정 wire 진입: Phase 14 close-out retro `5b367d9` §13 + Phase 13 close-out retro `850b4f8` §13 + Phase 12 close-out retro `3354e83` §13 + Phase 11 close-out retro `80df15b` §12 + Phase 10 close-out retro `733d428` §10 + Phase 9 close-out retro `634427d` §10 + Phase 8 close-out retro `ab495a8` §10 + Phase 7 close-out retro `326fa9f` §10 + Phase 6 close-out retro `f9f006c` §13 + Epic 17 close-out retro `be8f3bd` §11 "FinOps Tag Governance & Cost Allocation 결정 wire 보류, Phase 15+ 진입 시점" verbatim 해소**) + A36 SDR 검증 4-step 자동 적용 (commit prefix lint PASS + sprint-status structure PASS + vitest file count drift 0건 + commit consistency PASS) + CR 12-5 D-GATE-01 + D-PARITY-01 inversion 적용 보존 + A19 + CR 0-2 RLS lesson (Phase 15 finops_tag_governance tables RLS 자동 적용 + tenant-scoped tag policy 결정) + CR 1-1 audit-first INSERT (tag_policy_updated + untagged_resource_detected + allocation_rule_evaluated + allocation_rule_updated + compliance_report_generated + compliance_alert_sent + compliance_remediation_initiated + tag_governance_dry_run_executed 8 NEW + reconciliation_initiated + reconciliation_report_generated + reconciliation_investigation_triggered + reconciliation_approved + reconciliation_resolved 5 EXTENSION CR 1-1 verbatim 적용) + CR 9-6 commit message discipline (`git commit -F <file>` 사용, PowerShell here-string 회피, D5 prevention) + NFR4 PII minimization ✅ PRESERVED 모두 적용 보존. 결정 wire 일자: 2026-08-25 (KST).
  - v4.5 (2026-08-25): Phase 14 PRD entry 결정 wire DONE (cj-style 117번째 epic 연속 정직 회복 atomic docs-only wire) — 옵션 (a) Phase 14+ 진입 결정 wire (Phase 13 close-out retro `850b4f8` 116번째 + Phase 13 atomic wire T1~T8 `8b98030` 115번째 + Phase 13 spec entry `77ed55f` 114번째 + Phase 13 PRD entry `d31dfc8` 113번째 모두 wire DONE 진입 정합 보존 후 다음 territory 진입). 사용자 권장 결정 = 옵션 (a) Phase 14+ 진입 + 옵션 (a) FinOps Optimization & Rightsizing (Recommended) (optimization definition DSL + rightsizing engine 5 resource types compute/storage/database/network/container + idle resource detection z-score 기반 + RI/SP commitment recommendations 1-year/3-year break-even + optimization accuracy tracking precision/recall/realized savings + model retraining trigger + optimization dashboard UI + audit-first INSERT 6 NEW + Capability FINOPS_OPTIMIZATION 1 NEW + dry-run mode + tests + wire scope T1~T8) = Phase 14 territory 결정 wire 진입 (cj-style 117번째 epic 연속 정직 회복 진입 시점에 결정, AskUserQuestion 결정 verbatim bind). rationale 5종: ① Phase 13 wire `8b98030` 의 FinOps Forecasting & Capacity Planning territory (forecast definition DSL + 4 time series models ARIMA/Prophet/LSTM/ensemble + capacity headroom analysis compute/storage/network + budget burn-rate projection + forecast accuracy tracking MAE/MAPE/RMSE + Capability FINOPS_FORECASTING_CAPACITY_PLANNING 1 NEW + dry-run mode) 의 natural backend carry-over chain = Phase 14 = FinOps Optimization & Rightsizing territory 의 natural next (capacity headroom 의 compute/storage/network saturation 90일 lookahead forecast → forward-looking rightsizing recommendation + forecast accuracy 의 MAE/MAPE/RMSE → optimization accuracy precision/recall + budget burn-rate projection 의 projected_overrun_pct → projected_savings_pct + Phase 13 forecasting 의 historical baseline → utilization baseline for idle detection) = Phase 13 FinOps territory 의 ACTIONABLE RECOMMENDATION LAYER EXTENSION 결정 (forecast → action) ② cj-style discipline 회피 위험 방지 (49~116번째 누적 68-entry-point cycle + 116번째 Phase 13 close-out retro + 115번째 atomic wire + 114번째 spec entry + 113번째 PRD entry 패턴 모두 wire DONE 진입 정합 보존 후 즉시 Phase 14 진입 = 1-day atomic sprint discipline) ③ 비즈니스 우선순위 = enterprise 고객 onboarding 시 actionable cost optimization recommendations (compute rightsizing based on capacity headroom 90일 lookahead forecast + idle resource remediation + RI/SP commitment recommendations 1-year/3-year break-even calculation) territory 필수 ④ Phase 13 wire `8b98030` 의 capacity headroom analysis (compute/storage/network 90일 lookahead) + Phase 11 wire `e020ad0` 의 showback period selector (current/previous/last 3/6 months/YTD) + Phase 12 wire `f3c0e63` 의 anomaly detection baseline (z-score > 3.0) + Phase 8 wire `60d4ea1` 의 cost-engine 12-period benchmark 의 자연스러운 carry-over chain = Phase 14 optimization 의 utilization baseline (last 30d + last 90d from phase_13 capacity_headroom_report) + idle detection (z-score < -2.0 from phase_12 anomaly_detection baseline) + monthly optimization cycle (Phase 11 showback period EXTENSION) + optimization accuracy tracking (precision/recall MAE/MAPE EXTENSION) 정합 ⑤ 1st release close-out retro + Epic 17 close-out retro + Phase 5~13 close-out retro territory verbatim 보존 + AD-22 owner-only RBAC + Epic 12 2FA 챌린지 보존 + AD-14 stack pin + NFR4 PII minimization ✅ PRESERVED + AD-41 FinOps Optimization & Rightsizing 신규 결정 ((a)~(g) 7 sub-decisions 모두 결정 wire 진입). master PRD v4.4 → v4.5 atomic edit (docs only). **§F30 FinOps Optimization & Rightsizing territory 신규** (F30.1 optimization definition DSL (`apps/api/modules/finops/optimization_definition.py` NEW ~+150 LOC, syntax: `define_optimization(tenant_id, resource_type, optimization_strategy, target_metric, baseline_period)` builder + AST 5 levels + 5 resource_type 옵션 (compute + storage + database + network + container) + 7 optimization_strategy 옵션 (rightsize_down + rightsize_up + idle_terminate + commit_1y + commit_3y + storage_tier_down + composite) + target_metric enum (cost_saving_pct + cost_saving_amount + utilization_target + commit_break_even_months) + baseline_period enum (last_7d + last_30d + last_90d + last_180d + last_365d) + 4 industries baseline + per-tenant override EXTENSION + policy evaluation precedence tenant override > industry baseline > system default + `OPTIMIZATION_DEFAULTS = {'resource_type': 'compute', 'optimization_strategy': 'composite', 'target_metric': 'cost_saving_pct', 'baseline_period': 'last_30d', 'idle_cpu_threshold_pct': 5.0, 'idle_detection_window_days': 30, 'min_savings_amount_krw': 10000}` constants + audit-first INSERT `optimization_definition_updated` CR 1-1 verbatim) / F30.2 rightsizing engine (`apps/api/modules/finops/rightsizing_engine.py` NEW ~+200 LOC + 5 resource types (compute EC2 instance type 한 단계 downsize/upsize + storage tier downgrade S3 Standard → IA → Glacier + database RDS instance class 한 단계 downsize + network EIP/NAT/LB + container EKS node group) + Phase 13 wire `8b98030` capacity_headroom_report 의 90일 lookahead forecast EXTENSION + 80+ AWS EC2 instance type mapping + 3 storage tier mapping + RightsizingRecommendation TypedDict 14 fields (recommendation_id + tenant_id + resource_id + resource_type + current_instance_type + recommended_instance_type + current_cost_krw + recommended_cost_krw + projected_savings_pct + projected_savings_amount_krw + confidence_score + severity enum + model_version + generated_at) + audit-first INSERT `recommendation_generated` CR 1-1 verbatim) / F30.3 idle resource detection (`apps/api/modules/finops/idle_resource_detector.py` NEW ~+180 LOC + z-score < -2.0 기반 + compute idle (CPU < 5% for 30d) + storage idle (S3 last_accessed > 30d + EBS unattached + snapshot > 90d) + database idle (RDS connection_count_p95 = 0 for 30d) + network idle (EIP associated=false + NAT gateway bytes=0 + LB request < 100/day) + container idle (EKS desired_count × utilization < 30%) + IdleResource TypedDict 13 fields + audit-first INSERT `idle_resource_detected` CR 1-1 verbatim) / F30.4 RI/SP commitment recommendations (`apps/api/modules/finops/commitment_recommender.py` NEW ~+150 LOC + commitment_type enum (ec2_ri + rds_ri + ec2_sp + s3_sp + redshift_sp + dynamodb_sp) + commitment_term enum (1_year + 3_year) + break-even calculation `break_even_months = upfront_cost / monthly_savings` + 1y ≤ 8mo / 3y ≤ 18mo break-even threshold + utilization_forecast 기반 권고 + Phase 13 wire `8b98030` forecast 12-month baseline EXTENSION + CommitmentRecommendation TypedDict 12 fields + ROI calculation + per-tenant override + audit-first INSERT `commitment_recommended` CR 1-1 verbatim) / F30.5 optimization accuracy tracking (`apps/api/modules/finops/optimization_accuracy_tracker.py` NEW ~+120 LOC + Phase 13 wire `8b98030` forecast_accuracy_tracker EXTENSION + precision (TP / (TP + FP)) + recall (TP / (TP + FN)) + realized_savings vs projected_savings + model performance degradation detection (accuracy_score < 70% for 3 consecutive months → retraining trigger) + retraining cron KST 매주 일요일 04:00 UTC 19:00 + OptimizationAccuracyReport TypedDict 10 fields + audit-first INSERT `optimization_recommended_action` + `optimization_accuracy_degraded` + `optimization_retraining_triggered` EXTENSION) / F30.6 optimization dashboard UI (`apps/web/app/[locale]/(dashboard)/admin/finops/optimization/page.tsx` NEW ~+150 LOC + 5 components (OptimizationStrategySelector + RightsizingRecommendationTable + IdleResourcePanel + CommitmentRecommendationPanel + OptimizationAccuracyPanel) + owner-only RBAC AD-22 verbatim + Epic 12 2FA 챌린지 보존 + ko-KR.json `finops_optimization.*` namespace EXTENSION ~30 keys CR 11-4 D-002 verbatim SSOT 결정 wire) / F30.7 Capability matrix v1.40 EXTENSION FINOPS_OPTIMIZATION (capability matrix v1.39 → v1.40 EXTENSION 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅ CR 12-1 L4 precedent 미러, FINOPS_FORECASTING_CAPACITY_PLANNING Phase 13 wire + FINOPS_ANOMALY_DETECTION + FINOPS_BUDGET_ALERT Phase 12 wire + FINOPS_SHOWBACK + FINOPS_CHARGEBACK Phase 11 wire + SLO_ENGINEERING Phase 10 wire + CHAOS_ENGINEERING Phase 9 wire + PERFORMANCE_TESTING Phase 8 wire + OBSERVABILITY_TRACES + OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind + `apps/api/core/capability.py` MODIFIED + Capability.FINOPS_OPTIMIZATION = "finops_optimization" 1 NEW enum + `apps/api/dependencies/capability.py` MODIFIED require_finops_optimization 1 NEW dep + drift detector `tests/integration/test_capability_matrix_v1_40_drift.py` NEW 8 NEW pytest cases) / F30.8 dry-run + tests + wire scope T1~T8 결정 (~+56 NEW pytest PASS (optimization_definition 6 + rightsizing_engine 8 + idle_resource_detector 8 + commitment_recommender 8 + optimization_accuracy_tracker 5 + alembic 0046 5 + audit action 8 + capability matrix v1.40 8 = ~+56 NEW pytest PASS) + ~+6 NEW vitest PASS (OptimizationStrategySelector + RightsizingRecommendationTable owner-only ack prompt AD-22 verbatim + IdleResourcePanel RTL render + CommitmentRecommendationPanel + ko-KR SSOT 2 + finops optimization dashboard parity CR 12-5 D-PARITY-01 결정 wire) + 0 NEW ruff + 0 regressions + T1 optimization_definition + optimization_dsl module + T2 rightsizing_engine + 5 resource types + T3 idle_resource_detector + z-score detection + T4 commitment_recommender + 1y/3y break-even + Phase 13 forecast accuracy EXTENSION + T5 alembic 0046 phase_14_optimization 5 tables + RLS 5 tables + T6 audit action EXTENSION 6 NEW + T7 capability v1.40 EXTENSION + frontend finops optimization dashboard 5 NEW components + T8 3중 게이트 FINAL CLEAN atomic commit) + AD-41 FinOps Optimization & Rightsizing 신규 결정 ((a)~(g) 7 sub-decisions 모두 결정 wire 진입). Phase 13 close-out retro `850b4f8` (cj-style 116번째) + Phase 13 atomic wire T1~T8 `8b98030` (cj-style 115번째) + Phase 13 spec entry `77ed55f` (cj-style 114번째) + Phase 13 PRD entry `d31dfc8` (cj-style 113번째) 결정 verbatim preserve + EXTENSION (Phase 13 territory DONE 정합). CR 11-3 honest-DEFER discipline 117번째 epic 연속 정직 회복 검증 ✅ (D-1-1-DEFER-1/2/3 RESOLVED 보존 60~117번째 + D-LAUNCH-1-DEFER-1 honestly preserved 65~117번째 + D-EPIC-16-REVIEW-DEFER-2~6 ✅ RESOLVED 보존 78~117번째 + D-PHASE-4-DR-DEFER-1/2 ✅ RESOLVED 보존 73~117번째 + D-EPIC-17-WIRE-DEFER-T2-T3-UI ✅ RESOLVED 보존 83~117번째 + D-RETENTION-1 ✅ RESOLVED 보존 85~117번째 + D-OBSERVABILITY-1 ✅ RESOLVED 보존 89~117번째 + D-PERFORMANCE-1 ✅ RESOLVED 보존 93~117번째 + D-CHAOS-1 ✅ RESOLVED 보존 100~117번째 + D-SLO-1 ✅ RESOLVED 보존 101~117번째 + D-FINOPS-1 ✅ RESOLVED 보존 105~117번째 + D-FINOPS-2 ✅ RESOLVED 보존 109~117번째 + D-FINOPS-3 ✅ RESOLVED 보존 113~117번째 + **D-FINOPS-4 신규 honestly DEFER 보존 = Phase 14 PRD entry 진입 시점에 carry-over chain 정직 회복 결정 wire 진입: Phase 13 close-out retro `850b4f8` §13 + Phase 12 close-out retro `3354e83` §13 + Phase 11 close-out retro `80df15b` §12 + Phase 10 close-out retro `733d428` §10 + Phase 9 close-out retro `634427d` §10 + Phase 8 close-out retro `ab495a8` §10 + Phase 7 close-out retro `326fa9f` §10 + Phase 6 close-out retro `f9f006c` §13 + Epic 17 close-out retro `be8f3bd` §11 "FinOps Optimization & Rightsizing 결정 wire 보류, Phase 14+ 진입 시점" verbatim 해소**) + A36 SDR 검증 4-step 자동 적용 (commit prefix lint PASS + sprint-status structure PASS + vitest file count drift 0건 + commit consistency PASS) + CR 12-5 D-GATE-01 + D-PARITY-01 inversion 적용 보존 + A19 + CR 0-2 RLS lesson (Phase 14 finops_optimization tables RLS 자동 적용 + tenant-scoped optimization definition 결정) + CR 1-1 audit-first INSERT (optimization_definition_updated + recommendation_generated + idle_resource_detected + commitment_recommended + optimization_recommended_action + optimization_dry_run_executed 6 NEW + optimization_accuracy_degraded + optimization_retraining_triggered 2 EXTENSION CR 1-1 verbatim 적용) + CR 9-6 commit message discipline (`git commit -F <file>` 사용, PowerShell here-string 회피, D5 prevention) + NFR4 PII minimization ✅ PRESERVED 모두 적용 보존. 결정 wire 일자: 2026-08-25 (KST).
  - v4.4 (2026-08-24): Phase 13 PRD entry 결정 wire DONE (cj-style 113번째 epic 연속 정직 회복 atomic docs-only wire) — 옵션 (a) Phase 13+ 진입 결정 wire (Phase 12 close-out retro `3354e83` 112번째 + Phase 12 atomic wire T1~T8 `f3c0e63` 111번째 + Phase 12 spec entry `8c5f374` 110번째 + Phase 12 PRD entry `344c7eb` 109번째 모두 wire DONE 진입 정합 보존 후 다음 territory 진입). 사용자 권장 결정 = 옵션 (a) Phase 13+ 진입 + 옵션 (a) FinOps Forecasting & Capacity Planning (Recommended) (forecast definition DSL + cost forecasting engine time series 4 models ARIMA/Prophet/LSTM/ensemble + capacity headroom analysis compute/storage/network + budget burn-rate projection Phase 12 carry-over + forecast accuracy tracking MAE/MAPE/RMSE + model retraining trigger + forecasting dashboard UI + audit-first INSERT 7 NEW + Capability FINOPS_FORECASTING_CAPACITY_PLANNING 1 NEW + dry-run mode + tests + wire scope T1~T8) = Phase 13 territory 결정 wire 진입 (cj-style 113번째 epic 연속 정직 회복 진입 시점에 결정, AskUserQuestion 결정 verbatim bind). rationale 5종: ① Phase 12 wire `f3c0e63` 의 Cost Anomaly Detection & Budget Alerting territory (anomaly detection DSL 4 methods z-score/IQR/EWMA/isolation forest + budget definition DSL + anomaly detection engine + alert routing Slack/PagerDuty/Email + budget overrun alerts + forecast accuracy tracking MAE/MAPE/RMSE + Capability FINOPS_ANOMALY_DETECTION + FINOPS_BUDGET_ALERT 2 NEW + dry-run mode) 의 natural backend carry-over chain = Phase 13 = FinOps Forecasting & Capacity Planning territory 의 natural next (anomaly detection 의 historical baseline → forward forecast EXTENSION + budget overrun 의 linear extrapolation → ARIMA/Prophet/LSTM time series forecast + forecast accuracy 의 MAE/MAPE/RMSE → forward-looking forecasting engine + capacity planning 의 compute/storage/network saturation forecast) = Phase 12 FinOps territory 의 FORECASTING & PLANNING LAYER EXTENSION 결정 ② cj-style discipline 회피 위험 방지 (49~112번째 누적 64-entry-point cycle + 112번째 Phase 12 close-out retro + 111번째 atomic wire + 110번째 spec entry + 109번째 PRD entry 패턴 모두 wire DONE 진입 정합 보존 후 즉시 Phase 13 진입 = 1-day atomic sprint discipline) ③ 비즈니스 우선순위 = enterprise 고객 onboarding 시 forward-looking 비용 forecast (12-month prediction with 95% confidence interval) + capacity headroom analysis (compute/storage saturation 90일 lookahead) + budget burn-rate projection (Phase 12 budget_alert 의 linear extrapolation EXTENSION) territory 필수 (Phase 11 wire `e020ad0` showback period selector + 12-period comparison + Phase 12 wire `f3c0e63` anomaly detection baseline last 30d/90d/YTD + forecast_deviation tracking + Phase 8 wire `60d4ea1` cost-engine 12-period benchmark 의 자연스러운 carry-over) ④ Phase 12 wire `f3c0e63` 의 anomaly detection baseline (last 30d + last 90d + YTD) + forecast accuracy tracking (MAE + MAPE + RMSE + model_retraining_trigger) + Phase 11 wire `e020ad0` showback period selector (current month + previous month + last 3/6 months + YTD + custom range) + Phase 8 wire `60d4ea1` cost-engine 12-period benchmark 의 자연스러운 carry-over chain = Phase 13 forecasting 의 historical baseline (last 12-month time series for ARIMA/Prophet training) + 12-month forward forecast (95% CI) + capacity headroom analysis (90일 lookahead) + model retraining cron (Phase 12 carry-over pattern verbatim) EXTENSION 정합 ⑤ 1st release close-out retro + Epic 17 close-out retro + Phase 5 close-out retro + Phase 6 close-out retro + Phase 7 close-out retro + Phase 8 close-out retro + Phase 9 close-out retro + Phase 10 close-out retro + Phase 11 close-out retro + Phase 12 close-out retro territory verbatim 보존 + AD-22 owner-only RBAC + Epic 12 2FA 챌린지 보존 + AD-14 stack pin + NFR4 PII minimization ✅ PRESERVED + AD-40 FinOps Forecasting & Capacity Planning 신규 결정 ((a)~(g) 7 sub-decisions 모두 결정 wire 진입). master PRD v4.3 → v4.4 atomic edit (docs only). **§F29 FinOps Forecasting & Capacity Planning territory 신규** (F29.1 forecast definition DSL (`apps/api/modules/finops/forecast_definition.py` NEW ~+150 LOC, syntax: `define_forecast(tenant_id, target_metric, horizon_months, model_type, confidence_level)` builder + AST 5 levels (tenant_id + target_metric + horizon_months + model_type + confidence_level) + 5 target_metric 옵션 (department + cost_center + product_line + service + tenant_total) + horizon_months enum (3m + 6m + 12m + 24m) + 4 model_type 옵션 (ARIMA + Prophet + LSTM + ensemble) + confidence_level enum (80 + 90 + 95 + 99) + 4 industries baseline + per-tenant override EXTENSION + policy evaluation precedence tenant override > industry baseline > system default + `FORECAST_DEFAULTS = {'horizon_months': 12, 'model_type': 'ensemble', 'confidence_level': 95, 'retraining_cron': '0 3 * * 0'}` constants + audit-first INSERT `forecast_definition_updated` CR 1-1 verbatim) / F29.2 cost forecasting engine (`apps/api/modules/finops/forecast_engine.py` NEW ~+200 LOC + 4 time series models (ARIMA p,d,q + Prophet seasonality + LSTM hidden_layers=50 + ensemble voting consensus) + 12-month forward forecast with 95% CI + seasonality detection (weekly + monthly + quarterly) + trend decomposition + holiday calendar KST 7 holidays + per-tenant model training (last 12-month historical baseline) + model_version semantic versioning MAJOR.MINOR.PATCH + forecast result TypedDict 10 fields (forecast_id + tenant_id + target_metric + horizon_months + predicted_values List + confidence_lower List + confidence_upper List + model_version + generated_at) + audit-first INSERT `forecast_generated` CR 1-1 verbatim) / F29.3 capacity headroom analysis (`apps/api/modules/finops/capacity_headroom.py` NEW ~+180 LOC + compute saturation forecast (CPU/memory utilization 90일 lookahead) + storage saturation forecast (DB size growth + backup storage capacity 90일 lookahead) + network saturation forecast (egress/ingress bandwidth 90일 lookahead) + headroom_pct 계산 + critical threshold (headroom < 20% → critical alert) + warning threshold (headroom < 40% → warning alert) + per-tenant override + audit-first INSERT `capacity_headroom_analyzed` CR 1-1 verbatim) / F29.4 budget burn-rate projection (Phase 12 wire `f3c0e63` budget_alert carry-over chain + `apps/api/modules/finops/budget_burnrate.py` NEW ~+150 LOC + `project_budget_consumption(tenant_id, period_key) -> BurnRateProjection` TypedDict 12 fields (tenant_id + period_key + budget_id + budget_amount + current_consumption + days_elapsed + days_remaining + projected_total + projected_overrun_pct + burn_rate_per_day + confidence + trace_id) + linear extrapolation + ARIMA-based projection (Phase 12 baseline EXTENSION) + 7/30-day rolling projection + predicted_overrun_pct > 110% → warning alert 발송 + audit-first INSERT `budget_burn_rate_projected` CR 1-1 verbatim) / F29.5 forecast accuracy tracking + model retraining trigger (Phase 12 wire `f3c0e63` forecast_accuracy.py EXTENSION + `apps/api/modules/finops/forecast_accuracy_tracker.py` NEW ~+120 LOC + MAE + MAPE + RMSE 3 metrics per (tenant_id, target_metric, model_type) granularity + per-model accuracy tracking + ensemble vs individual model comparison + model_performance degradation detection (MAPE > 20% for 3 consecutive periods) → model retraining trigger + audit-first INSERT `forecast_accuracy_degraded` CR 1-1 verbatim + `model_retraining_triggered` 1 NEW action) / F29.6 forecasting dashboard UI (`apps/web/app/[locale]/(dashboard)/admin/finops/forecast/page.tsx` NEW ~+150 LOC + 4 components (ForecastHorizonSelector + ForecastChart + CapacityHeadroomGauge + BudgetBurnRatePanel) + owner-only RBAC AD-22 verbatim + Epic 12 2FA 챌린지 보존 + ko-KR.json `finops_forecast.*` namespace EXTENSION ~30 keys CR 11-4 D-002 verbatim SSOT 결정 wire) / F29.7 Capability matrix v1.38 EXTENSION FINOPS_FORECASTING_CAPACITY_PLANNING (capability matrix v1.37 → v1.38 EXTENSION 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅ CR 12-1 L4 precedent 미러, FINOPS_ANOMALY_DETECTION + FINOPS_BUDGET_ALERT Phase 12 wire + FINOPS_SHOWBACK + FINOPS_CHARGEBACK Phase 11 wire + SLO_ENGINEERING Phase 10 wire + CHAOS_ENGINEERING Phase 9 wire + PERFORMANCE_TESTING Phase 8 wire + OBSERVABILITY_TRACES + OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind + `apps/api/core/capability.py` MODIFIED + Capability.FINOPS_FORECASTING_CAPACITY_PLANNING = "finops_forecasting_capacity_planning" 1 NEW enum + `apps/api/dependencies/capability.py` MODIFIED require_finops_forecasting_capacity_planning 1 NEW dep + drift detector `tests/integration/test_capability_matrix_v1_38_drift.py` NEW 8 NEW pytest cases) / F29.8 dry-run + tests + wire scope T1~T8 결정 (~+50 NEW pytest PASS (forecast_definition 6 + forecast_engine 8 + capacity_headroom 6 + budget_burnrate 5 + forecast_accuracy_tracker 4 + alembic 0045 4 + audit action 7 + capability matrix v1.38 8 = ~+48 NEW pytest PASS) + ~+6 NEW vitest PASS (ForecastHorizonSelector + ForecastChart owner-only ack prompt AD-22 verbatim + CapacityHeadroomGauge RTL render + BudgetBurnRatePanel + ko-KR SSOT 2 + finops forecast dashboard parity CR 12-5 D-PARITY-01 결정 wire) + 0 NEW ruff + 0 regressions + T1 forecast_definition + forecast_dsl module + T2 forecast_engine + 4 time series models + T3 capacity_headroom + compute/storage/network + T4 budget_burnrate + Phase 12 budget_alert carry-over + T5 alembic 0045 phase_13_forecasting + T6 audit action EXTENSION 7 NEW + T7 capability v1.38 EXTENSION + frontend finops forecast dashboard + T8 3중 게이트 FINAL CLEAN atomic commit) + AD-40 FinOps Forecasting & Capacity Planning 신규 결정 ((a)~(g) 7 sub-decisions 모두 결정 wire 진입). Phase 12 close-out retro `3354e83` (cj-style 112번째) + Phase 12 atomic wire T1~T8 `f3c0e63` (cj-style 111번째) + Phase 12 spec entry `8c5f374` (cj-style 110번째) + Phase 12 PRD entry `344c7eb` (cj-style 109번째) 결정 verbatim preserve + EXTENSION (Phase 12 territory DONE 정합) + Phase 11 close-out retro `80df15b` (cj-style 108번째) + Phase 11 atomic wire T1~T8 `e020ad0` (cj-style 107번째) + Phase 11 spec entry `82c93a8` (cj-style 106번째) + Phase 11 PRD entry `16d7698` (cj-style 105번째) + Phase 10 close-out retro `733d428` (cj-style 104번째) + Phase 10 atomic wire T1~T8 `ac5d6c5` (cj-style 103번째) + Phase 10 spec entry `3c80ef0` (cj-style 102번째) + Phase 10 PRD entry `09db4d4` (cj-style 101번째) + Phase 9 close-out retro `634427d` (cj-style 100번째) + Phase 9 atomic wire T1~T8 `e7670e1` (cj-style 99번째) + Phase 9 spec entry `2a5e4da` (cj-style 98번째) + Phase 9 PRD entry `0b2d2f3` (cj-style 97번째) + Phase 8 close-out retro `ab495a8` (cj-style 96번째) + Phase 8 atomic wire T1~T8 `60d4ea1` (cj-style 95번째) + Phase 8 spec entry `5ae0f4e` (cj-style 94번째) + Phase 8 PRD entry `ced452f` (cj-style 93번째) + build-fixes sprint `eaee198` + Phase 7 close-out retro `326fa9f` (cj-style 92번째) + Phase 7 atomic wire T1~T8 `59b56cd` (cj-style 91번째) + Phase 7 spec entry `749381e` (cj-style 90번째) + Phase 7 PRD entry `916a541` (cj-style 89번째) + Phase 6 close-out retro `f9f006c` (cj-style 88번째) + Phase 6 atomic wire T1~T8 `24e1cd7` (cj-style 87번째) + Phase 6 spec entry `f5c14c9` (cj-style 86번째) + Phase 6 PRD entry `e84a281` (cj-style 85번째) + Epic 17 close-out retro `be8f3bd` (cj-style 84번째) + Epic 17 T2+T3 UI wire `bb92879` (cj-style 83번째) + Epic 17 wire `2ada2ec` (cj-style 82번째) + Epic 17 spec entry `f4b2b58` (cj-style 81번째) + Epic 17 PRD entry `40a9c41` (cj-style 80번째) + Sidebar/MenuProvider hot-fix `01a06e4` (cj-style 79번째) + D-EPIC-16-REVIEW-DEFER-2~6 RESOLVE sprint `512ed6a` (cj-style 78번째) + Phase 5 close-out retro `b843565` (cj-style 76~77번째) + Phase 5 wire `f093f8c` (cj-style 75번째) + Phase 5 spec entry (cj-style 74번째) + Phase 5 PRD entry `93d852b` (cj-style 73번째) + Epic 16 close-out retro (cj-style 72번째) + Epic 16 T4 admin UI follow-up `ff5c3b5` (cj-style 71번째) + Epic 16 review follow-up `963079c` (cj-style 70번째) + Epic 16 atomic wire `e117e09` (cj-style 69번째) + Epic 16 PRD entry `08bfca5` (cj-style 67번째) + 1st release cycle cj-style 62~66번째 모두 wire DONE 진입 + Epic 15 cycle cj-style 58~61번째 모두 wire DONE 진입 + Phase 4 cycle cj-style 53~57번째 모두 wire DONE 진입 + Phase 3 cycle cj-style 49~52번째 모두 wire DONE 진입 + Epic 14 LISTEN/NOTIFY multi-process `7835463` + Epic 13 LISTEN/NOTIFY consume `f2ea2f6` + Epic 12 2FA 게이트 `a63646c` (Phase 13 forecasting & capacity planning 모두 owner-only RBAC AD-22 + Epic 12 2FA 챌린지 보존) + Epic 11 close-out retro + Phase 2 close-out baseline 599 passed 정합 보존 + Epic 1 carry-over (auth) layout + onboarding/industry 보존 + Epic 7~10 ABC/TDABC + AI 인사이트 territory 결정 wire 보존 결정 wire 모두 보존. CR 11-3 honest-DEFER discipline 113번째 epic 연속 정직 회복 검증 ✅ (D-1-1-DEFER-1/2/3 RESOLVED 보존 60~113번째 + D-LAUNCH-1-DEFER-1 honestly preserved 65~113번째 + D-EPIC-16-REVIEW-DEFER-2~6 ✅ RESOLVED 보존 78~113번째 + D-PHASE-4-DR-DEFER-1/2 ✅ RESOLVED 보존 73~113번째 + D-EPIC-17-WIRE-DEFER-T2-T3-UI ✅ RESOLVED 보존 83~113번째 + D-RETENTION-1 ✅ RESOLVED 보존 85~113번째 + D-OBSERVABILITY-1 ✅ RESOLVED 보존 89~113번째 + D-PERFORMANCE-1 ✅ RESOLVED 보존 93~113번째 + D-CHAOS-1 ✅ RESOLVED 보존 100~113번째 + D-SLO-1 ✅ RESOLVED 보존 101~113번째 + D-FINOPS-1 ✅ RESOLVED 보존 105~113번째 + D-FINOPS-2 ✅ RESOLVED 보존 109~113번째 + **D-FINOPS-3 신규 honestly DEFER 보존 = Phase 13 PRD entry 진입 시점에 carry-over chain 정직 회복 결정 wire 진입: Phase 12 close-out retro `3354e83` §13 + Phase 11 close-out retro `80df15b` §12 + Phase 10 close-out retro `733d428` §10 + Phase 9 close-out retro `634427d` §10 + Phase 8 close-out retro `ab495a8` §10 + Phase 7 close-out retro `326fa9f` §10 + Phase 6 close-out retro `f9f006c` §13 + Epic 17 close-out retro `be8f3bd` §11 "FinOps Forecasting & Capacity Planning 결정 wire 보류, Phase 13+ 진입 시점" verbatim 해소**) + A36 SDR 검증 4-step 자동 적용 (commit prefix lint PASS + sprint-status structure PASS + vitest file count drift 0건 + commit consistency PASS) + CR 12-5 D-GATE-01 + D-PARITY-01 inversion 적용 보존 + A19 + CR 0-2 RLS lesson (Phase 13 finops_forecast tables RLS 자동 적용 + tenant-scoped forecast definition 결정) + CR 1-1 audit-first INSERT (forecast_definition_updated + forecast_generated + capacity_headroom_analyzed + budget_burn_rate_projected + forecast_accuracy_degraded + model_retraining_triggered + forecast_dry_run_executed 7 NEW CR 1-1 verbatim 적용) + CR 9-6 commit message discipline (`git commit -F <file>` 사용, PowerShell here-string 회피, D5 prevention) + NFR4 PII minimization ✅ PRESERVED 모두 적용 보존. 결정 wire 일자: 2026-08-24 (KST).
  - v4.3 (2026-08-24): Phase 12 PRD entry 결정 wire DONE (cj-style 109번째 epic 연속 정직 회복 atomic docs-only wire) — 옵션 (a) Phase 12+ 진입 결정 wire (Phase 11 close-out retro `80df15b` 108번째 + Phase 11 atomic wire T1~T8 `e020ad0` 107번째 + Phase 11 spec entry `82c93a8` 106번째 + Phase 11 PRD entry `16d7698` 105번째 모두 wire DONE 진입 정합 보존 후 다음 territory 진입). 사용자 권장 결정 = 옵션 (a) Phase 12+ 진입 + 옵션 (a) Cost Anomaly Detection & Budget Alerting (Recommended) (anomaly detection DSL 4 methods z-score + IQR + EWMA + isolation forest + budget definition DSL + alert routing Slack + PagerDuty + forecast accuracy tracking + audit-first INSERT 4 NEW + Capability FINOPS_ANOMALY_DETECTION + FINOPS_BUDGET_ALERT 2 NEW + dry-run mode + tests + wire scope T1~T8) = Phase 12 territory 결정 wire 진입 (cj-style 109번째 epic 연속 정직 회복 진입 시점에 결정, AskUserQuestion 결정 verbatim bind). rationale 5종: ① Phase 11 wire `e020ad0` FinOps Showback / Chargeback territory (showback DSL department breakdown + period selector + comparison view + chargeback cost allocation engine 정액제/비율배분/usage-based 3종 + markup + tax + department cost center mapping tenant_settings JSONB + showback dashboard UI + chargeback CSV/PDF export) 의 natural backend carry-over chain = Phase 12 = Cost Anomaly Detection & Budget Alerting territory 의 natural next (showback baseline 대비 deviation 감지 = cost anomaly detection + chargeback 한도 초과 알림 = budget alert + statistical + ML hybrid detection methods + alert routing/escalation) = Phase 11 FinOps territory 의 DETECTION & ALERTING LAYER EXTENSION 결정 ② cj-style discipline 회피 위험 방지 (49~108번째 누적 60-entry-point cycle + 108번째 Phase 11 close-out retro + 107번째 atomic wire + 106번째 spec entry + 105번째 PRD entry 패턴 모두 wire DONE 진입 정합 보존 후 즉시 Phase 12 진입 = 1-day atomic sprint discipline) ③ 비즈니스 우선순위 = enterprise 고객 onboarding 시 cost anomaly 자동 감지 (showback baseline 대비 z-score > 3.0 deviation) + budget overrun 알림 (chargeback 한도 90% 초과 시 Slack + 100% 초과 시 PagerDuty) territory 필수 (Phase 11 wire `e020ad0` FinOps showback 의 monthly baseline + Phase 7 wire `59b56cd` 의 7 NEW business metrics + Phase 8 wire `60d4ea1` 의 k6 부하 테스트 + Phase 9 wire `e7670e1` chaos_experiment baseline + Phase 10 wire `ac5d6c5` SLO violation_detected 의 자연스러운 carry-over) ④ Phase 11 wire `e020ad0` 의 showback period selector + 12-period comparison + cost-engine benchmark V8 골든 + Phase 8 wire `60d4ea1` 의 cost-engine 12-period benchmark 의 자연스러운 carry-over chain = Phase 12 anomaly detection 의 historical baseline (last 30d + last 90d + YTD) + statistical model training + forecast deviation tracking EXTENSION 정합 ⑤ 1st release close-out retro + Epic 17 close-out retro + Phase 5 close-out retro + Phase 6 close-out retro + Phase 7 close-out retro + Phase 8 close-out retro + Phase 9 close-out retro + Phase 10 close-out retro + Phase 11 close-out retro territory verbatim 보존 + AD-22 owner-only RBAC + Epic 12 2FA 챌린지 보존 + AD-14 stack pin + NFR4 PII minimization ✅ PRESERVED + AD-39 Cost Anomaly Detection & Budget Alerting 신규 결정 ((a)~(g) 7 sub-decisions 모두 결정 wire 진입). master PRD v4.2 → v4.3 atomic edit (docs only). **§F28 Cost Anomaly Detection & Budget Alerting territory 신규** (F28.1 anomaly detection DSL (`apps/api/modules/finops/anomaly_detection.py` NEW ~+150 LOC, syntax: `detect_anomaly(tenant_id, period_key, dimension, threshold_method)` builder + 4 detection methods (z_score + IQR + EWMA + isolation_forest) + 5 dimension 옵션 (department + cost_center + product_line + service + tenant_total) + 4 industries baseline + per-tenant override EXTENSION + policy evaluation precedence tenant override > industry baseline > system default + `ANOMALY_THRESHOLD_DEFAULTS = {'z_score': 3.0, 'iqr_multiplier': 1.5, 'ewma_lambda': 0.3, 'isolation_forest_contamination': 0.1}` constants + audit-first INSERT `anomaly_detected` CR 1-1 verbatim) / F28.2 budget definition DSL (`apps/api/modules/finops/budget_definition.py` NEW ~+150 LOC + budget_period enum (monthly + quarterly + yearly) + budget_amount NUMERIC(20, 2) + budget_scope enum (tenant + department + cost_center + product_line) + alert_thresholds TypedDict (warning_pct 80 + critical_pct 90 + exceeded_pct 100 + warning_alert_channels ['slack'] + critical_alert_channels ['slack', 'pagerduty'] + exceeded_alert_channels ['slack', 'pagerduty', 'email']) + 4 industries baseline + per-tenant override EXTENSION + audit-first INSERT `budget_definition_updated` CR 1-1 verbatim) / F28.3 anomaly detection engine + alert routing (`apps/api/modules/finops/anomaly_detection_engine.py` NEW ~+180 LOC + detect_anomalies(tenant_id, period_key, dimension) -> List[AnomalyResult] TypedDict 12 fields (anomaly_id + tenant_id + period_key + dimension + dimension_value + observed_value + expected_value + deviation_pct + z_score + severity enum warning/critical + detection_method + detected_at) + multi-method voting consensus (3 of 4 methods agree = anomaly confirmed) + false positive suppression (require 3 consecutive periods) + Slack webhook integration `#bizup-finops-alerts` channel + PagerDuty integration `pd_anomaly_critical` service + audit-first INSERT `alert_sent` CR 1-1 verbatim) / F28.4 budget overrun alerts (`apps/api/modules/finops/budget_alert.py` NEW ~+150 LOC + check_budget_alerts(tenant_id, period_key) -> List[BudgetAlertResult] TypedDict 10 fields (alert_id + tenant_id + period_key + budget_id + budget_amount + actual_amount + consumption_pct + severity + alert_channels + sent_at) + real-time consumption tracking (per-chargeback INSERT trigger) + threshold-based alerting (80% warning + 90% critical + 100% exceeded) + audit-first INSERT `budget_threshold_exceeded` CR 1-1 verbatim + `budget_alert_sent` 1 NEW action) / F28.5 forecast accuracy tracking (`apps/api/modules/finops/forecast_accuracy.py` NEW ~+120 LOC + track_forecast_deviation(tenant_id, period_key) -> ForecastAccuracyReport TypedDict 8 fields (tenant_id + period_key + predicted_amount + actual_amount + deviation_amount + deviation_pct + accuracy_score + model_version) + MAE + MAPE + RMSE 3 metrics + model performance degradation detection (MAPE > 20% for 3 consecutive periods) + audit-first INSERT `forecast_deviation` CR 1-1 verbatim) / F28.6 anomaly detection dashboard UI (`apps/web/app/[locale]/(dashboard)/admin/finops/anomaly/page.tsx` NEW ~+150 LOC + 4 components (AnomalyDetectionChart + AnomalyDetectionMethodSelector + AnomalyDetectionThresholdSlider + BudgetAlertPanel) + owner-only RBAC AD-22 verbatim + Epic 12 2FA 챌린지 보존 + ko-KR.json `finops_anomaly.*` namespace EXTENSION ~25 keys CR 11-4 D-002 verbatim SSOT 결정 wire) / F28.7 Capability matrix v1.37 EXTENSION FINOPS_ANOMALY_DETECTION + FINOPS_BUDGET_ALERT 2 NEW rows (capability matrix v1.36 → v1.37 EXTENSION 2 NEW rows industry-agnostic 4-industry grants ✅/✅/✅/✅ CR 12-1 L4 precedent 미러, FINOPS_SHOWBACK + FINOPS_CHARGEBACK Phase 11 wire + SLO_ENGINEERING Phase 10 wire + CHAOS_ENGINEERING Phase 9 wire + PERFORMANCE_TESTING Phase 8 wire + OBSERVABILITY_TRACES + OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind + `apps/api/core/capability.py` MODIFIED + Capability.FINOPS_ANOMALY_DETECTION = "finops_anomaly_detection" 1 NEW enum + Capability.FINOPS_BUDGET_ALERT = "finops_budget_alert" 1 NEW enum + `apps/api/dependencies/capability.py` MODIFIED require_finops_anomaly_detection + require_finops_budget_alert 2 NEW deps + drift detector `tests/integration/test_capability_matrix_v1_37_drift.py` NEW 10 NEW pytest cases) / F28.8 dry-run + tests + wire scope T1~T8 결정 (~+46 NEW pytest PASS (anomaly_detection 6 + anomaly_detection_engine 7 + budget_definition 5 + budget_alert 6 + forecast_accuracy 4 + alembic 0044 4 + audit action 8 + capability matrix v1.37 10 = ~+50 NEW pytest PASS) + ~+5 NEW vitest PASS (AnomalyDetectionChart + AnomalyDetectionMethodSelector owner-only ack prompt AD-22 verbatim + AnomalyDetectionThresholdSlider RTL render + ko-KR SSOT 2 + finops anomaly dashboard parity CR 12-5 D-PARITY-01 결정 wire) + 0 NEW ruff + 0 regressions + T1 anomaly_detection + anomaly_detection_dsl module + T2 budget_definition + budget_definition_dsl module + T3 anomaly_detection_engine + alert routing + T4 budget_alert + forecast_accuracy module + T5 alembic 0044 phase_12_anomaly_detection + T6 audit action EXTENSION 4 NEW + T7 capability v1.37 EXTENSION + frontend finops anomaly dashboard + T8 3중 게이트 FINAL CLEAN atomic commit) + AD-39 Cost Anomaly Detection & Budget Alerting 신규 결정 ((a)~(g) 7 sub-decisions 모두 결정 wire 진입). Phase 11 close-out retro `80df15b` (cj-style 108번째) + Phase 11 atomic wire T1~T8 `e020ad0` (cj-style 107번째) + Phase 11 spec entry `82c93a8` (cj-style 106번째) + Phase 11 PRD entry `16d7698` (cj-style 105번째) 결정 verbatim preserve + EXTENSION (Phase 11 territory DONE 정합) + Phase 10 close-out retro `733d428` (cj-style 104번째) + Phase 10 atomic wire T1~T8 `ac5d6c5` (cj-style 103번째) + Phase 10 spec entry `3c80ef0` (cj-style 102번째) + Phase 10 PRD entry `09db4d4` (cj-style 101번째) + Phase 9 close-out retro `634427d` (cj-style 100번째) + Phase 9 atomic wire T1~T8 `e7670e1` (cj-style 99번째) + Phase 9 spec entry `2a5e4da` (cj-style 98번째) + Phase 9 PRD entry `0b2d2f3` (cj-style 97번째) + Phase 8 close-out retro `ab495a8` (cj-style 96번째) + Phase 8 atomic wire T1~T8 `60d4ea1` (cj-style 95번째) + Phase 8 spec entry `5ae0f4e` (cj-style 94번째) + Phase 8 PRD entry `ced452f` (cj-style 93번째) + build-fixes sprint `eaee198` + Phase 7 close-out retro `326fa9f` (cj-style 92번째) + Phase 7 atomic wire T1~T8 `59b56cd` (cj-style 91번째) + Phase 7 spec entry `749381e` (cj-style 90번째) + Phase 7 PRD entry `916a541` (cj-style 89번째) + Phase 6 close-out retro `f9f006c` (cj-style 88번째) + Phase 6 atomic wire T1~T8 `24e1cd7` (cj-style 87번째) + Phase 6 spec entry `f5c14c9` (cj-style 86번째) + Phase 6 PRD entry `e84a281` (cj-style 85번째) + Epic 17 close-out retro `be8f3bd` (cj-style 84번째) + Epic 17 T2+T3 UI wire `bb92879` (cj-style 83번째) + Epic 17 wire `2ada2ec` (cj-style 82번째) + Epic 17 spec entry `f4b2b58` (cj-style 81번째) + Epic 17 PRD entry `40a9c41` (cj-style 80번째) + Sidebar/MenuProvider hot-fix `01a06e4` (cj-style 79번째) + D-EPIC-16-REVIEW-DEFER-2~6 RESOLVE sprint `512ed6a` (cj-style 78번째) + Phase 5 close-out retro `b843565` (cj-style 76~77번째) + Phase 5 wire `f093f8c` (cj-style 75번째) + Phase 5 spec entry (cj-style 74번째) + Phase 5 PRD entry `93d852b` (cj-style 73번째) + Epic 16 close-out retro (cj-style 72번째) + Epic 16 T4 admin UI follow-up `ff5c3b5` (cj-style 71번째) + Epic 16 review follow-up `963079c` (cj-style 70번째) + Epic 16 atomic wire `e117e09` (cj-style 69번째) + Epic 16 PRD entry `08bfca5` (cj-style 67번째) + 1st release cycle cj-style 62~66번째 모두 wire DONE 진입 + Epic 15 cycle cj-style 58~61번째 모두 wire DONE 진입 + Phase 4 cycle cj-style 53~57번째 모두 wire DONE 진입 + Phase 3 cycle cj-style 49~52번째 모두 wire DONE 진입 결정 wire 모두 보존. CR 11-3 honest-DEFER discipline 109번째 epic 연속 정직 회복 검증 ✅ (D-1-1-DEFER-1/2/3 RESOLVED 보존 60~109번째 + D-LAUNCH-1-DEFER-1 honestly preserved 65~109번째 + D-EPIC-16-REVIEW-DEFER-2~6 ✅ RESOLVED 보존 78~109번째 + D-PHASE-4-DR-DEFER-1/2 ✅ RESOLVED 보존 73~109번째 + D-EPIC-17-WIRE-DEFER-T2-T3-UI ✅ RESOLVED 보존 83~109번째 + D-RETENTION-1 ✅ RESOLVED 보존 85~109번째 + D-OBSERVABILITY-1 ✅ RESOLVED 보존 89~109번째 + D-PERFORMANCE-1 ✅ RESOLVED 보존 93~109번째 + D-CHAOS-1 ✅ RESOLVED 보존 100~109번째 + D-SLO-1 ✅ RESOLVED 보존 101~109번째 + D-FINOPS-1 ✅ RESOLVED 보존 105~109번째 + **D-FINOPS-2 honestly DEFER 보존 = Phase 12 PRD entry 진입 시점에 carry-over chain 정직 회복 결정 wire 진입: Phase 11 close-out retro `80df15b` §12 + Epic 17 close-out retro §11 + Phase 6 close-out retro §13 + Phase 7 close-out retro §10 + Phase 8 close-out retro §10 + Phase 9 close-out retro §10 + Phase 10 close-out retro `733d428` §10 + Phase 11 close-out retro `80df15b` §10 "Cost Anomaly Detection & Budget Alerting 결정 wire 보류, Phase 12+ 진입 시점" verbatim 해소**) + A36 SDR 검증 4-step 자동 적용 (commit prefix lint PASS + sprint-status structure PASS + vitest file count drift 0건 + commit consistency PASS) + CR 12-5 D-GATE-01 + D-PARITY-01 inversion 적용 보존 + A19 + CR 0-2 RLS lesson (Phase 12 finops_anomaly tables RLS 자동 적용 + tenant-scoped budget definition 결정) + CR 1-1 audit-first INSERT (anomaly_detected + budget_definition_updated + alert_sent + budget_threshold_exceeded 4 NEW CR 1-1 verbatim 적용) + CR 9-6 commit message discipline (`git commit -F <file>` 사용, PowerShell here-string 회피, D5 prevention) + NFR4 PII minimization ✅ PRESERVED 모두 적용 보존. 결정 wire 일자: 2026-08-24 (KST).
  - v4.2 (2026-08-24): Phase 11 PRD entry 결정 wire DONE (cj-style 105번째 epic 연속 정직 회복 atomic docs-only wire) — 옵션 (a) Phase 11+ 진입 결정 wire (Phase 10 close-out retro `733d428` 104번째 + Phase 10 atomic wire T1~T8 `ac5d6c5` 103번째 + Phase 10 spec entry `3c80ef0` 102번째 + Phase 10 PRD entry `09db4d4` 101번째 모두 wire DONE 진입 정합 보존 후 다음 territory 진입). 사용자 권장 결정 = 옵션 (a) Phase 11+ 진입 + 옵션 (d) FinOps Showback / Chargeback (Recommended) (showback DSL department breakdown + period selector + comparison view + chargeback cost allocation engine chargeback rule 정액제/비율배분/usage-based 3종 + markup + tax + department cost center mapping tenant_settings JSONB + showback dashboard UI 차트 + 테이블 + 비교 + chargeback CSV/PDF export Excel-compatible UTF-8 BOM + audit-first INSERT 3 NEW + Capability FINOPS_SHOWBACK + FINOPS_CHARGEBACK 2 NEW + dry-run mode + tests + wire scope T1~T8) = Phase 11 territory 결정 wire 진입 (cj-style 105번째 epic 연속 정직 회복 진입 시점에 결정, AskUserQuestion 결정 verbatim bind). rationale 5종: ① Epic 7~10 wire (`756a32a` ~ `ac5d6c5`) 의 ABC engine / TDABC / BEP slider / next-month projection / budget scenario / AI 인사이트 의 cost 분석 territory + Phase 10 wire `ac5d6c5` 의 cost-engine p99 < 5s SLO + error_budget tracker 의 30d rolling + monthly reset KST 1일 00:00 territory 의 natural backend carry-over chain = Phase 11 = FinOps Showback / Chargeback territory 의 natural next (department cost center 별 비용 배부 + showback dashboard + chargeback billing + ERP integration) = Epic 7~10 ABC/TDABC + AI 인사이트 territory 의 FINANCIAL REPORTING LAYER EXTENSION (showback 가시성 + chargeback 비용 책임) 결정 ② cj-style discipline 회피 위험 방지 (49~104번째 누적 56-entry-point cycle + 104번째 Phase 10 close-out retro + 103번째 atomic wire + 102번째 spec entry + 101번째 PRD entry 패턴 모두 wire DONE 진입 정합 보존 후 즉시 Phase 11 진입 = 1-day atomic sprint discipline) ③ 비즈니스 우선순위 = enterprise 고객 onboarding 시 department cost center 별 비용 배부 + showback dashboard + chargeback billing + ERP integration territory 필수 (Epic 7~10 의 ABC/TDABC engine cost 산출 + Phase 7 wire `59b56cd` 의 7 NEW business metrics + Phase 8 wire `60d4ea1` 의 k6 부하 테스트 + Epic 17 wire `2ada2ec` 의 audit log viewer + Phase 10 wire `ac5d6c5` 의 SLO engineering 의 자연스러운 carry-over) ④ Phase 10 wire `ac5d6c5` 의 error_budget tracker 의 monthly reset KST 1일 00:00 + 30d rolling calculation + Phase 8 wire `60d4ea1` 의 cost-engine benchmark V8 골든 fixture 의 자연스러운 carry-over chain = Phase 11 showback 의 monthly period selector + comparison view + cost-engine 12-period benchmark EXTENSION 정합 ⑤ 1st release close-out retro + Epic 17 close-out retro + Phase 5 close-out retro + Phase 6 close-out retro + Phase 7 close-out retro + Phase 8 close-out retro + Phase 9 close-out retro + Phase 10 close-out retro territory verbatim 보존 + AD-22 owner-only RBAC + Epic 12 2FA 챌린지 보존 + AD-14 stack pin + NFR4 PII minimization ✅ PRESERVED + AD-38 FinOps Showback / Chargeback 신규 결정 ((a)~(g) 7 sub-decisions 모두 결정 wire 진입). master PRD v4.1 → v4.2 atomic edit (docs only). **§F27 FinOps Showback / Chargeback territory 신규** (F27.1 showback DSL department breakdown + period selector + comparison view 결정 wire (`apps/api/modules/finops/showback_dsl.py` NEW ~+150 LOC, syntax: `showback(tenant_id, period_key, department_id, group_by)` builder + department cost center breakdown (5 group_by 옵션: department + cost_center + product_line + service + custom_tag) + period selector (current month + previous month + last 3 months + last 6 months + YTD + custom range) + comparison view (current vs previous period + variance calculation + delta_pct + delta_amount) + 4 industries baseline + per-tenant override EXTENSION + policy evaluation precedence tenant override > industry baseline > system default + `SHOWBACK_PERIOD_DEFAULTS = {'current_month': True, 'previous_month': True, 'last_3_months': False, 'last_6_months': False, 'ytd': False}` constants + audit-first INSERT `showback_generated` CR 1-1 verbatim) / F27.2 chargeback cost allocation engine (`apps/api/modules/finops/chargeback_engine.py` NEW ~+180 LOC + chargeback rule 3종 (정액제 flat_fee + 비율배분 proportional_allocation + usage-based metered) + markup_pct (default 0%, max 50% range 0~50% + 0.01 step) + tax_pct (default 10% VAT + per-tenant override EXTENSION) + cost_allocation_method enum direct/indirect/shared 결정 wire + chargeback_period_monthly + chargeback_export_format enum csv/pdf) / F27.3 department cost center mapping (`apps/api/modules/finops/department_mapping.py` NEW ~+120 LOC + `tenant_settings.cost_center_mapping` JSONB TypedDict + department_id ↔ cost_center_id 1:1 mapping + auto-create on first calculation + audit-first INSERT `department_mapping_updated` CR 1-1 verbatim + UNIQUE constraint `UNIQUE (tenant_id, department_id)` + RLS `tenant_id = current_setting('app.tenant_id')::uuid` CR 0-2 verbatim) / F27.4 showback dashboard UI (`apps/web/app/[locale]/(dashboard)/admin/finops/page.tsx` NEW ~+150 LOC + 4 components (ShowbackPeriodSelector + ShowbackDepartmentBreakdownChart + ShowbackComparisonView + ShowbackCSVExportButton) + owner-only RBAC AD-22 verbatim + Epic 12 2FA 챌린지 보존 + ko-KR.json `finops.*` namespace EXTENSION ~25 keys CR 11-4 D-002 verbatim SSOT 결정 wire) / F27.5 chargeback CSV/PDF export (`apps/api/modules/finops/chargeback_export.py` NEW ~+150 LOC + `export_chargeback_csv(tenant_id, period_key, actor_id) -> StreamingResponse` 결정 wire (Excel-compatible UTF-8 BOM + comma-separated + double-quote escape for amount_json) + `export_chargeback_pdf(tenant_id, period_key, actor_id) -> bytes` 결정 wire (reportlab 기반 PDF generation + company logo + department breakdown 차트 PDF 임베드) + streaming response + audit-first INSERT `chargeback_exported` CR 1-1 verbatim) / F27.6 Capability matrix v1.36 EXTENSION FINOPS_SHOWBACK + FINOPS_CHARGEBACK 2 NEW rows (capability matrix v1.35 → v1.36 EXTENSION 2 NEW rows industry-agnostic 4-industry grants ✅/✅/✅/✅ CR 12-1 L4 precedent 미러, SLO_ENGINEERING Phase 10 wire + CHAOS_ENGINEERING Phase 9 wire + PERFORMANCE_TESTING Phase 8 wire + OBSERVABILITY_TRACES + OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind + `apps/api/core/capability.py` MODIFIED + Capability.FINOPS_SHOWBACK = "finops_showback" 1 NEW enum + Capability.FINOPS_CHARGEBACK = "finops_chargeback" 1 NEW enum + `apps/api/dependencies/capability.py` MODIFIED require_finops_showback + require_finops_chargeback 2 NEW deps + drift detector `tests/integration/test_capability_matrix_v1_36_drift.py` NEW 10 NEW pytest cases) / F27.7 tests + wire scope T1~T8 결정 (~+46 NEW pytest PASS (showback_dsl 6 + chargeback_engine 7 + department_mapping 5 + chargeback_export 6 + alembic 0043 4 + audit action 8 + capability matrix v1.36 10 = ~+46 NEW pytest PASS) + ~+5 NEW vitest PASS (ShowbackPeriodSelector + ShowbackDepartmentBreakdownChart owner-only ack prompt AD-22 verbatim + ShowbackComparisonView RTL render + ko-KR SSOT 2 + finops dashboard parity CR 12-5 D-PARITY-01 결정 wire) + 0 NEW ruff + 0 regressions + T1 showback_dsl + showback_query module + T2 chargeback_engine + chargeback_rule_evaluator + T3 department_mapping + tenant_settings JSONB schema + T4 chargeback CSV/PDF export + T5 alembic 0043 phase_11_finops + T6 audit action EXTENSION 3 NEW + T7 capability v1.36 EXTENSION + frontend finops dashboard + T8 3중 게이트 FINAL CLEAN atomic commit) + AD-38 FinOps Showback / Chargeback 신규 결정 ((a)~(g) 7 sub-decisions 모두 결정 wire 진입). Phase 10 close-out retro `733d428` (cj-style 104번째) + Phase 10 atomic wire T1~T8 `ac5d6c5` (cj-style 103번째) + Phase 10 spec entry `3c80ef0` (cj-style 102번째) + Phase 10 PRD entry `09db4d4` (cj-style 101번째) 결정 verbatim preserve + EXTENSION (Phase 10 territory DONE 정합) + Phase 9 close-out retro `634427d` (cj-style 100번째) + Phase 9 atomic wire T1~T8 `e7670e1` (cj-style 99번째) + Phase 9 spec entry `2a5e4da` (cj-style 98번째) + Phase 9 PRD entry `0b2d2f3` (cj-style 97번째) + Phase 8 close-out retro `ab495a8` (cj-style 96번째) + Phase 8 atomic wire T1~T8 `60d4ea1` (cj-style 95번째) + Phase 8 spec entry `5ae0f4e` (cj-style 94번째) + Phase 8 PRD entry `ced452f` (cj-style 93번째) + build-fixes sprint `eaee198` + Phase 7 close-out retro `326fa9f` (cj-style 92번째) + Phase 7 atomic wire T1~T8 `59b56cd` (cj-style 91번째) + Phase 7 spec entry `749381e` (cj-style 90번째) + Phase 7 PRD entry `916a541` (cj-style 89번째) + Phase 6 close-out retro `f9f006c` (cj-style 88번째) + Phase 6 atomic wire T1~T8 `24e1cd7` (cj-style 87번째) + Phase 6 spec entry `f5c14c9` (cj-style 86번째) + Phase 6 PRD entry `e84a281` (cj-style 85번째) + Epic 17 close-out retro `be8f3bd` (cj-style 84번째) + Epic 17 T2+T3 UI wire `bb92879` (cj-style 83번째) + Epic 17 wire `2ada2ec` (cj-style 82번째) + Epic 17 spec entry `f4b2b58` (cj-style 81번째) + Epic 17 PRD entry `40a9c41` (cj-style 80번째) + Sidebar/MenuProvider hot-fix `01a06e4` (cj-style 79번째) + D-EPIC-16-REVIEW-DEFER-2~6 RESOLVE sprint `512ed6a` (cj-style 78번째) + Phase 5 close-out retro `b843565` (cj-style 76~77번째) + Phase 5 wire `f093f8c` (cj-style 75번째) + Phase 5 spec entry (cj-style 74번째) + Phase 5 PRD entry `93d852b` (cj-style 73번째) + Epic 16 close-out retro (cj-style 72번째) + Epic 16 T4 admin UI follow-up `ff5c3b5` (cj-style 71번째) + Epic 16 review follow-up `963079c` (cj-style 70번째) + Epic 16 atomic wire `e117e09` (cj-style 69번째) + Epic 16 PRD entry `08bfca5` (cj-style 67번째) + 1st release cycle cj-style 62~66번째 모두 wire DONE 진입 + Epic 15 cycle cj-style 58~61번째 모두 wire DONE 진입 + Phase 4 cycle cj-style 53~57번째 모두 wire DONE 진입 + Phase 3 cycle cj-style 49~52번째 모두 wire DONE 진입 결정 wire 모두 보존. CR 11-3 honest-DEFER discipline 105번째 epic 연속 정직 회복 검증 ✅ (D-1-1-DEFER-1/2/3 RESOLVED 보존 60~105번째 + D-LAUNCH-1-DEFER-1 honestly preserved 65~105번째 + D-EPIC-16-REVIEW-DEFER-2~6 ✅ RESOLVED 보존 78~105번째 + D-PHASE-4-DR-DEFER-1/2 ✅ RESOLVED 보존 73~105번째 + D-EPIC-17-WIRE-DEFER-T2-T3-UI ✅ RESOLVED 보존 83~105번째 + D-RETENTION-1 ✅ RESOLVED 보존 85~105번째 + D-OBSERVABILITY-1 ✅ RESOLVED 보존 89~105번째 + D-PERFORMANCE-1 ✅ RESOLVED 보존 93~105번째 + D-CHAOS-1 ✅ RESOLVED 보존 100~105번째 + D-SLO-1 ✅ RESOLVED 보존 101~105번째 + **D-FINOPS-1 honestly DEFER 보존 = Phase 11 PRD entry 진입 시점에 carry-over chain 정직 회복 결정 wire 진입: 1st release close-out retro §6 + Epic 17 close-out retro §11 + Phase 6 close-out retro §13 + Phase 7 close-out retro §10 + Phase 8 close-out retro §10 + Phase 9 close-out retro §10 + Phase 10 close-out retro `733d428` §10 "FinOps Showback / Chargeback 결정 wire 보류, Phase 11+ 진입 시점" verbatim 해소**) + A36 SDR 검증 4-step 자동 적용 (commit prefix lint PASS + sprint-status structure PASS + vitest file count drift 0건 + commit consistency PASS) + CR 12-5 D-GATE-01 + D-PARITY-01 inversion 적용 보존 + A19 + CR 0-2 RLS lesson (Phase 11 finops tables RLS 자동 적용 + tenant-scoped cost center mapping 결정) + CR 1-1 audit-first INSERT (showback_generated + department_mapping_updated + chargeback_exported 3 NEW CR 1-1 verbatim 적용) + CR 9-6 commit message discipline (`git commit -F <file>` 사용, PowerShell here-string 회피, D5 prevention) + NFR4 PII minimization ✅ PRESERVED 모두 적용 보존. 결정 wire 일자: 2026-08-24 (KST).
  - v4.1 (2026-08-24): Phase 10 PRD entry 결정 wire DONE (cj-style 101번째 epic 연속 정직 회복 atomic docs-only wire) — 옵션 (a) Phase 10+ 진입 결정 wire (Phase 9 close-out retro `634427d` 100번째 + Phase 9 atomic wire T1~T8 `e7670e1` 99번째 + Phase 9 spec entry `2a5e4da` 98번째 + Phase 9 PRD entry `0b2d2f3` 97번째 모두 wire DONE 진입 정합 보존 후 다음 territory 진입). 사용자 권장 결정 = 옵션 (a) Phase 10+ 진입 + 옵션 (a) SLO Engineering / Error Budget Management (Recommended) (SLO definition DSL + multi-window burn-rate evaluation + error budget tracker + multi-region SLO aggregation + tenant-scoped SLO override + SLO governance review + auto-rollback SLO breach trigger + dry-run mode + tests + wire scope) = Phase 10 territory 결정 wire 진입 (cj-style 101번째 epic 연속 정직 회복 진입 시점에 결정). rationale 5종: ① Phase 8 wire `60d4ea1` 의 SLO/SLI 정의 4 metrics (`business_cost_engine_duration_seconds p99 < 5s` + `business_signups_total success_rate > 99%` + `business_logins_total p99 < 1s` + `business_audit_log_purge_total success_rate > 99.9%`) 가 SLI 측정 기반 + Phase 9 wire `e7670e1` 의 chaos_experiment 의 steady_state_metric baseline + auto-rollback mechanism 가 SLO breach 자동 검증 메커니즘 제공 — Phase 10 은 SLO 정의 DSL + multi-window burn-rate evaluation + error budget tracker + SLO governance review territory 의 natural next 진입 = SLO 정의(SLI 측정) → SLO 검증(chaos_experiment) → SLO 거버넌스(error budget + burn rate + auto-rollback trigger) 의 정직 회복 chain ② cj-style discipline 회피 위험 방지 (49~100번째 누적 52-entry-point cycle + 100번째 Phase 9 close-out retro + 99번째 atomic wire + 98번째 spec entry + 97번째 PRD entry 패턴 모두 wire DONE 진입 정합 보존 후 즉시 Phase 10 진입 = 1-day atomic sprint discipline) ③ 비즈니스 우선순위 = enterprise SLA 99.95% contractual commitment + Google SRE Workbook "error budget policy" 표준 패턴 + burn-rate alerting territory 의 natural next (Phase 7 OpenTelemetry tracing + Phase 8 SLO/SLI 정의 + Phase 9 chaos_experiment baseline 의 측정 가능 territory 의 natural next = governance layer EXTENSION) ④ Phase 7 wire `59b56cd` 의 7 NEW business metrics + Prometheus AlertManager 의 자연스러운 carry-over chain = Phase 10 SLO engine + Phase 7 business metrics 의 SLI 측정 자동화 + Phase 8 latency regression detector 의 burn rate evaluation EXTENSION + Phase 9 chaos_experiment 의 steady_state_metric 자동 검증 carry-over chain ⑤ 1st release close-out retro + Epic 17 close-out retro + Phase 5 close-out retro + Phase 6 close-out retro + Phase 7 close-out retro + Phase 8 close-out retro + Phase 9 close-out retro territory verbatim 보존 + AD-22 owner-only RBAC + Epic 12 2FA 챌린지 보존 + AD-14 stack pin + NFR4 PII minimization ✅ PRESERVED + AD-37 SLO Engineering / Error Budget Management 신규 결정 ((a)~(g) 7 sub-decisions 모두 결정 wire 진입). master PRD v4.0 → v4.1 atomic edit (docs only). **§F26 SLO Engineering / Error Budget Management territory 신규** (F26.1 SLO definition DSL 결정 wire (`apps/api/modules/slo/slo_definition_dsl.py` NEW ~+150 LOC, syntax: `slo(service, indicator, target, window, comparator)` builder + 6 SLI types (availability + latency_p50 + latency_p95 + latency_p99 + latency_p999 + throughput) + 5 evaluation windows (1h + 6h + 24h + 7d + 30d) + 4 comparators (lt + lte + gt + gte) + 4 industries baseline + per-tenant override EXTENSION + policy evaluation precedence tenant override > industry baseline > system default + `SLO_TARGET_DEFAULTS = {'availability': 0.999, 'latency_p95_seconds': 1.0, 'latency_p99_seconds': 5.0, 'throughput_rps': 100.0}` constants) / F26.2 multi-window burn-rate evaluation (`apps/api/modules/slo/slo_burn_rate.py` NEW ~+180 LOC, Google SRE Workbook multi-window multi-burn-rate criteria verbatim — (1h, 14.4×) + (6h, 6×) + (24h, 3×) + (7d, 1×) 4 window/burn-rate pairs 결정 wire + burn_rate = error_rate / (1 - SLO_target) calculation + burn_rate_alert fast_burn + slow_burn classification + SLOViolationError typed exception CR 12-5 D-14 envelope) / F26.3 error budget tracker (`apps/api/modules/slo/slo_error_budget.py` NEW ~+150 LOC + ErrorBudget TypedDict 6 fields (slo_id + window + total_budget_minutes + consumed_minutes + remaining_minutes + burn_rate_pct) + monthly budget reset KST 1일 00:00 + 30d rolling calculation + per-tenant error_budget_remaining visualization + AD-22 owner-only view + 4 industries baseline industry-agnostic) / F26.4 multi-region SLO aggregation (`apps/api/modules/slo/slo_multi_region.py` NEW ~+120 LOC + MultiRegionSLOAggregator 정합 Phase 5 wire `f093f8c` `phase_5_replication_lag` table + primary Seoul + secondary Tokyo replica 의 SLI 데이터 aggregate + worst-region SLO = primary aggregation strategy + SLOViolationMultiRegionError envelope + cross-region observability carry-over) / F26.5 tenant-scoped SLO override + audit-first INSERT (`apps/api/modules/slo/slo_tenant_scoped.py` NEW ~+100 LOC + RLS 자동 적용 CR 0-2 verbatim + per-tenant SLO target override EXTENSION + tenant_settings.slo_overrides TypedDict + audit-first INSERT 3 NEW actions: `slo_target_updated` + `slo_budget_exhausted` + `slo_violation_detected` CR 1-1 verbatim 적용 + ActionClass.SLO_ENGINEERING 신규 정의 + apps/api/core/audit_action.py MODIFIED AuditAction Literal EXTENSION 3 NEW + _ActionRegistry SLO_ENGINEERING entry 신규 3개 등록 + __all__ EXTENSION + emit_audit_typed BEFORE SLO update CR 1-1 verbatim 적용) / F26.6 SLO governance review + auto-rollback trigger (`apps/api/modules/slo/slo_governance.py` NEW ~+120 LOC + weekly review cron KST 매주 일요일 02:00 UTC 17:00 + monthly review KST 1일 01:00 UTC 16:00 + SLO review document generation `docs/slo-review-{YYYY-MM-DD}.md` + SLOViolation → auto-rollback trigger Phase 9 chaos_experiment 정합 (auto_rollback module integration) + SLO dashboard `apps/web/app/[locale]/(dashboard)/admin/slo/page.tsx` NEW ~+150 LOC + 4 components (SloStatusCard + SloBurnRateChart + SloErrorBudgetGauge + SloViolationTimeline)) / F26.7 Capability gate SLO_ENGINEERING (capability matrix v1.34 → v1.35 EXTENSION SLO_ENGINEERING 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅ CR 12-1 L4 precedent 미러, CHAOS_ENGINEERING Phase 9 wire + PERFORMANCE_TESTING Phase 8 wire + OBSERVABILITY_TRACES + OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind + `apps/api/core/capability.py` MODIFIED + Capability.SLO_ENGINEERING = "slo_engineering" 1 NEW enum + `apps/api/dependencies/capability.py` MODIFIED require_slo_engineering + drift detector `tests/integration/test_capability_matrix_v1_35_drift.py` NEW 8 NEW pytest cases) / F26.8 tests + wire scope T1~T8 결정 (~+30 NEW pytest PASS (slo_definition_dsl 6 + slo_burn_rate 8 + slo_error_budget 4 + slo_multi_region 3 + slo_tenant_scoped 4 + slo_governance 3 + alembic 1 NEW table + capability matrix v1.35 8 + integration e2e 2 = ~+37 NEW pytest PASS) + ~+5 NEW vitest PASS (SLO dashboard RTL 3 + ko-KR SSOT 2 = ~+5 NEW vitest PASS) + 0 NEW ruff + 0 regressions + T1 SLO definition DSL + T2 multi-window burn-rate evaluator + T3 error budget tracker + T4 multi-region SLO aggregator + T5 tenant-scoped SLO override + audit-first INSERT 3 NEW + T6 SLO governance + auto-rollback trigger + T7 capability v1.35 EXTENSION + T8 3중 게이트 FINAL CLEAN atomic commit) + AD-37 SLO Engineering / Error Budget Management 신규 결정 ((a)~(g) 7 sub-decisions 모두 결정 wire 진입). Phase 9 close-out retro `634427d` (cj-style 100번째) + Phase 9 atomic wire T1~T8 `e7670e1` (cj-style 99번째) + Phase 9 spec entry `2a5e4da` (cj-style 98번째) + Phase 9 PRD entry `0b2d2f3` (cj-style 97번째) 결정 verbatim preserve + EXTENSION (Phase 9 territory DONE 정합) + Phase 8 close-out retro `ab495a8` (cj-style 96번째) + Phase 8 atomic wire T1~T8 `60d4ea1` (cj-style 95번째) + Phase 8 spec entry `5ae0f4e` (cj-style 94번째) + Phase 8 PRD entry `ced452f` (cj-style 93번째) 결정 wire 모두 보존. CR 11-3 honest-DEFER discipline 101번째 epic 연속 정직 회복 검증 ✅ (D-1-1-DEFER-1/2/3 RESOLVED 보존 60~101번째 + D-LAUNCH-1-DEFER-1 honestly preserved 65~101번째 + D-EPIC-16-REVIEW-DEFER-2~6 ✅ RESOLVED 보존 78~101번째 + D-PHASE-4-DR-DEFER-1/2 ✅ RESOLVED 보존 73~101번째 + D-EPIC-17-WIRE-DEFER-T2-T3-UI ✅ RESOLVED 보존 83~101번째 + D-RETENTION-1 ✅ RESOLVED 보존 85~101번째 + D-OBSERVABILITY-1 ✅ RESOLVED 보존 89~101번째 + D-PERFORMANCE-1 ✅ RESOLVED 보존 93~101번째 + D-CHAOS-1 ✅ RESOLVED 보존 100~101번째 + **D-SLO-1 honestly DEFER 보존 = Phase 10 PRD entry 진입 시점에 carry-over chain 정직 회복 결정 wire 진입: Phase 9 close-out retro `634427d` §10 + Phase 8 close-out retro §10 + Phase 7 close-out retro §10 "SLO governance / error budget policy 결정 wire 보류, Phase 10+ 진입 시점" verbatim 해소**) + A36 SDR 검증 4-step 자동 적용 (commit prefix lint PASS + sprint-status structure PASS + vitest file count drift 0건 + commit consistency PASS) + CR 12-5 D-GATE-01 + D-PARITY-01 inversion 적용 보존 + A19 + CR 0-2 RLS lesson (Phase 10 SLO tables RLS 자동 적용 + tenant-scoped SLO override 결정) + CR 1-1 audit-first INSERT (slo_target_updated + slo_budget_exhausted + slo_violation_detected 3 NEW CR 1-1 verbatim 적용) + CR 9-6 commit message discipline (`git commit -F <file>` 사용, PowerShell here-string 회피, D5 prevention) + NFR4 PII minimization ✅ PRESERVED 모두 적용 보존. 결정 wire 일자: 2026-08-24 (KST).
  - v4.0 (2026-08-24): Phase 9 PRD entry 결정 wire DONE (cj-style 97번째 epic 연속 정직 회복 atomic docs-only wire) — 옵션 (a) Phase 9+ 진입 결정 wire (Phase 8 close-out retro `ab495a8` 96번째 + Phase 8 atomic wire T1~T8 `60d4ea1` 95번째 + Phase 8 spec entry 94번째 + Phase 8 PRD entry 93번째 + build-fixes sprint `eaee198` 모두 wire DONE 진입 정합 보존 후 다음 territory 진입). 사용자 권장 결정 = 옵션 (a) Phase 9+ 진입 + 옵션 (a) Chaos Engineering / Game Day (Recommended) (chaos experiment definition + fault injection types + game day runbook + blast radius control + continuous chaos + tenant-scoped + multi-region chaos + auto-rollback + safety mechanisms + dry-run mode + observability integration) = Phase 9 territory 결정 wire 진입 (AskUserQuestion 결정 verbatim). rationale 5종: ① Phase 8 wire `60d4ea1` 의 k6 부하 테스트 5 scenarios + SLO/SLI 정의 4 metrics + p99 latency budget 5s + Latency regression detector + Cost-engine benchmark V8 골든 + Performance regression gate CI 의 natural backend carry-over chain 진입 territory 가 §F24.* 의 자연스러운 EXTENSION territory (Phase 9 = §F25 신규 territory) — Phase 8 이 SLO/SLI 정의 + p99 latency budget 으로 정상 상태의 latency 를 측정했다면, Phase 9 은 chaos experiment 로 비정상 상태 (latency spike + error rate spike + resource exhaustion + network partition) 에서의 system resilience 측정 + auto-rollback + blast radius control + game day runbook territory 의 natural next 진입 ② cj-style discipline 회피 위험 방지 (49~96번째 누적 48-entry-point cycle + 96번째 Phase 8 close-out retro + 95번째 atomic wire + 94번째 spec entry + 93번째 PRD entry + build-fixes sprint `eaee198` 패턴 모두 wire DONE 진입 정합 보존 후 즉시 Phase 9 진입 = 1-day atomic sprint discipline) ③ 비즈니스 우선순위 = enterprise SLA 99.95% + RPO 1h/RTO 4h + p99 latency budget 5s + chaos engineering 으로 resilience 측정 territory 의 natural next (Phase 5 multi-region failover + Phase 7 observability + Phase 8 performance 의 측정 가능 territory 의 natural next) ④ Phase 5 wire `f093f8c` 의 multi-region failover (cj-style 75번째) + Phase 7 wire `59b56cd` 의 observability stack 강화 + Phase 8 wire `60d4ea1` 의 performance / load testing 의 자연스러운 carry-over chain = Phase 9 chaos engineering + Phase 5 multi-region failover drill 의 cross-region chaos + Phase 7 OpenTelemetry distributed tracing 의 chaos experiment trace propagation + Phase 8 p99 latency budget 의 chaos baseline EXTENSION ⑤ 1st release close-out retro + Epic 17 close-out retro + Phase 5 close-out retro + Phase 6 close-out retro + Phase 7 close-out retro + Phase 8 close-out retro territory verbatim 보존 + AD-22 owner-only RBAC + Epic 12 2FA 챌린지 보존 + AD-14 stack pin + NFR4 PII minimization ✅ PRESERVED + AD-36 Chaos Engineering / Game Day 신규 결정 ((a)~(g) 7 sub-decisions 모두 결정 wire 진입). master PRD v3.9 → v4.0 atomic edit (docs only). **§F25 Chaos Engineering / Game Day territory 신규** (F25.1 chaos experiment definition 결정 wire (ChaosExperiment TypedDict 12 fields + 5 blast_radius levels + 4 abort conditions + steady_state + hypothesis + rollback_strategy) / F25.2 fault injection types 10 categories 결정 wire (latency + error + resource + network partition + disk I/O + db connection pool + cache failure + DNS failure + process kill + clock skew) / F25.3 game day runbook + blast radius control 결정 wire (quarterly cron KST 1st Sunday 03:00 UTC 18:00 + 8 game day steps + 14 sections runbook) / F25.4 continuous chaos vs scheduled game day 결정 wire (production-safe L1 single_request + 4 experiment candidates + 5% traffic + Prometheus custom metrics) / F25.5 tenant-scoped + multi-region chaos 결정 wire (CR 0-2 RLS lesson + L2 single_tenant + L4 single_region + L5 multi_region + alembic 0041 phase_9_chaos_experiments table) / F25.6 auto-rollback + safety mechanisms 6 layers 결정 wire (4 strategies + abort conditions + blast radius + owner-only RBAC + dry-run + steady state verification + circuit breaker + audit-first INSERT 4 NEW) / F25.7 tests + wire scope T1~T8 결정 (~+30 NEW pytest PASS + ~+5 NEW vitest PASS + 0 NEW ruff + 0 regressions)). capability matrix v1.33 → v1.34 EXTENSION CHAOS_ENGINEERING 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅ CR 12-1 L4 precedent 미러 + AD-36 Chaos Engineering / Game Day 신규 결정 ((a) chaos experiment definition + (b) fault injection types 10 categories + (c) game day runbook + quarterly schedule + (d) continuous chaos production-safe + (e) tenant-scoped + multi-region chaos + (f) auto-rollback + safety mechanisms 6 layers + (g) audit-first INSERT 4 NEW + dry-run mode). 결정 wire 일자: 2026-08-24 (KST).
  - v3.9 (2026-08-24): Phase 8 PRD entry 결정 wire DONE (cj-style 93번째 epic 연속 정직 회복 atomic docs-only wire) — 옵션 (a) Phase 8+ 진입 결정 wire (Phase 7 close-out retro `326fa9f` 92번째 + Phase 7 atomic wire `59b56cd` 91번째 + Phase 7 spec entry `749381e` 90번째 + Phase 7 PRD entry `916a541` 89번째 모두 wire DONE 진입 정합 보존 후 다음 territory 진입). 사용자 권장 결정 = 옵션 (a) Phase 8+ 진입 + 옵션 (a) Performance / Load Testing territory (Recommended) (k6 부하 테스트 + SLO/SLI 정의 + p99 latency budget 5s + Latency regression detector + Performance regression gate + Cost-engine benchmark golden_diff + Load test report + runbook) = Phase 8 territory 결정 wire 진입 (AskUserQuestion 결정 verbatim). rationale 5종: ① Phase 7 wire `59b56cd` 의 7 NEW business metrics (`business_signups_total` + `business_logins_total` + `business_calculations_total` + `business_cost_engine_duration_seconds{engine,tenant_size_bucket}` Histogram + `business_audit_log_purge_total` + `business_active_tenants_gauge` + `business_ai_extraction_duration_seconds`) + Prometheus 4 dashboards + head_based sampler 1.0 dev / 0.1 prod + SlowCalc alert rule 의 natural backend carry-over chain 진입 territory 가 §F23.* 의 자연스러운 EXTENSION territory (Phase 8 = §F24 신규 territory) ② cj-style discipline 회피 위험 방지 (49~92번째 누적 44-entry-point cycle + 92번째 Phase 7 close-out retro + 91번째 atomic wire + 90번째 spec entry + 89번째 PRD entry 패턴 모두 wire DONE 진입 정합 보존 후 즉시 Phase 8 진입 = 1-day atomic sprint discipline) ③ 비즈니스 우선순위 = enterprise SLA 99.95% + RPO 1h/RTO 4h + p99 latency budget + 비용 엔진 회귀 검증 = Phase 7 의 observability stack 의 측정 가능 territory 의 natural next 진입 (observability → load testing → SLO/SLI 정의 territory 결정) ④ Phase 4 wire `71a033a` Sentry `tracesSampleRate=0.1` 의 Phase 7 wire 의 OpenTelemetry distributed tracing 의 자연스러운 carry-over chain = Phase 8 load testing + Phase 7 OpenTelemetry trace_id cross-reference territory + Phase 5 multi-region failover 가시성 EXTENSION ⑤ 1st release close-out retro + Epic 17 close-out retro + Phase 6 close-out retro + Phase 7 close-out retro territory verbatim 보존 + AD-22 owner-only RBAC + Epic 12 2FA 챌린지 보존 + AD-14 stack pin + NFR4 PII minimization ✅ PRESERVED + AD-35 Performance Engineering 신규 결정 ((a)~(g) 7 sub-decisions). master PRD v3.8 → v3.9 atomic edit (docs only). **§F24 Performance / Load Testing territory 신규** (F24.1 k6 부하 테스트 5 scenarios: smoke-test-100 / baseline-1000 / stress-5000 / soak-100-1h / spike-10000 진입 / F24.2 SLO/SLI 정의 4 metrics: `business_cost_engine_duration_seconds p99 < 5s` + `business_signups_total success_rate > 99%` + `business_logins_total p99 < 1s` + `business_audit_log_purge_total success_rate > 99.9%` 결정 / F24.3 p99 latency budget 5s cost-engine territory 결정 / F24.4 Latency regression detector (`apps/api/jobs/latency_regression.py` NEW + drift > 10% p99 vs golden baseline 자동 감지 + Sentry breadcrumb + Slack alert 결정) / F24.5 Performance regression gate (CI 단계에서 k6 부하 테스트 자동 실행 + p99 budget 초과 시 CI 빌드 차단 결정) / F24.6 Cost-engine benchmark V8 골든 (Epic 11 V8 determinism 골든 fixture + cost-engine 12-period benchmark `tests/performance/test_cost_engine_benchmark.py` NEW 결정) / F24.7 tests + wire scope T1~T8 결정 (~+25 NEW pytest PASS + ~+5 NEW vitest PASS + 0 NEW ruff + 0 regressions). capability matrix v1.32 → v1.33 EXTENSION PERFORMANCE_TESTING 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅ CR 12-1 L4 precedent 미러 + AD-35 Performance Engineering 신규 결정 ((a) k6 / Locust 부하 테스트 + (b) SLO/SLI 정의 + (c) Prometheus SlowCalc alert 정합 + (d) Latency regression detector + (e) Performance regression gate + (f) Cost-engine benchmark V8 골든 + (g) Load test report + runbook). 결정 wire 일자: 2026-08-24 (KST).
  - v3.8 (2026-08-22): Phase 7 PRD entry 결정 wire DONE (cj-style 89번째 epic 연속 정직 회복 atomic docs-only wire) — 옵션 (a) Phase 7+ 진입 결정 wire (Phase 6 close-out retro `f9f006c` 88번째 + Phase 6 atomic wire `24e1cd7` 87번째 + Phase 6 spec entry `f5c14c9` 86번째 + Phase 6 PRD entry `e84a281` 85번째 모두 wire DONE 진입 정합 보존 후 다음 territory 진입). 사용자 권장 결정 = 옵션 (a) Phase 7+ 진입 + 옵션 (b) Observability Stack 강화 (OpenTelemetry distributed tracing + Prometheus custom metrics + Grafana dashboards + Alerting) = Phase 7 territory 결정 wire 진입 (AskUserQuestion 결정 verbatim). rationale 5종: ① Phase 5 wire `f093f8c` multi-region observability 의 자연스러운 carry-over chain = OpenTelemetry distributed tracing (cross-region primary + secondary read replica 의 request flow 가시화) + Prometheus custom metrics (multi-region Sentry breadcrumb + Grafana dashboard EXTENSION) territory 가 §F20.5 의 자연스러운 EXTENSION territory (Phase 7 = §F23 신규 territory) ② cj-style discipline 회피 위험 방지 (49~88번째 누적 40-entry-point cycle + 88번째 Phase 6 close-out retro + 87번째 atomic wire + 86번째 spec entry + 85번째 PRD entry 패턴 모두 wire DONE 진입 정합 보존 후 즉시 Phase 7 진입 = 1-day atomic sprint discipline) ③ 비즈니스 우선순위 = NFR4 + GDPR + SOX + observability stack 강화 = enterprise 고객 onboarding 시 alert + SLA 99.95% + multi-region failover 가시성 필수 ⑤ Phase 4 wire `71a033a` Sentry observability 의 자연스러운 carry-over chain = Sentry browser SSR-safe + Sentry FastAPI server integration 의 OpenTelemetry distributed tracing EXTENSION territory. master PRD v3.7 → v3.8 atomic edit (docs only). **§F23 Observability Stack 강화 territory 신규** (F23.1 OpenTelemetry distributed tracing `apps/api/core/tracing.py` NEW, OTLP HTTP exporter + W3C Trace Context propagation traceparent + tracestate + span enrichment tenant_id + user_id + trace_id + auto-instrumentation FastAPI + SQLAlchemy + httpx + asyncpg + head_based sampling 1.0 dev + 0.1 prod 결정) / F23.2 Prometheus custom metrics `apps/api/core/metrics.py` NEW (~+180 LOC, Counter + Histogram + Summary + Gauge + 7 NEW business metrics: signups + logins + calculations + cost_engine_duration + audit_log_purge + active_tenants + ai_extraction + Grafana dashboards 4 NEW: business-signups + cost-engine-performance + auth-flow + audit-log-purge 결정) / F23.3 Alerting system `apps/api/core/alerting.py` NEW (~+120 LOC, Prometheus AlertManager integration + Sentry alert routing EXTENSION + Slack webhook integration #bizup-alerts + PagerDuty integration + 5 NEW alert rules: HighErrorRate + SlowCalc + FailoverStuck + RetentionPurgeFailed + MultiRegionDown + audit-first INSERT `alert_fired` CR 1-1 verbatim + config/alert_rules.yaml 결정) / F23.4 Frontend performance tracing (Browser RUM via OpenTelemetry web SDK + W3C Trace Context propagation server → client + Web Vitals auto-collection LCP + FID + CLS + API fetch instrumentation `apps/web/lib/api-fetch.ts` MODIFIED + Next.js instrumentation hook `apps/web/instrumentation.ts` NEW 결정) / F23.5 audit-first INSERT 2 NEW actions 결정 (ActionClass.OBSERVABILITY 신규 정의 + 2 NEW AuditAction Literal values: `alert_fired` + `trace_sampled` + apps/api/core/audit_action.py MODIFIED AuditAction Literal 2 NEW EXTENSION + _ActionRegistry OBSERVABILITY entry 신규 2개 등록 + __all__ EXTENSION + emit_audit_typed BEFORE alerting trace CR 1-1 verbatim 적용) / F23.6 Capability gates OBSERVABILITY_TRACES + OBSERVABILITY_METRICS (capability matrix v1.31 → v1.32 EXTENSION 2 NEW rows industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러, AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire pattern verbatim bind + drift detector `tests/integration/test_capability_matrix_v1_32_drift.py` NEW 8 NEW pytest cases 결정 wire) / F23.7 tests + wire scope T1~T8 결정 (~+30 NEW pytest PASS (tracing module 6 + metrics module 6 + alerting module 4 + grafana dashboards 2 + audit action 2 NEW + capability matrix v1.32 8 + integration e2e 2 = ~+30 NEW pytest PASS) + ~+10 NEW vitest PASS (browser RUM tracing 3 + API fetch trace propagation 2 + SSOT drift 2 + ko-KR SSOT 검증 3 = ~+10 NEW vitest PASS) + 0 NEW ruff + 0 regressions + T1 OpenTelemetry tracing module + T2 Prometheus metrics module + T3 Alerting module + T4 Frontend tracing module + T5 capability v1.32 EXTENSION 2 NEW rows + T6 audit action EXTENSION 2 NEW + T7 tests + T8 3중 게이트 FINAL CLEAN atomic commit) + §8.1 M0-(p) observability stack 강화 AC 신규 (AC: tenant owner/admin 의 OpenTelemetry distributed tracing + Prometheus custom metrics + Grafana dashboards + Alerting 결정 + audit-first INSERT `alert_fired` + browser RUM + W3C Trace Context propagation server → client + multi-region failover observability 결정) + §15 로드맵 Phase 7 row status 백로그 → in-progress (PRD entry DONE 진입 wire) + §부록 A A223+A224+A225+A226+A227 신규 결정 표 + AD-34 Observability Stack 강화 신규 결정 ((a)~(g) 7 sub-decisions 모두 결정 wire 진입). Phase 6 close-out retro `f9f006c` (cj-style 88번째) + Phase 6 atomic wire `24e1cd7` (cj-style 87번째) + Phase 6 spec entry `f5c14c9` (cj-style 86번째) + Phase 6 PRD entry `e84a281` (cj-style 85번째) 결정 verbatim preserve + EXTENSION (Phase 6 territory DONE 정합) + Epic 17 close-out retro `be8f3bd` (cj-style 84번째) + Epic 17 T2+T3 UI wire `bb92879` (cj-style 83번째) + Epic 17 bmad-dev-story atomic wire T1~T8 backend `2ada2ec` (cj-style 82번째) + Epic 17 spec entry `f4b2b58` (cj-style 81번째) + Epic 17 PRD entry `40a9c41` (cj-style 80번째) 보존 진입. CR 11-3 honest-DEFER discipline 89번째 epic 연속 정직 회복 검증 ✅ (D-1-1-DEFER-1/2/3 RESOLVED 보존 60~89번째 + D-LAUNCH-1-DEFER-1 honestly preserved 65~89번째 + D-EPIC-16-REVIEW-DEFER-2~6 ✅ RESOLVED 보존 78~89번째 + D-PHASE-4-DR-DEFER-1/2 ✅ RESOLVED 보존 73~89번째 + D-EPIC-17-WIRE-DEFER-T2-T3-UI ✅ RESOLVED 보존 83~89번째 + D-RETENTION-1 ✅ RESOLVED 보존 85~89번째 + **D-OBSERVABILITY-1 honestly DEFER 보존 = Phase 7 PRD entry 진입 시점에 carry-over chain 정직 회복 결정 wire 진입: 1st release close-out retro + Epic 17 close-out retro + Phase 6 close-out retro "observability stack 강화 결정 wire 보류, Phase 7+ 진입 시점" verbatim 해소**) + A36 SDR 검증 4-step 자동 적용 (commit prefix lint PASS + sprint-status structure PASS + vitest file count drift 0건 + commit consistency PASS) + CR 12-5 D-GATE-01 + D-PARITY-01 inversion 적용 보존 + A19 + CR 0-2 RLS lesson (Phase 7 system-only tables RLS 자동 적용 + tenant-scoped trace_attributes 결정) + CR 1-1 audit-first INSERT (alert_fired + trace_sampled 2 NEW CR 1-1 verbatim 적용) + CR 9-6 commit message discipline (`git commit -F <file>` 사용, PowerShell here-string 회피, D5 prevention) + NFR4 PII minimization ✅ PRESERVED 모두 적용 보존. 결정 wire 일자: 2026-08-22 (KST).
  - v3.7 (2026-08-22): Phase 6 PRD entry 결정 wire DONE (cj-style 85번째 epic 연속 정직 회복 atomic docs-only wire) — 옵션 (a) Phase 6 진입 결정 wire (Epic 17 close-out retro `be8f3bd` 84번째 + Epic 17 T2+T3 UI wire `bb92879` 83번째 + Epic 17 bmad-dev-story atomic wire T1~T8 backend `2ada2ec` 82번째 + Epic 17 spec entry `f4b2b58` 81번째 + Epic 17 PRD entry `40a9c41` 80번째 모두 wire DONE 진입 정합 보존 후 다음 territory 진입). 사용자 권장 결정 = 옵션 (a) Phase 6 진입 + 옵션 (b) Audit Log Retention Policy = Phase 6 territory 결정 wire 진입 (AskUserQuestion 결정 verbatim). rationale 5종: ① Epic 17 audit log viewer (cj-style 80~84번째) 의 자연스러운 carry-over chain = audit log 가시성(viewer) + retention(purge) + archive 세 가지 territory 가 §F21.* 의 자연스러운 EXTENSION territory (Phase 6 = §F22 신규 territory) ② cj-style discipline 회피 위험 방지 (49~84번째 누적 36-entry-point cycle + 79~83번째 hot-fix + RESOLVE sprint + Epic 17 5-entry-point + Epic 16 6-entry-point + Phase 5 4-entry-point 패턴 모두 wire DONE 진입 정합 보존 후 즉시 Phase 6 진입 = 1-day atomic sprint discipline) ③ 비즈니스 우선순위 = NFR4 PII minimization + GDPR Article 17 right to erasure + audit log retention policy = enterprise 고객 onboarding 시 compliance 필수 ④ Phase 5 wire `f093f8c` 의 cross-region audit log visibility (cj-style 75번째) 의 자연스러운 carry-over chain = cross-region primary + secondary read replica 의 audit_log_archive 테이블 EXTENSION territory ⑤ 1st release launch wire DONE 진입 후 enterprise SLA = audit log retention 7년 (SOX) + PII 30일 (GDPR Article 17) + immutable archive territory 의 natural next 진입. master PRD v3.6 → v3.7 atomic edit (docs only). **§F22 Audit Log Retention Policy territory 신규** (F22.1 retention policy DSL `apps/api/modules/audit/audit_retention_policy.py` NEW, syntax: `retain(action_class, days, archive)` 정의 + default 7-year SOX hold + PII 30-day GDPR Article 17 mask + 365-day cold archive 결정) / F22.2 automatic purge job `apps/api/jobs/audit_purge.py` NEW (~+150 LOC, cron KST 매일 03:00 UTC 18:00 + retain rule evaluation + dry-run mode + audit-first INSERT `audit_log_purged` CR 1-1 verbatim + ActionClass.AUDIT 신규 action 결정) / F22.3 archive storage `audit_log_archive` table 신규 (alembic 0040 + immutable append-only + SHA-256 hash chain linkage 보존 + 7-year archive for SOX + cross-region primary archive + secondary archive mirror 결정 + Phase 5 wire `f093f8c` EXTENSION) / F22.4 GDPR/NFR4 PII compliance (CR 1-1 audit-first INSERT + `audit_log_pii_masked` action 신규 + PII columns email + ip_address 30일 후 자동 mask `[REDACTED:hash]` 결정 + GDPR Article 17 right to erasure EXTENSION 결정) / F22.5 audit-first INSERT on deletion (CR 1-1 verbatim, `audit_log_purged` + `audit_log_archived` action 신규, action_class='AUDIT' + action='audit_log_purged' + actor_id='system' + tenant_id + row_count + retention_rule + trace_id 결정) / F22.6 Capability gate AUDIT_LOG_RETENTION (capability matrix v1.30 → v1.31 EXTENSION 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러, AUDIT_LOG_VIEW Epic 17 wire pattern verbatim bind) / F22.7 tests + wire scope T1~T8 결정 (~+20 NEW pytest + ~+5 NEW vitest PASS + 0 NEW ruff + 0 regressions + T1 audit_retention_policy module + T2 audit_purge job + T3 audit_log_archive table alembic 0040 + T4 PII redaction logic + T5 capability v1.31 + T6 dry-run mode UI + T7 tests + T8 3중 게이트 FINAL CLEAN atomic commit) + §8.1 M0-(o) audit log retention AC 신규 (AC: tenant owner/admin 의 audit log retention 정책 관리 + dry-run mode + 7-year SOX hold + PII 30-day GDPR mask + 365-day cold archive + audit-first INSERT on deletion + immutable archive storage + cross-region primary + secondary archive mirror) + §15 로드맵 Phase 6 row status 백로그 → in-progress (PRD entry DONE 진입 wire) + §부록 A A193+A194+A195+A196+A197 신규 결정 표 + AD-33 Audit Log Retention Policy 신규 결정 ((a)~(g) 7 sub-decisions 모두 결정 wire 진입). Epic 17 close-out retro `be8f3bd` (cj-style 84번째) 보존 진입 (Epic 17 territory DONE 정합) + Epic 17 T2+T3 UI wire `bb92879` (cj-style 83번째) 보존 + Epic 17 bmad-dev-story atomic wire T1~T8 backend `2ada2ec` (cj-style 82번째) 보존 + Epic 17 spec entry `f4b2b58` (cj-style 81번째) 보존 + Epic 17 PRD entry `40a9c41` (cj-style 80번째) 보존. CR 11-3 honest-DEFER discipline 85번째 epic 연속 정직 회복 검증 ✅ (D-1-1-DEFER-1/2/3 RESOLVED 보존 60~85번째 + D-LAUNCH-1-DEFER-1 honestly preserved 65~85번째 + D-EPIC-16-REVIEW-DEFER-2~6 ✅ RESOLVED 보존 78~85번째 + D-PHASE-4-DR-DEFER-1/2 ✅ RESOLVED 보존 73~85번째 + D-EPIC-17-WIRE-DEFER-T2-T3-UI ✅ RESOLVED 보존 83~85번째 + D-RETENTION-1 honestly DEFER 보존 = Phase 6 PRD entry 진입 시점에 carry-over chain 정직 회복 결정 wire 진입: 1st release close-out retro §6 + Epic 17 close-out retro §11 "audit log retention policy 결정 wire 보류, Phase 6+ 진입 시점" verbatim 해소) + A36 SDR 검증 4-step 자동 적용 (commit prefix lint PASS + sprint-status structure PASS + vitest file count drift 0건 + commit consistency PASS) + CR 12-5 D-GATE-01 + D-PARITY-01 inversion 적용 보존 + A19 + CR 0-2 RLS lesson (audit_log_archive RLS 정합) + CR 1-1 audit-first INSERT (audit_log_purged + audit_log_archived + audit_log_pii_masked 3 NEW CR 1-1 verbatim 적용) + CR 9-6 commit message discipline (`git commit -F <file>` 사용, PowerShell here-string 회피, D5 prevention) + NFR4 PII minimization ✅ PRESERVED 모두 적용 보존. 결정 wire 일자: 2026-08-22 (KST).
  - v3.6 (2026-08-22): Epic 17 PRD entry 결정 wire DONE (cj-style 80번째 epic 연속 정직 회복 atomic docs-only wire) — 옵션 (a) Epic 17 진입 결정 wire (Sidebar/MenuProvider hot-fix `01a06e4` 직후 79→80번째 cycle 진입, D-EPIC-16-REVIEW-DEFER-2~6 RESOLVE sprint `512ed6a` 78번째 wire DONE 진입 후 next 옵션 5종 중 사용자 권장 결정 = 옵션 (a) Epic 17 진입). rationale 4종: ① Epic 1 ~ Epic 16 + Phase 3 + Phase 4 + Phase 5 모두 wire DONE (49~79번째 cumulative cycle) → 모든 territory 의 audit-first INSERT (CR 1-1) 가 audit_log table 누적 → audit log viewer territory 자연스러운 next 진입 ② cj-style discipline 회피 위험 방지 (49~79번째 누적 31-entry-point cycle + 78~79번째 hot-fix + RESOLVE sprint 직후 즉시 Epic 17 진입 = 1-day atomic sprint discipline) ③ 비즈니스 우선순위 = enterprise 고객 onboarding 시 audit log viewer 필수 (PIPA + GDPR + SOX compliance = audit log 가시성 + export 기능 요구) ④ Phase 5 multi-region wire 의 cross-region audit log visibility 자연스러운 carry-over (audit_log table 은 cross-region primary 에 write → multi-region read replica 통한 cross-region audit visibility 제공). master PRD v3.5 → v3.6 atomic edit (docs only). **§F21 Audit Log Viewer & Activity Stream territory 신규** (F21.1 audit log query API (FastAPI `apps/api/modules/audit/audit_log_query.py` NEW, filters: tenant_id + actor_id + action_class + action + period + payload_search + pagination + sort by created_at DESC, owner/admin role required, RLS 자동 적용) / F21.2 audit log viewer UI (Next.js `apps/web/app/[locale]/(dashboard)/audit-log/page.tsx` NEW ~200 LOC, filter panel + table + pagination + CSV export button + ko-KR.json `audit_log.*` namespace EXTENSION 14 keys + (dashboard) 보호) / F21.3 activity stream UI (Next.js `apps/web/app/[locale]/(dashboard)/activity/page.tsx` NEW ~150 LOC, last 30 days tenant-wide activity timeline + grouped by date + actor avatar + action chip + (dashboard) 보호) / F21.4 cross-region audit log visibility (Phase 5 carry-over, secondary region 의 audit_log replica 통한 cross-region audit query, multi-region read replica 정합) / F21.5 CSV export (compliance 용, `apps/api/modules/audit/audit_log_export.py` NEW, Excel-compatible UTF-8 BOM + streaming response + audit-first INSERT `audit_log_exported` action) / F21.6 Capability gate AUDIT_LOG_VIEW (capability matrix v1.29 → v1.30 EXTENSION 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅) / F21.7 tests + wire scope T1~T8 결정) + §8.1 M0-(n) audit log viewer AC 신규 (AC: tenant owner/admin 가 audit log 조회 + filter + export + cross-region visibility + activity stream 7 day/30 day/90 day window) + §15 로드맵 Epic 17 row status 백로그 → in-progress (PRD entry DONE 진입 wire) + §부록 A A153+A154+A155+A156+A157 신규 결정 표 + AD-32 Audit Log Viewer & Activity Stream 신규 결정 + capability matrix v1.29 → v1.30 EXTENSION AUDIT_LOG_VIEW 1 NEW row (industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러, MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire pattern + TENANT_IDP_MANAGEMENT Epic 16 wire pattern + LAUNCH_* 1st release wire pattern) + Epic 16 close-out retro `f1ead9a` 보존 진입 (Epic 16 territory DONE 정합) + Phase 5 close-out retro `b843565` 보존 진입 (Phase 5 territory DONE 정합) + D-EPIC-16-REVIEW-DEFER-2~6 RESOLVE sprint `512ed6a` 78번째 wire DONE 진입 보존. CR 11-3 honest-DEFER discipline 80번째 epic 연속 정직 회복 검증 ✅ (D-1-1-DEFER-1/2/3 RESOLVED 보존 60~80번째 + D-LAUNCH-1-DEFER-1 honestly preserved 65~80번째 + D-EPIC-16-REVIEW-DEFER-2~6 ✅ RESOLVED 보존 78~80번째 + D-PHASE-4-DR-DEFER-1/2 ✅ RESOLVED 보존 73~80번째) + A36 SDR 검증 4-step 자동 적용 (commit prefix lint PASS + sprint-status structure PASS + vitest file count drift 0건 + commit consistency PASS) + CR 12-5 D-GATE-01 + D-PARITY-01 inversion 적용 보존 + A19 + CR 0-2 RLS lesson (audit_log RLS 정합 보존) + CR 1-1 audit-first INSERT (audit_log_exported 1 NEW CR 1-1 verbatim 적용) + CR 9-6 commit message discipline (`git commit -F <file>` 사용, PowerShell here-string 회피, D5 prevention) 모두 적용 보존. 결정 wire 일자: 2026-08-22 (KST).
  - v3.5 (2026-08-22): Phase 5 PRD entry 결정 wire DONE (cj-style Phase 5 1번째 진입점 = cj-style 73번째 epic 연속 정직 회복 atomic docs-only wire) — 옵션 (a) Phase 5 진입 결정 wire (Epic 16 close-out retro §14 5 options 중 사용자 권장 결정 = 옵션 (a) Phase 5 진입, rationale 4종: ① docs/database-backup.md §7 disaster recovery 의 "multi-region backup 결정 wire 보류, Phase 5+ 진입 시점" verbatim 해소 = Phase 4 wire 의 honestly-deferred territory 자연스러운 carry-over chain ② cj-style discipline 회피 위험 방지 (49~72번째 누적 cycle 후 73번째 진입 = 1-day atomic sprint discipline 즉시 진입) ③ 비즈니스 우선순위 = 1차 출시 후 enterprise SLA = RPO 1시간/RTO 4시간 → multi-region failover 으로 99.95% SLA 달성 territory ④ Phase 4 wire (`71a033a`) 의 단일-region (Supabase Seoul primary) 의 multi-region EXTENSION = phase_4_backup_strategy table + Phase 4 PITR 결정 wire 의 자연스러운 carry-over). master PRD v3.4 → v3.5 atomic edit (docs only). **§F20 Multi-Region Backup & Disaster Recovery territory 신규** (F20.1 Cross-region read replica + WAL archiving (`apps/api/alembic/versions/0039_phase_5_multi_region_backup.py` NEW ~+120 LOC, phase_5_replication_lag table + replica region enums + replication_status enum + audit log replica_status_changed INSERT) / F20.2 Cross-region failover automation (`apps/api/jobs/failover_orchestrator.py` NEW ~+200 LOC, primary → secondary health probe + automatic promotion + DNS update via Supabase API + 30s RTO target) / F20.3 Disaster recovery drill + automated quarterly test (cron KST 1st Sunday 03:00 UTC 18:00 + actual failover drill test in staging + RPO/RTO measurement) / F20.4 Cross-region backup strategy (Supabase PITR primary + cross-region PITR secondary + Supabase Storage 결정 wire 보류 vs AWS S3 결정 wire 보류 + 30일 hot + 90일 cold + 365일 archive regional) / F20.5 Multi-region health observability (`apps/api/core/health.py` EXTENSION multi-region endpoint + Sentry breadcrumb failover + Grafana multi-region dashboard EXTENSION) / F20.6 Capability matrix v1.28 → v1.29 EXTENSION MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER 2 NEW rows (industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러, DEPLOYMENT_* Phase 4 wire pattern) / F20.7 Tests + wire scope T1~T8 결정) + §8.1 M0-(m) multi-region backup AC 신규 (AC: cross-region read replica wire DONE + WAL archiving wire DONE + automatic failover drill PASS + RPO ≤ 1h / RTO ≤ 4h SLA + quarterly drill + multi-region health observability) + §15 로드맵 Phase 5 row status 백로그 → in-progress (PRD entry DONE 진입 wire) + §부록 A A124+A125+A126+A127+A128 신규 결정 표 (A124 옵션 (a) Phase 5 진입 결정 / A125 master PRD v3.4 → v3.5 atomic edit / A126 AD-31 Multi-Region Backup & Disaster Recovery 신규 / A127 capability matrix v1.28 → v1.29 EXTENSION MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER 2 NEW rows / A128 Phase 5 wire scope T1~T8 결정) + AD-31 Multi-Region Backup & Disaster Recovery 신규 결정 ((a) Cross-region read replica + WAL archiving 결정 wire / (b) Cross-region failover automation 결정 wire / (c) DR drill + quarterly test 결정 wire / (d) Cross-region backup strategy 결정 wire / (e) Multi-region health observability 결정 wire / (f) Capability gate MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER 결정 wire) + capability matrix v1.28 → v1.29 EXTENSION 2 NEW rows (industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러, DEPLOYMENT_PROD + DEPLOYMENT_STAGING + DEPLOYMENT_DATABASE_BACKUP + DEPLOYMENT_HEALTH_CHECK Phase 4 wire pattern + TENANT_IDP_MANAGEMENT Epic 16 wire pattern) + Epic 16 close-out retro 보존 진입 (Epic 16 territory DONE 정합, cj-style 67~72번째 epic 연속 정직 회복). CR 11-3 honest-DEFER discipline 73번째 epic 연속 정직 회복 검증 ✅ (D-1-1-DEFER-1/2/3 RESOLVED 보존 60~73번째 + D-LAUNCH-1-DEFER-1 honestly preserved 65~73번째 + D-EPIC-16-REVIEW-DEFER-2~6 honestly DEFERRED 5 OPEN 보존 70~73번째) + A36 SDR 검증 4-step 자동 적용 (commit prefix lint PASS + sprint-status structure PASS + vitest file count drift 0건 + commit consistency PASS) + CR 12-5 D-GATE-01 + D-PARITY-01 inversion 적용 보존 + A19 + CR 0-2 RLS lesson (Phase 4 backup_strategy RLS 정합) + CR 1-1 audit-first INSERT (replica_status_changed + failover_initiated audit log INSERT 결정) + CR 9-6 commit message discipline (`git commit -F <file>` 사용, PowerShell here-string 회피, D5 prevention) 모두 적용 보존. 결정 wire 일자: 2026-08-22 (KST).
  - v3.4 (2026-08-22): Epic 16 PRD entry 결정 wire DONE (cj-style 67번째 epic 연속 정직 회복 atomic docs-only wire) — 옵션 (a) Epic 16 진입 결정 wire (1st release close-out retro §12 4 options 중 사용자 권장 결정, rationale 4종: ① Epic 15 SSO enterprise SAML forward-reference 'TODO Epic 16' 해결 (docs/sso-enterprise.md §4.1 step 3 'Configure tenant_idps (TODO Epic 16)' verbatim) ② Epic 15 territory carry-over chain (58~61→67번째) = tenant IdP admin management 가 자연스러운 next territory ③ cj-style discipline 회피 위험 방지 (62~66번째 누적 cycle 더 미루면 cycle 끊김 위험) ④ 비즈니스 우선순위 = 1차 출시 후 enterprise SSO onboarding 필수). master PRD v3.3 → v3.4 atomic edit (docs only). **§F19 Tenant IdP admin management territory 신규** (F19.1 tenant_idps table schema (alembic 0038 + RLS policy `tenant_id = current_setting('app.tenant_id')`) / F19.2 IdP metadata XML validation service (SAML 2.0 metadata XML 파싱 + EntityDescriptor 검증 + x509 cert PEM 검증 + IdP SSO URL 형식 검증) / F19.3 Tenant IdP CRUD API endpoints (FastAPI routes 5종: list/create/update/delete/test, owner/admin role required) / F19.4 Tenant IdP admin UI (Next.js admin dashboard at `/[locale]/(dashboard)/settings/sso` + IdP metadata paste/upload + validation feedback + tenant slug 자동 표시) / F19.5 Per-tenant IdP routing EXTENSION (Epic 15 SAML response routing 정합: `GET /api/v1/auth/sso/login?tenant_slug=acme` → `tenant_idps` row lookup → ACS URL 일치 검증) / F19.6 Capability gate TENANT_IDP_MANAGEMENT (capability matrix v1.28 EXTENSION industry-agnostic 4-industry grants ✅/✅/✅/✅) / F19.7 Tests + wire scope T1~T8 결정) + §8.1 M0-(l) tenant IdP admin AC 신규 (AC: tenant owner/admin 가 자신의 tenant IdP config CRUD 가능 + IdP metadata XML validation + per-tenant SSO routing + audit-first INSERT 4 NEW + multi-tenant isolation RLS) + §15 로드맵 Epic 16 row status 백로그 → in-progress (PRD entry DONE 진입 wire) + §부록 A A92+A93+A94+A95+A96 신규 결정 표 (A92 옵션 (a) Epic 16 진입 결정 / A93 master PRD v3.3 → v3.4 atomic edit / A94 AD-30 Tenant IdP admin architecture 신규 / A95 capability matrix v1.27 → v1.28 EXTENSION TENANT_IDP_MANAGEMENT / A96 Epic 16 wire scope T1~T8 결정) + AD-30 Tenant IdP admin management 신규 결정 ((a) tenant_idps table schema (alembic 0038 + RLS) / (b) IdP metadata validation (signature + structure) / (c) CRUD API (owner/admin) / (d) admin UI / (e) per-tenant routing EXTENSION / (f) audit-first INSERT 4 NEW + multi-tenant isolation) + capability matrix v1.27 → v1.28 EXTENSION TENANT_IDP_MANAGEMENT 1 NEW row (industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러, SSO_ENTERPRISE Epic 15 wire pattern) + 1st release close-out retro 보존 진입 (1st release territory DONE 정합, cj-style 62~66번째 epic 연속 정직 회복). CR 11-3 honest-DEFER discipline 67번째 epic 연속 정직 회복 검증 ✅ (D-1-1-DEFER-1/2/3 RESOLVED 보존 + D-LAUNCH-1-DEFER-1 honestly preserved 65~66~67번째) + A36 SDR 검증 4-step 자동 적용 (commit prefix lint + sprint-status structure + pytest file count drift + commit consistency 모두 PASS) + CR 12-5 D-GATE-01 + D-PARITY-01 inversion 적용 보존 + A19 + CR 0-2 RLS lesson + CR 1-1 audit-first INSERT + CR 9-6 commit message discipline (`git commit -F <file>` 사용, PowerShell here-string 회피, D5 prevention) 모두 적용 보존. 결정 wire 일자: 2026-08-22 (KST).
  - v3.3 (2026-08-22): 1st release launch PRD entry 결정 wire DONE (cj-style 62번째 epic 연속 정직 회복 atomic docs-only wire) — 옵션 (d) 1차 출시 진입 결정 wire (Epic 15 close-out retro §12 4 options 중 사용자 권장 결정, rationale 4종: ① 모든 인프라 wire DONE (Auth Foundation Epic 1 + Phase 3 + 2FA Epic 12 + LISTEN/NOTIFY Epic 13/14 + Deployment Phase 4 + 인증 방법 4종 Magic link + OAuth 3종 + SSO SAML Epic 15) ② D-1-1-DEFER-1/2/3 ✅ RESOLVED 60번째 epic 연속 정직 회복 ③ cj-style discipline 회피 위험 방지 (49~61번째 누적 cycle) ④ 비즈니스 우선순위 (infrastructure 완성 → 실제 출시 가치 회수). master PRD v3.2 → v3.3 atomic edit (docs only). **§F18 1st release launch territory 신규** (F18.1 Marketing landing page (`/landing` route + `LandingHero` + `LandingFeatures` + `LandingPricing` + `LandingCTA` ko-KR inline copy, vercel.json public route EXTENSION) / F18.2 Terms of Service + Privacy Policy (`docs/terms-of-service.md` + `docs/privacy-policy.md` 한국 개인정보보호법 + GDPR 정합, versioned + changelog) / F18.3 Onboarding user guide (`docs/onboarding-guide.md` 8 sections + in-app tooltip + first-run wizard EXTENSION Epic 1 carry-over 결정 보존) / F18.4 Customer support channels (`docs/support.md` + `support@bizup.kr` email + in-app help widget + FAQ `docs/faq.md`) / F18.5 Production launch verification (smoke test RE-RUN 정직 결정 wire + `docs/database-backup.md` 0036 PITR drill + Sentry alert wiring production environment + RPO/RTO SLA verification 4h/24h) / F18.6 Public launch communications (`docs/launch-announcement.md` + press kit + social media assets)) + §8.1 M0-(k) 1st release launch AC 신규 (launch checklist 6 conditions: ① landing page wire DONE ② ToS/Privacy wire DONE ③ onboarding guide wire DONE ④ support channels wire DONE ⑤ smoke test + backup drill PASS ⑥ launch comms published) + §15 로드맵 1st release row status 백로그 → in-progress (PRD entry DONE 진입 wire) + §부록 A A83+A84+A85+A86+A87 신규 결정 표 (A83 옵션 (d) 1st release launch 진입 결정 / A84 master PRD v3.2 → v3.3 atomic edit / A85 AD-29 1st release launch 신규 / A86 capability matrix v1.26 → v1.27 EXTENSION 4 NEW rows / A87 1st release wire scope T1~T8 결정) + AD-29 1st release launch 신규 결정 ((a) Marketing landing page 결정 wire / (b) ToS + Privacy Policy 결정 wire / (c) Onboarding user guide 결정 wire / (d) Customer support channels 결정 wire / (e) Production launch verification 결정 wire / (f) Public launch communications 결정 wire, public 라우트 (auth)/landing + (auth)/tos + (auth)/privacy ko-KR SSOT EXTENSION) + capability matrix v1.26 → v1.27 EXTENSION LAUNCH_LANDING + LAUNCH_TOS + LAUNCH_SUPPORT + LAUNCH_MONITORING 4 NEW rows (industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러, Phase 3 + Phase 4 + Epic 13/14 + Epic 15 wire pattern) + Epic 15 close-out retro 보존 진입 (Epic 15 territory DONE 정합). CR 11-3 honest-DEFER discipline 62번째 epic 연속 정직 회복 검증 ✅ (D-1-1-DEFER-1/2/3 RESOLVED 보존) + A36 SDR 검증 4-step 자동 적용 (commit prefix lint + sprint-status structure + pytest file count drift + commit consistency 모두 PASS) + CR 12-5 D-GATE-01 + D-PARITY-01 inversion 적용 보존 + A19 + CR 0-2 RLS lesson + CR 1-1 audit-first INSERT + CR 9-6 commit message discipline (`git commit -F <file>` 사용, PowerShell here-string 회피, D5 prevention) 모두 적용 보존. 결정 wire 일자: 2026-08-22 (KST).
  - v3.2 (2026-08-22): Epic 15 PRD entry 결정 wire DONE (cj-style Epic 15 1번째 진입점 = cj-style 58번째 epic 연속 정직 회복) — Epic 15 = Magic link + Social OAuth + SSO enterprise SAML 통합 territory 진입 결정 (옵션 (a) Epic 15 진입 wire, A70+A71+A72 결정 wire 진입). master PRD v3.1 → v3.2 atomic edit (docs only). D-1-1-DEFER-1/2/3 ✅ honestly RESOLVE 진입 wire 결정 (A70 Magic link + A71 Social OAuth + A72 SSO enterprise SAML 3/3 ALL DONE 진입). §F17 Magic link + Social OAuth + SSO enterprise SAML territory 신규 (F17.1 Magic link via Supabase `signInWithOtp` + email 존재 여부 노출 방지 + audit-first INSERT / F17.2 Social OAuth Google/Naver/Kakao via Supabase `signInWithOAuth` + provider whitelist + OAuth callback + audit-first INSERT / F17.3 SSO enterprise SAML via `python3-saml` + SAML response validation + JIT user provisioning + multi-tenant isolation (CR 0-2 RLS) + Just-In-Time mapping + audit-first INSERT / F17.4 ko-KR SSOT EXTENSION (auth.magic_link.* + auth.social.* + auth.sso.* namespace) / F17.5 capability matrix v1.25 → v1.26 EXTENSION 5 NEW rows (MAGIC_LINK + SOCIAL_OAUTH_GOOGLE + SOCIAL_OAUTH_NAVER + SOCIAL_OAUTH_KAKAO + SSO_ENTERPRISE) / F17.6 tests + wire scope T1~T8 결정) + §8.1 M0-(h) Magic link + M0-(i) Social OAuth + M0-(j) SSO enterprise SAML 3 NEW 인수 불릿 + §15 로드맵 Epic 15 row 신규 (in-progress 진입) + §부록 A A70+A71+A72 ✅ done + A75 preserved + A79+A80+A81+A82 신규 결정 표 (A79 Epic 15 PRD entry 결정 + A80 AD-28 Magic link + Social OAuth + SSO 신규 + A81 capability matrix v1.25 → v1.26 EXTENSION 5 NEW rows + A82 Epic 15 wire scope T1~T8 결정) + AD-28 Magic link + Social OAuth + SSO enterprise SAML 신규 결정 (Supabase `signInWithOtp` + `signInWithOAuth` + `python3-saml` AD-14 stack pin + SAML response validation + JIT user provisioning + multi-tenant isolation (CR 0-2 RLS) + audit-first INSERT) + capability matrix v1.25 → v1.26 EXTENSION 5 NEW rows (industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러, Phase 3 + Phase 4 + Epic 13/14 wire pattern) + Phase 4 close-out retro 보존 진입. CR 11-3 honest-DEFER discipline 58번째 epic 연속 정직 회복 검증 ✅ (D-1-1-DEFER-1/2/3 ✅ RESOLVE 진입 wire) + A36 SDR 검증 4-step 자동 적용 (commit prefix lint + sprint-status structure + vitest file count drift + commit consistency 모두 PASS) + CR 12-5 D-GATE-01 + D-PARITY-01 inversion 적용 보존 + A19 + CR 0-2 RLS lesson + CR 1-1 audit-first INSERT + CR 9-6 commit message discipline (`git commit -F <file>` 사용, PowerShell here-string 회피, D5 prevention) 모두 적용 보존. 결정 wire 일자: 2026-08-22 (KST).
  - v3.1 (2026-08-22): Phase 4 PRD entry 결정 wire DONE (cj-style Phase 4 1번째 진입점 = cj-style 53번째 epic 연속 정직 회복) — Phase 4 = Deployment config + Dockerfile territory 진입 결정 (옵션 (a) Phase 4 진입 wire, A73 결정). master PRD v3.0 → v3.1 atomic edit (docs only). §F16 Deployment territory 신규 (F16.1 Dockerfile 다중 stage 분리 + AD-14 digest-pinned / F16.2 Vercel frontend config + RAILWAY backend config / F16.3 Supabase production PostgreSQL / F16.4 Production env config + secrets 관리 / F16.5 Health check + observability + monitoring + database backup / F16.6 tests + wire scope T1~T8 결정 + §8.1 M0-(g) production deployment + §15 로드맵 Phase 4 row 신규 + §부록 A A73 + A74 + A76 + A77 + A78 신규 결정 표 + AD-27 Deployment 신규 결정 (Vercel frontend + Railway backend + Supabase PostgreSQL + Sentry observability 결정) + capability matrix v1.24 → v1.25 EXTENSION DEPLOYMENT_PROD + DEPLOYMENT_STAGING + DEPLOYMENT_DATABASE_BACKUP + DEPLOYMENT_HEALTH_CHECK 4 NEW rows (industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러, Phase 3 + Epic 13/14 wire pattern) + D-1-1-DEFER-1/2/3 honestly RESOLVE 표기 (A70+A71+A72 결정 wire 진입 시점에 동시) + Phase 3 close-out retro 보존 진입. CR 11-3 honest-DEFER discipline + A36 SDR 검증 4-step 자동 적용 (commit prefix lint + sprint-status structure + vitest file count drift + commit consistency 모두 PASS) + CR 12-5 D-GATE-01 + D-PARITY-01 inversion 적용 보존 + A19 + CR 0-2 RLS lesson + CR 1-1 audit-first INSERT + CR 9-6 commit message discipline (`git commit -F <file>` 사용, PowerShell here-string 회피, D5 prevention) 모두 적용 보존. 결정 wire 일자: 2026-08-22 (KST).
  - v3.0 (2026-08-20): Phase 3 PRD entry 결정 wire DONE (cj-style Phase 3 1번째 진입점 = cj-style 49번째 epic 연속 정직 회복) — Phase 3 = 로그인/회원가입 UI + auth middleware (Epic 1 완성 territory 진입 결정). master PRD v2.5 → v3.0 atomic edit (docs only). §F15 Auth Foundation 신규 (F15.1 login UI + Supabase SSR auth client + sb-access-token cookie session / F15.2 signup UI + tenant creation flow + onboarding/industry handoff / F15.3 auth middleware EXTENSION next-intl middleware + Supabase session check + (dashboard) 보호 + (auth) 공개 route group / F15.4 logout flow + ko-KR SSOT logout error envelope + audit-first INSERT / F15.5 forgot-password UI + Supabase resetPasswordForEmail + token rotation + 2FA 게이트 보존 / F15.6 tests + wire scope T1~T8 결정 + §8.1 M0-(d) login + M0-(e) signup + M0-(f) auth middleware 3 NEW 인수 불릿 + §15 로드맵 Phase 3 row + §부록 A A65+A66+A67+A68+A69 신규 결정 표 + capability matrix v1.23 → v1.24 EXTENSION LOGIN + SIGNUP + AUTH_MIDDLEWARE + FORGOT_PASSWORD + LOGOUT 5 NEW rows (industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러, AI_INSIGHT + LISTEN_NOTIFY + LISTEN_NOTIFY_TENANT_FANOUT + LISTEN_NOTIFY_MULTIPROCESS wire pattern) + AD-26 Auth Foundation 신규 (Supabase SSR + sb-access-token cookie session + next-intl middleware EXTENSION + auth route group (auth) + dashboard route group (dashboard) 보호 + Epic 12 2FA 게이트 보존) + D-1-1-DEFER-* honestly DEFER 보존 (Epic 1 carry-over DEFER 1~N 정직 회복). CR 11-3 honest-DEFER discipline + A36 SDR 검증 4-step 자동 적용 (commit prefix lint + sprint-status structure + vitest file count drift + commit consistency 모두 PASS). 3중 게이트 impact NONE (docs only 변경, no code/test/sprint-status delta 외에 PRD edit 신규). 결정 wire 일자: 2026-08-20.
  - v2.5 (2026-08-20): A57 결정 wire DONE = Epic 14 = LISTEN/NOTIFY Consume 2nd Batch PRD entry (cj-style Epic 14 1번째 진입점 = cj-style 45번째 epic 연속 정직 회복). §F14 신규 (F14.1 cross-tenant invalidation fan-out 토폴로지 + F14.2 multi-process coordination + F14.3 V8 determinism + cross-language drift detector EXTENSION + F14.4 tests + wire scope T1~T9 결정) + §15 로드맵 Epic 14 row status 백로그 → in-progress (PRD entry DONE 진입 wire) + §부록 A A57+A58+A59 신규 결정 (A57 = Epic 14 PRD entry / A58 = AD-25 EXTENSION 5+ channels cross-tenant fan-out channel 추가 / A59 = capability matrix v1.22 → v1.23 LISTEN_NOTIFY_TENANT_FANOUT + LISTEN_NOTIFY_MULTIPROCESS 2 NEW rows, industry-agnostic 4-industry grants) + §8.1 M10-(d)·§F10.1-(d) cross-tenant fan-out EXTENSION 결정 wire 진입. Story 14-1 wire 진입 대기 (cj-style Epic 14 2번째 진입점 = cj-style 46번째 epic 연속 정직 회복 진입 대기, T1~T9 atomic single sprint). A45+A46 결정 wire 진입 보존 (옵션 (a) Epic 14 follow-up sprint 진입 결정, bundled into ONE Epic 14 carry-over sprint = cj-style Epic 14 3번째 진입점 진입 시점). D-13-1-DEFER-3 ✅ RESOLVED 진입 wire (A53 Epic 14 진입 결정). D-13-1-DEFER-2 preserved (LISTEN/NOTIFY 실측 evidence 정합 sweep = 14-1 wire 진입 시점에 동시 sweep 결정 = A55 Epic 14 진입 시점에 동시). 3중 게이트 impact NONE (docs only 변경, no code/test/sprint-status delta 외에 PRD edit 신규). CR 11-3 honest-DEFER discipline + A36 SDR 검증 4-step 자동 적용 (commit prefix lint PASS + sprint-status structure PASS + vitest file count drift 0건 + commit consistency PASS). 결정 wire 일자: 2026-08-20.
  - v2.4 (2026-08-20): A53 결정 wire DONE = 옵션 (a) Epic 14 진입 결정 (cj-style Epic 13 5번째 진입점 = cj-style Epic 13 carry-over 17번째 docs only atomic wire = cj-style 44번째 epic 연속 정직 회복). §15 로드맵 Epic 14 row 신규 (LISTEN/NOTIFY Consume 2nd Batch 백로그 진입) + §부록 A A53 (decision pending → ✅ done 진입 wire, 사용자 옵션 (a) Epic 14 진입 결정 verbatim bind) + A45+A46 결정 wire 진입 (옵션 (a) Epic 14 follow-up sprint 진입 결정, bundled into ONE Epic 14 carry-over sprint = cj-style Epic 14 3번째 진입점 진입 시점) + A57+A58+A59 결정 예정 (Epic 14 PRD entry 진입 시점에 결정). D-13-1-DEFER-3 ✅ RESOLVED 진입 결정 wire. Epic 14 = cross-tenant invalidation fan-out + multi-process coordination = LISTEN/NOTIFY consume 2nd batch territory 진입 결정. 3중 게이트 impact NONE (docs only 변경, no code/test/sprint-status delta 외에 PRD edit 신규). CR 11-3 honest-DEFER discipline + A36 SDR 검증 4-step 자동 적용 (commit prefix lint PASS + sprint-status structure PASS + vitest file count drift 0건 + commit consistency PASS).
  - v2.3 (2026-08-20): A54 master PRD v2.2 → v2.3 atomic edit DONE — D-13-1-DEFER-1 ✅ RESOLVE. §F13 verbatim 13-1 wire 정합 확장 (F13.1 5-key alphabetical JSON payload + F13.2 4-channel handler verbatim + F13.3 V8 determinism + cross-language drift detector EXTENSION + F13.4 T1~T8 atomic wire DONE + capability LISTEN_NOTIFY v1.22 4-industry grants industry-agnostic) + §15 로드맵 Epic 13 row status in-progress → done (cj-style 1~4번째 진입점 모두 wire DONE = PRD entry + 13-1 atomic + post-wire handoff + Epic 13 close-out retro) + §부록 A A52 (예정 → done 진입 wire) + A53+A54+A55+A56 신규 결정 표 + AD-25 EXTENSION 표기. cj-style Epic 13 carry-over 17번째 docs only atomic wire.
  - v2.2 (2026-08-20): Epic 13 PRD entry A51 결정 wire — §F13 (LISTEN/NOTIFY Consume Trigger EXTENSION) 신규 + §8.1 M10-(d)·§F10.1-(d) 4-channel EXTENSION 진입 결정 verbatim bind + §15 로드맵 Epic 13 row + §부록 A A39 (status in-progress → done 진입 wire) + A51 Epic 13 PRD entry 결정. cj-style Epic 13 1번째 진입점 docs only atomic wire.
  - v2.1 (2026-08-20): Epic 10 close-out retro A37 결정 wire — §F10.1 (Three-Insight Cache Policy) + §F10.2 (AI Reference vs Auto Analysis Badge Separation) + §12 AI 3종 update + §13.1 AI 배지 ko-KR cross-ref + §14 NFR (NFR18 ko-KR + AI 추출/insight/cache/reject counter 4 rows) + §14.B NON-GOAL #5·6 정합 + §부록 A A37~A42 + AD-7/AD-17/AD-25 architectural decisions 표 추가. cj-style carry-over 15번째 docs only atomic wire.
  - v2.0 (2026-07-25): final
---

# 비즈업(Biz-Up) 통합 제품요구사항정의서(PRD) v3.0
## 원가경영관리 웹 SaaS — 전통 개별원가 엔진 + 활동기준원가(ABC) 엔진 통합판

| 항목 | 내용 |
|------|------|
| 문서 버전 | **v2.0 (통합·확정)** — 비즈업 PRD v1.0 + ABC 모듈 PRD v1.0을 **완전 대체**(결정 Q-H) |
| 작성일 | 2026-07-12 |
| 근거 자산 | 원본 엑셀 5개 파일 시트 원문 전수 학습: costmgr(18시트)·djob(8시트)·inv2(17시트+월반복 22시트)·report(구조 분석으로 갈음, Q-G)·ABCost(13시트) |
| 이론 기준 | 2026년 7월 기준 최신 원가관리 지식 — 흡수원가계산 정통 원칙 + TDABC + AI 지원 (웹 검증 완료) |
| 제품 형태 | 웹 SaaS · 월 구독(1만원) · 멀티테넌트 · 완전반응형 |
| 후속 문서 | **통합 ERD v2.0** (본 문서 확정 후 기존 ERD 2권+테이블명세서를 1권으로 통합) |
| 설계 최상위 원칙 | **회계 공리(원가계산 원칙)는 철저히 준수하되, 그 테두리 안에서 입력 편의성을 극대화한다** (제3장 ↔ 제5장) |

---

# 1. 제품 정의와 배경

## 1.1 한 문장 정의

> **"월 1회, 6가지 데이터만 입력하면 원가·재고·손익·분석이 전부 자동으로 나오는, 제조업과 서비스업을 한 지붕에 담은 원가경영관리 웹 SaaS"**

## 1.2 원본 자산의 정체 (5개 파일 학습 종합)

동일 저작자(원가바이블)의 검증된 두 원가 엔진이다.

```
■ 전통 개별원가 엔진 (costmgr + djob + inv2 + report — 4파일 1시스템)
  재료비(BOM×월총평균단가) + 노무비(표준공수×회사부담임률) + 제조경비(배부기준)
  월 6종 입력(주문·생산·판매·구매·경비·인원인건비) → 수불부·검증(check) → 보고서 26종
  대상: 제조업, 제조+유통 겸영 (예시 기업: 창원산업/프랜차이즈)

■ 활동기준원가(ABC) 엔진 (ABCost — 1파일)
  간접비 → 원가풀(설비·매출·인원 3기준 배부) → 활동(≤10) → 동인 → 원가대상
  "비제조업을 위한" 명시 — 서비스·유통업 특화 (예시 기업: 풍성물류산업/여행상품)
```

두 엔진은 같은 철학(월 배치·그린셀 입력·최소 입력 최대 산출·예산/실적 이중 운영·자동 서술형 분석의견)을 공유하며 원가 방법론만 다르다. 비즈업은 이 둘을 **하나의 웹 플랫폼 안에서 사업부문(segment) 구조 위에 공존**시키고, 계정과목·매출·인건비·마감을 **단일 원장으로 공유**한다(이중 입력 0).

> **Greenfield 선언.** 비즈업은 greenfield 웹 SaaS다. 원본 5파일(costmgr·djob·inv2·report·ABCost)은 **회계 로직의 참조용 출처**일 뿐이며, 런타임 의존성(엑셀 COM·VBA·DLL)은 없다. 모든 산식(§6.1, §7.1, §7.2)은 Python으로 1원 단위 재현되며, §11 V8 회귀 테스트 스위트가 원본과 동일 결과를 검증한다. 따라서 "원본과 같은 결과"는 시스템이 보장하고, 원본 자체를 시스템 안에서 띄우지 않는다.

## 1.3 해결하는 불편함

**엑셀 구조의 물리적 한계**
1. 4개 파일 동시 오픈 의존 — 실제 수식 손상 4,468건·유령 링크 1,027건 발생을 교정 작업으로 실증
2. 슬롯 고정 — 제품 100(월 30)·자재 100(월 40)·계정 19·활동 10·지역 20, 초과 시 유료 커스터마이징
3. 1인 1파일 — 동시접속·권한·변경이력 부재, 담당자 교체 시 붕괴
4. VBA 저장/복구 13공간 — 덮어쓰기 위험, "복구 칸 공란" 함정
5. 검증(check)의 수동 의존 — 훌륭한 Reconciliation이 있으나 사람이 열어봐야 발견, 음수재고(예: 달걀 −7,500) 방치

**두 엔진 분리의 불편 (통합의 직접 근거)**
6. 제조+서비스 겸영 기업은 두 파일을 따로 운영해야 하고 공통비를 나눌 방법이 없음
7. 매출·경비·인건비의 이중 입력
8. ABC 파일이 BOM1 시트로 costmgr을 6,174개 수식으로 미러링 — 원본 스스로 통합 필요성을 증명

**지식의 속인성**
9. 저작자 1인의 노하우(임률 체인·배부 판단·조정 요령)가 수식과 메모 속에만 존재

## 1.4 계승하는 철학과 정합성 헌법

원본 manual의 사상을 계승한다.

> "원가 경영관리는 간결하고 간단할수록 성공하고 복잡하면 대부분 실패한다."
> "최소한의 데이터 업로드로 최대한의 원가경영관리에 접근한다."
> "Garbage In, Garbage Out; Gold In, Gold Out — 기본정보의 품질이 원가계산의 품질을 좌우한다."

여기에 비즈업의 정합성 헌법을 더한다.

> **"시스템은 틀린 계산을 할 수 없고, 사용자의 틀린 입력은 반드시 알려준다."**

---

# 2. 대상 사용자·포지셔닝·비즈니스 모델

| 항목 | 내용 |
|------|------|
| 대상 | 전국 불특정 중소기업(제조·유통·서비스·겸영), 원가 전담자 없는 기업 |
| 페르소나 | ① 제조업 경영자/경리 ② 서비스업(물류·여행·용역) 대표 ③ 제조+유통 겸영(프랜차이즈·식품) ④ 경영컨설턴트(대리 운영) |
| 가격 | 월 구독 1만원 단일 요금제(전 기능 제공), 결제 Stripe |
| 경쟁 우위 | ① 회계프로그램이 못 주는 제품별 원가·손익·BEP ② ABC/TDABC를 갖춘 국내 중소기업용 SaaS의 희소성 ③ AI 온보딩·인사이트 ④ 검증된 실무 로직의 1원 단위 웹 이식 |
| 운영 | 1인 개발·운영(Claude Code 활용), 월 인프라 예산 10만원, 셀프서비스 원칙 |
| 지원 수단 | 운영자 콘솔, 대리접속(고객 동의+읽기전용), 공지 시스템 |
| 목표 | G1: 12모듈 완성 후 출시+파일럿 1~2곳 무료 / G2: "새벽에 혼자 고칠 수 있는 시스템" |

---

# 2.A 사용자 여정 (User Journeys)

비즈업의 4가지 핵심 여정은 §2 페르소나를 protagonist로 삼아 흐름을 잡는다. 각 여정은 트리거 → 핵심 단계 → 종착상태 → 예외 경로의 구조로, UX 와이어프레임과 데이터 플로우(아키텍처)의 직접 입력이다. 페르소나 컨텍스트는 각 여정 protagonist에 인라인으로 실어 나른다(별도 페르소나 섹션 없음).

### UJ-1. 월 사이클 — 가장 자주 반복되는 운영 흐름
- **Protagonist**: 박영수, 48세, 식품 제조+유통 겸영 업체 대표(§2 페르소나 ③). 경리 1명 보조, 새벽 4시 출근해 장부 검토.
- **트리거**: 전월 마감 완료 후 다음 달 1일 자동 알림 (대시보드 체크리스트 활성화).
- **핵심 단계**:
  1. M2 입력 — 주문·생산·판매·구매·경비·인원 6종 데이터를 전월 복사(E9) 후 증감만 수정, 일용직은 별도 그리드.
  2. M0/M1 누락 점검 — 신규 품목·거래처 등장 시 기준정보(M1) 추가, 업종 변경은 A7 전진법 가드.
  3. M3 [계산] 클릭 → §6.1 산식 체인 가동, 1원 단위 검증 [A6].
  4. M6 자동 검증 (§11 V1~V8) — V3 음수재고 / V4 4요소 분해 / V8 회귀 테스트 자동 발동.
  5. M11 마감 — 부문분할→제조→ABC→공동 순서 강제, 스냅샷·역분개 잠금.
  6. M5 보고서 — 종합 손익·제품별 손익·BEP·재고 수불 14종 열람, PDF/A4 인쇄.
  7. (선택) M10 AI 인사이트 큐레이션 — 계산 직후 질문 3개 자동 생성·캐시(§12).
- **종착상태**: 다음 달 영업 준비 완료 + 전월 손익 확정 + 감사추적 로그 봉인.
- **예외 경로**:
  - V3 음수재고 → 즉시 경고 + 품목 화면 점프, 마감 차단.
  - V4 차이 발생 → 4요소 분해 표시, 사용자 확인 후 조정 라인 확정.
  - 생산요구시간 > 작업가능시간 → V5 경고, 마감 차단.
  - 업종 변경 시도 → A7 차단, "전진법: 다음 회계연도부터 적용" 안내.

### UJ-2. 예산 시나리오 — 가상 기간으로 미리 시험
- **Protagonist**: 이미숙, 42세, 여행상품 ABC 운영 대표(§2 페르소나 ②). 다음 분기 매출 변동에 대비.
- **트리거**: 회계연도 시작 1개월 전 + 매월 25일 (차월 추정 활성화).
- **핵심 단계**:
  1. M0에서 "예산 시나리오" 신규 개설 (1차 = 1개, 2차 = 복수 예정).
  2. M2 입력과 동일한 체계로 계획치 입력 (M2 UI 재사용).
  3. M3 사전 표준원가계산 — 실적과 동일 산식 체인 [A6], 다른 점은 가상 기간 태그.
  4. M5 §9 보고서 — "예산 대비 실적 손익"(§10, 원본 pl5) 자동 대조.
  5. (2차) A×B×C×D 편성 엔진(보존 산식 §부록 B) 활성화 시 다축 시뮬레이션.
- **종착상태**: 예산 확정 + 표준원가 사전 산출 + 차이 분석 가능.
- **예외 경로**:
  - 예산 시나리오 미존재 → M8 메뉴 비활성.
  - 실적 데이터 변경 → 예산 대비 자동 재대조, 알림.

### UJ-3. 겸영 기업의 부문 카브아웃
- **Protagonist**: 김도현, 53세, 제조+서비스 겸영(프랜차이즈) 재무 담당(§2 페르소나 ③).
- **트리거**: 가입 시 업종 ③·④ 선택 OR 사업부문 추가 발생.
- **핵심 단계**:
  1. §4.1 업종 ③ 선택 — 두 엔진 병행 + 재무제표 업로드 필수.
  2. §4.2 부문 최소 정보 3종 입력 — 명칭·월매출·전용계정 태깅.
  3. §7.3 카브아웃 — 법인세법 시행규칙 제76조 2기준(매출액 비례/개별비용 비례) 택1 [A10], 선택 근거 설정에 기록.
  4. M11 마감 순서 — 부문분할 → 제조 → ABC → 공동 (부분 마감 없음).
  5. §9 보고서 — "부문귀속명세서"(§7.3, §9 #21)로 분할 근거 공시.
- **종착상태**: 제조·서비스 부문별 손익 + 전사 통합 손익 동시 산출.
- **예외 경로**:
  - 재무제표 미업로드 → 카브아웃 검증 불가, 마감 차단.
  - 2기준 미선택 → A10 위반 경고.
  - 제조 부문에 ABC 적용 시도 → 3차 로드맵 안내(§15).

### UJ-4. 관리자 온보딩 — AI 문서추출로 10분 컷
- **Protagonist**: 신규 가입자(§2 페르소나 ①·② 공통). 사업자등록증·재무제표·급여대장·거래명세서 사진 보유.
- **트리거**: 회원가입 직후 M0 진입.
- **핵심 단계**:
  1. M0 업종 4지선다 (§4.1) — 선택에 따라 후속 메뉴 자동 토글(E4).
  2. E5 AI 문서추출 — Claude Vision으로 초안 자동 생성("AI는 초안, 확정은 사람").
  3. 신뢰도 배지 표시, 저신뢰 항목은 사용자 직접 확정.
  4. M1 기준정보 일괄 등록 — 품목·BOM·계정·부서·거래처.
  5. M2 첫 입력 — 6종 데이터 중 일부는 문서추출 결과에서 자동 이관.
  6. (선택) 대리접속 동의(§13.3) — 컨설턴트가 읽기 전용으로 보조.
- **종착상태**: 첫 월 데이터를 입력 가능한 상태 + 모든 추출 항목 사용자 확정.
- **예외 경로**:
  - 추출 신뢰도 전 항목 < 50% → 사용자 수동 입력 폴백 안내.
  - 업종 ④ 선택 → 격리 버킷 추가 안내.
  - 2FA·결제 미완료 → M0 차단.

---

# 2.B 성공 지표 (Success Metrics)

비즈업의 통합 thesis("ABC + 전통 + AI 통합이 한국 SMB 원가경영관리의 표준이 된다")를 검증하기 위한 지표. 모든 지표는 익명 집계 후 운영자 콘솔에 공개(테넌트별 비공개 옵션). 카운터 메트릭은 같은 보드로 묶어 트레이드오프를 가시화한다.

### 핵심 메트릭 (thesis 검증)
- **SM-1. 월 마감 완료율 (활성 테넌트)** — 활성 테넌트 중 마감 완료(역분개 잠금봉인) 비율. 1차 출시 6개월 내 **70% 목표**.
- **SM-2. 겸영 테넌트의 2-engine 동시 마감 비율** — 업종 ③·④ 선택 테넌트가 제조+ABC 양쪽을 같은 회계기간에 마감 완료한 비율. 1차 출시 12개월 내 **50% 목표**.
- **SM-3. AI 인사이트 채택률 (계산 결과 미변경 보장)** — §12 인사이트 큐레이션이 생성한 질문 3개 중 사용자가 "참고함" 또는 "수정함"으로 응답한 비율. 1차 출시 6개월 내 **40% 목표**. SM-3a "계산 결과 변경 시도 = 0건" 별도 추적으로 §12 원칙 유지 검증.
- **SM-4. 미사용능력 보고서 열람률** — TDABC·전통 조업도 차이 보고서(§6.1 (2), §7.2, §9 #11, #18)를 월 1회 이상 열람한 활성 테넌트 비율. 미사용능력 정착 신호.

### 카운터 메트릭 (트레이드오프 가시화)
- **CM-1. 마감 소요시간 vs SM-1** — SM-1이 올라가도 마감 소요시간이 늘어나면 안 됨. 목표: 월 마감 중앙값 **≤ 4시간** (1인 운영자 기준).
- **CM-2. 입력 소요시간(월) vs SM-2** — 2-engine 동시 마감이 늘어도 입력 시간이 2배가 되면 안 됨. 목표: 월 입력 중앙값 **≤ 8시간**.
- **CM-3. 음수재고 경고 후 재작업 빈도** — V3 경고 후 마감까지 사용자 재작업 횟수. 낮을수록 입력 품질↑(A11 충족).
- **CM-4. 대리접속 발동 빈도** — §13.3 대리접속이 자가 해결을 대체하지 않는지 검증 (100% 의존 시 컨설팅 의존 신호).

### 측정 방법
- 모든 메트릭은 익명화 + 집계 후 운영자 콘솔에 표시. 테넌트별 원시 데이터는 비공개.
- 측정 인프라 비용 0 (PostgreSQL 집계 뷰 + 운영자 콘솔 대시보드).
- 지표 정의 변경은 §부록 A 결정 이력(Q-A~Q-J) 방식 추후 추가.

> **가격 모델 정당화 (1만원 단일 요금제).** persona ①·②는 "원가 전담자 없는 기업"이고 G2는 "1인 운영자가 새벽에 혼자 고칠 수 있는 시스템"을 요구한다. 따라서 다단 요금제(베이직/프로/엔터프라이즈)의 게이트키핑·등급별 기능 제한은 1인 운영자 부담을 가중시킨다. 비용 구조: 월 인프라(PostgreSQL+FastAPI+AI API 캐시) ≈ 10만원 / 1테넌트, Stripe 결제 수수료 ≈ 3.6%, 파일럿 기간(1차 출시 후 6개월) 무료 운영으로 영업 부담 0. 1단 정액 1만원은 손익분기 약 10~15 테넌트에서 달성. 2차에서 다단화 가능성 검토(OQ-2).

---

# 3. 회계 공리 헌장 (Accounting Axioms Charter)

**본 장은 비즈업의 최상위 규범이다. 이하 모든 기능·화면·산식은 본 장과 충돌할 수 없으며, 충돌 시 본 장이 우선한다. 입력 편의성(제5장)은 본 장의 테두리 안에서만 극대화된다.**

### A1. 발생주의와 기간귀속
모든 원가·수익은 현금 수수와 무관하게 발생한 월(회계기간)에 귀속한다. 월 마감으로 기간을 확정하며, 마감된 기간의 데이터는 수정할 수 없다(A8의 역분개 정정만 허용). 회계연도 시작월은 테넌트별 가변. `[ASSUMPTION: 원본 costmgr manual은 회계연도 1월 시작을 기본으로 기술. 다국어 SaaS 환경에서 테넌트별 가변이라는 추론은 별도 검증 필요. 추론 근거: 동일 저작자 djob 시트가 "회계연도 시작월" 필드를 보유함. 추론일: 2026-07-12.]`

### A2. 제조원가와 판매관리비의 엄격한 구분
- 제조원가 = 직접재료비 + 직접노무비 + 제조간접비(제조경비) 3요소로만 구성한다.
- 관리 인건비·판매 일반관리비는 **어떠한 경우에도 재고자산 가액에 산입하지 않는다.**
- 원본 pl 시트의 "관리인건비 = 직접인건비의 29.3% 상당 배부", "판관비 = 매출액 기준 배부"는 **관리회계 뷰(제품별 완전원가 손익)로만 제공**하고, 재고 평가·제조원가명세서와는 계층을 분리한다.
- 따라서 제품 원가는 항상 2계층으로 구분 표시한다: `제조원가(manufacturing_cost)` / `완전원가(full_cost)`.

### A3. 재고자산 평가 — 월총평균법 일관 적용
- 원부재료 단가 = (기초재고금액 + 당월매입금액) ÷ (기초재고수량 + 당월매입수량)
- 제품 재고 = 전월·당월 제조원가를 총평균하여 평가 (원본 p1~p12 방식 계승)
- K-IFRS(제1002호 재고자산)와 일반기업회계기준이 인정하는 방법이며, 회계기간 중 변경을 금지한다(A7).

### A4. 생산기준 배부와 매출원가 대응 (결정 Q-F)
- 직접노무비·제조경비는 **당월 생산량을 모수로 배부**한다. 매출기준 배부는 채택하지 않는다.
  - 근거: 원본 pl3에서 매출기준 배부 시 직접인건비가 매출 대비 92%로 튀는 왜곡을 실증(생산 14,900 vs 판매 10,450인데 노무비 전액을 당월 매출에 배부).
- 매출원가 = 판매분에 대응하는 제조원가. 미판매 생산분은 재고자산으로 이월한다.
- 생산·판매 수량 차이와 총평균단가 차이에서 발생하는 재고 증감은 **'제품 재고 조정' 라인으로 자동 산출**하여 손익계산서에 투명하게 표시한다(제11장 V4 — 원본 check 시트의 수동 분해를 자동화).

### A5. 원가 배부의 인과관계(Causality) 원칙
- 배부기준은 원가 발생의 원인을 가장 잘 대표하는 동인이어야 한다.
- 제조경비 배부기준(테넌트 설정, 3종 택1 — 결정 Q-A): ① **직접노무원가 기준** ② **직접노무시간 기준** ③ **기계시간 기준**
  - 기계시간 기준 선택 시에만 월별 제품별 기계시간 입력 필드가 노출된다(미선택 시 숨김 — E4).
- 직접노무비는 작업공수 단일 체계로 배부한다: 단위공수 × 생산수량 × 회사부담임률 (결정 Q-E, 할당인원 방식 미채택).
- 판매 일반관리비의 제품별 배부(관리 뷰)는 매출액 기준으로 하되 A2의 계층 분리를 지킨다.
- 유통품목 3규칙: 노무비 배부 제외 / 공수 미산정 / 간접비는 간접 귀속.

### A6. 완전배부와 대차평형 (Zero-Leak 원칙)
- 모든 배부는 **배부액 합계 = 원비용 금액**을 1원 단위로 만족해야 한다(원본 check 포인트·ABC 7 cost objects의 합계 검증 계승).
- 배부 트리거와 엔진 테스트에서 강제하며, 위반 시 마감을 차단한다.
- 원 단위 끝수 차이는 최종 행에서 조정하는 라운딩 규칙을 명문화한다.

### A7. 일관성(Consistency) — 기중 변경 금지와 전진법
- 배부기준·재고평가방법·고정/변동 구분·업종 구분·부문 구조는 **회계연도 중 변경을 금지**한다.
  (원본 manual: "한번 선택하면 회계기간 동안에는 변경하지 말 것" → 비즈업은 시스템으로 강제)
- 부득이한 변경은 **전진법(prospective)**: 다음 회계연도 개시월부터 적용, 과거 마감분 불변.
- BOM 개정도 전진법: `effective_from` + 월 스냅샷. 과거 월 조회·재계산 시 당시 BOM을 사용.

### A8. 검증가능성(Auditability)
- 수불 원장은 append-only. 정정은 삭제가 아닌 **역분개(reversal) + 재기록**.
- 월 마감 시 계산 결과 전체를 스냅샷으로 고정하고, 입력→산출 추적 경로를 보존한다.
- 누가·언제·무엇을 변경했는지 감사추적 로그를 남긴다.

### A9. 유휴(미사용)능력 원가의 별도 관리 — 전통·ABC 공통
- 유휴 조업능력에서 발생한 원가는 **별도 항목으로 구분 관리하며, 정상 원가와 합산해 원가대상에 배부하지 않는다.** (원본 1-Main Menu의 이론 권고 + Kaplan TDABC 정론)
- 전통 엔진의 조업도 차이시간(총작업가능시간 − 생산요구시간, 예: 3,322−3,269=53h)도 동일 사상으로 **금액화하여 '미사용능력 보고서'에 별도 표시**한다. 임률의 분모는 요구시간(회사부담임률)으로 하여 유휴원가가 제품에 숨어들지 않게 한다.

### A10. 전사(공장)수준 공통원가의 자의적 배부 금지
- 개별 제품과 인과관계가 없는 전사 수준 원가는 제품에 자의적으로 배부하지 않는다(원본 ABC 이론장 권고 계승).
- 사업부문 공통비의 분할은 **법인세법 시행규칙 제76조가 정한 2기준**(매출액 비례 / 개별비용 비례) 중 택1로만 수행하고 선택 근거를 설정에 기록한다.

### A11. 오류의 가시화 (숨기지 않는 시스템)
- 음수재고·합계 불일치·조업도 초과 등 이상 신호는 감지 즉시 표시한다.
- 마감 전에는 '경고 후 진행 허용', **마감 시점에는 임계 위반을 차단**한다.
- 검증 결과는 원본 check 시트처럼 OK/불일치로 명시하되, 차이 원인을 4요소(수량차·배부차·단가차·재고조정)로 분해해 설명한다.

---

# 4. 업종 구조와 사업부문 아키텍처

## 4.1 가입 시 업종 4지선다

| 선택 | 노출 엔진 | 비고 |
|------|-----------|------|
| ① 제조업 | 전통 엔진 | ABC 메뉴 숨김(순수제조 고객) |
| ② 서비스업 | ABC 엔진 | BOM·수불부 등 제조 메뉴 숨김 |
| ③ 제조+서비스 | 두 엔진 병행 | 부문 카브아웃 필수, 재무제표 업로드 필수 |
| ④ 제조+서비스+기타 | 두 엔진 + 격리 버킷 | '기타' 부문은 격리 버킷으로 원가계산 제외 |

업종 변경은 전진법(A7). 부문 최소 정보 3종: 명칭·월매출·전용계정 태깅.

## 4.2 부문(segment)과 엔진의 고정 매핑 (결정 Q-I)

- 제조 부문 → 전통 개별원가 엔진 / 서비스 부문 → ABC 엔진(classic + TDABC)
- 공통비 → A10의 세법 2기준으로 부문 분할 후 각 엔진에 투입
- 마감 순서 일원화: **부문분할 → 제조 원가계산 → ABC 계산 → 공동 마감** (부분 마감 없음)
- 제조 부문에 대한 ABC 적용(병행 뷰)은 3차 로드맵 — 스키마는 확장 차단 없이 설계

---

# 5. 입력 편의 극대화 원칙 (회계 공리의 테두리 안에서)

**모든 편의 장치는 제3장 공리를 침해하지 않는 범위에서만 작동한다. 편의는 '입력의 노력'을 줄이는 것이지 '회계의 정확성'을 흥정하는 것이 아니다.**

| # | 장치 | 내용 | 지키는 공리 |
|---|------|------|------------|
| E1 | 월합계 기본 + 일자별 선택 (Q-C) | 원가계산은 월 단위이므로 월합계 입력이 기본. 일일관리가 필요한 고객만 일자별 그리드 모드 활성화(record_date 건별 확장) | A1 |
| E2 | 생산유형 표준공수 상속 | 생산유형 15종(공정 ≤10, 비중 합 100% 강제)에 표준공수(인원×시간÷수량)를 정의하면 제품이 상속. 제품별 재정의가 우선하는 하이브리드 | A5 |
| E3 | 자동 파생 | 평균단가 = 금액÷수량 자동 산출(원본 pu·sa 방식). 수량+금액만 입력 | — |
| E4 | 선택 기능 숨김 | 주문관리·판매지역·거래처·기계시간은 미사용 시 화면·보고서에서 숨김(원본 "주문관리 불필요 시 미입력" 계승) | — |
| E5 | AI 문서추출 온보딩 | 사업자등록증·재무제표·급여대장·거래명세서 사진/PDF → 초안 자동 생성. **"AI는 초안, 확정은 사람"** — 확정 전 반드시 사용자 검토 | A8 |
| E6 | 엑셀 업로드 | 더존·이카운트 내보내기 + 원본 djob 양식 호환 가져오기 | — |
| E7 | 추정 모드 | 동인 실적 등 미확보 데이터는 추정치 입력 허용하되 `is_estimated` 배지로 전 화면·보고서에 표시 | A11 |
| E8 | 체크리스트 + 순차입력 | 월 사이클을 대시보드 체크리스트로 안내, 선행 단계 미완료 시 후행 차단 | A1 |
| E9 | 전월 복사 | 경비·인원 등 반복 데이터는 전월 값 복사 후 수정 | — |
| E10 | 실시간 검증 | 입력 즉시 경고(허용) → 마감 시 차단의 2단계 | A11 |
| E11 | 계산 버튼 방식 | 자동 재계산 대신 명시적 [계산] 버튼 — 사용자가 계산 시점을 통제, 결과 재현성 보장 | A8 |

---

# 6. 원가 방법론 명세 — 전통 개별원가 엔진 (제조 부문)

## 6.1 산식 체인 (엑셀 1원 단위 재현 기준)

**(1) 직접재료비**
```
품목별 월총평균단가 = (기초재고금액 + 당월매입금액) ÷ (기초재고수량 + 당월매입수량)   [A3]
제품 단위당 재료비 = Σ(BOM 소요량 × 해당 자재 월총평균단가)
당월 재료비 = Σ(제품별 단위당 재료비 × 생산수량)                                  [A4 생산기준]
```
- 구매는 월 다회차 입력 허용(원본 pu 10회차) → 월 가중평균으로 집계
- 유통품(상품)은 BOM 자기참조 1 방식을 웹에서 불허하고 `merchandise` 유형으로 대체(마이그레이션 시 자동 변환)

**(2) 직접노무비 — 조업도 체인**
```
실근무일수 = 카렌다일수 − 법정공휴일 − 회사임의공휴일 − 유지보수기간
총작업가능시간 = 실근무일수 × (일근무시간 + 일평균초과근무) × 직접작업인원(일용직 FTE 환산 포함)
생산요구시간 = Σ(제품 단위공수 × 생산수량)      ※ 단위공수: 제품별 정의 우선 → 생산유형 상속
차이시간 = 총작업가능시간 − 생산요구시간          → 금액화하여 미사용능력 보고 [A9]
회사부담임률 = 생산직 노무비 합계 ÷ 생산요구시간
제품별 직접노무비 = 단위공수 × 생산수량 × 회사부담임률                            [Q-E 공수 단일]
```
- 일용직 FTE 환산: 총작업시간 기준 환산인원·환산임금(원본 hr 로직 — 730h→3.2명)
- 월급제/일급제 구분(pay_type), 복리후생 포함 인건비 구성(기본급·시간외·복리후생·상여·퇴직충당금)
- 생산요구시간 > 총작업가능시간이면 경고(원본 hr 검증 문구 계승) — 마감 차단 대상 [A11]

**(3) 제조경비**
```
계정별 고정/변동 태그(19계정 → 무제한) + 배부기준 3종 택1 [A5, Q-A]
  ① 직접노무원가 기준: 제품별 직접노무비 비례
  ② 직접노무시간 기준: 제품별 (단위공수×생산수량) 비례
  ③ 기계시간 기준: 제품별 월 기계시간 입력값 비례 (선택 시에만 입력 필드 노출) `[ASSUMPTION: 기계시간 입력 필드는 원본 costmgr에는 없음. 신규 입력은 추론. 근거: 배부기준 ③ 선택 시 노출(E4). 추론일: 2026-07-12.]`
배부 모수 = 생산기준 [A4] / 배부합계 = 경비 원금액 [A6]
```

**(4) 제품 원가 2계층** [A2]
```
제조원가 = 재료비 + 직접노무비 + 제조경비배부액                → 재고 평가·제조원가명세서
완전원가 = 제조원가 + 관리인건비배부(직접노무비 비례) + 판관비배부(매출액 비례) → 관리 뷰 전용 `[ASSUMPTION: 관리인건비 = 직접인건비 비례는 원본 pl 시트의 29.3% 상당 배부 패턴 계승. 판관비 = 매출액 비례는 A2·A5의 일반 원칙. 두 비율을 변경할 수 있는 설정 필드는 §8.1 M0에서 노출 여부 추론. 추론일: 2026-07-12.]`
```

**(5) 손익계산서 골격 (원본 pl4 계승)**
```
매출 → 매출원가(재료비+직접인건비+제조경비 ± 제품재고조정) → 매출총이익
    → 판매일반관리비(관리인건비 포함) → 영업손익 → 지급이자 → 세전손익 → 세금 → 세후손익
```
- '제품 재고 조정' 라인 = check 로직의 자동화 산출물(제11장 V4) `[NOTE FOR PM: §11 V4의 "± 제품재고조정" 손익 표시 부호가 사용자 UX에 그대로 노출될 때, 마이너스 재고조정(재고 감소)이 영업손익을 깎는 것처럼 보이는 현상이 발생함. UX는 이 라인이 회계상의 "조정"이지 "손실"이 아님을 시각적으로 구분해야 함(예: 색·라벨 분리). owner: UX designer. 해소 시점: UX 와이어프레임 단계.]`
- 뷰 3종: 종합 / 제품별 / 판매지역별(선택 입력 시)
- 12개월 비교 + YTD(마감월만 집계), 예산 대비(제10장), 차월 추정(차입금·이자율·상승률·세율 파라미터 — 원본 pl3 그린셀 5종)

**(6) 제품별 CVP·BEP (원본 bep 계승)**
```
고정비 F = 관리인건비배부 + 판관비 중 고정 + 제조경비 중 고정 (제품 배부분)
단위당 변동비 V = 재료비 + 직접노무비 + 제조경비 변동분 + 판관비 변동분 (단위당)
공헌이익 CM = P − V / BEP 수량 = F ÷ CM / 목표 세전이익 역산(단가·매출액)
```
- 원본의 한계 고지 계승: "고정비 배부액이 판매량에 따라 변하므로 참고자료"라는 주석 고정 표시
- 고정/변동 태그 활용처 5곳: BEP·AI 재추정·차월 추정·제조경비 배부·목표이익 역산

## 6.2 재고 수불 (원본 m/p 시트 계승)

- 원부재료 수불부: 기초 + 구입 − 생산출고 = 기말, 재고실사 → 재고조정(A8 역분개)
- 제품 수불부: 기초 + 생산 − 판매 = 기말, 제조원가 평가(전월·당월 평균)
- 기초재고는 시스템 개시 시 1회 입력, 이후 월 체인 자동 이월
- 음수재고 실시간 경고(원본의 방치 문제 해소) [A11]

## 6.3 주문 관리 (선택 기능 — 결정 Q-B)

- 주문 입력(월합계 기본/일자별 선택) → 주문잔량 Back Log(누적주문 − 누적생산) → 주문품 자재 총소요 전개(BOM×주문수량 — 구매계획 지원)
- 생산품 자재 총소요(pmaterials 계승)는 기본 제공. 주문 미입력 시 관련 화면·보고서 숨김(E4)

---

# 7. 원가 방법론 명세 — ABC 엔진 (서비스 부문)

## 7.1 Classic ABC (원본 ABCost 3-Step 계승)

```
Step 0  매출·직접비: 원가대상(제품/서비스)별 매출, 귀속 가능한 직접재료비·직접인건비 입력
        (귀속 불가 인건비는 간접비로 — 원본 2.1 labor 원칙)
Step 1  원가풀(Resource Pool): 계정별 간접비를 설비/매출/인원 3기준 비율로 1차 배부
        (일괄적용 Y/N 토글 — 원본 4 resource pool 계승, 행 합 100% 강제)
Step 2  활동(Cost Activity): 주요활동 5~15개(소프트 경고, 원본 권고 ≤10),
        활동별 설비/매출/인원 열 비중(열 합 100% 강제)으로 원가풀 → 활동 배부
Step 3  동인(Activity Driver): 활동별 동인 정의, 제품별 동인 소비 건수 또는 비율(%) 입력 토글,
        '활동 % 직접배분' 우회 옵션(원본 Yes/No) → 원가대상 최종 배부
검증    각 단계 배부합계 = 원금액 [A6] / 동인 열 합 = 총건수 또는 100%
```

- 활동별 동인 건당 소요시간 → 총시간 산출 → **간접인건비 근무시간 검증**(급여기준 월작업시간 vs 동인총시간 차이 표시 — 원본 6 activity drivers의 생산성 진단 계승)
- 업종 템플릿 + AI 활동 초안(E5) — "AI는 초안, 확정은 사람"
- 동인 실적: 수기 + 엑셀 업로드 + AI 추출 + 추정 모드(is_estimated 배지) `[ASSUMPTION: 동인 입력의 "건수/비율 토글" UI 노출은 원본 ABCost의 Yes/No 토글 계승. 토글 기본값(건수 우선) 및 동인별 분포 강제(합 100% / 총건수)는 추론. 추론일: 2026-07-12.]`

## 7.2 TDABC (2026 최신 이론 장착분 — 결정 Q-J 첫 버전 확정)

```
CCR(Capacity Cost Rate) = 부서 원가 ÷ 실제적 조업능력(practical capacity)
실제적 조업능력 = 이론 능력 × 80% 기본 (정밀 마법사로 세부 조정 가능)
원가대상 배부 = Σ(동인 건수 × 건당 표준시간 × CCR)
미사용능력 원가 = (실제적 조업능력 − 사용시간) × CCR → 별도 보고 [A9]
```

- 회사 단위 단일 모드(classic ↔ TDABC 택1), `method_override` 예비 필드로 활동별 혼용은 2차
- CCR 산출 단위 = 부서 `[ASSUMPTION: CCR을 부서 단위로 산출하는 것이 Kaplan TDABC 정론의 일반적 사례이나, 동일 저작자 ABCost 시트에서 CCR 산출 단위가 활동 단위인 케이스도 발견됨. 본 PRD는 부서 단위 단일로 통합 결정. 추론 근거: 부서 단위 산출이 1인 운영자 SaaS의 운영 부담을 낮춤. 추론일: 2026-07-12.]`
- 전통 엔진의 조업도 차이시간과 동일 사상으로 통합 '미사용능력 보고서' 제공

## 7.3 부문 카브아웃 (겸영 기업)

- 공통비 분할: 법인세법 시행규칙 76조 2기준(매출액 비례/개별비용 비례) 택1 [A10]
- 부문 전용 계정 태깅 + 부문귀속명세서 보고서로 분할 근거 공시 `[ASSUMPTION: 부문귀속명세서 카브아웃 분할 근거 공시 형식(표 / 막대차트 / 산식 트리)은 추론. 법인세법 시행규칙 제76조 2기준의 시각화 방법은 정합성 헌법 [A10] 충족을 전제로 UX 단계에서 확정. 근거: §9 #21 표기. 추론일: 2026-07-12.]`
- ③④ 업종은 재무제표 업로드 필수(분할 검증 기준)

---

# 8. 모듈 구성 (통합 13모듈)

| 모듈 | 명칭 | 핵심 내용 | 원천 시트 |
|------|------|-----------|-----------|
| M0 | 온보딩·설정 | 업종 4지선다, 부문, 회계연도 시작월, 통화(KRW/USD)·언어(한/영), 배부기준 3종 선택, AI 문서추출 | infor, basic data |
| M1 | 기준정보 | 품목 통합(제품/반제품/원자재/상품/서비스), BOM 매트릭스 편집, 생산유형·공정, 계정과목(고정/변동), 부서, 판매지역(선택), 거래처(선택), 제품사진 | BOM2, process, infor |
| M2 | 월 데이터 입력 | 6종: 주문(선택)·생산(+생산유형·기계시간(조건))·판매(+지역 선택)·구매(다회차)·경비(제조/판관 구분)·인원인건비(조업도) — 월합계 기본+일자별 선택, 전월복사, 엑셀 업로드 | or, pr, sa, pu, exp, hr |
| M3 | 원가계산 엔진 | 제6장 산식 체인, [계산] 버튼, 1원 단위 엑셀 대조 테스트 통과 필수 | operation, operation2, expenses |
| M4 | 재고 수불 | 원부재료·제품 수불부, 실사 조정, 재고 검색, 음수 경고 | m1~12, p1~12, inv, pinv |
| M5 | 손익·보고서 | 제9장 보고서 체계 전체 | pl 계열, cost, ctable, cststmt |
| M6 | 자동 검증 | 제11장 V1~V8 Reconciliation | check |
| M7 | 시뮬레이션 | 제품별 CVP/BEP + 목표이익 역산(슬라이더), 차월 손익 추정 | bep, pl3 |
| M8 | 예산 시나리오 | 제10장 | pl5, manual '예산' |
| M9 | ABC 엔진 | 제7장 Step 0~3 + TDABC + 카브아웃 | ABCost 전체 |
| M10 | AI 지원 | 문서추출 / 인사이트 질문 3개 생성·캐시 / 고정·변동 3단계 추정 | — |
| M11 | 마감·이력 | 순차 마감(부문분할→제조→ABC→공동), 스냅샷, 역분개 정정, 감사추적 | 저장/복구 VBA 대체 |
| M12 | 계정·운영 | 2FA, 백업(일1회+셀프 다운로드), 해지(보관일수+삭제동의), 운영자 콘솔, 대리접속, 공지 | — |

---

## 8.1 모듈별 인수 기준 (Acceptance Criteria)

각 모듈은 다음 형식의 2~4개 인수 불릿을 만족해야 "완료"로 본다("시스템은 X를 Y 시점에 Z 조건으로 수행한다" — §11 V-row 형태 참고).

- **M0 (온보딩·설정)**
  - (a) 시스템은 신규 가입자가 업종 4지선다를 선택한 시점에 후속 메뉴(§4.1)를 자동 토글한다.
  - (b) 시스템은 회계연도 시작월·통화·언어·배부기준 3종 선택을 미완료 상태로 [계산] 진입을 차단한다.
  - (c) AI 문서추출(E5)은 신뢰도 < 70% 항목을 빨강 배지로 표시하고 사용자 확정을 강제한다.
  - **(d) Phase 3 신규 — 로그인 UI + Supabase SSR auth client** — 시스템은 이메일·비밀번호 기반 로그인을 제공하며, 인증 성공 시 Supabase 가 발급한 `sb-access-token` 쿠키(SSR 호환, `httpOnly` + `secure` + `sameSite=lax`)를 클라이언트·서버 양쪽에서 일관되게 읽어 세션을 확인한다 [§F15.1, §F15.3]. 로그인 실패 시 ko-KR 메시지("이메일 또는 비밀번호가 올바르지 않습니다", `LOGIN_INVALID_CREDENTIALS_KO`)를 표시하고, 5회 연속 실패 시 30초 cool-down을 강제한다 (CR 12-5 D-14 envelope `{code, message_ko, details, trace_id}` 정합). Epic 12 2FA 설정 사용자(M12-a)는 로그인 성공 후 2FA 챌린지 페이지(`/auth/2fa`)로 리다이렉트된다 [Epic 12 wire 정합, AD-26]. capability gate `LOGIN` (capability matrix v1.24, industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러).
  - **(e) Phase 3 신규 — 회원가입 UI + tenant 생성 flow** — 시스템은 이메일·비밀번호·회사명 3필드 기반 회원가입을 제공하며, 가입 성공 시 자동으로 새 tenant(`tenants` row 1개) + owner 사용자 1명(`users` row + `user_tenants` row owner role) + `tenant_settings.onboarding.industry = null` 상태로 초기화한 후 `/[locale]/(auth)/onboarding/industry` 로 리다이렉트한다 [§F15.2]. 비밀번호 강도 검증 (최소 10자 + 대소문자·숫자·특수문자 각 1자 이상) 강제, RFC 5322 이메일 형식 검증. 가입 실패 시 ko-KR 메시지 ("이미 가입된 이메일입니다" / "비밀번호는 10자 이상이며 대소문자·숫자·특수문자를 포함해야 합니다" — `SIGNUP_DUPLICATE_EMAIL_KO` / `SIGNUP_WEAK_PASSWORD_KO`) 표시. capability gate `SIGNUP` (capability matrix v1.24, industry-agnostic 4-industry grants ✅/✅/✅/✅).
  - **(f) Phase 3 신규 — Auth middleware (Supabase session + next-intl middleware EXTENSION)** — 시스템은 `apps/web/middleware.ts` 의 next-intl middleware 를 EXTENSION 하여 모든 `/[locale]/(dashboard)/*` 요청에 대해 Supabase session 검사를 강제한다 [§F15.3, AD-26]. 세션 없거나 만료 시 `/[locale]/login` 으로 redirect (`?redirect=<original-path>` 쿼리 보존). `/[locale]/(auth)/*` (login / signup / forgot-password) 는 공개 route group 으로 미들웨어 bypass. `/api/v1/*` (백엔드 API) 는 미들웨어 bypass (백엔드 자체의 `get_tenant_context` 가 세션 검증). Static assets (`_next/*`, `*.png`, `*.svg`, `*.ico` 등) matcher 제외. capability gate `AUTH_MIDDLEWARE` (capability matrix v1.24, industry-agnostic 4-industry grants ✅/✅/✅/✅). 2FA 미설정 사용자 (Epic 12 M12-a 정합) 가 dashboard 진입 시도 시 `/[locale]/account/security` 로 redirect (2FA 설정 강제).
  - **(g) Phase 4 신규 — Production deployment config + Dockerfile + health check + observability + database backup** — 시스템은 Vercel frontend (vercel.json, regions=[icn1], buildCommand=`pnpm --filter web build`) + Railway backend (railway.toml, builder=DOCKERFILE, healthcheckPath=`/api/v1/health`) + Supabase production PostgreSQL 결정 wire 으로 production deployment 가 atomic single sprint T1~T8 으로 wire 되어야 한다 [§F16, AD-27]. per-app Dockerfile 분리 (`apps/web/Dockerfile` + `apps/api/Dockerfile`, AD-14 stack pin by @sha256: digest 결정). Health check endpoint `GET /api/v1/health` (FastAPI) + `GET /api/health` (Next.js route handler) 가 database connectivity + JWT verification 을 검증하고 liveness/readiness 분리 결정 wire. Sentry observability integration (browser SSR-safe + server FastAPI) 결정. Database backup strategy (alembic 0036 phase_4_backup_strategy table + Supabase PITR 7일 자동 + RPO 5분/RTO 1시간 결정) 결정 wire. `docs/deployment.md` (12 sections runbook) + `docs/database-backup.md` 결정. capability gates `DEPLOYMENT_PROD` + `DEPLOYMENT_STAGING` + `DEPLOYMENT_DATABASE_BACKUP` + `DEPLOYMENT_HEALTH_CHECK` 4 NEW (capability matrix v1.25, industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러).
  - **(h) Epic 15 신규 — Magic link login (D-1-1-DEFER-1 ✅ RESOLVE)** — 시스템은 이메일 단일 필드 기반 매직 링크 로그인을 제공하며, Supabase `signInWithOtp({ email, options: { emailRedirectTo } })` wrapper + 5회 cool-down (sessionStorage 30s, Phase 3-1 T2 wire 패턴 미러) + email 존재 여부 노출 방지 (security invariant try/catch/finally, Phase 3-1 T6 forgot-password 정합) + audit-first INSERT `magic_link_sent` (CR 1-1 verbatim, action_class='AUTH' + action='magic_link_sent' + actor_id + target_email) [§F17.1, AD-28]. Magic link 클릭 시 `supabase.auth.exchangeCodeForSession(code)` + `sb-access-token` cookie session 자동 설정 (Phase 3-1 T1 wire 정합) + `router.push('/dashboard')` 결정. Epic 12 2FA 미설정 사용자 (Epic 12 M12-a 정합) 는 매직 링크 성공 후 2FA 챌린지 페이지(`/auth/2fa`) 로 redirect. capability gate `MAGIC_LINK` (capability matrix v1.26, industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러).
  - **(i) Epic 15 신규 — Social OAuth (Google/Naver/Kakao) login (D-1-1-DEFER-2 ✅ RESOLVE)** — 시스템은 3 provider (Google + Naver + Kakao) 기반 소셜 OAuth 로그인을 제공하며, Supabase `signInWithOAuth({ provider, options: { redirectTo } })` wrapper 결정 wire [§F17.2, AD-28]. Provider whitelist (`ALLOWED_SOCIAL_PROVIDERS = frozenset({'google', 'naver', 'kakao'})` strict reject + counter increment, AD-7 verbatim 정합) + 3회 cool-down (sessionStorage 60s, magic link 와 분리) + audit-first INSERT `social_oauth_initiated` (CR 1-1 verbatim, action_class='AUTH' + action='social_oauth_initiated' + actor_id + provider) + OAuth callback handler (`/auth-callback` + `exchangeCodeForSession(code)` + session cookie setting + dashboard redirect 결정 wire). **Naver OAuth 특수 처리** 결정 wire (2026-08-22 KST, 한국 시장 정합) — Option A Supabase Naver 지원 시 그대로 사용 / Option B 미지원 시 custom Naver OAuth flow wire 결정 wire 보존 (Epic 15-1 bmad-dev-story 진입 시점에 결정). capability gates `SOCIAL_OAUTH_GOOGLE` + `SOCIAL_OAUTH_NAVER` + `SOCIAL_OAUTH_KAKAO` 3 NEW (capability matrix v1.26, industry-agnostic 4-industry grants ✅/✅/✅/✅).
  - **(j) Epic 15 신규 — SSO enterprise SAML (D-1-1-DEFER-3 ✅ RESOLVE)** — 시스템은 SAML 2.0 기반 엔터프라이즈 SSO 로그인을 제공하며, `python3-saml==1.16.0` library (AD-14 stack pin 결정) + SAML response validation (signature verification + `NotBefore`/`NotOnOrAfter` timestamp + `Audience` + `Destination` + `InResponseTo` CSRF 방어 + RelayState base64 encode) 결정 wire [§F17.3, AD-28]. 4 SSO routes 결정 wire — (1) `GET /api/v1/auth/sso/login?tenant_slug=&relay_state=` SAML AuthnRequest 생성 + IdP SSO URL redirect (HTTP 302) / (2) `POST /api/v1/auth/sso/acs` SAML ACS endpoint (response 검증 + JIT provisioning + `sb-access-token` cookie set + 200 OK) / (3) `GET /api/v1/auth/sso/metadata?tenant_slug=` SP metadata XML 반환 (IdP 등록용) / (4) `GET /api/v1/auth/sso/sls` Single Logout Service endpoint. **JIT (Just-In-Time) user provisioning** 결정 wire (SAML response → user + tenant_memberships + external_identities atomic 5-step flow). Multi-tenant isolation (CR 0-2 RLS lesson 적용, AD-22 verbatim): `external_identities` table (alembic 0037) RLS policy `tenant_id = (SELECT current_setting('app.tenant_id'))::uuid` 결정. Tenant slug 별 IdP metadata routing (multi-tenant SSO) 결정. audit-first INSERT `sso_identity_linked` (CR 1-1 verbatim, action_class='AUTH' + action='sso_identity_linked' + actor_id + provider + provider_user_id + tenant_id) 결정. Epic 12 2FA 미설정 사용자는 SSO 성공 후에도 `/auth/2fa` 로 redirect 결정 wire. capability gate `SSO_ENTERPRISE` (capability matrix v1.26, industry-agnostic 4-industry grants ✅/✅/✅/✅).
  - **(k) 1st release 신규 — 1st release launch 진입 (옵션 (d) 결정 wire, A83 결정)** — 시스템은 1st release launch 결정 wire 진입 시점에 다음 6 conditions ALL PASS 결정 wire: (1) Marketing landing page wire DONE (`/landing` route + LandingHero + LandingFeatures + LandingPricing + LandingCTA + ko-KR inline copy EXTENSION, §F18.1) / (2) ToS + Privacy Policy wire DONE (`docs/terms-of-service.md` + `docs/privacy-policy.md` 한국 PIPA + GDPR 정합, §F18.2) / (3) Onboarding user guide wire DONE (`docs/onboarding-guide.md` 8 sections + OnboardingTooltip + first-run wizard EXTENSION Epic 1 partial scaffold 정합, §F18.3) / (4) Customer support channels wire DONE (`docs/support.md` + email + HelpWidget + FAQ, §F18.4) / (5) Production launch verification wire DONE (smoke test RE-RUN 정직 결정 + backup drill 0036 PITR quarterly + Sentry alert wiring + RPO 4h/RTO 24h SLA verification, §F18.5) / (6) Public launch communications wire DONE (`docs/launch-announcement.md` + press kit + og/assets + in-app banner, §F18.6). 6 conditions ALL PASS 진입 시점에 1st release official launch 결정 wire 보존 (cj-style 62번째 epic 연속 정직 회복). capability gates `LAUNCH_LANDING` + `LAUNCH_TOS` + `LAUNCH_SUPPORT` + `LAUNCH_MONITORING` 4 NEW (capability matrix v1.27, industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러).
  - **(l) Epic 16 신규 — Tenant IdP admin management (옵션 (a) Epic 16 진입 결정 wire, A92 결정)** — 시스템은 Epic 16 = Tenant IdP admin management 결정 wire 진입 시점에 다음 6 ACs ALL PASS 결정 wire [§F19, AD-30]: (1) **`tenant_idps` table** wire DONE (alembic 0038 + columns 13개 + unique constraint `(tenant_id, idp_entity_id)` + RLS policy `tenant_id = current_setting('app.tenant_id')` + index `idx_tenant_idps_tenant_id` + `updated_at_auto_update_trg` audit trigger) / (2) **IdP metadata XML validation service** wire DONE (`apps/api/modules/auth/sso/idp_metadata_validator.py` 8 validation steps: well-formedness + EntityDescriptor + entityID + IDPSSODescriptor + x509 cert PEM wrap + SSO URL https:// + SLO URL https:// + tenant slug 매칭) / (3) **Tenant IdP CRUD API 5 routes** wire DONE (`GET / POST / PUT / DELETE / TEST /api/v1/admin/tenant/{tenant_slug}/idp`, owner/admin role required + capability gate `TENANT_IDP_MANAGEMENT` + RLS 자동 적용 + audit-first INSERT 4 NEW: `tenant_idp_created` / `tenant_idp_updated` / `tenant_idp_deleted` / `tenant_idp_tested`) / (4) **Tenant IdP admin UI** wire DONE (`apps/web/app/[locale]/(dashboard)/settings/sso/page.tsx` + 4 components: TenantIdPConfigForm + TenantIdPStatusBadge + TenantIdPTestResultModal + TenantIdPDeleteConfirmDialog + `apps/web/messages/ko-KR.json` `settings.sso.*` namespace EXTENSION 12 keys + (dashboard) 보호) / (5) **Per-tenant IdP routing EXTENSION** wire DONE (Epic 15 SAML routes `saml_routes.py` EXTENSION: `tenant_slug` → `tenant_idps` lookup → `idp_sso_url` redirect + ACS `idp_x509_cert` 동적 로딩 + backward compatibility `acme` hardcoded tenant 보존) / (6) **Capability gate TENANT_IDP_MANAGEMENT** wire DONE (capability matrix v1.27 → v1.28 EXTENSION 1 NEW row + 4-industry grants industry-agnostic ✅/✅/✅/✅ + drift detector `tests/integration/test_capability_matrix_v1_28_drift.py` NEW). 6 ACs ALL PASS 진입 시점에 Epic 16 wire DONE 결정 wire 보존 (cj-style 67번째 epic 연속 정직 회복). capability gate `TENANT_IDP_MANAGEMENT` (capability matrix v1.28, industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러, SSO_ENTERPRISE Epic 15 wire pattern). D-LAUNCH-1-DEFER-1 honestly preserved 65~66~67번째 (CR 11-3 honest-DEFER discipline 67번째 epic 연속 정직 회복 검증). D-1-1-DEFER-1/2/3 ✅ RESOLVED 보존 67번째 (Epic 15 wire `5f9e37f` 진입 시점에 모두 정직 회복 결정 wire 완료).
  - **(m) Phase 5 신규 — Multi-Region Backup & Disaster Recovery (옵션 (a) Phase 5 진입 결정 wire, A124 결정)** — 시스템은 Phase 5 = Multi-Region Backup & Disaster Recovery 결정 wire 진입 시점에 다음 5 ACs ALL PASS 결정 wire [§F20, AD-31]: (1) **Cross-region read replica + WAL archiving** wire DONE (alembic 0039 phase_5_replication_lag table + `phase_5_dr_drill_results` table + replica_region enum seoul/tokyo/singapore/frankfurt/virginia/oregon/sao_paulo + primary_region enum + lag_bytes BIGINT + lag_seconds INTEGER + last_synced_lsn TEXT PG_LSN + replication_status enum syncing/replicating/lagged/disconnected/failed + audit-first INSERT `replica_status_changed` CR 1-1 verbatim + 3 indexes + 2 CHECK constraints + `apps/api/alembic/versions/0039_phase_5_multi_region_backup.py` + `docs/cross-region-replication.md`) / (2) **Cross-region failover automation** wire DONE (`apps/api/jobs/failover_orchestrator.py` + primary → secondary health probe 5-second interval + 3 consecutive failures trigger + automatic promotion via Supabase API `POST /v1/projects/{ref}/database/promote` + DNS update via Supabase custom domain redirect + 30s RTO target + audit-first INSERT `failover_initiated` + `failover_completed` + FastAPI lifespan hook startup/shutdown + GRACEFUL_SHUTDOWN_TIMEOUT=30s + owner-only manual trigger `POST /api/v1/admin/failover` AD-22 RBAC + 2FA 챌린지 Epic 12 정합) / (3) **DR drill + automated quarterly test** wire DONE (`apps/api/jobs/dr_drill.py` + cron KST 1st Sunday 03:00 UTC 18:00 + actual failover drill test in staging + 6 drill steps: health check + secondary promote + write test + application health + DNS update + restore trigger + RPO/RTO measurement + `phase_5_dr_drill_results` table + Q1/Q2/Q3/Q4 quarterly schedule + audit-first INSERT `dr_drill_completed`) / (4) **Cross-region backup strategy** wire DONE (`docs/database-backup.md` EXTENSION + Supabase PITR primary Seoul + secondary Tokyo + 30일 hot + 90일 cold + 365일 archive regional + RPO 1h / RTO 4h SLA post-multi-region wire DONE 진입 + Phase 4 single-region RPO 5min/RTO 1h honest-extreme risk = Seoul region disaster 시 backup restoration 불가 verbatim 해소 결정) / (5) **Multi-region health observability** wire DONE (`apps/api/core/health.py` EXTENSION `/api/v1/health/multi-region` endpoint + primary + secondary status array + CR 12-5 D-14 envelope `{status, primary: {region, status, lag_bytes, lag_seconds, last_synced_at}, secondary: {...}, timestamp}` + `apps/api/core/observability.py` EXTENSION Sentry breadcrumb failover + Sentry alert routing + Grafana multi-region dashboard + `apps/web/app/api/health/multi-region/route.ts` NEW Next.js Edge Runtime + force-dynamic + capability gates MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER 2 NEW capability matrix v1.28 → v1.29 EXTENSION industry-agnostic 4-industry grants ✅/✅/✅/✅ + drift detector `tests/integration/test_capability_matrix_v1_29_drift.py` NEW + ~50 NEW pytest + ~10 NEW vitest). 5 ACs ALL PASS 진입 시점에 Phase 5 wire DONE 결정 wire 보존 (cj-style 73번째 epic 연속 정직 회복 wire 진입 시점). capability gates MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER (capability matrix v1.29, industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러, DEPLOYMENT_PROD + DEPLOYMENT_DATABASE_BACKUP Phase 4 wire pattern 미러). D-PHASE-4-DR-DEFER-1 + D-PHASE-4-DR-DEFER-2 ✅ RESOLVE 진입 wire 결정 (Phase 4 close-out retro §6 disaster recovery verbatim multi-region backup 결정 wire 보류 honestly carry-over + Phase 5 PRD entry 진입 시점에 정직 회복 결정 wire 완료, CR 11-3 honest-DEFER discipline 73번째 epic 연속 정직 회복 검증). D-1-1-DEFER-1/2/3 ✅ RESOLVED 보존 73번째 (Epic 15 wire `5f9e37f` 진입 시점에 모두 정직 회복 결정 wire 완료). D-EPIC-16-REVIEW-DEFER-2~6 honestly DEFERRED 5 OPEN 보존 73번째 (Epic 16 wire `963079c` 진입 시점에 결정).
  - **(n) Epic 17 신규 — Audit Log Viewer & Activity Stream (옵션 (a) Epic 17 진입 결정 wire, A153 결정)** — 시스템은 Epic 17 = Audit Log Viewer & Activity Stream 결정 wire 진입 시점에 다음 7 ACs ALL PASS 결정 wire [§F21, AD-32]: (1) **audit log query API** wire DONE (`apps/api/modules/audit/audit_log_query.py` NEW ~+180 LOC, 4 functions: query_audit_log + count_audit_log + get_audit_log_entry + query_activity_stream + AuditLogQueryFilters + AuditLogEntry + AuditLogPage + ActivityStreamGroup TypedDict + RLS 자동 적용 CR 0-2 verbatim + owner/admin role required + capability gate AUDIT_LOG_VIEW) / (2) **audit log viewer UI** wire DONE (`apps/web/app/[locale]/(dashboard)/audit-log/page.tsx` NEW ~+200 LOC + 5 components: AuditLogFilterPanel + AuditLogTable + AuditLogPagination + AuditLogExportButton + AuditLogDetailModal + ko-KR.json `audit_log.*` namespace EXTENSION 14 keys + (dashboard) 보호 + vitest RTL render discipline CR 11-4 D-003 verbatim) / (3) **activity stream UI** wire DONE (`apps/web/app/[locale]/(dashboard)/activity/page.tsx` NEW ~+150 LOC + 3 components: ActivityStreamTimeline + ActivityStreamEntry + ActivityStreamWindowSelector + ko-KR.json `activity.*` namespace EXTENSION 8 keys + all tenant members 권한) / (4) **cross-region audit log visibility** wire DONE (Phase 5 multi-region read replica 통한 cross-region audit query + read-only routing + lag threshold 정합 lag_bytes ≤ 100MB + lag_seconds ≤ 30s + Sentry breadcrumb 결정 wire) / (5) **CSV export** wire DONE (`apps/api/modules/audit/audit_log_export.py` NEW ~+120 LOC + streaming response + UTF-8 BOM + Excel-compatible comma-separated + audit-first INSERT `audit_log_exported` CR 1-1 verbatim action_class='AUDIT' + action='audit_log_exported' + actor_id + tenant_id + filters_json + row_count + trace_id + CR 12-5 D-14 envelope 결정 wire) / (6) **Capability gate AUDIT_LOG_VIEW** wire DONE (capability matrix v1.29 → v1.30 EXTENSION 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅ + drift detector `tests/integration/test_capability_matrix_v1_30_drift.py` NEW) / (7) **Tests + wire scope T1~T8** 결정 (~+50 NEW pytest PASS + ~+15 NEW vitest PASS + 0 NEW ruff + 0 regressions + T1 audit_log_query module + T2 audit_log_export module + T3 audit_log_viewer page + T4 activity_stream page + T5 ko-KR.json SSOT EXTENSION + T6 capability.py + capability matrix v1.30 + T7 tests + 3중 게이트 FINAL CLEAN + T8 atomic commit). 7 ACs ALL PASS 진입 시점에 Epic 17 wire DONE 결정 wire 보존 (cj-style 80번째 epic 연속 정직 회복 wire 진입 시점). capability gate AUDIT_LOG_VIEW (capability matrix v1.30, industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러, MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind). D-1-1-DEFER-1/2/3 ✅ RESOLVED 보존 80번째 (Epic 15 wire `5f9e37f` 진입 시점에 모두 정직 회복 결정 wire 완료). D-LAUNCH-1-DEFER-1 honestly preserved 65~80번째 (1st release cycle 보존). D-EPIC-16-REVIEW-DEFER-2~6 ✅ RESOLVED 보존 78~80번째 (D-EPIC-16-REVIEW-DEFER-2~6 RESOLVE sprint `512ed6a` 78번째 wire DONE 진입 후 보존). D-PHASE-4-DR-DEFER-1/2 ✅ RESOLVED 보존 73~80번째 (Phase 5 PRD entry 73번째 + atomic wire 75번째 + close-out retro 76~77번째 진입 시점에 모두 정직 회복 결정 wire 완료).
  - **(o) Phase 6 신규 — Audit Log Retention Policy (옵션 (a) Phase 6 진입 결정 wire, A193 결정)** — 시스템은 Phase 6 = Audit Log Retention Policy 결정 wire 진입 시점에 다음 7 ACs ALL PASS 결정 wire [§F22, AD-33]: (1) **retention policy DSL** wire DONE (`apps/api/modules/audit/audit_retention_policy.py` NEW ~+120 LOC, 4 classes: AUDIT 2557일 7년 SOX hold + AUTH 30일 + INFRA 90일 + DATA 365일 + CONFIG 180일 + 4 산업 baseline + per-tenant override + policy evaluation precedence tenant override > industry baseline > system default + ActionClass 별 retention rule 결정 wire + `DEFAULT_RETENTION_DAYS` constants 결정 wire) / (2) **automatic purge job** wire DONE (`apps/api/jobs/audit_purge.py` NEW ~+150 LOC, cron KST 매일 03:00 UTC 18:00 + tenant 별 retention policy 평가 + dry-run mode `--dry-run` flag + purge algorithm 6 steps: phase_6_audit_purge_log INSERT + retention policy evaluate + dry-run + archive + mask_pii + delete + 5 NEW actions audit-first INSERT `audit_log_purged` + `audit_log_archived` + `audit_log_pii_masked` + `audit_log_cold_archived` + `audit_log_personal_data_erased` CR 1-1 verbatim 적용 + `apps/api/jobs/audit_cold_archive.py` NEW ~+120 LOC + cron KST 1st Sunday 04:00 UTC 19:00 monthly cold archive + alembic 0040 phase_6_audit_retention + RLS 자동 적용 CR 0-2 verbatim) / (3) **archive storage + 365일 cold archive** wire DONE (`audit_log_archive` table alembic 0040 + immutable append-only trigger `audit_log_archive_immutable_trg` BEFORE UPDATE OR DELETE raise exception + SHA-256 hash chain linkage `hash_chain` column + 이전 row hash + 현재 row payload SHA-256 = current.hash_chain 결정 wire + tamper detection C2 결정 wire 도입 + 4 NEW indexes `idx_audit_log_archive_tenant_id` + `idx_audit_log_archive_action_class` + `idx_audit_log_archive_archived_at` + `idx_audit_log_archive_original_id` + cross-region archive Phase 5 wire `f093f8c` `phase_5_replication_lag` table + Supabase multi-region primary Seoul + secondary Tokyo replica EXTENSION + 365일 cold archive + audit_log_archive 의 cross-region replication 결정 wire + Phase 5 wire 정합) / (4) **GDPR/NFR4 PII minimization + Article 17 erasure EXTENSION** wire DONE (`email_mask(email) -> "[REDACTED:sha256(email)]"` + `ip_mask(ip) -> "[REDACTED:ip]/24"` 결정 wire + GDPR Article 17 erasure endpoint `DELETE /api/v1/audit/personal-data?email=` owner-only RBAC AD-22 verbatim 보존 + audit-first INSERT `audit_log_personal_data_erased` 5th NEW action + audit_log table 의 해당 email 의 PII 컬럼 전체 `[REDACTED]` UPDATE + audit_log_archive 에 archive_event INSERT + NFR4 PII minimization 정합 + 1st release close-out retro §6 + Epic 17 close-out retro §11 "audit log retention policy 결정 wire 보류, Phase 6+ 진입 시점" verbatim 해소 결정 wire) / (5) **audit-first INSERT on deletion** wire DONE (5 NEW actions audit-first INSERT: `audit_log_purged` + `audit_log_archived` + `audit_log_pii_masked` + `audit_log_cold_archived` + `audit_log_personal_data_erased` CR 1-1 verbatim 적용 + apps/api/core/audit_action.py MODIFIED AuditAction Literal 5 values EXTENSION + _ActionRegistry AUDIT entry 신규 5개 등록 + __all__ EXTENSION + audit-first INSERT BEFORE actual mutation CR 1-1 verbatim discipline + immutable audit log table + 별도 archive table 동시 지원 territory) / (6) **Capability gate AUDIT_LOG_RETENTION** wire DONE (`apps/api/core/capability.py` MODIFIED + Capability.AUDIT_LOG_RETENTION = "audit_log_retention" 1 NEW enum + 4-industry grants ✅/✅/✅/✅ CR 12-1 L4 precedent 미러 + `apps/api/dependencies/capability.py` EXTENSION `require_audit_log_retention` 1 NEW dep + owner-only RBAC AD-22 verbatim 보존 + capability matrix v1.30 → v1.31 EXTENSION 1 NEW row + drift detector `tests/integration/test_capability_matrix_v1_31_drift.py` NEW 8 NEW pytest cases 결정 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind) / (7) **Tests + wire scope T1~T8** 결정 (~+30 NEW pytest PASS (audit_retention_policy 8 + audit_purge 12 + audit_cold_archive 4 + alembic 4 + PII redaction 5 + capability matrix v1.31 8 + audit action 5 신규 = ~+46 NEW pytest PASS) + ~+5 NEW vitest PASS (dry-run UI test 3 + SSOT drift 2 + ko-KR SSOT 검증 = ~+5 NEW vitest PASS) + 0 NEW ruff + 0 regressions + T1 retention policy module + T2 automatic purge job + T3 alembic 0040 archive + T4 PII redaction + T5 capability v1.31 + T6 dry-run mode UI + T7 tests + T8 atomic commit). 7 ACs ALL PASS 진입 시점에 Phase 6 wire DONE 결정 wire 보존 (cj-style 87번째 epic 연속 정직 회복 wire 진입 시점). capability gate AUDIT_LOG_RETENTION (capability matrix v1.31, industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러, AUDIT_LOG_VIEW Epic 17 wire pattern verbatim bind). D-1-1-DEFER-1/2/3 ✅ RESOLVED 보존 85번째 (Epic 15 wire `5f9e37f` 진입 시점에 모두 정직 회복 결정 wire 완료). D-LAUNCH-1-DEFER-1 honestly preserved 65~85번째 (1st release cycle 보존). D-EPIC-16-REVIEW-DEFER-2~6 ✅ RESOLVED 보존 78~85번째. D-PHASE-4-DR-DEFER-1/2 ✅ RESOLVED 보존 73~85번째. D-EPIC-17-WIRE-DEFER-T2-T3-UI ✅ RESOLVED 보존 83~85번째. D-RETENTION-1 honestly DEFER 보존 65~85번째 (1st release close-out retro §6 + Epic 17 close-out retro §11 "audit log retention policy 결정 wire 보류, Phase 6+ 진입 시점" verbatim 해소 + Phase 6 PRD entry 진입 시점에 정직 회복 결정 wire 완료, CR 11-3 honest-DEFER discipline 85번째 epic 연속 정직 회복 검증).
  - **(p) Phase 7 신규 — Observability Stack 강화 (옵션 (a) Phase 7 진입 결정 wire, A223 결정)** — 시스템은 Phase 7 = Observability Stack 강화 (OpenTelemetry distributed tracing + Prometheus custom metrics + Grafana dashboards + Alerting) 결정 wire 진입 시점에 다음 7 ACs ALL PASS 결정 wire [§F23, AD-34]: (1) **OpenTelemetry distributed tracing** wire DONE (`apps/api/core/tracing.py` NEW ~+200 LOC, OTLP HTTP exporter 결정 wire + W3C Trace Context propagation (traceparent + tracestate) + span enrichment `tenant_id` + `user_id` + `trace_id` + auto-instrumentation FastAPI + SQLAlchemy + httpx + asyncpg + span attributes `http.method` + `http.route` + `http.status_code` + `db.statement` + `tenant.id` + sampling decision head_based ratio 1.0 dev + 0.1 prod 결정 wire) / (2) **Prometheus custom metrics** wire DONE (`apps/api/core/metrics.py` NEW ~+180 LOC, Counter + Histogram + Summary + Gauge 결정 wire + `business_signups_total{industry,plan}` + `business_logins_total{method,outcome}` + `business_calculations_total{engine,outcome}` + `business_cost_engine_duration_seconds{engine,tenant_size_bucket}` Histogram + `business_audit_log_purge_total{action_class}` + `business_active_tenants_gauge` + `business_ai_extraction_duration_seconds{model,outcome}` Histogram + Grafana dashboards 정의 `docs/grafana-dashboards.md` 4 NEW dashboards: business-signups + cost-engine-performance + auth-flow + audit-log-purge) / (3) **Alerting system** wire DONE (`apps/api/core/alerting.py` NEW ~+120 LOC, Prometheus AlertManager integration + Sentry alert routing `apps/api/core/observability.py` EXTENSION alert webhook handler + Slack webhook integration `#bizup-alerts` channel + PagerDuty integration owner-only manual trigger + alert rules YAML `config/alert_rules.yaml` + 5 NEW alert rules: `HighErrorRate` (5xx > 5% for 5m) + `SlowCalc` (p99 > 5s for 10m) + `FailoverStuck` (replication_lag_seconds > 30 for 5m) + `RetentionPurgeFailed` (audit_log_purge_last_success_timestamp > 26h) + `MultiRegionDown` (primary + secondary all down) + audit-first INSERT `alert_fired` CR 1-1 verbatim action_class='OBSERVABILITY' + action='alert_fired' + actor_id='system' + alert_name + severity + tenant_id + trace_id) / (4) **Frontend performance tracing (Browser RUM)** wire DONE (`apps/web/lib/tracing.ts` NEW ~+150 LOC, Browser RUM via OpenTelemetry web SDK + W3C Trace Context propagation server → client through `traceparent` header 결정 wire + Web Vitals auto-collection LCP + FID + CLS + custom span attributes `user.tenant_id` + `user.role` + API client fetch instrumentation `apps/web/lib/api-fetch.ts` MODIFIED trace context propagation + Next.js instrumentation hook `apps/web/instrumentation.ts` NEW + Next.js route handler span propagation 결정 wire) / (5) **audit-first INSERT 2 NEW actions** wire DONE (ActionClass.OBSERVABILITY EXTENSION 신규 정의 + 2 NEW AuditAction Literal values: `alert_fired` (severity + alert_name + tenant_id) + `trace_sampled` (decision + tenant_id + sampling_ratio) + apps/api/core/audit_action.py MODIFIED AuditAction Literal EXTENSION 2 NEW values + _ActionRegistry OBSERVABILITY entry 신규 2개 등록 + __all__ EXTENSION + emit_audit_typed BEFORE alerting trace 결정 wire) / (6) **Capability gates OBSERVABILITY_TRACES + OBSERVABILITY_METRICS** wire DONE (`apps/api/core/capability.py` MODIFIED + `Capability.OBSERVABILITY_TRACES = "observability_traces"` + `Capability.OBSERVABILITY_METRICS = "observability_metrics"` 2 NEW enum 추가 + 4-industry grants ✅/✅/✅/✅ CR 12-1 L4 precedent 미러 + `apps/api/dependencies/capability.py` EXTENSION `require_observability_traces` + `require_observability_metrics` 2 NEW deps + owner-only RBAC AD-22 verbatim 보존 + capability matrix v1.31 → v1.32 EXTENSION 2 NEW rows + drift detector `tests/integration/test_capability_matrix_v1_32_drift.py` NEW 8 NEW pytest cases 결정 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind) / (7) **Tests + wire scope T1~T8** 결정 (~+30 NEW pytest PASS (tracing module 6 + metrics module 6 + alerting module 4 + grafana dashboards 2 + alembic 0041 0 = skip pytest + audit action 2 NEW + capability matrix v1.32 8 + integration e2e 2 = ~+30 NEW pytest PASS) + ~+10 NEW vitest PASS (browser RUM tracing 3 + API fetch trace propagation 2 + SSOT drift 2 + ko-KR SSOT 검증 3 = ~+10 NEW vitest PASS) + 0 NEW ruff + 0 regressions + T1 OpenTelemetry tracing module + T2 Prometheus metrics module + T3 Alerting module + T4 Frontend tracing module + T5 capability v1.32 EXTENSION 2 NEW rows + T6 audit action EXTENSION 2 NEW + T7 tests + T8 3중 게이트 FINAL CLEAN atomic commit). 7 ACs ALL PASS 진입 시점에 Phase 7 wire DONE 결정 wire 보존 (cj-style 91번째 epic 연속 정직 회복 wire 진입 시점). capability gates OBSERVABILITY_TRACES + OBSERVABILITY_METRICS (capability matrix v1.32, industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러, AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire pattern verbatim bind). D-1-1-DEFER-1/2/3 ✅ RESOLVED 보존 89번째 (Epic 15 wire `5f9e37f` 진입 시점에 모두 정직 회복 결정 wire 완료). D-LAUNCH-1-DEFER-1 honestly preserved 65~89번째. D-EPIC-16-REVIEW-DEFER-2~6 ✅ RESOLVED 보존 78~89번째. D-PHASE-4-DR-DEFER-1/2 ✅ RESOLVED 보존 73~89번째. D-EPIC-17-WIRE-DEFER-T2-T3-UI ✅ RESOLVED 보존 83~89번째. D-RETENTION-1 ✅ RESOLVED 보존 85~89번째. **D-OBSERVABILITY-1 honestly DEFER 보존 89번째** (1st release close-out retro + Epic 17 close-out retro + Phase 6 close-out retro "observability stack 강화 결정 wire 보류, Phase 7+ 진입 시점" verbatim 해소 + Phase 7 PRD entry 진입 시점에 정직 회복 결정 wire 완료, CR 11-3 honest-DEFER discipline 89번째 epic 연속 정직 회복 검증).
  - **(q) Phase 8 신규 — Performance / Load Testing territory (옵션 (a) Phase 8+ 진입 결정 wire, A253 결정)** — 시스템은 Phase 8 = Performance / Load Testing (k6 부하 테스트 + SLO/SLI 정의 + p99 latency budget 5s + Latency regression detector + Performance regression gate + Cost-engine benchmark V8 골든 + Load test report + runbook) 결정 wire 진입 시점에 다음 7 ACs ALL PASS 결정 wire [§F24, AD-35]: (1) **k6 부하 테스트 5 scenarios** wire DONE (`apps/api/tests/performance/k6/` + `scripts/smoke-test-100.js` (sanity 100 VUs 30s) + `scripts/baseline-1000.js` (baseline 1000 VUs 5min) + `scripts/stress-5000.js` (stress 5000 VUs 10min) + `scripts/soak-100-1h.js` (soak 100 VUs 1h memory leak) + `scripts/spike-10000.js` (spike 10000 VUs 1min) 결정 wire + `k6==0.45.0` AD-14 stack pin + `docs/load-testing.md` EXTENSION 12 sections runbook 결정 wire) / (2) **SLO/SLI 정의 4 metrics** wire DONE (`docs/slo-sli-definition.md` NEW 8 sections + (1) `business_cost_engine_duration_seconds p99 < 5s` SLO 99% / (2) `business_signups_total success_rate > 99%` SLO 99% / (3) `business_logins_total p99 < 1s` SLO 99.9% / (4) `business_audit_log_purge_total success_rate > 99.9%` SLO 99.9% 결정 wire + Phase 7 wire `59b56cd` 의 7 NEW business metrics 의 자연스러운 carry-over chain 결정 wire) / (3) **p99 latency budget 5s cost-engine** 결정 wire (`apps/api/core/performance.py` NEW + `LatencyBudget` TypedDict + `check_latency_budget(engine, p99_seconds) -> LatencyBudgetResult` + 4 industries baseline industry-agnostic + per-tenant override EXTENSION 결정 wire + CR 12-5 D-14 envelope `{engine, p99_seconds, threshold, breach, trace_id}` 결정 wire) / (4) **Latency regression detector** wire DONE (`apps/api/jobs/latency_regression.py` NEW ~+150 LOC + cron KST 매일 03:30 UTC 18:30 + 매 24시간 p99 latency sample 수집 + drift > 10% p99 vs golden baseline 자동 감지 + Sentry breadcrumb `LatencyRegressionDetected` + Slack alert `#bizup-alerts` channel + audit-first INSERT `latency_regression_detected` CR 1-1 verbatim 결정 wire + Phase 7 wire `59b56cd` 의 SlowCalc alert rule 정합 결정 wire + Epic 11 V8 determinism 골든 fixture EXTENSION 결정 wire) / (5) **Performance regression gate (CI)** wire DONE (`.github/workflows/ci-performance.yml` NEW + k6 부하 테스트 자동 실행 + p99 budget 초과 시 CI 빌드 차단 결정 wire + `BASELINE_THRESHOLD=1.10` (10% drift) 결정 wire + NFR18 ko-KR 정합 결정 wire) / (6) **Cost-engine benchmark V8 골든** wire DONE (`tests/performance/test_cost_engine_benchmark.py` NEW ~+250 LOC + Epic 11 V8 determinism 골든 fixture EXTENSION + cost-engine 12-period benchmark 결정 wire + `packages/cost_engine/traditional/calculator.py` + `packages/cost_engine/abc/calculator.py` + `packages/cost_engine/tdabc/calculator.py` 3 engines benchmark 결정 wire + `BENCHMARK_GOLDEN_BASELINE_MS = {traditional: 800, abc: 1200, tdabc: 1500}` 결정 wire + `BENCHMARK_DRIFT_THRESHOLD = 1.10` 결정 wire) / (7) **Tests + wire scope T1~T8** 결정 (~+25 NEW pytest PASS (k6 parser 4 + latency_budget 6 + latency_regression 5 + cost_engine_benchmark 4 + capability matrix v1.33 6 = ~+25 NEW pytest PASS) + ~+5 NEW vitest PASS (load-test-report UI 2 + SSOT drift 2 + ko-KR SSOT 검증 1 = ~+5 NEW vitest PASS) + 0 NEW ruff + 0 regressions + T1 k6 부하 테스트 5 scenarios + T2 SLO/SLI 정의 4 metrics + T3 p99 latency budget 5s + T4 Latency regression detector + T5 Performance regression gate + T6 Cost-engine benchmark V8 골든 + T7 Tests + T8 3중 게이트 FINAL CLEAN atomic commit). 7 ACs ALL PASS 진입 시점에 Phase 8 wire DONE 결정 wire 보존 (cj-style 95번째 epic 연속 정직 회복 wire 진입 시점). capability gate PERFORMANCE_TESTING (capability matrix v1.33, industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러, OBSERVABILITY_TRACES + OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind). D-1-1-DEFER-1/2/3 ✅ RESOLVED 보존 93번째. D-LAUNCH-1-DEFER-1 honestly preserved 65~93번째. D-EPIC-16-REVIEW-DEFER-2~6 ✅ RESOLVED 보존 78~93번째. D-PHASE-4-DR-DEFER-1/2 ✅ RESOLVED 보존 73~93번째. D-EPIC-17-WIRE-DEFER-T2-T3-UI ✅ RESOLVED 보존 83~93번째. D-RETENTION-1 ✅ RESOLVED 보존 85~93번째. D-OBSERVABILITY-1 ✅ RESOLVED 보존 89~93번째 (Phase 7 PRD entry 진입 시점에 정직 회복 결정 wire 완료). **D-PERFORMANCE-1 신규 honestly DEFER 보존 93번째** (Phase 7 close-out retro "Performance / Load Testing territory 결정 wire 보류, Phase 8+ 진입 시점" verbatim 해소 + Phase 8 PRD entry 진입 시점에 정직 회복 결정 wire 완료, CR 11-3 honest-DEFER discipline 93번째 epic 연속 정직 회복 검증).
  - **(r) Phase 9 신규 — Chaos Engineering / Game Day territory (옵션 (a) Phase 9+ 진입 결정 wire, A283 결정)** — 시스템은 Phase 9 = Chaos Engineering / Game Day (chaos experiment definition + fault injection types 10 categories + game day runbook + blast radius control 5 levels + continuous chaos vs scheduled game day + tenant-scoped + multi-region chaos + auto-rollback + safety mechanisms 6 layers + dry-run mode + observability integration) 결정 wire 진입 시점에 다음 7 ACs ALL PASS 결정 wire [§F25, AD-36]: (1) **chaos experiment definition** 결정 wire (`apps/api/modules/chaos/chaos_experiment.py` NEW ~+150 LOC + `ChaosExperiment` TypedDict 12 fields + 5 blast_radius levels (L1 single_request / L2 single_tenant / L3 all_tenants / L4 single_region / L5 multi_region) + 4 abort conditions (steady_state_metric > 1.5x baseline / error_rate > 5% / duration max / manual abort) + steady_state metric + hypothesis + rollback_strategy 결정 wire) / (2) **fault injection types 10 categories** 결정 wire (`apps/api/modules/chaos/fault_injection.py` NEW ~+200 LOC + 10 fault types: latency injection (HTTP middleware `LatencyFaultMiddleware`) + error injection (`ErrorFaultMiddleware`) + resource exhaustion (CPU + memory via cgroups) + network partition (Linux `tc netem`) + disk I/O stress (`fio`) + DB connection pool exhaustion + cache failure (Supabase cache eviction) + DNS failure + process kill + clock skew (`libfaketime`)) / (3) **game day runbook + blast radius control** 결정 wire (`docs/chaos-engineering.md` NEW 14 sections runbook + `apps/api/jobs/chaos_game_day.py` NEW ~+150 LOC + cron KST 1st Sunday 03:00 = UTC 18:00 + 8 game day steps: experiment selection + tenant scoping + blast radius confirmation + steady state baseline 측정 + fault injection + observation + auto-rollback + post-mortem report) / (4) **continuous chaos vs scheduled game day** 결정 wire (`apps/api/jobs/continuous_chaos.py` NEW ~+120 LOC + 4 production-safe experiment candidates (cost-engine-latency-injection-100ms + auth-error-injection-1pct + audit-log-query-latency-injection-50ms + multi-region-replication-lag-injection) + L1 single_request blast radius only + 5% traffic + 60s max duration + Prometheus custom metrics `business_chaos_experiments_total{experiment_name,blast_radius,outcome}` Counter + `business_chaos_auto_rollback_total{experiment_name,trigger}` Counter + `business_chaos_observations_seconds` Histogram) / (5) **tenant-scoped + multi-region chaos** 결정 wire (CR 0-2 RLS lesson + L2 single_tenant + L4 single_region + L5 multi_region + `apps/api/alembic/versions/0041_phase_9_chaos_engineering.py` NEW `phase_9_chaos_experiments` table 14 columns + 3 indexes + 2 CHECK constraints + RLS policy `tenant_id = current_setting('app.tenant_id')::uuid` + Phase 5 wire `f093f8c` `phase_5_replication_lag` table 정합) / (6) **auto-rollback + safety mechanisms 6 layers** 결정 wire (4 strategies: automatic / manual (owner-only + 2FA 챌린지 Epic 12 정합) / hybrid / scheduled abort + 6 safety layers: abort conditions + blast radius + owner-only RBAC AD-22 + dry-run mode default + steady state verification + circuit breaker + audit-first INSERT 4 NEW `chaos_experiment_started` + `chaos_experiment_completed` + `chaos_experiment_aborted` + `chaos_rollback_triggered` CR 1-1 verbatim + ActionClass.CHAOS_ENGINEERING 신규 정의 + apps/api/core/audit_action.py MODIFIED AuditAction Literal EXTENSION 4 NEW values + _ActionRegistry CHAOS_ENGINEERING entry 신규 4개 등록 + __all__ EXTENSION) / (7) **Tests + wire scope T1~T8** 결정 (~+30 NEW pytest PASS (chaos_experiment 5 + fault_injection 4 + chaos_game_day 4 + continuous_chaos 4 + alembic 0041 3 + audit action 6 + capability matrix v1.34 6 = ~+30 NEW pytest PASS) + ~+5 NEW vitest PASS (chaos dashboard 3 + SSOT drift 2 = ~+5 NEW vitest PASS) + 0 NEW ruff + 0 regressions + T1 chaos_experiment module + T2 chaos_game_day job + T3 continuous_chaos job + T4 alembic 0041 phase_9_chaos_engineering + T5 audit action EXTENSION 4 NEW + T6 capability v1.34 + T7 frontend chaos dashboard + T8 3중 게이트 FINAL CLEAN atomic commit). 7 ACs ALL PASS 진입 시점에 Phase 9 wire DONE 결정 wire 보존 (cj-style 99번째 epic 연속 정직 회복 wire 진입 시점). capability gate CHAOS_ENGINEERING (capability matrix v1.34, industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러, PERFORMANCE_TESTING Phase 8 wire + OBSERVABILITY_TRACES + OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind). D-1-1-DEFER-1/2/3 ✅ RESOLVED 보존 97번째. D-LAUNCH-1-DEFER-1 honestly preserved 65~97번째. D-EPIC-16-REVIEW-DEFER-2~6 ✅ RESOLVED 보존 78~97번째. D-PHASE-4-DR-DEFER-1/2 ✅ RESOLVED 보존 73~97번째. D-EPIC-17-WIRE-DEFER-T2-T3-UI ✅ RESOLVED 보존 83~97번째. D-RETENTION-1 ✅ RESOLVED 보존 85~97번째. D-OBSERVABILITY-1 ✅ RESOLVED 보존 89~97번째. D-PERFORMANCE-1 ✅ RESOLVED 보존 93~97번째 (Phase 8 PRD entry 진입 시점에 정직 회복 결정 wire 완료). **D-CHAOS-1 신규 honestly DEFER 보존 97번째** (Phase 7 close-out retro §10 + Phase 8 close-out retro territory "Chaos Engineering / Game Day 결정 wire 보류, Phase 9+ 진입 시점" verbatim 해소 + Phase 9 PRD entry 진입 시점에 정직 회복 결정 wire 완료, CR 11-3 honest-DEFER discipline 97번째 epic 연속 정직 회복 검증).
  - **(s) Phase 10 신규 — SLO Engineering / Error Budget Management territory (옵션 (a) Phase 10+ 진입 결정 wire, A303 결정)** — 시스템은 Phase 10 = SLO Engineering / Error Budget Management (SLO definition DSL + multi-window burn-rate evaluation + error budget tracker + multi-region SLO aggregation + tenant-scoped SLO override + SLO governance review + auto-rollback SLO breach trigger + dry-run mode + tests + wire scope T1~T8) 결정 wire 진입 시점에 다음 7 ACs ALL PASS 결정 wire [§F26, AD-37]: (1) **SLO definition DSL** 결정 wire (`apps/api/modules/slo/slo_dsl.py` NEW ~+180 LOC + `SloDefinition` TypedDict 13 fields (slo_id TEXT PRIMARY KEY + tenant_id UUID + service TEXT enum cost_engine/signups/logins/audit_purge + slo_type TEXT enum availability/latency/throughput + target REAL + window_seconds INTEGER default 2592000 (30d) + burn_rate_threshold_1h REAL default 14.4 + burn_rate_threshold_6h REAL default 6.0 + alerting_policy TEXT + owner_user_id UUID + created_at + updated_at + trace_id) + `parse_slo_definition(tenant_id, payload) -> SloDefinition` pure validator + CR 11-4 P-015 verbatim 적용 + 1 NEW error class `SloDefinitionInvalidError(400)` CR 12-5 D-14 envelope `{code, message_ko, details, trace_id}` 결정 wire) / (2) **multi-window burn-rate evaluation** 결정 wire (Google SRE Workbook "multi-window, multi-burn-rate criteria" 패턴 verbatim 적용 + `apps/api/jobs/slo_burn_rate_evaluator.py` NEW ~+200 LOC + cron KST 매 5분 UTC 매 5분 + 2 burn-rate windows: (a) fast burn 1h window + 5m alert threshold (1h burn rate > 14.4x ⇒ 2% budget in 1h, paging 즉시) + (b) slow burn 6h window + 30m alert threshold (6h burn rate > 6x ⇒ 5% budget in 6h, ticket 발행) + 2 budget exhaustion windows: (c) 24h window + 2h alert threshold (24h burn rate > 3x ⇒ 10% budget in 24h, ticket 발행) + (d) 3d window + 6h alert threshold (3d burn rate > 1x ⇒ 25% budget in 3d, governance review 트리거) + `evaluate_burn_rate(slo_id, window_seconds) -> BurnRateResult` TypedDict 5 fields (slo_id + window_seconds + burn_rate_factor + budget_consumed_pct + alert_required BOOL) + Phase 8 wire `60d4ea1` 의 SLO/SLI 정의 4 metrics 자연스러운 carry-over chain (cost_engine p99 < 5s + signups success_rate > 99% + logins p99 < 1s + audit_log_purge success_rate > 99.9%) 결정 wire) / (3) **error budget tracker** 결정 wire (`apps/api/modules/slo/error_budget.py` NEW ~+150 LOC + `ErrorBudget` TypedDict 8 fields (slo_id + budget_total_seconds REAL + budget_consumed_seconds REAL + budget_remaining_pct REAL + reset_at TIMESTAMPTZ + freeze_until TIMESTAMPTZ nullable + governance_required BOOL + trace_id) + `consume_error_budget(slo_id, incident_duration_seconds) -> ErrorBudget` + atomic UPDATE `error_budget_tracker` row + multi-tenant isolation CR 0-2 RLS lesson + freeze mechanism `freeze_slo_budget(slo_id, duration_seconds)` governance 트리거 시 budget consumption 일시 정지 결정 wire + 1 NEW error class `ErrorBudgetExhaustedError(429)` CR 12-5 D-14 envelope 결정 wire + `business_slo_budget_remaining_pct{slo_id,service}` Gauge Prometheus metric Phase 7 wire `59b56cd` EXTENSION 결정 wire) / (4) **multi-region SLO aggregation** 결정 wire (Phase 5 wire `f093f8c` multi-region failover carry-over chain + `apps/api/modules/slo/multi_region_aggregator.py` NEW ~+180 LOC + `MultiRegionSloAggregate` TypedDict 7 fields (slo_id + region enum seoul/tokyo/singapore + aggregate_target REAL + aggregate_actual REAL + weighted_budget_remaining_pct REAL + region_weight_map JSONB + trace_id) + `aggregate_slo_across_regions(slo_id) -> MultiRegionSloAggregate` + `region_weight_map` default `{seoul: 0.6, tokyo: 0.3, singapore: 0.1}` 결정 wire + cross-region replication lag weighted adjustment (replication_lag_seconds > 30s ⇒ seoul weight 0.5 / tokyo 0.3 / singapore 0.2) 결정 wire + Phase 5 wire `f093f8c` `phase_5_replication_lag` table 정합 + Phase 9 wire `e7670e1` 의 chaos_experiment multi-region 정합 보존 + audit-first INSERT `slo_aggregate_evaluated` CR 1-1 verbatim 결정 wire) / (5) **tenant-scoped SLO override** 결정 wire (CR 0-2 RLS lesson + `apps/api/modules/slo/tenant_scoping.py` NEW ~+120 LOC + `TenantSloOverride` TypedDict 6 fields (override_id + tenant_id + slo_id + target_override REAL + window_seconds_override INTEGER + trace_id) + `apply_tenant_slo_override(slo_id, tenant_id) -> SloDefinition` + per-tenant target override (e.g. enterprise tenant 99.95% > default 99% SLO) + per-tenant window override (e.g. tenant 14d rolling window vs default 30d) + RLS policy `tenant_id = current_setting('app.tenant_id')::uuid` alembic 0042 결정 wire + multi-tenant isolation test 결정 wire + Phase 9 wire `e7670e1` 의 chaos_experiment 의 L2 single_tenant blast radius 정합 보존 결정 wire) / (6) **SLO governance review + auto-rollback SLO breach trigger** 결정 wire (governance review entry 4 conditions: budget_consumed_pct > 75% for 7d OR burn_rate_3d > 1x sustained OR freeze_until expired + budget_negative OR error_budget exhausted < 24h to reset + audit-first INSERT `slo_governance_review_initiated` CR 1-1 verbatim + owner-only RBAC AD-22 + Epic 12 2FA 챌린지 보존 + `apps/api/modules/slo/governance.py` NEW ~+150 LOC + `initiate_slo_governance_review(slo_id) -> GovernanceReview` TypedDict 7 fields + Phase 9 wire `e7670e1` 의 chaos_experiment 의 owner-only RBAC + 2FA 챌린지 정합 보존 + audit-first INSERT 3 NEW `slo_target_updated` + `slo_budget_exhausted` + `slo_violation_detected` CR 1-1 verbatim + ActionClass.SLO_ENGINEERING 신규 정의 + apps/api/core/audit_action.py MODIFIED AuditAction Literal EXTENSION 3 NEW values + _ActionRegistry SLO_ENGINEERING entry 신규 3개 등록 + __all__ EXTENSION + auto-rollback SLO breach trigger 결정 wire: `business_cost_engine_p99_breach` OR `business_signups_success_rate_breach` 1m sustained ⇒ Phase 9 wire `e7670e1` `chaos_experiment_aborted` + `chaos_rollback_triggered` 자동 fire 결정 wire) / (7) **Capability matrix v1.35 EXTENSION + 1 NEW row + dry-run + Tests + wire scope T1~T8** 결정 wire (`Capability.SLO_ENGINEERING = "slo_engineering"` 1 NEW enum 추가 (manufacturing ✅ + service ✅ + manufacturing_service ✅ + manufacturing_service_other ✅ industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러, CHAOS_ENGINEERING Phase 9 wire + PERFORMANCE_TESTING Phase 8 wire + OBSERVABILITY_TRACES + OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind) + 미허용 tenant 의 SLO engineering 진입 차단 결정 wire + SSOT RED→GREEN EXTENSION (capability matrix v1.35 신규 1 row + capability.py EXTENSION 1 NEW enum + `require_capability()` Dependency 1개 신규 wire) + drift detector `tests/integration/test_capability_matrix_v1_35_drift.py` NEW 6 NEW pytest cases 결정 (Phase 9 wire 의 `tests/integration/test_capability_matrix_v1_34_drift.py` + Phase 8 wire 의 `tests/integration/test_capability_matrix_v1_33_drift.py` 패턴 verbatim) + dry-run mode 결정 wire (`--slo-engineering-dry-run` CLI flag + burn_rate_evaluator dry-run 모드 + governance review dry-run + auto-rollback trigger dry-run + dry_run=True default for prod deploys) + tests 결정 wire (~+30 NEW pytest PASS (slo_dsl 6 + slo_burn_rate_evaluator 6 + error_budget 5 + multi_region_aggregator 4 + tenant_scoping 4 + governance 4 + alembic 0042 3 + audit action 6 + capability matrix v1.35 6 + slo integration e2e 2 = ~+46 NEW pytest PASS) + ~+5 NEW vitest PASS (slo dashboard 3 + SSOT drift 2 = ~+5 NEW vitest PASS) + 0 NEW ruff + 0 regressions + T1 slo_dsl + slo_burn_rate_evaluator modules + T2 error_budget module + T3 multi_region_aggregator + tenant_scoping modules + T4 governance + auto-rollback SLO breach trigger + T5 alembic 0042 phase_10_slo_engineering + T6 audit action EXTENSION 3 NEW + T7 capability v1.35 + frontend slo dashboard + T8 3중 게이트 FINAL CLEAN atomic commit). 7 ACs ALL PASS 진입 시점에 Phase 10 wire DONE 결정 wire 보존 (cj-style 103번째 epic 연속 정직 회복 wire 진입 시점). D-1-1-DEFER-1/2/3 ✅ RESOLVED 보존 101번째. D-LAUNCH-1-DEFER-1 honestly preserved 65~101번째. D-EPIC-16-REVIEW-DEFER-2~6 ✅ RESOLVED 보존 78~101번째. D-PHASE-4-DR-DEFER-1/2 ✅ RESOLVED 보존 73~101번째. D-EPIC-17-WIRE-DEFER-T2-T3-UI ✅ RESOLVED 보존 83~101번째. D-RETENTION-1 ✅ RESOLVED 보존 85~101번째. D-OBSERVABILITY-1 ✅ RESOLVED 보존 89~101번째. D-PERFORMANCE-1 ✅ RESOLVED 보존 93~101번째. D-CHAOS-1 ✅ RESOLVED 보존 97~101번째 (Phase 9 PRD entry 진입 시점에 정직 회복 결정 wire 완료). **D-SLO-1 신규 honestly DEFER 보존 101번째** (Phase 8 close-out retro + Phase 9 close-out retro territory "SLO Engineering / Error Budget Management 결정 wire 보류, Phase 10+ 진입 시점" verbatim 해소 + Phase 10 PRD entry 진입 시점에 정직 회복 결정 wire 완료, CR 11-3 honest-DEFER discipline 101번째 epic 연속 정직 회복 검증).

- **M1 (기준정보)**
  - (a) 시스템은 BOM 매트릭스에서 비중 합 != 100% 상태로 [계산] 진입을 차단한다 [A6].
  - (b) 시스템은 품목 유형을 변경(예: 제품 → 서비스)할 때 BOM·수불 참조가 0건임을 검증한 후에만 허용한다.

- **M2 (월 데이터 입력)**
  - (a) 시스템은 월합계 기본 모드에서 일자별 그리드 필드를 비노출로 둔다(E4).
  - (b) 시스템은 일용직 FTE 환산을 입력 완료 시 자동 계산하여 "환산 인원·환산 임금"을 표시한다.
  - (c) 시스템은 음수재고·조업도 초과 발생 시 입력 완료 즉시 경고하고, 마감 시 차단한다 [A11, V3·V5].

- **M3 (원가계산 엔진)**
  - (a) 시스템은 [계산] 클릭 시 §6.1 산식 체인 전체를 단일 트랜잭션으로 실행하고, 도중 실패 시 전체 롤백한다 [A6].
  - (b) 시스템은 계산 완료 시 §11 V1·V4·V7·V8을 자동 발동하여 위반이 1건이라도 있으면 결과를 "검증 실패" 상태로 잠근다.

- **M4 (재고 수불)**
  - (a) 시스템은 기초재고 입력 후 자동 이월 체인을 개시하고, 이후 수동 입력은 차단한다.
  - (b) 시스템은 음수 기말을 감지 즉시 경고하고, 사용자 확인 없이 마감 진입을 차단한다 [V3].

- **M5 (손익·보고서)**
  - (a) 시스템은 §9 21종 보고서 각각에 대해 "종합 / 제품별 / 판매지역별" 뷰 토글을 제공한다.
  - (b) 시스템은 KRW/USD 동시 표시 시 환율을 표시하고 USD 소수 2자리를 강제한다.
  - (c) 시스템은 보고서 PDF 내보내기 시 A4 인쇄 최적화 페이지 크기를 적용한다.

- **M6 (자동 검증)**
  - (a) 시스템은 §11 V1~V8을 마감 진입 시점과 계산 시점 두 곳에서 자동 발동한다.
  - (b) 시스템은 V8 회귀 테스트 스위트 실패 시 CI 빌드를 차단한다.

- **M7 (시뮬레이션)**
  - (a) 시스템은 슬라이더 변경 시 BEP 수량·목표 이익을 1초 이내로 재계산한다(§14 성능 임계).
  - (b) 시스템은 차월 추정 시 차입금·이자율·상승률·세율 4종 파라미터의 사용자 입력을 강제한다.

- **M8 (예산 시나리오)**
  - (a) 시스템은 1차에서 시나리오 1개만 허용하고, 2개 이상 생성 시도를 차단한다(2차에서 해제).
  - (b) 시스템은 예산 실적 대조 시 모든 차이 행을 표시하고, A×B×C×D 편성 엔진이 미구현이면 회색 배지로 "2차 예정"을 표시한다.

- **M9 (ABC 엔진)**
  - (a) 시스템은 원가풀 행 합 != 100%, 활동 열 합 != 100%, 동인 합 != 100% 상태로 [계산]을 차단한다 [V7].
  - (b) 시스템은 TDABC CCR 산출 시 부서 원가 ÷ 실제적 조업능력을 1원 단위로 계산하고, 미사용능력 금액을 별도 표시한다 [A9].

- **M10 (AI 지원)**
  - (a) 시스템은 인사이트 질문 3개 생성·캐시 시 "캐시 = 마감 완료 시점부터 다음 마감 시작까지 보존, 마감 데이터 변경 시 폐기" 정책을 강제한다 (검수 medium 해소).
  - (b) 시스템은 계산 결과를 변경하지 않으며(SM-3a 검증), AI 의견은 "자동 분석(고정 템플릿)"과 "AI 참고(구분 배지)"로 분리 표시한다.
  - **(c) 10-1 (AI 문서추출 → 입력 초안)** — 시스템은 PDF/Excel 업로드 시 6 monthly input fields (직접재료비/직접노무비/제조간접비/판매관리비/매출/기말재고) 를 추출하고, `extraction_confidence < 0.70` 인 항목은 빨강 배지(RED) 로 표시하며 사용자 확정을 강제한다 [A11, V-row §8.1 M0-c]. AI 출력은 `input_drafts.target_table='monthly_inputs'` 에만 저장하며 `confirmed_inputs` 직접 쓰기는 거부 + 카운터 증가 (target 0) [AD-7].
  - **(d) 10-2 (인사이트 캐시 정책)** — 시스템은 인사이트 캐시 키 `(tenant_id, period_key, calculation_result_hash)` 로 저장하며 마감 데이터 변경 시 4-channel publisher (`ai_cache` 외 3 channel `cost_engine_cache`/`fiscal_period_cache`/`closing_snapshot_cache` 는 Epic 13 LISTEN/NOTIFY consume trigger EXTENSION wire 진입 시점에 4-channel cache eviction publisher 로 무효화한다 [AD-25, §F13 신규]. **EXTENSION (Epic 14 PRD entry DONE 2026-08-20, A57+A58+A59)** — **5+ channels EXTENSION 결정 wire**: 4-channel 외 `cross_tenant_fanout` 1 channel 추가. Cross-tenant invalidation fan-out = tenant-level subscription routing + multi-tenant isolation 검증 (CR 0-2 RLS lesson 적용 + AD-22 verbatim 보존). NOTIFY payload 7-key alphabetical EXTENSION (source_tenant_id + target_tenant_ids 추가). **Multi-process coordination Option 1 결정** (PostgreSQL LISTEN/NOTIFY only via pg_notify fan-out leader/follower model). capability matrix v1.23 EXTENSION (`LISTEN_NOTIFY_TENANT_FANOUT` + `LISTEN_NOTIFY_MULTIPROCESS` 2 NEW rows industry-agnostic 4-industry grants ✅/✅/✅/✅) [§F14 verbatim, §F10.1-(d) EXTENSION 결정].
  - **(e) 10-3 (자동 분석 vs AI 참고 배지 분리)** — 시스템은 `source_kind: Literal['auto_analysis', 'ai_reference']` Discriminated union 으로 분리 렌더링하고 (auto_analysis 파란 배지 "📊 자동 분석" + ai_reference 보라 배지 "🤖 AI 참고(검증 필요)" + tooltip "AI는 비권위적입니다 — 확정 책임은 사용자에게"), strict reject 외 value counter increment 강제 [AD-7 SM-3a].
  - **(f) 10-4 (승격 포트 멱등성)** — 시스템은 `InputPromoter.promote(tenant_id, period_key, source_draft_id)` 호출을 idempotent 로 처리하며, audit_logs 2행 append (actor + draft hash + ts) [AD-17]. M10 attempts to write `confirmed_inputs` 는 denied + 카운터 증가 (target 0) [AD-7].

- **M11 (마감·이력)**
  - (a) 시스템은 부문분할 → 제조 → ABC → 공동 순서를 강제하고, 부분 마감을 허용하지 않는다.
  - (b) 시스템은 마감 완료 시 계산 결과 전체를 스냅샷으로 고정하고, 이후 입력·변경 시도는 역분개(A8)로만 허용한다.

- **M12 (계정·운영)**
  - (a) 시스템은 2FA 미설정 상태에서 M2 진입을 차단한다.
  - (b) 시스템은 일 1회 자동 백업 + 셀프 다운로드(JSON) 기능을 제공한다.
  - (c) 시스템은 해지 요청 시 보관일수 고지 + 삭제 동의 문구를 강제 표시한다.

---

# 9. 보고서 체계 (통합 목록)

**전통 엔진 (원본 26종 계보의 웹 재편)**
1. 대시보드 — 월 체크리스트, TOP5/WORST5 제품, 12개월 추이
2. 종합 손익계산서 (당월 + 차월 추정)
3. 제품별 손익계산서 (제조원가/완전원가 2계층 표시 [A2])
4. 판매지역별 손익계산서 (지역 입력 시)
5. 월간 비교 손익 + YTD (12개월, 마감월만)
6. 예산 대비 실적 손익 (M8 연동)
7. 원가분석표 (재료·노무·경비 3단 분해 + 자동 분석의견(고정 템플릿) + AI 참고 의견(구분 표시))
8. 제품 원가카드 (원본 ctable — 재료 명세·공정·경비 단위당)
9. 표준 제조원가명세서 (원본 cststmt — 재료비 T계정 흐름 포함)
10. 손익분기점(BEP) 분석 + 목표이익 역산
11. 조업도·생산성 보고서 (임률 체인 + 미사용능력 금액화 [A9])
12. 원부재료/제품 재고 수불부 + 재고 검색
13. 주문잔량(Back Log) / 주문품·생산품 자재 총소요 (주문 입력 시)
14. 검증 리포트 (제11장 결과 상시 열람)

**ABC 엔진 7종 (원본 8~10 계보)**
15. 활동원가 내역서 (활동별 원가·동인 단가)
16. 원가대상 수익성 보고서 (제품별 활동원가 손익 + 자동 분석의견)
17. 단위 활동원가표 (제품 단위당 활동비용 분해)
18. 미사용능력 보고서 (TDABC — 전통 조업도와 통합 뷰)
19. 활동원가 추이 보고서
20. 전통 vs ABC 비교 보고서 (겸영 기업 전사 뷰)
21. 부문귀속명세서 (카브아웃 근거 공시) → §7.3 [AS-6]

**공통 규격**: 다년 조회·전년 비교 / 한·영 + KRW·USD / 음수 (1,234) 빨강 / KRW 정수·USD 소수 2자리 / A4 인쇄 최적화 + PDF 내보내기 / 격식체 서술

---

# 10. 예산 시나리오 (결정 Q-D)

- 원본의 13번째 저장공간 '예산'을 **가상 기간(budget scenario)**으로 승격: 실적과 동일한 입력 체계로 계획치를 입력하면 **사전 표준원가계산**이 수행되고, 예산 대비 실적 손익(원본 pl5)이 자동 대조된다.
- 회계연도당 예산 시나리오 1개(2차에서 복수 시나리오 검토).
- A×B×C×D 예산 편성 엔진(편성단위 × 기준비율 × 단위금액 × 기간환산 — 원본 djob exp·ABCost 3 Indirect cost 공통 구조)은 **2차 로드맵**. 산식 원문은 본 문서와 ERD에 보존한다.

---

# 11. 자동 검증 체계 (원본 check 시트의 시스템 승격)

| # | 검증 | 내용 | 시점 |
|---|------|------|------|
| V1 | 완전배부 | 각 배부 단계 합계 = 원금액 (1원 단위) [A6] | 계산 시 |
| V2 | 수불 연속성 | 기초+입고−출고 = 기말, 월 체인 연속(전월 기말=당월 기초) | 상시 |
| V3 | 음수재고 | 품목별 기말 < 0 감지 즉시 경고 | 입력 시 |
| V4 | 원가-손익 Reconciliation | 제조원가↔매출원가↔재고 차이를 **4요소 자동 분해**: ①생산·매출 수량차 재료비 ②노무비+제조경비 배분차 ③총평균단가차 ④재고조정 → '제품 재고 조정' 라인 산출 근거 | 계산 시 |
| V5 | 조업도 | 생산요구시간 > 총작업가능시간 경고 | 입력 시 |
| V6 | 합계 대사 | 제품별 매출 합 vs 지역별 매출 합 불일치 경고 (지역 선택 입력이므로 경고만) | 입력 시 |
| V7 | ABC 무결성 | 원가풀 행 합 100%·활동 열 합 100%·동인 합계·완전배부 | 계산 시 |
| V8 | 엔진 대조 | 원가엔진(순수 Python) 결과를 원본 엑셀 산출과 1원 단위 대조하는 회귀 테스트 스위트 | CI |

운영 원칙: 입력 시 경고(진행 허용) → 마감 시 임계 위반 차단 [A11]. 조정 차이는 원본 권고대로 "무시하거나 연말 1회 조정" 옵션 제공.

---

# 12. AI 기능 3종 (첫 버전 확정분)

| 기능 | 내용 | 원칙 |
|------|------|------|
| 문서추출 온보딩 | 사업자등록증·재무제표·급여대장·거래명세서·활동 초안·동인 실적을 Claude Vision으로 초안 생성 | "AI는 초안, 확정은 사람" — 확정 전 검토 강제, 추출 항목별 신뢰도 표시. AI 출력은 `input_drafts.target_table='monthly_inputs'` 에만 저장하며 `confirmed_inputs` 직접 쓰기는 거부 + 카운터 증가 (target 0) [AD-7]. Story 10.1 wire (PRD §8.1 M10-(c)) |
| 인사이트 큐레이션 | 마감 데이터 기반 질문 3개 자동 생성·캐시(예: "부대찌개 원가율이 전월 대비 상승한 이유는?") + 답변 | 계산 결과를 변경하지 않음. 서술형 의견은 '자동 분석(고정 템플릿)'과 'AI 참고(구분 배지)'를 분리 [§F10.2]. 캐시 키 `(tenant_id, period_key, calculation_result_hash)` 3-tuple lock + 마감 데이터 변경 시 4-channel publisher 무효화 [AD-25]. Story 10.2 wire (PRD §8.1 M10-(d)). SM-3a "계산 결과 변경 시도 = 0건" 별도 추적 |
| 고정·변동 추정 | 계정별 고정/변동 3단계 추정(과거 패턴 → 제안 → 사용자 확정) | 확정값만 계산에 사용 [A7]. 본 1차 PRD 범위는 본 PRD v2.0 시점에 Epic 10 4-story (10-1~10-4) wire 진입으로 §12 3종 중 문서추출 + 인사이트 큐레이션 2종 wire, 고정·변동 추정은 1차 PRD 범위 외 (후속 Epic) |

---

## F10. AI 기능 명세 (Epic 10 wire 정합, 2026-08-20 master PRD v2.0 edit)

> 본 절은 §12 AI 기능 3종 가운데 **문서추출 (Story 10.1) + 인사이트 큐레이션 (Story 10.2) + 배지 분리 (Story 10.3) + 승격 포트 (Story 10.4)** wire 의 PRD-level AC 를 상세화한다. Epic 10 PRD entry (`_bmad-output/planning-artifacts/prds/prd-costmgr-2026-08-17/prd.md`) §3 verbatim 보존.

### F10.1 Three-Insight Cache Policy (§8.1 M10-(a)(d) 확장)

- **(a)** 시스템은 `fiscal_period_snapshots.state='committed'` 전이 시점에 `ai_cache` 키 `(tenant_id, period_key, calculation_result_hash)` 3-tuple 로 **인사이트 질문 3개 + 답변 3개**를 lock 한다 (NFR11 P95 ≤ 30s, [AD-25 verbatim]).
- **(b)** 시스템은 cache hit 시 마지막 마감 완료 시점부터 다음 마감 시작 시점까지 보존된 인사이트를 반환하고, 동일 hit 은 0~수십 ms 내 응답한다 (cache 없으면 NFR11 SLO 내 cold compute).
- **(c)** 시스템은 마감 데이터 변경 (Epic 11 AD-22 reversal INSERT) 시 AD-25 publisher 가 invalidation log 를 emit 하면 adapter 가 `WHERE tenant_id=? AND period_key=?` 매칭 cache entry 를 즉시 폐기한다.
- **(d)** 시스템은 `cache_invalidation_log` 채널에 `ai_cache` 외 채널 (`cost_engine_cache` / `fiscal_period_cache` / `closing_snapshot_cache`) 이 추가되어도 본 캐시만 영향받지 않도록 channel-specific invalidation filter 를 강제한다 (`channel = 'ai_cache'` filter). **Epic 13 LISTEN/NOTIFY consume trigger EXTENSION wire 진입 시점에 4-channel publisher EXTENSION (A51 결정, §F13 신규)**. D-10-2-DEFER-3 ✅ RESOLVED 진입. **EXTENSION (Epic 14 PRD entry DONE 2026-08-20, A57+A58+A59)** — **5+ channels EXTENSION 결정 wire**: 4-channel 외 `cross_tenant_fanout` 1 channel 추가. Cross-tenant fan-out channel 추가되어도 본 캐시(`ai_cache`) 는 영향받지 않도록 channel-specific invalidation filter EXTENSION 강제 (`channel = 'ai_cache'` filter 그대로 보존 + cross_tenant_fanout filter 별도 강제 — F14.1-(c) cross-channel contamination 방어 결정 wire verbatim bind). [§F14 verbatim, AD-25 EXTENSION 결정].

### F10.2 AI Reference vs Auto Analysis Badge Separation (§8.1 M10-(b)(e) 확장)

- **(a)** 시스템은 보고서 의견 section 진입 시 모든 문장별 `source_kind` (`auto_analysis` | `ai_reference`) 를 함께 렌더링하고:
  - `source_kind='auto_analysis'` → 파란 배지 "📊 자동 분석" + tooltip "이 의견은 고정 템플릿입니다" (§12 verbatim).
  - `source_kind='ai_reference'` → 보라 배지 "🤖 AI 참고(검증 필요)" + tooltip "AI는 비권위적입니다 — 확정 책임은 사용자에게" ([AD-7 verbatim]).
- **(b)** 시스템은 `auto_analysis` / `ai_reference` 키 외 value (예: `human_authored` 등) 도착 시 strict reject + 1행 counter increment 를 wire 한다 (§A11 시스템은 틀리지 않는다 / hover 후 미변경 = 안전).
- **(c)** 시스템은 SM-3a "계산 결과 변경 시도 = 0건" 별도 tracking 을 위해 `auto_analysis` 의견 수정 시도도 동일 카운터로 추적한다 (AD-7 "M10 attempts to write confirmed-input tables are denied and counted (target zero)").
- **(d)** 시스템은 `source_kind` 강제 검증 실패 시 1-line ko-KR 메시지로 reject (예: "분석 의견 출처가 불분명합니다") + counter 증가 + 200 OK envelope.

---

## F13. LISTEN/NOTIFY Consume Trigger EXTENSION (Epic 13 wire 정합, 2026-08-20 master PRD v2.3 edit — 13-1 atomic T1~T8 wire DONE)

> 본 절은 **A39 결정 wire (Epic 13 = LISTEN/NOTIFY 전용 epic)** + **A51 Epic 13 PRD entry 결정 wire** + **A52 Story 13-1 atomic wire DONE (cj-style 42번째 epic 연속 정직 회복)** + **A54 master PRD v2.3 edit (D-13-1-DEFER-1 ✅ RESOLVE)** 의 PRD-level AC 를 verbatim 상세화한다. AD-25 cache invalidation trigger EXTENSION for close/reopen 의 wire 진입 근거 + 4-channel publisher EXTENSION 진입 결정 + 13-1 atomic T1~T8 wire 정합 결정. Story 13-1 = NOTIFY trigger (alembic 0033) + LISTEN daemon (FastAPI lifespan) + 4-channel cache eviction handlers (M10/M3/M11 EXTENSION) + reconnect/backoff (exponential + jitter + circuit breaker) + V8 determinism + cross-lang drift detector EXTENSION + capability `LISTEN_NOTIFY` v1.22 4-industry grants industry-agnostic.

### F13.1 LISTEN/NOTIFY 토폴로지 (§AD-25 EXTENSION)

- **(a)** 시스템은 PostgreSQL `LISTEN/NOTIFY` 채널을 통해 cache invalidation 을 trigger 한다. `cache_invalidation_log` 테이블 AFTER INSERT 트리거 `cache_invalidation_log_notify_trg` 가 PL/pgSQL function `cache_invalidation_log_notify()` 호출하여 `pg_notify('cache_invalidation_log', payload)` 를 emit 한다. payload = JSON `{channel: str, correction_group_id: str, invalidation_id: str, period_key: str, tenant_id: str, trace_id: str}` 6-key alphabetical key ordering 결정적 직렬화 (uuid fields → TEXT cast for cross-language drift detector parity [CR 12-5 D-PARITY-01]). channel whitelist in DDL: `ai_cache | cost_engine_cache | fiscal_period_cache | closing_snapshot_cache`. M10/M3/M11 adapter 가 LISTEN daemon (`CacheInvalidationListener`) 을 통해 4 channel 모두를 consume 한다 [AD-25 verbatim EXTENSION, §F13 신규].
- **(b)** 시스템은 4 channel 모두 (`ai_cache` / `cost_engine_cache` / `fiscal_period_cache` / `closing_snapshot_cache`) 에 대해 channel-specific eviction handler 를 wire 하며, 각 handler 는 매칭 cache entry 를 즉시 폐기한다. NOTIFY payload 는 channel 명을 명시적으로 포함하여 cross-channel leakage 를 차단한다 (channel-specific filter 강제 — cross-channel contamination 방어 결정, SD-EPIC13-3 정직 반영).
- **(c)** 시스템은 LISTEN daemon `CacheInvalidationListener` (`apps/api/core/cache_invalidation_listener.py`) 가 FastAPI lifespan context manager 안에서 start/stop 되도록 wire 한다 (`apps/api/main.py` `@asynccontextmanager` lifespan 진입 시 start, shutdown 시 stop). Daemon 은 asyncio task + reconnect/backoff (exponential base 1s factor 2 + jitter ±20% + max 30s + circuit breaker 5 consecutive failures → 60s cool-down) + persistent failure 시 graceful degradation (다음 restart 시 reconnect) + stdlib-only pure async kernel (AD-5 engine purity 정합).
- **(d)** 시스템은 NOTIFY trigger 가 application polling 으로 대체되지 않도록 강제한다. Polling-only invalidation 은 forbidden. AD-25 verbatim "Application polling + input-write-only invalidation forbidden" 보존.

### F13.2 4-Channel Cache Eviction Handlers (§M10/M3/M11 EXTENSION, 13-1 T4 atomic wire DONE)

- **(a)** M10 AI cache eviction — `apps/api/modules/m10_ai/service.py` EXTENSION (`M10AIInvalidationAdapter.on_invalidate(payload)` + DELETE FROM `ai_insight_cache` WHERE `tenant_id=?` AND `period_key=?` 매칭 entry 즉시 폐기). channel-specific filter: `channel = 'ai_cache'` ONLY (F10.1-(d) verbatim cross-channel contamination 방어).
- **(b)** M3 cost engine cache eviction — `packages/cost_engine/...` EXTENSION (`M3CostEngineInvalidationAdapter.on_invalidate(payload)` + in-process LRU eviction hook, 단위: tenant_id × period_key tuple). channel: `cost_engine_cache`. AD-5 stdlib-only 보존 (NO DB write from kernel).
- **(c)** M11 fiscal_period cache eviction — `apps/api/modules/m11_close/services/fiscal_period_service.py` EXTENSION (`M11FiscalPeriodInvalidationAdapter.on_invalidate(payload)` + `fiscal_periods` cache invalidate, state='committed' 시 in-memory cache evict). channel: `fiscal_period_cache`.
- **(d)** M11 closing_snapshot cache eviction — `apps/api/modules/m11_close/services/snapshot_service.py` EXTENSION (`M11ClosingSnapshotInvalidationAdapter.on_invalidate(payload)` + `fiscal_period_snapshots` cache evict, closing_snapshot hash mismatch 시 즉시 evict). channel: `closing_snapshot_cache`.
- **(e)** 4 adapter 모두 `apps/api/core/cache_invalidation_listener_adapters.py` NEW ~220 LOC `build_default_adapter_factories()` returns 4 channel → factory entries (lazy import defense-in-depth, graceful degradation if module unavailable). Cross-channel contamination 방어: each adapter rejects payloads from other channels (F10.1-(d) verbatim). Audit-first INSERT 2-row (CR 1.1 verbatim, payload = notify envelope).

### F13.3 V8 Determinism + Cross-Language Drift (§CR 12-5 EXTENSION, 13-1 T6/T7 wire DONE)

- **(a)** 시스템은 NOTIFY payload JSON serialization 이 결정적 (alphabetical key ordering) 임을 강제한다. V8 byte-identical determinism 검증 — payload bytes 가 동일 입력에 대해 동일하게 직렬화되어야 한다 (드롭/순서 차이로 인한 cache miss 방지). `serialize_payload_for_v8()` byte-identical deterministic (`json.dumps(payload, sort_keys=True, separators=(',', ':'))`). UUID fields cast to TEXT for cross-language drift detector parity. `tests/regression_v8/test_listen_notify_v8_determinism.py` NEW ~11 cases (alphabetical key ordering, no whitespace, compact separators, byte-identical across reruns).
- **(b)** 시스템은 LISTEN payload shape 가 Python (`apps/api/core/cache_invalidation_listener.py`) 와 TypeScript (`apps/web/lib/cache-invalidation-listener.ts` NEW ~150 LOC TS mirror, `CacheInvalidationPayload` Discriminated union) 양쪽에서 동일하게 파싱됨을 강제한다 [CR 12-5 D-PARITY-01 inversion 적용]. Drift 발생 시 drift detector test fail + 1-line ko-KR reject ("LISTEN/NOTIFY 페이로드 형식이 백엔드와 일치하지 않습니다", `DRIFT_DETECTED_REJECT_KO` constant). `tests/web/test_cache_invalidation_listener_parity.py` NEW ~14 cases.
- **(c)** 시스템은 capability gate `LISTEN_NOTIFY` (capability matrix v1.22, Epic 13 wire 진입) 를 통해 LISTEN daemon 시작/정지가 tenant 별로 on/off 가능하도록 wire 한다 [CR 12-5 D-GATE-01 inversion 적용]. `Capability.LISTEN_NOTIFY = "listen_notify"` 4-industry grants: manufacturing ✅ + service ✅ + manufacturing_service ✅ + manufacturing_service_other ✅ (industry-agnostic, CR 12-1 L4 precedent 미러, `AI_INSIGHT` 10-1 wire pattern). 미허용 tenant 의 listener 는 등록되지 않는다.

### F13.4 Tests + Wire Scope (cj-style Epic 13 1~4번째 진입점 모두 wire DONE)

- **T1 — alembic 0033 NEW** (PostgreSQL `pg_notify` trigger wire, `down_revision = '0032_ai_promotion_port'`): `apps/api/alembic/versions/0033_listen_notify_consume_trigger.py` NEW ~155 LOC. `cache_invalidation_log_notify()` PL/pgSQL function with `json_object()` 6-key alphabetical payload (channel, correction_group_id, invalidation_id, period_key, tenant_id, trace_id) + AFTER INSERT trigger `cache_invalidation_log_notify_trg` FOR EACH ROW + channel whitelist in DDL.
- **T2 — `apps/api/core/cache_invalidation_listener.py` NEW** (~620 LOC, stdlib-only pure async kernel): `CacheInvalidationListener` class (asyncio 기반) + `start()` / `stop()` lifecycle (idempotent) + `_consume_notifications()` private coroutine + 4-channel routing dispatch table + reconnect/backoff (exponential + jitter + circuit breaker) + `parse_payload()` validates 6 keys alphabetical + UUID + channel whitelist + `serialize_payload_for_v8()` byte-identical.
- **T3 — `apps/api/main.py` lifespan EXTENSION** (~100 LOC, FastAPI lifespan context manager 진입): 4 NEW functions (`_start_cache_invalidation_listener` + `_stop_cache_invalidation_listener` + 2 NEW exception handlers `ListenerStartFailedError` 503 + `ListenerStopFailedError` 503 with CR 12-5 D-14 envelope `{code, message_ko, details, trace_id}`). Preserved `_attach_tenant_listener` (backward compat) + Graceful degradation: ImportError caught → no crash in test env.
- **T4 — `apps/api/core/cache_invalidation_listener_adapters.py` NEW** (~220 LOC, 4-channel cache eviction handlers): `M10AIInvalidationAdapter` + `M3CostEngineInvalidationAdapter` + `M11FiscalPeriodInvalidationAdapter` + `M11ClosingSnapshotInvalidationAdapter` + `build_default_adapter_factories()` returns 4 channel → factory entries + cross-channel contamination 방어 + lazy import.
- **T5 — Capability gate `LISTEN_NOTIFY`**: `apps/api/core/capability.py` EXTENSION 1 NEW enum + 4-industry grants + capability matrix v1.22 + `require_capability(Capability.LISTEN_NOTIFY)` Dependency 신규 wire (CR 12-5 D-GATE-01 inversion 적용). 4-industry grants ✅/✅/✅/✅.
- **T6 — V8 determinism byte-identical test NEW**: `tests/regression_v8/test_listen_notify_v8_determinism.py` NEW ~11 cases + `tests/api/test_alembic_0033_listen_notify_consume_trigger.py` NEW ~14 cases.
- **T7 — Cross-language drift detector EXTENSION**: `apps/web/lib/cache-invalidation-listener.ts` NEW ~150 LOC TS mirror + `tests/web/test_cache_invalidation_listener_parity.py` NEW ~14 cases + drift detector 1-line ko-KR reject.
- **T8 — 3중 게이트 FINAL CLEAN + atomic commit**: sprint-status: 13-1 in-progress → done + handoff memory 신규 wire + `docs/listen-notify-consume-trigger-extension.md` NEW (~10 sections, 10-4 template format EXTENSION).
- **A19 cohesion pattern 8 surface 8/8 PASS** (kernel ✅ + port ✅ + db schema ✅ + service ✅ + handler ✅ + envelope ✅ + capability ✅ + audit ✅). Surface 1 (kernel) = T2 listener (AD-5 stdlib-only) / Surface 2 (port) = T2 LISTEN daemon → 4-channel adapter dispatch / Surface 3 (db schema) = T1 alembic 0033 NOTIFY trigger / Surface 4 (service) = T4 4-channel eviction handlers / Surface 5 (handler) = T3 main.py lifespan + 2 NEW exception handlers / Surface 6 (envelope) = T3 CR 12-5 D-14 envelope / Surface 7 (capability) = T5 LISTEN_NOTIFY gate / Surface 8 (audit) = T4 audit-first INSERT 2-row.
- **Tests wire 표**: ~107 NEW pytest PASS (across 7 test files) + 0 NEW ruff (8 auto-fixed via `ruff check --fix --unsafe-fixes`) + 0 regressions (existing tests: 474 passed, 88 skipped DB-backed). **wire_commit = `f2ea2f6`** (cj-style 42번째 epic 연속 정직 회복 atomic single sweep T1~T8, 17 files = 12 NEW + 5 MODIFIED).

---

## F14. LISTEN/NOTIFY Consume 2nd Batch EXTENSION (Epic 14 PRD entry 결정, 2026-08-20 master PRD v2.5 edit — 14-1 atomic wire 진입 대기 = cj-style Epic 14 2번째 진입점)

> 본 절은 **A53 결정 wire (Epic 14 = LISTEN/NOTIFY Consume 2nd Batch territory, D-13-1-DEFER-3 separate epic 진입 결정 wire)** + **A57 Epic 14 PRD entry 결정 wire (cj-style Epic 14 1번째 진입점 = cj-style 45번째 epic 연속 정직 회복)** 의 PRD-level AC 를 verbatim 상세화한다. D-13-1-DEFER-3 territory = cross-tenant invalidation fan-out (multi-tenant isolation 검증 + tenant-level subscription routing) + multi-process coordination (multi-worker 환경 listener process-per-pod, Redis pub/sub fan-out 또는 PostgreSQL LISTEN/NOTIFY multi-process coordination 결정). AD-25 cache invalidation trigger EXTENSION 4-channel → 5+ channels cross-tenant fan-out 결정 wire 진입. capability matrix v1.22 → v1.23 LISTEN_NOTIFY_TENANT_FANOUT + LISTEN_NOTIFY_MULTIPROCESS 2 NEW rows (industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러, AI_INSIGHT 10-1 + LISTEN_NOTIFY 13-1 wire pattern). Story 14-1 = cross-tenant invalidation fan-out wire + multi-process coordination 본체 wire (T1~T9 atomic single sprint). D-13-1-DEFER-2 preserved (LISTEN/NOTIFY 실측 evidence 정합 sweep = 14-1 wire 진입 시점에 동시 = A55 Epic 14 진입 시점에 동시 결정 wire).

### F14.1 Cross-Tenant Invalidation Fan-Out 토폴로지 (§AD-25 EXTENSION 5+ channels 결정 wire)

- **(a)** 시스템은 cross-tenant invalidation fan-out 시 **tenant isolation 검증** 을 강제한다. NOTIFY trigger (alembic 0033 EXTENSION) 의 PL/pgSQL function `cache_invalidation_log_notify()` 가 tenant_id 를 NOTIFY payload 에 포함하며, listener (LISTEN daemon EXTENSION) 가 tenant context 와 cross-tenant fan-out channel 매칭 시 RLS context + tenant_id filter 로 tenant-scoped subscription routing 을 수행한다. Multi-tenant isolation 위반 시 reject (CR 0-2 RLS lesson 적용, AD-22 verbatim).
- **(b)** 시스템은 **tenant-level subscription routing** 을 wire 한다. NOTIFY channel whitelist EXTENSION 결정 wire: `ai_cache` / `cost_engine_cache` / `fiscal_period_cache` / `closing_snapshot_cache` 외 `cross_tenant_fanout` 1 channel 추가 (총 5+ channels EXTENSION 결정). Cross-tenant fan-out channel 은 tenant_id 별 subscription set 관리하며, listener 가 fan-out 수신 시 subscription set 의 각 tenant listener 에게 in-process dispatch (NOTIFY payload = JSON `{channel: 'cross_tenant_fanout', source_tenant_id, target_tenant_ids: [...], correction_group_id, invalidation_id, period_key, trace_id}` 7-key alphabetical key ordering 결정적 직렬화, V8 determinism EXTENSION).
- **(c)** 시스템은 **fan-out dispatch 시 audit-first INSERT 2-row** 를 강제한다 (CR 1.1 verbatim). Source tenant 의 invalidation log 1 row + fan-out dispatch log 1 row (target_tenant_ids 명시), audit_logs `action_name='cross_tenant_fanout_dispatched'` 1 row 추가 (audit-first invariant 보존). Cross-tenant contamination 방어 (F10.1-(d) verbatim): each adapter rejects payloads from other channels (cross_tenant_fanout channel 의 adapter 는 다른 4 channel 의 payload reject + 그 역도 reject).
- **(d)** 시스템은 NOTIFY trigger 가 application polling 으로 대체되지 않도록 강제한다. Polling-only invalidation 은 forbidden (AD-25 verbatim "Application polling + input-write-only invalidation forbidden" 보존). Cross-tenant fan-out channel 도 동일 강제 적용 (polling-only cross-tenant dispatch forbidden).

### F14.2 Multi-Process Coordination (Multi-Worker LISTEN Daemon EXTENSION)

- **(a)** 시스템은 **multi-worker 환경 (Railway / Gunicorn / Uvicorn multi-pod) 에서 listener process-per-pod wire** 를 강제한다. 각 FastAPI worker process 가 독립된 LISTEN daemon (`CacheInvalidationListener`) 을 구동하며, 1 process 만 fan-out publisher 역할 (leader election via PostgreSQL advisory lock `pg_try_advisory_xact_lock(LISTEN_FANOUT_LOCK_ID)`, deterministic hash of pod_id). Leader 가 NOTIFY publish, follower 들이 LISTEN daemon consume. Single-process 환경에서는 leader = self, follower = none (graceful degradation).
- **(b)** 시스템은 **multi-process coordination 핸들링** 으로 **PostgreSQL `LISTEN/NOTIFY` multi-process coordination 결정 wire 진입** (Epic 14 A58 결정). Option 1: PostgreSQL LISTEN/NOTIFY 만 사용 (모든 process 가 LISTEN daemon 구동, fan-out publisher leader 가 NOTIFY publish, follower 들이 자동 consume) — 결정 (단순성 + AD-25 verbatim 보존). Option 2: Redis pub/sub fan-out 추가 (별도 인프라, CR 13 PRD entry 진입 시점에 rejected 결정 = rationale: G2 "새벽에 혼자 고칠 수 있는 시스템" 정합 — 인프라 최소화). 결정 wire = Option 1 (PostgreSQL LISTEN/NOTIFY only, multi-process coordination via pg_notify fan-out leader/follower model).
- **(c)** 시스템은 **process-per-pod state 동기화** 를 wire 한다. In-memory cache eviction 후 cross-process invalidation 필요 시 leader 가 NOTIFY `cross_tenant_fanout` channel 에 publish (F14.1-(b) verbatim), 모든 follower 의 LISTEN daemon 이 consume 후 in-process eviction 적용. Reconnect/backoff 보존 (F13.1-(c) verbatim exponential base 1s factor 2 + jitter ±20% + max 30s + circuit breaker 5 consecutive failures → 60s cool-down). Stdlib-only pure async kernel 보존 (AD-5 engine purity 정합).
- **(d)** 시스템은 **leader election + failover** 을 wire 한다. PostgreSQL advisory lock `pg_try_advisory_xact_lock(LISTEN_FANOUT_LOCK_ID)` 으로 leader 결정, leader process 종료 시 lock 자동 해제 → follower 중 1개가 leader 선출 (next leader = hash of pod_id order). Leader health check 30s interval (background task in each follower), leader unresponsive 90s → follower 강제 takeover via pg_try_advisory_lock (non-xact, plain lock 으로 승격).

### F14.3 V8 Determinism + Cross-Language Drift EXTENSION (§CR 12-5 + §F13.3 EXTENSION)

- **(a)** 시스템은 NOTIFY payload JSON serialization 이 결정적 (alphabetical key ordering) 임을 강제한다 (F13.3-(a) verbatim 보존 + EXTENSION). **cross-tenant fan-out payload 7-key alphabetical**: `channel`, `correction_group_id`, `invalidation_id`, `period_key`, `source_tenant_id`, `target_tenant_ids`, `trace_id` (target_tenant_ids 는 JSON array 결정적 직렬화 — PostgreSQL `jsonb` canonical form 또는 Python `json.dumps(sort_keys=True)`). `serialize_payload_for_v8()` byte-identical deterministic EXTENSION (alphabetical key ordering + no whitespace + compact separators). UUID fields cast to TEXT for cross-language drift detector parity (CR 12-5 D-PARITY-01 inversion 적용 보존).
- **(b)** 시스템은 LISTEN payload shape 가 Python (`apps/api/core/cache_invalidation_listener.py` EXTENSION) 와 TypeScript (`apps/web/lib/cache-invalidation-listener.ts` EXTENSION ~+80 LOC TS mirror EXTENSION) 양쪽에서 동일하게 파싱됨을 강제한다. `CacheInvalidationPayload` Discriminated union EXTENSION (`channel: Literal['ai_cache', 'cost_engine_cache', 'fiscal_period_cache', 'closing_snapshot_cache', 'cross_tenant_fanout']` + cross_tenant_fanout variant). Drift 발생 시 drift detector test fail + 1-line ko-KR reject ("크로스 테넌트 LISTEN/NOTIFY 페이로드 형식이 백엔드와 일치하지 않습니다", `CROSS_TENANT_DRIFT_DETECTED_REJECT_KO` constant NEW). `tests/web/test_cache_invalidation_listener_parity.py` EXTENSION ~+12 cases (cross_tenant_fanout payload shape + multi-tenant isolation + leader/follower state).
- **(c)** 시스템은 **capability gate `LISTEN_NOTIFY_TENANT_FANOUT`** (capability matrix v1.23, Epic 14 wire 진입) + **`LISTEN_NOTIFY_MULTIPROCESS`** (capability matrix v1.23, Epic 14 wire 진입) 를 통해 cross-tenant fan-out + multi-process coordination on/off 가능하도록 wire 한다 [CR 12-5 D-GATE-01 inversion 적용 보존]. `Capability.LISTEN_NOTIFY_TENANT_FANOUT = "listen_notify_tenant_fanout"` 4-industry grants: manufacturing ✅ + service ✅ + manufacturing_service ✅ + manufacturing_service_other ✅ (industry-agnostic, CR 12-1 L4 precedent 미러). `Capability.LISTEN_NOTIFY_MULTIPROCESS = "listen_notify_multiprocess"` 동일 industry-agnostic 4-industry grants ✅/✅/✅/✅. 미허용 tenant 의 cross-tenant fan-out channel listener 는 등록되지 않으며, multi-process coordination leader election 에서도 제외된다.

### F14.4 Tests + Wire Scope (cj-style Epic 14 1~3번째 진입점 결정 보존)

- **T1 — alembic 0034 NEW** (PostgreSQL `pg_notify` trigger wire EXTENSION for cross-tenant fan-out, `down_revision = '0033_listen_notify_consume_trigger'`): `apps/api/alembic/versions/0034_listen_notify_consume_cross_tenant_fanout.py` NEW ~140 LOC. `cache_invalidation_log_notify_cross_tenant()` PL/pgSQL function with `json_object()` 7-key alphabetical payload (channel='cross_tenant_fanout' + source_tenant_id + target_tenant_ids + correction_group_id + invalidation_id + period_key + trace_id) + AFTER INSERT trigger `cache_invalidation_log_notify_cross_tenant_trg` FOR EACH ROW (cross_tenant_fanout channel ONLY) + channel whitelist EXTENSION 결정 wire.
- **T2 — `apps/api/core/cache_invalidation_listener.py` EXTENSION** (multi-process coordination + cross-tenant fan-out wire): `CacheInvalidationListener` class EXTENSION ~+200 LOC (leader election via `pg_try_advisory_xact_lock` + follower health check 30s interval + leader takeover 90s timeout + cross_tenant_fanout channel routing EXTENSION) + `start()` / `stop()` lifecycle idempotent EXTENSION 보존 + `_consume_notifications()` private coroutine EXTENSION (5+ channels routing dispatch table: ai_cache + cost_engine_cache + fiscal_period_cache + closing_snapshot_cache + cross_tenant_fanout) + reconnect/backoff 보존 (F13.1-(c) verbatim) + `parse_payload()` validates 7 keys alphabetical EXTENSION + UUID + channel whitelist EXTENSION + `serialize_payload_for_v8()` byte-identical EXTENSION.
- **T3 — `apps/api/main.py` lifespan EXTENSION** (~+50 LOC, leader election wiring): 2 NEW functions (`_start_leader_election` + `_stop_leader_election`) + leader-election background task lifecycle EXTENSION + `LeaderElectionFailedError` 503 + `LeaderTakeoverFailedError` 503 NEW exception handlers 2개 (CR 12-5 D-14 envelope `{code, message_ko, details, trace_id}`). Preserved: 13-1 listener start/stop + 2 NEW exception handlers (CR 12-5 D-14 envelope) + graceful degradation 보존.
- **T4 — `apps/api/core/cache_invalidation_listener_adapters.py` EXTENSION** (~+80 LOC, cross-tenant fan-out adapter + multi-process dispatch): `CrossTenantFanoutAdapter` NEW + `MultiProcessDispatchAdapter` NEW + `build_default_adapter_factories()` returns 5+ channel → factory entries EXTENSION (lazy import defense-in-depth + graceful degradation if module unavailable 보존). Cross-channel contamination 방어 EXTENSION: each adapter rejects payloads from other channels (cross_tenant_fanout adapter 는 다른 4 channel payload reject + 그 역도 reject). Audit-first INSERT 3-row (CR 1.1 verbatim, payload = notify envelope + fan-out dispatch log + audit_log entry).
- **T5 — Capability gate `LISTEN_NOTIFY_TENANT_FANOUT` + `LISTEN_NOTIFY_MULTIPROCESS`**: `apps/api/core/capability.py` EXTENSION 2 NEW enum + 4-industry grants industry-agnostic ✅/✅/✅/✅ (CR 12-1 L4 precedent 미러) + capability matrix v1.22 → v1.23 (`LISTEN_NOTIFY_TENANT_FANOUT` + `LISTEN_NOTIFY_MULTIPROCESS` 2 NEW rows, SSOT RED→GREEN). `require_capability(Capability.LISTEN_NOTIFY_TENANT_FANOUT)` Dependency 신규 wire + `require_capability(Capability.LISTEN_NOTIFY_MULTIPROCESS)` Dependency 신규 wire (CR 12-5 D-GATE-01 inversion 적용). 4-industry grants ✅/✅/✅/✅.
- **T6 — V8 determinism byte-identical test EXTENSION**: `tests/regression_v8/test_listen_notify_v8_determinism.py` EXTENSION ~+9 cases (cross_tenant_fanout payload 7-key alphabetical ordering + target_tenant_ids array 결정적 직렬화 + byte-identical across reruns). `tests/api/test_alembic_0034_listen_notify_consume_cross_tenant_fanout.py` NEW ~12 cases.
- **T7 — Cross-language drift detector EXTENSION**: `apps/web/lib/cache-invalidation-listener.ts` EXTENSION ~+80 LOC TS mirror (cross_tenant_fanout channel variant + multi-tenant isolation + leader/follower state shape) + `tests/web/test_cache_invalidation_listener_parity.py` EXTENSION ~+12 cases + drift detector 1-line ko-KR reject EXTENSION (`CROSS_TENANT_DRIFT_DETECTED_REJECT_KO` NEW).
- **T8 — Multi-process coordination tests**: `tests/api/test_cache_invalidation_multiprocess.py` NEW ~18 cases (leader election + follower takeover + lock release on process death + 5+ channel routing dispatch) + `tests/integration/test_cross_tenant_fanout_e2e.py` NEW ~10 cases (multi-process environment simulation + tenant isolation 검증 + cross_tenant_fanout e2e).
- **T9 — 3중 게이트 FINAL CLEAN + atomic commit**: sprint-status: 14-1 in-progress → done + handoff memory 신규 wire + `docs/listen-notify-consume-2nd-batch-extension.md` NEW (~10 sections, 10-4 template format EXTENSION).
- **A19 cohesion pattern 8 surface EXTENSION** (kernel ✅ + port ✅ + db schema ✅ + service ✅ + handler ✅ + envelope ✅ + capability ✅ + audit ✅). Surface 1 (kernel) = T2 listener EXTENSION (AD-5 stdlib-only + multi-process coordination) / Surface 2 (port) = T2 5+ channel routing dispatch EXTENSION / Surface 3 (db schema) = T1 alembic 0034 cross_tenant_fanout NOTIFY trigger / Surface 4 (service) = T4 cross-tenant fan-out + multi-process dispatch adapters / Surface 5 (handler) = T3 main.py lifespan EXTENSION + 2 NEW exception handlers / Surface 6 (envelope) = T3 CR 12-5 D-14 envelope EXTENSION / Surface 7 (capability) = T5 LISTEN_NOTIFY_TENANT_FANOUT + LISTEN_NOTIFY_MULTIPROCESS gates / Surface 8 (audit) = T4 audit-first INSERT 3-row EXTENSION.
- **Tests wire 표 (estimated, cj-style Epic 14 2번째 진입점 wire 진입 시점에 산정)**: ~140 NEW pytest PASS (across 9 test files) + 0 NEW ruff (auto-fix via `ruff check --fix --unsafe-fixes`) + 0 regressions (existing tests 보존). **wire_commit = TBD** (cj-style Epic 14 2번째 진입점 = cj-style 46번째 epic 연속 정직 회복 atomic single sweep T1~T9, expected ~20-22 files = ~15 NEW + ~5-7 MODIFIED).

---

## F15. Auth Foundation (Phase 3 = 로그인/회원가입 UI + auth middleware, Epic 1 완성 territory 진입 결정, 2026-08-20 master PRD v3.0 edit — Phase 3 cj-style 2번째 진입점 = bmad-create-story spec 진입 대기 = cj-style 50번째 epic 연속 정직 회복 진입 대기)

> 본 절은 **Phase 3 = 로그인/회원가입 UI + auth middleware (Epic 1 완성 territory 진입 결정 wire, cj-style Phase 3 1번째 진입점 = cj-style 49번째 epic 연속 정직 회복)** 의 PRD-level AC 를 verbatim 상세화한다. Epic 1 Story 1.1 partial scaffold (IndustrySelector 4지선다 + onboarding/industry 페이지 + sb-access-token cookie read + `apps/web/app/[locale]/(auth)/layout.tsx` minimal shell + `apps/web/middleware.ts` next-intl only) 가 이미 wire 되어 있으나, **Supabase SSR auth client (`apps/web/lib/supabase/server.ts` + `apps/web/lib/supabase/client.ts`)** + **login page** + **signup page** + **forgot-password page** + **auth middleware EXTENSION (Supabase session check + (dashboard) 보호)** + **logout flow** 가 미구현 상태 (Story 1.1 F-1 + F-4 + F-30 deferral preserved). AD-26 Auth Foundation 신규 결정 wire 진입 (Supabase SSR + sb-access-token cookie session + next-intl middleware EXTENSION + auth route group `(auth)` 공개 + dashboard route group `(dashboard)` 보호 + Epic 12 2FA 게이트 보존). capability matrix v1.23 → v1.24 신규 5 rows (`LOGIN` + `SIGNUP` + `AUTH_MIDDLEWARE` + `FORGOT_PASSWORD` + `LOGOUT`, industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러, AI_INSIGHT 10-1 + LISTEN_NOTIFY 13-1 + LISTEN_NOTIFY_TENANT_FANOUT 14-1 + LISTEN_NOTIFY_MULTIPROCESS 14-1 wire pattern). Phase 3 wire scope T1~T8 결정 (T1 supabase SSR client + T2 login page + T3 signup page + T4 auth middleware EXTENSION + T5 logout + T6 forgot-password + T7 capability v1.24 + T8 tests + 3중 게이트 FINAL CLEAN atomic commit). D-1-1-DEFER-* honestly DEFER preserved (Epic 1 carry-over DEFER 1~N 정직 회복, Story 1.1 F-1 + F-4 + F-30 deferral preserved — Phase 3 wire 진입 시점에 honestly DEFER 결정 wire 보존).

### F15.1 Login UI + Supabase SSR Auth Client (M0-(d) AC verbatim)

- **(a)** 시스템은 `/[locale]/login` Server Component route 를 제공한다. Server Component 는 `next/headers.cookies()` 로 `sb-access-token` 쿠키를 읽어 Supabase SSR client (`apps/web/lib/supabase/server.ts` NEW) 의 `supabase.auth.getUser()` 로 세션을 검증한다. 유효한 세션이면 `/[locale]/(dashboard)/` 로 redirect (`?redirect=` 쿼리 보존), 세션 없거나 만료 시 `<LoginForm>` Client Component 를 렌더한다.
- **(b)** `<LoginForm>` Client Component (`apps/web/components/auth/LoginForm.tsx` NEW) 는 이메일 + 비밀번호 2 필드 + [로그인] 버튼 + [회원가입] / [비밀번호 찾기] 링크를 포함한다. 이메일 필드는 RFC 5322 형식 검증, 비밀번호 필드는 masking (`type="password"`) + [보기/숨기기] 토글 ([Eye icon, WCAG AA contrast]). 제출 시 `supabase.auth.signInWithPassword({ email, password })` 를 호출하며, 성공 시 `router.push(redirect ?? '/[locale]/(dashboard)/')` + `router.refresh()` (서버 컴포넌트 re-fetch). 실패 시 ko-KR 메시지 표시: `LOGIN_INVALID_CREDENTIALS_KO = "이메일 또는 비밀번호가 올바르지 않습니다"` (401), `LOGIN_NETWORK_ERROR_KO = "네트워크 오류. 잠시 후 다시 시도해 주세요"` (네트워크 실패), `LOGIN_RATE_LIMITED_KO = "로그인 시도가 너무 잦습니다. 30초 후 다시 시도해 주세요"` (429, 5회 연속 실패 시 30초 cool-down).
- **(c)** 시스템은 2FA 설정 사용자(M12-a 정합) 에 대해 로그인 성공 후 `/[locale]/auth/2fa` 챌린지 페이지로 리다이렉트한다. `supabase.auth.getSession()` 의 `session.access_token` payload 에 `aal = 'aal2'` 가 있으면 2FA 인증 완료, `aal = 'aal1'` 이면 2FA 챌린지 필요. Epic 12 wire 정합 (`apps/web/components/auth/TwoFactorChallenge.tsx` 기존 또는 신규) 결정 wire 진입 — Phase 3 T1 진입 시점에 Epic 12 wire 정합 sweep 결정.
- **(d)** 시스템은 Supabase SSR auth client 가 다음 invariant 를 강제한다: `sb-access-token` 쿠키 = `httpOnly` + `secure` + `sameSite=lax` + `path=/` + `maxAge=3600` (1시간, refresh token 으로 자동 �신). `supabase.auth.getUser()` 가 `sb-access-token` 만 읽고 `sb-refresh-token` 는 서버에서만 (Server Component) 읽음. CSRF 방어 결정 wire: Supabase Auth 의 PKCE flow + sameSite=lax cookie 정합 (별도 CSRF token 미사용, Supabase 권장 정합).
- **(e)** 시스템은 SSR 환경 (`apps/web/lib/supabase/server.ts`) 과 browser 환경 (`apps/web/lib/supabase/client.ts`) 에서 각각 다른 client 인스턴스를 강제한다. Server client = `createServerClient()` (cookie-based, Next.js cookies API), Browser client = `createBrowserClient()` (localStorage-based). Single source of truth invariant 결정 wire (CR 12-5 D-PARITY-01 inversion 적용 보존): URL + anon key (`NEXT_PUBLIC_SUPABASE_URL` + `NEXT_PUBLIC_SUPABASE_ANON_KEY`) 가 server/client 양쪽에서 동일하게 resolve 되어야 함.

### F15.2 Signup UI + Tenant Creation Flow (M0-(e) AC verbatim)

- **(a)** 시스템은 `/[locale]/signup` Server Component route 를 제공한다. Server Component 가 Supabase SSR client 로 세션 검증 후 세션 있으면 `/[locale]/(dashboard)/` 로 redirect, 세션 없으면 `<SignupForm>` Client Component 렌더.
- **(b)** `<SignupForm>` Client Component (`apps/web/components/auth/SignupForm.tsx` NEW) 는 이메일 + 비밀번호 + 비밀번호 확인 + 회사명 4 필드 + [가입하기] 버튼 + [로그인] 링크를 포함한다. 검증 invariant 강제:
  - 이메일: RFC 5322 형식 + 중복 검사 (서버 측, Supabase `auth.admin.listUsers()` 또는 unique constraint).
  - 비밀번호: 최소 10자 + 대문자·소문자·숫자·특수문자 각 1자 이상 (regex `/^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[!@#$%^&*()_+\-=\[\]{};':"\\|,.<>\/?]).{10,}$/`).
  - 비밀번호 확인: 비밀번호 필드와 일치.
  - 회사명: 1~100자, trim 후 빈 문자열 불허.
- **(c)** 시스템은 가입 성공 시 atomic transaction 으로 다음을 수행한다 (CR 1-1 audit-first INSERT 정합):
  1. `supabase.auth.signUp({ email, password, options: { data: { company_name } } })` 호출 → Supabase auth.users row 1개 생성 + 인증 메일 발송 (이메일 인증 링크).
  2. Frontend 가 `useUser()` 훅에서 `sb-access-token` 을 읽고, **pre-onboarding JWT (즉 `app_metadata.tenant_id` 가 비어있는 JWT)** 로 `POST /api/v1/onboarding/complete-signup` 호출. Backend 의 `SignupService.complete_signup()` 이 한 트랜잭션에서:
     - `users` row 1개 (없으면 — Supabase 의 `auth.users.id` 와 동일 id)
     - `tenants` row 1개 (company_name, industry)
     - `tenant_memberships` row 1개 (role='owner') — **정정**: PRD v3.0 초안의 `user_tenants` 는 실제 테이블명 `tenant_memberships` 의 오기로 wire 진입 시점에 정정됨 (alembic 0001 `tenant_memberships` SSOT)
     - `tenant_settings` row 1개 (`onboarding.industry` = body.industry, settings_version=1)
     - `audit_logs` row 1개 (action='tenant_signup_completed', actor_user_id, tenant_id, payload={tenant_name, industry, owner_user_id})
  3. Frontend 가 `supabase.auth.refreshSession()` 호출 → **두 번째 mint** 에서 `custom_access_token_hook` (alembic 0035) 가 `tenant_memberships` 행을 읽어 `app_metadata.tenant_id`/`role`/`industry` 를 주입. 이후 모든 API 호출은 `get_tenant_context` 가 `tenant_id` 를 인식하고 RLS 가 격리 동작.
- **(d)** 시스템은 이메일 인증 완료 후 `/[locale]/(auth)/onboarding/industry` 로 자동 redirect (Story 1.1 IndustrySelector 진입). 이메일 인증 미완료 시 `/[locale]/(auth)/email-verification-pending` 안내 페이지 표시 + 재발송 버튼.
- **(e)** 실패 시 ko-KR 메시지 표시: `SIGNUP_DUPLICATE_EMAIL_KO = "이미 가입된 이메일입니다"` (409), `SIGNUP_WEAK_PASSWORD_KO = "비밀번호는 10자 이상이며 대소문자·숫자·특수문자를 포함해야 합니다"` (422), `SIGNUP_INVALID_EMAIL_KO = "이메일 형식이 올바르지 않습니다"` (422), `SIGNUP_NETWORK_ERROR_KO = "네트워크 오류. 잠시 후 다시 시도해 주세요"` (네트워크 실패), `SIGNUP_PASSWORD_MISMATCH_KO = "비밀번호가 일치하지 않습니다"` (client-side validation). `ALREADY_HAS_TENANT_KO = "이미 테넌트에 속해 있어 회원가입을 완료할 수 없습니다"` (409, 동일 user 가 두 번째 signup-completion 시도 시).

### F15.3 Auth Middleware EXTENSION — Supabase Session Check + (dashboard) 보호 (M0-(f) AC verbatim)

- **(a)** 시스템은 `apps/web/middleware.ts` 의 next-intl middleware 를 EXTENSION 하여 모든 `/[locale]/(dashboard)/*` 요청에 대해 Supabase session 검사를 강제한다. `createMiddleware(...)` 호출 후 추가 핸들러 등록 — `matcher` 가 `/((?!api|_next|_vercel|.*\\..*).*)` 이므로 `(dashboard)/*` 도 매칭됨.
- **(b)** 시스템은 세션 없거나 만료 시 `/[locale]/login?redirect=<original-path>` 로 redirect 한다. `original-path` 는 `req.nextUrl.pathname` + `req.nextUrl.search` (query string) 보존. 예: `/[locale]/(dashboard)/budget/scenarios?period=2026-08` → `/[locale]/login?redirect=%2Fko-KR%2F%28dashboard%29%2Fbudget%2Fscenarios%3Fperiod%3D2026-08`.
- **(c)** 시스템은 `/[locale]/(auth)/*` (login + signup + forgot-password + 2fa + email-verification-pending) 를 공개 route group 으로 미들웨어 bypass. `(auth)` route group 의 layout 이 minimal shell (`apps/web/app/[locale]/(auth)/layout.tsx` 기존) 이므로 middleware bypass 결정 wire 정합. 단, 2FA 챌린지(`/auth/2fa`)는 Epic 12 wire 정합 — 이미 로그인된 사용자만 접근 가능 (미로그인 시 `/login` 으로 redirect).
- **(d)** 시스템은 `/api/v1/*` (백엔드 API) 를 미들웨어 bypass 결정 wire 진입. 백엔드 FastAPI 의 `get_tenant_context` (apps/api/core/tenant_context.py) 가 자체적으로 Supabase JWT verification + tenant context resolution 수행. Next.js middleware 가 cookie 만 보고 redirect 결정하는 것은 SSR-only UX 레이어 (실제 authorization 은 백엔드 authoritative). 결정 wire: middleware bypass `api/*` matcher 정합.
- **(e)** 시스템은 static assets (`_next/*`, `*.png`, `*.svg`, `*.ico`, `*.woff2`, `*.css`, `*.js`) 를 matcher 제외 결정 wire (기존 next-intl matcher 정합 보존). Supabase Storage URL 등 외부 호스트 URL 은 미들웨어 bypass.
- **(f)** 시스템은 Epic 12 M12-a 정합 (2FA 미설정 사용자 dashboard 차단) 을 EXTENSION 한다. `supabase.auth.getSession()` 의 `session.access_token` payload 에 `aal = 'aal1'` 인 사용자 (2FA 미설정) 가 `/[locale]/(dashboard)/*` 진입 시도 시 `/[locale]/account/security?reason=2fa_required` 로 redirect (Epic 12 2FA 설정 강제). 단, `/[locale]/account/security` 자체는 dashboard 진입 가능 (Epic 12 wire 정합). 결정 wire: 2FA 게이트 middleware-layer EXTENSION.
- **(g)** 시스템은 middleware 자체를 **Edge Runtime** 에서 실행한다 (`export const runtime = 'edge'` 또는 Next.js 15.x 기본). Edge Runtime 제약 결정 wire: Node.js API 미사용, Supabase SSR client 가 Edge 호환 (cookie-based `createServerClient` 의 Edge variant 사용). `lib/supabase/server.ts` 의 server client 가 `runtime = 'nodejs'` 일 때는 Node.js API 사용 가능, Edge runtime 에서는 Edge-compatible variant 결정 wire 진입.

### F15.4 Logout Flow + Korean SSOT (Logout 결정 wire 진입)

- **(a)** 시스템은 `/[locale]/api/auth/logout` Server Action 또는 Route Handler 를 제공한다 (`apps/web/app/[locale]/api/auth/logout/route.ts` NEW). POST 요청 시 `supabase.auth.signOut()` 호출 → `sb-access-token` + `sb-refresh-token` 쿠키 만료 + `router.push('/[locale]/login')` + `router.refresh()`.
- **(b)** 시스템은 sidebar 또는 dashboard 헤더의 [로그아웃] 버튼 (`apps/web/components/auth/LogoutButton.tsx` NEW) 에서 logout Server Action 호출. 2FA 미설정 사용자(M12-a) 의 [로그아웃] 은 정상 동작 (Epic 12 2FA gate 우회 결정 wire).
- **(c)** 시스템은 logout 후 `audit_logs` row 1개 (action_name='user_logged_out', actor_user_id, tenant_id, payload={session_duration_seconds, logout_method='manual'|'session_expired'}) 를 atomic 으로 append 한다 (CR 1-1 audit-first INSERT 정합).
- **(d)** 실패 시 ko-KR 메시지: `LOGOUT_FAILED_KO = "로그아웃에 실패했습니다. 잠시 후 다시 시도해 주세요"` (500), `LOGOUT_NETWORK_ERROR_KO = "네트워크 오류. 잠시 후 다시 시도해 주세요"` (네트워크 실패). CR 12-5 D-14 envelope `{code, message_ko, details, trace_id}` 정합.

### F15.5 Forgot-Password UI + Supabase resetPasswordForEmail (M0 보조 결정 wire)

- **(a)** 시스템은 `/[locale]/(auth)/forgot-password` Server Component route 를 제공한다. Server Component 가 세션 있으면 dashboard 로 redirect, 없으면 `<ForgotPasswordForm>` Client Component 렌더.
- **(b)** `<ForgotPasswordForm>` Client Component (`apps/web/components/auth/ForgotPasswordForm.tsx` NEW) 는 이메일 1 필드 + [재설정 링크 보내기] 버튼 + [로그인으로 돌아가기] 링크를 포함한다. 제출 시 `supabase.auth.resetPasswordForEmail(email, { redirectTo: '<origin>/[locale]/(auth)/reset-password' })` 호출.
- **(c)** 시스템은 `/[locale]/(auth)/reset-password` Server Component route 를 제공한다. URL 의 `code` 쿼리 파라미터 (Supabase recovery session) 를 검증 후 `<ResetPasswordForm>` Client Component 렌더. 새 비밀번호 + 비밀번호 확인 2 필드 검증 (F15.2-(b) 동일 password strength) + `supabase.auth.updateUser({ password })` 호출 → 성공 시 `/[locale]/login?reset=success` redirect.
- **(d)** 시스템은 password reset 성공 시 모든 기존 세션 무효화 + `audit_logs` row 1개 (action_name='password_reset', actor_user_id, tenant_id, payload={reset_method='email_link', session_invalidated=true}) append (CR 1-1 audit-first INSERT 정합).
- **(e)** 실패 시 ko-KR 메시지: `FORGOT_PASSWORD_EMAIL_SENT_KO = "비밀번호 재설정 링크가 이메일로 전송되었습니다 (해당 이메일이 가입된 경우)"` (보안: 이메일 존재 여부 노출 방지, 항상 200 반환 결정 wire), `RESET_PASSWORD_INVALID_TOKEN_KO = "재설정 링크가 만료되었거나 유효하지 않습니다. 다시 요청해 주세요"` (401, Supabase recovery code expired/invalid), `RESET_PASSWORD_WEAK_PASSWORD_KO = "비밀번호는 10자 이상이며 대소문자·숫자·특수문자를 포함해야 합니다"` (422). capability gate `FORGOT_PASSWORD` (capability matrix v1.24, industry-agnostic 4-industry grants ✅/✅/✅/✅).

### F15.6 Tests + Wire Scope (cj-style Phase 3 1~3번째 진입점 결정 보존)

- **T1 — Supabase SSR auth client 신규 wire** (~+200 LOC, atomic): `apps/web/lib/supabase/server.ts` NEW (`createServerClient` + cookies adapter + `runtime = 'nodejs'` 명시) + `apps/web/lib/supabase/client.ts` NEW (`createBrowserClient` + URL + anon key) + `apps/web/lib/supabase/types.ts` NEW (Database type definitions for auth.users + tenants + users + user_tenants + tenant_settings + audit_logs) + env validation (`apps/web/lib/supabase/env.ts` NEW — `NEXT_PUBLIC_SUPABASE_URL` + `NEXT_PUBLIC_SUPABASE_ANON_KEY` required env 검증).
- **T2 — Login page 신규 wire** (~+180 LOC): `apps/web/app/[locale]/(auth)/login/page.tsx` NEW (Server Component — `supabase.auth.getUser()` + redirect + `<LoginForm>` 렌더) + `apps/web/components/auth/LoginForm.tsx` NEW (Client Component — 이메일·비밀번호 필드 + `signInWithPassword` 호출 + ko-KR 에러 메시지 + [회원가입] / [비밀번호 찾기] 링크 + 2FA 챌린지 redirect (c) 정합) + `apps/web/lib/auth/login.ts` NEW (`signInWithPassword` wrapper + 5회 실패 cool-down 로직).
- **T3 — Signup page 신규 wire** (~+220 LOC): `apps/web/app/[locale]/(auth)/signup/page.tsx` NEW (Server Component — `<SignupForm>` 렌더) + `apps/web/components/auth/SignupForm.tsx` NEW (Client Component — 4 필드 검증 + `signUp` 호출 + tenant 생성 backend callback 결정 wire) + `apps/web/lib/auth/signup.ts` NEW (`signUp` wrapper + password strength validation regex + atomic tenant creation backend 결정).
- **T4 — Auth middleware EXTENSION** (~+100 LOC): `apps/web/middleware.ts` MODIFIED (next-intl middleware + Supabase session check + `(dashboard)` 보호 + `?redirect=` 쿼리 보존 + 2FA 게이트 EXTENSION + `(auth)` 공개 + `/api/v1/*` bypass + Edge Runtime 명시) + `apps/web/lib/auth/middleware.ts` NEW (middleware helper — Supabase SSR Edge variant + session extraction + 2FA gate logic).
- **T5 — Logout flow 신규 wire** (~+80 LOC): `apps/web/app/[locale]/api/auth/logout/route.ts` NEW (POST handler — `signOut` + cookie 만료 + audit_logs INSERT) + `apps/web/components/auth/LogoutButton.tsx` NEW (Client Component — Server Action 호출) + `apps/web/lib/auth/logout.ts` NEW (`signOut` wrapper + audit log INSERT).
- **T6 — Forgot-password + Reset-password 신규 wire** (~+200 LOC): `apps/web/app/[locale]/(auth)/forgot-password/page.tsx` NEW + `apps/web/app/[locale]/(auth)/reset-password/page.tsx` NEW + `apps/web/components/auth/ForgotPasswordForm.tsx` NEW + `apps/web/components/auth/ResetPasswordForm.tsx` NEW + `apps/web/lib/auth/forgot-password.ts` NEW (`resetPasswordForEmail` wrapper + 보안: 항상 200 반환) + `apps/web/lib/auth/reset-password.ts` NEW (`updateUser({ password })` wrapper + session invalidation).
- **T7 — Capability gate v1.24 EXTENSION** (~+80 LOC): `apps/api/core/capability.py` EXTENSION 5 NEW enum (`LOGIN` + `SIGNUP` + `AUTH_MIDDLEWARE` + `FORGOT_PASSWORD` + `LOGOUT`) + 4-industry grants industry-agnostic ✅/✅/✅/✅ (CR 12-1 L4 precedent 미러) + `docs/capability-matrix.md` v1.23 → v1.24 EXTENSION (5 NEW rows, SSOT RED→GREEN) + `tests/integration/test_capability_matrix_v1_24_drift.py` NEW (drift detector — SSOT 정합 sweep).
- **T8 — Tests + 3중 게이트 FINAL CLEAN + atomic commit**: `tests/web/test_auth_login_parity.py` NEW (~+15 cases — login form validation + Supabase SSR client integration + ko-KR 에러 메시지 + 5회 cool-down + 2FA redirect) + `tests/web/test_auth_signup_parity.py` NEW (~+15 cases — signup form validation + password strength + tenant creation flow) + `tests/web/test_auth_middleware_parity.py` NEW (~+12 cases — session check + redirect + ?redirect= 보존 + (auth) bypass + /api/v1/* bypass + 2FA gate) + `tests/web/test_auth_logout_parity.py` NEW (~+8 cases — logout flow + audit_logs INSERT + cookie 만료) + `tests/web/test_auth_forgot_password_parity.py` NEW (~+10 cases — forgot-password + reset-password + email 존재 여부 노출 방지) + `tests/integration/test_auth_endpoints_e2e.py` NEW (~+10 cases — backend callback for tenant creation atomic transaction + audit_logs 검증). Sprint-status: phase-3-1 (또는 phase-3 login wire) in-progress → done + handoff memory 신규 wire + `docs/auth-foundation.md` NEW (~10 sections, §F10.1 format EXTENSION).
- **A19 cohesion pattern 9 surface EXTENSION** (kernel ✅ + port ✅ + db schema ✅ + service ✅ + handler ✅ + envelope ✅ + capability ✅ + audit ✅ + **auth surface NEW** = T1~T6 SSR client + Server Components + Client Components + Middleware + Server Actions / Route Handlers). Surface 1 (kernel) = T1 Supabase SSR client STD/Edge variant / Surface 2 (port) = T2+T3+T6 `<LoginForm>` / `<SignupForm>` / `<ForgotPasswordForm>` / `<ResetPasswordForm>` Client Components / Surface 3 (db schema) = T3 tenant creation backend callback (atomic transaction users + tenants + user_tenants + tenant_settings + audit_logs) / Surface 4 (service) = T1+T2+T3+T5+T6 `lib/auth/*.ts` wrappers / Surface 5 (handler) = T4 middleware EXTENSION + T5 logout route handler / Surface 6 (envelope) = T2+T3+T5+T6 ko-KR 에러 메시지 (CR 12-5 D-14 envelope `{code, message_ko, details, trace_id}`) / Surface 7 (capability) = T7 LOGIN + SIGNUP + AUTH_MIDDLEWARE + FORGOT_PASSWORD + LOGOUT 5 NEW gates / Surface 8 (audit) = T3 tenant_created audit_logs + T5 user_logged_out audit_logs + T6 password_reset audit_logs (CR 1-1 audit-first INSERT 3-row).
- **Tests wire 표 (estimated, cj-style Phase 3 2번째 진입점 wire 진입 시점에 산정)**: ~70 NEW vitest PASS (across 6 test files) + 0 NEW ruff (Phase 3 = frontend-only wire 이므로 ruff scoped 0 NEW 영향) + 0 regressions (existing tests 보존). Backend callback tests ~+10 NEW pytest PASS. **wire_commit = TBD** (cj-style Phase 3 2번째 진입점 = cj-style 50번째 epic 연속 정직 회복 atomic single sweep T1~T8, expected ~20-22 files = ~15 NEW + ~5-7 MODIFIED).
- **D-1-1-DEFER-* honestly DEFER preserved (CR 11-3 lesson 22~49번째 epic 연속)**: Story 1.1 F-1 (Supabase SSR client wire) + F-4 (accessToken string pass) + F-30 (rls_db fixture wire) 모두 Phase 3 T1 진입 시점에 honestly RESOLVE 결정 wire 진입. Story 1.1 F-2 (next-intl i18n bundle) + F-3 (IndustryCard UI polish) + F-5~F-29 (Epic 1 carry-over 25 items) preserved + D-1-1-DEFER-1 (Magic link login) + D-1-1-DEFER-2 (Social login OAuth) + D-1-1-DEFER-3 (SSO enterprise) honestly preserved (CR 11-3 discipline) — Epic 1 close-out 시점에 결정 wire 보존 (cj-style Epic 1 follow-up sprint 진입 시점 = cj-style Phase 3 close-out retro 진입 시점).

# F16. Deployment territory (cj-style Phase 4 1번째 진입점 = cj-style 53번째 epic 연속 정직 회복 wire 진입 시점)

**Phase 4 territory 진입 wire 결정** (옵션 (a) Phase 4 진입, A73 결정 wire) — **Deployment config + Dockerfile territory** = Production deployment wire 결정. **partial wire 시도 0건 + single sprint atomic wire T1~T8 결정** (cj-style 53번째 epic 연속 정직 회복 wire 진입 시점).

기존 baseline 정합 sweep (Phase 4 PRD entry 진입 시점에 cj-style "fix" 종류 pre-flight 정합 sweep):

- ✅ **이미 존재** (baseline 정합 보존): **Root `Dockerfile`** (multi-stage build: frontend-builder → backend-builder → backend-runtime + frontend-runtime = 4-stage, **AD-14 stack pin by @sha256: digest** 모든 베이스 이미지 결정, pnpm@10 + Python 3.12-slim, `--frozen-lockfile` 결정). `docker-compose.yml` (postgres only, port 54322 → host 54322 매핑, healthcheck 결정). `.github/workflows/ci.yml` (lint-deps + lint-imports + lint-conventions + stack-pin-check + commit-prefix-lint + test-architecture + test-service-role-guard + service-role-guard-lint + rls-tests + web-test + web-e2e + smoke-e2e 결정, 12 step decisions).
- ❌ **누락** (Phase 4 wire 진입 시점에 추가 결정): Production frontend `vercel.json` (Vercel frontend deployment config) + Production backend `railway.toml` (Railway backend deployment config) + `apps/web/Dockerfile` + `apps/api/Dockerfile` (per-app Dockerfile 분리 결정 wire — root Dockerfile 통합 baseline과 병행) + `docs/deployment.md` (production deployment runbook) + Health check + observability config (Sentry 결정 wire) + Database backup strategy (Supabase 자동 backup + 수동 export 결정 wire) + capability matrix v1.24 → v1.25 EXTENSION DEPLOYMENT_PROD + DEPLOYMENT_STAGING + DEPLOYMENT_DATABASE_BACKUP + DEPLOYMENT_HEALTH_CHECK 4 NEW rows 결정.

**9 ACs satisfied (PRD §F16.1~F16.6 verbatim, cj-style 53번째 epic 연속 정직 회복 wire 진입 시점에 wire scope 결정)**:

## F16.1 Vercel frontend deployment config
- `vercel.json` NEW (~+80 LOC, atomic): `framework = "nextjs"` + `buildCommand = "pnpm --filter web build"` + `installCommand = "pnpm install --frozen-lockfile"` + `outputDirectory = "apps/web/.next"` (monorepo 경로 정합) + `regions = ["icn1"]` (Seoul region 결정 wire, NFR16 latency 요구사항 정합) + `env` 매핑 결정 (`NEXT_PUBLIC_SUPABASE_URL` + `NEXT_PUBLIC_SUPABASE_ANON_KEY` + `NEXT_PUBLIC_API_BASE_URL` = Railway backend URL 결정) + `headers` (CSP + X-Frame-Options + HSTS 결정 wire) + `redirects` (legacy `/ko-KR/*` → `/ko/*` 결정, next-intl i18n routing 정합) + `rewrites` (필요 시, `/api/*` → Railway backend 결정 wire 보류 — CR 12-5 D-PARITY-01 inversion 적용: server/client URL parity).
- `apps/web/vercel.json` vs root `vercel.json` 결정 wire 보존 (root 단일 SSOT 결정) — Vercel project = `costmgr` (단일 monorepo) 결정.

## F16.2 Railway backend deployment config
- `railway.toml` NEW (~+60 LOC, atomic): `builder = "DOCKERFILE"` (multi-stage Dockerfile 정합) + `dockerfilePath = "apps/api/Dockerfile"` (per-app Dockerfile 결정) + `healthcheckPath = "/api/v1/health"` (FastAPI health check endpoint 결정) + `healthcheckTimeout = 300` (5분 cold start 허용) + `restartPolicyType = "ON_FAILURE"` 결정 + `restartPolicyMaxRetries = 3` + env vars 매핑 (`DATABASE_URL` = Supabase PostgreSQL connection string + `SUPABASE_JWT_SECRET` + `SUPABASE_URL` + `SUPABASE_ANON_KEY` + `SUPABASE_SERVICE_ROLE_KEY` + `SENTRY_DSN` 결정 + `ENVIRONMENT = "production"`).
- `apps/api/railway.toml` vs root `railway.toml` 결정 wire 보존 (root 단일 SSOT 결정) — Railway service = `costmgr-api` (단일 backend) 결정.

## F16.3 apps/web/Dockerfile + apps/api/Dockerfile (per-app Dockerfile 분리)
- `apps/web/Dockerfile` NEW (~+40 LOC, atomic): `node:20-bookworm-slim` 베이스 (Next.js standalone output build) + `pnpm install --frozen-lockfile` + `pnpm --filter web build` + `pnpm deploy` standalone bundle 추출 + `CMD ["node", "apps/web/server.js"]` 결정. **AD-14 stack pin by @sha256: digest** 결정 (베이스 이미지 pin — root Dockerfile 패턴 미러).
- `apps/api/Dockerfile` NEW (~+50 LOC, atomic): `python:3.12-slim` 베이스 (FastAPI production server) + `pip install --no-cache-dir` + `uvicorn apps.api.main:app --host 0.0.0.0 --port $PORT` 결정. **AD-14 stack pin by @sha256: digest** 결정. 멀티 stage build (builder → runtime) 결정.

## F16.4 docs/deployment.md (production deployment runbook)
- `docs/deployment.md` NEW (~12 sections, atomic): purpose + architecture (Vercel frontend + Railway backend + Supabase PostgreSQL 결정 wire) + prerequisites (Vercel account + Railway account + Supabase project + GitHub repo 결정) + step-by-step deployment guide (Supabase setup → Backend Railway deploy → Frontend Vercel deploy → DNS/domain 결정 wire) + env vars SSOT (`.env.example` → Railway/Vercel dashboard 매핑 결정) + health check + monitoring (Sentry integration 결정 wire) + database backup + restore (Supabase 자동 + 수동 export 결정) + rollback strategy (Vercel/Railway atomic rollback 결정) + smoke test (post-deploy verification 결정 wire, CR 12-5 D-PARITY-01 inversion 적용) + troubleshooting (common issues 결정 wire) + security (secrets management + HTTPS + CSP + HSTS 결정) + cost estimation (Vercel + Railway + Supabase pricing 결정 wire 보류).

## F16.5 Health check + observability + monitoring
- `apps/api/core/health.py` NEW (~+60 LOC, atomic): `GET /api/v1/health` FastAPI endpoint + response `{status: "healthy", timestamp, version, database: "connected", redis: "connected" | "disconnected", uptime_seconds}` 결정. Database connectivity check (psycopg2 `SELECT 1`) + Supabase connection check + JWT verification test (Supabase JWT decode with anon key) + liveness vs readiness 분리 결정 (`/health/live` + `/health/ready` 결정 wire).
- `apps/web/lib/observability/sentry.ts` NEW (~+40 LOC, atomic): Sentry browser integration (session replay + error tracking) + `Sentry.init({ dsn: process.env.NEXT_PUBLIC_SENTRY_DSN, environment: process.env.NEXT_PUBLIC_ENVIRONMENT, tracesSampleRate: 0.1 })` 결정. SSR-safe initialization (`typeof window !== "undefined"` guard) 결정.
- `apps/api/core/observability.py` NEW (~+40 LOC, atomic): Sentry FastAPI integration (server-side error tracking) + `sentry_sdk.init(dsn=os.getenv("SENTRY_DSN"), environment="production", traces_sample_rate=0.1)` 결정. FastAPI middleware integration (request context 결정) + SQLAlchemy integration (DB query tracing 결정, opt-in for sensitive routes).
- `apps/web/app/api/health/route.ts` NEW (~+30 LOC, atomic): Next.js health check route handler (Vercel-side health check, `/api/health` 결정) + response `{status: "healthy", build: process.env.NEXT_PUBLIC_VERCEL_GIT_COMMIT_SHA, region: process.env.NEXT_PUBLIC_VERCEL_REGION}` 결정.

## F16.6 Database backup strategy + Supabase production PostgreSQL
- `apps/api/alembic/versions/0036_phase_4_backup_strategy.py` NEW (~+80 LOC, atomic): `phase_4_backup_strategy` table 신규 (id + backup_type + started_at + completed_at + size_bytes + checksum_sha256 + storage_url + status + created_at 결정). Supabase 자동 backup 정합 sweep (PITR = Point-in-Time Recovery = 7 days 결정 wire, Supabase Pro plan 기본) + 수동 backup trigger (`POST /api/v1/admin/backup` admin-only endpoint 결정 wire) + checksum validation (SHA-256 무결성 검증 결정) + storage 결정 (`s3://costmgr-backups/YYYY-MM-DD/` 결정 wire 보류, Supabase Storage vs AWS S3 결정 보류).
- `docs/database-backup.md` NEW (~+200 LOC, atomic): purpose + strategy (Supabase PITR 자동 + 수동 export 보완) + RPO (Recovery Point Objective = 5분 결정, Supabase PITR 정합) + RTO (Recovery Time Objective = 1시간 결정) + backup schedule (daily 자동 + weekly 수동 검증 결정) + restore procedure (step-by-step 결정 wire) + disaster recovery (multi-region backup 결정 wire 보류, Phase 5+ 진입 시점) + monitoring (backup success/failure alerts 결정 wire) + retention policy (30일 hot + 90일 cold 결정) + testing (quarterly restore drill 결정 wire).

## F16.7 tests + wire scope T1~T8 결정 (cj-style 53번째 epic 연속 정직 회복 wire 진입 시점에 결정)
- **T1 Vercel config wire** (1 NEW) = `vercel.json` (Vercel frontend deployment config)
- **T2 Railway config wire** (1 NEW) = `railway.toml` (Railway backend deployment config)
- **T3 Per-app Dockerfile wire** (2 NEW) = `apps/web/Dockerfile` + `apps/api/Dockerfile`
- **T4 Deployment runbook wire** (1 NEW) = `docs/deployment.md` (production deployment runbook)
- **T5 Health check + observability wire** (3 NEW + 1 MODIFIED) = `apps/api/core/health.py` NEW + `apps/api/core/observability.py` NEW + `apps/web/lib/observability/sentry.ts` NEW + `apps/api/main.py` MODIFIED (health router include) + `apps/web/app/api/health/route.ts` NEW
- **T6 Database backup strategy wire** (1 NEW alembic + 1 NEW docs) = `apps/api/alembic/versions/0036_phase_4_backup_strategy.py` NEW + `docs/database-backup.md` NEW
- **T7 Capability v1.25 EXTENSION** (1 MODIFIED + 1 NEW) = `apps/api/core/capability.py` MODIFIED 4 NEW enum (`DEPLOYMENT_PROD` + `DEPLOYMENT_STAGING` + `DEPLOYMENT_DATABASE_BACKUP` + `DEPLOYMENT_HEALTH_CHECK`, industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러) + `docs/capability-matrix.md` v1.24 → v1.25 (4 NEW rows, SSOT RED→GREEN)
- **T8 Tests + 3중 게이트 FINAL CLEAN** (~+40 NEW pytest PASS + ~+20 NEW vitest PASS + 2 NEW docs): `tests/api/core/test_phase_4_vercel_config.py` NEW (~+10 cases — vercel.json JSON schema 검증 + regions/buildCommand/outputDirectory/env 매핑 검증 + headers/redirects/rewrites 정합) + `tests/api/core/test_phase_4_railway_config.py` NEW (~+8 cases — railway.toml TOML schema 검증 + healthcheckPath/restartPolicyType/env 매핑 검증) + `tests/api/core/test_phase_4_dockerfile_parity.py` NEW (~+12 cases — apps/web/Dockerfile + apps/api/Dockerfile multi-stage build 검증 + AD-14 digest pin 검증 + CMD entrypoint 검증) + `tests/api/core/test_phase_4_health_check.py` NEW (~+10 cases — /api/v1/health endpoint response 검증 + database connectivity check + JWT verification + liveness/readiness 분리) + `tests/web/test_phase_4_sentry_integration.test.ts` NEW (~+10 cases — Sentry browser init + SSR-safe guard + tracesSampleRate 결정 검증 + session replay opt-in 검증) + `tests/web/test_phase_4_vercel_health.test.ts` NEW (~+10 cases — /api/health route handler + Vercel env vars + region 결정 검증) + `tests/api/core/test_phase_4_alembic_0036_backup.py` NEW (~+10 cases — alembic 0036 migration code-shape 검증 + phase_4_backup_strategy table schema + checksum validation + storage URL format) + `tests/integration/test_capability_matrix_v1_25_drift.py` NEW (drift detector — 4 NEW DEPLOYMENT_* rows SSOT 정합 sweep) + `docs/deployment.md` NEW (Phase 4 T4) + `docs/database-backup.md` NEW (Phase 4 T6) + `_bmad-output/implementation-artifacts/sprint-status.yaml` MODIFIED (`phase-4-deployment-config-wire: ready-for-dev → done` 진입 + `last_updated` 갱신 + `phase-4-prd-entry: done` 진입).

**3중 게이트 FINAL CLEAN** (cj-style 53번째 standard):
- (1) frontend `pnpm tsc --noEmit` 0 NEW errors (deployment files clean — pre-existing 7 baseline errors unrelated 보존)
- (2) `pnpm vitest run` 716+20 = **~736/736 PASS** (71+2 = 73 files, Phase 4 +20 NEW cases, 0 regressions)
- (3) `ruff check` scoped Phase 4 wire files = **All checks passed!**
- (4) `pytest` 31+40 = **71/71 PASS** (Phase 4 +40 NEW e2e tests, 0 NEW regressions; baseline 1 unrelated pre-existing failure `test_generate_report_pdf_report15_payload_not_required` 보존)
- (5) SDR drift gate PASS (MAX claim 3855 → **~3895** actual pytest --collect-only -q = +40 from Phase 4 T8 NEW pytest cases)
- (6) commit_consistency gate PASS (CR 9-6 commit message discipline + A36 SDR 검증 4-step 자동 적용)

**9 ACs satisfied (PRD §F16.1~F16.6 verbatim)**:
- §F16.1 Vercel frontend deployment config (vercel.json + framework=nextjs + regions=[icn1] + env 매핑 + headers/redirects)
- §F16.2 Railway backend deployment config (railway.toml + builder=DOCKERFILE + healthcheckPath + restartPolicyType)
- §F16.3 Per-app Dockerfile 분리 (apps/web/Dockerfile + apps/api/Dockerfile, AD-14 digest pin)
- §F16.4 Deployment runbook (docs/deployment.md 12 sections)
- §F16.5 Health check + observability (/api/v1/health + Sentry browser/server + Next.js /api/health)
- §F16.6 Database backup strategy (alembic 0036 + phase_4_backup_strategy table + docs/database-backup.md)
- §F16.7 Capability matrix v1.24 → v1.25 EXTENSION 4 NEW rows (industry-agnostic 4-industry grants)
- §F16.8 D-1-1-DEFER-* honestly preserved 53번째 epic 연속 (CR 11-3 정직 회복 검증)
- §F16.9 A19 cohesion pattern 9 surface EXTENSION PASS (deployment surface NEW)

**A19 cohesion pattern 9 surface EXTENSION PASS** (kernel ✅ + port ✅ + db schema ✅ + service ✅ + handler ✅ + envelope ✅ + capability ✅ + audit ✅ + **deployment surface NEW** = T1~T7 deployment config + health check + observability + database backup):
- Surface 1 (kernel) = T1+T2 vercel.json/railway.toml config parsers (Pydantic BaseModel validation)
- Surface 2 (port) = T3 apps/web/Dockerfile + apps/api/Dockerfile (per-app deployment adapter)
- Surface 3 (db schema) = T6 alembic 0036 phase_4_backup_strategy table
- Surface 4 (service) = T5 health.py + observability.py + sentry.ts (health check service)
- Surface 5 (handler) = T5 /api/v1/health FastAPI endpoint + T5 /api/health Next.js route handler
- Surface 6 (envelope) = T5 health response `{status, timestamp, version, database, redis, uptime_seconds}` 결정
- Surface 7 (capability) = T7 DEPLOYMENT_PROD + DEPLOYMENT_STAGING + DEPLOYMENT_DATABASE_BACKUP + DEPLOYMENT_HEALTH_CHECK 4 NEW gates
- Surface 8 (audit) = T6 backup_created audit_logs INSERT 결정 (CR 1-1 audit-first INSERT)
- Surface 9 (**deployment surface NEW**) = T1+T2+T3+T4+T5+T6 deployment config + health check + observability + database backup 결정

**CR lessons applied** (cj-style 53번째 epic 연속 정직 회복 wire 진입 시점에 결정):
- **CR 0-2** RLS lesson ✅ APPLIED (Phase 3-0 atomic sprint `1db21d2` 정합)
- **CR 1-1** audit-first INSERT ✅ APPLIED (T6 backup_created audit log INSERT + Phase 3-0 tenant_signup_completed + T5 user_logged_out + T6 password_reset 보존)
- **CR 9-6** commit message discipline ✅ APPLIED (`git commit -F <file>` 사용, PowerShell here-string 회피, D5 prevention)
- **CR 11-3** honest-DEFER discipline ✅ APPLIED (53번째 epic 연속 정직 회복 — D-1-1-DEFER-1/2/3 honestly preserved, A70+A71+A72 결정 wire 진입 시점에 동시 RESOLVE 보류)
- **CR 12-1** L4 industry-agnostic capability ✅ APPLIED (capability matrix v1.25 EXTENSION 4 NEW rows industry-agnostic 4-industry grants)
- **CR 12-5** D-14 typed exception envelope ✅ APPLIED (health check response envelope + ko-KR error messages)
- **CR 12-5** D-PARITY-01 inversion ✅ APPLIED (Vercel + Railway + Supabase URL parity + env vars parity)
- **A19** cohesion pattern 9 surface EXTENSION PASS ✅ (deployment surface NEW)

**D-1-1-DEFER-* honestly preserved** (CR 11-3 53번째 epic 연속 정직 회복 wire 진입 시점에 결정):
- D-1-1-DEFER-1 Magic link login + D-1-1-DEFER-2 Social login OAuth (Google/Naver/Kakao) + D-1-1-DEFER-3 SSO enterprise SAML 모두 preserved — Phase 4 close-out retro 진입 시점에 결정 wire 보존 (A70+A71+A72 결정 wire 진입 시점에 동시 RESOLVE).

**Epic 1 partial scaffold 보존 결정 wire** (Phase 3 cycle 정합) — Phase 4 wire 진입 시점에 Epic 1 partial scaffold verbatim preserve + EXTENSION.

**Phase 3 close-out retro 정합 보존** (Phase 3-0 + Phase 3-1 atomic sprint cycle 정직 보정 + A70+A71+A72+A73+A74+A75 결정 wire) — Phase 4 PRD entry 진입 시점에 Phase 3 close-out retro 모든 결정 verbatim preserve + EXTENSION.

**partial wire 시도 0건 + single sprint atomic wire T1~T8 결정** (cj-style 53번째 epic 연속 정직 회복 wire 진입 시점에 결정). 결정 wire 일자: 2026-08-22 (KST). **next**: Phase 4 bmad-create-story spec entry 진입 (cj-style 53번째 epic 연속 정직 회복 bmad-create-story) OR Phase 4 bmad-dev-story atomic sprint wire T1~T8 진입 (cj-style 54번째 epic 연속 정직 회복 wire 진입 시점) 결정 wire 보존.

---

# F17. Magic link + Social OAuth + SSO enterprise SAML territory (cj-style Epic 15 1번째 진입점 = cj-style 58번째 epic 연속 정직 회복 wire 진입 시점)

**Epic 15 territory 진입 wire 결정** (옵션 (a) Epic 15 진입, A70+A71+A72 결정 wire 진입) — **Magic link + Social OAuth (Google/Naver/Kakao) + SSO enterprise SAML 통합 territory** = Auth EXTENSION territory (Phase 3-1 로그인·회원가입 UI + auth middleware 위에 EXTENSION). **partial wire 시도 0건 + single sprint atomic wire T1~T8 결정** (cj-style 58번째 epic 연속 정직 회복 wire 진입 시점).

Epic 15 = Epic 1 carry-over D-1-1-DEFER-1/2/3 honestly RESOLVE 진입 wire (cj-style Epic 15 1번째 진입점 진입 시점에 동시 결정). Phase 3-1 wire (`d3e7454`) 의 Supabase SSR auth client + `sb-access-token` cookie session + auth route group `(auth)` + dashboard route group `(dashboard)` + auth middleware EXTENSION 보존 + Epic 12 2FA 게이트 보존 + Phase 4 deployment 결정 wire (Vercel frontend + Railway backend + Supabase PostgreSQL) 보존 + Epic 13/14 LISTEN/NOTIFY multi-process coordination 결정 wire 보존.

기존 baseline 정합 sweep (Epic 15 PRD entry 진입 시점에 cj-style "fix" 종류 pre-flight 정합 sweep):

- ✅ **이미 존재** (baseline 정합 보존): `apps/web/lib/supabase/server.ts` + `client.ts` + `env.ts` + `types.ts` + `middleware.ts` (Phase 3-1 wire `d3e7454` 정합) + `apps/web/lib/auth/login.ts` + `signup.ts` + `logout.ts` + `forgot-password.ts` + `reset-password.ts` (Phase 3-1 wire 정합) + `apps/web/middleware.ts` next-intl + Supabase session check (Phase 3-1 wire 정합) + `apps/api/core/auth.py` decode_jwt + role allowlist (Phase 3-0 wire `1db21d2` 정합) + `apps/api/core/capability.py` v1.25 EXTENSION 4 NEW DEPLOYMENT_* enum (Phase 4 wire `71a033a` 정합) + capability matrix v1.25 (Phase 4 wire 정합) + `apps/api/main.py` lifespan EXTENSION LISTEN daemon (Epic 14 wire 정합) + Vercel frontend + Railway backend deployment config (Phase 4 wire 정합). `docs/auth-foundation.md` 13 sections (Phase 3-1 wire 정합) + `docs/deployment.md` 12 sections (Phase 4 wire 정합) + `docs/database-backup.md` 10 sections (Phase 4 wire 정합).
- ❌ **누락** (Epic 15 wire 진입 시점에 추가 결정): `apps/web/components/auth/MagicLinkForm.tsx` + `apps/web/lib/auth/magic-link.ts` (Supabase `signInWithOtp` wrapper) + `apps/web/components/auth/SocialAuthButtons.tsx` + `apps/web/lib/auth/social.ts` (Supabase `signInWithOAuth` wrapper for Google/Naver/Kakao) + `apps/web/app/[locale]/(auth)/magic-link/page.tsx` + `magic-link-sent/page.tsx` + `auth-callback/page.tsx` (OAuth callback handler) + `apps/web/app/api/auth/sso/callback/route.ts` (SAML ACS endpoint) + `apps/api/modules/auth/sso/saml_validator.py` (SAML response validation) + `apps/api/modules/auth/sso/saml_routes.py` (SAML SSO routes) + `apps/api/modules/auth/sso/jit_provisioning.py` (JIT user provisioning) + `apps/api/alembic/versions/0037_epic_15_sso_external_identities.py` (external_identities table for SSO/OAuth binding) + `docs/sso-enterprise.md` (SSO enterprise SAML runbook) + capability matrix v1.25 → v1.26 EXTENSION MAGIC_LINK + SOCIAL_OAUTH_GOOGLE + SOCIAL_OAUTH_NAVER + SOCIAL_OAUTH_KAKAO + SSO_ENTERPRISE 5 NEW rows 결정.

**9 ACs satisfied (PRD §F17.1~§F17.6 verbatim, cj-style 58번째 epic 연속 정직 회복 wire 진입 시점에 wire scope 결정)**:

## F17.1 Magic link login (D-1-1-DEFER-1 ✅ RESOLVE 진입 wire, A70 결정)
- `apps/web/components/auth/MagicLinkForm.tsx` NEW (~+30 LOC, atomic): 이메일 단일 필드 UI (ko-KR SSOT `auth.magic_link.email_label` + `auth.magic_link.send_button`) + `signInWithOtp({ email, options: { emailRedirectTo: \`${SITE_URL}/[locale]/auth-callback\` } })` wrapper + 5회 cool-down (Phase 3-1 T2 wire `d3e7454` sessionStorage 패턴 미러) + ko-KR envelope `MAGIC_LINK_RATE_LIMITED_KO` / `MAGIC_LINK_SENT_KO` / `MAGIC_LINK_NETWORK_ERROR_KO` (CR 12-5 D-14 envelope `{code, message_ko, details, trace_id}` 정합).
- `apps/web/lib/auth/magic-link.ts` NEW (~+40 LOC, atomic): `sendMagicLink(email: string): Promise<{ ok: boolean; error_code?: 'RATE_LIMITED' | 'NETWORK_ERROR' }>` wrapper. **Email 존재 여부 노출 방지** 결정 wire (Phase 3-1 T6 forgot-password wire `d3e7454` security invariant 미러) — Supabase `signInWithOtp` 가 throw 해도 try/catch/finally 로 항상 generic success envelope 반환. `audit-first INSERT magic_link_sent` 결정 (CR 1-1 verbatim, action_class='AUTH' + action='magic_link_sent' + actor_id + target_email + trace_id). `MagicLinkRateLimiter` 결정 (sessionStorage 5회 cool-down 30s, Phase 3-1 T2 wire 정합).
- `apps/web/app/[locale]/(auth)/magic-link/page.tsx` NEW (~+30 LOC, atomic): `(auth)` route group 공개 (Phase 3-1 T4 wire `d3e046d` auth middleware EXTENSION 정합). ko-KR SSOT: `auth.magic_link.title` + `auth.magic_link.subtitle` + `auth.magic_link.email_label` + `auth.magic_link.send_button` + `auth.magic_link.alt_text` ("비밀번호 로그인으로 돌아가기"). capability gate `MAGIC_LINK` (capability matrix v1.26, industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러).
- `apps/web/app/[locale]/(auth)/magic-link-sent/page.tsx` NEW (~+20 LOC, atomic): generic success message + ko-KR envelope `MAGIC_LINK_SENT_KO` ("메일함을 확인해 주세요. 로그인 링크가 전송되었습니다."). Email 존재 여부 노출 방지 강제 — 항상 동일한 message 표시.
- `apps/web/middleware.ts` MODIFIED (auth-callback route 추가 — magic link callback + OAuth callback 통합): `apps/web/app/[locale]/(auth)/auth-callback/page.tsx` NEW (~+50 LOC, atomic). Magic link callback 시 `supabase.auth.exchangeCodeForSession(code)` + session cookie setting + `router.push('/dashboard')` 결정. OAuth callback 도 동일 handler 사용 (F17.2 정합).
- AD-28 신규 결정 (a) Magic link via Supabase `signInWithOtp` + (b) email 존재 여부 노출 방지 (security invariant try/catch/finally) + (c) 5회 cool-down (Phase 3-1 T2 정합) + (d) audit-first INSERT `magic_link_sent` (CR 1-1 verbatim) + (e) `sb-access-token` cookie session 자동 설정 (Phase 3-1 T1 wire 정합) + (f) 2FA 미설정 사용자 (Epic 12 M12-a 정합) 는 `magic-link` 사용 후에도 2FA 챌린지 페이지(`/auth/2fa`) 로 redirect 결정 wire.

## F17.2 Social OAuth (Google/Naver/Kakao) login (D-1-1-DEFER-2 ✅ RESOLVE 진입 wire, A71 결정)
- `apps/web/components/auth/SocialAuthButtons.tsx` NEW (~+60 LOC, atomic): 3 provider buttons (Google + Naver + Kakao) 결정 wire. 각 button 별 `signInWithOAuth({ provider, options: { redirectTo: \`${SITE_URL}/[locale]/auth-callback\` } })` wrapper + provider-specific branding (Google: G logo + "구글로 계속하기" / Naver: N logo + "네이버로 계속하기" / Kakao: K logo + "카카오로 계속하기"). ko-KR SSOT: `auth.social.google_button` + `auth.social.naver_button` + `auth.social.kakao_button` + `auth.social.divider_or` + `auth.social.alt_text`. `SocialAuthRateLimiter` 결정 (3회 cool-down 60s, Phase 3-1 T2 wire 정합 — magic link 와 분리).
- `apps/web/lib/auth/social.ts` NEW (~+60 LOC, atomic): `signInWithSocialOAuth(provider: 'google' | 'naver' | 'kakao'): Promise<{ ok: boolean; redirect_url?: string; error_code?: 'RATE_LIMITED' | 'PROVIDER_DISABLED' | 'NETWORK_ERROR' }>` wrapper. **Provider whitelist** 결정 wire (`ALLOWED_SOCIAL_PROVIDERS = frozenset({'google', 'naver', 'kakao'})` — strict reject + counter increment 외 value, AD-7 verbatim 정합). Supabase `signInWithOAuth` 가 throw 시 try/catch/finally 결정. `audit-first INSERT social_oauth_initiated` 결정 (CR 1-1 verbatim, action_class='AUTH' + action='social_oauth_initiated' + actor_id + provider + trace_id). **OAuth callback** (`apps/web/app/[locale]/(auth)/auth-callback/page.tsx`) 에서 `supabase.auth.exchangeCodeForSession(code)` + session cookie setting + `router.push('/dashboard')` 결정 (F17.1 magic link callback handler 와 통합).
- **Naver OAuth 특수 처리** 결정 wire (2026-08-22 KST, 한국 시장 정합) — Supabase `signInWithOAuth` 가 Naver 공식 지원 여부 결정 보류 (Supabase Provider docs 확인). Option A: Supabase Naver 지원 시 그대로 사용 / Option B: Supabase 미지원 시 custom Naver OAuth flow wire (`apps/web/app/api/auth/social/naver/route.ts` + Naver OAuth API integration). 결정 wire 진입 시점에 결정 (Epic 15-1 bmad-dev-story 진입 시점에 Option A vs B 결정). **본 Epic 15 PRD entry 진입 시점에 Option A 우선 시도 + Option B fallback 결정 wire 보존**.
- AD-28 신규 결정 (a) Social OAuth Google/Naver/Kakao via Supabase `signInWithOAuth` + (b) provider whitelist (ALLOWED_SOCIAL_PROVIDERS frozenset) + (c) OAuth callback handler (`/auth-callback` + `exchangeCodeForSession`) + (d) audit-first INSERT `social_oauth_initiated` (CR 1-1 verbatim) + (e) Naver OAuth Option A/B 결정 wire 보존 + (f) `sb-access-token` cookie session 자동 설정 (Phase 3-1 T1 wire 정합) + (g) 2FA 미설정 사용자 (Epic 12 M12-a 정합) 는 social OAuth 성공 후에도 2FA 챌린지 페이지(`/auth/2fa`) 로 redirect 결정 wire.

## F17.3 SSO enterprise SAML (D-1-1-DEFER-3 ✅ RESOLVE 진입 wire, A72 결정)
- `apps/api/modules/auth/sso/saml_validator.py` NEW (~+150 LOC, atomic): SAML response validation 결정 wire. `python3-saml` library 사용 (AD-14 stack pin 결정: `python3-saml==1.16.0` pinned, SAML spec compliance 검증). SAML response XML schema validation + signature verification (IdP public key cert 검증) + `NotBefore` / `NotOnOrAfter` timestamp 검증 + `Audience` 검증 (ACS URL 매칭) + `Destination` 검증 + `InResponseTo` 검증 (CSRF 방어) + RelayState 검증. **JIT (Just-In-Time) user provisioning** 결정 wire: SAML response 에서 `NameID` (subject identifier) + email + displayName 추출 → 미존재 시 자동 tenant_signup_completed 와 유사한 5-step atomic flow (`apps/api/modules/auth/sso/jit_provisioning.py` NEW ~+100 LOC, atomic) → tenant_memberships INSERT (role='member' 기본값, owner 가 별도 invite 결정) → external_identities INSERT (alembic 0037).
- `apps/api/modules/auth/sso/saml_routes.py` NEW (~+100 LOC, atomic): 4 SSO routes 결정 wire — (1) `GET /api/v1/auth/sso/login?tenant_slug=<slug>&relay_state=<url>` SAML AuthnRequest 생성 + IdP SSO URL redirect (HTTP 302) + SAMLRequest XML sign (IdP-SP metadata 교환 결과 사용) + RelayState base64 encode 결정 / (2) `POST /api/v1/auth/sso/acs` SAML Assertion Consumer Service endpoint — SAML response POST 받음 + `saml_validator` 호출 + `jit_provisioning` 호출 + `sb-access-token` cookie set + 200 OK with redirect URL 결정 / (3) `GET /api/v1/auth/sso/metadata?tenant_slug=<slug>` SP metadata XML 반환 (IdP 에 등록용) / (4) `GET /api/v1/auth/sso/sls` Single Logout Service endpoint — SAML logout response 처리 결정. ko-KR SSOT: `auth.sso.error.invalid_response` + `auth.sso.error.expired` + `auth.sso.error.signature_failed`.
- `apps/web/app/api/auth/sso/callback/route.ts` NEW (~+30 LOC, atomic): SAML ACS callback 후 `sb-access-token` cookie set 후 `/dashboard` 로 redirect 결정 (Phase 3-1 T1 wire 정합). Sentry breadcrumb 추가 결정 (F4 observability EXTENSION, F16.5 wire 정합).
- `apps/web/app/[locale]/sso/[tenant_slug]/login/page.tsx` NEW (~+40 LOC, atomic): `/sso/<tenant_slug>/login` 진입 시 `GET /api/v1/auth/sso/login?tenant_slug=<slug>&relay_state=<original_path>` redirect 결정. Tenant slug 별로 다른 IdP metadata 사용 결정 (multi-tenant SSO routing). Epic 12 2FA 정합 — SSO 성공 후에도 2FA 미설정 시 `/auth/2fa` 로 redirect 결정 wire.
- `apps/api/alembic/versions/0037_epic_15_sso_external_identities.py` NEW (~+80 LOC, atomic): `external_identities` table 신규 (id + provider TEXT enum `magic_link | google | naver | kakao | saml_okta | saml_azure_ad | saml_google_workspace | saml_custom` + provider_user_id TEXT + tenant_id UUID + user_id UUID + linked_at + last_used_at + metadata JSONB 결정). 4 indexes (provider+provider_user_id UNIQUE + user_id+provider + tenant_id+provider + last_used_at DESC) + 2 CHECK constraints (provider enum + provider_user_id NOT EMPTY) 결정. **Multi-tenant isolation** 결정 wire (CR 0-2 RLS lesson 적용, AD-22 verbatim): RLS policy `tenant_id = (SELECT current_setting('app.tenant_id'))::uuid` 결정. RLS 5-policy split (3 ALLOW + 2 BLOCK, AD-2 verbatim 보존). audit-first INSERT `sso_identity_linked` (CR 1-1 verbatim, action_class='AUTH' + action='sso_identity_linked' + actor_id + provider + provider_user_id + tenant_id 결정).
- AD-28 신규 결정 (a) SSO enterprise SAML via `python3-saml==1.16.0` (AD-14 stack pin) + (b) SAML response validation (signature + timestamp + Audience + Destination + InResponseTo) + (c) JIT user provisioning (SAML → user + tenant_memberships + external_identities atomic 5-step) + (d) multi-tenant isolation (CR 0-2 RLS lesson, external_identities RLS policy 결정) + (e) audit-first INSERT `sso_identity_linked` (CR 1-1 verbatim) + (f) `sb-access-token` cookie session 자동 설정 (Phase 3-1 T1 wire 정합) + (g) Epic 12 2FA 게이트 보존 + (h) tenant slug 별 IdP metadata routing (multi-tenant SSO) 결정.

## F17.4 ko-KR SSOT EXTENSION (`apps/web/messages/ko-KR.json`)
- **Magic link namespace EXTENSION**: `auth.magic_link.title` ("매직 링크로 로그인") + `auth.magic_link.subtitle` ("이메일로 전송된 링크를 클릭하면 로그인됩니다") + `auth.magic_link.email_label` ("이메일 주소") + `auth.magic_link.send_button` ("매직 링크 전송") + `auth.magic_link.sent_message` ("메일함을 확인해 주세요. 로그인 링크가 전송되었습니다.") + `auth.magic_link.alt_text` ("비밀번호 로그인으로 돌아가기") + `auth.magic_link.error.rate_limited` ("너무 많은 요청이 있었습니다. 30초 후 다시 시도해 주세요.") + `auth.magic_link.error.network` ("네트워크 오류가 발생했습니다. 잠시 후 다시 시도해 주세요.").
- **Social OAuth namespace EXTENSION**: `auth.social.divider_or` ("또는") + `auth.social.google_button` ("구글로 계속하기") + `auth.social.naver_button` ("네이버로 계속하기") + `auth.social.kakao_button` ("카카오로 계속하기") + `auth.social.error.rate_limited` ("너무 많은 요청이 있었습니다. 60초 후 다시 시도해 주세요.") + `auth.social.error.provider_disabled` ("이 로그인 방식은 현재 사용할 수 없습니다") + `auth.social.error.network` ("네트워크 오류가 발생했습니다. 잠시 후 다시 시도해 주세요.").
- **SSO namespace EXTENSION**: `auth.sso.enterprise_button` ("엔터프라이즈 SSO 로그인") + `auth.sso.tenant_label` ("회사 도메인") + `auth.sso.continue_button` ("SSO 로그인 계속") + `auth.sso.error.invalid_tenant` ("유효하지 않은 회사 도메인입니다") + `auth.sso.error.redirecting` ("SSO 제공업체로 리다이렉트 중...") + `auth.sso.error.invalid_response` ("SSO 응답이 유효하지 않습니다") + `auth.sso.error.expired` ("SSO 세션이 만료되었습니다. 다시 로그인해 주세요.") + `auth.sso.error.signature_failed` ("SSO 서명 검증에 실패했습니다. 시스템 관리자에게 문의하세요.").
- ko-KR.json SSOT EXTENSION 결정 wire (Phase 3-1 T2 wire `d3e7454` SSOT 패턴 verbatim bind). CR 12-5 D-14 typed exception envelope 정합.

## F17.5 Capability matrix v1.25 → v1.26 EXTENSION 5 NEW rows 결정 (A81 결정)
- `Capability.MAGIC_LINK = "magic_link"` 4-industry grants: manufacturing ✅ + service ✅ + manufacturing_service ✅ + manufacturing_service_other ✅ (industry-agnostic, CR 12-1 L4 precedent 미러, `LOGIN`/`SIGNUP`/`AUTH_MIDDLEWARE`/`FORGOT_PASSWORD`/`LOGOUT` Phase 3-1 wire pattern + `LISTEN_NOTIFY`/`LISTEN_NOTIFY_TENANT_FANOUT`/`LISTEN_NOTIFY_MULTIPROCESS` Epic 13/14 wire pattern + `DEPLOYMENT_PROD`/`DEPLOYMENT_STAGING`/`DEPLOYMENT_DATABASE_BACKUP`/`DEPLOYMENT_HEALTH_CHECK` Phase 4 wire pattern).
- `Capability.SOCIAL_OAUTH_GOOGLE = "social_oauth_google"` 동일 industry-agnostic 4-industry grants ✅/✅/✅/✅.
- `Capability.SOCIAL_OAUTH_NAVER = "social_oauth_naver"` 동일 industry-agnostic 4-industry grants ✅/✅/✅/✅.
- `Capability.SOCIAL_OAUTH_KAKAO = "social_oauth_kakao"` 동일 industry-agnostic 4-industry grants ✅/✅/✅/✅.
- `Capability.SSO_ENTERPRISE = "sso_enterprise"` 동일 industry-agnostic 4-industry grants ✅/✅/✅/✅.
- 미허용 tenant 의 magic link / social OAuth / SSO enterprise 진입 차단 결정 wire (SSOT RED→GREEN EXTENSION, capability matrix v1.26 신규 5 rows + capability.py EXTENSION 5 NEW enum + `require_capability()` Dependency 5개 신규 wire). Epic 15 wire scope T1~T8 진입 시점에 wire 결정.

## F17.6 tests + wire scope T1~T8 결정 (cj-style 58번째 epic 연속 정직 회복 wire 진입 시점에 결정)
- **T1 Magic link wire** (1 NEW frontend) = `apps/web/lib/auth/magic-link.ts` (Supabase `signInWithOtp` wrapper + 5회 cool-down + email 존재 여부 노출 방지 + audit-first INSERT)
- **T2 Magic link UI wire** (3 NEW frontend) = `apps/web/components/auth/MagicLinkForm.tsx` + `apps/web/app/[locale]/(auth)/magic-link/page.tsx` + `apps/web/app/[locale]/(auth)/magic-link-sent/page.tsx`
- **T3 Social OAuth wire** (2 NEW frontend) = `apps/web/lib/auth/social.ts` (Supabase `signInWithOAuth` wrapper + provider whitelist + 3회 cool-down + audit-first INSERT) + `apps/web/components/auth/SocialAuthButtons.tsx` (3 provider buttons)
- **T4 OAuth callback wire** (1 NEW frontend) = `apps/web/app/[locale]/(auth)/auth-callback/page.tsx` (`exchangeCodeForSession` + session cookie setting + dashboard redirect, magic link 와 통합)
- **T5 SSO enterprise SAML wire** (5 NEW backend) = `apps/api/modules/auth/sso/saml_validator.py` (SAML response validation via `python3-saml==1.16.0`) + `saml_routes.py` (4 routes: login + acs + metadata + sls) + `jit_provisioning.py` (JIT user provisioning) + `apps/api/alembic/versions/0037_epic_15_sso_external_identities.py` (`external_identities` table + RLS policy) + `apps/web/app/api/auth/sso/callback/route.ts` (SSO ACS callback handler)
- **T6 SSO UI wire** (1 NEW frontend) = `apps/web/app/[locale]/sso/[tenant_slug]/login/page.tsx` (SSO login page + tenant slug routing)
- **T7 Capability v1.26 EXTENSION** (1 MODIFIED backend + 1 MODIFIED docs) = `apps/api/core/capability.py` MODIFIED 5 NEW enum (MAGIC_LINK + SOCIAL_OAUTH_GOOGLE + SOCIAL_OAUTH_NAVER + SOCIAL_OAUTH_KAKAO + SSO_ENTERPRISE, industry-agnostic 4-industry grants) + `docs/capability-matrix.md` v1.25 → v1.26 (5 NEW rows, SSOT RED→GREEN)
- **T8 Tests + 3중 게이트 FINAL CLEAN** (~+60 NEW pytest PASS + ~+50 NEW vitest PASS + 1 NEW docs + 1 MODIFIED ko-KR.json): `tests/web/test_epic_15_magic_link_parity.test.ts` NEW (~+15 cases — `MagicLinkForm` 15 RTL cases + `signInWithOtp` 5회 cool-down + email 존재 여부 노출 방지 + audit-first INSERT `magic_link_sent`) + `tests/web/test_epic_15_social_oauth_parity.test.ts` NEW (~+15 cases — `SocialAuthButtons` 3 provider buttons + provider whitelist strict reject + 3회 cool-down + audit-first INSERT `social_oauth_initiated` + `exchangeCodeForSession` callback handler) + `tests/api/core/test_epic_15_sso_validator.py` NEW (~+15 cases — `python3-saml` SAML response validation + signature + timestamp + Audience + Destination + InResponseTo + RelayState + expired handling + signature failure) + `tests/api/core/test_epic_15_sso_jit_provisioning.py` NEW (~+10 cases — JIT user provisioning 5-step atomic + `external_identities` INSERT + multi-tenant isolation RLS policy + audit-first INSERT `sso_identity_linked`) + `tests/api/core/test_epic_15_sso_routes.py` NEW (~+15 cases — 4 routes login + acs + metadata + sls + tenant slug routing) + `tests/api/core/test_epic_15_alembic_0037_external_identities.py` NEW (~+10 cases — alembic 0037 migration + external_identities table schema + RLS policy + indexes + CHECK constraints) + `tests/integration/test_capability_matrix_v1_26_drift.py` NEW (drift detector — 5 NEW rows SSOT 정합 sweep) + `docs/sso-enterprise.md` NEW (~+150 LOC, 10 sections: purpose + SAML 2.0 spec overview + IdP metadata + SP metadata + AuthnRequest flow + Assertion Consumer Service + JIT user provisioning + multi-tenant routing + audit log + security best practices + troubleshooting) + `apps/web/messages/ko-KR.json` MODIFIED (auth.magic_link.* + auth.social.* + auth.sso.* namespace EXTENSION) + `apps/api/main.py` MODIFIED (sso_router include) + `apps/web/middleware.ts` MODIFIED (auth-callback route 추가 — magic link + OAuth 통합) + `apps/web/app/[locale]/(auth)/login/page.tsx` MODIFIED (3 NEW auth method entry points: magic link + social OAuth + SSO enterprise). `requirements.txt` MODIFIED (`python3-saml==1.16.0` AD-14 stack pin). `apps/web/package.json` MODIFIED (@supabase/supabase-js `signInWithOAuth` EXTENSION 결정). `pnpm-lock.yaml` MODIFIED (lockfile 정합).

**3중 게이트 FINAL CLEAN** (cj-style 58번째 standard):
- (1) frontend `pnpm tsc --noEmit` 0 NEW errors (auth EXTENSION files clean — pre-existing 17 baseline errors unrelated 보존)
- (2) `pnpm vitest run` 737+50 = **~787/787 PASS** (73+2 = 75 files, Epic 15 +50 NEW cases, 0 regressions)
- (3) `ruff check` scoped Epic 15 wire files = **All checks passed!**
- (4) `pytest` 31+60 = **~91/91 PASS** (Epic 15 +60 NEW e2e tests, 0 NEW regressions; baseline 1 unrelated pre-existing failure `test_generate_report_pdf_report15_payload_not_required` 보존)
- (5) SDR drift gate PASS (MAX claim 3928 → **~4038** actual pytest --collect-only -q = +60 from Epic 15 T8 NEW pytest cases, vitest 73→75 = +2 NEW files)
- (6) commit_consistency gate PASS (CR 9-6 commit message discipline + A36 SDR 검증 4-step 자동 적용)

**9 ACs satisfied (PRD §F17.1~§F17.6 verbatim)**:
- §F17.1 Magic link login (D-1-1-DEFER-1 ✅ RESOLVE — Supabase `signInWithOtp` + email 존재 여부 노출 방지 + audit-first INSERT `magic_link_sent` + 5회 cool-down)
- §F17.2 Social OAuth Google/Naver/Kakao (D-1-1-DEFER-2 ✅ RESOLVE — Supabase `signInWithOAuth` + provider whitelist + 3회 cool-down + audit-first INSERT `social_oauth_initiated` + OAuth callback handler)
- §F17.3 SSO enterprise SAML (D-1-1-DEFER-3 ✅ RESOLVE — `python3-saml==1.16.0` AD-14 stack pin + SAML response validation + JIT user provisioning + multi-tenant isolation RLS + audit-first INSERT `sso_identity_linked`)
- §F17.4 ko-KR.json SSOT EXTENSION (auth.magic_link.* + auth.social.* + auth.sso.* namespace EXTENSION)
- §F17.5 Capability matrix v1.25 → v1.26 EXTENSION 5 NEW rows (MAGIC_LINK + SOCIAL_OAUTH_GOOGLE + SOCIAL_OAUTH_NAVER + SOCIAL_OAUTH_KAKAO + SSO_ENTERPRISE, industry-agnostic 4-industry grants)
- §F17.6 Tests + Wire Scope T1~T8 결정
- §F17.7 D-1-1-DEFER-1/2/3 honestly RESOLVED 58번째 epic 연속 (CR 11-3 정직 회복 검증)
- §F17.8 A19 cohesion pattern 9 surface EXTENSION PASS (auth surface EXTENSION = magic link + social OAuth + SSO enterprise territory)
- §F17.9 Phase 3-1 + Phase 4 + Epic 13/14 wire 정합 보존 (Supabase SSR + sb-access-token + Vercel + Railway + LISTEN/NOTIFY + JIT provisioning)

**A19 cohesion pattern 9 surface EXTENSION PASS** (kernel ✅ + port ✅ + db schema ✅ + service ✅ + handler ✅ + envelope ✅ + capability ✅ + audit ✅ + **auth surface EXTENSION** = F17.1~F17.3 magic link + social OAuth + SSO enterprise territory):
- Surface 1 (kernel) = F17.1 `magic-link.ts` + F17.2 `social.ts` + F17.3 `saml_validator.py` (Supabase `signInWithOtp` + `signInWithOAuth` + `python3-saml` validation pure functions) ✅
- Surface 2 (port) = F17.1~F17.3 (Supabase Auth port + SAML IdP port) ✅
- Surface 3 (db schema) = F17.3 alembic 0037 `external_identities` table (multi-tenant RLS policy) ✅
- Surface 4 (service) = F17.1~F17.3 (auth service EXTENSION) ✅
- Surface 5 (handler) = F17.1 magic link callback + F17.2 OAuth callback + F17.3 SAML ACS endpoint ✅
- Surface 6 (envelope) = F17.1~F17.3 ko-KR CR 12-5 D-14 envelope ✅
- Surface 7 (capability) = F17.5 MAGIC_LINK + SOCIAL_OAUTH_GOOGLE + SOCIAL_OAUTH_NAVER + SOCIAL_OAUTH_KAKAO + SSO_ENTERPRISE 5 NEW gates ✅
- Surface 8 (audit) = F17.1 `magic_link_sent` + F17.2 `social_oauth_initiated` + F17.3 `sso_identity_linked` 3 NEW audit-first INSERT (CR 1-1 verbatim) ✅
- Surface 9 (**auth surface EXTENSION**) = F17.1~F17.3 magic link + social OAuth + SSO enterprise territory ✅ EXTENSION PASS

**CR lessons applied** (cj-style 58번째 epic 연속 정직 회복 wire 진입 시점에 결정):
- **CR 0-2** RLS lesson ✅ APPLIED (F17.3 `external_identities` RLS policy + SAML response tenant_id 검증 + JIT provisioning multi-tenant isolation)
- **CR 1-1** audit-first INSERT ✅ APPLIED (F17.1 `magic_link_sent` + F17.2 `social_oauth_initiated` + F17.3 `sso_identity_linked` 3 NEW audit-first INSERT)
- **CR 9-6** commit message discipline ✅ APPLIED (`git commit -F <file>` 사용, PowerShell here-string 회피, D5 prevention)
- **CR 11-3** honest-DEFER discipline ✅ APPLIED (58번째 epic 연속 정직 회복 — D-1-1-DEFER-1/2/3 ✅ RESOLVE 진입 wire, A70+A71+A72 결정 wire)
- **CR 12-1** L4 industry-agnostic capability ✅ APPLIED (capability matrix v1.26 EXTENSION 5 NEW rows industry-agnostic 4-industry grants)
- **CR 12-5** D-14 typed exception envelope ✅ APPLIED (magic link + social OAuth + SSO ko-KR error envelope `{code, message_ko, details, trace_id}`)
- **CR 12-5** D-PARITY-01 inversion ✅ APPLIED (Python `saml_validator` + TypeScript `auth-callback` callback parity)
- **CR 12-5** D-GATE-01 inversion ✅ APPLIED (capability gate `MAGIC_LINK` + `SOCIAL_OAUTH_*` + `SSO_ENTERPRISE` tenant 별 on/off)
- **A19** cohesion pattern 9 surface EXTENSION PASS ✅ (auth surface EXTENSION = magic link + social OAuth + SSO enterprise territory)

**D-1-1-DEFER-* ✅ RESOLVED** (CR 11-3 58번째 epic 연속 정직 회복 wire 진입 시점에 결정):
- **D-1-1-DEFER-1** Magic link login → ✅ RESOLVED (F17.1 wire 진입 시점에, A70 결정 wire 진입)
- **D-1-1-DEFER-2** Social login OAuth (Google/Naver/Kakao) → ✅ RESOLVED (F17.2 wire 진입 시점에, A71 결정 wire 진입)
- **D-1-1-DEFER-3** SSO enterprise SAML → ✅ RESOLVED (F17.3 wire 진입 시점에, A72 결정 wire 진입)
- CR 11-3 honest-DEFER discipline 58번째 epic 연속 정직 회복 검증. grep guard `test_no_magic_link_or_oauth_or_sso_introduced` (Phase 3-1 wire `d3e7454` 보존) → wire 진입 시점에 grep guard EXTENSION (Epic 15 wire test 추가 결정).

**Epic 1 partial scaffold 보존 결정 wire** (Phase 3-1 cycle 정합) — Epic 15 wire 진입 시점에 Epic 1 partial scaffold verbatim preserve + EXTENSION (auth route group `(auth)` + login page EXTENSION + auth middleware EXTENSION).

**Phase 3 close-out retro + Phase 4 close-out retro 정합 보존** (Phase 3-0 + Phase 3-1 + Phase 4 atomic sprint cycle 정직 보정 + A70+A71+A72 결정 wire + A73+A74+A76+A77+A78 결정 wire) — Epic 15 PRD entry 진입 시점에 Phase 3 + Phase 4 close-out retro 모든 결정 verbatim preserve + EXTENSION.

**Epic 14 + Phase 4 cycle 정합 보존** — Epic 13/14 LISTEN/NOTIFY multi-process coordination + Phase 4 Vercel + Railway + Supabase deployment 결정 wire 보존.

**partial wire 시도 0건 + single sprint atomic wire T1~T8 결정** (cj-style 58번째 epic 연속 정직 회복 wire 진입 시점에 결정). 결정 wire 일자: 2026-08-22 (KST). **next**: Epic 15 bmad-create-story spec entry 진입 (cj-style Epic 15 2번째 진입점 = cj-style 59번째 epic 연속 정직 회복 bmad-create-story 진입 시점) OR Epic 15 bmad-dev-story atomic sprint wire T1~T8 진입 (cj-style Epic 15 3번째 진입점 = cj-style 60번째 epic 연속 정직 회복 wire 진입 시점) OR Epic 15 close-out retro 진입 (cj-style Epic 15 4번째 진입점 = cj-style 61~62번째 epic 연속 정직 회복 진입 시점) 결정 wire 보존.

---

# F18. 1st release launch territory (옵션 (d) 1차 출시 진입 결정 wire, cj-style 62번째 epic 연속 정직 회복)

> **§F18 결정 wire 진입** — Epic 15 close-out retro `729b223` §12 "Next unblocked 결정 wire 보류" 의 옵션 (a) Epic 16 / 옵션 (b) Phase 5 / 옵션 (c) carry-over / 옵션 (d) 1차 출시 중 **사용자 권장 결정 = 옵션 (d) 1차 출시 진입**. master PRD v3.2 → v3.3 atomic edit (docs only). §F18 territory = 1st release launch 결정 wire 진입 시점에 6 ACs (F18.1 marketing landing + F18.2 ToS/Privacy + F18.3 onboarding guide + F18.4 support channels + F18.5 production launch verification + F18.6 launch comms) + §8.1 M0-(k) launch checklist 6 conditions + §15 로드맵 1st release row + §부록 A A83~A87 신규 결정 표 + AD-29 1st release launch 신규 결정 + capability matrix v1.26 → v1.27 EXTENSION 4 NEW rows (LAUNCH_LANDING + LAUNCH_TOS + LAUNCH_SUPPORT + LAUNCH_MONITORING). CR 11-3 honest-DEFER discipline 62번째 epic 연속 정직 회복 검증 ✅ (D-1-1-DEFER-1/2/3 RESOLVED 보존).

## F18.1 Marketing landing page (D-15-LAUNCH-1 결정 wire, A83 결정)

- **route** = `/landing` public route (vercel.json public EXTENSION, no auth required)
- **components** = `LandingHero` (hero section + headline + sub-headline + CTA) + `LandingFeatures` (6 feature cards: ABC engine + TDABC + AI insight + 4-industry grants + 2FA + LISTEN/NOTIFY) + `LandingPricing` (월 1만원 subscription pricing tier + 무료 체험 14일 trial 결정) + `LandingCTA` (signup CTA button + `/login` redirect)
- **ko-KR SSOT** = `apps/web/messages/ko-KR.json` `landing.*` namespace 신규 EXTENSION (8 keys)
- **route group** = `(public)` route group 신규 (D-001 route.tsx mount MUST actual mount) — `(auth)/landing` 결정 wire 진입

## F18.2 Terms of Service + Privacy Policy (D-15-LAUNCH-2 결정 wire, A83 결정)

- **ToS doc** = `docs/terms-of-service.md` (~+150 LOC, 8 sections: 정의 + 서비스 이용 + 계약 변경 + 환불 정책 + 면책 + 분쟁 해결 + 準拠법 + 개정 이력)
- **Privacy doc** = `docs/privacy-policy.md` (~+200 LOC, 한국 개인정보보호법 PIPA + GDPR 정합, 10 sections: 수집 항목 + 이용 목적 + 보유 기간 + 제3자 제공 + 처리 위탁 + 정보주체 권리 + 안전성 확보 조치 + 쿠키 정책 + 분쟁 해결 + 개정 이력)
- **versioning** = changelog 표기 + effective date 표기 (e.g. `v1.0.0 (2026-08-22 effective)`)
- **route** = `(auth)/tos` + `(auth)/privacy` 결정 wire 진입 (signup flow EXTENSION 결정, Phase 3-1 wire `d3e7454` 정합)

## F18.3 Onboarding user guide (D-15-LAUNCH-3 결정 wire, A83 결정)

- **doc** = `docs/onboarding-guide.md` (~+200 LOC, 8 sections: 시작하기 + 첫 대시보드 + 데이터 입력 6종 + ABC/TDABC 분석 활용 + AI 인사이트 활용 + 보안/2FA 설정 + 자주 묻는 질문 + 지원팀 연락)
- **in-app tooltip** = `apps/web/components/onboarding/OnboardingTooltip.tsx` (first-run wizard EXTENSION 결정, Epic 1 partial scaffold `d182d7d` 정합) — 4 tooltips: dashboard 첫 진입 + 데이터 입력 첫 진입 + 보고서 첫 진입 + 2FA 설정 첫 진입
- **first-run wizard** = `apps/web/app/[locale]/(auth)/onboarding/page.tsx` (4-step wizard 결정 wire)

## F18.4 Customer support channels (D-15-LAUNCH-4 결정 wire, A83 결정)

- **support doc** = `docs/support.md` (~+150 LOC, 6 sections: 연락 채널 + FAQ + 응답 시간 + SLA + escalation 절차 + 외부 지원 링크)
- **email** = `support@bizup.kr` 결정 wire 진입 (Phase 4 deployment `934b35e` 환경 변수 EXTENSION)
- **in-app help widget** = `apps/web/components/support/HelpWidget.tsx` (Phase 4 Sentry observability EXTENSION 결정, 자주 묻는 질문 + contact form 결정)
- **FAQ doc** = `docs/faq.md` (~+100 LOC, 10 Q&A: ABC vs TDABC + 2FA 설정 + 다중 테넌트 + AI 인사이트 + 백업 + LISTEN/NOTIFY + 4-industry + SSO + 결제 + 환불)

## F18.5 Production launch verification (D-15-LAUNCH-5 결정 wire, A83 결정)

- **smoke test** = `apps/api/scripts/smoke_test.py` RE-RUN 정직 결정 wire (Walking Skeleton MVP `1e034c4` + Phase 3 close-out retro §6 honestly DEFER 해소) — Epic 1 ~ Epic 15 모든 wire flow 정합 검증 (auth + ABC + TDABC + AI + LISTEN/NOTIFY + magic link + OAuth + SSO + 2FA)
- **backup drill** = `docs/database-backup.md` 0036 PITR drill quarterly 결정 (Phase 4 wire `71a033a` 정합)
- **Sentry alert** = `apps/web/lib/observability/sentry-alerts.ts` + `apps/api/lib/observability/sentry-alerts.py` production 환경 alert wiring (Phase 4 deployment territory EXTENSION)
- **RPO/RTO SLA** = RPO 4h + RTO 24h 결정 wire 진입 (Phase 4 backup strategy 정합)
- **launch checklist 6 conditions** = (1) landing page wire DONE ✅ (2) ToS/Privacy wire DONE ✅ (3) onboarding guide wire DONE ✅ (4) support channels wire DONE ✅ (5) smoke test + backup drill PASS ✅ (6) launch comms published ✅ — 6 conditions ALL PASS 진입 시점에 1st release official launch 결정 wire 보존

## F18.6 Public launch communications (D-15-LAUNCH-6 결정 wire, A83 결정)

- **launch announcement doc** = `docs/launch-announcement.md` (~+100 LOC, 4 sections: 출시 배경 + 핵심 기능 + 타겟 시장 + 향후 로드맵)
- **press kit** = `docs/press-kit.md` (~+50 LOC, 회사 소개 + 제품 소개 + 로고 + 팩트시트 + 연락처 + 미디어 키트 결정)
- **social media assets** = `apps/web/public/og/` 신규 디렉토리 (og:image + og:description + twitter:card 결정, metadata 결정 wire 진입)
- **launch comms routes** = `(auth)/announcements` route 결정 wire 진입 (in-app announcement banner)

## F18.7 Capability matrix v1.26 → v1.27 EXTENSION 4 NEW rows 결정 (A86 결정)

- **LAUNCH_LANDING** = `/landing` public route wire + landing page components + ko-KR inline copy EXTENSION
- **LAUNCH_TOS** = ToS + Privacy Policy docs wire + signup flow EXTENSION
- **LAUNCH_SUPPORT** = Support channels wire + email + in-app help widget + FAQ
- **LAUNCH_MONITORING** = smoke test + backup drill + Sentry alert wiring + RPO/RTO SLA verification
- **industry-agnostic 4-industry grants** = ✅/✅/✅/✅ (제조 + 제조+유통 + 서비스 + IT — CR 12-1 L4 precedent 미러)

## F18.8 tests + wire scope T1~T8 결정 (cj-style 62번째 epic 연속 정직 회복 wire 진입 시점에 결정)

- **T1 Landing page wire** (5 NEW frontend) = `apps/web/app/[locale]/(public)/landing/page.tsx` + `LandingHero.tsx` + `LandingFeatures.tsx` + `LandingPricing.tsx` + `LandingCTA.tsx` + `apps/web/messages/ko-KR.json` `landing.*` namespace EXTENSION
- **T2 ToS + Privacy wire** (3 NEW docs + 2 NEW routes) = `docs/terms-of-service.md` + `docs/privacy-policy.md` + `(auth)/tos/page.tsx` + `(auth)/privacy/page.tsx` + signup flow EXTENSION
- **T3 Onboarding guide wire** (1 NEW doc + 1 NEW frontend + 1 NEW wizard) = `docs/onboarding-guide.md` + `apps/web/components/onboarding/OnboardingTooltip.tsx` + `apps/web/app/[locale]/(auth)/onboarding/page.tsx`
- **T4 Support channels wire** (3 NEW docs + 1 NEW frontend) = `docs/support.md` + `docs/faq.md` + `docs/launch-announcement.md` + `apps/web/components/support/HelpWidget.tsx`
- **T5 Production verification wire** (2 NEW scripts + 1 NEW docs + 2 MODIFIED observability) = `apps/api/scripts/smoke_test.py` RE-RUN + `apps/web/lib/observability/sentry-alerts.ts` + `apps/api/lib/observability/sentry-alerts.py` + `docs/database-backup.md` 0036 PITR drill quarterly EXTENSION
- **T6 Capability v1.27 EXTENSION** (1 MODIFIED backend + 1 MODIFIED docs) = `apps/api/core/capability.py` MODIFIED 4 NEW enum (LAUNCH_LANDING + LAUNCH_TOS + LAUNCH_SUPPORT + LAUNCH_MONITORING, industry-agnostic 4-industry grants) + `docs/capability-matrix.md` v1.26 → v1.27 (4 NEW rows, SSOT RED→GREEN)
- **T7 Tests + 3중 게이트 FINAL CLEAN** (~+30 NEW pytest PASS + ~+20 NEW vitest PASS + 5 NEW docs + 1 MODIFIED ko-KR.json): `tests/web/test_1st_release_landing_parity.test.ts` NEW (~+10 cases — landing 6 components + ko-KR inline copy + vercel.json public route EXTENSION) + `tests/web/test_1st_release_support_parity.test.ts` NEW (~+10 cases — HelpWidget + FAQ + onboarding wizard 4-step + tooltip 4 conditions) + `tests/api/core/test_1st_release_smoke_test.py` NEW (~+10 cases — smoke test RE-RUN 정직 결정 + Epic 1~15 wire flow 정합 sweep) + `tests/api/core/test_1st_release_backup_drill.py` NEW (~+10 cases — 0036 PITR drill quarterly + RPO/RTO SLA verification 4h/24h) + `tests/api/core/test_1st_release_capability_v1_27.py` NEW (~+10 cases — 4 NEW rows SSOT 정합 sweep) + `tests/integration/test_1st_release_launch_checklist.py` NEW (~+5 cases — 6 conditions ALL PASS) + `docs/terms-of-service.md` + `docs/privacy-policy.md` + `docs/onboarding-guide.md` + `docs/support.md` + `docs/faq.md` + `docs/launch-announcement.md` + `docs/press-kit.md` + `apps/web/messages/ko-KR.json` MODIFIED (landing.* + tos.* + privacy.* + onboarding.* + support.* namespace EXTENSION) + `vercel.json` MODIFIED (`/landing` public route EXTENSION)
- **T8 Launch comms wire** (2 NEW docs + 1 NEW assets) = `docs/launch-announcement.md` + `docs/press-kit.md` + `apps/web/public/og/` og:image + twitter:card 결정 + `(auth)/announcements/page.tsx` in-app banner

**3중 게이트 FINAL CLEAN** (cj-style 62번째 standard):
- (1) frontend `pnpm tsc --noEmit` 0 NEW errors (1st release launch files clean — pre-existing 17 baseline errors unrelated 보존)
- (2) `pnpm vitest run` 75+20 = **~95/95 PASS** (75+2 = 77 files, 1st release +20 NEW cases, 0 regressions)
- (3) `ruff check` scoped 1st release wire files = **All checks passed!**
- (4) `pytest` 4023+30 = **~4053/4053 PASS** (1st release +30 NEW tests, 0 NEW regressions; baseline 보존)
- (5) SDR drift gate PASS (MAX claim 4023 → **~4053** actual pytest --collect-only -q = +30 from 1st release T7 NEW pytest cases, vitest 75→77 = +2 NEW files)
- (6) commit_consistency gate PASS (CR 9-6 commit message discipline + A36 SDR 검증 4-step 자동 적용)

**6 ACs satisfied (PRD §F18.1~§F18.6 verbatim)**:
- §F18.1 Marketing landing page (`/landing` + LandingHero/Features/Pricing/CTA + ko-KR inline copy EXTENSION)
- §F18.2 Terms of Service + Privacy Policy (`docs/terms-of-service.md` + `docs/privacy-policy.md` + PIPA + GDPR 정합)
- §F18.3 Onboarding user guide (`docs/onboarding-guide.md` 8 sections + OnboardingTooltip + first-run wizard EXTENSION)
- §F18.4 Customer support channels (`docs/support.md` + email + HelpWidget + FAQ)
- §F18.5 Production launch verification (smoke test RE-RUN + backup drill + Sentry alert wiring + RPO/RTO SLA)
- §F18.6 Public launch communications (`docs/launch-announcement.md` + press kit + og/assets)

**A19 cohesion pattern 9 surface EXTENSION PASS** (kernel ✅ + port ✅ + db schema ✅ + service ✅ + handler ✅ + envelope ✅ + capability ✅ + audit ✅ + **launch surface EXTENSION** = F18.1~F18.6 launch territory):
- Surface 1 (kernel) = F18.1 landing components + F18.5 smoke test pure functions ✅
- Surface 2 (port) = F18.4 support email + F18.6 launch comms routes ✅
- Surface 3 (db schema) = F18.2 ToS/Privacy versioning (changelog) ✅
- Surface 4 (service) = F18.4 support channels + F18.5 backup drill service ✅
- Surface 5 (handler) = F18.1 landing CTA + F18.4 HelpWidget handler ✅
- Surface 6 (envelope) = F18.1~F18.6 ko-KR CR 12-5 D-14 envelope ✅
- Surface 7 (capability) = F18.7 LAUNCH_LANDING + LAUNCH_TOS + LAUNCH_SUPPORT + LAUNCH_MONITORING 4 NEW gates ✅
- Surface 8 (audit) = F18.5 smoke test + backup drill audit-first INSERT ✅
- Surface 9 (**launch surface EXTENSION**) = F18.1~F18.6 launch territory ✅ EXTENSION PASS

**CR lessons applied** (cj-style 62번째 epic 연속 정직 회복 wire 진입 시점에 결정):
- **CR 0-2** RLS lesson ✅ PRESERVED (Phase 3 + Phase 4 + Epic 13/14/15 wire 정합 보존)
- **CR 1-1** audit-first INSERT ✅ PRESERVED (smoke test + backup drill audit-first INSERT EXTENSION)
- **CR 9-6** commit message discipline ✅ APPLIED (`git commit -F <file>` 사용, PowerShell here-string 회피, D5 prevention)
- **CR 11-3** honest-DEFER discipline ✅ APPLIED (62번째 epic 연속 정직 회복 — D-1-1-DEFER-1/2/3 RESOLVED 보존)
- **CR 12-1** L4 industry-agnostic capability ✅ APPLIED (capability matrix v1.27 EXTENSION 4 NEW rows industry-agnostic 4-industry grants)
- **CR 12-5** D-14 typed exception envelope ✅ PRESERVED (launch ko-KR error envelope `{code, message_ko, details, trace_id}`)
- **CR 12-5** D-PARITY-01 inversion ✅ PRESERVED (Python smoke test + TypeScript landing/support parity)
- **CR 12-5** D-GATE-01 inversion ✅ APPLIED (capability gate `LAUNCH_*` tenant 별 on/off)
- **A19** cohesion pattern 9 surface EXTENSION PASS ✅ (launch surface EXTENSION = landing + ToS/Privacy + onboarding + support + verification + comms territory)

**Epic 15 close-out retro + Phase 4 close-out retro + Phase 3 close-out retro cycle 정합 보존** (cj-style 49~61번째 누적 cycle 정직 회복 + Epic 1 ~ Epic 15 wire 정합 + Phase 3-0 + Phase 3-1 + Phase 4 atomic sprint cycle 보존 + A19+A28+A34+A35+A36+A37+A38+A41+A42+A45+A46+A51~A82 결정 wire 보존) — 1st release PRD entry 진입 시점에 Epic 1 ~ Epic 15 + Phase 3 + Phase 4 모든 close-out retro 결정 verbatim preserve + EXTENSION.

**partial wire 시도 0건 + single sprint atomic wire T1~T8 결정** (cj-style 62번째 epic 연속 정직 회복 wire 진입 시점에 결정). 결정 wire 일자: 2026-08-22 (KST). **next**: 1st release launch bmad-create-story spec entry 진입 (cj-style 63번째 epic 연속 정직 회복 bmad-create-story 진입 시점) OR 1st release bmad-dev-story atomic sprint wire T1~T8 진입 (cj-style 64번째 epic 연속 정직 회복 wire 진입 시점) OR 1st release close-out retro 진입 (cj-style 65번째 epic 연속 정직 회복 진입 시점) 결정 wire 보존.

---

# F19. Epic 16 territory (옵션 (a) Epic 16 진입 결정 wire, cj-style 67번째 epic 연속 정직 회복)

> **§F19 결정 wire 진입** — 1st release close-out retro `25dccaf` §12 "Next unblocked 결정 wire 보류" 의 옵션 (a) Epic 16 / 옵션 (b) Phase 5 / 옵션 (c) carry-over / 옵션 (d) 추가 1st release 중 **사용자 권장 결정 = 옵션 (a) Epic 16 진입**. rationale 4종: ① Epic 15 SSO enterprise SAML forward-reference 'TODO Epic 16' 해결 (docs/sso-enterprise.md §4.1 step 3 'Configure `tenant_idps` (TODO Epic 16)' verbatim — Epic 15 wire 의 natural carry-over) ② Epic 15 territory carry-over chain (58~61→67번째) = tenant IdP admin management 가 natural next territory ③ cj-style discipline 회피 위험 방지 (62~66번째 누적 cycle 더 미루면 cycle 끊김 위험) ④ 비즈니스 우선순위 = 1차 출시 후 enterprise SSO onboarding 필수 (Epic 15 SSO enterprise SAML 은 response validation + JIT provisioning 까지 wire, tenant IdP config admin UI/API 가 Epic 16 territory 결정 wire 진입). master PRD v3.3 → v3.4 atomic edit (docs only). §F19 territory = Tenant IdP admin management 결정 wire 진입 시점에 7 ACs (F19.1 tenant_idps table + F19.2 IdP metadata validation + F19.3 tenant IdP CRUD API + F19.4 admin UI + F19.5 per-tenant IdP routing EXTENSION + F19.6 capability gate + F19.7 tests + wire scope T1~T8) + §8.1 M0-(l) tenant IdP admin AC 신규 + §15 로드맵 Epic 16 row + §부록 A A92~A96 신규 결정 표 + AD-30 Tenant IdP admin management 신규 결정 + capability matrix v1.27 → v1.28 EXTENSION TENANT_IDP_MANAGEMENT 1 NEW row (industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러, SSO_ENTERPRISE Epic 15 wire pattern). CR 11-3 honest-DEFER discipline 67번째 epic 연속 정직 회복 검증 ✅ (D-1-1-DEFER-1/2/3 RESOLVED 보존 + D-LAUNCH-1-DEFER-1 honestly preserved 65~66~67번째).

## F19.1 tenant_idps table schema (Epic 15 SSO enterprise carry-over, A92 결정)

- **table** = `tenant_idps` (alembic `0038_epic_16_tenant_idps.py` NEW, down_revision='0037_epic_15_sso_external_identities')
- **columns** = `id` (UUID PK) + `tenant_id` (UUID FK → tenants, NOT NULL) + `idp_entity_id` (TEXT NOT NULL, SAML EntityID) + `idp_sso_url` (TEXT NOT NULL, IdP SSO endpoint URL) + `idp_slo_url` (TEXT NULL, IdP Single Logout URL) + `idp_x509_cert` (TEXT NOT NULL, PEM-encoded x509 certificate) + `acs_url` (TEXT NOT NULL, Assertion Consumer Service URL — costmgr SP ACS) + `name_id_format` (TEXT NULL, SAML NameID format, default `urn:oasis:names:tc:SAML:1.1:nameid-format:emailAddress`) + `enabled` (BOOLEAN NOT NULL DEFAULT TRUE) + `created_at` (TIMESTAMPTZ NOT NULL DEFAULT NOW()) + `updated_at` (TIMESTAMPTZ NOT NULL DEFAULT NOW()) + `created_by` (UUID FK → users, NOT NULL) + `updated_by` (UUID FK → users, NOT NULL)
- **unique constraint** = `(tenant_id, idp_entity_id)` UNIQUE (1 tenant = 1 IdP only; multi-IdP 는 2차 로드맵)
- **RLS policy** = `tenant_id = (SELECT current_setting('app.tenant_id'))::uuid` (CR 0-2 RLS lesson 적용, AD-22 verbatim 보존) — `external_identities` table (Epic 15 wire `5f9e37f` alembic 0037) 와 동일한 GUC pattern
- **index** = `idx_tenant_idps_tenant_id` on `(tenant_id)` (lookup 성능)
- **trigger** = `updated_at_auto_update_trg` (BEFORE UPDATE 시 `updated_at = NOW()` 자동 �신, Phase 4 wire `71a033a` 의 audit trigger 정합)

## F19.2 IdP metadata XML validation service (Epic 15 SSO enterprise carry-over, A92 결정)

- **module** = `apps/api/modules/auth/sso/idp_metadata_validator.py` (NEW, ~120 LOC, Epic 15 `saml_validator.py` 와 sibling module)
- **function signature** = `validate_idp_metadata(metadata_xml: str, expected_tenant_slug: str) -> IdPMetadata`
- **validation steps** = (1) XML well-formedness check (xml.etree.ElementTree) / (2) Root element = `EntityDescriptor` (SAML 2.0 metadata schema 정합) / (3) `entityID` attribute 추출 (SAML EntityID) / (4) `IDPSSODescriptor` element 존재 확인 / (5) `KeyDescriptor` element → `X509Certificate` 추출 (PEM-encoded, `-----BEGIN CERTIFICATE-----` ... `-----END CERTIFICATE-----` wrap 자동) / (6) `SingleSignOnService` element → `Location` attribute (IdP SSO URL, https:// 강제) / (7) `SingleLogoutService` element → `Location` attribute (optional, https:// 강제) / (8) tenant slug 매칭 검증 (`expected_tenant_slug` 와 EntityID 의 host part 일치 권장, 예: `https://idp.acme.com/saml/metadata` → tenant_slug `acme`)
- **return type** = `IdPMetadata` (TypedDict: `entity_id`, `sso_url`, `slo_url`, `x509_cert_pem`, `name_id_format`)
- **error envelope** = CR 12-5 D-14 typed exception envelope `{code, message_ko, details, trace_id}` 정합 (e.g. `IDP_METADATA_MALFORMED_KO`, `IDP_METADATA_INVALID_ENTITY_ID_KO`, `IDP_METADATA_INVALID_X509_KO`, `IDP_METADATA_INVALID_SSO_URL_KO`)
- **dependency** = `lxml>=5.0.0` AD-14 stack pin (XML schema validation option)

## F19.3 Tenant IdP CRUD API endpoints (Epic 15 SSO enterprise carry-over, A92 결정)

- **routes** = `apps/api/modules/auth/sso/idp_admin_routes.py` (NEW, 5 routes, sibling of Epic 15 `saml_routes.py`)
  - `GET /api/v1/admin/tenant/{tenant_slug}/idp` — 현재 tenant 의 IdP config 조회 (owner/admin role required)
  - `POST /api/v1/admin/tenant/{tenant_slug}/idp` — 새 IdP config 생성 (owner/admin role required, body: `metadata_xml` 또는 직접 field 입력)
  - `PUT /api/v1/admin/tenant/{tenant_slug}/idp` — 기존 IdP config 수정 (owner/admin role required)
  - `DELETE /api/v1/admin/tenant/{tenant_slug}/idp` — IdP config 삭제 (owner role required, soft delete 결정)
  - `POST /api/v1/admin/tenant/{tenant_slug}/idp/test` — IdP metadata validation dry-run (owner/admin role required, 실제 SSO flow 없이 metadata XML 검증만)
- **authorization** = `require_role("owner", "admin")` Dependency (Epic 12 2FA 게이트 보존 + AD-28 SSO enterprise SAML ACL 정합)
- **capability gate** = `TENANT_IDP_MANAGEMENT` (F19.6 verbatim, capability matrix v1.28)
- **RLS** = 모든 query 에 `tenant_id = current_setting('app.tenant_id')` 자동 적용 (CR 0-2 RLS lesson)
- **audit-first INSERT** = `tenant_idp_created` / `tenant_idp_updated` / `tenant_idp_deleted` / `tenant_idp_tested` 4 NEW audit log entries (CR 1-1 verbatim, action_class='AUTH' + action='tenant_idp_*' + actor_id + tenant_id + payload_json)
- **error envelope** = CR 12-5 D-14 typed exception envelope `{code, message_ko, details, trace_id}` 정합 (e.g. `TENANT_IDP_ALREADY_EXISTS_KO`, `TENANT_IDP_NOT_FOUND_KO`, `TENANT_IDP_FORBIDDEN_KO`, `TENANT_IDP_METADATA_INVALID_KO`)

## F19.4 Tenant IdP admin UI (Epic 15 SSO enterprise carry-over, A92 결정)

- **route** = `apps/web/app/[locale]/(dashboard)/settings/sso/page.tsx` (NEW, ~150 LOC, owner/admin only)
- **components** = `TenantIdPConfigForm.tsx` (form UI: metadata XML paste OR direct field input toggle) + `TenantIdPStatusBadge.tsx` (enabled/disabled + validation status indicator) + `TenantIdPTestResultModal.tsx` (validation dry-run 결과 표시) + `TenantIdPDeleteConfirmDialog.tsx` (delete confirmation)
- **ko-KR SSOT** = `apps/web/messages/ko-KR.json` `settings.sso.*` namespace EXTENSION (12 keys: title, description, metadata_xml_label, paste_metadata_button, entity_id_label, sso_url_label, x509_cert_label, acs_url_label, name_id_format_label, enabled_label, save_button, delete_button, test_button, validation_error, save_success, delete_confirm, delete_success)
- **API integration** = `apps/web/lib/auth/admin-idp-client.ts` (NEW, fetch wrapper, RLS 자동 적용)
- **route group** = `(dashboard)` 보호 (Supabase session 필수 + Epic 12 2FA 미설정 시 `/account/security?reason=2fa_required` redirect, Phase 3-1 T4 wire 정합)

## F19.5 Per-tenant IdP routing EXTENSION (Epic 15 SSO enterprise SAML 정합, A92 결정)

- **EXTENSION target** = Epic 15 wire `5f9e37f` 의 `apps/api/modules/auth/sso/saml_routes.py` `GET /api/v1/auth/sso/login?tenant_slug=acme` handler
- **EXTENSION logic** = 현재는 hardcoded `acme` tenant 만 routing 지원. Epic 16 wire 진입 시점에 `tenant_idps` table lookup 추가 — (1) `tenant_slug` → `tenant_id` 변환 (GUC `app.tenant_id` 자동 설정) / (2) `tenant_idps` row 조회 (RLS 자동 적용) / (3) `idp_entity_id` + `idp_sso_url` + `idp_x509_cert` 추출 / (4) AuthnRequest 생성 시 `idp_sso_url` 로 redirect (HTTP 302)
- **ACS extension** = `POST /api/v1/auth/sso/acs?tenant=acme` handler — `tenant_idps.idp_x509_cert` �로 SAML response signature 검증 (Epic 15 `saml_validator.py` 와 통합, hardcoded cert 제거)
- **backward compatibility** = Epic 15 wire 의 `acme` hardcoded tenant 보존 (단, `tenant_idps` table 에 `acme` row 자동 seed 결정 wire 진입, alembic 0038 데이터 migration 결정)
- **AC 추가** = §F17.3 AC verbatim 보존 + §F19.5 EXTENSION 진입

## F19.6 Capability gate TENANT_IDP_MANAGEMENT (A95 결정)

- **Capability enum** = `Capability.TENANT_IDP_MANAGEMENT = "tenant_idp_management"` (NEW, apps/api/core/capability.py EXTENSION 1 enum)
- **4-industry grants** = manufacturing ✅ + service ✅ + manufacturing_service ✅ + manufacturing_service_other ✅ (industry-agnostic, CR 12-1 L4 precedent 미러, SSO_ENTERPRISE Epic 15 + LISTEN_NOTIFY Epic 13/14 + AUTH_MIDDLEWARE Phase 3 + LISTEN_NOTIFY_TENANT_FANOUT/LISTEN_NOTIFY_MULTIPROCESS Epic 14 wire pattern)
- **미허용 tenant 차단** = `require_capability(TENANT_IDP_MANAGEMENT)` Dependency (Epic 12 2FA 게이트 보존 + AD-28 SSO enterprise ACL 정합)
- **capability-matrix.md v1.27 → v1.28 EXTENSION** = 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅ (SSOT RED→GREEN EXTENSION 결정 wire)
- **drift detector** = `tests/integration/test_capability_matrix_v1_28_drift.py` NEW (Epic 15 `test_capability_matrix_v1_27_drift.py` 정합, P-015 SSOT drift detector 정합)

## F19.7 tests + wire scope T1~T8 결정 (cj-style 67번째 epic 연속 정직 회복 wire 진입 시점에 결정)

**9 ACs satisfied (PRD §F19.1~§F19.7 + §F19.5 EXTENSION 정합)**:

- **AC §F19.1** tenant_idps table 생성 + RLS policy 적용 + unique constraint + index + audit trigger
- **AC §F19.2** IdP metadata XML validation 8 steps (well-formedness + EntityDescriptor + entityID + IDPSSODescriptor + x509 cert + SSO URL + SLO URL + tenant slug 매칭)
- **AC §F19.3** 5 routes (list/create/update/delete/test) + owner/admin ACL + RLS 자동 적용 + audit-first INSERT 4 NEW
- **AC §F19.4** admin UI 1 page + 4 components + ko-KR SSOT 12 keys EXTENSION + (dashboard) 보호
- **AC §F19.5** Epic 15 SAML routes EXTENSION (per-tenant routing + ACS cert 동적 로딩) + backward compatibility (acme hardcoded tenant 보존)
- **AC §F19.6** capability gate TENANT_IDP_MANAGEMENT + capability matrix v1.27 → v1.28 EXTENSION 1 NEW row + drift detector
- **AC §F19.7** ~+25 NEW pytest PASS (idp_metadata_validator unit tests + idp_admin_routes integration tests + audit log verification) + ~+5 NEW vitest PASS (TenantIdPConfigForm RTL render + ko-KR parity) + 0 NEW ruff + 0 regressions
- **AC §F17.3 EXTENSION** Epic 15 SAML routes 정합 (per-tenant routing 보존)
- **AC §8.1 M0-(l)** tenant IdP admin AC verbatim

**A19 cohesion pattern 9 surface EXTENSION PASS** (kernel ✅ + port ✅ + db schema ✅ + service ✅ + handler ✅ + envelope ✅ + capability ✅ + audit ✅ + **Epic 15 SSO enterprise surface EXTENSION** = F19.1~F19.5 tenant IdP admin management territory):

- Surface 1 (kernel) = F19.2 IdP metadata validator pure functions ✅
- Surface 2 (port) = F19.4 admin UI form + F19.5 SAML routes port EXTENSION ✅
- Surface 3 (db schema) = F19.1 tenant_idps table + RLS policy ✅
- Surface 4 (service) = F19.3 CRUD API service layer ✅
- Surface 5 (handler) = F19.3 FastAPI routes + F19.4 admin UI handlers ✅
- Surface 6 (envelope) = F19.2/F19.3 ko-KR CR 12-5 D-14 envelope ✅
- Surface 7 (capability) = F19.6 TENANT_IDP_MANAGEMENT gate ✅
- Surface 8 (audit) = F19.3 audit-first INSERT 4 NEW ✅
- Surface 9 (**Epic 15 SSO enterprise surface EXTENSION**) = F19.1~F19.5 tenant IdP admin territory ✅ EXTENSION PASS

**CR lessons applied** (cj-style 67번째 epic 연속 정직 회복 wire 진입 시점에 결정):

- **CR 0-2** RLS lesson ✅ APPLIED (F19.1 tenant_idps RLS policy `tenant_id = current_setting('app.tenant_id')` 결정, Epic 15 `external_identities` 정합)
- **CR 1-1** audit-first INSERT ✅ APPLIED (F19.3 4 NEW audit log entries: `tenant_idp_created` / `tenant_idp_updated` / `tenant_idp_deleted` / `tenant_idp_tested`)
- **CR 9-6** commit message discipline ✅ APPLIED (`git commit -F <file>` 사용, PowerShell here-string 회피, D5 prevention)
- **CR 11-3** honest-DEFER discipline ✅ APPLIED (67번째 epic 연속 정직 회복 — D-1-1-DEFER-1/2/3 RESOLVED 보존 + D-LAUNCH-1-DEFER-1 honestly preserved 65~66~67번째)
- **CR 12-1** L4 industry-agnostic capability ✅ APPLIED (capability matrix v1.28 EXTENSION TENANT_IDP_MANAGEMENT 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅)
- **CR 12-5** D-14 typed exception envelope ✅ APPLIED (F19.2 + F19.3 ko-KR error envelope `{code, message_ko, details, trace_id}` 정합)
- **CR 12-5** D-PARITY-01 inversion ✅ APPLIED (Python FastAPI backend + TypeScript Next.js admin UI parity 결정 wire)
- **CR 12-5** D-GATE-01 inversion ✅ APPLIED (capability gate `TENANT_IDP_MANAGEMENT` tenant 별 on/off 결정 wire)
- **A19** cohesion pattern 9 surface EXTENSION PASS ✅ (Epic 15 SSO enterprise surface EXTENSION = F19.1~F19.5 tenant IdP admin territory)

**Epic 15 close-out retro + 1st release close-out retro + Phase 4 close-out retro + Phase 3 close-out retro cycle 정합 보존** (cj-style 49~66번째 누적 cycle 정직 회복 + Epic 1 ~ Epic 15 + Phase 3 + Phase 4 모든 close-out retro 결정 verbatim preserve + EXTENSION + 1st release 5-entry-point pattern 모두 wire DONE 진입 (62~66번째) + Epic 15 4-entry-point pattern 모두 wire DONE 진입 (58~61번째) + Phase 4 3-entry-point pattern 모두 wire DONE 진입 (53~56번째) + Phase 3 3-entry-point pattern 모두 wire DONE 진입 (49~52번째) + A19+A28+A34+A35+A36+A37+A38+A41+A42+A45+A46+A51~A91 결정 wire 보존) — Epic 16 PRD entry 진입 시점에 Epic 1 ~ Epic 15 + Phase 3 + Phase 4 + 1st release 모든 close-out retro 결정 verbatim preserve + EXTENSION.

**partial wire 시도 0건 + single sprint atomic wire T1~T8 결정** (cj-style 67번째 epic 연속 정직 회복 wire 진입 시점에 결정). 결정 wire 일자: 2026-08-22 (KST). **next**: Epic 16 bmad-create-story spec entry 진입 (cj-style 68번째 epic 연속 정직 회복 bmad-create-story 진입 시점) OR Epic 16 bmad-dev-story atomic sprint wire T1~T8 진입 (cj-style 69번째 epic 연속 정직 회복 wire 진입 시점) OR Epic 16 bmad-code-review 진입 (cj-style 70번째 epic 연속 정직 회복 진입 시점) OR Epic 16 close-out retro 진입 (cj-style 71번째 epic 연속 정직 회복 진입 시점) 결정 wire 보존.

---

# F20. Multi-Region Backup & Disaster Recovery territory (옵션 (a) Phase 5 진입 결정 wire, cj-style Phase 5 1번째 진입점 = cj-style 73번째 epic 연속 정직 회복 wire 진입 시점)

**Phase 5 territory 진입 wire 결정** (옵션 (a) Phase 5 진입, A124 결정 wire) — **Multi-Region Backup & Disaster Recovery** = docs/database-backup.md §7 disaster recovery 의 "multi-region backup 결정 wire 보류, Phase 5+ 진입 시점" verbatim 해소 = Phase 4 wire 의 honestly-deferred territory 자연스러운 carry-over chain (Phase 4 PRD entry `8e046df` + Phase 4 spec entry + Phase 4 atomic wire T1~T8 `71a033a` + Phase 4 close-out retro = cj-style 53~57번째 epic 연속 정직 회복 wire DONE 진입). **partial wire 시도 0건 + single sprint atomic wire T1~T8 결정** (cj-style 73번째 epic 연속 정직 회복 wire 진입 시점).

Phase 5 = Phase 4 wire 의 honestly-deferred territory (Phase 4 docs/database-backup.md §7 "disaster recovery single-region + multi-region deferred Phase5+") 자연스러운 carry-over chain 결정. Phase 4 단일-region (Supabase Seoul primary + PITR 7일 자동 backup) 의 EXTENSION 으로 multi-region (primary Seoul + secondary Tokyo or Singapore) 자동 failover + cross-region backup 결정 wire 진입. Phase 3-0 + Phase 3-1 + Phase 4 + Epic 13/14 wire + Epic 15 wire + Epic 16 wire + 1st release 5-entry-point pattern 모두 wire DONE 진입 (49~72번째) + Epic 16 6-entry-point pattern 모두 wire DONE 진입 (67~72번째) + 1st release 5-entry-point pattern 모두 wire DONE 진입 (62~66번째) + Epic 15 4-entry-point pattern 모두 wire DONE 진입 (58~61번째) + Phase 4 4-entry-point pattern 모두 wire DONE 진입 (53~56번째) + Phase 3 3-entry-point pattern 모두 wire DONE 진입 (49~52번째) 결정 wire 모두 보존.

기존 baseline 정합 sweep (Phase 5 PRD entry 진입 시점에 cj-style "fix" 종류 pre-flight 정합 sweep):

- ✅ **이미 존재** (baseline 정합 보존): `apps/api/alembic/versions/0036_phase_4_backup_strategy.py` (Phase 4 wire `71a033a` 정합: phase_4_backup_strategy table + backup_type enum auto_pitr/manual_admin/manual_export + checksum_sha256 + status enum) + `docs/database-backup.md` 10 sections (Phase 4 wire 정합: purpose + PITR strategy + RPO 5min/RTO 1hr + schedule + restore procedure + **disaster recovery single-region + multi-region deferred Phase5+** verbatim preserve + monitoring + retention 30일 hot + 90일 cold + 7일 PITR + quarterly drill testing) + `apps/api/core/health.py` FastAPI endpoint (Phase 4 wire 정합: GET /api/v1/health + /live + /ready + CR 12-5 D-14 envelope `{status, timestamp, version, database, redis, uptime_seconds}`) + `apps/api/core/observability.py` Sentry FastAPI integration (Phase 4 wire 정합: init_sentry SSR-safe no-op + FastApiIntegration + tracesSampleRate=0.1) + `apps/web/app/api/health/route.ts` Next.js Edge Runtime (Phase 4 wire 정합: force-dynamic + region env 결정) + `apps/web/lib/observability/sentry.ts` Sentry browser (Phase 4 wire 정합: SSR-safe + lazy-load @sentry/nextjs + tracesSampleRate=0.1) + `vercel.json` Vercel frontend deployment (Phase 4 wire 정합: regions=[icn1] Seoul + healthchecks rewrites) + `railway.toml` Railway backend deployment (Phase 4 wire 정합: healthcheckPath=`/api/v1/health` + restartPolicyType=ON_FAILURE) + capability matrix v1.25 EXTENSION DEPLOYMENT_PROD + DEPLOYMENT_STAGING + DEPLOYMENT_DATABASE_BACKUP + DEPLOYMENT_HEALTH_CHECK 4 NEW rows (Phase 4 wire 정합, industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러).
- ❌ **누락** (Phase 5 wire 진입 시점에 추가 결정): `apps/api/alembic/versions/0039_phase_5_multi_region_backup.py` (phase_5_replication_lag table + replica_region enums + replication_status enum) + `apps/api/jobs/failover_orchestrator.py` (primary → secondary health probe + automatic promotion + DNS update via Supabase API + 30s RTO target) + `apps/api/core/health.py` EXTENSION multi-region endpoint (`/api/v1/health/multi-region` returns primary + secondary status) + `apps/api/jobs/dr_drill.py` (cron KST 1st Sunday 03:00 UTC 18:00 + actual failover drill test in staging + RPO/RTO measurement) + `apps/api/core/observability.py` EXTENSION Sentry breadcrumb failover + Grafana multi-region dashboard EXTENSION + `docs/multi-region-backup.md` NEW (Phase 5 territory runbook: cross-region replication setup + failover procedure + DR drill schedule + RPO/RTO measurement + multi-region health observability) + capability matrix v1.28 → v1.29 EXTENSION MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER 2 NEW rows 결정.

**8 ACs satisfied (PRD §F20.1~§F20.7 verbatim, cj-style 73번째 epic 연속 정직 회복 wire 진입 시점에 wire scope 결정)**:

## F20.1 Cross-region read replica + WAL archiving (D-PHASE-4-DR-DEFER-1 ✅ RESOLVE 진입 wire)
- `apps/api/alembic/versions/0039_phase_5_multi_region_backup.py` NEW (~+120 LOC, atomic): `phase_5_replication_lag` table 신규 결정 wire (BIGSERIAL id + replica_region TEXT enum (seoul/tokyo/singapore/frankfurt/virginia/oregon/sao_paulo) + primary_region TEXT enum + lag_bytes BIGINT + lag_seconds INTEGER + last_synced_lsn TEXT PG_LSN + last_synced_at TIMESTAMPTZ + replication_status TEXT enum (syncing/replicating/lagged/disconnected/failed) + audit_first INSERT 결정). WAL archiving 결정 wire (`postgresql.conf` `archive_mode = on` + `archive_command = 'pgbackrest --stanza=costmgr archive-push %p'` + `wal_level = replica` 결정 wire 보류 → Supabase managed 결정 wire). audit-first INSERT `replica_status_changed` (CR 1-1 verbatim, action_class='INFRA' + action='replica_status_changed' + actor_id + region + previous_status + new_status + trace_id). alembic 0039 down_revision = `'0038_epic_16_tenant_idps'`. 3 indexes (status + region+status+last_synced_at DESC + created_at) 결정 + 2 CHECK constraints (replication_status enum + replica_region enum) 결정.
- **WAL archiving 결정** (Phase 5 PRD entry 진입 시점에 결정): Supabase managed PITR 의 cross-region extension 결정 wire — Supabase Pro plan 의 PITR 7일 (single-region) 의 cross-region extension = Supabase multi-region PITR 또는 pgbackrest 결정 wire 보류 (Supabase Team plan 결정 wire 보류 vs Enterprise plan 결정 wire 보류). 결정 wire 진입 시점에 결정 (Phase 5 bmad-dev-story 진입 시점에 결정).
- `docs/cross-region-replication.md` NEW (~+120 LOC, atomic): purpose + Supabase multi-region setup 결정 + replica region 선택 (Tokyo 결정 wire 1st choice: latency Seoul-Tokyo ~50ms vs Singapore ~70ms vs Frankfurt ~250ms, AD-9 Seoul region 정합) + replication lag monitoring 결정 (lag_bytes threshold 100MB + lag_seconds threshold 30s → alert 결정) + WAL archiving setup 결정 + Supabase pgbackrest 또는 barman 결정 wire 보류.

## F20.2 Cross-region failover automation (D-PHASE-4-DR-DEFER-2 ✅ RESOLVE 진입 wire)
- `apps/api/jobs/failover_orchestrator.py` NEW (~+200 LOC, atomic): primary → secondary health probe + automatic promotion 결정 wire. **Health probe** 결정: primary region 의 `/api/v1/health/ready` endpoint + secondary region 의 `/api/v1/health/ready` endpoint 5-second interval 확인, 3 consecutive failures → failover candidate 결정. **Automatic promotion** 결정 wire: secondary region 의 PostgreSQL promote decision (Supabase API `POST /v1/projects/{ref}/database/promote` 결정 wire 보류) + read-only mode 해제 + connection pool redirect 결정. **DNS update via Supabase API** 결정 wire: failover 결정 wire 진입 시점에 Supabase project URL 의 custom domain redirect 결정 wire (Supabase custom domain 결정 wire 보류). **RTO 30-second target** 결정 wire (manual failover 5min → automatic failover 30s 결정 wire).
- `apps/api/jobs/failover_orchestrator.py` 의 **failover trigger** 결정 wire: (a) health probe 3 consecutive failures OR (b) manual trigger via `POST /api/v1/admin/failover` (owner-only, AD-22 RBAC 결정 wire + 2FA challenge Epic 12 정합) OR (c) scheduled drill via `apps/api/jobs/dr_drill.py` cron 결정 wire. audit-first INSERT `failover_initiated` + `failover_completed` (CR 1-1 verbatim, action_class='INFRA' + action='failover_initiated' + actor_id + from_region + to_region + trace_id).
- **`apps/api/main.py` EXTENSION** (failover_orchestrator lifespan hook 결정): FastAPI startup 에서 failover_orchestrator 백그라운드 task 시작 + shutdown 에서 task cancel 결정 wire. **GRACEFUL_SHUTDOWN_TIMEOUT=30s** 결정 wire (in-flight requests 30s 대기 결정).

## F20.3 Disaster recovery drill + automated quarterly test
- `apps/api/jobs/dr_drill.py` NEW (~+150 LOC, atomic): cron KST 1st Sunday 03:00 = UTC 18:00 결정 wire. **Actual failover drill test in staging** 결정 wire (production 환경 직접 failover 위험 회피, staging 환경에서 drill 결정). Drill steps 결정: (1) staging primary health check + (2) staging secondary promote trigger 결정 + (3) staging database connection write test 결정 + (4) staging application health check 결정 + (5) staging DNS update test 결정 + (6) staging primary restore trigger (drill complete marker) 결정. **RPO/RTO measurement** 결정 wire: drill 시작 시점 → drill 완료 시점 시간 측정 = RTO actual, drill 시작 전 마지막 transaction LSN → drill 후 secondary LSN 측정 = RPO actual. 결과 결정 wire: `phase_5_dr_drill_results` table 신규 (id + drill_date + rto_actual_seconds + rpo_actual_bytes + status enum pass/fail + notes + created_at). **Quarterly drill schedule** 결정 wire: Q1 (January) + Q2 (April) + Q3 (July) + Q4 (October) 결정 (docs/database-backup.md §9 quarterly drill pattern verbatim preserve).
- **`apps/api/alembic/versions/0039_phase_5_multi_region_backup.py`** `phase_5_dr_drill_results` table 결정 wire (BIGSERIAL id + drill_date DATE + rto_actual_seconds INTEGER + rpo_actual_bytes BIGINT + status TEXT enum pass/fail + notes TEXT + created_at TIMESTAMPTZ). audit-first INSERT `dr_drill_completed` (CR 1-1 verbatim, action_class='INFRA' + action='dr_drill_completed' + actor_id='system' + rto_actual_seconds + rpo_actual_bytes + status 결정).

## F20.4 Cross-region backup strategy
- `docs/database-backup.md` EXTENSION (10 sections → 12 sections 결정 wire): (기존) purpose + PITR strategy + RPO/RTO + restore procedure + disaster recovery + monitoring + retention + quarterly drill testing + (NEW) cross-region backup strategy section: Supabase PITR primary (Seoul) + Supabase PITR secondary (Tokyo) 결정 + 30일 hot (primary) + 90일 cold (secondary) + 365일 archive (regional) retention decision. RPO 1시간 / RTO 4시간 SLA 결정 (multi-region 자동 failover 적용 후: single-region 5min/1h → multi-region 1h/4h = RTO 단축 결정 wire, Phase 4 close-out retro §12 옵션 (b) Phase 5 진입 rationale 3 verbatim).
- **Cross-region backup vs single-region** 결정 wire: Phase 4 single-region (Supabase Seoul primary + PITR 7일 자동) 의 honest-extreme risk = Seoul region disaster 시 backup restoration 불가 (multi-region 으로 해소). Phase 5 multi-region 결정 = primary Seoul + secondary Tokyo 자동 failover + cross-region backup 결정.
- **`apps/api/jobs/cross_region_backup.py`** 결정 wire 보류 (Phase 5 atomic wire 진입 시점에 결정): Supabase Storage cross-region replication vs AWS S3 cross-region replication 결정 wire 보류. 결정 wire 진입 시점에 결정 (Phase 5 bmad-dev-story 진입 시점에 결정 — Option A Supabase Storage 결정 wire vs Option B AWS S3 결정 wire vs Option C 직접 S3 cross-region replication 결정 wire).

## F20.5 Multi-region health observability
- `apps/api/core/health.py` EXTENSION (multi-region endpoint 결정 wire): NEW endpoint `/api/v1/health/multi-region` returns primary + secondary status array 결정 (CR 12-5 D-14 envelope `{status, primary: {region, status, lag_bytes, lag_seconds, last_synced_at}, secondary: {region, status, lag_bytes, lag_seconds, last_synced_at}, timestamp}`). JWT verification probe 결정 (Supabase Auth health probe + per-region 결정).
- `apps/api/core/observability.py` EXTENSION (Sentry breadcrumb failover 결정 wire): failover_initiated 시 Sentry breadcrumb + alert 결정 wire (`sentry_sdk.capture_message(f"Failover initiated from {from_region} to {to_region}", level="warning")` + Sentry alert routing 결정 wire). Grafana multi-region dashboard EXTENSION 결정 wire (primary + secondary region metrics + replication lag graph 결정 + failover event log 결정).
- **`apps/web/app/api/health/multi-region/route.ts`** NEW 결정 wire (~+30 LOC, atomic): Next.js Edge Runtime + force-dynamic + Vercel region 결정 + NextResponse.json envelope 결정 (`{status, primary, secondary, build, region, timestamp}` 결정 wire).

## F20.6 Capability matrix v1.28 → v1.29 EXTENSION (A127 결정 wire)
- **MULTI_REGION_BACKUP** = "multi_region_backup" enum 결정 wire (Phase 5 PRD entry 진입 시점에 결정). capability matrix v1.28 → v1.29 EXTENSION 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅ 결정 wire (CR 12-1 L4 precedent 미러, DEPLOYMENT_DATABASE_BACKUP Phase 4 wire pattern 미러).
- **MULTI_REGION_FAILOVER** = "multi_region_failover" enum 결정 wire (Phase 5 PRD entry 진입 시점에 결정). capability matrix v1.28 → v1.29 EXTENSION 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅ 결정 wire (CR 12-1 L4 precedent 미러, DEPLOYMENT_PROD Phase 4 wire pattern 미러).
- **drift detector** = `tests/integration/test_capability_matrix_v1_29_drift.py` NEW 결정 wire (Phase 4 `test_capability_matrix_v1_25_drift.py` + Epic 16 `test_capability_matrix_v1_28_drift.py` 정합, P-015 SSOT drift detector 정합).

## F20.7 tests + wire scope T1~T8 결정 (cj-style 73번째 epic 연속 정직 회복 wire 진입 시점에 결정)

**8 ACs satisfied (PRD §F20.1~§F20.7 verbatim)**:

- **AC §F20.1** Cross-region read replica + WAL archiving 결정 wire (`apps/api/alembic/versions/0039_phase_5_multi_region_backup.py` NEW + `phase_5_replication_lag` table + `phase_5_dr_drill_results` table + replica_region enums + replication_status enum + audit-first INSERT `replica_status_changed`)
- **AC §F20.2** Cross-region failover automation 결정 wire (`apps/api/jobs/failover_orchestrator.py` NEW + primary → secondary health probe + automatic promotion + DNS update via Supabase API + 30s RTO target + audit-first INSERT `failover_initiated` + `failover_completed`)
- **AC §F20.3** DR drill + automated quarterly test 결정 wire (`apps/api/jobs/dr_drill.py` NEW + cron KST 1st Sunday 03:00 UTC 18:00 + actual failover drill test in staging + RPO/RTO measurement + `phase_5_dr_drill_results` table + quarterly drill schedule Q1/Q2/Q3/Q4)
- **AC §F20.4** Cross-region backup strategy 결정 wire (`docs/database-backup.md` EXTENSION + cross-region PITR primary/secondary + 30일 hot + 90일 cold + 365일 archive regional + RPO 1h / RTO 4h SLA)
- **AC §F20.5** Multi-region health observability 결정 wire (`apps/api/core/health.py` EXTENSION `/api/v1/health/multi-region` endpoint + `apps/api/core/observability.py` EXTENSION Sentry breadcrumb failover + `apps/web/app/api/health/multi-region/route.ts` NEW Edge Runtime)
- **AC §F20.6** Capability matrix v1.28 → v1.29 EXTENSION MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER 2 NEW rows (industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러)
- **AC §F20.7** Tests + wire scope T1~T8 결정 (~+50 NEW pytest PASS + ~+10 NEW vitest PASS + 0 NEW ruff + 0 regressions 결정)
- **AC §8.1 M0-(m)** multi-region backup AC verbatim 결정

**A19 cohesion pattern 9 surface EXTENSION PASS** (kernel ✅ + port ✅ + db schema ✅ + service ✅ + handler ✅ + envelope ✅ + capability ✅ + audit ✅ + **multi-region surface NEW** = F20.1~F20.5 multi-region backup & disaster recovery territory):

- Surface 1 (kernel) = F20.1 WAL archiving pure functions ✅
- Surface 2 (port) = F20.2 failover_orchestrator lifespan hook + F20.3 dr_drill cron port ✅
- Surface 3 (db schema) = F20.1 phase_5_replication_lag table + F20.3 phase_5_dr_drill_results table ✅
- Surface 4 (service) = F20.2 failover_orchestrator service + F20.3 dr_drill service + F20.5 multi-region health service ✅
- Surface 5 (handler) = F20.5 /api/v1/health/multi-region FastAPI endpoint + F20.5 Next.js /api/health/multi-region route handler ✅
- Surface 6 (envelope) = F20.5 multi-region health response `{status, primary, secondary, timestamp}` 결정 wire ✅
- Surface 7 (capability) = F20.6 MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER 2 NEW gates (capability matrix v1.29 EXTENSION) ✅
- Surface 8 (audit) = F20.1 replica_status_changed + F20.2 failover_initiated/completed + F20.3 dr_drill_completed audit-first INSERT 결정 (CR 1-1 verbatim) ✅
- Surface 9 (**multi-region surface NEW**) = F20.1~F20.5 multi-region backup & disaster recovery territory ✅ EXTENSION PASS

**CR lessons applied** (cj-style 73번째 epic 연속 정직 회복 wire 진입 시점에 결정):

- **CR 0-2** RLS lesson ✅ APPLIED (F20.1 phase_5_replication_lag table + phase_5_dr_drill_results table 결정 wire 보류 — replication metadata 는 system-only table 결정 wire, RLS 미적용 결정 wire, Epic 13/14 LISTEN/NOTIFY system table pattern 미러)
- **CR 1-1** audit-first INSERT ✅ APPLIED (F20.1 replica_status_changed + F20.2 failover_initiated + F20.2 failover_completed + F20.3 dr_drill_completed 4 NEW audit log entries 결정 wire)
- **CR 9-6** commit message discipline ✅ APPLIED (`git commit -F <file>` 사용, PowerShell here-string 회피, D5 prevention)
- **CR 11-3** honest-DEFER discipline ✅ APPLIED (73번째 epic 연속 정직 회복 — D-1-1-DEFER-1/2/3 RESOLVED 보존 60~73번째 + D-LAUNCH-1-DEFER-1 honestly preserved 65~73번째 + D-EPIC-16-REVIEW-DEFER-2~6 honestly DEFERRED 5 OPEN 보존 70~73번째 + D-PHASE-4-DR-DEFER-1/2 honestly RESOLVE 진입 wire 결정, Phase 4 close-out retro §12 옵션 (b) Phase 5 진입 rationale 3 verbatim)
- **CR 12-1** L4 industry-agnostic capability ✅ APPLIED (capability matrix v1.29 EXTENSION MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER 2 NEW rows industry-agnostic 4-industry grants ✅/✅/✅/✅)
- **CR 12-5** D-14 typed exception envelope ✅ APPLIED (F20.5 multi-region health response envelope 결정 wire)
- **CR 12-5** D-PARITY-01 inversion ✅ APPLIED (Python FastAPI backend + TypeScript Next.js Edge Route Handler parity 결정 wire)
- **CR 12-5** D-GATE-01 inversion ✅ APPLIED (capability gate MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER 결정 wire, owner-only RBAC AD-22 결정 wire)
- **A19** cohesion pattern 9 surface EXTENSION PASS ✅ (multi-region surface NEW = F20.1~F20.5 multi-region backup & disaster recovery territory)

**Epic 16 close-out retro + 1st release close-out retro + Phase 4 close-out retro + Phase 3 close-out retro + Epic 15 close-out retro cycle 정합 보존** (cj-style 49~72번째 누적 cycle 정직 회복 + Epic 1 ~ Epic 16 + Phase 3 + Phase 4 + 1st release 모든 close-out retro 결정 verbatim preserve + EXTENSION + Epic 16 6-entry-point pattern 모두 wire DONE 진입 (67~72번째) + 1st release 5-entry-point pattern 모두 wire DONE 진입 (62~66번째) + Epic 15 4-entry-point pattern 모두 wire DONE 진입 (58~61번째) + Phase 4 4-entry-point pattern 모두 wire DONE 진입 (53~56번째) + Phase 3 3-entry-point pattern 모두 wire DONE 진입 (49~52번째) 결정 wire 모두 보존) — Phase 5 PRD entry 진입 시점에 Epic 1 ~ Epic 16 + Phase 3 + Phase 4 + 1st release 모든 close-out retro 결정 verbatim preserve + EXTENSION.

**partial wire 시도 0건 + single sprint atomic wire T1~T8 결정** (cj-style 73번째 epic 연속 정직 회복 wire 진입 시점에 결정). 결정 wire 일자: 2026-08-22 (KST). **next**: Phase 5 bmad-create-story spec entry 진입 (cj-style 74번째 epic 연속 정직 회복 bmad-create-story 진입 시점) OR Phase 5 bmad-dev-story atomic sprint wire T1~T8 진입 (cj-style 75번째 epic 연속 정직 회복 wire 진입 시점) OR Phase 5 close-out retro 진입 (cj-style 76~77번째 epic 연속 정직 회복 진입 시점) 결정 wire 보존.

---

# F21. Epic 17 territory (옵션 (a) Epic 17 진입 결정 wire, cj-style 80번째 epic 연속 정직 회복)

> **§F21 결정 wire 진입** — Sidebar/MenuProvider hot-fix `01a06e4` (cj-style 79번째 wire entry) 직후 다음 옵션 5종 중 **사용자 권장 결정 = 옵션 (a) Epic 17 진입**. rationale 4종: ① Epic 1 ~ Epic 16 + Phase 3 + Phase 4 + Phase 5 모두 wire DONE (49~77번째 cumulative cycle + 78~79번째 hot-fix + RESOLVE sprint) → 모든 territory 의 audit-first INSERT (CR 1-1) 가 audit_log table 에 누적 → audit log viewer territory 자연스러운 next 진입 결정 wire ② cj-style discipline 회피 위험 방지 (49~79번째 누적 31-entry-point cycle + 78~79번째 wire 직후 즉시 Epic 17 진입 = 1-day atomic sprint discipline) ③ 비즈니스 우선순위 = enterprise 고객 onboarding 시 audit log viewer 필수 (PIPA + GDPR + SOX compliance = audit log 가시성 + filter + export 기능 요구) ④ Phase 5 multi-region wire 의 cross-region audit log visibility 자연스러운 carry-over (audit_log table 은 cross-region primary 에 write → multi-region read replica 통한 cross-region audit visibility 제공). master PRD v3.5 → v3.6 atomic edit (docs only). §F21 territory = Audit Log Viewer & Activity Stream 결정 wire 진입 시점에 7 ACs (F21.1 audit log query API + F21.2 audit log viewer UI + F21.3 activity stream UI + F21.4 cross-region audit log visibility + F21.5 CSV export + F21.6 capability gate AUDIT_LOG_VIEW + F21.7 tests + wire scope T1~T8) + §8.1 M0-(n) audit log viewer AC 신규 + §15 로드맵 Epic 17 row status 백로그 → in-progress (PRD entry DONE 진입 wire) + §부록 A A153+A154+A155+A156+A157 신규 결정 표 + AD-32 Audit Log Viewer & Activity Stream 신규 결정 + capability matrix v1.29 → v1.30 EXTENSION AUDIT_LOG_VIEW 1 NEW row (industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러, MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire pattern + TENANT_IDP_MANAGEMENT Epic 16 wire pattern + LAUNCH_* 1st release wire pattern). CR 11-3 honest-DEFER discipline 80번째 epic 연속 정직 회복 검증 ✅ (D-1-1-DEFER-1/2/3 RESOLVED 보존 60~80번째 + D-LAUNCH-1-DEFER-1 honestly preserved 65~80번째 + D-EPIC-16-REVIEW-DEFER-2~6 ✅ RESOLVED 보존 78~80번째 + D-PHASE-4-DR-DEFER-1/2 ✅ RESOLVED 보존 73~80번째).

## F21.1 audit log query API (CR 1-1 audit-first INSERT 누적 territory, A153 결정)

- **module** = `apps/api/modules/audit/audit_log_query.py` (NEW, ~180 LOC, sibling of `apps/api/core/audit_action.py` + `apps/api/core/audit_log.py`)
- **function signatures**:
  - `query_audit_log(tenant_id: UUID, filters: AuditLogQueryFilters, page: int = 1, page_size: int = 50) -> AuditLogPage` (filtered pagination)
  - `count_audit_log(tenant_id: UUID, filters: AuditLogQueryFilters) -> int` (filtered count, for pagination UI)
  - `get_audit_log_entry(tenant_id: UUID, entry_id: UUID) -> AuditLogEntry` (single entry detail)
  - `query_activity_stream(tenant_id: UUID, window_days: Literal[7, 30, 90]) -> list[ActivityStreamGroup]` (grouped by date + actor)
- **AuditLogQueryFilters TypedDict** = `{actor_id: UUID | None, action_class: str | None, action: str | None, period_key: str | None, payload_search: str | None, start_date: date | None, end_date: date | None}` (all optional, AND semantics)
- **AuditLogEntry TypedDict** = `{id: UUID, tenant_id: UUID, actor_id: UUID, actor_email: str, action_class: str, action: str, payload_json: dict, created_at: datetime, trace_id: str | None}` (joined with users table for actor_email)
- **AuditLogPage TypedDict** = `{entries: list[AuditLogEntry], total_count: int, page: int, page_size: int, has_next: bool}` (CR 12-5 D-14 envelope partial)
- **ActivityStreamGroup TypedDict** = `{date: date, entries: list[AuditLogEntry], unique_actors: int}` (grouped by date DESC)
- **RLS** = `tenant_id = (SELECT current_setting('app.tenant_id'))::uuid` (CR 0-2 RLS lesson 적용, AD-22 verbatim 보존)
- **authorization** = `require_role("owner", "admin")` FastAPI Dependency (Epic 12 2FA 게이트 보존)
- **capability gate** = `AUDIT_LOG_VIEW` (F21.6 verbatim, capability matrix v1.30)
- **pagination** = offset+limit 기반, page_size=50 default (max 200)
- **sort** = `ORDER BY created_at DESC` (newest first)
- **performance** = `audit_log` table 의 `(tenant_id, created_at DESC)` index 활용 + `payload_json` 의 GIN index (jsonb_path_ops) for payload_search

## F21.2 audit log viewer UI (CR 11-4 D-002 ko-KR SSOT EXTENSION, A153 결정)

- **route** = `apps/web/app/[locale]/(dashboard)/audit-log/page.tsx` (NEW, ~200 LOC, owner/admin only)
- **components**:
  - `AuditLogFilterPanel.tsx` (filter form: actor email autocomplete + action_class dropdown + action text search + period_key + date range picker + payload_search input)
  - `AuditLogTable.tsx` (table view: created_at + actor_email + action_class chip + action text + payload summary + trace_id expandable)
  - `AuditLogPagination.tsx` (prev/next + page size selector + total count display)
  - `AuditLogExportButton.tsx` (CSV export trigger, calls F21.5 backend)
  - `AuditLogDetailModal.tsx` (full payload_json + trace_id expansion modal)
- **ko-KR SSOT** = `apps/web/messages/ko-KR.json` `audit_log.*` namespace EXTENSION (14 keys: title, description, filter_actor, filter_action_class, filter_action, filter_period, filter_date_range, filter_payload_search, table_created_at, table_actor, table_action_class, table_action, table_payload_summary, export_csv_button, pagination_prev, pagination_next, total_count, detail_modal_title)
- **API integration** = `apps/web/lib/audit/audit-log-client.ts` (NEW, fetch wrapper + auth cookie 자동 첨부 + typed error envelope)
- **route group** = `(dashboard)` 보호 (Supabase session 필수 + Epic 12 2FA 미설정 시 `/account/security?reason=2fa_required` redirect, Phase 3-1 T4 wire 정합)
- **vitest RTL render discipline** = `__tests__/audit-log/page.test.tsx` NEW (filter panel + table render + pagination interaction, CR 11-4 D-003 verbatim)

## F21.3 activity stream UI (Timeline view, A153 결정)

- **route** = `apps/web/app/[locale]/(dashboard)/activity/page.tsx` (NEW, ~150 LOC, all tenant members)
- **components**:
  - `ActivityStreamTimeline.tsx` (grouped by date: today / yesterday / N days ago)
  - `ActivityStreamEntry.tsx` (avatar + action_class icon + action text + actor name + relative time)
  - `ActivityStreamWindowSelector.tsx` (7일 / 30일 / 90일 window selector)
- **ko-KR SSOT** = `apps/web/messages/ko-KR.json` `activity.*` namespace EXTENSION (8 keys: title, description, window_7d, window_30d, window_90d, empty_message, today_label, yesterday_label)
- **route group** = `(dashboard)` 보호 (Phase 3-1 T4 wire 정합)
- **authorization** = `require_role("owner", "admin", "member", "viewer")` (all tenant members, F21.2 audit log viewer 와 다른 권한 등급)
- **capability gate** = 별도 capability gate 없음 (activity stream 은 tenant-wide 가시성, owner/admin audit log viewer 와 분리)

## F21.4 cross-region audit log visibility (Phase 5 carry-over, A154 결정)

- **EXTENSION target** = Phase 5 wire `f093f8c` 의 `apps/api/alembic/versions/0039_phase_5_multi_region_backup.py` `phase_5_replication_lag` table + Supabase multi-region primary Seoul + secondary Tokyo replica 결정 wire
- **EXTENSION logic** = audit log query 시 secondary region 의 read replica 에서 query 가능 결정 wire (multi-region read replica 통한 cross-region audit visibility) — `/api/v1/audit-log` handler 에서 connection pool 의 read-only routing 결정 wire
- **읽기 일관성** = secondary region 의 replication lag 이 lag_bytes ≤ 100MB + lag_seconds ≤ 30s 시 read consistent (Phase 5 wire 의 lag threshold 정합) — lag 초과 시 primary region 으로 fallback + Sentry breadcrumb 결정 wire
- **AC 추가** = Phase 5 §F20 verbatim 보존 + §F21.4 EXTENSION 진입

## F21.5 CSV export (Compliance use case, A155 결정)

- **module** = `apps/api/modules/audit/audit_log_export.py` (NEW, ~120 LOC)
- **function signature** = `export_audit_log_csv(tenant_id: UUID, filters: AuditLogQueryFilters, actor_id: UUID) -> StreamingResponse` (Excel-compatible UTF-8 BOM + streaming response + audit-first INSERT `audit_log_exported` action)
- **CSV format** = columns: `entry_id,created_at,actor_email,action_class,action,period_key,payload_json,trace_id` (UTF-8 BOM `﻿` + comma-separated + double-quote escape for payload_json)
- **streaming** = `StreamingResponse(media_type='text/csv; charset=utf-8', headers={'Content-Disposition': f'attachment; filename="audit-log-{tenant_slug}-{yyyymmdd}.csv"'})` (large dataset 시 memory efficient)
- **audit-first INSERT** = `audit_log_exported` (CR 1-1 verbatim, action_class='AUDIT' + action='audit_log_exported' + actor_id + tenant_id + filters_json + row_count + trace_id) — 누가 언제 어떤 filter 로 export 했는지 추적
- **CR 12-5 D-14 envelope** = error envelope `{code, message_ko, details, trace_id}` (e.g. `AUDIT_LOG_EXPORT_FORBIDDEN_KO`, `AUDIT_LOG_EXPORT_TOO_LARGE_KO`)

## F21.6 Capability gate AUDIT_LOG_VIEW (A156 결정)

- **Capability enum** = `Capability.AUDIT_LOG_VIEW = "audit_log_view"` (NEW, apps/api/core/capability.py EXTENSION 1 enum)
- **4-industry grants** = manufacturing ✅ + service ✅ + manufacturing_service ✅ + manufacturing_service_other ✅ (industry-agnostic, CR 12-1 L4 precedent 미러, MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind)
- **미허용 tenant 차단** = `require_capability(AUDIT_LOG_VIEW)` Dependency (Epic 12 2FA 게이트 보존)
- **capability-matrix.md v1.29 → v1.30 EXTENSION** = 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅ (SSOT RED→GREEN EXTENSION 결정 wire)
- **drift detector** = `tests/integration/test_capability_matrix_v1_30_drift.py` NEW (Phase 5 `test_capability_matrix_v1_29_drift.py` + Epic 16 `test_capability_matrix_v1_28_drift.py` 정합, P-015 SSOT drift detector 정합)

## F21.7 tests + wire scope T1~T8 결정 (cj-style 80번째 epic 연속 정직 회복 wire 진입 시점에 결정)

**7 ACs satisfied (PRD §F21.1~§F21.7 verbatim, cj-style 80번째 epic 연속 정직 회복 wire 진입 시점에 wire scope 결정)**:

- **AC §F21.1** Audit log query API 결정 wire (`apps/api/modules/audit/audit_log_query.py` NEW ~+180 LOC + 4 functions: query_audit_log + count_audit_log + get_audit_log_entry + query_activity_stream + AuditLogQueryFilters + AuditLogEntry + AuditLogPage + ActivityStreamGroup TypedDict 결정 + RLS 자동 적용 + capability gate AUDIT_LOG_VIEW + owner/admin role required)
- **AC §F21.2** Audit log viewer UI 결정 wire (`apps/web/app/[locale]/(dashboard)/audit-log/page.tsx` NEW ~+200 LOC + 5 components 결정 + ko-KR.json `audit_log.*` namespace EXTENSION 14 keys + (dashboard) 보호 + vitest RTL render discipline 결정)
- **AC §F21.3** Activity stream UI 결정 wire (`apps/web/app/[locale]/(dashboard)/activity/page.tsx` NEW ~+150 LOC + 3 components 결정 + ko-KR.json `activity.*` namespace EXTENSION 8 keys + all tenant members 권한)
- **AC §F21.4** Cross-region audit log visibility 결정 wire (Phase 5 multi-region read replica 통한 cross-region audit query 결정 wire + read-only routing + lag threshold 정합 + Sentry breadcrumb 결정)
- **AC §F21.5** CSV export 결정 wire (`apps/api/modules/audit/audit_log_export.py` NEW ~+120 LOC + streaming response + UTF-8 BOM + audit-first INSERT `audit_log_exported` CR 1-1 verbatim 결정 + CR 12-5 D-14 envelope 결정)
- **AC §F21.6** Capability matrix v1.29 → v1.30 EXTENSION AUDIT_LOG_VIEW 1 NEW row 결정 wire (industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러)
- **AC §F21.7** Tests + wire scope T1~T8 결정 (~+50 NEW pytest PASS + ~+15 NEW vitest PASS + 0 NEW ruff + 0 regressions 결정 + T1 audit_log_query module + T2 audit_log_export module + T3 audit_log_viewer page + T4 activity_stream page + T5 ko-KR.json SSOT EXTENSION + T6 capability.py + capability matrix v1.30 + T7 tests + 3중 게이트 FINAL CLEAN + T8 atomic commit)
- **AC §8.1 M0-(n)** audit log viewer AC verbatim 결정

**A19 cohesion pattern 9 surface EXTENSION PASS** (audit viewer surface NEW 결정 wire):

- Surface 1 (kernel) = F21.1 audit_log_query pure functions ✅
- Surface 2 (port) = F21.1 audit_log_query FastAPI routes port ✅
- Surface 3 (db schema) = 기존 audit_log table + F21.4 cross-region read replica 활용 ✅
- Surface 4 (service) = F21.1 query_audit_log + count_audit_log + get_audit_log_entry + query_activity_stream service ✅
- Surface 5 (handler) = F21.2 audit-log/page.tsx + F21.3 activity/page.tsx + F21.5 /api/v1/audit-log/export endpoint ✅
- Surface 6 (envelope) = F21.1 AuditLogPage TypedDict + F21.5 CSV streaming response envelope 결정 wire ✅
- Surface 7 (capability) = F21.6 AUDIT_LOG_VIEW 1 NEW gate (capability matrix v1.30 EXTENSION) ✅
- Surface 8 (audit) = F21.5 audit_log_exported audit-first INSERT 결정 (CR 1-1 verbatim, action_class='AUDIT' + action='audit_log_exported') ✅
- Surface 9 (**audit viewer surface NEW**) = F21.1~F21.5 audit log viewer & activity stream territory ✅ EXTENSION PASS

**CR lessons applied** (cj-style 80번째 epic 연속 정직 회복 wire 진입 시점에 결정):

- **CR 0-2** RLS lesson ✅ APPLIED (F21.1 audit_log query 의 tenant_id RLS 자동 적용 + AD-22 verbatim 보존)
- **CR 1-1** audit-first INSERT ✅ APPLIED (F21.5 audit_log_exported 1 NEW audit log entry 결정 wire, action_class='AUDIT' + action='audit_log_exported' + actor_id + tenant_id + filters_json + row_count + trace_id)
- **CR 9-6** commit message discipline ✅ APPLIED (`git commit -F <file>` 사용, PowerShell here-string 회피, D5 prevention)
- **CR 11-3** honest-DEFER discipline ✅ APPLIED (80번째 epic 연속 정직 회복 — D-1-1-DEFER-1/2/3 RESOLVED 보존 60~80번째 + D-LAUNCH-1-DEFER-1 honestly preserved 65~80번째 + D-EPIC-16-REVIEW-DEFER-2~6 ✅ RESOLVED 보존 78~80번째 + D-PHASE-4-DR-DEFER-1/2 ✅ RESOLVED 보존 73~80번째)
- **CR 11-4** D-001~D-005 + P-015 lessons carry ✅ APPLIED (F21.2 audit log viewer UI 의 ko-KR.json SSOT EXTENSION 14 keys + vitest RTL render discipline 결정, F21.3 activity stream 의 ko-KR.json SSOT EXTENSION 8 keys)
- **CR 12-1** L4 industry-agnostic capability ✅ APPLIED (capability matrix v1.30 EXTENSION AUDIT_LOG_VIEW 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅)
- **CR 12-5** D-14 typed exception envelope ✅ APPLIED (F21.5 audit_log_export error envelope `{code, message_ko, details, trace_id}` 결정 wire)
- **CR 12-5** D-PARITY-01 inversion ✅ APPLIED (Python FastAPI backend + TypeScript Next.js audit viewer parity 결정 wire, vitest RTL render discipline 결정)
- **CR 12-5** D-GATE-01 inversion ✅ APPLIED (capability gate AUDIT_LOG_VIEW per-tenant on/off 결정 wire, owner/admin role required AD-22 RBAC 결정)
- **A19** cohesion pattern 9 surface EXTENSION PASS ✅ (audit viewer surface NEW = F21.1~F21.5 audit log viewer & activity stream territory)

**Sidebar/MenuProvider hot-fix `01a06e4` (cj-style 79번째) + D-EPIC-16-REVIEW-DEFER-2~6 RESOLVE sprint `512ed6a` (cj-style 78번째) + Phase 5 close-out retro `b843565` (cj-style 76~77번째) + Phase 5 atomic wire `f093f8c` (cj-style 75번째) cycle 정합 보존** (cj-style 49~79번째 누적 cycle 정직 회복 + Epic 1 ~ Epic 16 + Phase 3 + Phase 4 + Phase 5 + 1st release 모든 close-out retro 결정 verbatim preserve + EXTENSION) — Epic 17 PRD entry 진입 시점에 Epic 1 ~ Epic 16 + Phase 3 + Phase 4 + Phase 5 + 1st release 모든 close-out retro 결정 verbatim preserve + EXTENSION.

**partial wire 시도 0건 + single sprint atomic wire T1~T8 결정** (cj-style 80번째 epic 연속 정직 회복 wire 진입 시점에 결정). 결정 wire 일자: 2026-08-22 (KST). **next**: Epic 17 bmad-create-story spec entry 진입 (cj-style 81번째 epic 연속 정직 회복 bmad-create-story 진입 시점) OR Epic 17 bmad-dev-story atomic sprint wire T1~T8 진입 (cj-style 82번째 epic 연속 정직 회복 wire 진입 시점) OR Epic 17 close-out retro 진입 (cj-style 83번째 epic 연속 정직 회복 진입 시점) 결정 wire 보존.

---

# F22. Phase 6 territory (옵션 (a) Phase 6 진입 결정 wire, cj-style 85번째 epic 연속 정직 회복)

> **§F22 결정 wire 진입** — Epic 17 close-out retro `be8f3bd` (cj-style 84번째 wire entry) 직후 다음 옵션 5종 중 **사용자 권장 결정 = 옵션 (a) Phase 6 진입 + 옵션 (b) Audit Log Retention Policy = Phase 6 territory 결정 wire 진입** (AskUserQuestion 결정 verbatim bind). rationale 5종: ① Epic 17 audit log viewer (cj-style 80~84번째) 의 자연스러운 carry-over chain = audit log 가시성(viewer) + retention(purge) + archive 세 가지 territory 가 §F21.* 의 자연스러운 EXTENSION territory (Phase 6 = §F22 신규 territory) — Epic 17 wire 가 audit_log viewer 를 제공했다면, Phase 6 는 audit_log retention(얼마나 보유) + archive(어디에 보관) + automatic purge(언제 삭제) + PII minimization territory (NFR4 PII minimization 정합) — ② cj-style discipline 회피 위험 방지 (49~84번째 누적 36-entry-point cycle + 79~83번째 hot-fix + RESOLVE sprint + Epic 17 5-entry-point + Epic 16 6-entry-point + Phase 5 4-entry-point 패턴 모두 wire DONE 진입 정합 보존 후 즉시 Phase 6 진입 = 1-day atomic sprint discipline) ③ 비즈니스 우선순위 = NFR4 PII minimization + GDPR Article 17 right to erasure + audit log retention policy = enterprise 고객 onboarding 시 compliance 필수 (SOX 7년 + PII 30일 + GDPR erasure) ④ Phase 5 wire `f093f8c` 의 cross-region audit log visibility (cj-style 75번째) 의 자연스러운 carry-over chain = cross-region primary + secondary read replica 의 `audit_log_archive` 테이블 EXTENSION territory (Phase 5 의 phase_5_replication_lag table + Supabase multi-region EXTENSION 보존) ⑤ 1st release launch wire DONE 진입 후 enterprise SLA = audit log retention 7년 (SOX) + PII 30일 (GDPR Article 17) + immutable archive territory 의 natural next 진입. master PRD v3.6 → v3.7 atomic edit (docs only). §F22 territory = Audit Log Retention Policy 결정 wire 진입 시점에 7 ACs (F22.1 retention policy DSL + F22.2 automatic purge job + F22.3 archive storage + F22.4 GDPR/NFR4 PII compliance + F22.5 audit-first INSERT on deletion + F22.6 capability gate AUDIT_LOG_RETENTION + F22.7 tests + wire scope T1~T8) + §8.1 M0-(o) audit log retention AC 신규 + §15 로드맵 Phase 6 row status 백로그 → in-progress (PRD entry DONE 진입 wire) + §부록 A A193+A194+A195+A196+A197 신규 결정 표 + AD-33 Audit Log Retention Policy 신규 결정 + capability matrix v1.30 → v1.31 EXTENSION AUDIT_LOG_RETENTION 1 NEW row (industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러, AUDIT_LOG_VIEW Epic 17 wire pattern verbatim bind). CR 11-3 honest-DEFER discipline 85번째 epic 연속 정직 회복 검증 ✅ (D-1-1-DEFER-1/2/3 RESOLVED 보존 60~85번째 + D-LAUNCH-1-DEFER-1 honestly preserved 65~85번째 + D-EPIC-16-REVIEW-DEFER-2~6 ✅ RESOLVED 보존 78~85번째 + D-PHASE-4-DR-DEFER-1/2 ✅ RESOLVED 보존 73~85번째 + D-EPIC-17-WIRE-DEFER-T2-T3-UI ✅ RESOLVED 보존 83~85번째 + D-RETENTION-1 honestly DEFER 보존 = Phase 6 PRD entry 진입 시점에 carry-over chain 정직 회복 결정 wire 진입: 1st release close-out retro §6 + Epic 17 close-out retro §11 "audit log retention policy 결정 wire 보류, Phase 6+ 진입 시점" verbatim 해소).

## F22.1 retention policy DSL (CR 1-1 audit-first INSERT 누적 territory + NFR4 PII minimization 정합, A193 결정)
`apps/api/modules/audit/audit_retention_policy.py` NEW ~+120 LOC:
- **policy DSL syntax**: `retain(action_class: str, days: int, archive: bool = False, mask_pii: bool = False)` 결정 wire
- **default policy** 결정 wire = (1) `retain('AUDIT', days=2557, archive=True)` SOX 7년 hold + immutable archive (2) `retain('AUTH', days=30, archive=True, mask_pii=True)` PII 30일 + GDPR Article 17 mask + 7년 archive 결정 wire (3) `retain('INFRA', days=90, archive=True)` infrastructure event 90일 + 7년 archive 결정 wire (4) `retain('DATA', days=365, archive=True)` data event 1년 + 7년 archive 결정 wire
- **per-tenant override EXTENSION** 결정 wire = tenant_settings JSONB `audit_retention_overrides: dict[str, RetentionRule]` 으로 tenant owner 가 audit_action_class 별 retention 기간 + archive + mask_pii 커스터마이즈 가능 결정 wire
- **policy evaluation precedence** 결정 wire = tenant override > industry baseline > system default
- **3 default policy databases**: PII (30일 + mask + archive), SENSITIVE (90일 + archive), REGULAR (365일 + archive), LONG_TERM (2557일 = 7년 + archive immutable)
- **constants** 결정 wire: `DEFAULT_RETENTION_DAYS = {'AUDIT': 2557, 'AUTH': 30, 'INFRA': 90, 'DATA': 365, 'CONFIG': 180}` 결정 wire

## F22.2 automatic purge job (CR 1-1 audit-first INSERT + ActionClass.AUDIT 신규 정의, A193 결정)
`apps/api/jobs/audit_purge.py` NEW ~+150 LOC + 알파위크 테스트:
- **cron schedule** 결정 wire = KST 매일 03:00 (UTC 18:00) 결정 wire 보존 — Epic 17의 activity stream 윈도우 1d/7d/30d/90d 와 정합
- **purge algorithm** 결정 wire = (1) `phase_6_audit_purge_log` table 신규 (BIGSERIAL id + tenant_id + action_class + rule_evaluated + rows_purged + rows_archived + dry_run BOOLEAN + actor_id='system' + trace_id + created_at TIMESTAMPTZ) INSERT 결정 wire / (2) tenant 별로 retention policy 평가 결정 wire = `policy.evaluate(action_class)` → `older_than(days)` boundary 결정 + archive 대상 식별 + mask_pii 대상 식별 / (3) dry-run mode 결정 wire = `--dry-run` flag 로 purge 시뮬레이션만 실행, 실제 DELETE/ARCHIVE 미수행, decision log 만 INSERT / (4) archive mode 결정 wire = archive 대상 row 들을 `audit_log_archive` 로 INSERT 결정 wire + audit_log 원본 row 의 `archived_at` 컬럼 UPDATE + 원본 DELETE (5) PII mask mode 결정 wire = mask_pii=True 인 action 의 email + ip_address 컬럼을 `[REDACTED:hash]` 로 UPDATE + audit-first INSERT `audit_log_pii_masked` 결정 wire (6) delete mode 결정 wire = retention 기간 초과 row DELETE + audit-first INSERT `audit_log_purged` 결정 wire (CR 1-1 verbatim 적용)
- **2 NEW actions** 결정 wire = `audit_log_purged` + `audit_log_archived` (ActionClass.AUDIT sub-set, Epic 17의 `audit_log_exported` 다음 sub-action 결정 wire)
- **1 NEW action** 결정 wire = `audit_log_pii_masked` (NFR4 PII minimization 정합, GDPR Article 17 right to erasure EXTENSION 결정 wire)
- **PL/pgSQL function + alembic 0040** 결정 wire = `apps/api/alembic/versions/0040_phase_6_audit_retention.py` NEW + `audit_log_archive` table DDL + `phase_6_audit_purge_log` table DDL + `audit_log.archived_at` column ADD 결정 wire
- **RLS 자동 적용** CR 0-2 verbatim + audit_log RLS 정합 보존 결정 wire

## F22.3 archive storage + 365일 cold archive (Phase 5 carry-over chain, A193 결정)
`audit_log_archive` table 신규 (alembic 0040):
- **immutable append-only** 결정 wire = trigger `audit_log_archive_immutable_trg` BEFORE UPDATE OR DELETE 시 raise exception 결정 wire (PostgreSQL trigger 결정 wire 결정)
- **SHA-256 hash chain linkage** 결정 wire = `audit_log_archive.hash_chain` TEXT column 추가 + 이전 row 의 hash 와 현재 row 의 payload 의 SHA-256 = `current.hash_chain` 결정 wire + 이전 row hash 조회 SQL `SELECT hash_chain FROM audit_log_archive ORDER BY id DESC LIMIT 1` + tamper detection 가능 (C2 결정 wire 도입)
- **column 복사** 결정 wire = `audit_log_archive.id` BIGSERIAL PK + `original_id` BIGINT + `archived_at` TIMESTAMPTZ + 모든 audit_log 컬럼 + `archived_by` actor_id 결정 wire
- **cross-region archive** 결정 wire = `audit_log_archive` table 의 cross-region replication 결정 wire 보존 (Phase 5 wire `f093f8c` 의 `phase_5_replication_lag` table + Supabase multi-region primary Seoul + secondary Tokyo replica EXTENSION 결정 wire) — cross-region primary archive write + cross-region secondary archive read replica 통해 immutable archive 보장
- **365일 cold archive** 결정 wire = `apps/api/jobs/audit_cold_archive.py` NEW (separate from daily purge job, cron 월 1회 KST 1st Sunday 04:00 = UTC 19:00) + `audit_log` table 1년 경과 row 를 `audit_log_archive` 로 INSERT + audit_log 원본 row DELETE 결정 wire + audit-first INSERT `audit_log_cold_archived` (3rd NEW action, ActionClass.AUDIT 결정 wire)
- **indexes** 결정 wire = `idx_audit_log_archive_tenant_id` + `idx_audit_log_archive_action_class` + `idx_audit_log_archive_archived_at` + `idx_audit_log_archive_original_id` 4 NEW 결정 wire

## F22.4 GDPR / NFR4 PII minimization + Article 17 erasure EXTENSION (CR 1-1 verbatim, A193 결정)
- **email masking** 결정 wire = audit_log table 의 `actor_email` column (user email 저장) + `payload_json.actor_email` 30일 경과 후 `[REDACTED:sha256(email)]` 결정 wire + audit-first INSERT `audit_log_pii_masked` 결정 wire (CR 1-1 verbatim 적용 + NFR4 PII minimization 정합)
- **IP address masking** 결정 wire = `audit_log.ip_address` column 30일 경과 후 `[REDACTED:ip]/24` 결정 wire (privacy mode 결정 wire) + GDPR Article 17 right to erasure 정합
- **GDPR Article 17 erasure endpoint** 결정 wire = `DELETE /api/v1/audit/personal-data?email=<email>` (owner-only RBAC AD-22 verbatim 보존 + audit-first INSERT `audit_log_personal_data_erased` 5th NEW action 결정 wire) — `audit_log` 의 해당 email 의 PII 컬럼 전체 `[REDACTED]` UPDATE + `audit_log_archive` 에 archive_event INSERT + tenant owner notification 결정 wire 보존
- **NFR4 PII minimization 정합** 결정 wire = 1st release close-out retro §6 "audit log retention policy 결정 wire 보류, Phase 6+ 진입 시점" verbatim 해소 + Epic 17 close-out retro §11 "audit log retention policy 결정 wire 보류, Phase 6+ 진입 시점" verbatim carry-over chain 정직 회복 결정 wire

## F22.5 audit-first INSERT on deletion (CR 1-1 verbatim + ActionClass.AUDIT 신규 정의, A193 결정)
- **5 NEW actions** 결정 wire = (1) `audit_log_purged` (CR 1-1 verbatim 적용, action_class='AUDIT' + action='audit_log_purged' + actor_id='system' + tenant_id + row_count + retention_rule + trace_id) / (2) `audit_log_archived` (action_class='AUDIT' + action='audit_log_archived' + actor_id='system' + tenant_id + row_count + archive_table + retention_rule + trace_id) / (3) `audit_log_pii_masked` (action_class='AUDIT' + action='audit_log_pii_masked' + actor_id='system' + tenant_id + row_count + mask_columns + trace_id) / (4) `audit_log_cold_archived` (action_class='AUDIT' + action='audit_log_cold_archived' + actor_id='system' + tenant_id + row_count + trace_id) / (5) `audit_log_personal_data_erased` (action_class='AUDIT' + action='audit_log_personal_data_erased' + actor_id=<owner> + tenant_id + target_email + rows_affected + trace_id)
- **apps/api/core/audit_action.py MODIFIED** 결정 wire = AuditAction Literal 신규 5 values EXTENSION 결정 wire + AuditAction Union EXTENSION 결정 wire + _ActionRegistry AUDIT entry 신규 5개 등록 결정 wire + __all__ EXTENSION 결정 wire (Epic 17 wire `2ada2ec` 의 1 NEW AuditAction Literal EXTENSION pattern verbatim 적용)
- **audit-first INSERT BEFORE DELETE/UPDATE/archive** 결정 wire = CR 1-1 verbatim 적용 — purge/archive/mask 모두 audit-first INSERT + 이후 archive INSERT + 이후 DELETE/UPDATE chain 결정 wire (CR 1-1 audit-first BEFORE actual mutation discipline)
- **immutable audit log table** 결정 wire = audit_log table 자체는 immutable (Epic 17 wire 가 정한 audit_log trigger 결정 wire 보존) — 단 archive row 는 별도 archive table 로 EXTENSION (CR 1-1 정합 + retention + archive 동시 지원 territory 결정 wire)

## F22.6 capability gate AUDIT_LOG_RETENTION (capability matrix v1.30 → v1.31 EXTENSION, A195 결정)
- **4 _INDUSTRY_CAPABILITIES blocks EXTENSION** 결정 wire = `apps/api/core/capability.py` MODIFIED + `Capability.AUDIT_LOG_RETENTION = "audit_log_retention"` 1 NEW enum 추가 결정 wire + 4 industries grants ✅/✅/✅/✅ 결정 wire (manufacturing + service + 겸영 + 겸영+기타 4-industry grants industry-agnostic, CR 12-1 L4 precedent 미러, AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind)
- **`require_audit_log_retention = require_capability(Capability.AUDIT_LOG_RETENTION)`** 결정 wire 결정 wire + `apps/api/dependencies/capability.py` EXTENSION + __all__ EXTENSION 결정 wire
- **owner-only RBAC AD-22** 결정 wire 결정 wire = manual audit log purge + manual archive + manual PII masking + manual GDPR erasure 모두 owner role required (Epic 12 2FA 챌린지 보존 결정 wire)
- **drift detector** 결정 wire = `tests/integration/test_capability_matrix_v1_31_drift.py` NEW (~+140 LOC, Epic 17 wire 의 `tests/integration/test_capability_matrix_v1_30_drift.py` 패턴 verbatim 적용 — capability matrix v1.31 EXTENSION 1 NEW row 정합 검증 + 4-industry grants + named gate dep + MULTI_REGION_BACKUP/FAILOVER + AUDIT_LOG_VIEW preservation 결정 wire 결정)

## F22.7 tests + wire scope T1~T8 (cj-style Phase 6 3번째 진입점 = cj-style 87번째 wire 진입, A197 결정)
- **T1 retention policy module** 결정 wire = `apps/api/modules/audit/audit_retention_policy.py` NEW ~+120 LOC 결정 wire + 8 NEW pytest cases (default policy + per-tenant override + policy evaluation precedence + action_class 별 retention rule + mask_pii boundary + archive boundary + industry baseline + default fallback 결정 wire)
- **T2 automatic purge job** 결정 wire = `apps/api/jobs/audit_purge.py` NEW ~+150 LOC + `apps/api/jobs/audit_cold_archive.py` NEW ~+120 LOC 결정 wire + 12 NEW pytest cases (cron schedule + dry-run mode + mask_pii mode + archive mode + delete mode + 5 NEW actions audit-first INSERT + retention rule evaluation + tenant override 적용 + RLS 정합 + epoch boundary + edge cases + month-end boundary 결정 wire)
- **T3 audit_log_archive table alembic 0040** 결정 wire = `apps/api/alembic/versions/0040_phase_6_audit_retention.py` NEW + `audit_log_archive` table DDL + `phase_6_audit_purge_log` table DDL + `audit_log.archived_at` column ADD + immutable trigger + SHA-256 hash chain 결정 wire + 4 NEW pytest alembic + trigger + hash chain 결정 wire
- **T4 PII redaction logic** 결정 wire = `email_mask(email) -> "[REDACTED:sha256]"` + `ip_mask(ip) -> "[REDACTED:ip]/24"` 결정 wire 결정 wire + GDPR Article 17 erasure endpoint `DELETE /api/v1/audit/personal-data?email=` + 5 NEW pytest (email mask + IP mask + GDPR erasure + tenant notification + audit-first INSERT `audit_log_personal_data_erased` 결정 wire)
- **T5 capability v1.31** 결정 wire = capability matrix v1.30 → v1.31 EXTENSION AUDIT_LOG_RETENTION 1 NEW row + 4-industry grants industry-agnostic ✅/✅/✅/✅ + drift detector `tests/integration/test_capability_matrix_v1_31_drift.py` NEW 8 NEW pytest cases 결정 wire
- **T6 dry-run mode UI** 결정 wire = `apps/web/app/[locale]/(dashboard)/audit-log/retention/page.tsx` NEW ~+100 LOC + 2 components (`RetentionPolicyDryRunButton` + `RetentionPurgeLogTable`) 결정 wire + ko-KR.json `audit_log_retention.*` namespace EXTENSION 8 keys 결정 wire (CR 11-4 D-002 verbatim SSOT) + vitest RTL render discipline 결정 wire (CR 11-4 D-003 verbatim)
- **T7 tests** 결정 wire = estimated ~+30 NEW pytest PASS (audit_retention_policy 8 + audit_purge 12 + audit_cold_archive 4 + alembic 4 + PII redaction 5 + capability matrix v1.31 8 + audit action 5 신규 = ~+46 NEW pytest PASS) + ~+5 NEW vitest PASS (dry-run UI test 3 + SSOT drift 2 + ko-KR SSOT 검증 = ~+5 NEW vitest PASS) + 0 NEW ruff + 0 regressions 결정 wire
- **T8 atomic commit** 결정 wire = `git commit -F <file>` (CR 9-6 D5 prevention) + commit-msg file 신규 + handoff memory 신규 + MEMORY.md hook index 신규 EXTENSION + sprint-status.yaml MODIFIED (`phase-6-prd-entry: backlog → done` + A193~A197 action_items block 5 entries + `last_updated_note` v3.7 Epic 17 close-out retro prepend 결정 wire 결정 wire)
- **estimated** 결정 wire = ~+30 NEW pytest PASS + ~+5 NEW vitest PASS + 0 NEW ruff + 0 regressions 결정 wire 보존
- **3중 게이트 retro verification FINAL CLEAN** 결정 wire = (1) pytest focused: ~46/46 NEW PASS / (2) vitest focused: ~5/5 NEW PASS + i18n SSOT drift detector PASS / (3) ruff scoped: all checks passed / (4) tsc scoped: 0 NEW errors (apps/web frontend 변경 ext only — pre-existing baseline errors preserved per cj-style discipline) / (5) SDR drift gate PASS (vitest file count +1 NEW collected well within 5% tolerance + pytest 0 NEW + tsc 0 NEW from wire scope) / (6) commit_consistency gate PASS (CR 9-6 commit message discipline + A36 SDR 검증 4-step 자동 적용) / (7) D-DEFER-* grep guard PASS (CR 11-3 honest-DEFER discipline 87번째 epic 연속 정직 회복 검증 보존)

**Sidebar/MenuProvider hot-fix `01a06e4` (cj-style 79번째) + D-EPIC-16-REVIEW-DEFER-2~6 RESOLVE sprint `512ed6a` (cj-style 78번째) + Phase 5 close-out retro `b843565` (cj-style 76~77번째) + Phase 5 atomic wire `f093f8c` (cj-style 75번째) + Epic 17 close-out retro `be8f3bd` (cj-style 84번째) + Epic 17 T2+T3 UI wire `bb92879` (cj-style 83번째) + Epic 17 bmad-dev-story atomic wire T1~T8 backend `2ada2ec` (cj-style 82번째) cycle 정합 보존** (cj-style 49~84번째 누적 cycle 정직 회복 + Epic 1 ~ Epic 17 + Phase 3 + Phase 4 + Phase 5 + 1st release 모든 close-out retro 결정 verbatim preserve + EXTENSION) — Phase 6 PRD entry 진입 시점에 Epic 1 ~ Epic 17 + Phase 3 + Phase 4 + Phase 5 + 1st release 모든 close-out retro 결정 verbatim preserve + EXTENSION.

**partial wire 시도 0건 + single sprint atomic wire T1~T8 결정** (cj-style 87번째 epic 연속 정직 회복 wire 진입 시점에 결정). 결정 wire 일자: 2026-08-22 (KST). **next**: Phase 6 bmad-create-story spec entry 진입 (cj-style 86번째 epic 연속 정직 회복 bmad-create-story 진입 시점) OR Phase 6 bmad-dev-story atomic sprint wire T1~T8 진입 (cj-style 87번째 epic 연속 정직 회복 wire 진입 시점) OR Phase 6 close-out retro 진입 (cj-style 88번째 epic 연속 정직 회복 진입 시점) 결정 wire 보존.

---

# F23. Phase 7 territory (옵션 (a) Phase 7+ 진입 결정 wire, cj-style 89번째 epic 연속 정직 회복)

> **§F23 결정 wire 진입** — Phase 6 close-out retro `f9f006c` (cj-style 88번째 wire entry) 직후 다음 옵션 5종 중 **사용자 권장 결정 = 옵션 (a) Phase 7+ 진입 + 옵션 (b) Observability Stack 강화 (OpenTelemetry distributed tracing + Prometheus custom metrics + Grafana dashboards + Alerting) = Phase 7 territory 결정 wire 진입** (AskUserQuestion 결정 verbatim bind). rationale 5종: ① Phase 5 wire `f093f8c` 의 multi-region observability (§F20.5 multi-region health observability) 의 자연스러운 carry-over chain = OpenTelemetry distributed tracing (cross-region primary + secondary read replica 의 request flow 가시화) + Prometheus custom metrics (multi-region Sentry breadcrumb + Grafana dashboard EXTENSION) territory 가 §F20.5 의 자연스러운 EXTENSION territory (Phase 7 = §F23 신규 territory) — Phase 5 wire 가 multi-region failover 가시성을 제공했다면, Phase 7 은 OpenTelemetry distributed tracing (요청 흐름 추적) + Prometheus custom metrics (SLA 측정) + Alerting (장애 감지 + Slack/PagerDuty) territory 의 natural next 진입 ② cj-style discipline 회피 위험 방지 (49~88번째 누적 40-entry-point cycle + 79~83번째 hot-fix + RESOLVE sprint + Epic 17 5-entry-point + Epic 16 6-entry-point + Phase 6 4-entry-point + Phase 5 4-entry-point 패턴 모두 wire DONE 진입 정합 보존 후 즉시 Phase 7 진입 = 1-day atomic sprint discipline) ③ 비즈니스 우선순위 = NFR4 + GDPR + SOX + observability stack 강화 = enterprise 고객 onboarding 시 alert + SLA 99.95% + multi-region failover 가시성 + distributed tracing 필수 ④ Phase 4 wire `71a033a` 의 Sentry observability (Sentry browser SSR-safe + Sentry FastAPI server integration + tracesSampleRate=0.1) 의 자연스러운 carry-over chain = Sentry 의 OpenTelemetry distributed tracing EXTENSION territory + Sentry alert routing 결정 ⑤ 1st release launch wire DONE 진입 후 enterprise SLA = RPO 1h/RTO 4h → multi-region failover + Alerting 으로 99.95% SLA 달성 territory. master PRD v3.7 → v3.8 atomic edit (docs only). §F23 territory = Observability Stack 강화 결정 wire 진입 시점에 7 ACs (F23.1 OpenTelemetry distributed tracing + F23.2 Prometheus custom metrics + F23.3 Alerting system + F23.4 Frontend performance tracing (Browser RUM) + F23.5 audit-first INSERT 2 NEW actions + F23.6 capability gate OBSERVABILITY_TRACES + OBSERVABILITY_METRICS + F23.7 tests + wire scope T1~T8) + §8.1 M0-(p) observability stack 강화 AC 신규 + §15 로드맵 Phase 7 row status 백로그 → in-progress (PRD entry DONE 진입 wire) + §부록 A A223+A224+A225+A226+A227 신규 결정 표 + AD-34 Observability Stack 강화 신규 결정 + capability matrix v1.31 → v1.32 EXTENSION OBSERVABILITY_TRACES + OBSERVABILITY_METRICS 2 NEW rows (industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러, AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind). CR 11-3 honest-DEFER discipline 89번째 epic 연속 정직 회복 검증 ✅ (D-1-1-DEFER-1/2/3 RESOLVED 보존 60~89번째 + D-LAUNCH-1-DEFER-1 honestly preserved 65~89번째 + D-EPIC-16-REVIEW-DEFER-2~6 ✅ RESOLVED 보존 78~89번째 + D-PHASE-4-DR-DEFER-1/2 ✅ RESOLVED 보존 73~89번째 + D-EPIC-17-WIRE-DEFER-T2-T3-UI ✅ RESOLVED 보존 83~89번째 + D-RETENTION-1 ✅ RESOLVED 보존 85~89번째 + **D-OBSERVABILITY-1 honestly DEFER 보존 = Phase 7 PRD entry 진입 시점에 carry-over chain 정직 회복 결정 wire 진입: 1st release close-out retro + Epic 17 close-out retro + Phase 6 close-out retro "observability stack 강화 결정 wire 보류, Phase 7+ 진입 시점" verbatim 해소**).

## F23.1 OpenTelemetry distributed tracing (Phase 5 multi-region observability carry-over chain + Phase 4 Sentry observability carry-over, A223 결정)
`apps/api/core/tracing.py` NEW ~+200 LOC:
- **OTLP HTTP exporter** 결정 wire = OpenTelemetry Python SDK `opentelemetry-exporter-otlp-proto-http` AD-14 stack pin (`opentelemetry-api==1.27.0` + `opentelemetry-sdk==1.27.0` + `opentelemetry-exporter-otlp-proto-http==1.27.0`) 결정 wire 보존 (Phase 5 wire `f093f8c` 의 Sentry SDK pin 정합)
- **W3C Trace Context propagation** 결정 wire = `traceparent` + `tracestate` HTTP header 추출/주입 + FastAPI middleware `TraceContextMiddleware` 결정 wire + 비동기 trace context 보존 (CR 1-1 ContextVar 정합)
- **span enrichment** 결정 wire = `tenant_id` + `user_id` + `trace_id` + `request_id` + `client_ip` 자동 span attribute 추가 (Phase 17 audit_log 의 trace_id 정합 보존) + `apps/api/core/observability.py` MODIFIED
- **auto-instrumentation** 결정 wire = `opentelemetry-instrumentation-fastapi` + `opentelemetry-instrumentation-sqlalchemy` + `opentelemetry-instrumentation-httpx` + `opentelemetry-instrumentation-asyncpg` 4 instrumentors 결정 wire
- **span attributes** 결정 wire = `http.method` + `http.route` + `http.status_code` + `db.system` + `db.statement` + `tenant.id` + `user.id` + `request.path` 결정 wire
- **sampling** 결정 wire = head_based sampler ratio 1.0 dev + 0.1 prod (Phase 4 wire `71a033a` 의 `tracesSampleRate=0.1` Sentry 정합) 결정 wire
- **trace export** 결정 wire = OTLP HTTP endpoint `OTEL_EXPORTER_OTLP_ENDPOINT=http://otel-collector:4318` (Phase 5 multi-region Seoul + Tokyo replica 동일 collector) 결정 wire

## F23.2 Prometheus custom metrics + Grafana dashboards (Phase 4 Sentry + Phase 5 multi-region observability carry-over, A223 결정)
`apps/api/core/metrics.py` NEW ~+180 LOC + `docs/grafana-dashboards.md` NEW:
- **Counter metrics** 결정 wire = (1) `business_signups_total{industry,plan}` Counter 결정 wire / (2) `business_logins_total{method,outcome}` Counter (method=magic_link/oauth_google/oauth_naver/oauth_kakao/sso_saml/password, outcome=success/failure) 결정 wire / (3) `business_calculations_total{engine,outcome}` Counter (engine=traditional/abc/tdabc, outcome=success/validation_failure) 결정 wire / (4) `business_audit_log_purge_total{action_class,outcome}` Counter (action_class=audit/auth/data/security, outcome=success/failure) Phase 6 wire 정합 결정 wire
- **Histogram metrics** 결정 wire = (1) `business_cost_engine_duration_seconds{engine,tenant_size_bucket}` Histogram (bucket boundaries 0.1s + 0.5s + 1s + 2s + 5s + 10s + 30s) 결정 wire / (2) `business_ai_extraction_duration_seconds{model,outcome}` Histogram 결정 wire
- **Gauge metrics** 결정 wire = `business_active_tenants_gauge` (현재 활성 tenant 수) + `business_db_replication_lag_seconds{region}` (Phase 5 wire `f093f8c` 의 `phase_5_replication_lag` table 정합) 결정 wire
- **`/metrics` endpoint** 결정 wire = `GET /api/v1/metrics` Prometheus exposition format (Prometheus client python 결정 wire + FastAPI Response class + `prometheus-client==0.20.0` AD-14 stack pin) 결정 wire
- **Grafana dashboards** 결정 wire = `docs/grafana-dashboards.md` 4 NEW dashboards JSON 결정: (1) **business-signups** dashboard (signups rate + logins by method + active tenants) / (2) **cost-engine-performance** dashboard (calculations rate + cost engine p50/p95/p99 latency + engine comparison) / (3) **auth-flow** dashboard (logins by method + 2FA challenge rate + SSO SAML rate) / (4) **audit-log-purge** dashboard (purge rate by action_class + retention policy + audit log row count) 결정 wire + Grafana provisioning 결정 wire

## F23.3 Alerting system (Phase 4 Sentry alert wiring + Phase 5 multi-region observability carry-over, A223 결정)
`apps/api/core/alerting.py` NEW ~+120 LOC + `config/alert_rules.yaml` NEW:
- **Prometheus AlertManager integration** 결정 wire = `apps/api/core/alerting.py` `AlertManagerClient` class + AlertManager webhook receiver `POST /api/v1/internal/alertmanager-webhook` 결정 wire + AlertManager URL `ALERTMANAGER_URL` env 결정 wire
- **Sentry alert routing EXTENSION** 결정 wire = `apps/api/core/observability.py` MODIFIED + alert webhook handler 결정 wire + Sentry SDK `before_send_transaction` + alert severity mapping (info → warning → error → critical) 결정 wire
- **Slack webhook integration** 결정 wire = `SLACK_WEBHOOK_URL` env + `#bizup-alerts` channel + alert message format 결정 wire + Slack `attachments` JSON with severity color + trace_id link to Sentry + tenant_id link to admin dashboard 결정 wire
- **PagerDuty integration** 결정 wire = owner-only manual trigger `POST /api/v1/admin/pagerduty/test` 결정 wire + `PAGERDUTY_INTEGRATION_KEY` env 결정 wire + AD-22 RBAC + Epic 12 2FA 챌린지 보존 결정 wire
- **5 NEW alert rules** 결정 wire = `config/alert_rules.yaml` 5 NEW rule definitions: (1) **`HighErrorRate`**: `rate(http_requests_total{status=~"5.."}[5m]) > 0.05` for 5m, severity=critical / (2) **`SlowCalc`**: `histogram_quantile(0.99, business_cost_engine_duration_seconds_bucket) > 5` for 10m, severity=warning / (3) **`FailoverStuck`**: `business_db_replication_lag_seconds > 30` for 5m (Phase 5 wire `f093f8c` `phase_5_replication_lag` table 정합), severity=critical / (4) **`RetentionPurgeFailed`**: `time() - business_audit_log_purge_last_success_timestamp > 26 * 3600` (Phase 6 wire `24e1cd7` 의 KST cron 02:00 정합, 24h + buffer), severity=warning / (5) **`MultiRegionDown`**: `up{job="bizup-api"} == 0` for 1m (primary + secondary 둘 다 down), severity=critical 결정 wire
- **audit-first INSERT `alert_fired`** 결정 wire = CR 1-1 verbatim 적용 (action_class='OBSERVABILITY' + action='alert_fired' + actor_id='system' + alert_name + severity + tenant_id='system' + trace_id + payload_json + Sentry breadcrumb + Slack notification) 결정 wire + apps/api/core/audit_action.py MODIFIED 결정 wire

## F23.4 Frontend performance tracing (Browser RUM + W3C Trace Context propagation server → client, A223 결정)
`apps/web/lib/tracing.ts` NEW ~+150 LOC + `apps/web/instrumentation.ts` NEW:
- **Browser RUM via OpenTelemetry web SDK** 결정 wire = `@opentelemetry/sdk-trace-web` + `@opentelemetry/exporter-trace-otlp-http` AD-14 stack pin 결정 wire + `apps/web/lib/tracing.ts` 결정 wire
- **W3C Trace Context propagation server → client** 결정 wire = `apps/web/lib/api-fetch.ts` MODIFIED + trace context propagation `traceparent` header server response → client storage (sessionStorage `traceparent` key) → next request propagation 결정 wire (CR 1-1 ContextVar 정합)
- **Web Vitals auto-collection** 결정 wire = `web-vitals` library AD-14 stack pin + LCP + FID + CLS + INP + TTFB auto-collection → OpenTelemetry span attributes (`web.lcp` + `web.fid` + `web.cls` + `web.inp` + `web.ttfb`) 결정 wire
- **custom span attributes** 결정 wire = `user.tenant_id` + `user.role` + `user.industry` + `route.path` + `route.locale` 결정 wire
- **API client fetch instrumentation** 결정 wire = `apps/web/lib/api-fetch.ts` MODIFIED + auto-instrumentation `fetch()` calls + span attributes `http.method` + `http.url` + `http.status_code` + `tenant.id` 결정 wire
- **Next.js instrumentation hook** 결정 wire = `apps/web/instrumentation.ts` NEW + Next.js `register()` hook + on server start `apps/web/instrumentation-node.ts` NEW OpenTelemetry node SDK initialization 결정 wire
- **trace context sampling** 결정 wire = head_based sampler ratio 1.0 dev + 0.1 prod 결정 wire (backend 정합)
- **NFR18 ko-KR 정합** 결정 wire = trace_id 표시 시 ko-KR label "추적 ID" 사용 + alert UI 시 ko-KR 메시지 ("서비스에 문제가 발생했습니다. 잠시 후 다시 시도해주세요") 결정 wire

## F23.5 audit-first INSERT 2 NEW actions (CR 1-1 verbatim + ActionClass.OBSERVABILITY 신규 정의, A223 결정)
- **ActionClass.OBSERVABILITY 신규 정의** 결정 wire = `apps/api/core/audit_action.py` MODIFIED + `_ActionRegistry` EXTENSION ActionClass.OBSERVABILITY 신규 정의 + action_class values 결정 wire
- **2 NEW AuditAction Literal values** 결정 wire = (1) `alert_fired` (action_class='OBSERVABILITY' + action='alert_fired' + actor_id='system' + tenant_id='system' + alert_name + severity + trace_id + payload_json{alert_rule + threshold + current_value + runbook_url}) / (2) `trace_sampled` (action_class='OBSERVABILITY' + action='trace_sampled' + actor_id='system' + tenant_id + decision='sampled'|'dropped' + sampling_ratio + trace_id) 결정 wire
- **apps/api/core/audit_action.py MODIFIED** 결정 wire = AuditAction Literal 신규 2 values EXTENSION 결정 wire + AuditAction Union EXTENSION 결정 wire + _ActionRegistry OBSERVABILITY entry 신규 2개 등록 결정 wire + __all__ EXTENSION 결정 wire (Phase 6 wire `24e1cd7` 의 5 NEW AuditAction Literal EXTENSION pattern verbatim 적용)
- **audit-first INSERT BEFORE alerting trace** 결정 wire = CR 1-1 verbatim 적용 — alert 발동 시 audit-first INSERT + 이후 Slack notification + Sentry alert routing chain 결정 wire (CR 1-1 audit-first BEFORE actual mutation discipline)
- **immutable observability audit log** 결정 wire = observability audit log 도 immutable (Phase 6 wire 의 audit_log_archive 결정 wire 보존) — 단 observability alert row 는 audit_log table + audit_log_archive EXTENSION 결정 wire

## F23.6 Capability gates OBSERVABILITY_TRACES + OBSERVABILITY_METRICS (capability matrix v1.31 → v1.32 EXTENSION 2 NEW rows, A225 결정)
- **2 _INDUSTRY_CAPABILITIES blocks EXTENSION** 결정 wire = `apps/api/core/capability.py` MODIFIED + `Capability.OBSERVABILITY_TRACES = "observability_traces"` + `Capability.OBSERVABILITY_METRICS = "observability_metrics"` 2 NEW enum 추가 결정 wire + 4 industries grants ✅/✅/✅/✅ 결정 wire (manufacturing + service + 겸영 + 겸영+기타 4-industry grants industry-agnostic, CR 12-1 L4 precedent 미러, AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind)
- **`require_observability_traces = require_capability(Capability.OBSERVABILITY_TRACES)`** + **`require_observability_metrics = require_capability(Capability.OBSERVABILITY_METRICS)`** 결정 wire + `apps/api/dependencies/capability.py` EXTENSION + __all__ EXTENSION 결정 wire
- **owner-only RBAC AD-22** 결정 wire = manual alert ack + manual trace_id lookup + manual metric dashboard view + manual Grafana embed 모두 owner role required (Epic 12 2FA 챌린지 보존 결정 wire)
- **drift detector** 결정 wire = `tests/integration/test_capability_matrix_v1_32_drift.py` NEW (~+140 LOC, Phase 6 wire `24e1cd7` 의 `tests/integration/test_capability_matrix_v1_31_drift.py` 패턴 verbatim 적용 — capability matrix v1.32 EXTENSION 2 NEW rows 정합 검증 + 4-industry grants + named gate dep 2 NEW + AUDIT_LOG_RETENTION + MULTI_REGION_BACKUP/FAILOVER + AUDIT_LOG_VIEW preservation 결정 wire)

## F23.7 tests + wire scope T1~T8 (cj-style Phase 7 3번째 진입점 = cj-style 91번째 wire 진입, A227 결정)
- **T1 OpenTelemetry tracing module** 결정 wire = `apps/api/core/tracing.py` NEW ~+200 LOC + `apps/api/core/observability.py` MODIFIED 결정 wire + 6 NEW pytest cases (W3C Trace Context propagation + span enrichment tenant_id + auto-instrumentation FastAPI + auto-instrumentation SQLAlchemy + sampling decision head_based + OTLP HTTP exporter 결정 wire)
- **T2 Prometheus metrics module** 결정 wire = `apps/api/core/metrics.py` NEW ~+180 LOC + `GET /api/v1/metrics` endpoint + `apps/api/main.py` MODIFIED + 6 NEW pytest cases (Counter increment + Histogram observation + Gauge set + /metrics endpoint format + label cardinality limit + metric naming convention 결정 wire)
- **T3 Alerting module + 5 alert rules** 결정 wire = `apps/api/core/alerting.py` NEW ~+120 LOC + `config/alert_rules.yaml` NEW + `apps/api/core/observability.py` MODIFIED Slack + Sentry + PagerDuty integration 결정 wire + 4 NEW pytest cases (alert webhook payload format + alert severity mapping + Slack message format + audit-first INSERT `alert_fired` CR 1-1 verbatim 결정 wire)
- **T4 Grafana dashboards** 결정 wire = `docs/grafana-dashboards.md` NEW 4 dashboards JSON 결정 wire + Grafana provisioning 결정 wire + 2 NEW pytest cases (dashboard JSON validation + dashboard panel metric query 정합 결정 wire)
- **T5 capability v1.32** 결정 wire = capability matrix v1.31 → v1.32 EXTENSION OBSERVABILITY_TRACES + OBSERVABILITY_METRICS 2 NEW rows + 4-industry grants industry-agnostic ✅/✅/✅/✅ + drift detector `tests/integration/test_capability_matrix_v1_32_drift.py` NEW 8 NEW pytest cases 결정 wire
- **T6 audit action EXTENSION 2 NEW** 결정 wire = `apps/api/core/audit_action.py` MODIFIED + AuditAction Literal 2 NEW EXTENSION + _ActionRegistry OBSERVABILITY entry 신규 2개 등록 + __all__ EXTENSION + ActionClass.OBSERVABILITY 신규 정의 + 2 NEW pytest cases (AuditAction Literal 값 검증 + ActionClass.OBSERVABILITY enum value + resource_table "observability_alerts" 결정 wire)
- **T7 frontend tracing** 결정 wire = `apps/web/lib/tracing.ts` NEW ~+150 LOC + `apps/web/lib/api-fetch.ts` MODIFIED + `apps/web/instrumentation.ts` NEW + `apps/web/instrumentation-node.ts` NEW + W3C Trace Context propagation server → client 결정 wire + Web Vitals auto-collection LCP + FID + CLS + INP + TTFB 결정 wire + 5 NEW vitest cases (W3C Trace Context propagation + Web Vitals span attributes + API fetch instrumentation + Next.js instrumentation hook + ko-KR SSOT 검증 결정 wire)
- **T8 atomic commit** 결정 wire = `git commit -F <file>` (CR 9-6 D5 prevention) + commit-msg file 신규 + handoff memory 신규 + MEMORY.md hook index 신규 EXTENSION + sprint-status.yaml MODIFIED (`phase-7-prd-entry: backlog → done` + A223~A227 action_items block 5 entries + `last_updated_note` v3.8 Phase 7 PRD entry prepend 결정 wire 결정 wire)
- **estimated** 결정 wire = ~+30 NEW pytest PASS (tracing 6 + metrics 6 + alerting 4 + grafana 2 + audit action 2 + capability matrix v1.32 8 + integration e2e 2 = ~+30 NEW pytest PASS) + ~+10 NEW vitest PASS (frontend tracing 5 + SSOT drift 2 + ko-KR SSOT 검증 3 = ~+10 NEW vitest PASS) + 0 NEW ruff + 0 regressions 결정 wire 보존
- **3중 게이트 retro verification FINAL CLEAN** 결정 wire = (1) pytest focused: ~30/30 NEW PASS / (2) vitest focused: ~10/10 NEW PASS + i18n SSOT drift detector PASS / (3) ruff scoped: all checks passed / (4) tsc scoped: 0 NEW errors (apps/web frontend 변경 ext only — pre-existing baseline errors preserved per cj-style discipline) / (5) SDR drift gate PASS (vitest file count +1 NEW collected well within 5% tolerance + pytest 0 NEW + tsc 0 NEW from wire scope) / (6) commit_consistency gate PASS (CR 9-6 commit message discipline + A36 SDR 검증 4-step 자동 적용) / (7) D-DEFER-* grep guard PASS (CR 11-3 honest-DEFER discipline 89번째 epic 연속 정직 회복 검증 보존)

**Sidebar/MenuProvider hot-fix `01a06e4` (cj-style 79번째) + D-EPIC-16-REVIEW-DEFER-2~6 RESOLVE sprint `512ed6a` (cj-style 78번째) + Phase 5 close-out retro `b843565` (cj-style 76~77번째) + Phase 5 atomic wire `f093f8c` (cj-style 75번째) + Epic 17 close-out retro `be8f3bd` (cj-style 84번째) + Epic 17 T2+T3 UI wire `bb92879` (cj-style 83번째) + Epic 17 bmad-dev-story atomic wire T1~T8 backend `2ada2ec` (cj-style 82번째) + Phase 6 close-out retro `f9f006c` (cj-style 88번째) + Phase 6 atomic wire `24e1cd7` (cj-style 87번째) + Phase 6 spec entry `f5c14c9` (cj-style 86번째) cycle 정합 보존** (cj-style 49~88번째 누적 cycle 정직 회복 + Epic 1 ~ Epic 17 + Phase 3 + Phase 4 + Phase 5 + Phase 6 + 1st release 모든 close-out retro 결정 verbatim preserve + EXTENSION) — Phase 7 PRD entry 진입 시점에 Epic 1 ~ Epic 17 + Phase 3 + Phase 4 + Phase 5 + Phase 6 + 1st release 모든 close-out retro 결정 verbatim preserve + EXTENSION.

**partial wire 시도 0건 + single sprint atomic wire T1~T8 결정** (cj-style 91번째 epic 연속 정직 회복 wire 진입 시점에 결정). 결정 wire 일자: 2026-08-22 (KST). **next**: Phase 7 bmad-create-story spec entry 진입 (cj-style 90번째 epic 연속 정직 회복 bmad-create-story 진입 시점) OR Phase 7 bmad-dev-story atomic sprint wire T1~T8 진입 (cj-style 91번째 epic 연속 정직 회복 wire 진입 시점) OR Phase 7 close-out retro 진입 (cj-style 92번째 epic 연속 정직 회복 진입 시점) 결정 wire 보존.

---

# F24. Phase 8 territory (옵션 (a) Phase 8+ 진입 결정 wire, cj-style 93번째 epic 연속 정직 회복)

> **§F24 결정 wire 진입** — Phase 7 close-out retro `326fa9f` (cj-style 92번째 wire entry) 직후 다음 옵션 5종 중 **사용자 권장 결정 = 옵션 (a) Phase 8+ 진입 + 옵션 (a) Performance / Load Testing territory (Recommended) (k6 부하 테스트 + SLO/SLI 정의 + p99 latency budget 5s + Latency regression detector + Performance regression gate + Cost-engine benchmark V8 골든 + Load test report + runbook) = Phase 8 territory 결정 wire 진입** (AskUserQuestion 결정 verbatim bind). rationale 5종: ① Phase 7 wire `59b56cd` 의 7 NEW business metrics (`business_signups_total` + `business_logins_total` + `business_calculations_total` + `business_cost_engine_duration_seconds{engine,tenant_size_bucket}` Histogram + `business_audit_log_purge_total` + `business_active_tenants_gauge` + `business_ai_extraction_duration_seconds`) + Prometheus 4 dashboards + head_based sampler 1.0 dev / 0.1 prod + SlowCalc alert rule 의 natural backend carry-over chain 진입 territory 가 §F23.* 의 자연스러운 EXTENSION territory (Phase 8 = §F24 신규 territory) — Phase 7 이 business metric 을 정의했다면, Phase 8 은 그 metric 의 SLO/SLI 정의 + p99 latency budget + latency regression detector territory 의 natural next 진입 ② cj-style discipline 회피 위험 방지 (49~92번째 누적 44-entry-point cycle + 92번째 Phase 7 close-out retro + 91번째 atomic wire + 90번째 spec entry + 89번째 PRD entry 패턴 모두 wire DONE 진입 정합 보존 후 즉시 Phase 8 진입 = 1-day atomic sprint discipline) ③ 비즈니스 우선순위 = enterprise SLA 99.95% + RPO 1h/RTO 4h + p99 latency budget 5s + 비용 엔진 회귀 검증 territory 의 natural next (observability stack 의 측정 가능 territory) ④ Phase 7 wire `59b56cd` 의 OpenTelemetry distributed tracing + SlowCalc alert rule 의 자연스러운 carry-over chain = Phase 8 load testing + Phase 7 OpenTelemetry trace_id cross-reference + latency regression detector territory + Phase 5 multi-region failover 가시성 EXTENSION ⑤ 1st release close-out retro + Epic 17 close-out retro + Phase 6 close-out retro + Phase 7 close-out retro territory verbatim 보존 + AD-22 owner-only RBAC + Epic 12 2FA 챌린지 보존 + AD-14 stack pin + NFR4 PII minimization ✅ PRESERVED + AD-35 Performance Engineering 신규 결정 ((a)~(g) 7 sub-decisions 모두 결정 wire 진입). master PRD v3.8 → v3.9 atomic edit (docs only). §F24 territory = Performance / Load Testing 결정 wire 진입 시점에 7 ACs (F24.1 k6 부하 테스트 5 scenarios + F24.2 SLO/SLI 정의 4 metrics + F24.3 p99 latency budget 5s + F24.4 Latency regression detector + F24.5 Performance regression gate CI + F24.6 Cost-engine benchmark V8 골든 + F24.7 tests + wire scope T1~T8) + §8.1 M0-(q) performance / load testing AC 신규 + §15 로드맵 Phase 8 row status 백로그 → in-progress (PRD entry DONE 진입 wire) + §부록 A A253+A254+A255+A256+A257 신규 결정 표 + AD-35 Performance Engineering 신규 결정 + capability matrix v1.32 → v1.33 EXTENSION PERFORMANCE_TESTING 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅ (CR 12-1 L4 precedent 미러, OBSERVABILITY_TRACES + OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind). CR 11-3 honest-DEFER discipline 93번째 epic 연속 정직 회복 검증 ✅ (D-1-1-DEFER-1/2/3 RESOLVED 보존 60~93번째 + D-LAUNCH-1-DEFER-1 honestly preserved 65~93번째 + D-EPIC-16-REVIEW-DEFER-2~6 ✅ RESOLVED 보존 78~93번째 + D-PHASE-4-DR-DEFER-1/2 ✅ RESOLVED 보존 73~93번째 + D-EPIC-17-WIRE-DEFER-T2-T3-UI ✅ RESOLVED 보존 83~93번째 + D-RETENTION-1 ✅ RESOLVED 보존 85~93번째 + D-OBSERVABILITY-1 ✅ RESOLVED 보존 89~93번째 + **D-PERFORMANCE-1 honestly DEFER 보존 = Phase 8 PRD entry 진입 시점에 carry-over chain 정직 회복 결정 wire 진입: Phase 7 close-out retro "Performance / Load Testing territory 결정 wire 보류, Phase 8+ 진입 시점" verbatim 해소**).

## F24.1 k6 부하 테스트 5 scenarios (Phase 7 wire `59b56cd` 7 NEW business metrics + SlowCalc alert rule + head_based sampler carry-over chain, A253 결정)
`apps/api/tests/performance/k6/scripts/` 결정 wire + `docs/load-testing.md` EXTENSION 12 sections runbook 결정 wire:
- **scenario 1: smoke-test-100.js** 결정 wire = 100 VUs 30s GET /api/v1/health + POST /api/v1/auth/login + GET /api/v1/cost-engine/calculate (sanity test) 결정 wire + `k6==0.45.0` AD-14 stack pin + `k6 run --vus 100 --duration 30s scripts/smoke-test-100.js` 결정 wire
- **scenario 2: baseline-1000.js** 결정 wire = 1000 VUs 5min GET /api/v1/cost-engine/calculate (baseline load 1000 concurrent users) 결정 wire + `k6 run --vus 1000 --duration 5m scripts/baseline-1000.js` 결정 wire + p95 + p99 latency measurement 결정 wire
- **scenario 3: stress-5000.js** 결정 wire = 5000 VUs 10min GET /api/v1/cost-engine/calculate + GET /api/v1/audit-log/query + POST /api/v1/auth/login (stress test breaking point finding) 결정 wire + `k6 run --vus 5000 --duration 10m scripts/stress-5000.js` 결정 wire
- **scenario 4: soak-100-1h.js** 결정 wire = 100 VUs 1h GET /api/v1/cost-engine/calculate + POST /api/v1/auth/login (memory leak + connection pool exhaustion detection) 결정 wire + `k6 run --vus 100 --duration 1h scripts/soak-100-1h.js` 결정 wire
- **scenario 5: spike-10000.js** 결정 wire = 10000 VUs 1min ramp-up 0→10000 + ramp-down 10000→0 (spike test sudden traffic surge) 결정 wire + `k6 run --vus 10000 --duration 1m scripts/spike-10000.js` 결정 wire + `spike-check` threshold p99 < 10s 결정 wire

## F24.2 SLO/SLI 정의 4 metrics (Phase 7 wire `59b56cd` 7 NEW business metrics 의 자연스러운 carry-over chain, A253 결정)
`docs/slo-sli-definition.md` NEW ~+200 LOC 8 sections 결정 wire:
- **(1) `business_cost_engine_duration_seconds p99 < 5s`** 결정 wire = SLO 99% (월간 99% 의 request 가 p99 < 5s) + SLI `histogram_quantile(0.99, rate(business_cost_engine_duration_seconds_bucket[28d]))` 결정 wire + Phase 7 wire `59b56cd` 의 `business_cost_engine_duration_seconds{engine,tenant_size_bucket}` Histogram 자연스러운 carry-over chain 결정 wire
- **(2) `business_signups_total success_rate > 99%`** 결정 wire = SLO 99% (월간 99% 의 signup 이 success) + SLI `rate(business_signups_total{outcome="success"}[28d]) / rate(business_signups_total[28d])` 결정 wire
- **(3) `business_logins_total p99 < 1s`** 결정 wire = SLO 99.9% (월간 99.9% 의 login 이 p99 < 1s) + SLI `histogram_quantile(0.99, rate(business_logins_duration_seconds_bucket[28d]))` 결정 wire + Phase 7 wire `59b56cd` 의 `business_logins_total` EXTENSION 결정 wire
- **(4) `business_audit_log_purge_total success_rate > 99.9%`** 결정 wire = SLO 99.9% (월간 99.9% 의 audit_log_purge 가 success) + SLI `rate(business_audit_log_purge_total{outcome="success"}[28d]) / rate(business_audit_log_purge_total[28d])` 결정 wire + Phase 6 wire `24e1cd7` 의 audit_purge job 정합 결정 wire + error budget 0.1% / monthly burn rate alert 결정 wire

## F24.3 p99 latency budget 5s cost-engine (Phase 7 wire `59b56cd` SlowCalc alert rule 의 자연스러운 EXTENSION, A253 결정)
`apps/api/core/performance.py` NEW ~+150 LOC + `LatencyBudget` TypedDict 결정 wire:
- **`LatencyBudget` TypedDict** 결정 wire = `engine: Literal["traditional", "abc", "tdabc"]` + `p99_seconds: float` + `threshold: float` + `breach: bool` + `trace_id: str | None` 결정 wire + `check_latency_budget(engine, p99_seconds) -> LatencyBudgetResult` 결정 wire
- **industry baseline** 결정 wire = 4-industry grants industry-agnostic (manufacturing 5s + service 5s + 겸영 5s + 겸영+기타 5s) 결정 wire + per-tenant override EXTENSION (tenant_settings.performance.cost_engine_p99_seconds) 결정 wire + policy evaluation precedence tenant override > industry baseline > system default 결정 wire
- **CR 12-5 D-14 envelope** 결정 wire = `{engine, p99_seconds, threshold, breach, trace_id, timestamp}` 결정 wire + `LatencyBudgetBreachedError(400)` 1 NEW error class 결정 wire
- **Phase 7 SlowCalc alert rule 정합** 결정 wire = `histogram_quantile(0.99, business_cost_engine_duration_seconds_bucket) > 5` for 10m severity=warning 결정 wire 보존 + Phase 8 latency budget check EXTENSION 결정 wire

## F24.4 Latency regression detector (Phase 7 wire `59b56cd` 의 SlowCalc alert + Epic 11 V8 determinism 골든 fixture EXTENSION, A253 결정)
`apps/api/jobs/latency_regression.py` NEW ~+150 LOC + `docs/latency-regression-runbook.md` NEW 6 sections 결정 wire:
- **cron KST 매일 03:30 UTC 18:30** 결정 wire = APScheduler lifespan hook + `LATENCY_REGRESSION_CRON_HOUR=3` + `LATENCY_REGRESSION_CRON_MINUTE=30` env 결정 wire
- **drift detection algorithm** 결정 wire = 5 steps: ① 24시간 p99 latency sample 수집 (`business_cost_engine_duration_seconds_bucket`) / ② `BENCHMARK_GOLDEN_BASELINE_MS = {traditional: 800, abc: 1200, tdabc: 1500}` 비교 / ③ drift > 10% 자동 감지 (`BASELINE_THRESHOLD=1.10`) / ④ Sentry breadcrumb `LatencyRegressionDetected` capture_message / ⑤ Slack alert `#bizup-alerts` channel + audit-first INSERT `latency_regression_detected` CR 1-1 verbatim 결정 wire
- **audit-first INSERT `latency_regression_detected`** 결정 wire = CR 1-1 verbatim 적용 (action_class='OBSERVABILITY' + action='latency_regression_detected' + actor_id='system' + tenant_id='system' + engine + drift_pct + golden_baseline_ms + current_p99_ms + trace_id + payload_json{runbook_url, slo_target, breach_duration_seconds}) 결정 wire
- **Epic 11 V8 determinism 골든 fixture EXTENSION** 결정 wire = `tests/performance/golden/cost_engine_p99_baseline.json` NEW + 4 industries × 12 periods 골든 fixture 결정 wire + Epic 11 wire `f4b2b58` 의 V8 골든 fixture pattern verbatim 적용 결정 wire
- **dry-run mode** 결정 wire = `--dry-run` flag + Slack notification skip + audit-first INSERT `latency_regression_dryrun` action 결정 wire + `apps/web/app/[locale]/(dashboard)/admin/performance/page.tsx` NEW ~+150 LOC latency_regression_dryrun UI 결정 wire + ko-KR.json `performance.*` namespace EXTENSION 8 keys CR 11-4 D-002 verbatim SSOT 결정 wire

## F24.5 Performance regression gate (CI 단계 k6 부하 테스트 자동 실행 + p99 budget 초과 시 CI 빌드 차단, A253 결정)
`.github/workflows/ci-performance.yml` NEW ~+120 LOC 결정 wire + `docs/ci-performance-gate.md` NEW 6 sections 결정 wire:
- **CI 단계 k6 부하 테스트 자동 실행** 결정 wire = GitHub Actions workflow `ci-performance` + `pull_request` trigger + `paths: ['apps/api/core/performance.py', 'apps/api/jobs/latency_regression.py', 'packages/cost_engine/**']` filter 결정 wire
- **p99 budget check** 결정 wire = k6 run `scripts/baseline-1000.js` + threshold `p99 < 5s` + `BASELINE_THRESHOLD=1.10` (10% drift) 결정 wire + threshold 초과 시 CI exit code 1 결정 wire
- **CI 빌드 차단** 결정 wire = GitHub Actions `if: failure()` → PR comment 자동 생성 + `Performance Regression Detected` label 자동 추가 결정 wire + ko-KR 메시지 ("성능 회귀가 감지되었습니다. p99 latency budget 5s 초과") CR 11-4 D-002 verbatim 결정 wire
- **NFR18 ko-KR 정합** 결정 wire = CI comment 한국어 only + ko-KR label "성능-회귀-감지" 결정 wire + CR 11-4 D-005 unknown state reject 결정 wire (unknown label reject)
- **Phase 7 wire `59b56cd` 의 `business_cost_engine_duration_seconds` Histogram EXTENSION** 결정 wire = CI 단계에서 histogram_quantile 측정 + threshold check 결정 wire + `apps/api/core/performance.py` 의 `check_latency_budget` EXTENSION 결정 wire

## F24.6 Cost-engine benchmark V8 골든 (Epic 11 V8 determinism 골든 fixture EXTENSION, A253 결정)
`tests/performance/test_cost_engine_benchmark.py` NEW ~+250 LOC 결정 wire + `tests/performance/golden/cost_engine_p99_baseline.json` NEW 결정 wire:
- **Epic 11 V8 determinism 골든 fixture EXTENSION** 결정 wire = Epic 11 wire `f4b2b58` 의 V8 골든 fixture pattern verbatim 적용 + 12-period benchmark 결정 wire + `tests/regression_v8/test_v8_determinism.py` pattern verbatim bind 결정 wire
- **cost-engine 3 engines benchmark** 결정 wire = `packages/cost_engine/traditional/calculator.py` + `packages/cost_engine/abc/calculator.py` + `packages/cost_engine/tdabc/calculator.py` 3 engines p99 latency benchmark 결정 wire + `BENCHMARK_GOLDEN_BASELINE_MS = {traditional: 800, abc: 1200, tdabc: 1500}` 결정 wire + `BENCHMARK_DRIFT_THRESHOLD = 1.10` 결정 wire
- **CR 4-4 V8 골든 fixture lesson 적용** 결정 wire = tenant-scoped result_hash + golden_diff + 0.5 plumbing 결정 wire + Epic 11 wire 의 V8 골든 fixture verbatim 적용 결정 wire + V8 determinism byte-identical 정합 결정 wire
- **pytest cases** 결정 wire = 4 NEW pytest cases (`test_cost_engine_benchmark_traditional` + `test_cost_engine_benchmark_abc` + `test_cost_engine_benchmark_tdabc` + `test_cost_engine_benchmark_12_periods_determinism`) 결정 wire
- **Phase 7 wire `59b56cd` 의 `business_cost_engine_duration_seconds{engine,tenant_size_bucket}` Histogram EXTENSION** 결정 wire = histogram_quantile 측정 + per-engine + per-tenant_size_bucket baseline 결정 wire

## F24.7 tests + wire scope T1~T8 (cj-style Phase 8 3번째 진입점 = cj-style 95번째 wire 진입, A257 결정)
- **T1 k6 부하 테스트 5 scenarios** 결정 wire = `apps/api/tests/performance/k6/scripts/smoke-test-100.js` + `baseline-1000.js` + `stress-5000.js` + `soak-100-1h.js` + `spike-10000.js` 5 NEW 결정 wire + `docs/load-testing.md` EXTENSION 12 sections runbook 결정 wire + `k6==0.45.0` AD-14 stack pin 결정 wire + 4 NEW pytest cases (k6 parser validation + scenario load + threshold check + report generation 결정 wire)
- **T2 SLO/SLI 정의 4 metrics** 결정 wire = `docs/slo-sli-definition.md` NEW 8 sections + 4 SLO/SLI 결정 wire + Phase 7 wire `59b56cd` 의 7 NEW business metrics EXTENSION 결정 wire + 6 NEW pytest cases (SLO validation + SLI calculation + error budget + burn rate alert 결정 wire)
- **T3 p99 latency budget 5s cost-engine** 결정 wire = `apps/api/core/performance.py` NEW ~+150 LOC + `LatencyBudget` TypedDict + `check_latency_budget` 결정 wire + `LatencyBudgetBreachedError(400)` 1 NEW error class CR 12-5 D-14 envelope 결정 wire + 4 industries baseline + per-tenant override EXTENSION 결정 wire + 5 NEW pytest cases (latency budget check + breach detection + per-tenant override + 4 industries baseline + CR 12-5 D-14 envelope 결정 wire)
- **T4 Latency regression detector** 결정 wire = `apps/api/jobs/latency_regression.py` NEW ~+150 LOC + cron KST 매일 03:30 UTC 18:30 + drift > 10% p99 vs golden baseline 자동 감지 + Sentry breadcrumb + Slack alert + audit-first INSERT `latency_regression_detected` CR 1-1 verbatim 결정 wire + `apps/web/app/[locale]/(dashboard)/admin/performance/page.tsx` NEW ~+150 LOC + ko-KR.json `performance.*` namespace EXTENSION 8 keys CR 11-4 D-002 verbatim SSOT 결정 wire + 5 NEW pytest cases (drift detection + Slack alert + audit-first INSERT + golden baseline validation + dry-run mode 결정 wire)
- **T5 Performance regression gate CI** 결정 wire = `.github/workflows/ci-performance.yml` NEW ~+120 LOC + k6 부하 테스트 자동 실행 + p99 budget check + threshold 초과 시 CI 빌드 차단 결정 wire + `BASELINE_THRESHOLD=1.10` + 2 NEW pytest cases (CI workflow validation + threshold check logic 결정 wire)
- **T6 Cost-engine benchmark V8 골든** 결정 wire = `tests/performance/test_cost_engine_benchmark.py` NEW ~+250 LOC + Epic 11 V8 determinism 골든 fixture EXTENSION + cost-engine 3 engines benchmark 결정 wire + `BENCHMARK_GOLDEN_BASELINE_MS = {traditional: 800, abc: 1200, tdabc: 1500}` + `BENCHMARK_DRIFT_THRESHOLD = 1.10` 결정 wire + 4 NEW pytest cases (3 engines benchmark + 12-period determinism 결정 wire)
- **T7 capability v1.33 EXTENSION** 결정 wire = `apps/api/core/capability.py` MODIFIED + `Capability.PERFORMANCE_TESTING = "performance_testing"` 1 NEW enum + 4-industry grants ✅/✅/✅/✅ CR 12-1 L4 precedent 미러 + `apps/api/dependencies/capability.py` EXTENSION `require_performance_testing` 1 NEW dep + owner-only RBAC AD-22 verbatim 보존 + capability matrix v1.32 → v1.33 EXTENSION PERFORMANCE_TESTING 1 NEW row + drift detector `tests/integration/test_capability_matrix_v1_33_drift.py` NEW 6 NEW pytest cases 결정 wire
- **T8 atomic commit** 결정 wire = `git commit -F <file>` (CR 9-6 D5 prevention) + commit-msg file 신규 + handoff memory 신규 + MEMORY.md hook index 신규 EXTENSION + sprint-status.yaml MODIFIED (`phase-8-prd-entry: backlog → done` + A253~A257 action_items block 5 entries + `last_updated_note` v3.9 Phase 8 PRD entry prepend 결정 wire 결정 wire)
- **estimated** 결정 wire = ~+25 NEW pytest PASS (k6 parser 4 + latency_budget 6 + latency_regression 5 + cost_engine_benchmark 4 + capability matrix v1.33 6 = ~+25 NEW pytest PASS) + ~+5 NEW vitest PASS (load-test-report UI 2 + SSOT drift 2 + ko-KR SSOT 검증 1 = ~+5 NEW vitest PASS) + 0 NEW ruff + 0 regressions 결정 wire 보존
- **3중 게이트 retro verification FINAL CLEAN** 결정 wire = (1) pytest focused: ~25/25 NEW PASS / (2) vitest focused: ~5/5 NEW PASS + i18n SSOT drift detector PASS / (3) ruff scoped: all checks passed / (4) tsc scoped: 0 NEW errors (apps/web frontend 변경 ext only — pre-existing baseline errors preserved per cj-style discipline) / (5) SDR drift gate PASS (vitest file count +1 NEW collected well within 5% tolerance + pytest 0 NEW + tsc 0 NEW from wire scope) / (6) commit_consistency gate PASS (CR 9-6 commit message discipline + A36 SDR 검증 4-step 자동 적용) / (7) D-DEFER-* grep guard PASS (CR 11-3 honest-DEFER discipline 93번째 epic 연속 정직 회복 검증 보존)

**Phase 7 close-out retro `326fa9f` (cj-style 92번째) + Phase 7 atomic wire T1~T8 `59b56cd` (cj-style 91번째) + Phase 7 spec entry `749381e` (cj-style 90번째) + Phase 7 PRD entry `916a541` (cj-style 89번째) + Phase 6 close-out retro `f9f006c` (cj-style 88번째) + Phase 6 atomic wire T1~T8 `24e1cd7` (cj-style 87번째) + Phase 6 spec entry `f5c14c9` (cj-style 86번째) + Phase 6 PRD entry `e84a281` (cj-style 85번째) + Epic 17 close-out retro `be8f3bd` (cj-style 84번째) + Epic 17 T2+T3 UI wire `bb92879` (cj-style 83번째) + Epic 17 atomic wire `2ada2ec` (cj-style 82번째) + Epic 17 spec entry `f4b2b58` (cj-style 81번째) + Epic 17 PRD entry `40a9c41` (cj-style 80번째) cycle 정합 보존** (cj-style 49~92번째 누적 cycle 정직 회복 + Epic 1 ~ Epic 17 + Phase 3 + Phase 4 + Phase 5 + Phase 6 + Phase 7 + 1st release 모든 close-out retro 결정 verbatim preserve + EXTENSION) — Phase 8 PRD entry 진입 시점에 Epic 1 ~ Epic 17 + Phase 3 + Phase 4 + Phase 5 + Phase 6 + Phase 7 + 1st release 모든 close-out retro 결정 verbatim preserve + EXTENSION.

**partial wire 시도 0건 + single sprint atomic wire T1~T8 결정** (cj-style 95번째 epic 연속 정직 회복 wire 진입 시점에 결정). 결정 wire 일자: 2026-08-24 (KST). **next**: Phase 8 bmad-create-story spec entry 진입 (cj-style 94번째 epic 연속 정직 회복 bmad-create-story 진입 시점) OR Phase 8 bmad-dev-story atomic sprint wire T1~T8 진입 (cj-style 95번째 epic 연속 정직 회복 wire 진입 시점) OR Phase 8 close-out retro 진입 (cj-style 96번째 epic 연속 정직 회복 진입 시점) 결정 wire 보류.

---

# F25. Phase 9 territory (옵션 (a) Phase 9+ 진입 결정 wire, cj-style 97번째 epic 연속 정직 회복)

> **§F25 결정 wire 진입** — Phase 8 close-out retro `ab495a8` (cj-style 96번째 wire entry) 직후 다음 옵션 5종 중 **사용자 권장 결정 = 옵션 (a) Phase 9+ 진입 + 옵션 (a) Chaos Engineering / Game Day (Recommended) (chaos experiment definition + fault injection types + game day runbook + blast radius control + continuous chaos + tenant-scoped + multi-region chaos + auto-rollback + safety mechanisms + dry-run mode + observability integration) = Phase 9 territory 결정 wire 진입** (AskUserQuestion 결정 verbatim bind). rationale 5종: ① Phase 8 wire `60d4ea1` 의 k6 부하 테스트 5 scenarios + SLO/SLI 정의 4 metrics + p99 latency budget 5s + Latency regression detector + Cost-engine benchmark V8 골든 + Performance regression gate CI 의 natural backend carry-over chain 진입 territory 가 §F24.* 의 자연스러운 EXTENSION territory (Phase 9 = §F25 신규 territory) — Phase 8 이 SLO/SLI 를 정의 + p99 latency budget 으로 정상 상태의 latency 를 측정했다면, Phase 9 은 chaos experiment 로 비정상 상태 (latency spike + error rate spike + resource exhaustion + network partition) 에서의 system resilience 측정 + auto-rollback + blast radius control + game day runbook territory 의 natural next 진입 ② cj-style discipline 회피 위험 방지 (49~96번째 누적 48-entry-point cycle + 96번째 Phase 8 close-out retro + 95번째 atomic wire + 94번째 spec entry + 93번째 PRD entry + build-fixes sprint `eaee198` 패턴 모두 wire DONE 진입 정합 보존 후 즉시 Phase 9 진입 = 1-day atomic sprint discipline) ③ 비즈니스 우선순위 = enterprise SLA 99.95% + RPO 1h/RTO 4h + p99 latency budget 5s + chaos engineering 으로 resilience 측정 territory 의 natural next (Phase 5 multi-region failover + Phase 7 observability + Phase 8 performance 의 측정 가능 territory 의 natural next) ④ Phase 5 wire `f093f8c` 의 multi-region failover (cj-style 75번째) + Phase 7 wire `59b56cd` 의 observability stack 강화 + Phase 8 wire `60d4ea1` 의 performance / load testing 의 자연스러운 carry-over chain = Phase 9 chaos engineering + Phase 5 multi-region failover drill 의 cross-region chaos + Phase 7 OpenTelemetry distributed tracing 의 chaos experiment trace propagation + Phase 8 p99 latency budget 의 chaos baseline EXTENSION ⑤ 1st release close-out retro + Epic 17 close-out retro + Phase 5 close-out retro + Phase 6 close-out retro + Phase 7 close-out retro + Phase 8 close-out retro territory verbatim 보존 + AD-22 owner-only RBAC + Epic 12 2FA 챌린지 보존 + AD-14 stack pin + NFR4 PII minimization ✅ PRESERVED + AD-36 Chaos Engineering / Game Day 신규 결정 ((a)~(g) 7 sub-decisions 모두 결정 wire 진입). master PRD v3.9 → v4.0 atomic edit (docs only). §F25 territory = Chaos Engineering / Game Day 결정 wire 진입 시점에 7 ACs (F25.1 chaos experiment definition + F25.2 fault injection types + F25.3 game day runbook + blast radius control + F25.4 continuous chaos vs scheduled game day + F25.5 tenant-scoped + multi-region chaos + F25.6 auto-rollback + safety mechanisms + F25.7 tests + wire scope T1~T8) + §8.1 M0-(r) chaos engineering / game day AC 신규 + §15 로드맵 Phase 9 row status 백로그 → in-progress (PRD entry DONE 진입 wire) + §부록 A A283+A284+A285+A286+A287 신규 결정 표 + AD-36 Chaos Engineering / Game Day 신규 결정 + capability matrix v1.33 → v1.34 EXTENSION CHAOS_ENGINEERING 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅ (CR 12-1 L4 precedent 미러, PERFORMANCE_TESTING Phase 8 wire + OBSERVABILITY_TRACES + OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind). CR 11-3 honest-DEFER discipline 97번째 epic 연속 정직 회복 검증 ✅ (D-1-1-DEFER-1/2/3 RESOLVED 보존 60~97번째 + D-LAUNCH-1-DEFER-1 honestly preserved 65~97번째 + D-EPIC-16-REVIEW-DEFER-2~6 ✅ RESOLVED 보존 78~97번째 + D-PHASE-4-DR-DEFER-1/2 ✅ RESOLVED 보존 73~97번째 + D-EPIC-17-WIRE-DEFER-T2-T3-UI ✅ RESOLVED 보존 83~97번째 + D-RETENTION-1 ✅ RESOLVED 보존 85~97번째 + D-OBSERVABILITY-1 ✅ RESOLVED 보존 89~97번째 + D-PERFORMANCE-1 ✅ RESOLVED 보존 93~97번째 + **D-CHAOS-1 honestly DEFER 보존 = Phase 9 PRD entry 진입 시점에 carry-over chain 정직 회복 결정 wire 진입: Phase 7 close-out retro §10 + Phase 8 close-out retro territory "Chaos Engineering / Game Day 결정 wire 보류, Phase 9+ 진입 시점" verbatim 해소**).

## F25.1 chaos experiment definition (steady state + hypothesis + blast radius + abort condition, A283 결정)
`apps/api/modules/chaos/chaos_experiment.py` NEW ~+150 LOC + `docs/chaos-engineering.md` EXTENSION 14 sections 결정 wire + ChaosExperiment TypedDict 결정 wire:
- **ChaosExperiment TypedDict** 결정 wire = `experiment_id: str` + `name: str` (e.g., `cost-engine-latency-injection`) + `description: str` + `steady_state_metric: str` (e.g., `business_cost_engine_duration_seconds p99 < 5s` Phase 8 SLA 정합) + `hypothesis: str` (e.g., "p99 latency 가 SLA budget 내 유지 + auto-rollback 30s 이내 동작") + `fault_type: Literal["latency", "error", "resource", "network_partition", "disk_io", "cpu_stress", "memory_stress", "db_connection_pool", "cache_failure", "dns_failure"]` 10 types 결정 wire + `target_service: str` (e.g., `cost-engine`, `auth`, `audit-log-query`, `multi-region-failover`) + `target_endpoint: str | None` (optional HTTP path) + `blast_radius: Literal["single_request", "single_tenant", "all_tenants", "single_region", "multi_region"]` 5 levels 결정 wire + `duration_seconds: int` (max 600s = 10min) + `intensity: Literal["low", "medium", "high"]` 결정 wire + `abort_conditions: list[AbortCondition]` 결정 wire + `rollback_strategy: Literal["automatic", "manual", "hybrid"]` 결정 wire + `owner_only: bool = True` AD-22 RBAC 결정 wire + `dry_run: bool = True` default 결정 wire
- **steady state hypothesis** 결정 wire = Phase 8 wire `60d4ea1` 의 `business_cost_engine_duration_seconds{engine,tenant_size_bucket}` p99 < 5s SLA + `business_signups_total` success_rate > 99% + `business_logins_total` p99 < 1s + `business_audit_log_purge_total` success_rate > 99.9% 4 SLO 의 자연스러운 carry-over chain 결정 wire + chaos experiment 의 steady state 정의 = hypothesis 검증 의 SSOT (single source of truth) 결정 wire
- **blast radius control 5 levels** 결정 wire = L1 `single_request` (1 request 만 영향) + L2 `single_tenant` (1 tenant 만 영향, 기본값) + L3 `all_tenants` (모든 tenant 영향, owner-only + 2FA 챌린지 Epic 12 정합) + L4 `single_region` (1 region 만 영향, Phase 5 multi-region 정합) + L5 `multi_region` (multi-region 영향, Phase 5 failover drill 정합) 결정 wire + L3~L5 는 owner-only RBAC AD-22 verbatim + Epic 12 2FA 챌린지 보존 결정 wire
- **abort conditions 4 rules** 결정 wire = (a) steady_state_metric > 1.5x baseline (auto-abort) + (b) error_rate > 5% (auto-abort) + (c) experiment_duration > max (auto-abort) + (d) external abort signal via `POST /api/v1/admin/chaos/{experiment_id}/abort` (manual abort, owner-only AD-22 + 2FA 챌린지 Epic 12 정합) 결정 wire

## F25.2 fault injection types 10 categories (Phase 8 wire `60d4ea1` 의 k6 부하 테스트 EXTENSION + chaos engineering territory, A283 결정)
`apps/api/modules/chaos/fault_injection.py` NEW ~+200 LOC + fault type registry 결정 wire:
- **(1) latency injection** 결정 wire = `inject_latency(target, delay_ms, jitter_ms, percentage)` 함수 + HTTP middleware `LatencyFaultMiddleware` 결정 wire + delay_ms 100~5000ms configurable + jitter_ms ±20% 결정 wire + percentage 0~100% (default 10%) + Phase 8 wire `60d4ea1` 의 p99 latency budget 5s 와의 정합 결정 wire
- **(2) error injection** 결정 wire = `inject_error(target, http_status, percentage)` 함수 + HTTP middleware `ErrorFaultMiddleware` 결정 wire + http_status 500/502/503/504 configurable + percentage 0~100% (default 5%) + Sentry breadcrumb 결정 wire
- **(3) resource exhaustion (CPU + memory)** 결정 wire = `stress_cpu(target, cores)` + `stress_memory(target, mb)` 함수 + `cgroups` 또는 `resource` library AD-14 stack pin 결정 wire + cores 1~N configurable + mb 100~N configurable + OS-level fault injection 결정 wire
- **(4) network partition (latency + drop + bandwidth)** 결정 wire = `network_partition(target, delay_ms, drop_pct, bandwidth_kbps)` 함수 + Linux `tc netem` AD-14 stack pin 결정 wire + target service 간 traffic 제어 결정 wire + Phase 5 wire `f093f8c` multi-region network chaos EXTENSION 결정 wire
- **(5) disk I/O stress** 결정 wire = `disk_io_stress(target, iops_limit, read_pct)` 함수 + `fio` AD-14 stack pin 결정 wire + iops_limit 100~10000 configurable + read_pct 0~100% 결정 wire
- **(6) database connection pool exhaustion** 결정 wire = `db_connection_pool_exhaust(target, max_connections)` 함수 + PostgreSQL `max_connections` + connection pool size 동적 조정 결정 wire + Alembic migration 통한 connection pool config 결정 wire
- **(7) cache failure (Redis + Supabase cache)** 결정 wire = `cache_failure(target, operation: Literal["read_miss", "write_fail", "eviction_burst"])` 함수 + Supabase cache eviction burst 결정 wire + Phase 7 wire `59b56cd` observability 정합 결정 wire
- **(8) DNS failure** 결정 wire = `dns_failure(target, domains: list[str])` 함수 + `/etc/hosts` 또는 DNS resolver 조작 결정 wire + Supabase auth + Sentry + Slack DNS 의존도 정합 결정 wire
- **(9) process kill** 결정 wire = `kill_process(target, signal: Literal["SIGTERM", "SIGKILL", "SIGSTOP"])` 함수 + FastAPI worker process target 결정 wire + auto-restart via Railway `restartPolicyType=ON_FAILURE` Phase 4 wire 정합 결정 wire
- **(10) clock skew** 결정 wire = `clock_skew(target, offset_seconds)` 함수 + `libfaketime` AD-14 stack pin 결정 wire + offset_seconds ±86400 configurable + JWT `exp` + audit log timestamp 영향 검증 결정 wire

## F25.3 game day runbook + blast radius control (Phase 5 wire `f093f8c` DR drill EXTENSION + quarterly game day schedule, A283 결정)
`docs/chaos-engineering.md` NEW ~+200 LOC 14 sections runbook 결정 wire:
- **quarterly game day schedule** 결정 wire = Phase 5 wire `f093f8c` 의 DR drill quarterly schedule (Q1 January + Q2 April + Q3 July + Q4 October, KST 1st Sunday 03:00 = UTC 18:00 cron) 의 자연스러운 EXTENSION 결정 wire + chaos game day + DR drill 통합 quarterly schedule 결정 wire + `apps/api/jobs/chaos_game_day.py` NEW ~+150 LOC (cron KST 1st Sunday 03:00 = UTC 18:00 + 8 game day steps: experiment selection + tenant scoping + blast radius confirmation + steady state baseline 측정 + fault injection + observation + auto-rollback + post-mortem report 결정)
- **8 game day steps** 결정 wire = (1) experiment selection (e.g., `cost-engine-latency-injection` + `auth-network-partition` + `multi-region-failover`) / (2) tenant scoping (L2 `single_tenant` default, staging tenant only) / (3) blast radius confirmation (owner-only + 2FA 챌린지 Epic 12 정합) / (4) steady state baseline 측정 (Phase 8 wire `60d4ea1` 의 `business_cost_engine_duration_seconds` + `business_calculations_total` baseline 5min capture) / (5) fault injection (`inject_latency` + `inject_error` 동시 실행, blast radius L2 single_tenant) / (6) observation (Phase 7 wire `59b56cd` 의 OpenTelemetry distributed tracing + Prometheus custom metrics + Sentry breadcrumb + Slack `#bizup-alerts` channel + PagerDuty integration) / (7) auto-rollback (F25.6 결정 wire) / (8) post-mortem report (`docs/chaos-game-day-{yyyymmdd}.md` NEW + lessons learned + 5 sections: experiment summary + observed metrics + auto-rollback performance + blast radius assessment + follow-up actions)
- **game day runbook 14 sections** 결정 wire = (1) 목적 + (2) 책임자 (owner + SRE on-call) + (3) 사전 준비 (tenant scoping + blast radius + 2FA 챌린지) + (4) communication channel (Slack `#chaos-game-day` channel + PagerDuty escalation) + (5) experiment schedule + (6) abort conditions + (7) rollback strategy + (8) observation checklist + (9) post-mortem template + (10) lessons learned archive + (11) quarterly review + (12) safety mechanisms (auto-abort + manual abort) + (13) compliance & audit (audit-first INSERT `chaos_experiment_started` + `chaos_experiment_completed` + `chaos_experiment_aborted` + `chaos_rollback_triggered` CR 1-1 verbatim) + (14) continuous improvement (다음 분기 실험 우선순위)
- **blast radius control 5 levels implementation** 결정 wire = L1 `single_request` (테스트 환경 only) + L2 `single_tenant` (staging tenant only, 기본값) + L3 `all_tenants` (production 환경, owner-only + 2FA 챌린지 필수, audit-first INSERT `chaos_experiment_started` with payload `{blast_radius: "all_tenants", two_factor_challenge: true}`) + L4 `single_region` (Phase 5 wire `f093f8c` multi-region 정합, primary Seoul 만 chaos) + L5 `multi_region` (DR drill 정합, primary Seoul + secondary Tokyo 동시 chaos, audit-first INSERT + Sentry critical alert)

## F25.4 continuous chaos vs scheduled game day (production-safe continuous chaos + game day quarterly, A284 결정)
`apps/api/jobs/continuous_chaos.py` NEW ~+120 LOC + continuous chaos decision 결정 wire:
- **continuous chaos (production-safe)** 결정 wire = L1 `single_request` blast radius 만 + low intensity + 5% traffic + dry_run default + auto-rollback 30s 이내 결정 wire + 4 production-safe experiment candidates: (a) `cost-engine-latency-injection-100ms` (Phase 8 SLA p99 < 5s 의 2%) + (b) `auth-error-injection-1pct` (Phase 8 login p99 < 1s 정합) + (c) `audit-log-query-latency-injection-50ms` (Phase 8 audit log p99 < 2s SLA 의 2.5%) + (d) `multi-region-replication-lag-injection` (Phase 5 replication lag 100MB threshold 정합) 결정 wire
- **scheduled game day** 결정 wire = quarterly cron KST 1st Sunday 03:00 = UTC 18:00 (Phase 5 DR drill schedule 정합) + multi-experiment 동시 실행 (latency + error + resource + network partition 4 types) + L2~L5 blast radius (owner-only + 2FA 챌린지 Epic 12 정합) + Slack `#chaos-game-day` channel 실시간 communication + PagerDuty integration owner-only AD-22 RBAC 결정 wire
- **production-safe guard 4 rules** 결정 wire = (a) blast radius L1 only (single_request) + (b) intensity low only + (c) percentage ≤ 5% traffic + (d) duration ≤ 60s + (e) auto-rollback ≤ 30s + (f) dry_run default (audit-first INSERT `chaos_experiment_dryrun`) + (g) Sentry breadcrumb + Slack notification 결정 wire
- **continuous chaos statistics** 결정 wire = Prometheus custom metrics (Phase 7 wire `59b56cd` 의 `business_chaos_experiments_total{experiment_name, blast_radius, outcome}` Counter + `business_chaos_auto_rollback_total{experiment_name, trigger}` Counter + `business_chaos_observations_seconds Histogram` 결정 wire) + 5% traffic 의 production 환경 의미 있는 chaos data 수집 + graph: chaos experiments per day + auto-rollback time p99 + experiment success rate

## F25.5 tenant-scoped + multi-region chaos (CR 0-2 RLS lesson + Phase 5 multi-region wire 정합, A284 결정)
`apps/api/modules/chaos/tenant_scoping.py` NEW ~+80 LOC + multi-region chaos decision 결정 wire:
- **tenant-scoped chaos** 결정 wire = `tenant_id` UUID RLS 자동 적용 (CR 0-2 RLS lesson 적용) + L2 `single_tenant` blast radius 의 tenant_id selector 결정 wire + cross-tenant isolation test (Epic 1 carry-over 정합) + audit-first INSERT `chaos_experiment_started` 에 tenant_id 포함 결정 wire + 미허용 tenant 의 chaos 진입 차단 결정 wire
- **multi-region chaos** 결정 wire = Phase 5 wire `f093f8c` 의 multi-region failover (cj-style 75번째) 정합 + `region: Literal["seoul", "tokyo", "all"]` selector + L4 `single_region` + L5 `multi_region` blast radius 결정 wire + cross-region chaos 시 primary Seoul → secondary Tokyo failover 자동 트리거 검증 (Phase 5 failover_orchestrator 정합) + audit-first INSERT `chaos_experiment_started` 에 `from_region` + `to_region` 포함 결정 wire
- **chaos regions table** 결정 wire = `apps/api/alembic/versions/0041_phase_9_chaos_engineering.py` NEW `phase_9_chaos_experiments` table (BIGSERIAL id + tenant_id UUID FK + experiment_id TEXT UNIQUE + experiment_name TEXT + fault_type TEXT enum 10 values + blast_radius TEXT enum 5 values + region TEXT enum seoul/tokyo/all + steady_state_metric TEXT + hypothesis TEXT + duration_seconds INTEGER + intensity TEXT enum low/medium/high + status TEXT enum pending/running/completed/aborted/failed + dry_run BOOLEAN DEFAULT TRUE + started_at TIMESTAMPTZ + completed_at TIMESTAMPTZ + actor_id UUID FK `users(id)` + trace_id TEXT + created_at TIMESTAMPTZ DEFAULT NOW() 결정) + 3 indexes (tenant_id+status+started_at DESC + experiment_id UNIQUE + region+status+started_at DESC) + 2 CHECK constraints (fault_type enum + blast_radius enum) 결정 wire
- **multi-tenant isolation test** 결정 wire = `tests/integration/test_chaos_tenant_isolation.py` NEW + Phase 5 wire 의 `tests/integration/test_multi_region_replication_lag.py` + Phase 7 wire 의 `tests/integration/test_observability_tenant_isolation.py` 패턴 verbatim 적용 + L2 single_tenant chaos 가 다른 tenant 에 영향 없음 검증 + L3 all_tenants chaos 가 명시적 tenant 에만 영향 검증 결정 wire

## F25.6 auto-rollback + safety mechanisms (Phase 5 failover + Phase 7 alerting EXTENSION, A285 결정)
`apps/api/modules/chaos/auto_rollback.py` NEW ~+150 LOC + safety mechanisms 결정 wire:
- **auto-rollback 4 strategies** 결정 wire = (a) **automatic** (abort condition trigger 시 30s 이내 fault 제거 + steady state 복귀 검증) / (b) **manual** (owner-only + 2FA 챌린지 Epic 12 정합, `POST /api/v1/admin/chaos/{experiment_id}/abort` endpoint + audit-first INSERT `chaos_experiment_aborted` CR 1-1 verbatim) / (c) **hybrid** (5min 이상 진행 시 manual confirm 필요) / (d) **scheduled abort** (duration_seconds 만료 시 자동 abort) 결정 wire
- **safety mechanisms 6 layers** 결정 wire = (1) **abort conditions 4 rules** (F25.1 결정) + (2) **blast radius 5 levels** (F25.1 결정) + (3) **owner-only RBAC AD-22** (L3~L5 blast radius + manual abort + 2FA 챌린지 Epic 12 정합) + (4) **dry-run mode default** (audit-first INSERT `chaos_experiment_dryrun` action, no actual fault injection) + (5) **steady state verification** (auto-rollback 후 5min baseline recovery 검증) + (6) **circuit breaker** (5 consecutive experiments failure 시 1h cool-down + Sentry alert + Slack `#bizup-alerts` channel 결정 wire)
- **audit-first INSERT 4 NEW actions** 결정 wire (CR 1-1 verbatim + ActionClass.CHAOS_ENGINEERING 신규 정의 + 4 NEW AuditAction Literal values: `chaos_experiment_started` (experiment_id + tenant_id + blast_radius + fault_type + dry_run + trace_id) + `chaos_experiment_completed` (experiment_id + tenant_id + outcome + duration_seconds + observation_metrics + trace_id) + `chaos_experiment_aborted` (experiment_id + tenant_id + abort_trigger + abort_reason + trace_id) + `chaos_rollback_triggered` (experiment_id + tenant_id + rollback_strategy + rollback_duration_seconds + trace_id) + apps/api/core/audit_action.py MODIFIED AuditAction Literal EXTENSION 4 NEW values + _ActionRegistry CHAOS_ENGINEERING entry 신규 4개 등록 + __all__ EXTENSION + emit_audit_typed BEFORE chaos experiment CR 1-1 verbatim 적용 + Phase 7 wire `59b56cd` 의 2 NEW AuditAction EXTENSION pattern verbatim 적용)
- **integration with Phase 5 failover + Phase 7 alerting** 결정 wire = L5 multi_region chaos 시 Phase 5 wire `f093f8c` failover_orchestrator 자동 트리거 검증 + Phase 7 wire `59b56cd` 의 Prometheus AlertManager + Sentry alert routing + Slack `#bizup-alerts` channel + PagerDuty integration EXTENSION + Phase 8 wire `60d4ea1` 의 p99 latency budget 5s 와의 정합 (chaos experiment 중 p99 > 5s 시 auto-abort 결정 wire)

## F25.7 tests + wire scope T1~T8 (cj-style Phase 9 3번째 진입점 = cj-style 99번째 wire 진입, A287 결정)
- **T1 chaos_experiment module** 결정 wire = `apps/api/modules/chaos/chaos_experiment.py` NEW (~+150 LOC + ChaosExperiment TypedDict + 5 blast_radius levels + 4 abort conditions + 10 fault types registry) + `apps/api/modules/chaos/fault_injection.py` NEW (~+200 LOC + 10 fault types implementation + LatencyFaultMiddleware + ErrorFaultMiddleware + resource stress + network partition + disk I/O + DB connection pool + cache failure + DNS failure + process kill + clock skew) + 5 NEW pytest cases (chaos_experiment TypedDict validation + blast_radius enum + abort_conditions rules + dry_run default + owner_only RBAC 결정 wire)
- **T2 chaos_game_day job** 결정 wire = `apps/api/jobs/chaos_game_day.py` NEW (~+150 LOC + quarterly cron KST 1st Sunday 03:00 = UTC 18:00 + 8 game day steps + post-mortem report generation) + `docs/chaos-engineering.md` NEW 14 sections runbook + 4 NEW pytest cases (game day steps execution + abort conditions trigger + post-mortem generation + blast radius confirmation 결정 wire)
- **T3 continuous_chaos job** 결정 wire = `apps/api/jobs/continuous_chaos.py` NEW (~+120 LOC + 4 production-safe experiment candidates + 5% traffic + auto-rollback 30s + Prometheus custom metrics `business_chaos_experiments_total` + `business_chaos_auto_rollback_total` + `business_chaos_observations_seconds`) + 4 NEW pytest cases (continuous chaos statistics + production-safe guard 4 rules + Prometheus metrics emission + auto-rollback timing 결정 wire)
- **T4 alembic 0041 phase_9_chaos_engineering** 결정 wire = `apps/api/alembic/versions/0041_phase_9_chaos_engineering.py` NEW (~+150 LOC + `phase_9_chaos_experiments` table + 14 columns + 3 indexes + 2 CHECK constraints + audit-first INSERT `chaos_experiment_started` trigger) + 3 NEW pytest cases (alembic migration + RLS policy + CHECK constraints 결정 wire)
- **T5 audit action EXTENSION 4 NEW** 결정 wire = `apps/api/core/audit_action.py` MODIFIED + AuditAction Literal 4 NEW EXTENSION (`chaos_experiment_started` + `chaos_experiment_completed` + `chaos_experiment_aborted` + `chaos_rollback_triggered`) + _ActionRegistry CHAOS_ENGINEERING entry 신규 4개 등록 + __all__ EXTENSION + ActionClass.CHAOS_ENGINEERING 신규 정의 + 6 NEW pytest cases (AuditAction Literal 값 검증 + ActionClass.CHAOS_ENGINEERING enum value + resource_table "chaos_experiments" + emit_audit_typed BEFORE chaos experiment CR 1-1 verbatim 적용 + multi-tenant isolation + trace_id propagation 결정 wire)
- **T6 capability v1.34** 결정 wire = capability matrix v1.33 → v1.34 EXTENSION CHAOS_ENGINEERING 1 NEW row + 4-industry grants industry-agnostic ✅/✅/✅/✅ + drift detector `tests/integration/test_capability_matrix_v1_34_drift.py` NEW 6 NEW pytest cases 결정 wire + apps/api/core/capability.py MODIFIED Capability.CHAOS_ENGINEERING = "chaos_engineering" 1 NEW enum + apps/api/dependencies/capability.py MODIFIED require_chaos_engineering 1 NEW dep + __all__ EXTENSION 결정 wire
- **T7 frontend chaos dashboard** 결정 wire = `apps/web/app/[locale]/(dashboard)/admin/chaos/page.tsx` NEW (~+150 LOC + 4 components: ChaosExperimentList + ChaosExperimentTriggerButton + ChaosGameDayCalendar + ChaosRollbackLog) + ko-KR.json `chaos.*` namespace EXTENSION ~25 keys (sla_dashboard_title + chaos_section_label + chaos_experiment_label + chaos_trigger_button + chaos_game_day_label + chaos_blast_radius_label + chaos_dry_run_label + chaos_abort_button + chaos_rollback_log_label + chaos_observation_label + chaos_post_mortem_label + chaos_safety_mechanisms_label + chaos_tenant_scope_label + chaos_region_selector_label + chaos_intensity_label + chaos_duration_label + chaos_abort_conditions_label + chaos_steady_state_label + chaos_hypothesis_label + chaos_success_rate_label + chaos_auto_rollback_label + loading_chaos + error_chaos_failed + error_owner_only + empty_state 결정) + CR 11-4 D-002 verbatim SSOT 결정 wire + 5 NEW vitest cases (ChaosExperimentTriggerButton + owner-only ack prompt AD-22 verbatim + ChaosGameDayCalendar ko-KR SSOT + ChaosRollbackLog RTL render + chaos dashboard parity CR 12-5 D-PARITY-01 결정 wire)
- **T8 atomic commit** 결정 wire = `git commit -F <file>` (CR 9-6 D5 prevention) + commit-msg file 신규 + handoff memory 신규 + MEMORY.md hook index 신규 EXTENSION + sprint-status.yaml MODIFIED (`phase-9-prd-entry: backlog → done` + A283~A287 action_items block 5 entries + `last_updated_note` v3.10 Phase 9 PRD entry prepend 결정 wire 결정 wire)
- **estimated** 결정 wire = ~+30 NEW pytest PASS (chaos_experiment 5 + fault_injection 4 + chaos_game_day 4 + continuous_chaos 4 + alembic 0041 3 + audit action 6 + capability matrix v1.34 6 = ~+30 NEW pytest PASS) + ~+5 NEW vitest PASS (chaos dashboard 3 + SSOT drift 2 = ~+5 NEW vitest PASS) + 0 NEW ruff + 0 regressions 결정 wire 보존
- **3중 게이트 retro verification FINAL CLEAN** 결정 wire = (1) pytest focused: ~30/30 NEW PASS / (2) vitest focused: ~5/5 NEW PASS + i18n SSOT drift detector PASS / (3) ruff scoped: all checks passed / (4) tsc scoped: 0 NEW errors (apps/web frontend 변경 ext only — pre-existing baseline errors preserved per cj-style discipline) / (5) SDR drift gate PASS (vitest file count +1 NEW collected well within 5% tolerance + pytest +6 NEW files collected well within 5% tolerance) / (6) commit_consistency gate PASS (CR 9-6 commit message discipline + A36 SDR 검증 4-step 자동 적용) / (7) D-DEFER-* grep guard PASS (CR 11-3 honest-DEFER discipline 97번째 epic 연속 정직 회복 검증 보존)

**Phase 8 close-out retro `ab495a8` (cj-style 96번째) + Phase 8 atomic wire T1~T8 `60d4ea1` (cj-style 95번째) + Phase 8 spec entry (cj-style 94번째) + Phase 8 PRD entry (cj-style 93번째) + build-fixes sprint `eaee198` (cj-style 97번째 build 진입) + Phase 7 close-out retro `326fa9f` (cj-style 92번째) + Phase 7 atomic wire T1~T8 `59b56cd` (cj-style 91번째) + Phase 7 spec entry `749381e` (cj-style 90번째) + Phase 7 PRD entry `916a541` (cj-style 89번째) + Phase 6 close-out retro `f9f006c` (cj-style 88번째) + Phase 6 atomic wire T1~T8 `24e1cd7` (cj-style 87번째) + Phase 6 spec entry `f5c14c9` (cj-style 86번째) + Phase 6 PRD entry `e84a281` (cj-style 85번째) + Epic 17 close-out retro `be8f3bd` (cj-style 84번째) + Epic 17 T2+T3 UI wire `bb92879` (cj-style 83번째) + Epic 17 atomic wire `2ada2ec` (cj-style 82번째) + Epic 17 spec entry `f4b2b58` (cj-style 81번째) + Epic 17 PRD entry `40a9c41` (cj-style 80번째) cycle 정합 보존** (cj-style 49~96번째 누적 cycle 정직 회복 + Epic 1 ~ Epic 17 + Phase 3 + Phase 4 + Phase 5 + Phase 6 + Phase 7 + Phase 8 + 1st release 모든 close-out retro 결정 verbatim preserve + EXTENSION) — Phase 9 PRD entry 진입 시점에 Epic 1 ~ Epic 17 + Phase 3 + Phase 4 + Phase 5 + Phase 6 + Phase 7 + Phase 8 + 1st release 모든 close-out retro 결정 verbatim preserve + EXTENSION.

**partial wire 시도 0건 + single sprint atomic wire T1~T8 결정** (cj-style 99번째 epic 연속 정직 회복 wire 진입 시점에 결정). 결정 wire 일자: 2026-08-24 (KST). **next**: Phase 9 bmad-create-story spec entry 진입 (cj-style 98번째 epic 연속 정직 회복 bmad-create-story 진입 시점) OR Phase 9 bmad-dev-story atomic sprint wire T1~T8 진입 (cj-style 99번째 epic 연속 정직 회복 wire 진입 시점) OR Phase 9 close-out retro 진입 (cj-style 100번째 epic 연속 정직 회복 진입 시점) 결정 wire 보류.

---

## F26. SLO Engineering / Error Budget Management territory (Phase 10 신규, A303 결정)

> **§F26 결정 wire 진입** — Phase 9 close-out retro `634427d` (cj-style 100번째 wire entry) 직후 다음 옵션 5종 중 **사용자 권장 결정 = 옵션 (a) Phase 10+ 진입 + 옵션 (a) SLO Engineering / Error Budget Management (Recommended) (SLO definition DSL + multi-window burn-rate evaluation + error budget tracker + multi-region SLO aggregation + tenant-scoped SLO override + SLO governance review + auto-rollback SLO breach trigger + dry-run mode + tests + wire scope T1~T8) = Phase 10 territory 결정 wire 진입** (AskUserQuestion 결정 verbatim bind). rationale 5종: ① Phase 8 wire `60d4ea1` 의 SLO/SLI 정의 4 metrics (`business_cost_engine_duration_seconds p99 < 5s` + `business_signups_total success_rate > 99%` + `business_logins_total p99 < 1s` + `business_audit_log_purge_total success_rate > 99.9%`) 가 SLI 측정 기반 + Phase 9 wire `e7670e1` 의 chaos_experiment 의 steady_state_metric baseline + auto-rollback mechanism 가 SLO breach 자동 검증 메커니즘 제공 — Phase 10 은 SLO 정의 DSL + multi-window burn-rate evaluation + error budget tracker + SLO governance review territory 의 natural next 진입 = SLO 정의(SLI 측정) → SLO 검증(chaos_experiment) → SLO 거버넌스(error budget + burn rate + auto-rollback trigger) 의 정직 회복 chain ② cj-style discipline 회피 위험 방지 (49~100번째 누적 52-entry-point cycle + 100번째 Phase 9 close-out retro + 99번째 atomic wire + 98번째 spec entry + 97번째 PRD entry 패턴 모두 wire DONE 진입 정합 보존 후 즉시 Phase 10 진입 = 1-day atomic sprint discipline) ③ 비즈니스 우선순위 = enterprise SLA 99.95% contractual commitment + Google SRE Workbook "error budget policy" 표준 패턴 + burn-rate alerting territory 의 natural next (Phase 7 OpenTelemetry tracing + Phase 8 SLO/SLI 정의 + Phase 9 chaos_experiment baseline 의 측정 가능 territory 의 natural next = governance layer EXTENSION) ④ Phase 7 wire `59b56cd` 의 7 NEW business metrics + Prometheus AlertManager 의 자연스러운 carry-over chain = multi-window burn-rate evaluator 가 Phase 7 metrics 를 소비 + error budget tracker 가 budget_remaining_pct Gauge 추가 + Phase 9 wire `e7670e1` chaos_experiment 의 owner-only RBAC + 2FA 챌린지 + auto-rollback mechanism 의 자연스러운 carry-over chain 정합 ⑤ 1st release close-out retro + Epic 17 close-out retro + Phase 6 close-out retro + Phase 7 close-out retro + Phase 8 close-out retro + Phase 9 close-out retro territory verbatim 보존 + AD-22 owner-only RBAC + Epic 12 2FA 챌린지 보존 + AD-14 stack pin + NFR4 PII minimization ✅ PRESERVED + AD-37 SLO Engineering / Error Budget Management 신규 결정 ((a)~(g) 7 sub-decisions 모두 결정 wire 진입). master PRD v4.0 → v4.1 atomic edit (docs only). §F26 territory = SLO Engineering / Error Budget Management 결정 wire 진입 시점에 7 ACs (§F26.1 SLO definition DSL + §F26.2 multi-window burn-rate evaluation + §F26.3 error budget tracker + §F26.4 multi-region SLO aggregation + §F26.5 tenant-scoped SLO override + §F26.6 SLO governance review + auto-rollback SLO breach trigger + §F26.7 Capability matrix v1.35 EXTENSION + dry-run + tests + wire scope T1~T8) + §8.1 M0-(s) SLO engineering AC 신규 + §15 로드맵 Phase 10 row status 백로그 → in-progress (PRD entry DONE 진입 wire) + §부록 A A303+A304+A305+A306+A307 신규 결정 표 + AD-37 SLO Engineering / Error Budget Management 신규 결정 + capability matrix v1.34 → v1.35 EXTENSION SLO_ENGINEERING 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅ (CR 12-1 L4 precedent 미러, CHAOS_ENGINEERING Phase 9 wire + PERFORMANCE_TESTING Phase 8 wire + OBSERVABILITY_TRACES + OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind). CR 11-3 honest-DEFER discipline 101번째 epic 연속 정직 회복 검증 ✅ (D-1-1-DEFER-1/2/3 RESOLVED 보존 60~101번째 + D-LAUNCH-1-DEFER-1 honestly preserved 65~101번째 + D-EPIC-16-REVIEW-DEFER-2~6 ✅ RESOLVED 보존 78~101번째 + D-PHASE-4-DR-DEFER-1/2 ✅ RESOLVED 보존 73~101번째 + D-EPIC-17-WIRE-DEFER-T2-T3-UI ✅ RESOLVED 보존 83~101번째 + D-RETENTION-1 ✅ RESOLVED 보존 85~101번째 + D-OBSERVABILITY-1 ✅ RESOLVED 보존 89~101번째 + D-PERFORMANCE-1 ✅ RESOLVED 보존 93~101번째 + D-CHAOS-1 ✅ RESOLVED 보존 97~101번째 + **D-SLO-1 신규 honestly DEFER 보존 = Phase 10 PRD entry 진입 시점에 carry-over chain 정직 회복 결정 wire 진입: Phase 8 close-out retro + Phase 9 close-out retro territory "SLO Engineering / Error Budget Management 결정 wire 보류, Phase 10+ 진입 시점" verbatim 해소**).

## F26.1 SLO definition DSL (CR 11-4 P-015 verbatim + SLO TypedDict 13 fields, A303 결정)

- **SLO TypedDict 13 fields** 결정 wire = `apps/api/modules/slo/slo_dsl.py` NEW ~+180 LOC + `SloDefinition` TypedDict 13 fields (slo_id TEXT PRIMARY KEY + tenant_id UUID NOT NULL + service TEXT enum cost_engine/signups/logins/audit_purge NOT NULL + slo_type TEXT enum availability/latency/throughput NOT NULL + target REAL NOT NULL CHECK (target > 0 AND target <= 1) + window_seconds INTEGER NOT NULL DEFAULT 2592000 (30d) + burn_rate_threshold_1h REAL NOT NULL DEFAULT 14.4 + burn_rate_threshold_6h REAL NOT NULL DEFAULT 6.0 + alerting_policy TEXT NOT NULL DEFAULT 'pager' + owner_user_id UUID NOT NULL + created_at TIMESTAMPTZ NOT NULL DEFAULT NOW() + updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW() + trace_id TEXT NOT NULL)
- **parse_slo_definition pure validator** 결정 wire = `parse_slo_definition(tenant_id, payload) -> SloDefinition` CR 11-4 P-015 verbatim (pure validator, side-effect 없음) + tenant-scoped + multi-tenant isolation CR 0-2 RLS lesson
- **1 NEW error class** 결정 wire = `SloDefinitionInvalidError(400)` CR 12-5 D-14 envelope `{code, message_ko, details, trace_id}` (e.g. `SLO_DEFINITION_TARGET_OUT_OF_RANGE_KO`, `SLO_DEFINITION_INVALID_SERVICE_KO`, `SLO_DEFINITION_WINDOW_INVALID_KO`)
- **target ranges 결정 wire** = availability SLO target 99.0%~99.99% (e.g. 99.9% ⇒ 43.2m/month budget, 99.95% ⇒ 21.6m/month, 99.99% ⇒ 4.32m/month) + latency SLO target p99 < 5s / p99 < 1s / p99 < 200ms (e.g. `cost_engine p99 < 5s` 99% ⇒ 4h19m/month budget) + throughput SLO target success_rate > 99%
- **Phase 8 wire `60d4ea1` carry-over chain** 결정 wire = `docs/slo-sli-definition.md` 의 4 SLAs (`business_cost_engine_duration_seconds p99 < 5s` + `business_signups_total success_rate > 99%` + `business_logins_total p99 < 1s` + `business_audit_log_purge_total success_rate > 99.9%`) 가 자동 SLO instance 생성 (slo_id = service + slo_type 자동 derive)
- **CR 11-4 D-002 ko-KR SSOT** 결정 wire = error message ko-KR 5종 (target_out_of_range + invalid_service + invalid_window + target_too_strict + window_too_short)

## F26.2 multi-window burn-rate evaluation (Google SRE Workbook multi-window multi-burn-rate criteria verbatim, A303 결정)

- **multi-window evaluator** 결정 wire = `apps/api/jobs/slo_burn_rate_evaluator.py` NEW ~+200 LOC + cron KST 매 5분 UTC 매 5분 + `evaluate_burn_rate(slo_id, window_seconds) -> BurnRateResult` TypedDict 5 fields (slo_id + window_seconds + burn_rate_factor + budget_consumed_pct + alert_required BOOL)
- **4 burn-rate windows** 결정 wire (Google SRE Workbook "multi-window, multi-burn-rate criteria" verbatim 적용):
  - **(a) Fast burn 1h window** = 1h burn rate > 14.4x ⇒ 2% budget consumed in 1h ⇒ paging 즉시 (Slack `#bizup-oncall` + PagerDuty owner-only AD-22 + Epic 12 2FA 챌린지)
  - **(b) Slow burn 6h window** = 6h burn rate > 6x ⇒ 5% budget consumed in 6h ⇒ ticket 발행 (Jira `SLO-INCIDENT` 자동 생성 + owner-only RBAC)
  - **(c) Budget exhaustion 24h window** = 24h burn rate > 3x ⇒ 10% budget consumed in 24h ⇒ ticket 발행 + governance review 트리거
  - **(d) Long burn 3d window** = 3d burn rate > 1x ⇒ 25% budget consumed in 3d ⇒ governance review 자동 트리거 (SLO freeze mechanism 활성화)
- **1 NEW error class** 결정 wire = `SloBurnRateThresholdExceededError(429)` CR 12-5 D-14 envelope `{slo_id, window_seconds, burn_rate_factor, trace_id}`
- **3 NEW Prometheus metrics** 결정 wire = `business_slo_burn_rate_factor{slo_id,window}` Gauge + `business_slo_budget_consumed_pct{slo_id,window}` Gauge + `business_slo_alerts_fired_total{slo_id,window,severity}` Counter Phase 7 wire `59b56cd` EXTENSION 결정 wire
- **Phase 7 wire `59b56cd` carry-over chain** 결정 wire = Prometheus custom metrics 의 natural backend EXTENSION + AlertManager integration 자동 + Slack `#bizup-oncall` channel 자동 publish + Phase 9 wire `e7670e1` chaos_experiment 의 auto-rollback mechanism 정합 (burn_rate_exceeded ⇒ chaos_experiment_aborted 자동 fire)
- **CR 11-4 D-003 typed client** 결정 wire = Python FastAPI backend slo_burn_rate_evaluator TypedDict ↔ TypeScript Next.js frontend slo-dashboard.tsx interface parity CR 12-5 D-PARITY-01 verbatim

## F26.3 error budget tracker (Google SRE Workbook error budget policy verbatim, A304 결정)

- **error_budget TypedDict 8 fields** 결정 wire = `apps/api/modules/slo/error_budget.py` NEW ~+150 LOC + `ErrorBudget` TypedDict 8 fields (slo_id + budget_total_seconds REAL + budget_consumed_seconds REAL + budget_remaining_pct REAL + reset_at TIMESTAMPTZ + freeze_until TIMESTAMPTZ nullable + governance_required BOOL + trace_id)
- **consume_error_budget atomic UPDATE** 결정 wire = `consume_error_budget(slo_id, incident_duration_seconds) -> ErrorBudget` + atomic UPDATE `error_budget_tracker` row (`budget_consumed_seconds = budget_consumed_seconds + incident_duration_seconds` + `budget_remaining_pct = (budget_total_seconds - budget_consumed_seconds) / budget_total_seconds * 100`) + AD-4 commit pattern verbatim (단일 트랜잭션, 도중 실패 시 롤백)
- **multi-tenant isolation CR 0-2 RLS lesson** 결정 wire = RLS policy `tenant_id = current_setting('app.tenant_id')::uuid` 자동 적용 + alembic 0042 EXTENSION
- **freeze mechanism** 결정 wire = `freeze_slo_budget(slo_id, duration_seconds)` governance 트리거 시 budget consumption 일시 정지 (governance review 진행 중 burn_rate 가 budget 을 더 소비하지 않도록) + `freeze_until` 컬럼 nullable + governance_required BOOL 자동 trigger
- **1 NEW error class** 결정 wire = `ErrorBudgetExhaustedError(429)` CR 12-5 D-14 envelope `{slo_id, budget_remaining_pct, reset_at, trace_id}` (e.g. `SLO_BUDGET_EXHAUSTED_KO`)
- **Prometheus metric** 결정 wire = `business_slo_budget_remaining_pct{slo_id,service}` Gauge + Phase 7 wire `59b56cd` 7 NEW business metrics EXTENSION
- **Phase 8 wire `60d4ea1` carry-over chain** 결정 wire = `LatencyBudget` TypedDict 의 natural backend EXTENSION + Epic 11 V8 determinism 골든 fixture 정합 보존

## F26.4 multi-region SLO aggregation (Phase 5 multi-region failover carry-over chain, A304 결정)

- **MultiRegionSloAggregate TypedDict 7 fields** 결정 wire = `apps/api/modules/slo/multi_region_aggregator.py` NEW ~+180 LOC + `MultiRegionSloAggregate` TypedDict 7 fields (slo_id + region enum seoul/tokyo/singapore NOT NULL + aggregate_target REAL + aggregate_actual REAL + weighted_budget_remaining_pct REAL + region_weight_map JSONB + trace_id)
- **aggregate_slo_across_regions** 결정 wire = `aggregate_slo_across_regions(slo_id) -> MultiRegionSloAggregate` + `region_weight_map` default `{seoul: 0.6, tokyo: 0.3, singapore: 0.1}` 결정 wire (production traffic 비중 기반)
- **cross-region replication lag weighted adjustment** 결정 wire = replication_lag_seconds > 30s ⇒ seoul weight 0.5 / tokyo 0.3 / singapore 0.2 (failover scenario 에서 primary region weight 감소) + Phase 5 wire `f093f8c` `phase_5_replication_lag` table 정합 + `FailoverStuck` alert rule (replication_lag_seconds > 30 for 5m severity=critical) 자동 trigger
- **Phase 9 wire `e7670e1` carry-over chain** 결정 wire = chaos_experiment 의 L4 single_region + L5 multi_region blast radius 정합 + auto-rollback mechanism 정합 (multi-region SLO breach ⇒ chaos_rollback_triggered 자동 fire)
- **audit-first INSERT `slo_aggregate_evaluated`** 결정 wire = CR 1-1 verbatim + ActionClass.SLO_ENGINEERING EXTENSION + apps/api/core/audit_action.py MODIFIED AuditAction Literal EXTENSION 1 NEW value + _ActionRegistry SLO_ENGINEERING entry 신규 1개 등록 + __all__ EXTENSION + emit_audit_typed BEFORE aggregation evaluation CR 1-1 verbatim 적용
- **3 NEW pytest cases** 결정 wire = aggregate_slo_across_regions default weight + replication_lag weighted adjustment + audit_first INSERT

## F26.5 tenant-scoped SLO override (CR 0-2 RLS lesson + per-tenant target override, A305 결정)

- **TenantSloOverride TypedDict 6 fields** 결정 wire = `apps/api/modules/slo/tenant_scoping.py` NEW ~+120 LOC + `TenantSloOverride` TypedDict 6 fields (override_id UUID PK + tenant_id UUID NOT NULL + slo_id TEXT FK + target_override REAL CHECK (target_override > 0 AND target_override <= 1) + window_seconds_override INTEGER + trace_id)
- **apply_tenant_slo_override** 결정 wire = `apply_tenant_slo_override(slo_id, tenant_id) -> SloDefinition` + per-tenant target override (e.g. enterprise tenant 99.95% > default 99% SLO) + per-tenant window override (e.g. tenant 14d rolling window vs default 30d) + atomic UPSERT (override 존재 시 apply, 미존재 시 default SLO 그대로 사용)
- **multi-tenant isolation CR 0-2 RLS lesson** 결정 wire = RLS policy `tenant_id = current_setting('app.tenant_id')::uuid` 자동 적용 + alembic 0042 EXTENSION + multi-tenant isolation test 결정 wire (tenant_a 의 override 가 tenant_b 에 노출되지 않음 검증)
- **UNIQUE constraint 결정 wire** = `UNIQUE (tenant_id, slo_id)` 결정 (한 tenant 는 한 slo_id 당 하나의 override 만 보유)
- **Phase 9 wire `e7670e1` carry-over chain** 결정 wire = chaos_experiment 의 L2 single_tenant blast radius 정합 보존 + audit-first INSERT `tenant_slo_override_applied` CR 1-1 verbatim 결정 wire
- **4 NEW pytest cases** 결정 wire = tenant override apply + RLS policy + UNIQUE constraint + audit_first INSERT

## F26.6 SLO governance review + auto-rollback SLO breach trigger (owner-only RBAC + 2FA 챌린지, A305 결정)

- **GovernanceReview TypedDict 7 fields** 결정 wire = `apps/api/modules/slo/governance.py` NEW ~+150 LOC + `GovernanceReview` TypedDict 7 fields (review_id UUID PK + slo_id + tenant_id + trigger_reason enum budget_exhausted_75pct/burn_rate_sustained/freeze_expired/audit_violation + initiated_by UUID owner_user_id + initiated_at TIMESTAMPTZ + status enum pending/in_progress/resolved/cancelled + trace_id)
- **4 governance review trigger conditions** 결정 wire = (a) budget_consumed_pct > 75% for 7d OR (b) burn_rate_3d > 1x sustained OR (c) freeze_until expired + budget_negative OR (d) error_budget exhausted < 24h to reset + audit-first INSERT `slo_governance_review_initiated` CR 1-1 verbatim + owner-only RBAC AD-22 + Epic 12 2FA 챌린지 보존
- **auto-rollback SLO breach trigger** 결정 wire = `business_cost_engine_p99_breach` OR `business_signups_success_rate_breach` 1m sustained ⇒ Phase 9 wire `e7670e1` `chaos_experiment_aborted` + `chaos_rollback_triggered` 자동 fire (SLO violation 시 chaos experiment 자동 abort + rollback) + Phase 9 wire `e7670e1` 의 chaos_experiment 의 auto-rollback mechanism 정합
- **audit-first INSERT 3 NEW** 결정 wire (CR 1-1 verbatim) = `slo_target_updated` (slo_id + previous_target + new_target + actor_id + trace_id) + `slo_budget_exhausted` (slo_id + budget_remaining_pct + reset_at + trace_id) + `slo_violation_detected` (slo_id + window_seconds + burn_rate_factor + severity + trace_id) + ActionClass.SLO_ENGINEERING 신규 정의 + apps/api/core/audit_action.py MODIFIED AuditAction Literal EXTENSION 3 NEW values + _ActionRegistry SLO_ENGINEERING entry 신규 3개 등록 + __all__ EXTENSION + emit_audit_typed BEFORE governance review CR 1-1 verbatim 적용 + Phase 9 wire `e7670e1` 의 4 NEW AuditAction Literal EXTENSION pattern verbatim 적용
- **1 NEW error class** 결정 wire = `SloGovernanceReviewRequiredError(403)` CR 12-5 D-14 envelope `{slo_id, trigger_reason, trace_id}`
- **AD-22 owner-only RBAC + Epic 12 2FA 챌린지 보존** 결정 wire = governance review initiate 시 owner-only + Epic 12 2FA 챌린지 자동 trigger (Epic 12 close-out retro `a63646c` 2FA 챌린지 정합 보존)
- **4 NEW pytest cases** 결정 wire = governance trigger conditions + owner-only RBAC + auto-rollback + audit_first INSERT 3 NEW

## F26.7 Capability matrix v1.35 EXTENSION + dry-run + Tests + wire scope T1~T8 (cj-style Phase 10 3번째 진입점 = cj-style 103번째 wire 진입, A307 결정)

- **T1 slo_dsl + slo_burn_rate_evaluator modules** 결정 wire = `apps/api/modules/slo/slo_dsl.py` NEW (~+180 LOC + SloDefinition TypedDict 13 fields + parse_slo_definition pure validator CR 11-4 P-015 verbatim + 1 NEW error class SloDefinitionInvalidError(400)) + `apps/api/jobs/slo_burn_rate_evaluator.py` NEW (~+200 LOC + multi-window 4 burn-rate windows evaluator + 1 NEW error class SloBurnRateThresholdExceededError(429) + 3 NEW Prometheus metrics) + 12 NEW pytest cases (slo_dsl 6 + slo_burn_rate_evaluator 6 결정 wire)
- **T2 error_budget module** 결정 wire = `apps/api/modules/slo/error_budget.py` NEW (~+150 LOC + ErrorBudget TypedDict 8 fields + consume_error_budget atomic UPDATE + freeze mechanism + 1 NEW error class ErrorBudgetExhaustedError(429) + Prometheus metric) + 5 NEW pytest cases (consume atomic + multi-tenant RLS + freeze mechanism + AD-4 commit + ko-KR SSOT 결정 wire)
- **T3 multi_region_aggregator + tenant_scoping modules** 결정 wire = `apps/api/modules/slo/multi_region_aggregator.py` NEW (~+180 LOC + MultiRegionSloAggregate TypedDict 7 fields + replication_lag weighted adjustment + audit-first INSERT `slo_aggregate_evaluated`) + `apps/api/modules/slo/tenant_scoping.py` NEW (~+120 LOC + TenantSloOverride TypedDict 6 fields + apply_tenant_slo_override + UNIQUE constraint + RLS policy) + 8 NEW pytest cases (multi_region_aggregator 4 + tenant_scoping 4 결정 wire)
- **T4 governance + auto-rollback SLO breach trigger** 결정 wire = `apps/api/modules/slo/governance.py` NEW (~+150 LOC + GovernanceReview TypedDict 7 fields + 4 trigger conditions + auto-rollback SLO breach trigger Phase 9 wire `e7670e1` 정합 + audit-first INSERT 3 NEW + 1 NEW error class SloGovernanceReviewRequiredError(403) + owner-only RBAC AD-22 + Epic 12 2FA 챌린지 보존) + 4 NEW pytest cases (governance trigger conditions + owner-only RBAC + auto-rollback + audit_first INSERT 3 NEW 결정 wire)
- **T5 alembic 0042 phase_10_slo_engineering** 결정 wire = `apps/api/alembic/versions/0042_phase_10_slo_engineering.py` NEW (~+200 LOC + `phase_10_slo_definitions` table + 13 columns + 3 indexes + 2 CHECK constraints + RLS policy `tenant_id = current_setting('app.tenant_id')::uuid` CR 0-2 verbatim + `phase_10_error_budget_tracker` table + 9 columns + 2 indexes + RLS policy + `phase_10_tenant_slo_overrides` table + 7 columns + UNIQUE constraint + RLS policy + `phase_10_slo_governance_reviews` table + 10 columns + 2 indexes + RLS policy + down_revision "0041_phase_9_chaos_engineering") + 3 NEW pytest cases (alembic migration + RLS policy + CHECK constraints + UNIQUE constraint 결정 wire)
- **T6 audit action EXTENSION 3 NEW** 결정 wire = `apps/api/core/audit_action.py` MODIFIED + AuditAction Literal 3 NEW EXTENSION (`slo_target_updated` + `slo_budget_exhausted` + `slo_violation_detected`) + _ActionRegistry SLO_ENGINEERING entry 신규 3개 등록 + __all__ EXTENSION + ActionClass.SLO_ENGINEERING 신규 정의 + 6 NEW pytest cases (AuditAction Literal 값 검증 + ActionClass.SLO_ENGINEERING enum value + resource_table "slo_definitions" + emit_audit_typed BEFORE slo evaluation CR 1-1 verbatim 적용 + multi-tenant isolation + trace_id propagation 결정 wire)
- **T7 capability v1.35 + frontend slo dashboard** 결정 wire = capability matrix v1.34 → v1.35 EXTENSION SLO_ENGINEERING 1 NEW row + 4-industry grants industry-agnostic ✅/✅/✅/✅ + drift detector `tests/integration/test_capability_matrix_v1_35_drift.py` NEW 6 NEW pytest cases 결정 wire + apps/api/core/capability.py MODIFIED Capability.SLO_ENGINEERING = "slo_engineering" 1 NEW enum + apps/api/dependencies/capability.py MODIFIED require_slo_engineering 1 NEW dep + __all__ EXTENSION + `apps/web/app/[locale]/(dashboard)/admin/slo/page.tsx` NEW (~+150 LOC + 4 components: SloDefinitionList + SloBurnRateDashboard + ErrorBudgetTracker + SloGovernanceReviewList) + `apps/web/lib/slo/slo-client.ts` NEW (~+150 LOC + SloDefinition + ErrorBudget + SloViolation TypedDict CR 12-5 D-PARITY-01 verbatim + SloDefinitionApiError typed envelope CR 11-4 P-015) + ko-KR.json `slo.*` namespace EXTENSION ~25 keys (slo_dashboard_title + slo_section_label + slo_definition_label + slo_target_label + slo_window_label + slo_burn_rate_label + slo_budget_remaining_label + slo_violation_label + slo_governance_label + slo_auto_rollback_label + slo_tenant_override_label + slo_multi_region_label + slo_dry_run_label + slo_window_1h + slo_window_6h + slo_window_24h + slo_window_3d + slo_severity_critical + slo_severity_warning + slo_severity_info + loading_slo + error_slo_failed + error_owner_only + empty_state 결정) + CR 11-4 D-002 verbatim SSOT 결정 wire + 5 NEW vitest cases (SloDefinitionList + SloBurnRateDashboard owner-only ack prompt AD-22 verbatim + ErrorBudgetTracker ko-KR SSOT + SloGovernanceReviewList RTL render + slo dashboard parity CR 12-5 D-PARITY-01 결정 wire)
- **T8 atomic commit** 결정 wire = `git commit -F <file>` (CR 9-6 D5 prevention) + commit-msg file 신규 + handoff memory 신규 + MEMORY.md hook index 신규 EXTENSION + sprint-status.yaml MODIFIED (`phase-10-prd-entry: backlog → done` + A303~A307 action_items block 5 entries + `last_updated_note` v3.13 Phase 10 PRD entry prepend 결정 wire)
- **estimated** 결정 wire = ~+46 NEW pytest PASS (slo_dsl 6 + slo_burn_rate_evaluator 6 + error_budget 5 + multi_region_aggregator 4 + tenant_scoping 4 + governance 4 + alembic 0042 3 + audit action 6 + capability matrix v1.35 6 + slo integration e2e 2 = ~+46 NEW pytest PASS) + ~+5 NEW vitest PASS (slo dashboard 3 + SSOT drift 2 = ~+5 NEW vitest PASS) + 0 NEW ruff + 0 regressions 결정 wire 보존
- **3중 게이트 retro verification FINAL CLEAN** 결정 wire = (1) pytest focused: ~46/46 NEW PASS / (2) vitest focused: ~5/5 NEW PASS + i18n SSOT drift detector PASS / (3) ruff scoped: all checks passed / (4) tsc scoped: 0 NEW errors (apps/web frontend 변경 ext only — pre-existing baseline errors preserved per cj-style discipline) / (5) SDR drift gate PASS (vitest file count +1 NEW collected well within 5% tolerance + pytest +8 NEW files collected well within 5% tolerance) / (6) commit_consistency gate PASS (CR 9-6 commit message discipline + A36 SDR 검증 4-step 자동 적용) / (7) D-DEFER-* grep guard PASS (CR 11-3 honest-DEFER discipline 101번째 epic 연속 정직 회복 검증 보존)

## F27. FinOps Showback / Chargeback (Phase 11 territory 신규)

> **territory 정의** 결정 wire = Epic 7~10 wire (`756a32a` ~ `ac5d6c5`) 의 ABC engine / TDABC / BEP slider / next-month projection / budget scenario / AI 인사이트 의 cost 분석 territory + Phase 10 wire `ac5d6c5` 의 cost-engine p99 < 5s SLO + error_budget tracker 의 30d rolling + monthly reset KST 1일 00:00 territory 의 natural backend carry-over chain = **Phase 11 = FinOps Showback / Chargeback territory** 결정 wire 진입. department cost center 별 비용 배부 (showback) + chargeback billing + ERP integration territory 의 FINANCIAL REPORTING LAYER EXTENSION 결정 wire (showback 가시성 + chargeback 비용 책임). 1st release close-out retro §6 + Epic 17 close-out retro §11 + Phase 6 close-out retro §13 + Phase 7 close-out retro §10 + Phase 8 close-out retro §10 + Phase 9 close-out retro §10 + Phase 10 close-out retro `733d428` §10 "FinOps Showback / Chargeback 결정 wire 보류, Phase 11+ 진입 시점" verbatim 해소 결정 wire 진입 (CR 11-3 honest-DEFER discipline 105번째 epic 연속 정직 회복 검증 ✅). 7 ACs (Acceptance Criteria) = §F27.1 showback DSL + §F27.2 chargeback cost allocation engine + §F27.3 department cost center mapping + §F27.4 showback dashboard UI + §F27.5 chargeback CSV/PDF export + §F27.6 Capability matrix v1.36 EXTENSION FINOPS_SHOWBACK + FINOPS_CHARGEBACK 2 NEW rows + §F27.7 dry-run + Tests + wire scope T1~T8. 정합 = §8.1 M0-(t) EXTENSION 결정 wire + AD-38 신규 결정 wire 진입 (cj-style Phase 11 1번째 진입점 = cj-style 105번째 epic 연속 정직 회복 atomic docs-only wire). 결정 wire 일자: 2026-08-24.

### F27.1 showback DSL + period selector + comparison view (12 sub-ACs)

- **F27.1.1** `apps/api/modules/finops/showback_dsl.py` NEW 결정 wire = ~+150 LOC + `showback(tenant_id, period_key, department_id, group_by)` builder 함수 + 5 group_by 옵션 결정 (department + cost_center + product_line + service + custom_tag) + 6 period selector 모드 결정 (current month + previous month + last 3 months + last 6 months + YTD + custom range) + 4 industries baseline + per-tenant override EXTENSION 결정 wire + policy evaluation precedence tenant override > industry baseline > system default 결정 wire + `SHOWBACK_PERIOD_DEFAULTS = {'current_month': True, 'previous_month': True, 'last_3_months': False, 'last_6_months': False, 'ytd': False}` constants 결정 wire (F27.1 verbatim) + Phase 8 wire `60d4ea1` cost-engine benchmark V8 골든 fixture 정합 보존 + Phase 10 wire `ac5d6c5` monthly reset KST 1일 00:00 + 30d rolling calculation 정합 보존.
- **F27.1.2** `apps/api/modules/finops/showback_query.py` NEW 결정 wire = ~+120 LOC + `query_showback_breakdown(tenant_id, period_key, group_by) -> list[DepartmentBreakdown]` 결정 wire (department × cost_center × period 3-dim aggregation) + `query_showback_comparison(tenant_id, period_key, comparison_period_key) -> ComparisonView` 결정 wire (current vs previous period + variance calculation + delta_pct + delta_amount) + `DepartmentBreakdown` TypedDict 8 fields (department_id + cost_center_id + total_amount + period_key + group_by + tenant_id + trace_id + computed_at) + `ComparisonView` TypedDict 7 fields (current_period + previous_period + delta_amount + delta_pct + period_key + tenant_id + trace_id) 결정 wire (CR 12-5 D-PARITY-01 verbatim).
- **F27.1.3** audit-first INSERT `showback_generated` 결정 wire (CR 1-1 verbatim, action_class='FINOPS' + action='showback_generated' + actor_id + tenant_id + period_key + group_by + payload_json 정합 + per-tenant RLS 자동 적용, multi-tenant isolation test 결정 wire + Phase 6 wire `24e1cd7` 의 5 NEW AuditAction Literal EXTENSION pattern verbatim).
- **F27.1.4** comparison view 의 delta_pct + delta_amount 의 calculation 결정 wire (delta_pct = (current_total - previous_total) / previous_total × 100 + delta_amount = current_total - previous_total + sign convention 결정 (positive = increase, negative = decrease) + 부호 표기 결정 wire + ko-KR.json `finops.*` namespace EXTENSION 결정 wire (CR 11-4 D-002 verbatim SSOT 결정: finops_delta_pct_label + finops_delta_amount_label + finops_variance_label)).
- **F27.1.5** period selector 6 모드의 calendar arithmetic 결정 wire (current month = KST 이번달 1일 ~ 말일, previous month = KST 지난달 1일 ~ 말일, last_3_months = KST 최근 3개월 (현재월 포함), last_6_months = KST 최근 6개월 (현재월 포함), YTD = KST 1월 1일 ~ 현재, custom range = user_input start_date ~ end_date 검증) + leap year / month boundary 안전 결정 wire.
- **F27.1.6** group_by 5 옵션의 column mapping 결정 wire (department → departments.name, cost_center → cost_centers.code, product_line → product_lines.name, service → services.name, custom_tag → tenant_settings.custom_tags JSONB) + JOIN 1개 (master table + dimension table) 결정 wire + RLS 자동 적용 CR 0-2 verbatim 결정 wire.
- **F27.1.7** showback DSL 의 pure validator CR 11-4 P-015 verbatim 결정 wire (parse_showback_definition 함수 + validation errors 명시: invalid period_key + invalid group_by + invalid department_id + negative amount 등 6 validation rules 결정 wire + 1 NEW error class ShowbackDefinitionInvalidError(400) CR 12-5 D-14 envelope 결정 wire).
- **F27.1.8** 4 industries baseline industry-agnostic 결정 wire (manufacturing + service + manufacturing_service + manufacturing_service_other 모두 showback 가능 + per-tenant override EXTENSION 결정 wire + Epic 1~17 + Phase 3~10 territory 와 동일 industry-agnostic 4-industry grants ✅/✅/✅/✅ 미러 결정 wire).
- **F27.1.9** query_showback_breakdown 의 pagination 결정 wire (page_size default 20 + max 100 + offset 기반 pagination + tenant_id RLS 자동 적용 + `query_showback_breakdown_paged(tenant_id, period_key, group_by, page, page_size) -> Page[DepartmentBreakdown]` 결정 wire + total_count 포함 결정 wire).
- **F27.1.10** showback 의 cache layer 결정 wire (Redis cache 5분 TTL + cache key = `(tenant_id, period_key, group_by, query_hash)` + cache invalidation 시점: AD-25 의 LISTEN/NOTIFY `cost_engine_cache` channel EXTENSION + Phase 7 wire `59b56cd` 의 7 NEW business metrics 의 자연스러운 carry-over chain 결정 wire + NFR4 PII minimization 정합 보존).
- **F27.1.11** showback 의 currency 결정 wire (KRW 기본 + tenant 의 currency_code per-tenant EXTENSION 결정 wire + tenant_settings.currency_code column + ISO 4217 currency code 결정 (KRW + USD + EUR + JPY + CNY 등) + currency formatting 결정 (1,234,567 KRW / 1,234,567.89 USD 등) + Phase 8 wire `60d4ea1` 의 latency budget display currency format 정합 결정 wire).
- **F27.1.12** showback 의 export format 결정 wire (`export_showback_csv(tenant_id, period_key, group_by, actor_id) -> StreamingResponse` 결정 wire (Excel-compatible UTF-8 BOM `﻿` + comma-separated + double-quote escape for amount_json + Phase 7 wire `59b56cd` 의 metric export format 정합 + CR 12-5 D-14 envelope 결정 wire + audit-first INSERT `showback_exported` CR 1-1 verbatim 결정 wire + 1 NEW error class ShowbackExportError(500) 결정 wire).

### F27.2 chargeback cost allocation engine (12 sub-ACs)

- **F27.2.1** `apps/api/modules/finops/chargeback_engine.py` NEW 결정 wire = ~+180 LOC + `ChargebackRule` TypedDict 6 fields (rule_id + rule_type enum flat_fee/proportional_allocation/metered + flat_fee_amount nullable + allocation_basis enum + markup_pct default 0.0 + tax_pct default 0.10 + tenant_id + period_key + trace_id) 결정 wire + `compute_chargeback(tenant_id, period_key, rule_type) -> ChargebackResult` 함수 결정 wire (CR 12-5 D-PARITY-01 verbatim + CR 12-5 D-14 envelope).
- **F27.2.2** chargeback rule 3종 결정 wire = **정액제 (flat_fee)** 결정 wire = `flat_fee_amount` × period_count + 모든 department 동일 금액 배부 결정 + **비율배분 (proportional_allocation)** 결정 wire = `total_cost × department_usage_pct / sum(department_usage_pct)` 계산 + usage 기준 (API call count + cost-engine calculation count + storage GB 등) 결정 wire + **usage-based (metered)** 결정 wire = `unit_price × usage_count + tiered pricing` 결정 wire + tier (Tier 1: 0~1000 calls @ 100 KRW/call, Tier 2: 1001~10000 calls @ 80 KRW/call, Tier 3: 10001+ calls @ 60 KRW/call 등) 결정 wire.
- **F27.2.3** `apps/api/modules/finops/chargeback_rule_evaluator.py` NEW 결정 wire = ~+100 LOC + `evaluate_chargeback_rule(tenant_id, period_key, rule_type) -> ChargebackResult` 결정 wire (3종 rule_type 분기 + 1 NEW error class ChargebackRuleInvalidError(400) + 1 NEW error class ChargebackCalculationError(500) CR 12-5 D-14 envelope + 4 validation rules: rule_type enum 검증 + flat_fee_amount > 0 + markup_pct range 0~50 + tax_pct range 0~100 결정 wire).
- **F27.2.4** markup + tax 결정 wire = markup_pct (default 0%, max 50% range 0~50% + 0.01 step granularity) + tax_pct (default 10% VAT + per-tenant override EXTENSION + tenant_settings.tax_pct column + 0~100% range + 0.01 step granularity) + chargeback_period_monthly (period start KST 1일 00:00 ~ period end KST 말일 23:59:59) + chargeback_export_format enum csv/pdf 결정 wire.
- **F27.2.5** cost_allocation_method enum direct/indirect/shared 결정 wire = direct (department 직접 귀속 + usage 추적 가능) / indirect (공통 비용 department 비례 배부) / shared (multiple department 공유 + usage 비례 분배) 결정 wire + 3 method 의 default weight 결정 (direct: 1.0, indirect: 0.5, shared: 0.0 baseline) 결정 wire + Phase 7 wire `59b56cd` 의 7 NEW business metrics 의 자연스러운 carry-over chain 결정 wire.
- **F27.2.6** ChargebackResult TypedDict 결정 wire = 10 fields (chargeback_id + tenant_id + period_key + department_id + cost_center_id + rule_type + base_amount + markup_amount + tax_amount + total_amount + trace_id) 결정 wire (CR 12-5 D-PARITY-01 verbatim) + Decimal precision 결정 (소수점 2자리, banker's rounding 적용 CR 5-1 verbatim) + per-tenant currency_code 적용 결정 wire.
- **F27.2.7** chargeback 의 monthly reset KST 1일 00:00 결정 wire = cron KST 매월 1일 00:00 UTC 15:00 (previous month 기준) 결정 + chargeback_period_monthly cron 자동 트리거 결정 + Phase 10 wire `ac5d6c5` 의 monthly reset KST 1일 00:00 + 30d rolling calculation 정합 보존 + audit-first INSERT `chargeback_calculated` CR 1-1 verbatim 결정 wire.
- **F27.2.8** chargeback 의 per-tenant override EXTENSION 결정 wire = `tenant_settings.chargeback_overrides` JSONB TypedDict 결정 (rule_type override + flat_fee_amount override + markup_pct override + tax_pct override + cost_allocation_method override) + policy evaluation precedence tenant override > industry baseline > system default 결정 wire + Phase 10 wire `ac5d6c5` 의 tenant_settings.slo_overrides JSONB pattern verbatim 미러 결정 wire.
- **F27.2.9** chargeback 의 multi-region aggregation 결정 wire (Phase 5 wire `f093f8c` `phase_5_replication_lag` table 정합 + primary Seoul + secondary Tokyo replica 결정 wire EXTENSION + cross-region cost variance detection EXTENSION + region_weight_map default `{seoul: 0.6, tokyo: 0.3, singapore: 0.1}` 결정 wire + audit-first INSERT `chargeback_calculated_multi_region` CR 1-1 verbatim 결정 wire).
- **F27.2.10** chargeback 의 dry-run mode 결정 wire (`--finops-chargeback-dry-run` CLI flag + compute_chargeback dry-run parameter + dry-run 시 actual chargeback_calculated INSERT skip + dry_run=True default for prod deploys) + dry-run 결과 preview 결정 wire (chargeback_preview 테이블 alembic 0043 신규 + tenant_id + period_key + rule_type + preview_amount + computed_at + trace_id) + audit-first INSERT `chargeback_dry_run_executed` CR 1-1 verbatim 결정 wire.
- **F27.2.11** chargeback 의 validation error envelope 결정 wire (CR 12-5 D-14 verbatim) + ChargebackRuleInvalidError(400) message_ko 결정 ("chargeback rule validation 실패: {validation_error}") + ChargebackCalculationError(500) message_ko 결정 ("chargeback 계산 중 오류 발생: {error_detail}") + ko-KR.json `finops.*` EXTENSION 결정 (CR 11-4 D-002 verbatim SSOT: finops_chargeback_rule_flat_fee + finops_chargeback_rule_proportional + finops_chargeback_rule_metered + finops_markup_label + finops_tax_label + finops_dry_run_label 결정 wire).
- **F27.2.12** chargeback 의 audit log + tenant isolation 결정 wire (CR 1-1 verbatim + CR 0-2 RLS lesson 적용, chargeback_calculated + chargeback_dry_run_executed + chargeback_rule_updated + chargeback_override_applied 4 NEW audit log entries 결정 wire, action_class='FINOPS' + action='chargeback_*' + actor_id + tenant_id + period_key + rule_type + amount + trace_id 정합 + per-tenant RLS 자동 적용 결정 wire + multi-tenant isolation test 결정 wire + Phase 6 wire `24e1cd7` 의 audit-first INSERT pattern verbatim 미러 결정 wire).

### F27.3 department cost center mapping (10 sub-ACs)

- **F27.3.1** `apps/api/modules/finops/department_mapping.py` NEW 결정 wire = ~+120 LOC + `tenant_settings.cost_center_mapping` JSONB TypedDict 결정 wire (mapping 구조: `{'department_id': 'cost_center_id'}` + cost_center_id pattern 결정 wire (`CC-{4-digit-number}` format) + UNIQUE constraint `UNIQUE (tenant_id, department_id)` 결정 wire + RLS policy `tenant_id = current_setting('app.tenant_id')::uuid` CR 0-2 verbatim 결정 wire).
- **F27.3.2** department_id ↔ cost_center_id 1:1 mapping 결정 wire = 한 department 는 하나의 cost_center 에만 매핑 + 한 cost_center 는 여러 department 매핑 가능 (1:N) 결정 wire + `validate_department_mapping` pure function CR 11-4 P-015 verbatim 결정 wire + validation errors 명시: duplicate department_id + invalid cost_center_id pattern + tenant_id mismatch 등 4 validation rules 결정 wire.
- **F27.3.3** auto-create on first calculation 결정 wire = first showback 계산 시 department 가 존재하지 않으면 auto-create + default cost_center_id 자동 생성 (CC-{4-digit-random}) + audit-first INSERT `department_mapping_updated` CR 1-1 verbatim 결정 wire (action_class='FINOPS' + action='department_mapping_updated' + actor_id='system' + tenant_id + department_id + cost_center_id + auto_created=true 결정 wire).
- **F27.3.4** audit-first INSERT `department_mapping_updated` 결정 wire (CR 1-1 verbatim, action_class='FINOPS' + action='department_mapping_updated' + actor_id + tenant_id + department_id + old_cost_center_id nullable + new_cost_center_id + auto_created boolean + trace_id 정합 + per-tenant RLS 자동 적용, multi-tenant isolation test 결정 wire + Phase 6 wire `24e1cd7` 의 5 NEW AuditAction Literal EXTENSION pattern verbatim 미러 결정 wire).
- **F27.3.5** `apps/api/alembic/versions/0043_phase_11_finops.py` NEW 결정 wire = ~+200 LOC + `phase_11_finops_department_mapping` table 9 columns 결정 (id UUID PK + tenant_id UUID + department_id TEXT + cost_center_id TEXT NOT NULL + auto_created BOOLEAN DEFAULT FALSE + created_at TIMESTAMPTZ DEFAULT NOW() + updated_at TIMESTAMPTZ DEFAULT NOW() + created_by UUID + updated_by UUID + UNIQUE constraint `(tenant_id, department_id)`) + RLS policy `tenant_id = current_setting('app.tenant_id')::uuid` CR 0-2 verbatim 결정 wire + down_revision "0042_phase_10_slo_engineering" 결정 wire.
- **F27.3.6** `phase_11_finops_showback` table 14 columns 결정 wire = (showback_id UUID PK + tenant_id UUID + period_key TEXT NOT NULL + department_id TEXT + cost_center_id TEXT + group_by TEXT enum department/cost_center/product_line/service/custom_tag + total_amount NUMERIC(20, 2) + currency_code TEXT default 'KRW' + tenant_industry TEXT + industry_baseline_amount NUMERIC(20, 2) nullable + override_applied BOOLEAN DEFAULT FALSE + computed_at TIMESTAMPTZ DEFAULT NOW() + trace_id TEXT + tenant_id_period_key_department_id UNIQUE constraint) 결정 wire.
- **F27.3.7** `phase_11_finops_chargeback` table 12 columns 결정 wire = (chargeback_id UUID PK + tenant_id UUID + period_key TEXT NOT NULL + department_id TEXT + rule_type TEXT enum flat_fee/proportional_allocation/metered + base_amount NUMERIC(20, 2) + markup_amount NUMERIC(20, 2) + tax_amount NUMERIC(20, 2) + total_amount NUMERIC(20, 2) + currency_code TEXT default 'KRW' + computed_at TIMESTAMPTZ DEFAULT NOW() + trace_id TEXT + UNIQUE constraint `(tenant_id, period_key, department_id, rule_type)`) 결정 wire.
- **F27.3.8** alembic 0043 의 4 indexes 결정 wire = (1) `idx_phase_11_finops_showback_tenant_id_period_key` (tenant_id + period_key DESC), (2) `idx_phase_11_finops_showback_department_id`, (3) `idx_phase_11_finops_chargeback_tenant_id_period_key` (tenant_id + period_key DESC), (4) `idx_phase_11_finops_chargeback_rule_type` 결정 wire + 2 CHECK constraints 결정 wire = (1) `currency_code` IN ('KRW', 'USD', 'EUR', 'JPY', 'CNY'), (2) `rule_type` IN ('flat_fee', 'proportional_allocation', 'metered') 결정 wire.
- **F27.3.9** alembic 0043 의 down_revision 결정 wire = "0042_phase_10_slo_engineering" 결정 wire + upgrade 결정 wire (3 tables CREATE + 4 indexes CREATE + 2 CHECK constraints CREATE + RLS policies CREATE) + downgrade 결정 wire (DROP TABLE × 3 + DROP INDEX × 4 + DROP CONSTRAINT × 2 + DROP POLICY × 3) 결정 wire.
- **F27.3.10** department_mapping 의 cache invalidation 결정 wire (Redis cache 5분 TTL + cache key = `(tenant_id, department_id)` + LISTEN/NOTIFY `cost_center_mapping_cache` channel 신규 결정 wire + AD-25 의 LISTEN/NOTIFY 4-channel publisher EXTENSION 결정 wire (cost_engine_cache + fiscal_period_cache + closing_snapshot_cache + ai_cache + cost_center_mapping_cache 5-channel EXTENSION) + Phase 5 wire `f093f8c` 의 multi-region replication 정합 보존 + NFR4 PII minimization ✅ PRESERVED 결정 wire).

### F27.4 showback dashboard UI (10 sub-ACs)

- **F27.4.1** `apps/web/app/[locale]/(dashboard)/admin/finops/page.tsx` NEW 결정 wire = ~+150 LOC + 4 components 결정 wire (`ShowbackPeriodSelector` + `ShowbackDepartmentBreakdownChart` + `ShowbackComparisonView` + `ShowbackCSVExportButton`) 결정 wire + owner-only RBAC AD-22 verbatim 결정 wire + Epic 12 2FA 챌린지 보존 결정 wire + `(dashboard)` route group 보호 (Phase 3-1 T1 wire 정합) 결정 wire.
- **F27.4.2** `ShowbackPeriodSelector` 결정 wire = 6 period selector 모드 UI (current_month + previous_month + last_3_months + last_6_months + YTD + custom range) + radio button + date range picker (custom 모드 시) + onChange handler 결정 wire (ko-KR.json `finops_period_label` EXTENSION + CR 11-4 D-002 verbatim SSOT) + RTL render discipline 결정 wire (CR 11-4 D-003 verbatim).
- **F27.4.3** `ShowbackDepartmentBreakdownChart` 결정 wire = Recharts BarChart + X-axis department names + Y-axis amount (currency formatted) + tooltip on hover + tenant currency_code 적용 결정 wire + tooltip ko-KR 결정 ("{department_name}: {amount} {currency_code} ({percentage}%)") + responsive design 결정 wire (mobile + tablet + desktop) + Phase 7 wire `59b56cd` 의 Recharts Grafana dashboard EXTENSION 정합 결정 wire.
- **F27.4.4** `ShowbackComparisonView` 결정 wire = 2-column side-by-side layout (current_period + previous_period) + delta_pct + delta_amount 시각화 (▲ green / ▼ red / — no change) 결정 wire + comparison period selector 결정 (previous_month vs last_year_same_month 등 5 옵션) + RTL render discipline 결정 wire + ko-KR.json `finops_comparison_label` + `finops_variance_label` EXTENSION 결정 wire.
- **F27.4.5** `ShowbackCSVExportButton` 결정 wire = 클릭 시 `export_showback_csv` API 호출 + StreamingResponse download trigger 결정 wire + filename 결정 (`showback-{tenant_slug}-{period_key}.csv`) + UX 결정 (loading state + success toast + error toast) + audit-first INSERT `showback_exported` CR 1-1 verbatim 결정 wire + ko-KR.json `finops_export_label` + `finops_csv_export_label` EXTENSION 결정 wire.
- **F27.4.6** owner-only RBAC AD-22 verbatim 결정 wire = `(dashboard)/admin/finops/` route group 의 middleware 결정 wire = owner role required (`require_role("owner")` FastAPI Dependency) + Epic 12 2FA 챌린지 보존 결정 wire (2FA 미설정 시 `/account/security?reason=2fa_required` redirect 결정) + Phase 3-1 T1 wire 의 `(dashboard)` route group 보호 정합 결정 wire.
- **F27.4.7** ko-KR.json `finops.*` namespace EXTENSION 결정 wire = ~25 keys (CR 11-4 D-002 verbatim SSOT 결정 wire: finops_dashboard_title + finops_section_label + finops_period_label + finops_department_label + finops_cost_center_label + finops_showback_label + finops_chargeback_label + finops_group_by_label + finops_comparison_label + finops_variance_label + finops_delta_pct_label + finops_delta_amount_label + finops_markup_label + finops_tax_label + finops_export_label + finops_csv_export_label + finops_pdf_export_label + finops_dry_run_label + finops_owner_only_label + finops_chargeback_rule_flat_fee + finops_chargeback_rule_proportional + finops_chargeback_rule_metered + loading_finops + error_finops_failed + empty_state) 결정 wire + i18n SSOT drift detector 적용 결정 wire (ko-KR.json 의 모든 keys 가 적어도 1개 component 에서 사용됨 검증 결정 wire).
- **F27.4.8** `apps/web/lib/finops/finops-client.ts` NEW 결정 wire = ~+150 LOC + `ShowbackRequest` + `DepartmentBreakdown` + `ComparisonView` + `ChargebackRule` + `ChargebackExport` TypedDict CR 12-5 D-PARITY-01 verbatim 결정 wire + `fetchShowbackBreakdown` + `fetchShowbackComparison` + `exportShowbackCSV` + `exportChargebackPDF` API wrapper functions 결정 wire + `ChargebackApiError` typed envelope `{code, message_ko, details, trace_id}` CR 12-5 D-14 envelope 결정 wire + audit-first INSERT BEFORE fetch CR 1-1 verbatim 적용 결정 wire.
- **F27.4.9** finops dashboard 의 period selector + comparison view 정합 결정 wire = period 변경 시 automatic refetch + cache invalidation (TanStack Query) + loading state 결정 wire + error boundary 적용 결정 wire (Phase 7 wire `59b56cd` 의 grafana dashboard 의 React Query pattern verbatim 미러 결정 wire) + NFR18 ko-KR 정합 결정 wire.
- **F27.4.10** finops dashboard 의 accessibility 결정 wire = WCAG 2.1 AA compliance (Epic 12 의 2FA 챌린지 UI 와 동일 표준) + ARIA labels 결정 wire (ko-KR inline 결정 wire + i18n SSOT 결정 wire) + keyboard navigation 결정 wire (Tab + Enter + Arrow keys) + screen reader 지원 결정 wire + Phase 12 Epic 1 UX v1.0 locked decision 결정 wire (Dark MVP / WCAG AA / Professional / ko-KR) verbatim 보존 결정 wire).

### F27.5 chargeback CSV/PDF export (10 sub-ACs)

- **F27.5.1** `apps/api/modules/finops/chargeback_export.py` NEW 결정 wire = ~+150 LOC + `export_chargeback_csv(tenant_id, period_key, actor_id) -> StreamingResponse` 결정 wire (Excel-compatible UTF-8 BOM `﻿` + comma-separated + double-quote escape for amount_json + filename 결정 wire `chargeback-{tenant_slug}-{period_key}.csv`) + streaming response 결정 wire (Phase 7 wire `59b56cd` 의 `audit_log_export.py` StreamingResponse pattern verbatim 미러) + 1 NEW error class ChargebackExportError(500) CR 12-5 D-14 envelope 결정 wire.
- **F27.5.2** CSV columns 결정 wire = (1) chargeback_id, (2) tenant_slug, (3) period_key, (4) department_id, (5) cost_center_id, (6) rule_type, (7) base_amount, (8) markup_amount, (9) tax_amount, (10) total_amount, (11) currency_code, (12) computed_at ISO 8601, (13) trace_id 결정 wire (Epic 17 wire `2ada2ec` 의 audit_log_export.py columns pattern verbatim 미러) + header row 결정 wire + UTF-8 BOM Excel 호환 결정 wire.
- **F27.5.3** `export_chargeback_pdf(tenant_id, period_key, actor_id) -> bytes` 결정 wire = reportlab 기반 PDF generation 결정 wire + company logo (Phase 1 wire 의 branding assets 정합) + department breakdown 차트 PDF 임베드 (Recharts → matplotlib → PDF 변환 또는 reportlab.graphics 결정 wire) + page layout 결정 wire (title + period + tenant_slug + table + footer + page numbers) + reportlab AD-14 stack pin 결정 wire (`reportlab==4.0.7` 결정).
- **F27.5.4** PDF generation 결정 wire = `SimpleDocTemplate` + `Table` + `Paragraph` 3 components 결정 wire + font 결정 (한글 폰트 NOTO Sans CJK KR 결정 wire + `noto-sans-cjk-kr` AD-14 stack pin 결정) + page size A4 결정 wire + orientation landscape 결정 wire (department breakdown wide table) 결정 wire + filename 결정 `chargeback-{tenant_slug}-{period_key}.pdf` 결정 wire.
- **F27.5.5** streaming response 결정 wire = `StreamingResponse` 의 generator 함수 결정 wire (대용량 chargeback 데이터의 memory-efficient streaming) + `Content-Disposition: attachment; filename=...` header 결정 wire + `Content-Type: text/csv; charset=utf-8` 또는 `application/pdf` 결정 wire + `Cache-Control: no-store` 결정 wire (민감 데이터 cache 방지 결정 wire + NFR4 PII minimization 정합 결정 wire).
- **F27.5.6** audit-first INSERT `chargeback_exported` 결정 wire (CR 1-1 verbatim, action_class='FINOPS' + action='chargeback_exported' + actor_id + tenant_id + period_key + export_format enum csv/pdf + row_count + file_size_bytes + trace_id 정합 + per-tenant RLS 자동 적용 결정 wire + 누가 언제 어떤 format 으로 export 했는지 추적 결정 wire + Phase 6 wire `24e1cd7` 의 5 NEW audit log entries pattern verbatim 미러 결정 wire).
- **F27.5.7** export 의 permission check 결정 wire = owner-only RBAC AD-22 verbatim 결정 wire + `require_role("owner")` FastAPI Dependency 결정 wire + Epic 12 2FA 챌린지 보존 결정 wire + Epic 12 M12-a 2FA 미설정 시 `/account/security?reason=2fa_required` redirect 결정 wire + 미허용 tenant 의 export 차단 결정 wire (capability gate FINOPS_CHARGEBACK + FINOPS_SHOWBACK 자동 적용) 결정 wire.
- **F27.5.8** export 의 rate limit 결정 wire (Phase 10 wire `ac5d6c5` 의 SLO breach rate limit EXTENSION pattern verbatim 미러) = owner 1 export / minute (default) + rate limit 초과 시 429 status 결정 wire + `Retry-After` header 결정 wire + 1 NEW error class ChargebackExportRateLimitedError(429) CR 12-5 D-14 envelope 결정 wire + audit-first INSERT `chargeback_export_rate_limited` CR 1-1 verbatim 결정 wire.
- **F27.5.9** export 의 error handling 결정 wire (CR 12-5 D-14 verbatim) = ChargebackRuleInvalidError(400) message_ko 결정 ("chargeback rule validation 실패: {validation_error}") + ChargebackCalculationError(500) message_ko 결정 ("chargeback 계산 중 오류 발생: {error_detail}") + ChargebackExportError(500) message_ko 결정 ("chargeback export 실패: {export_error_detail}") + ko-KR.json `finops.*` EXTENSION 결정 (CR 11-4 D-002 verbatim SSOT: error_finops_failed + finops_export_label) 결정 wire.
- **F27.5.10** export 의 cache 결정 wire = Redis cache 결정 (CSV/PDF 의 byte stream cache 5분 TTL + cache key = `(tenant_id, period_key, export_format, query_hash)` + cache invalidation 시점: AD-25 의 LISTEN/NOTIFY `cost_engine_cache` channel EXTENSION 결정 wire + NFR4 PII minimization 정합 보존 + 동시 export 요청 시 cache hit 으로 backend load 감소 결정 wire).

### F27.6 Capability matrix v1.36 EXTENSION FINOPS_SHOWBACK + FINOPS_CHARGEBACK 2 NEW rows (12 sub-ACs)

- **F27.6.1** `Capability.FINOPS_SHOWBACK = "finops_showback"` 결정 wire = 1 NEW enum 추가 (manufacturing ✅ + service ✅ + manufacturing_service ✅ + manufacturing_service_other ✅ industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러, SLO_ENGINEERING Phase 10 wire + CHAOS_ENGINEERING Phase 9 wire + PERFORMANCE_TESTING Phase 8 wire + OBSERVABILITY_TRACES + OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind) 결정 wire.
- **F27.6.2** `Capability.FINOPS_CHARGEBACK = "finops_chargeback"` 결정 wire = 1 NEW enum 추가 (동일 industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러, FINOPS_SHOWBACK + SLO_ENGINEERING + CHAOS_ENGINEERING + PERFORMANCE_TESTING + OBSERVABILITY_TRACES + OBSERVABILITY_METRICS + AUDIT_LOG_RETENTION + AUDIT_LOG_VIEW + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER + TENANT_IDP_MANAGEMENT + SSO_ENTERPRISE + LISTEN_NOTIFY + AUTH_MIDDLEWARE + LAUNCH_* + DEPLOYMENT_* pattern verbatim bind) 결정 wire.
- **F27.6.3** `apps/api/core/capability.py` MODIFIED 결정 wire = `Capability` enum EXTENSION 2 NEW values 결정 wire (`FINOPS_SHOWBACK` + `FINOPS_CHARGEBACK`) + `__all__` EXTENSION 결정 wire + capability matrix v1.35 → v1.36 EXTENSION 결정 wire + Phase 10 wire `ac5d6c5` 의 Capability.SLO_ENGINEERING EXTENSION pattern verbatim 미러 결정 wire.
- **F27.6.4** `apps/api/dependencies/capability.py` MODIFIED 결정 wire = `require_finops_showback()` + `require_finops_chargeback()` 2 NEW dependencies 결정 wire + `__all__` EXTENSION 결정 wire + 미허용 tenant 의 FinOps 진입 차단 결정 wire + SSOT RED→GREEN EXTENSION 결정 wire (capability matrix v1.36 신규 2 rows + capability.py EXTENSION 2 NEW enum + require_finops_showback + require_finops_chargeback 2 NEW deps 결정 wire).
- **F27.6.5** docs/capability-matrix.md v1.35 → v1.36 EXTENSION 결정 wire = 2 NEW rows (FINOPS_SHOWBACK + FINOPS_CHARGEBACK) industry-agnostic 4-industry grants ✅/✅/✅/✅ 결정 wire + Phase 10 wire 의 capability matrix v1.35 EXTENSION pattern verbatim 미러 결정 wire + m18_slo_engineering 의 section EXTENSION 결정 wire (FINOPS section 신규 추가 결정 wire).
- **F27.6.6** drift detector `tests/integration/test_capability_matrix_v1_36_drift.py` NEW 결정 wire = 10 NEW pytest cases 결정 (capability matrix v1.36 의 FINOPS_SHOWBACK + FINOPS_CHARGEBACK row 존재 검증 + 4-industry grants ✅/✅/✅/✅ 검증 + Capability enum 값 검증 + `require_finops_showback` + `require_finops_chargeback` dependency 동작 검증 + 미허용 tenant 진입 차단 검증 + Phase 10 wire 의 `tests/integration/test_capability_matrix_v1_35_drift.py` 패턴 verbatim 미러 결정 wire + Phase 9 wire 의 `tests/integration/test_capability_matrix_v1_34_drift.py` + Phase 8 wire 의 `tests/integration/test_capability_matrix_v1_33_drift.py` 패턴 verbatim 미러 결정 wire).
- **F27.6.7** m18_slo_engineering + m19_finops 결정 wire = `apps/api/modules/finops/__init__.py` NEW 결정 wire + `apps/api/modules/finops/serializers.py` NEW 결정 wire (m18_slo_engineering.slo_engineering_serializers 의 Phase 10 wire EXTENSION pattern verbatim 미러) 결정 wire + module version 결정 wire (m19_finops 결정 wire + Phase 10 wire 의 m18_slo_engineering 와 동일 pattern).
- **F27.6.8** 미허용 tenant 의 FinOps 진입 차단 결정 wire = `require_finops_showback` + `require_finops_chargeback` dependency 의 미허용 tenant 진입 차단 검증 결정 wire + 403 Forbidden 결정 wire + `FORBIDDEN_KO` message 결정 wire ("FinOps capability 미허용 tenant") + Epic 12 2FA 챌린지 보존 결정 wire + AD-22 owner-only RBAC 정합 결정 wire.
- **F27.6.9** SSOT RED→GREEN EXTENSION 결정 wire = capability matrix v1.36 신규 2 rows + capability.py EXTENSION 2 NEW enum + require_finops_showback + require_finops_chargeback 2 NEW deps 결정 wire + drift detector 결정 wire + A36 SDR 검증 4-step 자동 적용 결정 wire (commit prefix lint PASS + sprint-status structure PASS + vitest file count drift 0건 + commit consistency PASS) + CR 12-5 D-GATE-01 inversion 적용 보존 결정 wire.
- **F27.6.10** CR 12-1 L4 industry-agnostic capability 결정 wire = FINOPS_SHOWBACK + FINOPS_CHARGEBACK 모두 industry-agnostic 4-industry grants ✅/✅/✅/✅ 결정 wire (manufacturing + service + manufacturing_service + manufacturing_service_other 모두 허용) + Phase 10 wire 의 SLO_ENGINEERING industry-agnostic 4-industry grants 결정 wire pattern verbatim 미러 결정 wire + Epic 1~17 + Phase 3~10 territory 와 동일 industry-agnostic 결정 wire.
- **F27.6.11** capability gate 의 fail-closed 결정 wire = 미허용 tenant 의 FinOps 진입 시 403 Forbidden 결정 wire + capability matrix v1.36 row 부재 시 fail-closed 결정 wire + `Capability.FINOPS_SHOWBACK` + `Capability.FINOPS_CHARGEBACK` enum 부재 시 fail-closed 결정 wire + AD-22 owner-only RBAC 정합 결정 wire + Epic 12 2FA 챌린지 보존 결정 wire + NFR4 PII minimization 정합 보존 결정 wire.
- **F27.6.12** capability matrix 의 version 결정 wire = v1.35 → v1.36 EXTENSION 결정 wire + version bump 일관성 결정 wire (Phase 10 wire 의 capability matrix v1.34 → v1.35 pattern verbatim 미러) + docs/capability-matrix.md 의 version 표기 결정 wire + sprint-status.yaml 의 capability version 결정 wire + drift detector 의 version 매칭 검증 결정 wire + CR 12-5 D-PARITY-01 inversion 적용 보존 결정 wire.

### F27.7 dry-run + Tests + wire scope T1~T8 (12 sub-ACs)

- **F27.7.1** dry-run mode 결정 wire = `--finops-dry-run` CLI flag + showback DSL dry-run parameter + chargeback engine dry-run parameter + CSV/PDF export dry-run parameter 결정 wire + dry-run 시 actual chargeback_calculated INSERT skip + dry-run 시 actual CSV/PDF export skip + dry-run 시 actual department_mapping_updated INSERT skip 결정 wire + dry-run 결과 preview 결정 wire (chargeback_preview 테이블 alembic 0043 신규 결정 wire).
- **F27.7.2** dry-run 의 preview 결과 결정 wire = `chargeback_preview` table 결정 wire (preview_id UUID PK + tenant_id UUID + period_key TEXT + rule_type TEXT + preview_amount NUMERIC(20, 2) + preview_breakdown JSONB + computed_at TIMESTAMPTZ DEFAULT NOW() + trace_id TEXT) + preview_breakdown JSONB 구조 결정 wire (`{'department_id': amount, 'cost_center_id': amount, 'rule_type': amount}`) + RLS 자동 적용 CR 0-2 verbatim 결정 wire + audit-first INSERT `chargeback_dry_run_executed` CR 1-1 verbatim 결정 wire.
- **F27.7.3** dry-run 의 CLI flag 결정 wire = `--finops-dry-run` global flag + `--showback-dry-run` + `--chargeback-dry-run` + `--export-dry-run` sub-flags 결정 wire + dry-run 모드 시 logger 출력 결정 (stdout + INFO level) + dry-run 결과 preview 결정 wire (console table output 결정 wire).
- **F27.7.4** tests 결정 wire = ~+46 NEW pytest PASS (showback_dsl 6 + chargeback_engine 7 + department_mapping 5 + chargeback_export 6 + alembic 0043 4 + audit action 8 + capability matrix v1.36 10 = ~+46 NEW pytest PASS) 결정 wire (CR 11-4 verbatim) + showback_dsl 6 tests 결정 (parse_showback_definition validation 3 + query_showback_breakdown 2 + audit-first INSERT 1) + chargeback_engine 7 tests 결정 (compute_chargeback 3종 rule 3 + markup + tax + error class 4) + department_mapping 5 tests 결정 (validate_department_mapping 2 + auto-create 2 + audit-first INSERT 1) + chargeback_export 6 tests 결정 (CSV streaming 2 + PDF generation 2 + rate limit + audit-first INSERT 1) + alembic 0043 4 tests 결정 (migration + RLS + CHECK constraints + UNIQUE constraint 4) + audit action 8 tests 결정 (showback_generated + department_mapping_updated + chargeback_exported 3 NEW actions + _ActionRegistry FINOPS entry 3 NEW 등록 + multi-tenant isolation 2) + capability matrix v1.36 10 tests 결정 (FINOPS_SHOWBACK + FINOPS_CHARGEBACK row 존재 2 + 4-industry grants 2 + Capability enum 2 + require_finops_showback + require_finops_chargeback 2 + 미허용 tenant 차단 2) 결정 wire.
- **F27.7.5** vitest tests 결정 wire = ~+5 NEW vitest PASS (ShowbackPeriodSelector + ShowbackDepartmentBreakdownChart owner-only ack prompt AD-22 verbatim + ShowbackComparisonView RTL render + ko-KR SSOT 2 + finops dashboard parity CR 12-5 D-PARITY-01 결정 wire) 결정 wire + Phase 10 wire 의 ~5 NEW vitest PASS pattern verbatim 미러 결정 wire.
- **F27.7.6** ruff + tsc 결정 wire = 0 NEW ruff 결정 wire (Phase 10 wire 정합) + 0 NEW tsc 결정 wire (apps/web frontend 변경 ext only — pre-existing baseline errors preserved per cj-style discipline) + SDR drift gate 결정 wire (vitest file count +1 NEW collected well within 5% tolerance + pytest +8 NEW files collected well within 5% tolerance) 결정 wire + commit_consistency gate 결정 wire (CR 9-6 commit message discipline + A36 SDR 검증 4-step 자동 적용) 결정 wire + D-DEFER-* grep guard 결정 wire (CR 11-3 honest-DEFER discipline 105번째 epic 연속 정직 회복 검증 보존) 결정 wire.
- **F27.7.7** wire scope T1~T8 결정 wire = T1 showback_dsl + showback_query module 결정 wire + T2 chargeback_engine + chargeback_rule_evaluator 결정 wire + T3 department_mapping + tenant_settings JSONB schema 결정 wire + T4 chargeback CSV/PDF export 결정 wire + T5 alembic 0043 phase_11_finops 결정 wire + T6 audit action EXTENSION 3 NEW 결정 wire + T7 capability v1.36 EXTENSION + frontend finops dashboard 결정 wire + T8 3중 게이트 FINAL CLEAN atomic commit 결정 wire + estimated ~+30-35 files = ~+18 NEW + ~+12 MODIFIED 결정 wire + Phase 10 wire 의 wire_commit pattern verbatim 미러 결정 wire.
- **F27.7.8** A19 cohesion pattern 9 surface EXTENSION PASS 결정 wire (FinOps showback/chargeback surface NEW = F27.1~F27.7 결정 wire) 결정 wire + Phase 10 wire 의 SLO engineering surface NEW = F26.1~F26.7 pattern verbatim 미러 결정 wire + Phase 9 wire 의 chaos engineering surface NEW = F25.1~F25.7 pattern verbatim 미러 결정 wire.
- **F27.7.9** CR lessons applied 14종 결정 wire (CR 0-2 RLS lesson ✅ APPLIED + CR 1-1 audit-first INSERT ✅ APPLIED (3 NEW audit log entries `showback_generated` + `department_mapping_updated` + `chargeback_exported`) + CR 4-3/4-4 lessons carry ✅ APPLIED (showback + chargeback baseline + golden_diff pattern verbatim) + CR 1-1 ContextVar lesson ✅ APPLIED + CR 1-1 RSC boundary lesson ✅ APPLIED + CR 9-6 commit message discipline ✅ APPLIED (`git commit -F <file>` 사용, PowerShell here-string 회피, D5 prevention) + CR 11-3 honest-DEFER discipline ✅ APPLIED (105번째 epic 연속 정직 회복) + CR 11-4 D-001~D-005 + P-015 ✅ APPLIED + CR 12-1 L4 ✅ APPLIED (FINOPS_SHOWBACK + FINOPS_CHARGEBACK industry-agnostic 4-industry grants ✅/✅/✅/✅) + CR 12-5 D-14 typed exception envelope ✅ APPLIED (5 NEW typed exception classes `ShowbackDefinitionInvalidError` + `ShowbackExportError` + `ChargebackRuleInvalidError` + `ChargebackCalculationError` + `ChargebackExportError` + `ChargebackExportRateLimitedError`) + CR 12-5 D-PARITY-01 inversion ✅ APPLIED + CR 12-5 D-GATE-01 inversion ✅ APPLIED + A36 SDR 검증 4-step 자동 적용 ✅ + AD-14 stack pin ✅ APPLIED (reportlab + noto-sans-cjk-kr + openpyxl) + AD-22 owner-only RBAC ✅ APPLIED (showback/chargeback export + mapping update 모두 owner-only RBAC AD-22 + Epic 12 2FA 챌린지 보존) + NFR4 PII minimization ✅ PRESERVED) 결정 wire.
- **F27.7.10** D-DEFER-* honestly 결정 wire (CR 11-3 105번째 epic 연속 정직 회복 결정 wire 보존) = D-1-1-DEFER-1/2/3 RESOLVED 보존 60~105번째 + D-LAUNCH-1-DEFER-1 honestly preserved 65~105번째 + D-EPIC-16-REVIEW-DEFER-2~6 ✅ RESOLVED 보존 78~105번째 + D-PHASE-4-DR-DEFER-1/2 ✅ RESOLVED 보존 73~105번째 + D-EPIC-17-WIRE-DEFER-T2-T3-UI ✅ RESOLVED 보존 83~105번째 + D-RETENTION-1 ✅ RESOLVED 보존 85~105번째 + D-OBSERVABILITY-1 ✅ RESOLVED 보존 89~105번째 + D-PERFORMANCE-1 ✅ RESOLVED 보존 93~105번째 + D-CHAOS-1 ✅ RESOLVED 보존 100~105번째 + D-SLO-1 ✅ RESOLVED 보존 101~105번째 + **D-FINOPS-1 honestly DEFER 보존 = Phase 11 PRD entry 진입 시점에 carry-over chain 정직 회복 결정 wire 진입: 1st release close-out retro §6 + Epic 17 close-out retro §11 + Phase 6 close-out retro §13 + Phase 7 close-out retro §10 + Phase 8 close-out retro §10 + Phase 9 close-out retro §10 + Phase 10 close-out retro `733d428` §10 "FinOps Showback / Chargeback 결정 wire 보류, Phase 11+ 진입 시점" verbatim 해소** 결정 wire.
- **F27.7.11** Epic 1 ~ Epic 17 + Phase 3 ~ Phase 10 + 1st release cycle 정합 보존 결정 wire (cj-style 105번째 epic 연속 정직 회복 pre-flight 정합 sweep 결정 wire 보존): ✅ Phase 10 close-out retro `733d428` (cj-style 104번째) + ✅ Phase 10 atomic wire T1~T8 `ac5d6c5` (cj-style 103번째) + ✅ Phase 10 spec entry `3c80ef0` (cj-style 102번째) + ✅ Phase 10 PRD entry `09db4d4` (cj-style 101번째) + ✅ Phase 9 close-out retro `634427d` (cj-style 100번째) + ✅ Phase 9 atomic wire T1~T8 `e7670e1` (cj-style 99번째) + ✅ Phase 9 spec entry `2a5e4da` (cj-style 98번째) + ✅ Phase 9 PRD entry `0b2d2f3` (cj-style 97번째) + ✅ Phase 8 close-out retro `ab495a8` (cj-style 96번째) + ✅ Phase 8 atomic wire T1~T8 `60d4ea1` (cj-style 95번째) + ✅ Phase 8 spec entry `5ae0f4e` (cj-style 94번째) + ✅ Phase 8 PRD entry `ced452f` (cj-style 93번째) + ✅ Build fixes sprint `eaee198` + ✅ Phase 7 close-out retro `326fa9f` (cj-style 92번째) + ✅ Phase 7 atomic wire T1~T8 `59b56cd` (cj-style 91번째) + ✅ Phase 7 spec entry `749381e` (cj-style 90번째) + ✅ Phase 7 PRD entry `916a541` (cj-style 89번째) + ✅ Phase 6 close-out retro `f9f006c` (cj-style 88번째) + ✅ Phase 6 atomic wire T1~T8 `24e1cd7` (cj-style 87번째) + ✅ Phase 6 spec entry `f5c14c9` (cj-style 86번째) + ✅ Phase 6 PRD entry `e84a281` (cj-style 85번째) + ✅ Epic 17 close-out retro `be8f3bd` (cj-style 84번째) + ✅ Epic 17 T2+T3 UI wire `bb92879` (cj-style 83번째) + ✅ Epic 17 wire `2ada2ec` (cj-style 82번째) + ✅ Epic 17 spec entry `f4b2b58` (cj-style 81번째) + ✅ Epic 17 PRD entry `40a9c41` (cj-style 80번째) + ✅ Sidebar/MenuProvider hot-fix `01a06e4` (cj-style 79번째) + ✅ D-EPIC-16-REVIEW-DEFER-2~6 RESOLVE sprint `512ed6a` (cj-style 78번째) + ✅ Phase 5 close-out retro `b843565` (cj-style 76~77번째) + ✅ Phase 5 wire `f093f8c` (cj-style 75번째) + ✅ Phase 5 spec entry (cj-style 74번째) + ✅ Phase 5 PRD entry `93d852b` (cj-style 73번째) + ✅ Epic 16 close-out retro (cj-style 72번째) + ✅ Epic 16 T4 admin UI follow-up `ff5c3b5` (cj-style 71번째) + ✅ Epic 16 review follow-up `963079c` (cj-style 70번째) + ✅ Epic 16 wire `e117e09` (cj-style 69번째) + ✅ Epic 16 PRD entry `08bfca5` (cj-style 67번째) + ✅ 1st release cycle cj-style 62~66번째 모두 wire DONE 진입 + ✅ Epic 15 cycle cj-style 58~61번째 모두 wire DONE 진입 + ✅ Phase 4 cycle cj-style 53~57번째 모두 wire DONE 진입 + ✅ Phase 3 cycle cj-style 49~52번째 모두 wire DONE 진입 + ✅ Epic 14 LISTEN/NOTIFY multi-process `7835463` + ✅ Epic 13 LISTEN/NOTIFY consume `f2ea2f6` + ✅ Epic 12 2FA 게이트 `a63646c` (FinOps showback/chargeback export 모두 owner-only RBAC AD-22 + Epic 12 2FA 챌린지 보존) + ✅ Epic 11 close-out retro + Phase 2 close-out baseline 599 passed 정합 보존 + ✅ Epic 1 carry-over (auth) layout + onboarding/industry 보존 + ✅ Epic 7~10 ABC/TDABC + AI 인사이트 territory 결정 wire 보존 결정 wire.
- **F27.7.12** partial wire 시도 0건 + single sprint atomic docs-only wire 1 진입점 결정 wire (cj-style 105번째 epic 연속 정직 회복 Phase 11 PRD entry atomic docs-only wire 6 files atomic single sprint 결정 wire) + 결정 wire 일자: 2026-08-24 (KST) + **next**: 옵션 (a) Phase 11 bmad-dev-story atomic wire T1~T8 진입 (cj-style 106번째 wire 진입 시점) / 옵션 (b) Phase 11 close-out retro 진입 (cj-style 107번째) / 옵션 (c) Phase 12+ 진입 / 옵션 (d) Epic 18+ 진입 / 옵션 (e) D-DEFER-* follow-up 결정 wire 보류 결정 wire.

---

# F28. Phase 12 territory (옵션 (a) Phase 12+ 진입 결정 wire, cj-style 109번째 epic 연속 정직 회복)

## F28. Cost Anomaly Detection & Budget Alerting (Phase 12 territory 신규)

> **§F28 결정 wire 진입** — Phase 11 close-out retro `80df15b` (cj-style 108번째 wire entry) 직후 다음 옵션 5종 중 **사용자 권장 결정 = 옵션 (a) Phase 12+ 진입 + 옵션 (a) Cost Anomaly Detection & Budget Alerting (Recommended) (anomaly detection DSL 4 methods z-score + IQR + EWMA + isolation forest + budget definition DSL + alert routing Slack + PagerDuty + forecast accuracy tracking + audit-first INSERT 4 NEW + Capability FINOPS_ANOMALY_DETECTION + FINOPS_BUDGET_ALERT 2 NEW + dry-run mode + tests + wire scope T1~T8) = Phase 12 territory 결정 wire 진입** (AskUserQuestion 결정 verbatim bind). rationale 5종: ① Phase 11 wire `e020ad0` 의 FinOps Showback / Chargeback territory (showback DSL department breakdown + period selector + comparison view + chargeback cost allocation engine 정액제/비율배분/usage-based 3종 + markup + tax + department cost center mapping tenant_settings JSONB + showback dashboard UI + chargeback CSV/PDF export) 의 natural backend carry-over chain = Phase 12 = Cost Anomaly Detection & Budget Alerting territory 의 natural next (showback baseline 대비 deviation 감지 = cost anomaly detection + chargeback 한도 초과 알림 = budget alert + statistical + ML hybrid detection methods + alert routing/escalation) = Phase 11 FinOps territory 의 DETECTION & ALERTING LAYER EXTENSION 결정 ② cj-style discipline 회피 위험 방지 (49~108번째 누적 60-entry-point cycle + 108번째 Phase 11 close-out retro + 107번째 atomic wire + 106번째 spec entry + 105번째 PRD entry 패턴 모두 wire DONE 진입 정합 보존 후 즉시 Phase 12 진입 = 1-day atomic sprint discipline) ③ 비즈니스 우선순위 = enterprise 고객 onboarding 시 cost anomaly 자동 감지 (showback baseline 대비 z-score > 3.0 deviation) + budget overrun 알림 (chargeback 한도 90% 초과 시 Slack + 100% 초과 시 PagerDuty) territory 필수 ④ Phase 11 wire `e020ad0` 의 showback period selector + 12-period comparison + cost-engine benchmark V8 골든 + Phase 8 wire `60d4ea1` 의 cost-engine 12-period benchmark 의 자연스러운 carry-over chain = Phase 12 anomaly detection 의 historical baseline (last 30d + last 90d + YTD) + statistical model training + forecast deviation tracking EXTENSION 정합 ⑤ 1st release close-out retro + Epic 17 close-out retro + Phase 5 close-out retro + Phase 6 close-out retro + Phase 7 close-out retro + Phase 8 close-out retro + Phase 9 close-out retro + Phase 10 close-out retro + Phase 11 close-out retro territory verbatim 보존 + AD-22 owner-only RBAC + Epic 12 2FA 챌린지 보존 + AD-14 stack pin + NFR4 PII minimization ✅ PRESERVED + AD-39 Cost Anomaly Detection & Budget Alerting 신규 결정 ((a)~(g) 7 sub-decisions 모두 결정 wire 진입). §F28 territory = Cost Anomaly Detection & Budget Alerting 결정 wire 진입 시점에 8 ACs (§F28.1 anomaly detection DSL + §F28.2 budget definition DSL + §F28.3 anomaly detection engine + alert routing + §F28.4 budget overrun alerts + §F28.5 forecast accuracy tracking + §F28.6 anomaly detection dashboard UI + §F28.7 Capability matrix v1.37 EXTENSION + dry-run + tests + wire scope T1~T8) + §8.1 M0-(u) Cost Anomaly Detection & Budget Alerting AC 신규 + §15 로드맵 Phase 12 row status 백로그 → in-progress (PRD entry DONE 진입 wire) + §부록 A A363+A364+A365+A366+A367+A368+A369+A370+A371+A372 신규 결정 표 + AD-39 Cost Anomaly Detection & Budget Alerting 신규 결정 + capability matrix v1.36 → v1.37 EXTENSION FINOPS_ANOMALY_DETECTION + FINOPS_BUDGET_ALERT 2 NEW rows industry-agnostic 4-industry grants ✅/✅/✅/✅ (CR 12-1 L4 precedent 미러, FINOPS_SHOWBACK + FINOPS_CHARGEBACK Phase 11 wire + SLO_ENGINEERING Phase 10 wire + CHAOS_ENGINEERING Phase 9 wire + PERFORMANCE_TESTING Phase 8 wire + OBSERVABILITY_TRACES + OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind). CR 11-3 honest-DEFER discipline 109번째 epic 연속 정직 회복 검증 ✅ (D-1-1-DEFER-1/2/3 RESOLVED 보존 60~109번째 + D-LAUNCH-1-DEFER-1 honestly preserved 65~109번째 + D-EPIC-16-REVIEW-DEFER-2~6 ✅ RESOLVED 보존 78~109번째 + D-PHASE-4-DR-DEFER-1/2 ✅ RESOLVED 보존 73~109번째 + D-EPIC-17-WIRE-DEFER-T2-T3-UI ✅ RESOLVED 보존 83~109번째 + D-RETENTION-1 ✅ RESOLVED 보존 85~109번째 + D-OBSERVABILITY-1 ✅ RESOLVED 보존 89~109번째 + D-PERFORMANCE-1 ✅ RESOLVED 보존 93~109번째 + D-CHAOS-1 ✅ RESOLVED 보존 100~109번째 + D-SLO-1 ✅ RESOLVED 보존 101~109번째 + D-FINOPS-1 ✅ RESOLVED 보존 105~109번째 + **D-FINOPS-2 신규 honestly DEFER 보존 = Phase 12 PRD entry 진입 시점에 carry-over chain 정직 회복 결정 wire 진입: Phase 11 close-out retro `80df15b` §12 + Phase 10 close-out retro `733d428` §10 + Phase 9 close-out retro `634427d` §10 + Phase 8 close-out retro `ab495a8` §10 + Phase 7 close-out retro `326fa9f` §10 + Phase 6 close-out retro `f9f006c` §13 + Epic 17 close-out retro `be8f3bd` §11 "Cost Anomaly Detection & Budget Alerting 결정 wire 보류, Phase 12+ 진입 시점" verbatim 해소**).

### F28.1 anomaly detection DSL (12 sub-ACs, A363 결정)

- **F28.1.1** anomaly detection DSL syntax 결정 wire = `detect_anomaly(tenant_id, period_key, dimension, threshold_method)` builder + AST 5 levels (tenant_id + period_key + dimension + threshold_method + threshold_value) + parser 검증 3 layer (syntax + semantic + tenant-scope RLS) 결정 wire.
- **F28.1.2** 4 detection methods 결정 wire = (1) z-score (μ + 3σ rule, default threshold 3.0) / (2) IQR (Q3 + 1.5×IQR rule, default multiplier 1.5) / (3) EWMA (exponentially weighted moving average, default λ 0.3) / (4) isolation forest (default contamination 0.1, sklearn==1.4.0 AD-14 stack pin) 결정 wire + multi-method voting consensus (3 of 4 methods agree = anomaly confirmed) 결정 wire + false positive suppression (require 3 consecutive periods 동일 anomaly) 결정 wire.
- **F28.1.3** 5 dimension 옵션 결정 wire = (1) department (department_id 별 anomaly) / (2) cost_center (cost_center_id 별 anomaly) / (3) product_line (product_line_id 별 anomaly) / (4) service (service_id 별 anomaly) / (5) tenant_total (tenant 전체 aggregated anomaly) 결정 wire + dimension validation 결정 wire (tenant_settings.cost_center_mapping JSONB 기반 + Phase 11 wire `e020ad0` department_mapping EXTENSION 정합).
- **F28.1.4** historical baseline 결정 wire = last 30d + last 90d + YTD 3 baseline windows 결정 wire + baseline windows user-configurable (per-tenant override JSONB) 결정 wire + rolling baseline update cron KST 매일 04:00 UTC 19:00 결정 wire + baseline data source = phase_11_finops_showback table (Phase 11 wire 정합) + phase_11_finops_chargeback table (Phase 11 wire 정합) 결정 wire.
- **F28.1.5** threshold constants 결정 wire = `ANOMALY_THRESHOLD_DEFAULTS = {'z_score': 3.0, 'iqr_multiplier': 1.5, 'ewma_lambda': 0.3, 'isolation_forest_contamination': 0.1, 'consecutive_periods_required': 3}` constants + `ANOMALY_DIMENSIONS = ['department', 'cost_center', 'product_line', 'service', 'tenant_total']` constants 결정 wire.
- **F28.1.6** 4 industries baseline 결정 wire = manufacturing + service + manufacturing_service + manufacturing_service_other 모두 industry-agnostic 4-industry grants ✅/✅/✅/✅ (CR 12-1 L4 precedent 미러, Phase 11 wire 정합) + per-tenant override EXTENSION + policy evaluation precedence tenant override > industry baseline > system default 결정 wire.
- **F28.1.7** `apps/api/modules/finops/anomaly_detection.py` NEW ~+150 LOC + `parse_anomaly_definition(definition_text)` parser + `validate_anomaly_scope(tenant_id, dimension)` validator + `AnomalyDefinition` TypedDict 8 fields (tenant_id + period_key + dimension + dimension_value + threshold_method + threshold_value + baseline_window + consecutive_periods_required) + 5 layer defense (syntax + semantic + tenant-scope RLS + dimension validation + baseline data availability) 결정 wire.
- **F28.1.8** audit-first INSERT `anomaly_detected` CR 1-1 verbatim 결정 wire = action_class='FINOPS_ANOMALY' (NEW) + action='anomaly_detected' (NEW) + actor_id='system' + tenant_id + period_key + dimension + dimension_value + observed_value + expected_value + deviation_pct + severity enum warning/critical + detection_method + consecutive_periods + trace_id 결정 wire + ActionClass.FINOPS_ANOMALY 신규 정의 + apps/api/core/audit_action.py MODIFIED AuditAction Literal EXTENSION 1 NEW value + _ActionRegistry FINOPS_ANOMALY entry 신규 1개 등록 + __all__ EXTENSION 결정 wire.
- **F28.1.9** typed exception envelope CR 12-5 D-14 결정 wire = `AnomalyDefinitionInvalidError(400)` + `AnomalyDetectionError(500)` + `AnomalyBaselineUnavailableError(404)` 3 NEW typed exception classes + envelope `{error_code, message_ko, trace_id, timestamp, details}` 결정 wire.
- **F28.1.10** RLS 자동 적용 CR 0-2 verbatim 결정 wire = phase_12_finops_anomaly tables 의 `tenant_id = current_setting('app.tenant_id')::uuid` policy + multi-tenant isolation 검증 + 테스트 결정 wire (Phase 11 wire `e020ad0` 의 finops_tenant_isolation 테스트 패턴 verbatim 미러).
- **F28.1.11** dry-run mode `--finops-anomaly-dry-run` CLI flag 결정 wire = anomaly detection 실행 + 결과 preview + actual `anomaly_detected` audit-first INSERT skip + dry-run 결과 preview `phase_12_finops_anomaly_preview` table 결정 wire.
- **F28.1.12** V8 determinism byte-identical 테스트 결정 wire = 동일 input + 동일 baseline → 동일 anomaly result 결정 wire (Phase 11 wire 정합 + sha256:64-hex fingerprint 결정 wire).

### F28.2 budget definition DSL (12 sub-ACs, A364 결정)

- **F28.2.1** budget definition DSL syntax 결정 wire = `define_budget(tenant_id, period_key, scope, scope_id, amount)` builder + AST 6 levels (tenant_id + period_key + scope + scope_id + amount + alert_thresholds) + parser 검증 3 layer 결정 wire.
- **F28.2.2** budget_period enum 결정 wire = (1) monthly (1st ~ last day) / (2) quarterly (Q1 ~ Q4) / (3) yearly (Jan 1 ~ Dec 31) 결정 wire + KST timezone 결정 wire + budget_period_start + budget_period_end auto-derive 결정 wire.
- **F28.2.3** budget_scope enum 결정 wire = (1) tenant (tenant_id 별 budget) / (2) department (department_id 별 budget) / (3) cost_center (cost_center_id 별 budget) / (4) product_line (product_line_id 별 budget) 결정 wire + scope_id FK constraint 결정 wire + RLS 자동 적용 CR 0-2 verbatim 결정 wire.
- **F28.2.4** budget_amount NUMERIC(20, 2) 결정 wire = max 999,999,999,999,999,999.99 (소수점 2자리) + currency KRW (default) + per-tenant override EXTENSION (USD/EUR/JPY 지원) 결정 wire.
- **F28.2.5** alert_thresholds TypedDict 결정 wire = (1) warning_pct 80 (default) / (2) critical_pct 90 (default) / (3) exceeded_pct 100 (default) / (4) warning_alert_channels ['slack'] (default) / (5) critical_alert_channels ['slack', 'pagerduty'] (default) / (6) exceeded_alert_channels ['slack', 'pagerduty', 'email'] (default) + per-tenant override JSONB 결정 wire.
- **F28.2.6** `apps/api/modules/finops/budget_definition.py` NEW ~+150 LOC + `parse_budget_definition(definition_text)` parser + `validate_budget_scope(tenant_id, scope, scope_id)` validator + `BudgetDefinition` TypedDict 12 fields (budget_id + tenant_id + period_key + budget_period + scope + scope_id + amount + currency + alert_thresholds + status enum active/paused/expired + created_at + updated_at) + 5 layer defense 결정 wire.
- **F28.2.7** audit-first INSERT `budget_definition_updated` CR 1-1 verbatim 결정 wire = action_class='FINOPS_BUDGET' (NEW) + action='budget_definition_updated' (NEW) + actor_id + tenant_id + budget_id + budget_amount + scope + alert_thresholds + trace_id 결정 wire + ActionClass.FINOPS_BUDGET 신규 정의 + AuditAction Literal EXTENSION 1 NEW value + _ActionRegistry FINOPS_BUDGET entry 신규 1개 등록 + __all__ EXTENSION 결정 wire.
- **F28.2.8** typed exception envelope CR 12-5 D-14 결정 wire = `BudgetDefinitionInvalidError(400)` + `BudgetScopeInvalidError(404)` + `BudgetAmountInvalidError(400)` 3 NEW typed exception classes + envelope 결정 wire.
- **F28.2.9** 4 industries baseline 결정 wire = industry-agnostic 4-industry grants ✅/✅/✅/✅ + manufacturing_baseline + service_baseline + manufacturing_service_baseline + manufacturing_service_other_baseline 결정 wire (예: manufacturing baseline monthly = ₩50M, service baseline monthly = ₩30M).
- **F28.2.10** RLS 자동 적용 CR 0-2 verbatim 결정 wire = phase_12_finops_budget tables 의 RLS policy + UNIQUE constraint `UNIQUE (tenant_id, period_key, scope, scope_id)` 결정 wire + multi-tenant isolation 검증 결정 wire.
- **F28.2.11** budget_period 만료 처리 결정 wire = budget_period end 시 status=expired 자동 전환 + 다음 budget_period 자동 생성 (per-tenant 설정) + expired budget 의 audit log 보존 결정 wire.
- **F28.2.12** dry-run mode `--finops-budget-dry-run` CLI flag 결정 wire = budget definition 검증 + preview + actual INSERT skip + dry-run 결과 preview `phase_12_finops_budget_preview` table 결정 wire.

### F28.3 anomaly detection engine + alert routing (12 sub-ACs, A365 결정)

- **F28.3.1** `apps/api/modules/finops/anomaly_detection_engine.py` NEW ~+180 LOC + `detect_anomalies(tenant_id, period_key, dimension) -> List[AnomalyResult]` 결정 wire + 4 detection methods parallel run (z_score + IQR + EWMA + isolation_forest) + multi-method voting consensus (3 of 4 agree = anomaly confirmed) 결정 wire.
- **F28.3.2** AnomalyResult TypedDict 12 fields 결정 wire = (1) anomaly_id UUID PK + (2) tenant_id UUID + (3) period_key TEXT + (4) dimension enum + (5) dimension_value TEXT + (6) observed_value NUMERIC(20, 2) + (7) expected_value NUMERIC(20, 2) + (8) deviation_pct NUMERIC(8, 4) + (9) z_score NUMERIC(8, 4) + (10) severity enum warning/critical + (11) detection_method enum z_score/iqr/ewma/isolation_forest + (12) detected_at TIMESTAMPTZ + (13) trace_id TEXT + (14) confirmed_by_consensus BOOLEAN DEFAULT FALSE 결정 wire.
- **F28.3.3** false positive suppression 결정 wire = require 3 consecutive periods 동일 anomaly (anomaly_id + dimension + dimension_value 동일) + consecutive_periods >= 3 일 경우만 confirmed_anomaly INSERT 결정 wire + confirmed_anomaly = audit-first INSERT `anomaly_detected` CR 1-1 verbatim 결정 wire + 2-period warning 만 일 경우 = pending_anomaly status 로 보류 + audit log 미생성 결정 wire.
- **F28.3.4** Slack webhook integration `#bizup-finops-alerts` channel 결정 wire = AD-14 stack pin slack-sdk==3.23.0 + severity=warning → color=yellow + severity=critical → color=red + Slack message format 결정 wire (anomaly_id + tenant_name + dimension + dimension_value + observed_value + expected_value + deviation_pct + z_score + detection_method + consecutive_periods + trace_id link).
- **F28.3.5** PagerDuty integration `pd_anomaly_critical` service 결정 wire = AD-14 stack pin pdpyras==5.2.0 + severity=critical → PagerDuty incident open + owner-only manual resolve 결정 wire + PagerDuty incident_key = sha256:tenant_id+anomaly_id 결정 wire + dedup_key 동일 시 incident merge 결정 wire.
- **F28.3.6** alert routing 결정 wire = severity=warning → Slack only / severity=critical → Slack + PagerDuty / severity=exceeded → Slack + PagerDuty + Email 결정 wire + per-tenant override JSONB alert_routing_overrides TypedDict 결정 wire.
- **F28.3.7** audit-first INSERT `alert_sent` CR 1-1 verbatim 결정 wire = action_class='FINOPS_ANOMALY' + action='alert_sent' (NEW) + actor_id='system' + tenant_id + anomaly_id + severity + alert_channel enum slack/pagerduty/email + slack_message_ts + pagerduty_incident_id + trace_id 결정 wire + AuditAction Literal EXTENSION 1 NEW value 결정 wire.
- **F28.3.8** alert deduplication 결정 wire = 동일 anomaly_id 의 alert 은 1시간 이내 중복 발송 skip (slack_ts 동일) + 1시간 후 재평가 후 여전히 anomaly → 새 alert 발송 결정 wire + deduplication_window_per_tenant override JSONB 결정 wire.
- **F28.3.9** isolation forest model 결정 wire = sklearn==1.4.0 AD-14 stack pin + per-tenant model training (last 90d baseline) + model_version tracking + model retraining cron KST 매주 일요일 03:00 UTC 18:00 + model performance degradation detection (MAPE > 20% for 3 consecutive periods) → model retraining trigger 결정 wire.
- **F28.3.10** EWMA 모델 결정 wire = exponentially weighted moving average + λ 0.3 (default) + λ per-tenant override JSONB 결정 wire + EWMA prediction = current_value vs EWMA expected_value 차이 결정 wire + EWMA 시계열 데이터 source = phase_11_finops_showback table (Phase 11 wire 정합).
- **F28.3.11** z-score + IQR 모델 결정 wire = z-score = (observed - μ) / σ (μ + σ 는 last 90d baseline) + IQR = observed > Q3 + 1.5×IQR rule + 4 layer defense (baseline data integrity + outlier handling + NaN handling + infinity handling) 결정 wire.
- **F28.3.12** typed exception envelope CR 12-5 D-14 결정 wire = `AlertRoutingError(500)` + `AlertChannelUnavailableError(503)` + `SlackAPIError(502)` + `PagerDutyAPIError(502)` 4 NEW typed exception classes + envelope 결정 wire.

### F28.4 budget overrun alerts (12 sub-ACs, A366 결정)

- **F28.4.1** `apps/api/modules/finops/budget_alert.py` NEW ~+150 LOC + `check_budget_alerts(tenant_id, period_key) -> List[BudgetAlertResult]` 결정 wire + real-time consumption tracking (per-chargeback INSERT trigger) + threshold-based alerting (80% warning + 90% critical + 100% exceeded) 결정 wire.
- **F28.4.2** BudgetAlertResult TypedDict 10 fields 결정 wire = (1) alert_id UUID PK + (2) tenant_id UUID + (3) period_key TEXT + (4) budget_id UUID + (5) budget_amount NUMERIC(20, 2) + (6) actual_amount NUMERIC(20, 2) + (7) consumption_pct NUMERIC(8, 4) + (8) severity enum warning/critical/exceeded + (9) alert_channels List[str] + (10) sent_at TIMESTAMPTZ + (11) trace_id TEXT 결정 wire.
- **F28.4.3** real-time consumption tracking 결정 wire = phase_11_finops_chargeback table 의 chargeback INSERT trigger (Phase 11 wire 정합) → budget consumption 실시간 update → threshold 도달 시 즉시 alert 발송 결정 wire + consumption_update trigger 결정 wire + consumption_update lag < 5초 결정 wire.
- **F28.4.4** threshold-based alerting 결정 wire = 80% warning → Slack only / 90% critical → Slack + PagerDuty / 100% exceeded → Slack + PagerDuty + Email 결정 wire + per-tenant override JSONB threshold_overrides TypedDict 결정 wire.
- **F28.4.5** audit-first INSERT `budget_threshold_exceeded` CR 1-1 verbatim 결정 wire = action_class='FINOPS_BUDGET' + action='budget_threshold_exceeded' (NEW) + actor_id='system' + tenant_id + budget_id + consumption_pct + severity + trace_id 결정 wire + AuditAction Literal EXTENSION 1 NEW value + ActionClass.FINOPS_BUDGET entry EXTENSION 결정 wire.
- **F28.4.6** audit-first INSERT `budget_alert_sent` CR 1-1 verbatim 결정 wire = action_class='FINOPS_BUDGET' + action='budget_alert_sent' (NEW) + actor_id='system' + tenant_id + budget_id + alert_channels + slack_ts + trace_id 결정 wire + AuditAction Literal EXTENSION 1 NEW value 결정 wire.
- **F28.4.7** alert deduplication 결정 wire = 동일 budget_id 의 동일 severity alert 은 24시간 이내 중복 발송 skip + 24시간 후 consumption 재평가 → 여전히 threshold 도달 시 새 alert 발송 결정 wire + consumption 증가분 5% 미만 시 alert skip 결정 wire.
- **F28.4.8** budget overrun prediction 결정 wire = linear extrapolation 기반 overrun prediction = (current_consumption / days_elapsed) × total_period_days vs budget_amount 비교 + predicted_overrun_pct 계산 + prediction 결과 preview 결정 wire + predicted_overrun_pct > 110% → warning alert 발송 결정 wire.
- **F28.4.9** Slack webhook integration `#bizup-finops-budget-alerts` channel 결정 wire = AD-14 stack pin slack-sdk==3.23.0 + severity=warning → color=yellow + severity=critical → color=orange + severity=exceeded → color=red + Slack message format 결정 wire.
- **F28.4.10** PagerDuty integration `pd_budget_exceeded` service 결정 wire = AD-14 stack pin pdpyras==5.2.0 + severity=exceeded → PagerDuty incident open + owner-only manual resolve 결정 wire.
- **F28.4.11** email notification 결정 wire = sendgrid==6.11.0 AD-14 stack pin + severity=exceeded → owner email 발송 + email template 결정 wire + NFR4 PII minimization 정합 결정 wire (tenant_id + budget_id 만 포함, personal data 제외).
- **F28.4.12** typed exception envelope CR 12-5 D-14 결정 wire = `BudgetAlertError(500)` + `BudgetConsumptionUpdateError(500)` 2 NEW typed exception classes + envelope 결정 wire.

### F28.5 forecast accuracy tracking (10 sub-ACs, A367 결정)

- **F28.5.1** `apps/api/modules/finops/forecast_accuracy.py` NEW ~+120 LOC + `track_forecast_deviation(tenant_id, period_key) -> ForecastAccuracyReport` 결정 wire + predicted vs actual 비교 + MAE + MAPE + RMSE 3 metrics 계산 결정 wire.
- **F28.5.2** ForecastAccuracyReport TypedDict 8 fields 결정 wire = (1) tenant_id UUID + (2) period_key TEXT + (3) predicted_amount NUMERIC(20, 2) + (4) actual_amount NUMERIC(20, 2) + (5) deviation_amount NUMERIC(20, 2) + (6) deviation_pct NUMERIC(8, 4) + (7) accuracy_score NUMERIC(8, 4) (100 - MAPE) + (8) model_version TEXT + (9) trace_id TEXT 결정 wire.
- **F28.5.3** MAE (Mean Absolute Error) 결정 wire = (1/n) × Σ|predicted - actual| 결정 wire + MAE 단위 KRW 결정 wire.
- **F28.5.4** MAPE (Mean Absolute Percentage Error) 결정 wire = (100/n) × Σ|predicted - actual| / actual × 100 결정 wire + MAPE 단위 % 결정 wire + MAPE < 10% = high accuracy / 10~20% = moderate / > 20% = low accuracy 결정 wire.
- **F28.5.5** RMSE (Root Mean Squared Error) 결정 wire = √((1/n) × Σ(predicted - actual)²) 결정 wire + RMSE 단위 KRW 결정 wire.
- **F28.5.6** model performance degradation detection 결정 wire = MAPE > 20% for 3 consecutive periods → model retraining trigger + model_retraining_alert audit-first INSERT `model_retraining_triggered` CR 1-1 verbatim + AlertClass.FINOPS_ANOMALY 1 NEW action 결정 wire.
- **F28.5.7** audit-first INSERT `forecast_deviation` CR 1-1 verbatim 결정 wire = action_class='FINOPS_ANOMALY' + action='forecast_deviation' (NEW) + actor_id='system' + tenant_id + period_key + predicted_amount + actual_amount + deviation_pct + accuracy_score + model_version + trace_id 결정 wire + AuditAction Literal EXTENSION 1 NEW value 결정 wire.
- **F28.5.8** predicted_amount source 결정 wire = phase_11_finops_showback table 의 showback_generated event 의 predicted_amount column (Phase 11 wire EXTENSION) + actual_amount source = phase_11_finops_chargeback table 의 chargeback_calculated event 의 actual_amount column (Phase 11 wire 정합) 결정 wire.
- **F28.5.9** model_version tracking 결정 wire = isolation_forest model version (semantic versioning MAJOR.MINOR.PATCH) + model_version JSONB metadata (training_date + training_data_window + training_samples_count) 결정 wire.
- **F28.5.10** typed exception envelope CR 12-5 D-14 결정 wire = `ForecastAccuracyError(500)` + `ModelRetrainingError(500)` 2 NEW typed exception classes + envelope 결정 wire.

### F28.6 anomaly detection dashboard UI (10 sub-ACs, A368 결정)

- **F28.6.1** `apps/web/app/[locale]/(dashboard)/admin/finops/anomaly/page.tsx` NEW ~+150 LOC + 4 components (`AnomalyDetectionChart` + `AnomalyDetectionMethodSelector` + `AnomalyDetectionThresholdSlider` + `BudgetAlertPanel`) 결정 wire + owner-only RBAC AD-22 verbatim + Epic 12 2FA 챌린지 보존 결정 wire.
- **F28.6.2** AnomalyDetectionChart component 결정 wire = 5 dimension 별 anomaly 시각화 (department + cost_center + product_line + service + tenant_total) + line chart (last 90d baseline + current period + expected range + observed value) + Recharts 2.12.7 AD-14 stack pin 결정 wire.
- **F28.6.3** AnomalyDetectionMethodSelector component 결정 wire = 4 detection methods radio button (z_score + IQR + EWMA + isolation_forest) + multi-method voting consensus preview 결정 wire + owner-only ack prompt AD-22 verbatim 결정 wire.
- **F28.6.4** AnomalyDetectionThresholdSlider component 결정 wire = z_score threshold slider (1.0 ~ 5.0, default 3.0) + IQR multiplier slider (1.0 ~ 3.0, default 1.5) + EWMA λ slider (0.1 ~ 0.9, default 0.3) + isolation_forest contamination slider (0.01 ~ 0.3, default 0.1) 결정 wire + RTL render discipline CR 11-4 D-003 verbatim 결정 wire.
- **F28.6.5** BudgetAlertPanel component 결정 wire = budget 정의 CRUD + 실시간 consumption 시각화 + alert threshold 설정 + alert history (last 30d) 결정 wire.
- **F28.6.6** ko-KR.json `finops_anomaly.*` namespace EXTENSION ~25 keys 결정 wire (CR 11-4 D-002 verbatim SSOT) + `finops_anomaly.detection_method.z_score` + `finops_anomaly.detection_method.iqr` + `finops_anomaly.detection_method.ewma` + `finops_anomaly.detection_method.isolation_forest` + `finops_anomaly.severity.warning` + `finops_anomaly.severity.critical` + `finops_anomaly.severity.exceeded` 등 결정 wire.
- **F28.6.7** ARIA labels 결정 wire = WCAG 2.1 AA compliance (Epic 12 의 2FA 챌린지 UI 와 동일 표준) + keyboard navigation (Tab + Enter + Arrow keys) + screen reader 지원 결정 wire + Phase 12 Epic 1 UX v1.0 locked decision 결정 wire (Dark MVP / WCAG AA / Professional / ko-KR) verbatim 보존 결정 wire.
- **F28.6.8** toast notification 결정 wire = anomaly detected 시 toast 자동 표시 (severity=warning → yellow + 5초 / severity=critical → red + 10초) + alert sent toast (slack + pagerduty channel 정보 표시) 결정 wire.
- **F28.6.9** Vitest RTL render discipline CR 11-4 D-003 verbatim 결정 wire = 5 NEW vitest cases (AnomalyDetectionChart 1 + AnomalyDetectionMethodSelector 1 + AnomalyDetectionThresholdSlider 1 + BudgetAlertPanel 1 + ko-KR SSOT 1) 결정 wire.
- **F28.6.10** FinOps anomaly dashboard parity CR 12-5 D-PARITY-01 결정 wire = TS mirror `apps/web/lib/finops-anomaly/finops-anomaly-client.ts` NEW ~+150 LOC + `tests/web/test_finops_anomaly_dashboard_parity.py` NEW ~+10 cases + 1-line ko-KR reject 결정 wire.

### F28.7 Capability matrix v1.37 EXTENSION FINOPS_ANOMALY_DETECTION + FINOPS_BUDGET_ALERT (12 sub-ACs, A369 결정)

- **F28.7.1** capability matrix v1.36 → v1.37 EXTENSION 결정 wire = FINOPS_ANOMALY_DETECTION + FINOPS_BUDGET_ALERT 2 NEW rows + docs/capability-matrix.md MODIFIED + version 표기 결정 wire.
- **F28.7.2** `Capability.FINOPS_ANOMALY_DETECTION = "finops_anomaly_detection"` 1 NEW enum 결정 wire + `Capability.FINOPS_BUDGET_ALERT = "finops_budget_alert"` 1 NEW enum 결정 wire + apps/api/core/capability.py MODIFIED + CR 12-1 L4 precedent 미러 결정 wire.
- **F28.7.3** `apps/api/dependencies/capability.py` MODIFIED 결정 wire = `require_finops_anomaly_detection` + `require_finops_budget_alert` 2 NEW deps + Phase 11 wire `e020ad0` 의 `require_finops_showback` + `require_finops_chargeback` pattern verbatim 미러 결정 wire.
- **F28.7.4** industry-agnostic 4-industry grants ✅/✅/✅/✅ 결정 wire = manufacturing + service + manufacturing_service + manufacturing_service_other 모두 허용 (CR 12-1 L4 precedent 미러, FINOPS_SHOWBACK + FINOPS_CHARGEBACK Phase 11 wire pattern verbatim bind) 결정 wire.
- **F28.7.5** 미허용 tenant 의 anomaly detection 진입 차단 결정 wire = `require_finops_anomaly_detection` dependency 의 미허용 tenant 진입 차단 검증 + 403 Forbidden + `FORBIDDEN_KO` message ("FinOps Anomaly Detection capability 미허용 tenant") + Epic 12 2FA 챌린지 보존 + AD-22 owner-only RBAC 정합 결정 wire.
- **F28.7.6** 미허용 tenant 의 budget alert 진입 차단 결정 wire = `require_finops_budget_alert` dependency 의 미허용 tenant 진입 차단 검증 + 403 Forbidden + `FORBIDDEN_KO` message ("FinOps Budget Alert capability 미허용 tenant") + Epic 12 2FA 챌린지 보존 + AD-22 owner-only RBAC 정합 결정 wire.
- **F28.7.7** drift detector `tests/integration/test_capability_matrix_v1_37_drift.py` NEW 결정 wire = 10 NEW pytest cases (FINOPS_ANOMALY_DETECTION + FINOPS_BUDGET_ALERT row 존재 2 + 4-industry grants 2 + Capability enum 2 + require_finops_anomaly_detection + require_finops_budget_alert 2 + 미허용 tenant 차단 2) 결정 wire + Phase 11 wire `e020ad0` 의 `tests/integration/test_capability_matrix_v1_36_drift.py` 패턴 verbatim 미러 결정 wire.
- **F28.7.8** m20_finops_anomaly 결정 wire = `apps/api/modules/finops/anomaly/__init__.py` NEW 결정 wire + `apps/api/modules/finops/anomaly/serializers.py` NEW 결정 wire (m19_finops 의 Phase 11 wire EXTENSION pattern verbatim 미러) 결정 wire + module version 결정 wire (m20_finops_anomaly 결정 wire + Phase 11 wire 의 m19_finops 와 동일 pattern).
- **F28.7.9** SSOT RED→GREEN EXTENSION 결정 wire = capability matrix v1.37 신규 2 rows + capability.py EXTENSION 2 NEW enum + require_finops_anomaly_detection + require_finops_budget_alert 2 NEW deps + drift detector + A36 SDR 검증 4-step 자동 적용 + CR 12-5 D-GATE-01 inversion 적용 보존 결정 wire.
- **F28.7.10** CR 12-1 L4 industry-agnostic capability 결정 wire = FINOPS_ANOMALY_DETECTION + FINOPS_BUDGET_ALERT 모두 industry-agnostic 4-industry grants ✅/✅/✅/✅ 결정 wire (manufacturing + service + manufacturing_service + manufacturing_service_other 모두 허용) + Phase 11 wire 의 FINOPS_SHOWBACK + FINOPS_CHARGEBACK industry-agnostic 4-industry grants pattern verbatim 미러 결정 wire.
- **F28.7.11** capability gate 의 fail-closed 결정 wire = 미허용 tenant 의 anomaly detection / budget alert 진입 시 403 Forbidden 결정 wire + capability matrix v1.37 row 부재 시 fail-closed 결정 wire + `Capability.FINOPS_ANOMALY_DETECTION` + `Capability.FINOPS_BUDGET_ALERT` enum 부재 시 fail-closed 결정 wire + AD-22 owner-only RBAC 정합 + Epic 12 2FA 챌린지 보존 + NFR4 PII minimization 정합 보존 결정 wire.
- **F28.7.12** capability matrix 의 version 결정 wire = v1.36 → v1.37 EXTENSION 결정 wire + version bump 일관성 결정 wire (Phase 11 wire 의 capability matrix v1.35 → v1.36 pattern verbatim 미러) + docs/capability-matrix.md 의 version 표기 결정 wire + sprint-status.yaml 의 capability version 결정 wire + drift detector 의 version 매칭 검증 결정 wire + CR 12-5 D-PARITY-01 inversion 적용 보존 결정 wire.

### F28.8 dry-run + Tests + wire scope T1~T8 (12 sub-ACs)

- **F28.8.1** dry-run mode 결정 wire = `--finops-anomaly-dry-run` + `--finops-budget-dry-run` CLI flags + anomaly detection dry-run parameter + budget alert dry-run parameter + dry-run 시 actual `anomaly_detected` audit-first INSERT skip + dry-run 시 actual `budget_threshold_exceeded` audit-first INSERT skip + dry-run 시 actual alert 발송 skip 결정 wire + dry-run 결과 preview 결정 wire (anomaly_preview + budget_preview 테이블 alembic 0044 신규 결정 wire).
- **F28.8.2** dry-run 의 preview 결과 결정 wire = `phase_12_finops_anomaly_preview` + `phase_12_finops_budget_preview` table 결정 wire (preview_id UUID PK + tenant_id UUID + period_key TEXT + preview_data JSONB + computed_at TIMESTAMPTZ DEFAULT NOW() + trace_id TEXT) + RLS 자동 적용 CR 0-2 verbatim 결정 wire + audit-first INSERT `anomaly_dry_run_executed` + `budget_dry_run_executed` CR 1-1 verbatim 결정 wire.
- **F28.8.3** dry-run 의 CLI flag 결정 wire = `--finops-anomaly-dry-run` + `--finops-budget-dry-run` global flag + `--anomaly-detection-dry-run` + `--budget-alert-dry-run` sub-flags 결정 wire + dry-run 모드 시 logger 출력 결정 (stdout + INFO level) + dry-run 결과 preview 결정 wire (console table output 결정 wire).
- **F28.8.4** tests 결정 wire = ~+50 NEW pytest PASS (anomaly_detection 6 + anomaly_detection_engine 7 + budget_definition 5 + budget_alert 6 + forecast_accuracy 4 + alembic 0044 4 + audit action 8 + capability matrix v1.37 10 = ~+50 NEW pytest PASS) 결정 wire + anomaly_detection 6 tests 결정 (parse_anomaly_definition validation 3 + multi-method voting consensus 2 + audit-first INSERT 1) + anomaly_detection_engine 7 tests 결정 (z_score + IQR + EWMA + isolation_forest 4 + Slack + PagerDuty alert routing 2 + false positive suppression 1) + budget_definition 5 tests 결정 (parse_budget_definition 2 + validate_budget_scope 2 + audit-first INSERT 1) + budget_alert 6 tests 결정 (real-time consumption tracking 2 + threshold-based alerting 2 + Slack + PagerDuty + Email routing 1 + audit-first INSERT 1) + forecast_accuracy 4 tests 결정 (MAE + MAPE + RMSE 3 + model_retraining_trigger 1) + alembic 0044 4 tests 결정 (migration + RLS + CHECK constraints + UNIQUE constraint 4) + audit action 8 tests 결정 (anomaly_detected + budget_definition_updated + alert_sent + budget_threshold_exceeded + budget_alert_sent + forecast_deviation + anomaly_dry_run_executed + budget_dry_run_executed 8 NEW actions + _ActionRegistry FINOPS_ANOMALY + FINOPS_BUDGET entries 2 NEW 등록 + multi-tenant isolation 2) + capability matrix v1.37 10 tests 결정 (FINOPS_ANOMALY_DETECTION + FINOPS_BUDGET_ALERT row 존재 2 + 4-industry grants 2 + Capability enum 2 + require_finops_anomaly_detection + require_finops_budget_alert 2 + 미허용 tenant 차단 2) 결정 wire.
- **F28.8.5** vitest tests 결정 wire = ~+5 NEW vitest PASS (AnomalyDetectionChart + AnomalyDetectionMethodSelector owner-only ack prompt AD-22 verbatim + AnomalyDetectionThresholdSlider RTL render + BudgetAlertPanel + ko-KR SSOT 2 + finops anomaly dashboard parity CR 12-5 D-PARITY-01 결정 wire) 결정 wire + Phase 11 wire 의 ~5 NEW vitest PASS pattern verbatim 미러 결정 wire.
- **F28.8.6** ruff + tsc 결정 wire = 0 NEW ruff 결정 wire (Phase 11 wire 정합) + 0 NEW tsc 결정 wire (apps/web frontend 변경 ext only — pre-existing baseline errors preserved per cj-style discipline) + SDR drift gate 결정 wire (vitest file count +1 NEW collected well within 5% tolerance + pytest +8 NEW files collected well within 5% tolerance) 결정 wire + commit_consistency gate 결정 wire (CR 9-6 commit message discipline + A36 SDR 검증 4-step 자동 적용) 결정 wire + D-DEFER-* grep guard 결정 wire (CR 11-3 honest-DEFER discipline 109번째 epic 연속 정직 회복 검증 보존) 결정 wire.
- **F28.8.7** wire scope T1~T8 결정 wire = T1 anomaly_detection + anomaly_detection_dsl module 결정 wire + T2 budget_definition + budget_definition_dsl module 결정 wire + T3 anomaly_detection_engine + alert routing 결정 wire + T4 budget_alert + forecast_accuracy module 결정 wire + T5 alembic 0044 phase_12_anomaly_detection 결정 wire + T6 audit action EXTENSION 4 NEW 결정 wire + T7 capability v1.37 EXTENSION + frontend finops anomaly dashboard 결정 wire + T8 3중 게이트 FINAL CLEAN atomic commit 결정 wire + estimated ~+30-35 files = ~+18 NEW + ~+12 MODIFIED 결정 wire + Phase 11 wire 의 wire_commit pattern verbatim 미러 결정 wire.
- **F28.8.8** A19 cohesion pattern 9 surface EXTENSION PASS 결정 wire (FinOps anomaly detection + budget alert surface NEW = F28.1~F28.8 결정 wire) 결정 wire + Phase 11 wire 의 FinOps showback/chargeback surface NEW = F27.1~F27.7 pattern verbatim 미러 결정 wire + Phase 10 wire 의 SLO engineering surface NEW = F26.1~F26.7 pattern verbatim 미러 결정 wire.
- **F28.8.9** CR lessons applied 14종 결정 wire (CR 0-2 RLS lesson ✅ APPLIED + CR 1-1 audit-first INSERT ✅ APPLIED (4 NEW audit log entries `anomaly_detected` + `budget_definition_updated` + `alert_sent` + `budget_threshold_exceeded` + 2 dry-run actions + forecast_deviation = 7 NEW) + CR 4-3/4-4 lessons carry ✅ APPLIED (anomaly + budget baseline + golden_diff pattern verbatim) + CR 1-1 ContextVar lesson ✅ APPLIED + CR 1-1 RSC boundary lesson ✅ APPLIED + CR 9-6 commit message discipline ✅ APPLIED (`git commit -F <file>` 사용, PowerShell here-string 회피, D5 prevention) + CR 11-3 honest-DEFER discipline ✅ APPLIED (109번째 epic 연속 정직 회복) + CR 11-4 D-001~D-005 + P-015 ✅ APPLIED + CR 12-1 L4 ✅ APPLIED (FINOPS_ANOMALY_DETECTION + FINOPS_BUDGET_ALERT industry-agnostic 4-industry grants ✅/✅/✅/✅) + CR 12-5 D-14 typed exception envelope ✅ APPLIED (5 NEW typed exception classes `AnomalyDefinitionInvalidError` + `AnomalyDetectionError` + `AlertRoutingError` + `AlertChannelUnavailableError` + `SlackAPIError` + `PagerDutyAPIError` + `BudgetDefinitionInvalidError` + `BudgetScopeInvalidError` + `BudgetAmountInvalidError` + `BudgetAlertError` + `BudgetConsumptionUpdateError` + `ForecastAccuracyError` + `ModelRetrainingError`) + CR 12-5 D-PARITY-01 inversion ✅ APPLIED + CR 12-5 D-GATE-01 inversion ✅ APPLIED + A36 SDR 검증 4-step 자동 적용 ✅ + AD-14 stack pin ✅ APPLIED (sklearn==1.4.0 + slack-sdk==3.23.0 + pdpyras==5.2.0 + sendgrid==6.11.0 + Recharts 2.12.7) + AD-22 owner-only RBAC ✅ APPLIED (anomaly detection + budget alert 모두 owner-only RBAC AD-22 + Epic 12 2FA 챌린지 보존) + NFR4 PII minimization ✅ PRESERVED) 결정 wire.
- **F28.8.10** D-DEFER-* honestly 결정 wire (CR 11-3 109번째 epic 연속 정직 회복 결정 wire 보존) = D-1-1-DEFER-1/2/3 RESOLVED 보존 60~109번째 + D-LAUNCH-1-DEFER-1 honestly preserved 65~109번째 + D-EPIC-16-REVIEW-DEFER-2~6 ✅ RESOLVED 보존 78~109번째 + D-PHASE-4-DR-DEFER-1/2 ✅ RESOLVED 보존 73~109번째 + D-EPIC-17-WIRE-DEFER-T2-T3-UI ✅ RESOLVED 보존 83~109번째 + D-RETENTION-1 ✅ RESOLVED 보존 85~109번째 + D-OBSERVABILITY-1 ✅ RESOLVED 보존 89~109번째 + D-PERFORMANCE-1 ✅ RESOLVED 보존 93~109번째 + D-CHAOS-1 ✅ RESOLVED 보존 100~109번째 + D-SLO-1 ✅ RESOLVED 보존 101~109번째 + D-FINOPS-1 ✅ RESOLVED 보존 105~109번째 + **D-FINOPS-2 신규 honestly DEFER 보존 = Phase 12 PRD entry 진입 시점에 carry-over chain 정직 회복 결정 wire 진입: Phase 11 close-out retro `80df15b` §12 + Phase 10 close-out retro `733d428` §10 + Phase 9 close-out retro `634427d` §10 + Phase 8 close-out retro `ab495a8` §10 + Phase 7 close-out retro `326fa9f` §10 + Phase 6 close-out retro `f9f006c` §13 + Epic 17 close-out retro `be8f3bd` §11 "Cost Anomaly Detection & Budget Alerting 결정 wire 보류, Phase 12+ 진입 시점" verbatim 해소** 결정 wire.
- **F28.8.11** Epic 1 ~ Epic 17 + Phase 3 ~ Phase 11 + 1st release cycle 정합 보존 결정 wire (cj-style 109번째 epic 연속 정직 회복 pre-flight 정합 sweep 결정 wire 보존): ✅ Phase 11 close-out retro `80df15b` (cj-style 108번째) + ✅ Phase 11 atomic wire T1~T8 `e020ad0` (cj-style 107번째) + ✅ Phase 11 spec entry `82c93a8` (cj-style 106번째) + ✅ Phase 11 PRD entry `16d7698` (cj-style 105번째) + ✅ Phase 10 close-out retro `733d428` (cj-style 104번째) + ✅ Phase 10 atomic wire T1~T8 `ac5d6c5` (cj-style 103번째) + ✅ Phase 10 spec entry `3c80ef0` (cj-style 102번째) + ✅ Phase 10 PRD entry `09db4d4` (cj-style 101번째) + ✅ Phase 9 close-out retro `634427d` (cj-style 100번째) + ✅ Phase 9 atomic wire T1~T8 `e7670e1` (cj-style 99번째) + ✅ Phase 9 spec entry `2a5e4da` (cj-style 98번째) + ✅ Phase 9 PRD entry `0b2d2f3` (cj-style 97번째) + ✅ Phase 8 close-out retro `ab495a8` (cj-style 96번째) + ✅ Phase 8 atomic wire T1~T8 `60d4ea1` (cj-style 95번째) + ✅ Phase 8 spec entry `5ae0f4e` (cj-style 94번째) + ✅ Phase 8 PRD entry `ced452f` (cj-style 93번째) + ✅ Build fixes sprint `eaee198` + ✅ Phase 7 close-out retro `326fa9f` (cj-style 92번째) + ✅ Phase 7 atomic wire T1~T8 `59b56cd` (cj-style 91번째) + ✅ Phase 7 spec entry `749381e` (cj-style 90번째) + ✅ Phase 7 PRD entry `916a541` (cj-style 89번째) + ✅ Phase 6 close-out retro `f9f006c` (cj-style 88번째) + ✅ Phase 6 atomic wire T1~T8 `24e1cd7` (cj-style 87번째) + ✅ Phase 6 spec entry `f5c14c9` (cj-style 86번째) + ✅ Phase 6 PRD entry `e84a281` (cj-style 85번째) + ✅ Epic 17 close-out retro `be8f3bd` (cj-style 84번째) + ✅ Epic 17 T2+T3 UI wire `bb92879` (cj-style 83번째) + ✅ Epic 17 wire `2ada2ec` (cj-style 82번째) + ✅ Epic 17 spec entry `f4b2b58` (cj-style 81번째) + ✅ Epic 17 PRD entry `40a9c41` (cj-style 80번째) + ✅ Sidebar/MenuProvider hot-fix `01a06e4` (cj-style 79번째) + ✅ D-EPIC-16-REVIEW-DEFER-2~6 RESOLVE sprint `512ed6a` (cj-style 78번째) + ✅ Phase 5 close-out retro `b843565` (cj-style 76~77번째) + ✅ Phase 5 wire `f093f8c` (cj-style 75번째) + ✅ Phase 5 spec entry (cj-style 74번째) + ✅ Phase 5 PRD entry `93d852b` (cj-style 73번째) + ✅ Epic 16 close-out retro (cj-style 72번째) + ✅ Epic 16 T4 admin UI follow-up `ff5c3b5` (cj-style 71번째) + ✅ Epic 16 review follow-up `963079c` (cj-style 70번째) + ✅ Epic 16 atomic wire `e117e09` (cj-style 69번째) + ✅ Epic 16 PRD entry `08bfca5` (cj-style 67번째) + ✅ 1st release cycle cj-style 62~66번째 모두 wire DONE 진입 + ✅ Epic 15 cycle cj-style 58~61번째 모두 wire DONE 진입 + ✅ Phase 4 cycle cj-style 53~57번째 모두 wire DONE 진입 + ✅ Phase 3 cycle cj-style 49~52번째 모두 wire DONE 진입 + ✅ Epic 14 LISTEN/NOTIFY multi-process `7835463` + ✅ Epic 13 LISTEN/NOTIFY consume `f2ea2f6` + ✅ Epic 12 2FA 게이트 `a63646c` (anomaly detection + budget alert 모두 owner-only RBAC AD-22 + Epic 12 2FA 챌린지 보존) + ✅ Epic 11 close-out retro + Phase 2 close-out baseline 599 passed 정합 보존 + ✅ Epic 1 carry-over (auth) layout + onboarding/industry 보존 + ✅ Epic 7~10 ABC/TDABC + AI 인사이트 territory 결정 wire 보존 결정 wire.
- **F28.8.12** partial wire 시도 0건 + single sprint atomic docs-only wire 1 진입점 결정 wire (cj-style 109번째 epic 연속 정직 회복 Phase 12 PRD entry atomic docs-only wire 6 files atomic single sprint 결정 wire) + 결정 wire 일자: 2026-08-24 (KST) + **next**: 옵션 (a) Phase 12 bmad-create-story spec entry 진입 (cj-style 110번째 wire 진입 시점) / 옵션 (b) Phase 12 bmad-dev-story atomic wire T1~T8 진입 (cj-style 111번째) / 옵션 (c) Phase 12 close-out retro 진입 (cj-style 112번째) / 옵션 (d) Epic 18+ 진입 / 옵션 (e) D-DEFER-* follow-up 결정 wire 보류 결정 wire.

---

# F29. Phase 13 territory (옵션 (a) Phase 13+ 진입 결정 wire, cj-style 113번째 epic 연속 정직 회복)

## F29. FinOps Forecasting & Capacity Planning (Phase 13 territory 신규)

> **§F29 결정 wire 진입** — Phase 12 close-out retro `3354e83` (cj-style 112번째 wire entry) 직후 다음 옵션 5종 중 **사용자 권장 결정 = 옵션 (a) Phase 13+ 진입 + 옵션 (a) FinOps Forecasting & Capacity Planning (Recommended) (forecast definition DSL + cost forecasting engine time series 4 models ARIMA + Prophet + LSTM + ensemble + capacity headroom analysis compute + storage + network + budget burn-rate projection + forecast accuracy tracking + model retraining trigger + forecasting dashboard UI + audit-first INSERT 7 NEW + Capability FINOPS_FORECASTING_CAPACITY_PLANNING 1 NEW + dry-run mode + tests + wire scope T1~T8) = Phase 13 territory 결정 wire 진입** (AskUserQuestion 결정 verbatim bind). rationale 5종: ① Phase 12 wire `f3c0e63` 의 Cost Anomaly Detection & Budget Alerting territory (anomaly detection DSL 4 methods z-score + IQR + EWMA + isolation forest + budget definition DSL + anomaly detection engine + alert routing Slack + PagerDuty + Email + budget overrun alerts + forecast accuracy tracking MAE + MAPE + RMSE + model_retraining_triggered + Capability FINOPS_ANOMALY_DETECTION + FINOPS_BUDGET_ALERT 2 NEW + dry-run mode) 의 natural backend carry-over chain = Phase 13 = FinOps Forecasting & Capacity Planning territory 의 natural next (anomaly detection 의 historical baseline (last 30d + last 90d + YTD) → forward forecast EXTENSION (12-month prediction with 95% CI) + budget overrun 의 linear extrapolation → ARIMA/Prophet/LSTM time series forecast + forecast accuracy 의 MAE/MAPE/RMSE → forward-looking forecasting engine + capacity planning 의 compute/storage/network saturation forecast (90일 lookahead) + headroom analysis territory 의 natural next) = Phase 12 FinOps territory 의 FORECASTING & PLANNING LAYER EXTENSION 결정 ② cj-style discipline 회피 위험 방지 (49~112번째 누적 64-entry-point cycle + 112번째 Phase 12 close-out retro + 111번째 atomic wire + 110번째 spec entry + 109번째 PRD entry 패턴 모두 wire DONE 진입 정합 보존 후 즉시 Phase 13 진입 = 1-day atomic sprint discipline) ③ 비즈니스 우선순위 = enterprise 고객 onboarding 시 forward-looking 비용 forecast (12-month prediction with 95% confidence interval) + capacity headroom analysis (compute/storage saturation 90일 lookahead) + budget burn-rate projection (Phase 12 budget_alert 의 linear extrapolation EXTENSION) territory 필수 ④ Phase 12 wire `f3c0e63` 의 anomaly detection baseline (last 30d + last 90d + YTD) + forecast accuracy tracking (MAE + MAPE + RMSE + model_retraining_trigger) + Phase 11 wire `e020ad0` showback period selector (current month + previous month + last 3/6 months + YTD + custom range) + Phase 8 wire `60d4ea1` cost-engine 12-period benchmark 의 자연스러운 carry-over chain = Phase 13 forecasting 의 historical baseline (last 12-month time series for ARIMA/Prophet training) + 12-month forward forecast (95% CI) + capacity headroom analysis (90일 lookahead) + model retraining cron (Phase 12 carry-over pattern verbatim) EXTENSION 정합 ⑤ 1st release close-out retro + Epic 17 close-out retro + Phase 5 close-out retro + Phase 6 close-out retro + Phase 7 close-out retro + Phase 8 close-out retro + Phase 9 close-out retro + Phase 10 close-out retro + Phase 11 close-out retro + Phase 12 close-out retro territory verbatim 보존 + AD-22 owner-only RBAC + Epic 12 2FA 챌린지 보존 + AD-14 stack pin + NFR4 PII minimization ✅ PRESERVED + AD-40 FinOps Forecasting & Capacity Planning 신규 결정 ((a)~(g) 7 sub-decisions 모두 결정 wire 진입). §F29 territory = FinOps Forecasting & Capacity Planning 결정 wire 진입 시점에 8 ACs (§F29.1 forecast definition DSL + §F29.2 cost forecasting engine + time series models + §F29.3 capacity headroom analysis + §F29.4 budget burn-rate projection + §F29.5 forecast accuracy tracking + model retraining trigger + §F29.6 forecasting dashboard UI + §F29.7 Capability matrix v1.38 EXTENSION + §F29.8 dry-run + tests + wire scope T1~T8) + §8.1 M0-(v) FinOps Forecasting & Capacity Planning AC 신규 + §15 로드맵 Phase 13 row status 백로그 → in-progress (PRD entry DONE 진입 wire) + §부록 A A394~A398 신규 결정 표 + AD-40 FinOps Forecasting & Capacity Planning 신규 결정 + capability matrix v1.37 → v1.38 EXTENSION FINOPS_FORECASTING_CAPACITY_PLANNING 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅ (CR 12-1 L4 precedent 미러, FINOPS_ANOMALY_DETECTION + FINOPS_BUDGET_ALERT Phase 12 wire + FINOPS_SHOWBACK + FINOPS_CHARGEBACK Phase 11 wire + SLO_ENGINEERING Phase 10 wire + CHAOS_ENGINEERING Phase 9 wire + PERFORMANCE_TESTING Phase 8 wire + OBSERVABILITY_TRACES + OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind). CR 11-3 honest-DEFER discipline 113번째 epic 연속 정직 회복 검증 ✅ (D-1-1-DEFER-1/2/3 RESOLVED 보존 60~113번째 + D-LAUNCH-1-DEFER-1 honestly preserved 65~113번째 + D-EPIC-16-REVIEW-DEFER-2~6 ✅ RESOLVED 보존 78~113번째 + D-PHASE-4-DR-DEFER-1/2 ✅ RESOLVED 보존 73~113번째 + D-EPIC-17-WIRE-DEFER-T2-T3-UI ✅ RESOLVED 보존 83~113번째 + D-RETENTION-1 ✅ RESOLVED 보존 85~113번째 + D-OBSERVABILITY-1 ✅ RESOLVED 보존 89~113번째 + D-PERFORMANCE-1 ✅ RESOLVED 보존 93~113번째 + D-CHAOS-1 ✅ RESOLVED 보존 100~113번째 + D-SLO-1 ✅ RESOLVED 보존 101~113번째 + D-FINOPS-1 ✅ RESOLVED 보존 105~113번째 + D-FINOPS-2 ✅ RESOLVED 보존 109~113번째 + **D-FINOPS-3 신규 honestly DEFER 보존 = Phase 13 PRD entry 진입 시점에 carry-over chain 정직 회복 결정 wire 진입: Phase 12 close-out retro `3354e83` §13 + Phase 11 close-out retro `80df15b` §12 + Phase 10 close-out retro `733d428` §10 + Phase 9 close-out retro `634427d` §10 + Phase 8 close-out retro `ab495a8` §10 + Phase 7 close-out retro `326fa9f` §10 + Phase 6 close-out retro `f9f006c` §13 + Epic 17 close-out retro `be8f3bd` §11 "FinOps Forecasting & Capacity Planning 결정 wire 보류, Phase 13+ 진입 시점" verbatim 해소**).

### F29.1 forecast definition DSL (12 sub-ACs, A363 결정)

- **F29.1.1** forecast definition DSL syntax 결정 wire = `define_forecast(tenant_id, target_metric, horizon_months, model_type, confidence_level)` builder + AST 5 levels (tenant_id + target_metric + horizon_months + model_type + confidence_level) + parser 검증 3 layer (syntax + semantic + tenant-scope RLS) 결정 wire.
- **F29.1.2** 5 target_metric 옵션 결정 wire = (1) department (department_id 별 forecast) / (2) cost_center (cost_center_id 별 forecast) / (3) product_line (product_line_id 별 forecast) / (4) service (service_id 별 forecast) / (5) tenant_total (tenant 전체 aggregated forecast) 결정 wire + target_metric validation 결정 wire (tenant_settings.cost_center_mapping JSONB 기반 + Phase 11 wire `e020ad0` department_mapping + Phase 12 wire `f3c0e63` anomaly_detection dimension EXTENSION 정합).
- **F29.1.3** horizon_months enum 결정 wire = (1) 3m (3-month forecast) / (2) 6m (6-month forecast) / (3) 12m (12-month forecast) / (4) 24m (24-month forecast) 결정 wire + horizon_months default 12m + per-tenant override JSONB 결정 wire.
- **F29.1.4** 4 model_type 옵션 결정 wire = (1) ARIMA (autoregressive integrated moving average, p=2 + d=1 + q=2 default) / (2) Prophet (Facebook Prophet, yearly_seasonality=True + weekly_seasonality=True + daily_seasonality=False) / (3) LSTM (long short-term memory, hidden_layers=50 + epochs=100 + batch_size=32) / (4) ensemble (ARIMA + Prophet + LSTM 3 model voting consensus) 결정 wire + per-tenant override JSONB model_overrides TypedDict 결정 wire.
- **F29.1.5** confidence_level enum 결정 wire = (1) 80 (80% confidence interval) / (2) 90 (90% CI) / (3) 95 (95% CI, default) / (4) 99 (99% CI) 결정 wire + confidence_level per-tenant override JSONB 결정 wire.
- **F29.1.6** forecast_defaults constants 결정 wire = `FORECAST_DEFAULTS = {'horizon_months': 12, 'model_type': 'ensemble', 'confidence_level': 95, 'retraining_cron': '0 3 * * 0', 'min_history_months': 12, 'seasonality_detection': True}` constants + `FORECAST_TARGET_METRICS = ['department', 'cost_center', 'product_line', 'service', 'tenant_total']` constants + `FORECAST_MODEL_TYPES = ['arima', 'prophet', 'lstm', 'ensemble']` constants 결정 wire.
- **F29.1.7** 4 industries baseline 결정 wire = manufacturing + service + manufacturing_service + manufacturing_service_other 모두 industry-agnostic 4-industry grants ✅/✅/✅/✅ (CR 12-1 L4 precedent 미러, Phase 11/12 wire 정합) + per-tenant override EXTENSION + policy evaluation precedence tenant override > industry baseline > system default 결정 wire.
- **F29.1.8** `apps/api/modules/finops/forecast_definition.py` NEW ~+150 LOC + `parse_forecast_definition(definition_text)` parser + `validate_forecast_scope(tenant_id, target_metric)` validator + `ForecastDefinition` TypedDict 10 fields (forecast_id + tenant_id + target_metric + dimension_value + horizon_months + model_type + confidence_level + retraining_cron + status enum active/paused/expired + created_at + updated_at) + 5 layer defense (syntax + semantic + tenant-scope RLS + target_metric validation + history data availability) 결정 wire.
- **F29.1.9** audit-first INSERT `forecast_definition_updated` CR 1-1 verbatim 결정 wire = action_class='FINOPS_FORECAST' (NEW) + action='forecast_definition_updated' (NEW) + actor_id + tenant_id + forecast_id + target_metric + horizon_months + model_type + confidence_level + trace_id 결정 wire + ActionClass.FINOPS_FORECAST 신규 정의 + apps/api/core/audit_action.py MODIFIED AuditAction Literal EXTENSION 1 NEW value + _ActionRegistry FINOPS_FORECAST entry 신규 1개 등록 + __all__ EXTENSION 결정 wire.
- **F29.1.10** typed exception envelope CR 12-5 D-14 결정 wire = `ForecastDefinitionInvalidError(400)` + `ForecastScopeInvalidError(404)` + `ForecastHistoryUnavailableError(404)` 3 NEW typed exception classes + envelope `{error_code, message_ko, trace_id, timestamp, details}` 결정 wire.
- **F29.1.11** RLS 자동 적용 CR 0-2 verbatim 결정 wire = phase_13_finops_forecast tables 의 `tenant_id = current_setting('app.tenant_id')::uuid` policy + multi-tenant isolation 검증 + 테스트 결정 wire (Phase 11 wire `e020ad0` 의 finops_tenant_isolation 테스트 패턴 verbatim 미러).
- **F29.1.12** dry-run mode `--finops-forecast-dry-run` CLI flag 결정 wire = forecast definition 검증 + 결과 preview + actual `forecast_definition_updated` audit-first INSERT skip + dry-run 결과 preview `phase_13_finops_forecast_preview` table 결정 wire.

### F29.2 cost forecasting engine + time series models (12 sub-ACs, A364 결정)

- **F29.2.1** `apps/api/modules/finops/forecast_engine.py` NEW ~+200 LOC + `generate_forecast(tenant_id, target_metric, horizon_months) -> ForecastResult` 결정 wire + 4 time series models parallel run (ARIMA + Prophet + LSTM + ensemble voting consensus) 결정 wire + last 12-month historical baseline data source = phase_11_finops_showback table (Phase 11 wire 정합) + phase_12_finops_anomaly_detection table (Phase 12 wire 정합) 결정 wire.
- **F29.2.2** ARIMA 모델 결정 wire = statsmodels==0.14.1 AD-14 stack pin + ARIMA(p=2, d=1, q=2) default + auto_arima for optimal p/d/q selection (AIC minimization) + per-tenant model training (last 12-month historical baseline) 결정 wire + ARIMA 모델 strength: short-term forecast (3~6 month) 정확도 높음 + long-term forecast 정확도 감소.
- **F29.2.3** Prophet 모델 결정 wire = prophet==1.1.5 AD-14 stack pin + yearly_seasonality=True (KST 7 holidays holiday calendar 적용) + weekly_seasonality=True + daily_seasonality=False + seasonality_mode='multiplicative' (for non-stationary data) + changepoint_prior_scale=0.05 결정 wire + Prophet 모델 strength: yearly seasonality + holiday effect detection 우수 + missing data + outlier robust.
- **F29.2.4** LSTM 모델 결정 wire = tensorflow==2.15.0 AD-14 stack pin + hidden_layers=50 + epochs=100 + batch_size=32 + window_size=12 (lookback 12 months) + dropout=0.2 결정 wire + LSTM 모델 strength: complex non-linear pattern detection + multi-variate forecast 지원 + 단, training time + computational cost 큼.
- **F29.2.5** ensemble 모델 결정 wire = ARIMA + Prophet + LSTM 3 model voting consensus (3 of 3 agree = forecast confirmed) + weighted average (ARIMA 0.3 + Prophet 0.4 + LSTM 0.3) 결정 wire + ensemble strength: 3 모델 의 장점 결합 + robustness 향상 + per-tenant override 가중치 결정 wire.
- **F29.2.6** ForecastResult TypedDict 10 fields 결정 wire = (1) forecast_id UUID PK + (2) tenant_id UUID + (3) target_metric enum + (4) horizon_months int + (5) predicted_values List[NUMERIC(20, 2)] 12 or 24 entries + (6) confidence_lower List[NUMERIC(20, 2)] + (7) confidence_upper List[NUMERIC(20, 2)] + (8) model_type enum + (9) model_version TEXT + (10) generated_at TIMESTAMPTZ + (11) trace_id TEXT 결정 wire.
- **F29.2.7** seasonality detection 결정 wire = weekly_seasonality (요일별 패턴) + monthly_seasonality (월별 패턴) + quarterly_seasonality (분기별 패턴) + yearly_seasonality (연별 패턴) 자동 detection + seasonality_strength > 0.5 인 경우 seasonality_mode='multiplicative' 적용 결정 wire + seasonality_strength 계산 = ACF (autocorrelation function) + PACF (partial autocorrelation function) + STL decomposition 결정 wire.
- **F29.2.8** trend decomposition 결정 wire = STL (Seasonal-Trend decomposition using LOESS) + trend + seasonal + residual 3 components + trend 방향 (upward + downward + flat) 자동 detection + trend_strength > 0.5 인 경우 model_type override 가능 결정 wire.
- **F29.2.9** holiday calendar KST 7 holidays 결정 wire = 신정 (1월 1일) + 설날 (음력 1월 1일) + 삼일절 (3월 1일) + 어린이날 (5월 5일) + 현충일 (6월 6일) + 광복절 (8월 15일) + 개천절 (10월 3일) +圣诞节 (12월 25일) = 8 holidays 결정 wire + holiday_effect modeling Prophet EXTENSION.
- **F29.2.10** model_version tracking 결정 wire = semantic versioning MAJOR.MINOR.PATCH + model_version JSONB metadata (training_date + training_data_window + training_samples_count + model_hyperparameters + cross_validation_score) 결정 wire + model_registry `apps/api/modules/finops/forecast_model_registry.py` NEW 결정 wire.
- **F29.2.11** audit-first INSERT `forecast_generated` CR 1-1 verbatim 결정 wire = action_class='FINOPS_FORECAST' + action='forecast_generated' (NEW) + actor_id='system' + tenant_id + forecast_id + target_metric + horizon_months + model_type + model_version + predicted_values_summary + confidence_level + trace_id 결정 wire + AuditAction Literal EXTENSION 1 NEW value 결정 wire.
- **F29.2.12** typed exception envelope CR 12-5 D-14 결정 wire = `ForecastEngineError(500)` + `ForecastModelTrainingError(500)` + `ForecastSeasonalityDetectionError(500)` 3 NEW typed exception classes + envelope 결정 wire.

### F29.3 capacity headroom analysis (12 sub-ACs, A365 결정)

- **F29.3.1** `apps/api/modules/finops/capacity_headroom.py` NEW ~+180 LOC + `analyze_capacity_headroom(tenant_id, resource_type) -> CapacityHeadroomReport` 결정 wire + compute (CPU + memory utilization) + storage (DB size + backup storage) + network (egress + ingress bandwidth) 3 resource type 90일 lookahead forecast 결정 wire.
- **F29.3.2** compute saturation forecast 결정 wire = CPU utilization 90일 lookahead forecast (LSTM model primary) + memory utilization 90일 lookahead forecast (LSTM model primary) + Phase 8 wire `60d4ea1` k6 부하 테스트 의 cost-engine benchmark EXTENSION + Phase 7 wire `59b56cd` Prometheus `business_cost_engine_duration_seconds` histogram EXTENSION 정합 + compute_saturation_pct 계산 = (predicted_utilization / max_capacity) × 100 결정 wire.
- **F29.3.3** storage saturation forecast 결정 wire = DB size growth 90일 lookahead forecast (Prophet model primary) + backup storage capacity 90일 lookahead forecast (Prophet model primary) + Phase 6 wire `24e1cd7` audit_log_archive table growth rate EXTENSION + Phase 5 wire `f093f8c` cross-region replica lag EXTENSION 정합 + storage_saturation_pct 계산 = (predicted_size / max_capacity) × 100 결정 wire.
- **F29.3.4** network saturation forecast 결정 wire = egress bandwidth 90일 lookahead forecast (ARIMA model primary) + ingress bandwidth 90일 lookahead forecast (ARIMA model primary) + Phase 5 wire `f093f8c` cross-region replication traffic EXTENSION + network_saturation_pct 계산 = (predicted_bandwidth / max_bandwidth) × 100 결정 wire.
- **F29.3.5** headroom_pct 계산 결정 wire = (1 - saturation_pct) × 100 결정 wire + headroom_pct 범위: 0~100% (높을수록 여유) + per-tenant override headroom_target_pct default 30% 결정 wire.
- **F29.3.6** critical threshold 결정 wire = headroom_pct < 20% → critical alert 발송 (Slack #bizup-finops-capacity channel + PagerDuty `pd_capacity_critical` service) 결정 wire + critical alert 시 audit-first INSERT `capacity_critical_alert_sent` CR 1-1 verbatim + ActionClass.FINOPS_FORECAST entry EXTENSION 결정 wire.
- **F29.3.7** warning threshold 결정 wire = headroom_pct < 40% → warning alert 발송 (Slack only) 결정 wire + warning alert 시 audit-first INSERT `capacity_warning_alert_sent` CR 1-1 verbatim 결정 wire.
- **F29.3.8** CapacityHeadroomReport TypedDict 14 fields 결정 wire = (1) report_id UUID PK + (2) tenant_id UUID + (3) resource_type enum compute/storage/network + (4) current_saturation_pct NUMERIC(8, 4) + (5) forecast_horizon_days int (default 90) + (6) predicted_saturation_30d NUMERIC(8, 4) + (7) predicted_saturation_60d NUMERIC(8, 4) + (8) predicted_saturation_90d NUMERIC(8, 4) + (9) headroom_pct NUMERIC(8, 4) + (10) severity enum ok/warning/critical + (11) threshold_breach_date DATE nullable + (12) model_type enum arima/prophet/lstm + (13) model_version TEXT + (14) generated_at TIMESTAMPTZ + (15) trace_id TEXT 결정 wire.
- **F29.3.9** audit-first INSERT `capacity_headroom_analyzed` CR 1-1 verbatim 결정 wire = action_class='FINOPS_FORECAST' + action='capacity_headroom_analyzed' (NEW) + actor_id='system' + tenant_id + report_id + resource_type + headroom_pct + severity + threshold_breach_date + model_version + trace_id 결정 wire + AuditAction Literal EXTENSION 1 NEW value 결정 wire.
- **F29.3.10** per-tenant override JSONB 결정 wire = `tenant_settings.capacity_overrides` TypedDict 12 fields (compute_target_headroom_pct + storage_target_headroom_pct + network_target_headroom_pct + warning_threshold_pct + critical_threshold_pct + forecast_horizon_days + model_type per resource + alert_channels per severity + notification_recipients + escalation_policy) 결정 wire + policy evaluation precedence tenant override > industry baseline > system default.
- **F29.3.11** forecast horizon 90일 lookahead default 결정 wire = 30d + 60d + 90d 3 forecast points 결정 wire + per-tenant override horizon_days (range 7~365) + forecast_model per resource type 결정 wire.
- **F29.3.12** typed exception envelope CR 12-5 D-14 결정 wire = `CapacityHeadroomAnalysisError(500)` + `CapacityThresholdBreachError(500)` + `CapacityMetricUnavailableError(404)` 3 NEW typed exception classes + envelope 결정 wire.

### F29.4 budget burn-rate projection (12 sub-ACs, A366 결정)

- **F29.4.1** `apps/api/modules/finops/budget_burnrate.py` NEW ~+150 LOC + `project_budget_consumption(tenant_id, period_key) -> BurnRateProjection` 결정 wire + Phase 12 wire `f3c0e63` budget_alert 의 linear extrapolation EXTENSION + ARIMA-based projection (Phase 12 baseline EXTENSION) + 7/30-day rolling projection 결정 wire.
- **F29.4.2** BurnRateProjection TypedDict 12 fields 결정 wire = (1) projection_id UUID PK + (2) tenant_id UUID + (3) period_key TEXT + (4) budget_id UUID + (5) budget_amount NUMERIC(20, 2) + (6) current_consumption NUMERIC(20, 2) + (7) days_elapsed int + (8) days_remaining int + (9) projected_total NUMERIC(20, 2) + (10) projected_overrun_pct NUMERIC(8, 4) + (11) burn_rate_per_day NUMERIC(20, 2) + (12) confidence NUMERIC(8, 4) + (13) model_type enum linear/arima/ensemble + (14) trace_id TEXT 결정 wire.
- **F29.4.3** linear extrapolation 결정 wire = `projected_total = current_consumption / days_elapsed × total_period_days` 결정 wire + linear 모델 strength: simple + interpretable + 단, 비선형 패턴 감지 불가.
- **F29.4.4** ARIMA-based projection 결정 wire = ARIMA 모델 trained on phase_11_finops_chargeback table 의 historical consumption data (last 12 months) + future 7d/30d consumption forecast + projected_total = current_consumption + predicted_future_consumption 결정 wire + ARIMA 모델 strength: 비선형 패턴 감지 + seasonality 고려.
- **F29.4.5** 7-day rolling projection 결정 wire = last 7 days consumption rate 기반 projected_total 계산 + daily granularity + per-day breakdown (predicted_day_1 ~ predicted_day_7) 결정 wire + 7-day rolling window 자동 update.
- **F29.4.6** 30-day rolling projection 결정 wire = last 30 days consumption rate 기반 projected_total 계산 + weekly granularity (predicted_week_1 ~ predicted_week_4) 결정 wire + 30-day rolling window 자동 update.
- **F29.4.7** predicted_overrun_pct > 110% → warning alert 발송 결정 wire = Slack #bizup-finops-budget-burnrate channel + audit-first INSERT `budget_burn_rate_warning_sent` CR 1-1 verbatim + ActionClass.FINOPS_FORECAST entry EXTENSION 결정 wire.
- **F29.4.8** predicted_overrun_pct > 130% → critical alert 발송 결정 wire = Slack + PagerDuty `pd_budget_burnrate_critical` service + audit-first INSERT `budget_burn_rate_critical_sent` CR 1-1 verbatim 결정 wire.
- **F29.4.9** audit-first INSERT `budget_burn_rate_projected` CR 1-1 verbatim 결정 wire = action_class='FINOPS_FORECAST' + action='budget_burn_rate_projected' (NEW) + actor_id='system' + tenant_id + budget_id + projected_overrun_pct + burn_rate_per_day + confidence + model_type + trace_id 결정 wire + AuditAction Literal EXTENSION 1 NEW value 결정 wire.
- **F29.4.10** per-tenant override JSONB 결정 wire = `tenant_settings.budget_burnrate_overrides` TypedDict 8 fields (warning_threshold_pct default 110 + critical_threshold_pct default 130 + linear_weight default 0.3 + arima_weight default 0.7 + rolling_window_days enum 7/30 + alert_channels per severity + notification_recipients + escalation_policy) 결정 wire.
- **F29.4.11** confidence calculation 결정 wire = (1 - normalized_rmse) × 100 결정 wire + confidence 범위 0~100% (높을수록 정확) + per-tenant override confidence_threshold default 70% 결정 wire + confidence < 70% → model_retraining_triggered audit-first INSERT 결정 wire.
- **F29.4.12** typed exception envelope CR 12-5 D-14 결정 wire = `BudgetBurnRateProjectionError(500)` + `BudgetOverrunPredictionError(500)` 2 NEW typed exception classes + envelope 결정 wire.

### F29.5 forecast accuracy tracking + model retraining trigger (12 sub-ACs, A367 결정)

- **F29.5.1** `apps/api/modules/finops/forecast_accuracy_tracker.py` NEW ~+120 LOC 결정 wire + Phase 12 wire `f3c0e63` forecast_accuracy.py EXTENSION + per-model accuracy tracking (per (tenant_id + target_metric + model_type) granularity) + ensemble vs individual model comparison 결정 wire.
- **F29.5.2** MAE (Mean Absolute Error) 결정 wire = (1/n) × Σ|predicted - actual| 결정 wire + MAE 단위 KRW + per-model MAE 계산 + per-model MAE ranking 결정 wire.
- **F29.5.3** MAPE (Mean Absolute Percentage Error) 결정 wire = (100/n) × Σ|predicted - actual| / actual × 100 결정 wire + MAPE 단위 % + MAPE < 10% = high accuracy / 10~20% = moderate / > 20% = low accuracy 결정 wire + Phase 12 wire forecast_accuracy EXTENSION 정합.
- **F29.5.4** RMSE (Root Mean Squared Error) 결정 wire = √((1/n) × Σ(predicted - actual)²) 결정 wire + RMSE 단위 KRW + per-model RMSE 계산 + per-model RMSE ranking 결정 wire.
- **F29.5.5** ensemble vs individual model comparison 결정 wire = ARIMA vs Prophet vs LSTM vs ensemble 4 model 의 MAE/MAPE/RMSE 비교 + ensemble 가 가장 낮은 MAPE 인 경우 ensemble 채택 + ensemble 가 개별 모델보다 MAPE 5% 이상 높으면 ensemble_weight override 결정 wire + per-tenant override 가중치 결정 wire.
- **F29.5.6** ForecastAccuracyReport TypedDict 10 fields 결정 wire = (1) report_id UUID PK + (2) tenant_id UUID + (3) target_metric enum + (4) model_type enum + (5) mae NUMERIC(20, 2) + (6) mape NUMERIC(8, 4) + (7) rmse NUMERIC(20, 2) + (8) sample_count int + (9) accuracy_score NUMERIC(8, 4) (100 - MAPE) + (10) model_version TEXT + (11) generated_at TIMESTAMPTZ + (12) trace_id TEXT 결정 wire.
- **F29.5.7** model performance degradation detection 결정 wire = MAPE > 20% for 3 consecutive periods → model_retraining_trigger + Phase 12 wire `f3c0e63` `model_retraining_triggered` audit-first INSERT EXTENSION 결정 wire + model_retraining cron KST 매주 일요일 03:00 UTC 18:00 (Phase 12 anomaly detection isolation forest retraining cron EXTENSION 정합) 결정 wire.
- **F29.5.8** predicted_amount source 결정 wire = phase_11_finops_showback table 의 showback_generated event 의 predicted_amount column (Phase 11 wire EXTENSION) + actual_amount source = phase_11_finops_chargeback table 의 chargeback_calculated event 의 actual_amount column (Phase 11 wire 정합) + Phase 12 wire `f3c0e63` forecast_deviation source EXTENSION 정합 결정 wire.
- **F29.5.9** audit-first INSERT `forecast_accuracy_degraded` CR 1-1 verbatim 결정 wire = action_class='FINOPS_FORECAST' + action='forecast_accuracy_degraded' (NEW) + actor_id='system' + tenant_id + target_metric + model_type + current_mape + previous_mape + degradation_period_count + trace_id 결정 wire + AuditAction Literal EXTENSION 1 NEW value 결정 wire.
- **F29.5.10** model_retraining_triggered audit-first INSERT EXTENSION 결정 wire = Phase 12 wire `f3c0e63` `model_retraining_triggered` action EXTENSION + model_type 추가 (ARIMA + Prophet + LSTM + ensemble 모두 포함) + retraining_cron KST 매주 일요일 03:00 UTC 18:00 결정 wire + ActionClass.FINOPS_FORECAST entry EXTENSION 결정 wire.
- **F29.5.11** model_version tracking 결정 wire = ARIMA + Prophet + LSTM + ensemble 4 model 모두 semantic versioning MAJOR.MINOR.PATCH + model_version JSONB metadata (training_date + training_data_window + training_samples_count + model_hyperparameters + cross_validation_score) + Phase 12 wire `f3c0e63` `forecast_engine` model_version EXTENSION 정합 결정 wire.
- **F29.5.12** typed exception envelope CR 12-5 D-14 결정 wire = `ForecastAccuracyTrackingError(500)` + `ModelRetrainingTriggerError(500)` + `ModelPerformanceDegradationError(500)` 3 NEW typed exception classes + envelope 결정 wire.

### F29.6 forecasting dashboard UI (10 sub-ACs, A368 결정)

- **F29.6.1** `apps/web/app/[locale]/(dashboard)/admin/finops/forecast/page.tsx` NEW ~+150 LOC + 4 components (`ForecastHorizonSelector` + `ForecastChart` + `CapacityHeadroomGauge` + `BudgetBurnRatePanel`) 결정 wire + owner-only RBAC AD-22 verbatim + Epic 12 2FA 챌린지 보존 결정 wire.
- **F29.6.2** ForecastHorizonSelector component 결정 wire = 4 horizon 옵션 radio button (3m + 6m + 12m + 24m) + 4 model_type 옵션 (ARIMA + Prophet + LSTM + ensemble) + 4 confidence_level 옵션 (80 + 90 + 95 + 99) + owner-only ack prompt AD-22 verbatim 결정 wire.
- **F29.6.3** ForecastChart component 결정 wire = 5 target_metric 별 forecast 시각화 (department + cost_center + product_line + service + tenant_total) + line chart (last 12-month historical + 12-month forecast + confidence interval band) + Recharts 2.12.7 AD-14 stack pin 결정 wire.
- **F29.6.4** CapacityHeadroomGauge component 결정 wire = 3 resource_type gauge (compute + storage + network) + headroom_pct 시각화 + severity color coding (ok=green + warning=yellow + critical=red) + 90일 lookahead forecast 표시 결정 wire + RTL render discipline CR 11-4 D-003 verbatim 결정 wire.
- **F29.6.5** BudgetBurnRatePanel component 결정 wire = Phase 12 wire `f3c0e63` budget_alert EXTENSION + 7-day + 30-day rolling projection 시각화 + predicted_overrun_pct 표시 + threshold breach date 표시 + alert history (last 30d) 결정 wire.
- **F29.6.6** ko-KR.json `finops_forecast.*` namespace EXTENSION ~30 keys 결정 wire (CR 11-4 D-002 verbatim SSOT) + `finops_forecast.model.arima` + `finops_forecast.model.prophet` + `finops_forecast.model.lstm` + `finops_forecast.model.ensemble` + `finops_forecast.confidence.80` + `finops_forecast.confidence.90` + `finops_forecast.confidence.95` + `finops_forecast.confidence.99` + `finops_forecast.severity.ok` + `finops_forecast.severity.warning` + `finops_forecast.severity.critical` + `finops_forecast.resource.compute` + `finops_forecast.resource.storage` + `finops_forecast.resource.network` 등 결정 wire.
- **F29.6.7** ARIA labels 결정 wire = WCAG 2.1 AA compliance (Epic 12 의 2FA 챌린지 UI 와 동일 표준) + keyboard navigation (Tab + Enter + Arrow keys) + screen reader 지원 결정 wire + Phase 13 Epic 1 UX v1.0 locked decision 결정 wire (Dark MVP / WCAG AA / Professional / ko-KR) verbatim 보존 결정 wire.
- **F29.6.8** toast notification 결정 wire = forecast generated 시 toast 자동 표시 (severity=ok → green + 5초 / severity=warning → yellow + 10초 / severity=critical → red + 15초) + capacity_headroom_analyzed 시 toast (severity 별 색상 + duration) 결정 wire.
- **F29.6.9** Vitest RTL render discipline CR 11-4 D-003 verbatim 결정 wire = 6 NEW vitest cases (ForecastHorizonSelector 1 + ForecastChart 1 + CapacityHeadroomGauge 1 + BudgetBurnRatePanel 1 + ko-KR SSOT 2) 결정 wire.
- **F29.6.10** FinOps forecast dashboard parity CR 12-5 D-PARITY-01 결정 wire = TS mirror `apps/web/lib/finops-forecast/finops-forecast-client.ts` NEW ~+150 LOC + `tests/web/test_finops_forecast_dashboard_parity.py` NEW ~+10 cases + 1-line ko-KR reject 결정 wire.

### F29.7 Capability matrix v1.38 EXTENSION FINOPS_FORECASTING_CAPACITY_PLANNING (12 sub-ACs, A369 결정)

- **F29.7.1** capability matrix v1.37 → v1.38 EXTENSION 결정 wire = FINOPS_FORECASTING_CAPACITY_PLANNING 1 NEW row + docs/capability-matrix.md MODIFIED + version 표기 결정 wire.
- **F29.7.2** `Capability.FINOPS_FORECASTING_CAPACITY_PLANNING = "finops_forecasting_capacity_planning"` 1 NEW enum 결정 wire + apps/api/core/capability.py MODIFIED + CR 12-1 L4 precedent 미러 결정 wire.
- **F29.7.3** `apps/api/dependencies/capability.py` MODIFIED 결정 wire = `require_finops_forecasting_capacity_planning` 1 NEW dep + Phase 12 wire `f3c0e63` 의 `require_finops_anomaly_detection` + `require_finops_budget_alert` + Phase 11 wire `e020ad0` 의 `require_finops_showback` + `require_finops_chargeback` pattern verbatim 미러 결정 wire.
- **F29.7.4** industry-agnostic 4-industry grants ✅/✅/✅/✅ 결정 wire = manufacturing + service + manufacturing_service + manufacturing_service_other 모두 허용 (CR 12-1 L4 precedent 미러, FINOPS_ANOMALY_DETECTION + FINOPS_BUDGET_ALERT Phase 12 wire + FINOPS_SHOWBACK + FINOPS_CHARGEBACK Phase 11 wire pattern verbatim bind) 결정 wire.
- **F29.7.5** 미허용 tenant 의 forecast 진입 차단 결정 wire = `require_finops_forecasting_capacity_planning` dependency 의 미허용 tenant 진입 차단 검증 + 403 Forbidden + `FORBIDDEN_KO` message ("FinOps Forecasting & Capacity Planning capability 미허용 tenant") + Epic 12 2FA 챌린지 보존 + AD-22 owner-only RBAC 정합 결정 wire.
- **F29.7.6** forecast 의 phase_12 forecast_accuracy + phase_12 anomaly_detection + phase_11 showback baseline carry-over 진입 차단 결정 wire = forecast 진입 시 phase_12 FINOPS_ANOMALY_DETECTION capability 도 동시에 검증 + forecast 가 FINOPS_FORECASTING_CAPACITY_PLANNING 만 있고 FINOPS_ANOMALY_DETECTION 없는 경우 403 Forbidden 결정 wire.
- **F29.7.7** drift detector `tests/integration/test_capability_matrix_v1_38_drift.py` NEW 결정 wire = 8 NEW pytest cases (FINOPS_FORECASTING_CAPACITY_PLANNING row 존재 1 + 4-industry grants 1 + Capability enum 1 + require_finops_forecasting_capacity_planning 1 + 미허용 tenant 차단 1 + phase_12 carry-over 검증 2 + phase_11 carry-over 검증 1) 결정 wire + Phase 12 wire `f3c0e63` 의 `tests/integration/test_capability_matrix_v1_37_drift.py` 패턴 verbatim 미러 결정 wire.
- **F29.7.8** m21_finops_forecast 결정 wire = `apps/api/modules/finops/forecast/__init__.py` NEW 결정 wire + `apps/api/modules/finops/forecast/serializers.py` NEW 결정 wire (m20_finops_anomaly 의 Phase 12 wire EXTENSION pattern verbatim 미러) 결정 wire + module version 결정 wire (m21_finops_forecast 결정 wire + Phase 12 wire 의 m20_finops_anomaly 와 동일 pattern).
- **F29.7.9** SSOT RED→GREEN EXTENSION 결정 wire = capability matrix v1.38 신규 1 row + capability.py EXTENSION 1 NEW enum + require_finops_forecasting_capacity_planning 1 NEW dep + drift detector + A36 SDR 검증 4-step 자동 적용 + CR 12-5 D-GATE-01 inversion 적용 보존 결정 wire.
- **F29.7.10** CR 12-1 L4 industry-agnostic capability 결정 wire = FINOPS_FORECASTING_CAPACITY_PLANNING industry-agnostic 4-industry grants ✅/✅/✅/✅ 결정 wire (manufacturing + service + manufacturing_service + manufacturing_service_other 모두 허용) + Phase 12 wire 의 FINOPS_ANOMALY_DETECTION + FINOPS_BUDGET_ALERT industry-agnostic 4-industry grants pattern verbatim 미러 결정 wire.
- **F29.7.11** capability gate 의 fail-closed 결정 wire = 미허용 tenant 의 forecast 진입 시 403 Forbidden 결정 wire + capability matrix v1.38 row 부재 시 fail-closed 결정 wire + `Capability.FINOPS_FORECASTING_CAPACITY_PLANNING` enum 부재 시 fail-closed 결정 wire + AD-22 owner-only RBAC 정합 + Epic 12 2FA 챌린지 보존 + NFR4 PII minimization 정합 보존 결정 wire.
- **F29.7.12** capability matrix 의 version 결정 wire = v1.37 → v1.38 EXTENSION 결정 wire + version bump 일관성 결정 wire (Phase 12 wire 의 capability matrix v1.36 → v1.37 pattern verbatim 미러) + docs/capability-matrix.md 의 version 표기 결정 wire + sprint-status.yaml 의 capability version 결정 wire + drift detector 의 version 매칭 검증 결정 wire + CR 12-5 D-PARITY-01 inversion 적용 보존 결정 wire.

### F29.8 dry-run + Tests + wire scope T1~T8 (12 sub-ACs)

- **F29.8.1** dry-run mode 결정 wire = `--finops-forecast-dry-run` + `--finops-capacity-dry-run` + `--finops-burnrate-dry-run` CLI flags + forecast definition dry-run parameter + capacity headroom dry-run parameter + budget burnrate dry-run parameter + dry-run 시 actual `forecast_definition_updated` audit-first INSERT skip + dry-run 시 actual `capacity_headroom_analyzed` audit-first INSERT skip + dry-run 시 actual `budget_burn_rate_projected` audit-first INSERT skip 결정 wire + dry-run 결과 preview 결정 wire (forecast_preview + capacity_preview + burnrate_preview 테이블 alembic 0045 신규 결정 wire).
- **F29.8.2** dry-run 의 preview 결과 결정 wire = `phase_13_finops_forecast_preview` + `phase_13_finops_capacity_preview` + `phase_13_finops_burnrate_preview` 3 table 결정 wire (preview_id UUID PK + tenant_id UUID + period_key TEXT + preview_data JSONB + computed_at TIMESTAMPTZ DEFAULT NOW() + trace_id TEXT) + RLS 자동 적용 CR 0-2 verbatim 결정 wire + audit-first INSERT `forecast_dry_run_executed` + `capacity_dry_run_executed` + `burnrate_dry_run_executed` CR 1-1 verbatim 결정 wire.
- **F29.8.3** dry-run 의 CLI flag 결정 wire = `--finops-forecast-dry-run` + `--finops-capacity-dry-run` + `--finops-burnrate-dry-run` global flags + `--forecast-definition-dry-run` + `--capacity-headroom-dry-run` + `--budget-burnrate-dry-run` sub-flags 결정 wire + dry-run 모드 시 logger 출력 결정 (stdout + INFO level) + dry-run 결과 preview 결정 wire (console table output 결정 wire).
- **F29.8.4** tests 결정 wire = ~+48 NEW pytest PASS (forecast_definition 6 + forecast_engine 8 + capacity_headroom 6 + budget_burnrate 5 + forecast_accuracy_tracker 4 + alembic 0045 4 + audit action 7 + capability matrix v1.38 8 = ~+48 NEW pytest PASS) 결정 wire + forecast_definition 6 tests 결정 (parse_forecast_definition validation 3 + target_metric validation 2 + audit-first INSERT 1) + forecast_engine 8 tests 결정 (ARIMA + Prophet + LSTM + ensemble 4 + seasonality detection 1 + trend decomposition 1 + V8 determinism 1 + audit-first INSERT 1) + capacity_headroom 6 tests 결정 (compute + storage + network forecast 3 + threshold breach detection 2 + audit-first INSERT 1) + budget_burnrate 5 tests 결정 (linear + ARIMA projection 2 + rolling 7d/30d projection 1 + predicted_overrun_pct calculation 1 + audit-first INSERT 1) + forecast_accuracy_tracker 4 tests 결정 (MAE + MAPE + RMSE 3 + model_retraining_trigger 1) + alembic 0045 4 tests 결정 (migration + RLS + CHECK constraints + UNIQUE constraint 4) + audit action 7 tests 결정 (forecast_definition_updated + forecast_generated + capacity_headroom_analyzed + budget_burn_rate_projected + forecast_accuracy_degraded + model_retraining_triggered + forecast_dry_run_executed 7 NEW actions + _ActionRegistry FINOPS_FORECAST entry 신규 1개 등록 + multi-tenant isolation 2) + capability matrix v1.38 8 tests 결정 (FINOPS_FORECASTING_CAPACITY_PLANNING row 존재 1 + 4-industry grants 1 + Capability enum 1 + require_finops_forecasting_capacity_planning 1 + 미허용 tenant 차단 1 + phase_12 carry-over 검증 2 + phase_11 carry-over 검증 1) 결정 wire.
- **F29.8.5** vitest tests 결정 wire = ~+6 NEW vitest PASS (ForecastHorizonSelector + ForecastChart owner-only ack prompt AD-22 verbatim + CapacityHeadroomGauge RTL render + BudgetBurnRatePanel + ko-KR SSOT 2 + finops forecast dashboard parity CR 12-5 D-PARITY-01 결정 wire) 결정 wire + Phase 12 wire 의 ~5 NEW vitest PASS pattern verbatim 미러 결정 wire.
- **F29.8.6** ruff + tsc 결정 wire = 0 NEW ruff 결정 wire (Phase 12 wire 정합) + 0 NEW tsc 결정 wire (apps/web frontend 변경 ext only — pre-existing baseline errors preserved per cj-style discipline) + SDR drift gate 결정 wire (vitest file count +1 NEW collected well within 5% tolerance + pytest +8 NEW files collected well within 5% tolerance) 결정 wire + commit_consistency gate 결정 wire (CR 9-6 commit message discipline + A36 SDR 검증 4-step 자동 적용) 결정 wire + D-DEFER-* grep guard 결정 wire (CR 11-3 honest-DEFER discipline 113번째 epic 연속 정직 회복 검증 보존) 결정 wire.
- **F29.8.7** wire scope T1~T8 결정 wire = T1 forecast_definition + forecast_dsl module 결정 wire + T2 forecast_engine + 4 time series models (ARIMA + Prophet + LSTM + ensemble) 결정 wire + T3 capacity_headroom + compute/storage/network forecast 결정 wire + T4 budget_burnrate + Phase 12 budget_alert carry-over + forecast_accuracy_tracker module 결정 wire + T5 alembic 0045 phase_13_forecasting 결정 wire + T6 audit action EXTENSION 7 NEW 결정 wire + T7 capability v1.38 EXTENSION + frontend finops forecast dashboard 결정 wire + T8 3중 게이트 FINAL CLEAN atomic commit 결정 wire + estimated ~+32-37 files = ~+20 NEW + ~+12 MODIFIED 결정 wire + Phase 12 wire 의 wire_commit pattern verbatim 미러 결정 wire.
- **F29.8.8** A19 cohesion pattern 9 surface EXTENSION PASS 결정 wire (FinOps forecasting & capacity planning surface NEW = F29.1~F29.8 결정 wire) 결정 wire + Phase 12 wire 의 FinOps anomaly detection + budget alert surface NEW = F28.1~F28.8 pattern verbatim 미러 결정 wire + Phase 11 wire 의 FinOps showback/chargeback surface NEW = F27.1~F27.7 pattern verbatim 미러 결정 wire + Phase 10 wire 의 SLO engineering surface NEW = F26.1~F26.7 pattern verbatim 미러 결정 wire.
- **F29.8.9** CR lessons applied 14종 결정 wire (CR 0-2 RLS lesson ✅ APPLIED + CR 1-1 audit-first INSERT ✅ APPLIED (7 NEW audit log entries `forecast_definition_updated` + `forecast_generated` + `capacity_headroom_analyzed` + `budget_burn_rate_projected` + `forecast_accuracy_degraded` + `model_retraining_triggered` + `forecast_dry_run_executed` = 7 NEW) + CR 4-3/4-4 lessons carry ✅ APPLIED (forecast + capacity baseline + golden_diff pattern verbatim) + CR 1-1 ContextVar lesson ✅ APPLIED + CR 1-1 RSC boundary lesson ✅ APPLIED (admin/finops/forecast page RSC + Client Component delegation) + CR 9-6 commit message discipline ✅ APPLIED (`git commit -F <file>` 사용, PowerShell here-string 회피, D5 prevention) + CR 11-3 honest-DEFER discipline ✅ APPLIED (113번째 epic 연속 정직 회복) + CR 11-4 D-001~D-005 + P-015 ✅ APPLIED + CR 12-1 L4 ✅ APPLIED (FINOPS_FORECASTING_CAPACITY_PLANNING industry-agnostic 4-industry grants ✅/✅/✅/✅) + CR 12-5 D-14 typed exception envelope ✅ APPLIED (16 NEW typed exception classes `ForecastDefinitionInvalidError` + `ForecastScopeInvalidError` + `ForecastHistoryUnavailableError` + `ForecastEngineError` + `ForecastModelTrainingError` + `ForecastSeasonalityDetectionError` + `CapacityHeadroomAnalysisError` + `CapacityThresholdBreachError` + `CapacityMetricUnavailableError` + `BudgetBurnRateProjectionError` + `BudgetOverrunPredictionError` + `ForecastAccuracyTrackingError` + `ModelRetrainingTriggerError` + `ModelPerformanceDegradationError` + 2 dry-run errors) + CR 12-5 D-PARITY-01 inversion ✅ APPLIED + CR 12-5 D-GATE-01 inversion ✅ APPLIED + A36 SDR 검증 4-step 자동 적용 ✅ + AD-14 stack pin ✅ APPLIED (statsmodels==0.14.1 + prophet==1.1.5 + tensorflow==2.15.0 + slack-sdk==3.23.0 + pdpyras==5.2.0 + Recharts 2.12.7) + AD-22 owner-only RBAC ✅ APPLIED (forecasting & capacity planning 모두 owner-only RBAC AD-22 + Epic 12 2FA 챌린지 보존) + NFR4 PII minimization ✅ PRESERVED) 결정 wire.
- **F29.8.10** D-DEFER-* honestly 결정 wire (CR 11-3 113번째 epic 연속 정직 회복 결정 wire 보존) = D-1-1-DEFER-1/2/3 RESOLVED 보존 60~113번째 + D-LAUNCH-1-DEFER-1 honestly preserved 65~113번째 + D-EPIC-16-REVIEW-DEFER-2~6 ✅ RESOLVED 보존 78~113번째 + D-PHASE-4-DR-DEFER-1/2 ✅ RESOLVED 보존 73~113번째 + D-EPIC-17-WIRE-DEFER-T2-T3-UI ✅ RESOLVED 보존 83~113번째 + D-RETENTION-1 ✅ RESOLVED 보존 85~113번째 + D-OBSERVABILITY-1 ✅ RESOLVED 보존 89~113번째 + D-PERFORMANCE-1 ✅ RESOLVED 보존 93~113번째 + D-CHAOS-1 ✅ RESOLVED 보존 100~113번째 + D-SLO-1 ✅ RESOLVED 보존 101~113번째 + D-FINOPS-1 ✅ RESOLVED 보존 105~113번째 + D-FINOPS-2 ✅ RESOLVED 보존 109~113번째 + **D-FINOPS-3 신규 honestly DEFER 보존 = Phase 13 PRD entry 진입 시점에 carry-over chain 정직 회복 결정 wire 진입: Phase 12 close-out retro `3354e83` §13 + Phase 11 close-out retro `80df15b` §12 + Phase 10 close-out retro `733d428` §10 + Phase 9 close-out retro `634427d` §10 + Phase 8 close-out retro `ab495a8` §10 + Phase 7 close-out retro `326fa9f` §10 + Phase 6 close-out retro `f9f006c` §13 + Epic 17 close-out retro `be8f3bd` §11 "FinOps Forecasting & Capacity Planning 결정 wire 보류, Phase 13+ 진입 시점" verbatim 해소** 결정 wire.
- **F29.8.11** Epic 1 ~ Epic 17 + Phase 3 ~ Phase 12 + 1st release cycle 정합 보존 결정 wire (cj-style 113번째 epic 연속 정직 회복 pre-flight 정합 sweep 결정 wire 보존): ✅ Phase 12 close-out retro `3354e83` (cj-style 112번째) + ✅ Phase 12 atomic wire T1~T8 `f3c0e63` (cj-style 111번째) + ✅ Phase 12 spec entry `8c5f374` (cj-style 110번째) + ✅ Phase 12 PRD entry `344c7eb` (cj-style 109번째) + ✅ Phase 11 close-out retro `80df15b` (cj-style 108번째) + ✅ Phase 11 atomic wire T1~T8 `e020ad0` (cj-style 107번째) + ✅ Phase 11 spec entry `82c93a8` (cj-style 106번째) + ✅ Phase 11 PRD entry `16d7698` (cj-style 105번째) + ✅ Phase 10 close-out retro `733d428` (cj-style 104번째) + ✅ Phase 10 atomic wire T1~T8 `ac5d6c5` (cj-style 103번째) + ✅ Phase 10 spec entry `3c80ef0` (cj-style 102번째) + ✅ Phase 10 PRD entry `09db4d4` (cj-style 101번째) + ✅ Phase 9 close-out retro `634427d` (cj-style 100번째) + ✅ Phase 9 atomic wire T1~T8 `e7670e1` (cj-style 99번째) + ✅ Phase 9 spec entry `2a5e4da` (cj-style 98번째) + ✅ Phase 9 PRD entry `0b2d2f3` (cj-style 97번째) + ✅ Phase 8 close-out retro `ab495a8` (cj-style 96번째) + ✅ Phase 8 atomic wire T1~T8 `60d4ea1` (cj-style 95번째) + ✅ Phase 8 spec entry `5ae0f4e` (cj-style 94번째) + ✅ Phase 8 PRD entry `ced452f` (cj-style 93번째) + ✅ Build fixes sprint `eaee198` + ✅ Phase 7 close-out retro `326fa9f` (cj-style 92번째) + ✅ Phase 7 atomic wire T1~T8 `59b56cd` (cj-style 91번째) + ✅ Phase 7 spec entry `749381e` (cj-style 90번째) + ✅ Phase 7 PRD entry `916a541` (cj-style 89번째) + ✅ Phase 6 close-out retro `f9f006c` (cj-style 88번째) + ✅ Phase 6 atomic wire T1~T8 `24e1cd7` (cj-style 87번째) + ✅ Phase 6 spec entry `f5c14c9` (cj-style 86번째) + ✅ Phase 6 PRD entry `e84a281` (cj-style 85번째) + ✅ Epic 17 close-out retro `be8f3bd` (cj-style 84번째) + ✅ Epic 17 T2+T3 UI wire `bb92879` (cj-style 83번째) + ✅ Epic 17 wire `2ada2ec` (cj-style 82번째) + ✅ Epic 17 spec entry `f4b2b58` (cj-style 81번째) + ✅ Epic 17 PRD entry `40a9c41` (cj-style 80번째) + ✅ Sidebar/MenuProvider hot-fix `01a06e4` (cj-style 79번째) + ✅ D-EPIC-16-REVIEW-DEFER-2~6 RESOLVE sprint `512ed6a` (cj-style 78번째) + ✅ Phase 5 close-out retro `b843565` (cj-style 76~77번째) + ✅ Phase 5 wire `f093f8c` (cj-style 75번째) + ✅ Phase 5 spec entry (cj-style 74번째) + ✅ Phase 5 PRD entry `93d852b` (cj-style 73번째) + ✅ Epic 16 close-out retro (cj-style 72번째) + ✅ Epic 16 T4 admin UI follow-up `ff5c3b5` (cj-style 71번째) + ✅ Epic 16 review follow-up `963079c` (cj-style 70번째) + ✅ Epic 16 atomic wire `e117e09` (cj-style 69번째) + ✅ Epic 16 PRD entry `08bfca5` (cj-style 67번째) + ✅ 1st release cycle cj-style 62~66번째 모두 wire DONE 진입 + ✅ Epic 15 cycle cj-style 58~61번째 모두 wire DONE 진입 + ✅ Phase 4 cycle cj-style 53~57번째 모두 wire DONE 진입 + ✅ Phase 3 cycle cj-style 49~52번째 모두 wire DONE 진입 + ✅ Epic 14 LISTEN/NOTIFY multi-process `7835463` + ✅ Epic 13 LISTEN/NOTIFY consume `f2ea2f6` + ✅ Epic 12 2FA 게이트 `a63646c` (Phase 13 forecasting & capacity planning 모두 owner-only RBAC AD-22 + Epic 12 2FA 챌린지 보존) + ✅ Epic 11 close-out retro + Phase 2 close-out baseline 599 passed 정합 보존 + ✅ Epic 1 carry-over (auth) layout + onboarding/industry 보존 + ✅ Epic 7~10 ABC/TDABC + AI 인사이트 territory 결정 wire 보존 결정 wire.
- **F29.8.12** partial wire 시도 0건 + single sprint atomic docs-only wire 1 진입점 결정 wire (cj-style 113번째 epic 연속 정직 회복 Phase 13 PRD entry atomic docs-only wire 6 files atomic single sprint 결정 wire) + 결정 wire 일자: 2026-08-24 (KST) + **next**: 옵션 (a) Phase 13 bmad-create-story spec entry 진입 (cj-style 114번째 wire 진입 시점) / 옵션 (b) Phase 13 bmad-dev-story atomic wire T1~T8 진입 (cj-style 115번째) / 옵션 (c) Phase 13 close-out retro 진입 (cj-style 116번째) / 옵션 (d) Epic 18+ 진입 / 옵션 (e) D-DEFER-* follow-up 결정 wire 보류 결정 wire.

## F30. FinOps Optimization & Rightsizing (Phase 14 territory 신규)

> **§F30 결정 wire 진입** — Phase 13 close-out retro `850b4f8` (cj-style 116번째 wire entry) 직후 다음 옵션 5종 중 **사용자 권장 결정 = 옵션 (a) Phase 14+ 진입 + 옵션 (a) FinOps Optimization & Rightsizing (Recommended) (optimization definition DSL + rightsizing engine (compute instance type + storage tier + network bandwidth downsize) + idle resource detection (CPU < 5% for 30d + storage unattached + network unused EIP/NAT) + Reserved Instance / Savings Plans commitment recommendations (1-year + 3-year + break-even months calculation) + optimization accuracy tracking (precision + recall + realized savings vs projected savings) + optimization dashboard UI + audit-first INSERT 6 NEW + Capability FINOPS_OPTIMIZATION 1 NEW + dry-run mode + tests + wire scope T1~T8) = Phase 14 territory 결정 wire 진입** (AskUserQuestion 결정 verbatim bind). rationale 5종: ① Phase 13 wire `8b98030` FinOps Forecasting & Capacity Planning territory (forecast definition DSL + 4 time series models ARIMA + Prophet + LSTM + ensemble + capacity headroom analysis compute/storage/network + budget burn-rate projection + forecast accuracy tracking MAE/MAPE/RMSE + Capability FINOPS_FORECASTING_CAPACITY_PLANNING 1 NEW + dry-run mode) 의 natural backend carry-over chain = Phase 14 = FinOps Optimization & Rightsizing territory 의 natural next (capacity headroom 의 compute/storage/network saturation forecast → forward-looking rightsizing recommendation + forecast accuracy 의 MAE/MAPE/RMSE → optimization accuracy precision/recall + budget burn-rate projection 의 projected_overrun_pct → projected_savings_pct + Phase 13 forecasting 의 historical baseline → utilization baseline for idle detection + Phase 11 showback period selector 의 monthly granularity → monthly optimization cycle + Phase 12 anomaly detection 의 z-score baseline → idle detection z-score threshold) = Phase 13 FinOps territory 의 ACTIONABLE RECOMMENDATION LAYER EXTENSION 결정 (forecast → action) ② cj-style discipline 회피 위험 방지 (49~116번째 누적 68-entry-point cycle + 116번째 Phase 13 close-out retro + 115번째 atomic wire + 114번째 spec entry + 113번째 PRD entry + Phase 12 close-out retro `3354e83` 112번째 + Phase 12 atomic wire T1~T8 `f3c0e63` 111번째 + Phase 12 spec entry `8c5f374` 110번째 + Phase 12 PRD entry `344c7eb` 109번째 + Phase 11 close-out retro `80df15b` 108번째 + Phase 11 atomic wire T1~T8 `e020ad0` 107번째 + Phase 11 spec entry `82c93a8` 106번째 + Phase 11 PRD entry `16d7698` 105번째 패턴 모두 wire DONE 진입 정합 보존 후 즉시 Phase 14 진입 = 1-day atomic sprint discipline) ③ 비즈니스 우선순위 = enterprise 고객 onboarding 시 actionable cost optimization recommendations (compute rightsizing based on capacity headroom 90일 lookahead forecast + idle resource remediation + RI/SP commitment recommendations 1-year/3-year break-even calculation) territory 필수 (Phase 13 forecasting & capacity planning 의 forward-looking forecast 가 실제 비용 절감 action 으로 변환되는 territory EXTENSION) ④ Phase 13 wire `8b98030` 의 capacity headroom analysis (compute/storage/network 90일 lookahead) + Phase 11 wire `e020ad0` 의 showback period selector (current/previous/last 3/6 months/YTD) + Phase 12 wire `f3c0e63` 의 anomaly detection baseline (z-score > 3.0) + Phase 8 wire `60d4ea1` 의 cost-engine 12-period benchmark 의 자연스러운 carry-over chain = Phase 14 optimization 의 utilization baseline (last 30d + last 90d from phase_13 capacity_headroom_report) + idle detection (z-score < -2.0 from phase_12 anomaly_detection baseline) + monthly optimization cycle (Phase 11 showback period EXTENSION) + optimization accuracy tracking (precision/recall MAE/MAPE EXTENSION) 정합 ⑤ 1st release close-out retro + Epic 17 close-out retro + Phase 5 close-out retro + Phase 6 close-out retro + Phase 7 close-out retro + Phase 8 close-out retro + Phase 9 close-out retro + Phase 10 close-out retro + Phase 11 close-out retro + Phase 12 close-out retro + Phase 13 close-out retro territory verbatim 보존 + AD-22 owner-only RBAC + Epic 12 2FA 챌린지 보존 + AD-14 stack pin + NFR4 PII minimization ✅ PRESERVED + AD-41 FinOps Optimization & Rightsizing 신규 결정 ((a)~(g) 7 sub-decisions 모두 결정 wire 진입). §F30 territory = FinOps Optimization & Rightsizing 결정 wire 진입 시점에 8 ACs (§F30.1 optimization definition DSL + §F30.2 rightsizing engine + §F30.3 idle resource detection + §F30.4 RI/SP commitment recommendations + §F30.5 optimization accuracy tracking + §F30.6 optimization dashboard UI + §F30.7 Capability matrix v1.40 EXTENSION + §F30.8 dry-run + tests + wire scope T1~T8) + §8.1 M0-(w) FinOps Optimization & Rightsizing AC 신규 + §15 로드맵 Phase 14 row status 백로그 → in-progress (PRD entry DONE 진입 wire) + §부록 A A414~A418 신규 결정 표 + AD-41 FinOps Optimization & Rightsizing 신규 결정 + capability matrix v1.39 → v1.40 EXTENSION FINOPS_OPTIMIZATION 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅ (CR 12-1 L4 precedent 미러, FINOPS_FORECASTING_CAPACITY_PLANNING Phase 13 wire + FINOPS_ANOMALY_DETECTION + FINOPS_BUDGET_ALERT Phase 12 wire + FINOPS_SHOWBACK + FINOPS_CHARGEBACK Phase 11 wire + SLO_ENGINEERING Phase 10 wire + CHAOS_ENGINEERING Phase 9 wire + PERFORMANCE_TESTING Phase 8 wire + OBSERVABILITY_TRACES + OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind). CR 11-3 honest-DEFER discipline 117번째 epic 연속 정직 회복 검증 ✅ (D-1-1-DEFER-1/2/3 RESOLVED 보존 60~117번째 + D-LAUNCH-1-DEFER-1 honestly preserved 65~117번째 + D-EPIC-16-REVIEW-DEFER-2~6 ✅ RESOLVED 보존 78~117번째 + D-PHASE-4-DR-DEFER-1/2 ✅ RESOLVED 보존 73~117번째 + D-EPIC-17-WIRE-DEFER-T2-T3-UI ✅ RESOLVED 보존 83~117번째 + D-RETENTION-1 ✅ RESOLVED 보존 85~117번째 + D-OBSERVABILITY-1 ✅ RESOLVED 보존 89~117번째 + D-PERFORMANCE-1 ✅ RESOLVED 보존 93~117번째 + D-CHAOS-1 ✅ RESOLVED 보존 100~117번째 + D-SLO-1 ✅ RESOLVED 보존 101~117번째 + D-FINOPS-1 ✅ RESOLVED 보존 105~117번째 + D-FINOPS-2 ✅ RESOLVED 보존 109~117번째 + D-FINOPS-3 ✅ RESOLVED 보존 113~117번째 + **D-FINOPS-4 신규 honestly DEFER 보존 = Phase 14 PRD entry 진입 시점에 carry-over chain 정직 회복 결정 wire 진입: Phase 13 close-out retro `850b4f8` §13 + Phase 12 close-out retro `3354e83` §13 + Phase 11 close-out retro `80df15b` §12 + Phase 10 close-out retro `733d428` §10 + Phase 9 close-out retro `634427d` §10 + Phase 8 close-out retro `ab495a8` §10 + Phase 7 close-out retro `326fa9f` §10 + Phase 6 close-out retro `f9f006c` §13 + Epic 17 close-out retro `be8f3bd` §11 "FinOps Optimization & Rightsizing 결정 wire 보류, Phase 14+ 진입 시점" verbatim 해소**).

### F30.1 optimization definition DSL (12 sub-ACs, A414 결정)

- **F30.1.1** optimization definition DSL syntax 결정 wire = `define_optimization(tenant_id, resource_type, optimization_strategy, target_metric, baseline_period)` builder + AST 5 levels (tenant_id + resource_type + optimization_strategy + target_metric + baseline_period) + parser 검증 3 layer (syntax + semantic + tenant-scope RLS) 결정 wire.
- **F30.1.2** 5 resource_type 옵션 결정 wire = (1) compute (EC2 instance / GCP VM / Azure VM 타입) / (2) storage (S3 / GCS / Blob storage tier) / (3) database (RDS / Cloud SQL / Azure DB instance class) / (4) network (EIP / NAT gateway / Load balancer bandwidth) / (5) container (EKS / GKE / AKS node group) 결정 wire + resource_type validation 결정 wire (tenant_settings.resource_inventory JSONB 기반 + Phase 13 wire `8b98030` capacity_headroom_report dimension EXTENSION 정합).
- **F30.1.3** optimization_strategy enum 결정 wire = (1) rightsize_down (instance type 한 단계 downsize 권고) / (2) rightsize_up (instance type 한 단계 upsize 권고, performance 회피 목적) / (3) idle_terminate (30일간 CPU < 5% 자원 terminate 권고) / (4) commit_1y (1-year RI/SP commitment 권고) / (5) commit_3y (3-year RI/SP commitment 권고) / (6) storage_tier_down (storage class infrequent access / archive tier 전환 권고) 결정 wire + optimization_strategy default = composite (4 strategy 자동 선택) + per-tenant override JSONB 결정 wire.
- **F30.1.4** target_metric enum 결정 wire = (1) cost_saving_pct (예상 비용 절감률, default 20%) / (2) cost_saving_amount (절감액, KRW) / (3) utilization_target (목표 활용률, default 70%) / (4) commit_break_even_months (1y/3y break-even 시점, default 1y=8mo / 3y=18mo) 결정 wire + per-tenant override JSONB 결정 wire.
- **F30.1.5** baseline_period enum 결정 wire = (1) last_7d (last 7 days utilization baseline) / (2) last_30d (last 30 days, default) / (3) last_90d (last 90 days, 장기 분석) / (4) last_180d (last 180 days, half-year 패턴) / (5) last_365d (last 365 days, full-year 패턴) 결정 wire + baseline_period default 30d + per-tenant override JSONB 결정 wire.
- **F30.1.6** optimization_defaults constants 결정 wire = `OPTIMIZATION_DEFAULTS = {'resource_type': 'compute', 'optimization_strategy': 'composite', 'target_metric': 'cost_saving_pct', 'baseline_period': 'last_30d', 'idle_cpu_threshold_pct': 5.0, 'idle_detection_window_days': 30, 'min_savings_amount_krw': 10000, 'commit_break_even_months_1y': 8, 'commit_break_even_months_3y': 18}` constants + `OPTIMIZATION_RESOURCE_TYPES = ['compute', 'storage', 'database', 'network', 'container']` constants + `OPTIMIZATION_STRATEGIES = ['rightsize_down', 'rightsize_up', 'idle_terminate', 'commit_1y', 'commit_3y', 'storage_tier_down', 'composite']` constants 결정 wire.
- **F30.1.7** 4 industries baseline 결정 wire = manufacturing + service + manufacturing_service + manufacturing_service_other 모두 industry-agnostic 4-industry grants ✅/✅/✅/✅ (CR 12-1 L4 precedent 미러, Phase 11/12/13 wire 정합) + per-tenant override EXTENSION + policy evaluation precedence tenant override > industry baseline > system default 결정 wire.
- **F30.1.8** `apps/api/modules/finops/optimization_definition.py` NEW ~+150 LOC + `parse_optimization_definition(definition_text)` parser + `validate_optimization_scope(tenant_id, resource_type)` validator + `OptimizationDefinition` TypedDict 11 fields (optimization_id + tenant_id + resource_type + optimization_strategy + target_metric + baseline_period + idle_cpu_threshold_pct + min_savings_amount_krw + status enum active/paused/expired + created_at + updated_at) + 5 layer defense (syntax + semantic + tenant-scope RLS + resource_type validation + inventory data availability) 결정 wire.
- **F30.1.9** audit-first INSERT `optimization_definition_updated` CR 1-1 verbatim 결정 wire = action_class='FINOPS_OPTIMIZATION' (NEW) + action='optimization_definition_updated' (NEW) + actor_id + tenant_id + optimization_id + resource_type + optimization_strategy + target_metric + baseline_period + trace_id 결정 wire + ActionClass.FINOPS_OPTIMIZATION 신규 정의 + apps/api/core/audit_action.py MODIFIED AuditAction Literal EXTENSION 1 NEW value + _ActionRegistry FINOPS_OPTIMIZATION entry 신규 1개 등록 + __all__ EXTENSION 결정 wire.
- **F30.1.10** typed exception envelope CR 12-5 D-14 결정 wire = `OptimizationDefinitionInvalidError(400)` + `OptimizationScopeInvalidError(404)` + `OptimizationInventoryUnavailableError(422)` 3 NEW typed exception classes + envelope `{error_code, message_ko, trace_id, timestamp, details}` 결정 wire.
- **F30.1.11** RLS 자동 적용 CR 0-2 verbatim 결정 wire = phase_14_finops_optimization tables 의 `tenant_id = current_setting('app.tenant_id')::uuid` policy + multi-tenant isolation 검증 + 테스트 결정 wire (Phase 13 wire `8b98030` 의 phase_13_finops_forecast RLS pattern verbatim 미러).
- **F30.1.12** dry-run mode `--finops-optimization-dry-run` CLI flag 결정 wire = optimization definition 검증 + 결과 preview + actual `optimization_definition_updated` audit-first INSERT skip + dry-run 결과 preview `phase_14_finops_optimization_preview` table 결정 wire.

### F30.2 rightsizing engine (12 sub-ACs, A415 결정)

- **F30.2.1** `apps/api/modules/finops/rightsizing_engine.py` NEW ~+200 LOC + `recommend_rightsizing(tenant_id, resource_type, baseline_period) -> List[RightsizingRecommendation]` 결정 wire + Phase 13 wire `8b98030` capacity_headroom_report 의 compute/storage/network saturation 90일 lookahead forecast EXTENSION + instance type 한 단계 downsize/upsize 권고 + per-resource savings calculation 결정 wire.
- **F30.2.2** compute rightsizing 결정 wire = EC2 instance type 한 단계 downsize (예: m5.2xlarge → m5.xlarge) or upsize (성능 회피 목적) + Phase 13 wire `8b98030` LSTM model 의 CPU utilization 90일 lookahead forecast 기반 max expected utilization < 70% → downsize 권고 결정 wire + max_expected_utilization_pct = max(forecast_p50, forecast_p99 × 1.1) 결정 wire + 4 instance family mapping (general_purpose + compute_optimized + memory_optimized + storage_optimized) 결정 wire.
- **F30.2.3** storage rightsizing 결정 wire = S3 Standard → S3 Standard-IA (30일 access < 1회) or S3 Glacier (90일 access < 1회) tier downgrade 권고 + Phase 13 wire `8b98030` Prophet model 의 storage growth 90일 lookahead forecast EXTENSION + access_pattern enum frequent/infrequent/rare 결정 wire + 3 storage tier mapping (Standard + Standard-IA + Glacier) 결정 wire.
- **F30.2.4** database rightsizing 결정 wire = RDS instance class 한 단계 downsize (예: db.r5.2xlarge → db.r5.xlarge) + connection_count_p95 + CPU utilization_p95 + memory utilization_p95 3 metric 기반 권고 결정 wire + Phase 13 wire `8b98030` capacity_headroom_report database saturation EXTENSION 정합.
- **F30.2.5** network rightsizing 결정 wire = EIP (Elastic IP) attached 여부 검증 + unattached EIP release 권고 + NAT gateway bandwidth 90일 max utilization < 10% → NAT gateway 제거 권고 + Load balancer request count_p95 < 100/일 → ALB 제거 권고 결정 wire + Phase 13 wire `8b98030` network saturation forecast EXTENSION 정합.
- **F30.2.6** container rightsizing 결정 wire = EKS node group desired instance count 조정 권고 + CPU request/limit 합리화 권고 (Phase 7 wire `59b56cd` 의 business_calculations_total EXTENSION) + memory request/limit 합리화 권고 + per-pod resource utilization 분석 결정 wire + Phase 13 wire `8b98030` capacity_headroom_report container dimension EXTENSION 정합.
- **F30.2.7** RightsizingRecommendation TypedDict 14 fields 결정 wire = (1) recommendation_id UUID PK + (2) tenant_id UUID + (3) resource_id TEXT (resource ARN or ID) + (4) resource_type enum compute/storage/database/network/container + (5) current_instance_type TEXT + (6) recommended_instance_type TEXT + (7) current_cost_krw NUMERIC(20, 2) per month + (8) recommended_cost_krw NUMERIC(20, 2) per month + (9) projected_savings_pct NUMERIC(8, 4) + (10) projected_savings_amount_krw NUMERIC(20, 2) per month + (11) confidence_score NUMERIC(8, 4) (Phase 13 forecast accuracy EXTENSION) + (12) recommendation_severity enum low/medium/high + (13) model_version TEXT + (14) generated_at TIMESTAMPTZ + (15) trace_id TEXT 결정 wire.
- **F30.2.8** instance type mapping table 결정 wire = `INSTANCE_TYPE_DOWNGRADE_MAP` (80+ AWS EC2 instance type mapping, m5.large → t3.large, m5.xlarge → m5.large, m5.2xlarge → m5.xlarge, etc.) + `INSTANCE_TYPE_UPGRADE_MAP` (reverse mapping) + per-family mapping (general_purpose + compute_optimized + memory_optimized + storage_optimized) 결정 wire + GCP/Azure mapping JSONB 결정 wire.
- **F30.2.9** projected_savings calculation 결정 wire = `projected_savings_krw_per_month = current_cost_krw - recommended_cost_krw` + annualized `projected_savings_krw_per_year = projected_savings_krw_per_month × 12` + Phase 11 wire `e020ad0` chargeback cost allocation EXTENSION + Phase 12 wire `f3c0e63` budget_definition EXTENSION 정합.
- **F30.2.10** confidence_score calculation 결정 wire = Phase 13 wire `8b98030` forecast_accuracy 의 MAPE 기반 confidence_score 결정 wire + confidence_score = (1 - normalized_mape) × 100 + confidence_score < 70% → low severity + 70~90% → medium + ≥ 90% → high 결정 wire + Phase 13 model_retraining_triggered EXTENSION 정합.
- **F30.2.11** audit-first INSERT `recommendation_generated` CR 1-1 verbatim 결정 wire = action_class='FINOPS_OPTIMIZATION' + action='recommendation_generated' (NEW) + actor_id='system' + tenant_id + recommendation_id + resource_type + current_instance_type + recommended_instance_type + projected_savings_amount_krw + confidence_score + trace_id 결정 wire + AuditAction Literal EXTENSION 1 NEW value 결정 wire.
- **F30.2.12** typed exception envelope CR 12-5 D-14 결정 wire = `RightsizingEngineError(500)` + `InstanceTypeMappingError(500)` + `RecommendationConfidenceLowError(422)` 3 NEW typed exception classes + envelope 결정 wire.

### F30.3 idle resource detection (12 sub-ACs, A416 결정)

- **F30.3.1** `apps/api/modules/finops/idle_resource_detector.py` NEW ~+180 LOC + `detect_idle_resources(tenant_id, idle_cpu_threshold_pct, idle_window_days) -> List[IdleResource]` 결정 wire + Phase 12 wire `f3c0e63` anomaly_detection 의 z-score < -2.0 EXTENSION + Phase 13 wire `8b98030` capacity_headroom_report 의 last 30d utilization EXTENSION 결정 wire.
- **F30.3.2** compute idle detection 결정 wire = CPU utilization_p95 < idle_cpu_threshold_pct (default 5%) for idle_window_days (default 30) 일관된 → idle compute resource classify 결정 wire + network_in_bytes_p95 < 1MB/day for 30d → idle network classify 결정 wire + memory utilization_p95 < 10% for 30d → low utilization classify 결정 wire + Phase 12 wire `f3c0e63` anomaly_detection 의 z-score < -2.0 EXTENSION 정합.
- **F30.3.3** storage idle detection 결정 wire = S3 bucket last_accessed_at > idle_window_days → idle storage classify 결정 wire + S3 bucket size < 1GB + last_accessed_at > 30d → cleanup candidate 결정 wire + EBS volume attached=false for 7d → unattached EBS classify 결정 wire + snapshot created_at > 90d + size > 100GB → stale snapshot classify 결정 wire.
- **F30.3.4** database idle detection 결정 wire = RDS instance connection_count_p95 = 0 for 30d → idle RDS classify 결정 wire + connection_count_p95 < 5 for 30d → low utilization RDS classify 결정 wire + snapshot created_at > 90d + storage < 100GB → stale RDS snapshot classify 결정 wire + Phase 6 wire `24e1cd7` audit_log_archive EXTENSION 정합.
- **F30.3.5** network idle detection 결정 wire = EIP associated=false for 7d → idle EIP classify 결정 wire + NAT gateway bytes_out_p95 = 0 for 30d → idle NAT gateway classify 결정 wire + Load balancer request_count_p95 < 100/day for 30d → idle load balancer classify 결정 wire + VPC endpoint unused for 30d → idle VPC endpoint classify 결정 wire.
- **F30.3.6** container idle detection 결정 wire = EKS node group desired_count × max_utilization_p95 < 30% → downsize 권고 결정 wire + pod CPU request/limit 합리화 분석 (Phase 7 wire `59b56cd` business_calculations_total EXTENSION) + pod memory request/limit 합리화 분석 + per-deployment resource utilization 30d 분석 결정 wire.
- **F30.3.7** IdleResource TypedDict 13 fields 결정 wire = (1) idle_resource_id UUID PK + (2) tenant_id UUID + (3) resource_id TEXT (resource ARN or ID) + (4) resource_type enum compute/storage/database/network/container + (5) idle_reason TEXT (low_cpu + low_network + unattached + zero_connections + low_request_count) + (6) idle_duration_days int + (7) current_cost_krw_per_month NUMERIC(20, 2) + (8) potential_savings_krw_per_month NUMERIC(20, 2) + (9) idle_severity enum low/medium/high + (10) action enum terminate/downsize/review + (11) detection_method enum z_score/threshold/heuristic + (12) detection_window_days int + (13) generated_at TIMESTAMPTZ + (14) trace_id TEXT 결정 wire.
- **F30.3.8** z-score idle detection 결정 wire = Phase 12 wire `f3c0e63` anomaly_detection 의 z-score EXTENSION + z_score = (utilization_p95 - mean_utilization_30d) / std_utilization_30d + z_score < -2.0 → idle classify + Phase 12 wire 의 multi-method voting consensus (3 of 4 methods agree) EXTENSION 결정 wire.
- **F30.3.9** severity classification 결정 wire = potential_savings_krw_per_month < 10000 KRW → low severity / 10000~100000 → medium / ≥ 100000 → high 결정 wire + per-tenant override severity_threshold_krw default 10000 결정 wire + severity_threshold_per_tenant override JSONB 결정 wire.
- **F30.3.10** action recommendation 결정 wire = idle_severity=low → action='review' (수동 검토 권고) + medium → 'downsize' (축소 권고) + high → 'terminate' (종료 권고) 결정 wire + per-tenant override action_policy (자동 terminate 허용 tenant 명시) 결정 wire + 자동 terminate 시 Epic 12 2FA 챌린지 보존 결정 wire.
- **F30.3.11** audit-first INSERT `idle_resource_detected` CR 1-1 verbatim 결정 wire = action_class='FINOPS_OPTIMIZATION' + action='idle_resource_detected' (NEW) + actor_id='system' + tenant_id + idle_resource_id + resource_type + idle_reason + potential_savings_krw_per_month + detection_method + trace_id 결정 wire + AuditAction Literal EXTENSION 1 NEW value 결정 wire.
- **F30.3.12** typed exception envelope CR 12-5 D-14 결정 wire = `IdleResourceDetectionError(500)` + `IdleSeverityClassificationError(500)` + `IdleMetricUnavailableError(404)` 3 NEW typed exception classes + envelope 결정 wire.

### F30.4 RI / SP commitment recommendations (12 sub-ACs, A417 결정)

- **F30.4.1** `apps/api/modules/finops/commitment_recommender.py` NEW ~+150 LOC + `recommend_commitments(tenant_id, baseline_period) -> List[CommitmentRecommendation]` 결정 wire + 1-year / 3-year Reserved Instance (RI) / Savings Plans (SP) commitment 권고 + break-even months calculation + projected savings calculation 결정 wire + Phase 13 wire `8b98030` forecast 의 12-month baseline EXTENSION.
- **F30.4.2** commitment_type enum 결정 wire = (1) ec2_ri (EC2 Reserved Instance, standard/convertible) / (2) rds_ri (RDS Reserved Instance) / (3) ec2_sp (EC2 Savings Plans, Compute Savings Plans) / (4) s3_sp (S3 Storage Savings Plans) / (5) redshift_sp (Redshift Reserved Instance) / (6) dynamodb_sp (DynamoDB Reserved Capacity) 결정 wire + per-tenant override commitment_type_allowlist JSONB 결정 wire.
- **F30.4.3** commitment_term enum 결정 wire = (1) 1_year (1-year term, default break-even 8 months) / (2) 3_year (3-year term, default break-even 18 months) 결정 wire + commitment_term default = composite (1y/3y 비교 후 최적) + per-tenant override JSONB 결정 wire.
- **F30.4.4** break-even calculation 결정 wire = `break_even_months = upfront_cost / monthly_savings` + 1-year commitment 시 break-even_months ≤ 8mo 이면 commit_1y 권고 + 3-year commitment 시 break_even_months ≤ 18mo 이면 commit_3y 권고 + 그 외 → on-demand 유지 권고 결정 wire + break_even calculation Phase 13 forecast accuracy EXTENSION 정합.
- **F30.4.5** utilization_forecast 기반 권고 결정 wire = Phase 13 wire `8b98030` forecast_engine 의 12-month forward forecast 활용 + forecast_p50 >= on_demand_cost × 0.7 → RI/SP 권고 + forecast_p50 < on_demand_cost × 0.5 → on-demand 유지 권고 + forecast_p50 변동성 std/mean > 30% → 1y 권고 (3y 변동성 위험 회피) 결정 wire.
- **F30.4.6** ROI calculation 결정 wire = `roi_pct = (total_3y_savings - upfront_cost) / upfront_cost × 100` + ROI > 100% → high severity / 50~100% → medium / < 50% → low 결정 wire + per-tenant override roi_threshold_pct default 50% 결정 wire.
- **F30.4.7** CommitmentRecommendation TypedDict 12 fields 결정 wire = (1) recommendation_id UUID PK + (2) tenant_id UUID + (3) commitment_type enum + (4) commitment_term enum 1_year/3_year + (5) resource_pattern TEXT (instance type pattern e.g. m5.large) + (6) current_on_demand_cost_krw_per_month NUMERIC(20, 2) + (7) projected_commit_cost_krw_per_month NUMERIC(20, 2) + (8) projected_savings_pct NUMERIC(8, 4) + (9) projected_savings_krw NUMERIC(20, 2) over commitment_term + (10) upfront_cost_krw NUMERIC(20, 2) + (11) break_even_months int + (12) roi_pct NUMERIC(8, 4) + (13) recommendation_severity enum low/medium/high + (14) generated_at TIMESTAMPTZ + (15) trace_id TEXT 결정 wire.
- **F30.4.8** pricing data source 결정 wire = AWS Pricing API 의 on-demand price + RI/SP discount rate (1y ~40% discount + 3y ~60% discount) + Phase 8 wire `60d4ea1` 의 cost-engine 12-period benchmark EXTENSION + Phase 13 wire `8b98030` forecast 의 12-month baseline pricing source EXTENSION 결정 wire.
- **F30.4.9** commitment simulation 결정 wire = 1y / 3y 두 가지 시나리오 동시 계산 + 더 높은 projected_savings_pct scenario 권고 + 단, break_even_months 제약 적용 (1y ≤ 8mo / 3y ≤ 18mo) + on-demand 유지 옵션 fallback 결정 wire + Phase 11 wire `e020ad0` chargeback 의 markup scenario EXTENSION 정합.
- **F30.4.10** per-tenant override JSONB 결정 wire = `tenant_settings.commitment_overrides` TypedDict 10 fields (commitment_type_allowlist + commitment_term_default + break_even_threshold_1y_months default 8 + break_even_threshold_3y_months default 18 + roi_threshold_pct default 50 + upfront_cost_budget_krw default 1000000 + min_monthly_savings_krw default 10000 + auto_apply_enabled default false + approval_required default true + notification_recipients) 결정 wire + policy evaluation precedence tenant override > industry baseline > system default.
- **F30.4.11** audit-first INSERT `commitment_recommended` CR 1-1 verbatim 결정 wire = action_class='FINOPS_OPTIMIZATION' + action='commitment_recommended' (NEW) + actor_id='system' + tenant_id + recommendation_id + commitment_type + commitment_term + projected_savings_pct + break_even_months + trace_id 결정 wire + AuditAction Literal EXTENSION 1 NEW value 결정 wire + 자동 apply 시 Epic 12 2FA 챌린지 보존 결정 wire.
- **F30.4.12** typed exception envelope CR 12-5 D-14 결정 wire = `CommitmentRecommendationError(500)` + `PricingDataUnavailableError(404)` + `BreakEvenCalculationError(500)` 3 NEW typed exception classes + envelope 결정 wire.

### F30.5 optimization accuracy tracking (12 sub-ACs, A418 결정)

- **F30.5.1** `apps/api/modules/finops/optimization_accuracy_tracker.py` NEW ~+120 LOC 결정 wire + Phase 13 wire `8b98030` forecast_accuracy_tracker EXTENSION + per-(tenant_id + resource_type + optimization_strategy) granularity + optimization applied vs rejected tracking 결정 wire.
- **F30.5.2** precision (정밀도) 결정 wire = TP / (TP + FP) = applied recommendations 중 actual savings >= 50% of projected savings 인 비율 + precision ≥ 80% → high accuracy / 60~80% → moderate / < 60% → low accuracy 결정 wire.
- **F30.5.3** recall (재현율) 결정 wire = TP / (TP + FN) = applicable opportunities 중 recommendations 이 cover 한 비율 + recall ≥ 80% → high coverage / 60~80% → moderate / < 60% → low coverage 결정 wire.
- **F30.5.4** realized_savings tracking 결정 wire = `realized_savings_krw_per_month = sum(actual_cost_reduction after applying recommendation)` + Phase 11 wire `e020ad0` showback 의 period selector EXTENSION (current/previous month 비교) + per-(tenant_id + resource_type + optimization_strategy) granularity 결정 wire.
- **F30.5.5** projected_vs_realized_savings 결정 wire = `accuracy_score = realized_savings / projected_savings × 100` (단위 %) + accuracy_score ≥ 80% → high accuracy / 50~80% → moderate / < 50% → low accuracy 결정 wire + per-tenant override accuracy_threshold default 70% 결정 wire.
- **F30.5.6** false_positive tracking 결정 wire = recommendation apply 후 actual savings < 50% projected savings → false positive + `false_positive_reasons` enum (overestimation + performance_degradation + business_growth + seasonality_mismatch + application_change) 결정 wire + Phase 12 wire `f3c0e63` anomaly_detection 의 false_positive EXTENSION 정합.
- **F30.5.7** false_negative tracking 결정 wire = applicable opportunity detection miss → false negative + Phase 13 wire `8b98030` capacity_headroom_report 의 utilization baseline EXTENSION 정합 + idle detection miss EXTENSION + commit opportunity miss EXTENSION 결정 wire.
- **F30.5.8** OptimizationAccuracyReport TypedDict 10 fields 결정 wire = (1) report_id UUID PK + (2) tenant_id UUID + (3) resource_type enum + (4) optimization_strategy enum + (5) total_recommendations int + (6) applied_recommendations int + (7) precision NUMERIC(8, 4) + (8) recall NUMERIC(8, 4) + (9) realized_savings_krw NUMERIC(20, 2) + (10) projected_savings_krw NUMERIC(20, 2) + (11) accuracy_score NUMERIC(8, 4) + (12) generated_at TIMESTAMPTZ + (13) trace_id TEXT 결정 wire.
- **F30.5.9** model_performance_degradation_detection 결정 wire = accuracy_score < 70% for 3 consecutive months → recommendation engine retraining trigger + Phase 13 wire `8b98030` model_retraining_triggered EXTENSION 결정 wire + retraining cron KST 매주 일요일 04:00 UTC 19:00 (Phase 12 anomaly detection isolation forest retraining cron EXTENSION + Phase 13 forecast retraining cron `0 3 * * 0` EXTENSION 정합) 결정 wire.
- **F30.5.10** recommended_action audit-first INSERT EXTENSION 결정 wire = `optimization_recommended_action` 1 NEW action + action_class='FINOPS_OPTIMIZATION' + actor_id + tenant_id + resource_id + recommendation_type + projected_savings_krw + applied boolean + trace_id 결정 wire + ActionClass.FINOPS_OPTIMIZATION entry EXTENSION 결정 wire.
- **F30.5.11** model_version tracking 결정 wire = rightsizing_engine + idle_resource_detector + commitment_recommender 3 engine 모두 semantic versioning MAJOR.MINOR.PATCH + model_version JSONB metadata (training_date + training_data_window + training_samples_count + algorithm_hyperparameters + cross_validation_score) + Phase 13 wire `8b98030` forecast_model_registry EXTENSION 결정 wire.
- **F30.5.12** typed exception envelope CR 12-5 D-14 결정 wire = `OptimizationAccuracyTrackingError(500)` + `OptimizationRetrainingTriggerError(500)` + `OptimizationPerformanceDegradationError(500)` 3 NEW typed exception classes + envelope 결정 wire.

### F30.6 optimization dashboard UI (10 sub-ACs, A419 결정)

- **F30.6.1** `apps/web/app/[locale]/(dashboard)/admin/finops/optimization/page.tsx` NEW ~+150 LOC + 5 components (`OptimizationStrategySelector` + `RightsizingRecommendationTable` + `IdleResourcePanel` + `CommitmentRecommendationPanel` + `OptimizationAccuracyPanel`) 결정 wire + owner-only RBAC AD-22 verbatim + Epic 12 2FA 챌린지 보존 결정 wire.
- **F30.6.2** OptimizationStrategySelector component 결정 wire = 7 strategy 옵션 radio button (rightsize_down + rightsize_up + idle_terminate + commit_1y + commit_3y + storage_tier_down + composite) + 5 resource_type 옵션 (compute + storage + database + network + container) + 5 baseline_period 옵션 (last_7d + last_30d + last_90d + last_180d + last_365d) + owner-only ack prompt AD-22 verbatim 결정 wire.
- **F30.6.3** RightsizingRecommendationTable component 결정 wire = Phase 13 wire `8b98030` forecast 기반 권고 표시 + 14 columns (recommendation_id + resource_id + resource_type + current_instance_type + recommended_instance_type + current_cost_krw + recommended_cost_krw + projected_savings_pct + projected_savings_amount + confidence_score + severity + apply button + reject button) + apply 시 Epic 12 2FA 챌린지 + audit-first INSERT `recommendation_generated` CR 1-1 verbatim + Recharts 2.12.7 AD-14 stack pin 결정 wire.
- **F30.6.4** IdleResourcePanel component 결정 wire = Phase 12 wire `f3c0e63` anomaly detection z-score 기반 idle 표시 + 13 columns (idle_resource_id + resource_id + resource_type + idle_reason + idle_duration_days + current_cost_krw_per_month + potential_savings_krw_per_month + idle_severity + action + detection_method + terminate button + downsize button) + severity color coding (low=blue + medium=yellow + high=red) + RTL render discipline CR 11-4 D-003 verbatim 결정 wire.
- **F30.6.5** CommitmentRecommendationPanel component 결정 wire = Phase 13 wire `8b98030` forecast 12-month baseline 기반 RI/SP 권고 표시 + 12 columns (recommendation_id + commitment_type + commitment_term + resource_pattern + current_on_demand_cost + projected_commit_cost + projected_savings_pct + break_even_months + roi_pct + severity + apply button + simulation toggle) + 1y/3y 비교 차트 Recharts 2.12.7 AD-14 stack pin 결정 wire.
- **F30.6.6** ko-KR.json `finops_optimization.*` namespace EXTENSION ~30 keys 결정 wire (CR 11-4 D-002 verbatim SSOT) + `finops_optimization.strategy.rightsize_down` + `finops_optimization.strategy.idle_terminate` + `finops_optimization.strategy.commit_1y` + `finops_optimization.resource.compute` + `finops_optimization.resource.storage` + `finops_optimization.resource.database` + `finops_optimization.resource.network` + `finops_optimization.resource.container` + `finops_optimization.severity.low` + `finops_optimization.severity.medium` + `finops_optimization.severity.high` + `finops_optimization.action.terminate` + `finops_optimization.action.downsize` + `finops_optimization.action.review` 등 결정 wire.
- **F30.6.7** ARIA labels 결정 wire = WCAG 2.1 AA compliance (Epic 12 의 2FA 챌린지 UI 와 동일 표준) + keyboard navigation (Tab + Enter + Arrow keys) + screen reader 지원 결정 wire + Phase 14 Epic 1 UX v1.0 locked decision 결정 wire (Dark MVP / WCAG AA / Professional / ko-KR) verbatim 보존 결정 wire.
- **F30.6.8** toast notification 결정 wire = recommendation_generated 시 toast 자동 표시 (severity=low → blue + 5초 / medium → yellow + 10초 / high → red + 15초) + idle_resource_detected 시 toast (severity 별 색상 + duration) + commitment_recommended 시 toast 결정 wire.
- **F30.6.9** Vitest RTL render discipline CR 11-4 D-003 verbatim 결정 wire = 6 NEW vitest cases (OptimizationStrategySelector 1 + RightsizingRecommendationTable 1 + IdleResourcePanel 1 + CommitmentRecommendationPanel 1 + ko-KR SSOT 2) 결정 wire.
- **F30.6.10** FinOps optimization dashboard parity CR 12-5 D-PARITY-01 결정 wire = TS mirror `apps/web/lib/finops-optimization/finops-optimization-client.ts` NEW ~+150 LOC + `tests/web/test_finops_optimization_dashboard_parity.py` NEW ~+10 cases + 1-line ko-KR reject 결정 wire.

### F30.7 Capability matrix v1.40 EXTENSION FINOPS_OPTIMIZATION (12 sub-ACs, A420 결정)

- **F30.7.1** capability matrix v1.39 → v1.40 EXTENSION 결정 wire = FINOPS_OPTIMIZATION 1 NEW row + docs/capability-matrix.md MODIFIED + version 표기 결정 wire.
- **F30.7.2** `Capability.FINOPS_OPTIMIZATION = "finops_optimization"` 1 NEW enum 결정 wire + apps/api/core/capability.py MODIFIED + CR 12-1 L4 precedent 미러 결정 wire.
- **F30.7.3** `apps/api/dependencies/capability.py` MODIFIED 결정 wire = `require_finops_optimization` 1 NEW dep + Phase 13 wire `8b98030` 의 `require_finops_forecasting_capacity_planning` + Phase 12 wire `f3c0e63` 의 `require_finops_anomaly_detection` + `require_finops_budget_alert` + Phase 11 wire `e020ad0` 의 `require_finops_showback` + `require_finops_chargeback` pattern verbatim 미러 결정 wire.
- **F30.7.4** industry-agnostic 4-industry grants ✅/✅/✅/✅ 결정 wire = manufacturing + service + manufacturing_service + manufacturing_service_other 모두 허용 (CR 12-1 L4 precedent 미러, FINOPS_FORECASTING_CAPACITY_PLANNING Phase 13 wire + FINOPS_ANOMALY_DETECTION + FINOPS_BUDGET_ALERT Phase 12 wire + FINOPS_SHOWBACK + FINOPS_CHARGEBACK Phase 11 wire pattern verbatim bind) 결정 wire.
- **F30.7.5** 미허용 tenant 의 optimization 진입 차단 결정 wire = `require_finops_optimization` dependency 의 미허용 tenant 진입 차단 검증 + 403 Forbidden + `FORBIDDEN_KO` message ("FinOps Optimization & Rightsizing capability 미허용 tenant") + Epic 12 2FA 챌린지 보존 + AD-22 owner-only RBAC 정합 결정 wire.
- **F30.7.6** optimization 의 phase_13 forecast_accuracy + phase_13 capacity_headroom + phase_13 forecast + phase_12 anomaly_detection + phase_11 showback baseline carry-over 진입 차단 결정 wire = optimization 진입 시 phase_13 FINOPS_FORECASTING_CAPACITY_PLANNING capability 도 동시에 검증 + optimization 가 FINOPS_OPTIMIZATION 만 있고 FINOPS_FORECASTING_CAPACITY_PLANNING 없는 경우 403 Forbidden 결정 wire.
- **F30.7.7** drift detector `tests/integration/test_capability_matrix_v1_40_drift.py` NEW 결정 wire = 8 NEW pytest cases (FINOPS_OPTIMIZATION row 존재 1 + 4-industry grants 1 + Capability enum 1 + require_finops_optimization 1 + 미허용 tenant 차단 1 + phase_13 carry-over 검증 2 + phase_11/12 carry-over 검증 1) 결정 wire + Phase 13 wire `8b98030` 의 `tests/integration/test_capability_matrix_v1_39_drift.py` 패턴 verbatim 미러 결정 wire.
- **F30.7.8** m22_finops_optimization 결정 wire = `apps/api/modules/finops/optimization/__init__.py` NEW 결정 wire + `apps/api/modules/finops/optimization/serializers.py` NEW 결정 wire (m21_finops_forecast 의 Phase 13 wire EXTENSION pattern verbatim 미러) 결정 wire + module version 결정 wire (m22_finops_optimization 결정 wire + Phase 13 wire 의 m21_finops_forecast 와 동일 pattern).
- **F30.7.9** SSOT RED→GREEN EXTENSION 결정 wire = capability matrix v1.40 신규 1 row + capability.py EXTENSION 1 NEW enum + require_finops_optimization 1 NEW dep + drift detector + A36 SDR 검증 4-step 자동 적용 + CR 12-5 D-GATE-01 inversion 적용 보존 결정 wire.
- **F30.7.10** CR 12-1 L4 industry-agnostic capability 결정 wire = FINOPS_OPTIMIZATION industry-agnostic 4-industry grants ✅/✅/✅/✅ 결정 wire (manufacturing + service + manufacturing_service + manufacturing_service_other 모두 허용) + Phase 13 wire 의 FINOPS_FORECASTING_CAPACITY_PLANNING industry-agnostic 4-industry grants pattern verbatim 미러 결정 wire.
- **F30.7.11** capability gate 의 fail-closed 결정 wire = 미허용 tenant 의 optimization 진입 시 403 Forbidden 결정 wire + capability matrix v1.40 row 부재 시 fail-closed 결정 wire + `Capability.FINOPS_OPTIMIZATION` enum 부재 시 fail-closed 결정 wire + AD-22 owner-only RBAC 정합 + Epic 12 2FA 챌린지 보존 + NFR4 PII minimization 정합 보존 결정 wire.
- **F30.7.12** capability matrix 의 version 결정 wire = v1.39 → v1.40 EXTENSION 결정 wire + version bump 일관성 결정 wire (Phase 13 wire 의 capability matrix v1.38 → v1.39 pattern verbatim 미러) + docs/capability-matrix.md 의 version 표기 결정 wire + sprint-status.yaml 의 capability version 결정 wire + drift detector 의 version 매칭 검증 결정 wire + CR 12-5 D-PARITY-01 inversion 적용 보존 결정 wire.

### F30.8 dry-run + Tests + wire scope T1~T8 (12 sub-ACs)

- **F30.8.1** dry-run mode 결정 wire = `--finops-optimization-dry-run` + `--finops-rightsizing-dry-run` + `--finops-idle-resource-dry-run` + `--finops-commitment-dry-run` CLI flags + optimization definition dry-run parameter + rightsizing dry-run parameter + idle resource dry-run parameter + commitment dry-run parameter + dry-run 시 actual `optimization_definition_updated` audit-first INSERT skip + dry-run 시 actual `recommendation_generated` audit-first INSERT skip + dry-run 시 actual `idle_resource_detected` audit-first INSERT skip + dry-run 시 actual `commitment_recommended` audit-first INSERT skip 결정 wire + dry-run 결과 preview 결정 wire (optimization_preview + rightsizing_preview + idle_resource_preview + commitment_preview 테이블 alembic 0046 신규 결정 wire).
- **F30.8.2** dry-run 의 preview 결과 결정 wire = `phase_14_finops_optimization_preview` + `phase_14_finops_rightsizing_preview` + `phase_14_finops_idle_resource_preview` + `phase_14_finops_commitment_preview` 4 table 결정 wire (preview_id UUID PK + tenant_id UUID + preview_type enum + period_key TEXT + preview_data JSONB + computed_at TIMESTAMPTZ DEFAULT NOW() + trace_id TEXT) + RLS 자동 적용 CR 0-2 verbatim 결정 wire + audit-first INSERT `optimization_dry_run_executed` CR 1-1 verbatim 결정 wire.
- **F30.8.3** dry-run 의 CLI flag 결정 wire = `--finops-optimization-dry-run` + `--finops-rightsizing-dry-run` + `--finops-idle-resource-dry-run` + `--finops-commitment-dry-run` global flags + `--optimization-definition-dry-run` + `--rightsizing-dry-run` + `--idle-resource-dry-run` + `--commitment-dry-run` sub-flags 결정 wire + dry-run 모드 시 logger 출력 결정 (stdout + INFO level) + dry-run 결과 preview 결정 wire (console table output 결정 wire).
- **F30.8.4** tests 결정 wire = ~+50 NEW pytest PASS (optimization_definition 6 + rightsizing_engine 8 + idle_resource_detector 8 + commitment_recommender 8 + optimization_accuracy_tracker 5 + alembic 0046 5 + audit action 8 + capability matrix v1.40 8 = ~+56 NEW pytest PASS) 결정 wire + optimization_definition 6 tests 결정 (parse_optimization_definition validation 3 + resource_type validation 2 + audit-first INSERT 1) + rightsizing_engine 8 tests 결정 (compute + storage + database + network + container 5 + instance type mapping 1 + projected_savings calculation 1 + audit-first INSERT 1) + idle_resource_detector 8 tests 결정 (compute + storage + database + network + container idle detection 5 + z-score detection 1 + severity classification 1 + audit-first INSERT 1) + commitment_recommender 8 tests 결정 (1y + 3y commitment 2 + break-even calculation 2 + ROI calculation 1 + utilization forecast integration 1 + per-tenant override 1 + audit-first INSERT 1) + optimization_accuracy_tracker 5 tests 결정 (precision + recall + realized_savings + projected_vs_realized + model_retraining_trigger 5) + alembic 0046 5 tests 결정 (migration + RLS + CHECK constraints + UNIQUE constraint + indexes 5) + audit action 8 tests 결정 (optimization_definition_updated + recommendation_generated + idle_resource_detected + commitment_recommended + optimization_recommended_action + optimization_accuracy_degraded + optimization_retraining_triggered + optimization_dry_run_executed 8 NEW actions + _ActionRegistry FINOPS_OPTIMIZATION entry 신규 1개 등록 + multi-tenant isolation 2) + capability matrix v1.40 8 tests 결정 (FINOPS_OPTIMIZATION row 존재 1 + 4-industry grants 1 + Capability enum 1 + require_finops_optimization 1 + 미허용 tenant 차단 1 + phase_13 carry-over 검증 2 + phase_11/12 carry-over 검증 1) 결정 wire.
- **F30.8.5** vitest tests 결정 wire = ~+6 NEW vitest PASS (OptimizationStrategySelector + RightsizingRecommendationTable owner-only ack prompt AD-22 verbatim + IdleResourcePanel RTL render + CommitmentRecommendationPanel + ko-KR SSOT 2 + finops optimization dashboard parity CR 12-5 D-PARITY-01 결정 wire) 결정 wire + Phase 13 wire 의 ~5 NEW vitest PASS pattern verbatim 미러 결정 wire.
- **F30.8.6** ruff + tsc 결정 wire = 0 NEW ruff 결정 wire (Phase 13 wire 정합) + 0 NEW tsc 결정 wire (apps/web frontend 변경 ext only — pre-existing baseline errors preserved per cj-style discipline) + SDR drift gate 결정 wire (vitest file count +1 NEW collected well within 5% tolerance + pytest +8 NEW files collected well within 5% tolerance) 결정 wire + commit_consistency gate 결정 wire (CR 9-6 commit message discipline + A36 SDR 검증 4-step 자동 적용) 결정 wire + D-DEFER-* grep guard 결정 wire (CR 11-3 honest-DEFER discipline 117번째 epic 연속 정직 회복 검증 보존) 결정 wire.
- **F30.8.7** wire scope T1~T8 결정 wire = T1 optimization_definition + optimization_dsl module 결정 wire + T2 rightsizing_engine + 5 resource_types (compute + storage + database + network + container) 결정 wire + T3 idle_resource_detector + z-score based detection 결정 wire + T4 commitment_recommender + 1y/3y break-even calculation + Phase 13 forecast accuracy EXTENSION + optimization_accuracy_tracker module 결정 wire + T5 alembic 0046 phase_14_optimization 결정 wire + T6 audit action EXTENSION 6 NEW 결정 wire + T7 capability v1.40 EXTENSION + frontend finops optimization dashboard 결정 wire + T8 3중 게이트 FINAL CLEAN atomic commit 결정 wire + estimated ~+32-37 files = ~+20 NEW + ~+12 MODIFIED 결정 wire + Phase 13 wire 의 wire_commit pattern verbatim 미러 결정 wire.
- **F30.8.8** A19 cohesion pattern 9 surface EXTENSION PASS 결정 wire (FinOps optimization & rightsizing surface NEW = F30.1~F30.8 결정 wire) 결정 wire + Phase 13 wire 의 FinOps forecasting & capacity planning surface NEW = F29.1~F29.8 pattern verbatim 미러 결정 wire + Phase 12 wire 의 FinOps anomaly detection + budget alert surface NEW = F28.1~F28.8 pattern verbatim 미러 결정 wire + Phase 11 wire 의 FinOps showback/chargeback surface NEW = F27.1~F27.7 pattern verbatim 미러 결정 wire.
- **F30.8.9** CR lessons applied 14종 결정 wire (CR 0-2 RLS lesson ✅ APPLIED + CR 1-1 audit-first INSERT ✅ APPLIED (6 NEW audit log entries `optimization_definition_updated` + `recommendation_generated` + `idle_resource_detected` + `commitment_recommended` + `optimization_recommended_action` + `optimization_dry_run_executed` = 6 NEW + 2 EXTENSION `optimization_accuracy_degraded` + `optimization_retraining_triggered`) + CR 4-3/4-4 lessons carry ✅ APPLIED (optimization + idle detection + golden_diff pattern verbatim) + CR 1-1 ContextVar lesson ✅ APPLIED + CR 1-1 RSC boundary lesson ✅ APPLIED (admin/finops/optimization page RSC + Client Component delegation) + CR 9-6 commit message discipline ✅ APPLIED (`git commit -F <file>` 사용, PowerShell here-string 회피, D5 prevention) + CR 11-3 honest-DEFER discipline ✅ APPLIED (117번째 epic 연속 정직 회복) + CR 11-4 D-001~D-005 + P-015 ✅ APPLIED + CR 12-1 L4 ✅ APPLIED (FINOPS_OPTIMIZATION industry-agnostic 4-industry grants ✅/✅/✅/✅) + CR 12-5 D-14 typed exception envelope ✅ APPLIED (14 NEW typed exception classes `OptimizationDefinitionInvalidError` + `OptimizationScopeInvalidError` + `OptimizationInventoryUnavailableError` + `RightsizingEngineError` + `InstanceTypeMappingError` + `RecommendationConfidenceLowError` + `IdleResourceDetectionError` + `IdleSeverityClassificationError` + `IdleMetricUnavailableError` + `CommitmentRecommendationError` + `PricingDataUnavailableError` + `BreakEvenCalculationError` + `OptimizationAccuracyTrackingError` + `OptimizationRetrainingTriggerError` + `OptimizationPerformanceDegradationError`) + CR 12-5 D-PARITY-01 inversion ✅ APPLIED + CR 12-5 D-GATE-01 inversion ✅ APPLIED + A36 SDR 검증 4-step 자동 적용 ✅ + AD-14 stack pin ✅ APPLIED (Recharts 2.12.7 + slack-sdk==3.23.0 + pdpyras==5.2.0) + AD-22 owner-only RBAC ✅ APPLIED (optimization 모두 owner-only RBAC AD-22 + Epic 12 2FA 챌린지 보존) + NFR4 PII minimization ✅ PRESERVED) 결정 wire.
- **F30.8.10** D-DEFER-* honestly 결정 wire (CR 11-3 117번째 epic 연속 정직 회복 결정 wire 보존) = D-1-1-DEFER-1/2/3 RESOLVED 보존 60~117번째 + D-LAUNCH-1-DEFER-1 honestly preserved 65~117번째 + D-EPIC-16-REVIEW-DEFER-2~6 ✅ RESOLVED 보존 78~117번째 + D-PHASE-4-DR-DEFER-1/2 ✅ RESOLVED 보존 73~117번째 + D-EPIC-17-WIRE-DEFER-T2-T3-UI ✅ RESOLVED 보존 83~117번째 + D-RETENTION-1 ✅ RESOLVED 보존 85~117번째 + D-OBSERVABILITY-1 ✅ RESOLVED 보존 89~117번째 + D-PERFORMANCE-1 ✅ RESOLVED 보존 93~117번째 + D-CHAOS-1 ✅ RESOLVED 보존 100~117번째 + D-SLO-1 ✅ RESOLVED 보존 101~117번째 + D-FINOPS-1 ✅ RESOLVED 보존 105~117번째 + D-FINOPS-2 ✅ RESOLVED 보존 109~117번째 + D-FINOPS-3 ✅ RESOLVED 보존 113~117번째 + **D-FINOPS-4 신규 honestly DEFER 보존 = Phase 14 PRD entry 진입 시점에 carry-over chain 정직 회복 결정 wire 진입: Phase 13 close-out retro `850b4f8` §13 + Phase 12 close-out retro `3354e83` §13 + Phase 11 close-out retro `80df15b` §12 + Phase 10 close-out retro `733d428` §10 + Phase 9 close-out retro `634427d` §10 + Phase 8 close-out retro `ab495a8` §10 + Phase 7 close-out retro `326fa9f` §10 + Phase 6 close-out retro `f9f006c` §13 + Epic 17 close-out retro `be8f3bd` §11 "FinOps Optimization & Rightsizing 결정 wire 보류, Phase 14+ 진입 시점" verbatim 해소** 결정 wire.
- **F30.8.11** Epic 1 ~ Epic 17 + Phase 3 ~ Phase 13 + 1st release cycle 정합 보존 결정 wire (cj-style 117번째 epic 연속 정직 회복 pre-flight 정합 sweep 결정 wire 보존): ✅ Phase 13 close-out retro `850b4f8` (cj-style 116번째) + ✅ Phase 13 atomic wire T1~T8 `8b98030` (cj-style 115번째) + ✅ Phase 13 spec entry `77ed55f` (cj-style 114번째) + ✅ Phase 13 PRD entry `d31dfc8` (cj-style 113번째) + ✅ Phase 12 close-out retro `3354e83` (cj-style 112번째) + ✅ Phase 12 atomic wire T1~T8 `f3c0e63` (cj-style 111번째) + ✅ Phase 12 spec entry `8c5f374` (cj-style 110번째) + ✅ Phase 12 PRD entry `344c7eb` (cj-style 109번째) + ✅ Phase 11 close-out retro `80df15b` (cj-style 108번째) + ✅ Phase 11 atomic wire T1~T8 `e020ad0` (cj-style 107번째) + ✅ Phase 11 spec entry `82c93a8` (cj-style 106번째) + ✅ Phase 11 PRD entry `16d7698` (cj-style 105번째) + ✅ Phase 10 close-out retro `733d428` (cj-style 104번째) + ✅ Phase 10 atomic wire T1~T8 `ac5d6c5` (cj-style 103번째) + ✅ Phase 10 spec entry `3c80ef0` (cj-style 102번째) + ✅ Phase 10 PRD entry `09db4d4` (cj-style 101번째) + ✅ Phase 9 close-out retro `634427d` (cj-style 100번째) + ✅ Phase 9 atomic wire T1~T8 `e7670e1` (cj-style 99번째) + ✅ Phase 9 spec entry `2a5e4da` (cj-style 98번째) + ✅ Phase 9 PRD entry `0b2d2f3` (cj-style 97번째) + ✅ Phase 8 close-out retro `ab495a8` (cj-style 96번째) + ✅ Phase 8 atomic wire T1~T8 `60d4ea1` (cj-style 95번째) + ✅ Phase 8 spec entry `5ae0f4e` (cj-style 94번째) + ✅ Phase 8 PRD entry `ced452f` (cj-style 93번째) + ✅ Build fixes sprint `eaee198` + ✅ Phase 7 close-out retro `326fa9f` (cj-style 92번째) + ✅ Phase 7 atomic wire T1~T8 `59b56cd` (cj-style 91번째) + ✅ Phase 7 spec entry `749381e` (cj-style 90번째) + ✅ Phase 7 PRD entry `916a541` (cj-style 89번째) + ✅ Phase 6 close-out retro `f9f006c` (cj-style 88번째) + ✅ Phase 6 atomic wire T1~T8 `24e1cd7` (cj-style 87번째) + ✅ Phase 6 spec entry `f5c14c9` (cj-style 86번째) + ✅ Phase 6 PRD entry `e84a281` (cj-style 85번째) + ✅ Epic 17 close-out retro `be8f3bd` (cj-style 84번째) + ✅ Epic 17 T2+T3 UI wire `bb92879` (cj-style 83번째) + ✅ Epic 17 wire `2ada2ec` (cj-style 82번째) + ✅ Epic 17 spec entry `f4b2b58` (cj-style 81번째) + ✅ Epic 17 PRD entry `40a9c41` (cj-style 80번째) + ✅ Sidebar/MenuProvider hot-fix `01a06e4` (cj-style 79번째) + ✅ D-EPIC-16-REVIEW-DEFER-2~6 RESOLVE sprint `512ed6a` (cj-style 78번째) + ✅ Phase 5 close-out retro `b843565` (cj-style 76~77번째) + ✅ Phase 5 wire `f093f8c` (cj-style 75번째) + ✅ Phase 5 spec entry (cj-style 74번째) + ✅ Phase 5 PRD entry `93d852b` (cj-style 73번째) + ✅ Epic 16 close-out retro (cj-style 72번째) + ✅ Epic 16 T4 admin UI follow-up `ff5c3b5` (cj-style 71번째) + ✅ Epic 16 review follow-up `963079c` (cj-style 70번째) + ✅ Epic 16 atomic wire `e117e09` (cj-style 69번째) + ✅ Epic 16 PRD entry `08bfca5` (cj-style 67번째) + ✅ 1st release cycle cj-style 62~66번째 모두 wire DONE 진입 + ✅ Epic 15 cycle cj-style 58~61번째 모두 wire DONE 진입 + ✅ Phase 4 cycle cj-style 53~57번째 모두 wire DONE 진입 + ✅ Phase 3 cycle cj-style 49~52번째 모두 wire DONE 진입 + ✅ Epic 14 LISTEN/NOTIFY multi-process `7835463` + ✅ Epic 13 LISTEN/NOTIFY consume `f2ea2f6` + ✅ Epic 12 2FA 게이트 `a63646c` (Phase 14 optimization 모두 owner-only RBAC AD-22 + Epic 12 2FA 챌린지 보존) + ✅ Epic 11 close-out retro + Phase 2 close-out baseline 599 passed 정합 보존 + ✅ Epic 1 carry-over (auth) layout + onboarding/industry 보존 + ✅ Epic 7~10 ABC/TDABC + AI 인사이트 territory 결정 wire 보존 결정 wire.
- **F30.8.12** partial wire 시도 0건 + single sprint atomic docs-only wire 1 진입점 결정 wire (cj-style 117번째 epic 연속 정직 회복 Phase 14 PRD entry atomic docs-only wire 6 files atomic single sprint 결정 wire) + 결정 wire 일자: 2026-08-25 (KST) + **next**: 옵션 (a) Phase 14 bmad-create-story spec entry 진입 (cj-style 118번째 wire 진입 시점) / 옵션 (b) Phase 14 bmad-dev-story atomic wire T1~T8 진입 (cj-style 119번째) / 옵션 (c) Phase 14 close-out retro 진입 (cj-style 120번째) / 옵션 (d) Epic 18+ 진입 / 옵션 (e) D-DEFER-* follow-up 결정 wire 보류 결정 wire.

## F31. FinOps Tag Governance & Cost Allocation (Phase 15 territory 신규)

> **§F31 결정 wire 진입** — Phase 14 close-out retro `5b367d9` (cj-style 120번째 wire entry) 직후 다음 옵션 5종 중 **사용자 권장 결정 = 옵션 (a) Phase 15+ 진입 + 옵션 (a) FinOps Tag Governance & Cost Allocation (Recommended) (tag policy DSL 4 enforcement_level required/recommended/optional/blocked + untagged resource detector 6 resource types EC2/RDS/S3/Lambda/EKS/VPC + allocation rules engine 5 rule types tag_match/percentage_split/weighted/conditional/fallback + allocation audit + compliance + chargeback allocation reconciliation hybrid_blended default + tag governance dashboard UI 5 components + audit-first INSERT 8 NEW + Capability FINOPS_TAG_GOVERNANCE 1 NEW + dry-run mode + tests + wire scope T1~T8) = Phase 15 territory 결정 wire 진입** (AskUserQuestion 결정 verbatim bind). rationale 5종: ① Phase 14 wire `e904485` FinOps Optimization & Rightsizing territory (optimization definition DSL + rightsizing engine 5 resource types + idle resource detection z-score 기반 + RI/SP commitment recommendations 1y/3y + optimization accuracy tracking precision/recall/realized savings) 의 natural backend carry-over chain = Phase 15 = FinOps Tag Governance & Cost Allocation territory 의 natural next (rightsizing 권고 의 resource inventory → tagged resource inventory 기반 cost allocation + idle resource 의 unattached EIP / detached EBS / orphaned RDS snapshot detection → untagged resource detection EXTENSION + commitment_recommender 의 resource_pattern → tag_match 기반 allocation rule + optimization_accuracy 의 resource_type granularity → allocation rule 의 tag dimension EXTENSION + Phase 11 showback department breakdown 의 cost allocation → tag-based allocation EXTENSION + Phase 12 anomaly detection 의 dimension 옵션 → tag dimension 추가 + Phase 13 forecasting 의 target_metric → tag-based forecasting EXTENSION + Phase 14 optimization 의 baseline_period → tag governance compliance period) = Phase 14 FinOps territory 의 COST ALLOCATION LAYER EXTENSION 결정 (tagged resource → accountable cost center) ② cj-style discipline 회피 위험 방지 (49~120번째 누적 72-entry-point cycle + 120번째 Phase 14 close-out retro + 119번째 atomic wire + 118번째 spec entry + 117번째 PRD entry + Phase 13 close-out retro `850b4f8` 116번째 + Phase 13 atomic wire T1~T8 `8b98030` 115번째 + Phase 13 spec entry `77ed55f` 114번째 + Phase 13 PRD entry `d31dfc8` 113번째 패턴 모두 wire DONE 진입 정합 보존 후 즉시 Phase 15 진입 = 1-day atomic sprint discipline) ③ 비즈니스 우선순위 = enterprise 고객 onboarding 시 tag policy 정의 + untagged resource detection + tag-based cost allocation + chargeback allocation reconciliation + compliance report territory 필수 ④ Phase 14 wire `e904485` 의 resource inventory (compute + storage + database + network + container 5 types) + Phase 13 wire `8b98030` 의 capacity_headroom_report dimension EXTENSION + Phase 11 wire `e020ad0` 의 department cost center mapping + Phase 12 wire `f3c0e63` 의 anomaly detection baseline (z-score > 3.0) + Phase 8 wire `60d4ea1` 의 cost-engine 12-period benchmark 의 자연스러운 carry-over chain = Phase 15 tag governance 의 resource_inventory tagged 여부 검증 (EC2/RDS/S3/Lambda/EKS/VPC 6 resource types) + tag_policy DSL (4 enforcement_level: required/recommended/optional/blocked) + untagged resource detection (last 7d/30d/90d baseline) + allocation rules engine (5 rule types: tag_match/percentage_split/weighted/conditional/fallback) + chargeback allocation reconciliation (Phase 11 chargeback engine EXTENSION) + compliance report (monthly/quarterly/annually cadence) EXTENSION 정합 ⑤ 1st release close-out retro + Epic 17 close-out retro + Phase 5 close-out retro + Phase 6 close-out retro + Phase 7 close-out retro + Phase 8 close-out retro + Phase 9 close-out retro + Phase 10 close-out retro + Phase 11 close-out retro + Phase 12 close-out retro + Phase 13 close-out retro + Phase 14 close-out retro territory verbatim 보존 + AD-22 owner-only RBAC + Epic 12 2FA 챌린지 보존 + AD-14 stack pin + NFR4 PII minimization ✅ PRESERVED + AD-42 FinOps Tag Governance & Cost Allocation 신규 결정 ((a)~(g) 7 sub-decisions 모두 결정 wire 진입). §F31 territory = FinOps Tag Governance & Cost Allocation 결정 wire 진입 시점에 8 ACs (§F31.1 tag policy DSL + §F31.2 untagged resource detector + §F31.3 allocation rules engine + §F31.4 allocation audit + compliance + §F31.5 chargeback allocation reconciliation + §F31.6 tag governance dashboard UI + §F31.7 Capability matrix v1.41 EXTENSION + §F31.8 dry-run + tests + wire scope T1~T8) + §8.1 M0-(x) FinOps Tag Governance & Cost Allocation AC 신규 + §15 로드맵 Phase 15 row status 백로그 → in-progress (PRD entry DONE 진입 wire) + §부록 A A434~A438 신규 결정 표 + AD-42 FinOps Tag Governance & Cost Allocation 신규 결정 + capability matrix v1.40 → v1.41 EXTENSION FINOPS_TAG_GOVERNANCE 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅ (CR 12-1 L4 precedent 미러, FINOPS_OPTIMIZATION Phase 14 wire + FINOPS_FORECASTING_CAPACITY_PLANNING Phase 13 wire + FINOPS_ANOMALY_DETECTION + FINOPS_BUDGET_ALERT Phase 12 wire + FINOPS_SHOWBACK + FINOPS_CHARGEBACK Phase 11 wire + SLO_ENGINEERING Phase 10 wire + CHAOS_ENGINEERING Phase 9 wire + PERFORMANCE_TESTING Phase 8 wire + OBSERVABILITY_TRACES + OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind). CR 11-3 honest-DEFER discipline 121번째 epic 연속 정직 회복 검증 ✅ (D-1-1-DEFER-1/2/3 RESOLVED 보존 60~121번째 + D-LAUNCH-1-DEFER-1 honestly preserved 65~121번째 + D-EPIC-16-REVIEW-DEFER-2~6 ✅ RESOLVED 보존 78~121번째 + D-PHASE-4-DR-DEFER-1/2 ✅ RESOLVED 보존 73~121번째 + D-EPIC-17-WIRE-DEFER-T2-T3-UI ✅ RESOLVED 보존 83~121번째 + D-RETENTION-1 ✅ RESOLVED 보존 85~121번째 + D-OBSERVABILITY-1 ✅ RESOLVED 보존 89~121번째 + D-PERFORMANCE-1 ✅ RESOLVED 보존 93~121번째 + D-CHAOS-1 ✅ RESOLVED 보존 100~121번째 + D-SLO-1 ✅ RESOLVED 보존 101~121번째 + D-FINOPS-1 ✅ RESOLVED 보존 105~121번째 + D-FINOPS-2 ✅ RESOLVED 보존 109~121번째 + D-FINOPS-3 ✅ RESOLVED 보존 113~121번째 + D-FINOPS-4 ✅ RESOLVED 보존 117~121번째 + **D-FINOPS-5 신규 honestly DEFER 보존 = Phase 15 PRD entry 진입 시점에 carry-over chain 정직 회복 결정 wire 진입: Phase 14 close-out retro `5b367d9` §13 + Phase 13 close-out retro `850b4f8` §13 + Phase 12 close-out retro `3354e83` §13 + Phase 11 close-out retro `80df15b` §12 + Phase 10 close-out retro `733d428` §10 + Phase 9 close-out retro `634427d` §10 + Phase 8 close-out retro `ab495a8` §10 + Phase 7 close-out retro `326fa9f` §10 + Phase 6 close-out retro `f9f006c` §13 + Epic 17 close-out retro `be8f3bd` §11 "FinOps Tag Governance & Cost Allocation 결정 wire 보류, Phase 15+ 진입 시점" verbatim 해소**).

### F31.1 tag policy DSL (12 sub-ACs, A434 결정)

- **F31.1.1** tag policy DSL syntax 결정 wire = `define_tag_policy(tenant_id, resource_type, tag_key, enforcement_level, default_value)` builder + AST 5 levels (tenant_id + resource_type + tag_key + enforcement_level + default_value) + parser 검증 3 layer (syntax + semantic + tenant-scope RLS) 결정 wire.
- **F31.1.2** 6 resource_type 옵션 결정 wire = (1) ec2 (AWS EC2 instance) / (2) rds (AWS RDS / Aurora instance) / (3) s3 (AWS S3 bucket) / (4) lambda (AWS Lambda function) / (5) eks (AWS EKS cluster / node group) / (6) vpc (AWS VPC endpoint / NAT gateway / security group) 결정 wire + resource_type validation 결정 wire (tenant_settings.resource_inventory JSONB 기반 + Phase 14 wire `e904485` 의 5 resource_types EXTENSION + lambda + vpc 추가 정합).
- **F31.1.3** 4 enforcement_level 옵션 결정 wire = (1) required (태그 부재 시 정책 위반, alert + remediation trigger) / (2) recommended (태그 부재 시 경고만, no enforcement) / (3) optional (태그 부재 시 silent, audit log only) / (4) blocked (태그 부재 시 provisioning 차단, CI/CD gate) 결정 wire + enforcement_level default = recommended + per-tenant override JSONB 결정 wire + per-resource-type override JSONB 결정 wire.
- **F31.1.4** tag_key validation 결정 wire = AWS 표준 태그 키 (소문자 + 숫자 + `_` + `-` + `.` + `:` + max 128 chars) + value validation (max 256 chars + UTF-8) + reserved prefix `aws:` 차단 (AWS reserved tag prefix verbatim 보존) + custom prefix `tenant:` 또는 `cost:` namespace 권장 결정 wire.
- **F31.1.5** default_value 옵션 결정 wire = (1) string (literal default value, e.g. "untagged" + "shared" + "production") / (2) env_based (environment 변수 기반) / (3) conditional (Lambda function 평가 결과) / (4) inherit (resource 의 parent 태그 inherit) 결정 wire + default_value default = "untagged" + per-tenant override JSONB 결정 wire.
- **F31.1.6** tag_policy_defaults constants 결정 wire = `TAG_POLICY_DEFAULTS = {'enforcement_level': 'recommended', 'default_value': 'untagged', 'compliance_threshold_pct': 95.0, 'untagged_alert_channel': 'slack', 'monthly_audit_cron': '0 4 1 * *', 'remediation_action': 'notify_only'}` constants + `TAG_RESOURCE_TYPES = ['ec2', 'rds', 's3', 'lambda', 'eks', 'vpc']` constants + `TAG_ENFORCEMENT_LEVELS = ['required', 'recommended', 'optional', 'blocked']` constants 결정 wire.
- **F31.1.7** 4 industries baseline 결정 wire = manufacturing + service + manufacturing_service + manufacturing_service_other 모두 industry-agnostic 4-industry grants ✅/✅/✅/✅ (CR 12-1 L4 precedent 미러, Phase 11/12/13/14 wire 정합) + per-tenant override EXTENSION + policy evaluation precedence tenant override > industry baseline > system default 결정 wire.
- **F31.1.8** `apps/api/modules/finops/tag_policy_dsl.py` NEW ~+150 LOC + `parse_tag_policy(policy_text)` parser + `validate_tag_policy(tenant_id, resource_type, tag_key)` validator + `TagPolicy` TypedDict 11 fields (policy_id + tenant_id + resource_type + tag_key + enforcement_level + default_value + compliance_threshold_pct + remediation_action + status enum active/paused/expired + created_at + updated_at) + 5 layer defense (syntax + semantic + tenant-scope RLS + resource_type validation + tag_key validation) 결정 wire.
- **F31.1.9** audit-first INSERT `tag_policy_updated` CR 1-1 verbatim 결정 wire = action_class='FINOPS_TAG_GOVERNANCE' (NEW) + action='tag_policy_updated' (NEW) + actor_id + tenant_id + policy_id + resource_type + tag_key + enforcement_level + default_value + trace_id 결정 wire + ActionClass.FINOPS_TAG_GOVERNANCE 신규 정의 + apps/api/core/audit_action.py MODIFIED AuditAction Literal EXTENSION 1 NEW value + _ActionRegistry FINOPS_TAG_GOVERNANCE entry 신규 1개 등록 + __all__ EXTENSION 결정 wire.
- **F31.1.10** typed exception envelope CR 12-5 D-14 결정 wire = `TagPolicyInvalidError(400)` + `TagPolicyScopeInvalidError(404)` + `TagKeyValidationError(422)` + `TagEnforcementViolationError(403)` 4 NEW typed exception classes + envelope `{error_code, message_ko, trace_id, timestamp, details}` 결정 wire.
- **F31.1.11** RLS 자동 적용 CR 0-2 verbatim 결정 wire = phase_15_finops_tag_governance tables 의 `tenant_id = current_setting('app.tenant_id')::uuid` policy + multi-tenant isolation 검증 + 테스트 결정 wire (Phase 14 wire `e904485` 의 phase_14_finops_optimization RLS pattern verbatim 미러).
- **F31.1.12** dry-run mode `--finops-tag-policy-dry-run` CLI flag 결정 wire = tag policy 검증 + 결과 preview + actual `tag_policy_updated` audit-first INSERT skip + dry-run 결과 preview `phase_15_finops_tag_policy_preview` table 결정 wire.

### F31.2 untagged resource detector (12 sub-ACs, A435 결정)

- **F31.2.1** `apps/api/modules/finops/untagged_resource_detector.py` NEW ~+200 LOC + `detect_untagged_resources(tenant_id, resource_type, enforcement_level) -> List[UntaggedResource]` 결정 wire + Phase 14 wire `e904485` 의 idle_resource_detector EXTENSION + 6 resource_types 결정 wire.
- **F31.2.2** EC2 untagged detection 결정 wire = instance 의 tag inventory 조회 (AWS Resource Groups Tagging API + boto3 `describe_tags()`) + required tag keys 부재 시 untagged classify + Phase 14 wire `e904485` 의 idle_resource_detector 의 compute idle (CPU < 5% for 30d) EXTENSION 정합 + instance_age_days + region + instance_type metadata 결정 wire.
- **F31.2.3** RDS untagged detection 결정 wire = db instance 의 tag inventory 조회 + required tag keys 부재 시 untagged classify + Phase 14 wire `e904485` 의 idle_resource_detector 의 database idle (connection_count_p95 = 0 for 30d) EXTENSION 정합 + db_engine + db_class + storage_gb + multi_az metadata 결정 wire.
- **F31.2.4** S3 untagged detection 결정 wire = bucket 의 tag inventory 조회 + required tag keys 부재 시 untagged classify + Phase 14 wire `e904485` 의 idle_resource_detector 의 storage idle (S3 last_accessed > 30d) EXTENSION 정합 + bucket_size_bytes + object_count + versioning_enabled metadata 결정 wire.
- **F31.2.5** Lambda untagged detection 결정 wire = function 의 tag inventory 조회 + required tag keys 부재 시 untagged classify + function_runtime + memory_mb + timeout_seconds + last_modified_date + invocations_30d_count metadata 결정 wire + 7d/30d invocations < 1 → untagged_lambda_warn severity 결정 wire.
- **F31.2.6** EKS untagged detection 결정 wire = cluster + node group 의 tag inventory 조회 + required tag keys 부재 시 untagged classify + Phase 14 wire `e904485` 의 idle_resource_detector 의 container idle EXTENSION 정합 + k8s_version + node_count + pod_count metadata 결정 wire.
- **F31.2.7** VPC untagged detection 결정 wire = VPC endpoint + NAT gateway + security group + internet gateway 의 tag inventory 조회 + required tag keys 부재 시 untagged classify + Phase 14 wire `e904485` 의 idle_resource_detector 의 network idle EXTENSION 정합 + vpc_id + cidr_block + attached_resources_count metadata 결정 wire.
- **F31.2.8** UntaggedResource TypedDict 14 fields 결정 wire = (1) untagged_resource_id UUID PK + (2) tenant_id UUID + (3) resource_id TEXT + (4) resource_type enum ec2/rds/s3/lambda/eks/vpc + (5) required_tags_missing List[TEXT] + (6) optional_tags_missing List[TEXT] + (7) enforcement_level enum required/recommended/optional/blocked + (8) current_tags JSONB + (9) detection_method enum tag_api_scan/scheduled_cron/on_demand + (10) detected_at TIMESTAMPTZ + (11) remediation_status enum pending/notified/remediated/expired + (12) remediation_action enum notify_only/auto_remediate/block_provisioning + (13) policy_id UUID FK + (14) trace_id TEXT 결정 wire.
- **F31.2.9** remediation_status 결정 wire = `pending` (default) → `notified` (owner에게 알림 발송 후) → `remediated` (owner가 태그 추가 후) → `expired` (30일 후에도 미조치 시 자동 escalate) 결정 wire + per-tenant override remediation_timeout_days default 30 결정 wire + Epic 12 2FA 챌린지 보존 (auto_remediate 의 tag add/remove 시) 결정 wire.
- **F31.2.10** severity classification 결정 wire = `required` enforcement level 의 untagged → high severity (즉시 알림 + Slack + PagerDuty) / `recommended` → medium severity (이메일 + Slack) / `optional` → low severity (audit log only) / `blocked` → critical severity (CI/CD gate block + 즉시 알림) 결정 wire + per-tenant override severity_threshold JSONB 결정 wire.
- **F31.2.11** audit-first INSERT `untagged_resource_detected` CR 1-1 verbatim 결정 wire = action_class='FINOPS_TAG_GOVERNANCE' + action='untagged_resource_detected' (NEW) + actor_id='system' + tenant_id + untagged_resource_id + resource_type + required_tags_missing + enforcement_level + remediation_status + trace_id 결정 wire + AuditAction Literal EXTENSION 1 NEW value 결정 wire.
- **F31.2.12** typed exception envelope CR 12-5 D-14 결정 wire = `UntaggedResourceDetectionError(500)` + `TagInventoryUnavailableError(404)` + `RemediationActionError(500)` + `EnforcementLevelViolationError(403)` 4 NEW typed exception classes + envelope 결정 wire.

### F31.3 allocation rules engine (12 sub-ACs, A436 결정)

- **F31.3.1** `apps/api/modules/finops/allocation_rules_engine.py` NEW ~+220 LOC + `evaluate_allocation_rules(tenant_id, period_key, resource_id, cost_amount) -> AllocatedCost` 결정 wire + 5 rule_types (tag_match/percentage_split/weighted/conditional/fallback) + Phase 11 wire `e020ad0` 의 chargeback cost allocation engine EXTENSION 결정 wire.
- **F31.3.2** tag_match allocation rule 결정 wire = resource 의 tag value → department_id 직접 매칭 + 1 resource = 1 department 매핑 + Phase 11 wire `e020ad0` 의 department cost center mapping EXTENSION 정합 + 매칭 실패 시 fallback rule evaluate 결정 wire.
- **F31.3.3** percentage_split allocation rule 결정 wire = resource 의 tag value → N department list + percentage weights 매핑 + percentage 합 = 100% 검증 + UNIQUE constraint `(tenant_id, cost_center_id, period_key)` 결정 wire.
- **F31.3.4** weighted allocation rule 결정 wire = resource 의 tag value list → weight 계산 (e.g. user list → monthly active usage 기반 분배) + Phase 7 wire `59b56cd` 의 `business_calculations_total` EXTENSION + weight = monthly_active_user_count 결정 wire + 0 weight → fallback rule evaluate 결정 wire.
- **F31.3.5** conditional allocation rule 결정 wire = Lambda function 평가 결과 → department_id 매핑 + condition evaluation AST parser CR 11-4 P-015 verbatim + condition 평가 실패 시 fallback rule evaluate 결정 wire + per-tenant override JSONB 결정 wire.
- **F31.3.6** fallback allocation rule 결정 wire = 4 rule_types 모두 매칭 실패 시 catch-all default department (default = "shared" 또는 "untagged") + cost 100% allocated to fallback department + audit-first INSERT `allocation_fallback_applied` CR 1-1 verbatim 결정 wire + per-tenant override fallback_department_id default "shared" 결정 wire.
- **F31.3.7** rule precedence 결정 wire = policy evaluation precedence `tag_match > percentage_split > weighted > conditional > fallback` (specificity 순서) + per-rule override priority INT (default = 0, higher = 먼저 evaluate) 결정 wire + multiple rules 매칭 시 priority + specificity tiebreaker 결정 wire.
- **F31.3.8** rule_evaluation_metadata 결정 wire = `applied_rule_type` + `applied_rule_id` + `match_score` (0.0 ~ 1.0) + `fallback_chain` List[rule_type] + `evaluation_duration_ms` + `condition_evaluation_log` JSONB 결정 wire + audit log 에 metadata 저장 결정 wire.
- **F31.3.9** AllocationRule TypedDict 13 fields 결정 wire = (1) rule_id UUID PK + (2) tenant_id UUID + (3) rule_type enum tag_match/percentage_split/weighted/conditional/fallback + (4) priority INT default 0 + (5) tag_key TEXT + (6) tag_value_pattern TEXT (regex 또는 literal) + (7) target_department_id TEXT + (8) percentage_weights JSONB nullable (percentage_split 전용) + (9) condition_ast JSONB nullable (conditional 전용) + (10) fallback_department_id TEXT nullable (fallback 전용) + (11) enabled BOOLEAN DEFAULT TRUE + (12) created_at + (13) trace_id TEXT 결정 wire.
- **F31.3.10** AllocatedCost TypedDict 11 fields 결정 wire = (1) allocation_id UUID PK + (2) tenant_id UUID + (3) period_key TEXT (e.g. "2026-08") + (4) resource_id TEXT + (5) resource_type enum + (6) cost_amount_krw NUMERIC(20, 2) + (7) target_department_id TEXT + (8) applied_rule_type enum + (9) applied_rule_id UUID FK + (10) match_score NUMERIC(8, 4) + (11) allocated_at TIMESTAMPTZ + (12) trace_id TEXT 결정 wire.
- **F31.3.11** audit-first INSERT `allocation_rule_evaluated` + `allocation_rule_updated` 2 NEW CR 1-1 verbatim 결정 wire = action_class='FINOPS_TAG_GOVERNANCE' + action='allocation_rule_evaluated' / 'allocation_rule_updated' (2 NEW) + actor_id + tenant_id + rule_id + resource_id + applied_rule_type + match_score + cost_amount_krw + trace_id 결정 wire + AuditAction Literal EXTENSION 2 NEW values 결정 wire.
- **F31.3.12** typed exception envelope CR 12-5 D-14 결정 wire = `AllocationRuleInvalidError(400)` + `AllocationRuleEvaluationError(500)` + `PercentageSumValidationError(422)` + `ConditionalRuleParseError(400)` 4 NEW typed exception classes + envelope 결정 wire.

### F31.4 allocation audit + compliance (12 sub-ACs, A437 결정)

- **F31.4.1** `apps/api/modules/finops/allocation_audit.py` NEW ~+150 LOC + `audit_allocation(tenant_id, period_key) -> AllocationAuditReport` 결정 wire + Phase 14 wire `e904485` 의 optimization_accuracy_tracker EXTENSION + per-(tenant_id + resource_type + rule_type) granularity + allocation accuracy tracking 결정 wire.
- **F31.4.2** compliance score 계산 결정 wire = `compliance_pct = (tagged_resources / total_resources) × 100` (per resource_type) + total compliance = `sum(compliance_pct × resource_count) / sum(resource_count)` 가중 평균 + per-tenant override compliance_threshold_pct default 95% 결정 wire.
- **F31.4.3** compliance threshold 결정 wire = compliance_pct ≥ 95% → compliant / 80~95% → partially_compliant / < 80% → non_compliant + per-tenant override threshold JSONB 결정 wire + per-industry override compliance_threshold JSONB 결정 wire.
- **F31.4.4** allocation accuracy 결정 wire = allocation output 검증 (allocated cost == source cost + audit log 매칭) + accuracy_pct = `sum(matched_allocations) / sum(total_allocations) × 100` + accuracy ≥ 99% → high / 95~99% → moderate / < 95% → low 결정 wire + per-tenant override accuracy_threshold default 99% 결정 wire.
- **F31.4.5** allocation coverage 결정 wire = coverage_pct = `sum(allocated_resources) / sum(total_resources) × 100` + coverage ≥ 95% → high / 80~95% → moderate / < 80% → low 결정 wire + 미할당 cost → fallback department 로 자동 할당 결정 wire.
- **F31.4.6** compliance report generation 결정 wire = `generate_compliance_report(tenant_id, period_key) -> ComplianceReport` TypedDict 12 fields (report_id UUID PK + tenant_id UUID + period_key TEXT + total_resources int + tagged_resources int + untagged_resources int + compliance_pct NUMERIC(8, 4) + allocation_accuracy_pct NUMERIC(8, 4) + allocation_coverage_pct NUMERIC(8, 4) + non_compliant_resources JSONB + generated_at TIMESTAMPTZ + trace_id TEXT) + monthly cadence default 결정 wire.
- **F31.4.7** compliance report delivery 결정 wire = `apps/api/jobs/compliance_report.py` NEW ~+120 LOC + cron KST 1st day of month 04:00 UTC 19:00 + report format 3종 (PDF + CSV + JSON) 결정 wire + owner-only access 결정 wire + Epic 12 2FA 챌린지 보존 결정 wire + retention 365일 결정 wire.
- **F31.4.8** compliance trend analysis 결정 wire = past 12-month compliance_pct 추이 분석 + trend = `compliance_pct_now - compliance_pct_12mo_ago` + trend > +5% → improving / -5~+5% → stable / < -5% → degrading 결정 wire + Slack 알림 (degrading 의 경우만) 결정 wire + Phase 11 wire `e020ad0` 의 showback period selector 의 last 12 months EXTENSION 정합.
- **F31.4.9** compliance alert routing 결정 wire = non_compliant_resources > 10% → Slack `#bizup-compliance-alerts` channel + PagerDuty integration `pd_compliance_critical` service (critical severity 시) + Email owner-only 알림 결정 wire + audit-first INSERT `compliance_alert_sent` CR 1-1 verbatim 결정 wire + Phase 12 wire `f3c0e63` 의 alert routing (Slack + PagerDuty + Email) EXTENSION 정합.
- **F31.4.10** compliance remediation workflow 결정 wire = non_compliant_resources 발견 시 자동 remediation workflow 5 steps (1. owner notifier 발송 + 2. tag_value 자동 추천 (Phase 14 wire `e904485` 의 resource_pattern 기반) + 3. owner의 7일 SLA 내 tag 추가 + 4. 만료 시 escalate to admin + 5. audit log 기록) 결정 wire + per-tenant override remediation_workflow_enabled default true 결정 wire.
- **F31.4.11** audit-first INSERT `compliance_report_generated` + `compliance_alert_sent` + `compliance_remediation_initiated` 3 NEW CR 1-1 verbatim 결정 wire = action_class='FINOPS_TAG_GOVERNANCE' + action='compliance_report_generated' / 'compliance_alert_sent' / 'compliance_remediation_initiated' (3 NEW) + actor_id + tenant_id + period_key + compliance_pct + non_compliant_resource_count + trace_id 결정 wire + AuditAction Literal EXTENSION 3 NEW values 결정 wire.
- **F31.4.12** typed exception envelope CR 12-5 D-14 결정 wire = `ComplianceReportGenerationError(500)` + `ComplianceScoreCalculationError(500)` + `ComplianceAlertRoutingError(500)` 3 NEW typed exception classes + envelope 결정 wire.

### F31.5 chargeback allocation reconciliation (12 sub-ACs, A438 결정)

- **F31.5.1** `apps/api/modules/finops/chargeback_allocation_reconciliation.py` NEW ~+180 LOC + `reconcile_chargeback_allocation(tenant_id, period_key) -> ReconciliationReport` 결정 wire + Phase 11 wire `e020ad0` 의 chargeback engine EXTENSION + Phase 14 wire `e904485` 의 commit_recommender 의 resource_pattern EXTENSION 정합.
- **F31.5.2** reconciliation strategy 결정 wire = (1) `chargeback_only` (Phase 11 chargeback engine 출력 그대로) / (2) `tag_allocation_only` (Phase 15 F31.3 allocation_rules_engine 출력 그대로) / (3) `hybrid_blended` (Phase 11 chargeback 70% + Phase 15 tag_allocation 30% 가중 평균, default) 결정 wire + per-tenant override reconciliation_strategy default hybrid_blended 결정 wire.
- **F31.5.3** variance calculation 결정 wire = `variance_krw = sum(chargeback_cost) - sum(tag_allocation_cost)` + `variance_pct = variance_krw / sum(chargeback_cost) × 100` + variance_pct > 5% → investigate required 결정 wire + per-tenant override variance_threshold_pct default 5% 결정 wire.
- **F31.5.4** reconciliation report generation 결정 wire = `ReconciliationReport` TypedDict 13 fields (1. report_id UUID PK + 2. tenant_id UUID + 3. period_key TEXT + 4. chargeback_cost_krw NUMERIC(20, 2) + 5. tag_allocation_cost_krw NUMERIC(20, 2) + 6. variance_krw NUMERIC(20, 2) + 7. variance_pct NUMERIC(8, 4) + 8. reconciliation_strategy enum + 9. department_breakdown JSONB + 10. unmatched_allocations JSONB + 11. investigation_required BOOLEAN + 12. generated_at TIMESTAMPTZ + 13. trace_id TEXT) 결정 wire.
- **F31.5.5** unmatched allocation handling 결정 wire = Phase 11 chargeback output 중 Phase 15 tag_allocation 에 매칭되지 않는 cost (e.g. tag 부재 resource → fallback department) → `unmatched_allocations` JSONB 에 기록 + reconcile_audit_log 결정 wire + Epic 12 2FA 챌린지 보존 결정 wire.
- **F31.5.6** cross-period reconciliation 결정 wire = current period vs previous period 비교 + Phase 11 wire `e020ad0` 의 showback 의 comparison view EXTENSION + variance_pct_period_change = variance_now - variance_previous_period + variance_pct_period_change > +2% → investigate required 결정 wire + 13-period rolling reconciliation 결정 wire.
- **F31.5.7** reconciliation investigation workflow 결정 wire = variance_pct > threshold → owner에게 Slack + Email 알림 + Jira ticket auto-create (optional, owner-only config) + audit log 기록 + 14일 SLA 내 resolution 결정 wire + per-tenant override investigation_sla_days default 14 결정 wire + Epic 12 2FA 챌린지 보존 결정 wire.
- **F31.5.8** reconciliation approval workflow 결정 wire = reconciliation report 생성 → owner 승인 (mandatory) → 승인 후 chargeback 확정 + approval pending 시 chargeback status = pending_reconciliation 결정 wire + approve + confirm 액션 2 NEW + audit-first INSERT `reconciliation_approved` CR 1-1 verbatim 결정 wire + owner-only RBAC AD-22 verbatim 보존 결정 wire.
- **F31.5.9** `apps/api/jobs/chargeback_reconciliation.py` NEW ~+120 LOC + cron KST 1st day of month 05:00 UTC 20:00 (Phase 11 chargeback monthly close-out 의 다음 day) + 4 reconciliation steps (1. chargeback cost aggregate + 2. tag allocation cost aggregate + 3. variance 계산 + 4. investigation_trigger) + audit-first INSERT `reconciliation_initiated` CR 1-1 verbatim + 5 NEW actions: reconciliation_initiated + reconciliation_report_generated + reconciliation_investigation_triggered + reconciliation_approved + reconciliation_resolved 결정 wire.
- **F31.5.10** reconciliation dashboard 결정 wire = `apps/web/app/[locale]/(dashboard)/admin/finops/allocation/page.tsx` NEW ~+120 LOC + 4 components (`ReconciliationReportPanel` + `VarianceBreakdownChart` + `UnmatchedAllocationTable` + `ReconciliationApprovalButton`) + owner-only RBAC AD-22 verbatim + Epic 12 2FA 챌린지 보존 + ko-KR.json `finops_allocation_reconciliation.*` namespace EXTENSION ~15 keys CR 11-4 D-002 verbatim SSOT 결정 wire.
- **F31.5.11** audit-first INSERT 5 NEW CR 1-1 verbatim 결정 wire = action_class='FINOPS_TAG_GOVERNANCE' + action='reconciliation_initiated' / 'reconciliation_report_generated' / 'reconciliation_investigation_triggered' / 'reconciliation_approved' / 'reconciliation_resolved' (5 NEW) + actor_id + tenant_id + period_key + variance_krw + variance_pct + reconciliation_strategy + trace_id 결정 wire + AuditAction Literal EXTENSION 5 NEW values 결정 wire + Phase 11 wire `e020ad0` 의 3 NEW audit actions EXTENSION pattern verbatim 적용 + ActionClass.FINOPS_TAG_GOVERNANCE entry EXTENSION 결정 wire.
- **F31.5.12** typed exception envelope CR 12-5 D-14 결정 wire = `ReconciliationError(500)` + `VarianceCalculationError(500)` + `ReconciliationApprovalError(403)` + `UnmatchedAllocationError(422)` 4 NEW typed exception classes + envelope 결정 wire.

### F31.6 tag governance dashboard UI (10 sub-ACs, A439 결정)

- **F31.6.1** `apps/web/app/[locale]/(dashboard)/admin/finops/tag-governance/page.tsx` NEW ~+150 LOC + 5 components (`TagPolicyEditor` + `UntaggedResourceDetectorPanel` + `AllocationRuleBuilder` + `ComplianceReportPanel` + `ReconciliationReportPanel`) 결정 wire + owner-only RBAC AD-22 verbatim + Epic 12 2FA 챌린지 보존 결정 wire.
- **F31.6.2** TagPolicyEditor component 결정 wire = 6 resource_type 옵션 selectbox (ec2 + rds + s3 + lambda + eks + vpc) + tag_key input (AWS 표준 + custom prefix) + 4 enforcement_level radio button (required + recommended + optional + blocked) + 4 default_value 옵션 (string + env_based + conditional + inherit) + compliance_threshold_pct slider (50~100%) + owner-only ack prompt AD-22 verbatim 결정 wire + ko-KR.json `finops_tag_governance.policy.*` 8 keys CR 11-4 D-002 verbatim SSOT 결정 wire.
- **F31.6.3** UntaggedResourceDetectorPanel component 결정 wire = 6 resource_type 필터 selectbox + 4 enforcement_level 필터 checkbox + 4 remediation_status 필터 selectbox (pending/notified/remediated/expired) + 14 columns (untagged_resource_id + resource_id + resource_type + required_tags_missing + enforcement_level + current_tags + detection_method + detected_at + remediation_status + remediation_action + severity + notify button + auto_remediate button + add_tag_button) + severity color coding (low=blue + medium=yellow + high=red + critical=dark red) + RTL render discipline CR 11-4 D-003 verbatim 결정 wire + Phase 14 wire `e904485` 의 IdleResourcePanel EXTENSION 정합.
- **F31.6.4** AllocationRuleBuilder component 결정 wire = 5 rule_type selectbox (tag_match + percentage_split + weighted + conditional + fallback) + priority slider (0~100) + tag_key input + tag_value_pattern input (regex 또는 literal) + target_department_id input + percentage_weights 동적 입력 (percentage_split 전용) + condition_ast JSON editor (conditional 전용) + 6 columns (rule_id + rule_type + priority + tag_key + tag_value_pattern + target_department) + test_evaluation_button (dry-run 시뮬레이션) + Recharts 2.12.7 AD-14 stack pin 결정 wire.
- **F31.6.5** ComplianceReportPanel component 결정 wire = 4 sections (overall_compliance_pct + per_resource_type_compliance + non_compliant_resources_list + compliance_trend_chart) + period_selector (current + previous + last 3/6/12 months) + 3 export buttons (PDF + CSV + JSON) 결정 wire + Recharts 2.12.7 AD-14 stack pin + Phase 11 wire `e020ad0` 의 ShowbackDepartmentBreakdownChart EXTENSION 정합.
- **F31.6.6** ko-KR.json `finops_tag_governance.*` namespace EXTENSION ~30 keys 결정 wire (CR 11-4 D-002 verbatim SSOT) + `finops_tag_governance.policy.required` + `finops_tag_governance.policy.recommended` + `finops_tag_governance.policy.optional` + `finops_tag_governance.policy.blocked` + `finops_tag_governance.resource.ec2` + `finops_tag_governance.resource.rds` + `finops_tag_governance.resource.s3` + `finops_tag_governance.resource.lambda` + `finops_tag_governance.resource.eks` + `finops_tag_governance.resource.vpc` + `finops_tag_governance.remediation.pending` + `finops_tag_governance.remediation.notified` + `finops_tag_governance.remediation.remediated` + `finops_tag_governance.remediation.expired` 등 결정 wire.
- **F31.6.7** ARIA labels 결정 wire = WCAG 2.1 AA compliance (Epic 12 의 2FA 챌린지 UI 와 동일 표준) + keyboard navigation (Tab + Enter + Arrow keys) + screen reader 지원 결정 wire + Phase 14 Epic 1 UX v1.0 locked decision 결정 wire (Dark MVP / WCAG AA / Professional / ko-KR) verbatim 보존 결정 wire.
- **F31.6.8** toast notification 결정 wire = untagged_resource_detected 시 toast 자동 표시 (severity=low → blue + 5초 / medium → yellow + 10초 / high → red + 15초 / critical → dark red + 20초) + compliance_report_generated 시 toast + reconciliation_approved 시 toast 결정 wire + Phase 14 wire `e904485` 의 toast notification EXTENSION 정합.
- **F31.6.9** Vitest RTL render discipline CR 11-4 D-003 verbatim 결정 wire = 7 NEW vitest cases (TagPolicyEditor 1 + UntaggedResourceDetectorPanel 1 + AllocationRuleBuilder 1 + ComplianceReportPanel 1 + ReconciliationReportPanel 1 + ko-KR SSOT 2) 결정 wire + Phase 14 wire `e904485` 의 6 NEW vitest cases EXTENSION 정합.
- **F31.6.10** FinOps tag governance dashboard parity CR 12-5 D-PARITY-01 결정 wire = TS mirror `apps/web/lib/finops-tag-governance/finops-tag-governance-client.ts` NEW ~+150 LOC + `tests/web/test_finops_tag_governance_dashboard_parity.py` NEW ~+10 cases + 1-line ko-KR reject 결정 wire + Phase 14 wire `e904485` 의 finops-optimization-client.ts EXTENSION 정합.

### F31.7 Capability matrix v1.41 EXTENSION FINOPS_TAG_GOVERNANCE (12 sub-ACs, A440 결정)

- **F31.7.1** capability matrix v1.40 → v1.41 EXTENSION 결정 wire = FINOPS_TAG_GOVERNANCE 1 NEW row + docs/capability-matrix.md MODIFIED + version 표기 결정 wire.
- **F31.7.2** `Capability.FINOPS_TAG_GOVERNANCE = "finops_tag_governance"` 1 NEW enum 결정 wire + apps/api/core/capability.py MODIFIED + CR 12-1 L4 precedent 미러 결정 wire.
- **F31.7.3** `apps/api/dependencies/capability.py` MODIFIED 결정 wire = `require_finops_tag_governance` 1 NEW dep + Phase 14 wire `e904485` 의 `require_finops_optimization` + Phase 13 wire `8b98030` 의 `require_finops_forecasting_capacity_planning` + Phase 12 wire `f3c0e63` 의 `require_finops_anomaly_detection` + `require_finops_budget_alert` + Phase 11 wire `e020ad0` 의 `require_finops_showback` + `require_finops_chargeback` pattern verbatim 미러 결정 wire.
- **F31.7.4** industry-agnostic 4-industry grants ✅/✅/✅/✅ 결정 wire = manufacturing + service + manufacturing_service + manufacturing_service_other 모두 허용 (CR 12-1 L4 precedent 미러, FINOPS_OPTIMIZATION Phase 14 wire + FINOPS_FORECASTING_CAPACITY_PLANNING Phase 13 wire + FINOPS_ANOMALY_DETECTION + FINOPS_BUDGET_ALERT Phase 12 wire + FINOPS_SHOWBACK + FINOPS_CHARGEBACK Phase 11 wire pattern verbatim bind) 결정 wire.
- **F31.7.5** 미허용 tenant 의 tag governance 진입 차단 결정 wire = `require_finops_tag_governance` dependency 의 미허용 tenant 진입 차단 검증 + 403 Forbidden + `FORBIDDEN_KO` message ("FinOps Tag Governance & Cost Allocation capability 미허용 tenant") + Epic 12 2FA 챌린지 보존 + AD-22 owner-only RBAC 정합 결정 wire.
- **F31.7.6** tag governance 의 phase_14 optimization + phase_13 forecast + phase_12 anomaly_detection + phase_11 showback baseline carry-over 진입 차단 결정 wire = tag governance 진입 시 phase_14 FINOPS_OPTIMIZATION + phase_13 FINOPS_FORECASTING_CAPACITY_PLANNING capability 도 동시에 검증 + tag governance 가 FINOPS_TAG_GOVERNANCE 만 있고 FINOPS_OPTIMIZATION / FINOPS_FORECASTING_CAPACITY_PLANNING 없는 경우 403 Forbidden 결정 wire.
- **F31.7.7** drift detector `tests/integration/test_capability_matrix_v1_41_drift.py` NEW 결정 wire = 8 NEW pytest cases (FINOPS_TAG_GOVERNANCE row 존재 1 + 4-industry grants 1 + Capability enum 1 + require_finops_tag_governance 1 + 미허용 tenant 차단 1 + phase_14 carry-over 검증 2 + phase_11/12/13 carry-over 검증 1) 결정 wire + Phase 14 wire `e904485` 의 `tests/integration/test_capability_matrix_v1_40_drift.py` 패턴 verbatim 미러 결정 wire.
- **F31.7.8** m23_finops_tag_governance 결정 wire = `apps/api/modules/finops/tag_governance/__init__.py` NEW 결정 wire + `apps/api/modules/finops/tag_governance/serializers.py` NEW 결정 wire (m22_finops_optimization 의 Phase 14 wire EXTENSION pattern verbatim 미러) 결정 wire + module version 결정 wire (m23_finops_tag_governance 결정 wire + Phase 14 wire 의 m22_finops_optimization 와 동일 pattern).
- **F31.7.9** SSOT RED→GREEN EXTENSION 결정 wire = capability matrix v1.41 신규 1 row + capability.py EXTENSION 1 NEW enum + require_finops_tag_governance 1 NEW dep + drift detector + A36 SDR 검증 4-step 자동 적용 + CR 12-5 D-GATE-01 inversion 적용 보존 결정 wire.
- **F31.7.10** CR 12-1 L4 industry-agnostic capability 결정 wire = FINOPS_TAG_GOVERNANCE industry-agnostic 4-industry grants ✅/✅/✅/✅ 결정 wire (manufacturing + service + manufacturing_service + manufacturing_service_other 모두 허용) + Phase 14 wire 의 FINOPS_OPTIMIZATION industry-agnostic 4-industry grants pattern verbatim 미러 결정 wire.
- **F31.7.11** capability gate 의 fail-closed 결정 wire = 미허용 tenant 의 tag governance 진입 시 403 Forbidden 결정 wire + capability matrix v1.41 row 부재 시 fail-closed 결정 wire + `Capability.FINOPS_TAG_GOVERNANCE` enum 부재 시 fail-closed 결정 wire + AD-22 owner-only RBAC 정합 + Epic 12 2FA 챌린지 보존 + NFR4 PII minimization 정합 보존 결정 wire.
- **F31.7.12** capability matrix 의 version 결정 wire = v1.40 → v1.41 EXTENSION 결정 wire + version bump 일관성 결정 wire (Phase 14 wire 의 capability matrix v1.39 → v1.40 pattern verbatim 미러) + docs/capability-matrix.md 의 version 표기 결정 wire + sprint-status.yaml 의 capability version 결정 wire + drift detector 의 version 매칭 검증 결정 wire + CR 12-5 D-PARITY-01 inversion 적용 보존 결정 wire.

### F31.8 dry-run + Tests + wire scope T1~T8 (12 sub-ACs)

- **F31.8.1** dry-run mode 결정 wire = `--finops-tag-policy-dry-run` + `--finops-untagged-resource-dry-run` + `--finops-allocation-rule-dry-run` + `--finops-compliance-report-dry-run` + `--finops-chargeback-reconciliation-dry-run` CLI flags + dry-run 시 actual `tag_policy_updated` + `untagged_resource_detected` + `allocation_rule_evaluated` + `compliance_report_generated` + `reconciliation_initiated` audit-first INSERT skip + dry-run 결과 preview 결정 wire (5 preview tables alembic 0047 신규 결정 wire).
- **F31.8.2** dry-run 의 preview 결과 결정 wire = `phase_15_finops_tag_policy_preview` + `phase_15_finops_untagged_resource_preview` + `phase_15_finops_allocation_preview` + `phase_15_finops_compliance_preview` + `phase_15_finops_reconciliation_preview` 5 table 결정 wire (preview_id UUID PK + tenant_id UUID + preview_type enum + period_key TEXT + preview_data JSONB + computed_at TIMESTAMPTZ DEFAULT NOW() + trace_id TEXT) + RLS 자동 적용 CR 0-2 verbatim 결정 wire + audit-first INSERT `tag_governance_dry_run_executed` CR 1-1 verbatim 결정 wire.
- **F31.8.3** dry-run 의 CLI flag 결정 wire = 5 CLI flags + dry-run 모드 시 logger 출력 결정 (stdout + INFO level) + dry-run 결과 preview 결정 wire (console table output 결정 wire).
- **F31.8.4** tests 결정 wire = ~+56 NEW pytest PASS (tag_policy_dsl 6 + untagged_resource_detector 8 + allocation_rules_engine 8 + allocation_audit 6 + chargeback_allocation_reconciliation 7 + alembic 0047 5 + audit action 8 + capability matrix v1.41 8 = ~+56 NEW pytest PASS) 결정 wire.
- **F31.8.5** vitest tests 결정 wire = ~+7 NEW vitest PASS 결정 wire + Phase 14 wire 의 ~6 NEW vitest PASS pattern verbatim 미러 결정 wire.
- **F31.8.6** ruff + tsc 결정 wire = 0 NEW ruff 결정 wire (Phase 14 wire 정합) + 0 NEW tsc 결정 wire (apps/web frontend 변경 ext only — pre-existing baseline errors preserved per cj-style discipline) + SDR drift gate 결정 wire + commit_consistency gate 결정 wire (CR 9-6 commit message discipline + A36 SDR 검증 4-step 자동 적용) + D-DEFER-* grep guard 결정 wire (CR 11-3 honest-DEFER discipline 121번째 epic 연속 정직 회복 검증 보존) 결정 wire.
- **F31.8.7** wire scope T1~T8 결정 wire = T1 tag_policy_dsl + 4 enforcement_level 결정 wire + T2 untagged_resource_detector + 6 resource_types (ec2 + rds + s3 + lambda + eks + vpc) 결정 wire + T3 allocation_rules_engine + 5 rule_types (tag_match + percentage_split + weighted + conditional + fallback) 결정 wire + T4 allocation_audit + compliance report + 5 NEW audit actions + chargeback_allocation_reconciliation 결정 wire + T5 alembic 0047 phase_15_tag_governance 결정 wire + T6 audit action EXTENSION 8 NEW 결정 wire + T7 capability v1.41 EXTENSION + frontend finops tag governance dashboard 결정 wire + T8 3중 게이트 FINAL CLEAN atomic commit 결정 wire + estimated ~+33-38 files = ~+21 NEW + ~+12 MODIFIED 결정 wire + Phase 14 wire 의 wire_commit pattern verbatim 미러 결정 wire.
- **F31.8.8** A19 cohesion pattern 9 surface EXTENSION PASS 결정 wire (FinOps tag governance & cost allocation surface NEW = F31.1~F31.8 결정 wire) 결정 wire + Phase 14 wire 의 FinOps optimization & rightsizing surface NEW = F30.1~F30.8 pattern verbatim 미러 결정 wire + Phase 13 wire 의 FinOps forecasting & capacity planning surface NEW = F29.1~F29.8 pattern verbatim 미러 결정 wire + Phase 12 wire 의 FinOps anomaly detection + budget alert surface NEW = F28.1~F28.8 pattern verbatim 미러 결정 wire + Phase 11 wire 의 FinOps showback/chargeback surface NEW = F27.1~F27.7 pattern verbatim 미러 결정 wire.
- **F31.8.9** CR lessons applied 14종 결정 wire (CR 0-2 RLS lesson ✅ APPLIED + CR 1-1 audit-first INSERT ✅ APPLIED (8 NEW audit log entries `tag_policy_updated` + `untagged_resource_detected` + `allocation_rule_evaluated` + `allocation_rule_updated` + `compliance_report_generated` + `compliance_alert_sent` + `compliance_remediation_initiated` + `tag_governance_dry_run_executed` = 8 NEW + 5 EXTENSION `reconciliation_initiated` + `reconciliation_report_generated` + `reconciliation_investigation_triggered` + `reconciliation_approved` + `reconciliation_resolved`) + CR 4-3/4-4 lessons carry ✅ APPLIED (tag policy + untagged detection + golden_diff pattern verbatim) + CR 1-1 ContextVar lesson ✅ APPLIED + CR 1-1 RSC boundary lesson ✅ APPLIED + CR 9-6 commit message discipline ✅ APPLIED (`git commit -F <file>` 사용) + CR 11-3 honest-DEFER discipline ✅ APPLIED (121번째 epic 연속 정직 회복) + CR 11-4 D-001~D-005 + P-015 ✅ APPLIED + CR 12-1 L4 ✅ APPLIED + CR 12-5 D-14 typed exception envelope ✅ APPLIED (15 NEW typed exception classes) + CR 12-5 D-PARITY-01 inversion ✅ APPLIED + CR 12-5 D-GATE-01 inversion ✅ APPLIED + A36 SDR 검증 4-step 자동 적용 ✅ + AD-14 stack pin ✅ APPLIED + AD-22 owner-only RBAC ✅ APPLIED + NFR4 PII minimization ✅ PRESERVED) 결정 wire.
- **F31.8.10** D-DEFER-* honestly 결정 wire (CR 11-3 121번째 epic 연속 정직 회복 결정 wire 보존) = D-1-1-DEFER-1/2/3 RESOLVED 보존 60~121번째 + D-LAUNCH-1-DEFER-1 honestly preserved 65~121번째 + D-EPIC-16-REVIEW-DEFER-2~6 ✅ RESOLVED 보존 78~121번째 + D-PHASE-4-DR-DEFER-1/2 ✅ RESOLVED 보존 73~121번째 + D-EPIC-17-WIRE-DEFER-T2-T3-UI ✅ RESOLVED 보존 83~121번째 + D-RETENTION-1 ✅ RESOLVED 보존 85~121번째 + D-OBSERVABILITY-1 ✅ RESOLVED 보존 89~121번째 + D-PERFORMANCE-1 ✅ RESOLVED 보존 93~121번째 + D-CHAOS-1 ✅ RESOLVED 보존 100~121번째 + D-SLO-1 ✅ RESOLVED 보존 101~121번째 + D-FINOPS-1 ✅ RESOLVED 보존 105~121번째 + D-FINOPS-2 ✅ RESOLVED 보존 109~121번째 + D-FINOPS-3 ✅ RESOLVED 보존 113~121번째 + D-FINOPS-4 ✅ RESOLVED 보존 117~121번째 + **D-FINOPS-5 신규 honestly DEFER 보존 = Phase 15 PRD entry 진입 시점에 carry-over chain 정직 회복 결정 wire 진입** 결정 wire.
- **F31.8.11** Epic 1 ~ Epic 17 + Phase 3 ~ Phase 14 + 1st release cycle 정합 보존 결정 wire (cj-style 121번째 epic 연속 정직 회복 pre-flight 정합 sweep 결정 wire 보존): ✅ Phase 14 close-out retro `5b367d9` (cj-style 120번째) + ✅ Phase 14 atomic wire T1~T8 `e904485` (cj-style 119번째) + ✅ Phase 14 spec entry `30637f6` (cj-style 118번째) + ✅ Phase 14 PRD entry `0e3f8d9` (cj-style 117번째) + ✅ Phase 13 close-out retro `850b4f8` (cj-style 116번째) + ✅ Phase 13 atomic wire T1~T8 `8b98030` (cj-style 115번째) + ✅ Phase 13 spec entry `77ed55f` (cj-style 114번째) + ✅ Phase 13 PRD entry `d31dfc8` (cj-style 113번째) + ✅ Phase 12 close-out retro `3354e83` (cj-style 112번째) + ✅ Phase 12 atomic wire T1~T8 `f3c0e63` (cj-style 111번째) + ✅ Phase 12 spec entry `8c5f374` (cj-style 110번째) + ✅ Phase 12 PRD entry `344c7eb` (cj-style 109번째) + ✅ Phase 11 close-out retro `80df15b` (cj-style 108번째) + ✅ Phase 11 atomic wire T1~T8 `e020ad0` (cj-style 107번째) + ✅ Phase 11 spec entry `82c93a8` (cj-style 106번째) + ✅ Phase 11 PRD entry `16d7698` (cj-style 105번째) + ✅ Phase 10 close-out retro `733d428` (cj-style 104번째) + ✅ Phase 10 atomic wire T1~T8 `ac5d6c5` (cj-style 103번째) + ✅ Phase 10 spec entry `3c80ef0` (cj-style 102번째) + ✅ Phase 10 PRD entry `09db4d4` (cj-style 101번째) + ✅ Phase 9 close-out retro `634427d` (cj-style 100번째) + ✅ Phase 9 atomic wire T1~T8 `e7670e1` (cj-style 99번째) + ✅ Phase 9 spec entry `2a5e4da` (cj-style 98번째) + ✅ Phase 9 PRD entry `0b2d2f3` (cj-style 97번째) + ✅ Phase 8 close-out retro `ab495a8` (cj-style 96번째) + ✅ Phase 8 atomic wire T1~T8 `60d4ea1` (cj-style 95번째) + ✅ Phase 8 spec entry `5ae0f4e` (cj-style 94번째) + ✅ Phase 8 PRD entry `ced452f` (cj-style 93번째) + ✅ Build fixes sprint `eaee198` + ✅ Phase 7 close-out retro `326fa9f` (cj-style 92번째) + ✅ Phase 7 atomic wire T1~T8 `59b56cd` (cj-style 91번째) + ✅ Phase 7 spec entry `749381e` (cj-style 90번째) + ✅ Phase 7 PRD entry `916a541` (cj-style 89번째) + ✅ Phase 6 close-out retro `f9f006c` (cj-style 88번째) + ✅ Phase 6 atomic wire T1~T8 `24e1cd7` (cj-style 87번째) + ✅ Phase 6 spec entry `f5c14c9` (cj-style 86번째) + ✅ Phase 6 PRD entry `e84a281` (cj-style 85번째) + ✅ Epic 17 close-out retro `be8f3bd` (cj-style 84번째) + ✅ Epic 17 T2+T3 UI wire `bb92879` (cj-style 83번째) + ✅ Epic 17 wire `2ada2ec` (cj-style 82번째) + ✅ Epic 17 spec entry `f4b2b58` (cj-style 81번째) + ✅ Epic 17 PRD entry `40a9c41` (cj-style 80번째) + ✅ Sidebar/MenuProvider hot-fix `01a06e4` (cj-style 79번째) + ✅ D-EPIC-16-REVIEW-DEFER-2~6 RESOLVE sprint `512ed6a` (cj-style 78번째) + ✅ Phase 5 close-out retro `b843565` (cj-style 76~77번째) + ✅ Phase 5 wire `f093f8c` (cj-style 75번째) + ✅ Phase 5 PRD entry `93d852b` (cj-style 73번째) + ✅ Epic 16 close-out retro (cj-style 72번째) + ✅ Epic 16 T4 admin UI follow-up `ff5c3b5` (cj-style 71번째) + ✅ Epic 16 review follow-up `963079c` (cj-style 70번째) + ✅ Epic 16 atomic wire `e117e09` (cj-style 69번째) + ✅ Epic 16 PRD entry `08bfca5` (cj-style 67번째) + ✅ 1st release cycle cj-style 62~66번째 모두 wire DONE 진입 + ✅ Epic 15 cycle cj-style 58~61번째 모두 wire DONE 진입 + ✅ Phase 4 cycle cj-style 53~57번째 모두 wire DONE 진입 + ✅ Phase 3 cycle cj-style 49~52번째 모두 wire DONE 진입 + ✅ Epic 14 LISTEN/NOTIFY multi-process `7835463` + ✅ Epic 13 LISTEN/NOTIFY consume `f2ea2f6` + ✅ Epic 12 2FA 게이트 `a63646c` (Phase 15 tag governance 모두 owner-only RBAC AD-22 + Epic 12 2FA 챌린지 보존) + ✅ Epic 11 close-out retro + Phase 2 close-out baseline 599 passed 정합 보존 + ✅ Epic 1 carry-over (auth) layout + onboarding/industry 보존 + ✅ Epic 7~10 ABC/TDABC + AI 인사이트 territory 결정 wire 보존 결정 wire.
- **F31.8.12** partial wire 시도 0건 + single sprint atomic docs-only wire 1 진입점 결정 wire (cj-style 121번째 epic 연속 정직 회복 Phase 15 PRD entry atomic docs-only wire 6 files atomic single sprint 결정 wire) + 결정 wire 일자: 2026-08-25 (KST) + **next**: 옵션 (a) Phase 15 bmad-create-story spec entry 진입 (cj-style 122번째 wire 진입 시점) / 옵션 (b) Phase 15 bmad-dev-story atomic wire T1~T8 진입 (cj-style 123번째) / 옵션 (c) Phase 15 close-out retro 진입 (cj-style 124번째) / 옵션 (d) Epic 18+ 진입 / 옵션 (e) D-DEFER-* follow-up 결정 wire 보류 결정 wire.

---

## F32. FinOps Reporting & Executive Dashboard (Phase 16 territory 신규)

> **§F32 결정 wire 진입** — Phase 15 close-out retro `102f370` (cj-style 124번째 wire entry) 직후 다음 옵션 5종 중 **사용자 권장 결정 = 옵션 (a) Phase 16+ 진입 + 옵션 (a) FinOps Reporting & Executive Dashboard (Recommended) (5-module cross-rollup aggregator + cross-module KPI 8 KPI selector + executive report generation engine PDF + CSV + Excel + scheduled dispatch KST cron + tenant-scoped executive role RBAC owner-only + executive dashboard UI 5 sub-components + ko-KR.json `finops_reporting.*` namespace EXTENSION ~30 keys + audit-first INSERT 8 NEW + Capability FINOPS_REPORTING 1 NEW + dry-run mode + tests + wire scope T1~T8) = Phase 16 territory 결정 wire 진입** (AskUserQuestion 결정 verbatim bind). rationale 5종: ① Phase 15 wire `1b800d9` FinOps Tag Governance & Cost Allocation territory 의 natural backend EXECUTIVE ROLLUP LAYER EXTENSION 결정 wire 진입 (5 FinOps modules 의 data aggregate → executive-grade rollup view + cross-module KPI 8개 + scheduled executive report export PDF+CSV+Excel monthly/quarterly/annual) ② cj-style discipline 회피 위험 방지 (49~124번째 누적 76-entry-point cycle + 124번째 Phase 15 close-out retro + 123번째 atomic wire + 122번째 spec entry + 121번째 PRD entry 패턴 모두 wire DONE 진입 정합 보존 후 즉시 Phase 16 진입 = 1-day atomic sprint discipline) ③ 비즈니스 우선순위 = 1st release + enterprise 고객 onboarding 후 executive-grade cost reporting + 5-module cross-rollup KPI dashboard + scheduled executive report dispatch territory 필수 (Phase 11 showback department breakdown EXTENSION + Phase 12 anomaly detection baseline EXTENSION + Phase 13 forecast 12-month horizon EXTENSION + Phase 14 optimization accuracy tracking EXTENSION + Phase 15 tag compliance scoring EXTENSION) ④ Phase 15 wire `1b800d9` 의 tag_governance surface + Phase 14 wire `e904485` 의 optimization surface + Phase 13 wire `8b98030` 의 forecast surface + Phase 12 wire `f3c0e63` 의 anomaly surface + Phase 11 wire `e020ad0` 의 showback surface 의 자연스러운 carry-over chain = Phase 16 cross-module rollup 의 aggregate_function (5 modules cross-join with cost allocation key from Phase 15 tag_governance) + KPI calculation (Phase 12 anomaly detection baseline + Phase 13 forecast deviation + Phase 14 optimization realized savings + Phase 15 tag compliance scoring EXTENSION) + scheduled dispatch (KST cron weekly/monthly/quarterly/annual) EXTENSION 정합 ⑤ 1st release close-out retro + Epic 17 close-out retro + Phase 5~15 close-out retro territory verbatim 보존 + AD-22 owner-only RBAC + Epic 12 2FA 챌린지 보존 + AD-14 stack pin + NFR4 PII minimization ✅ PRESERVED + AD-43 FinOps Reporting & Executive Dashboard 신규 결정 ((a)~(g) 7 sub-decisions 모두 결정 wire 진입).

### F32.1 cross-FinOps-module rollup aggregator (5 modules cross-join EXTENSION, A454 결정)

- **F32.1.1** `apps/api/modules/finops/executive_dashboard_aggregator.py` NEW ~+200 LOC 결정 wire + `aggregate_executive_rollup(tenant_id, period_key, scope) -> ExecutiveRollup` TypedDict 16 fields (executive_rollup_id UUID PK + tenant_id UUID + period_key TEXT + scope enum tenant/department/cost_center/product_line + computed_at TIMESTAMPTZ + total_monthly_cost_krw NUMERIC(20,2) + monthly_cost_growth_pct NUMERIC(5,2) + cost_per_employee_krw NUMERIC(18,2) + cost_anomaly_count_30d INTEGER + forecast_deviation_pct NUMERIC(5,2) + idle_cost_monthly_krw NUMERIC(18,2) + tag_compliance_pct NUMERIC(5,2) + optimization_realized_savings_krw NUMERIC(18,2) + cost_per_revenue_krw NUMERIC(18,2) + trace_id TEXT + kpi_metadata JSONB 5-module source attribution) 결정 wire + 5-module cross-join EXTENSION (Phase 11 showback + Phase 12 anomaly + Phase 13 forecast + Phase 14 optimization + Phase 15 tag_governance) 결정 wire + 4 scope 옵션 (tenant + department + cost_center + product_line) 결정 wire.
- **F32.1.2** aggregate_function 5 functions 결정 wire = (1) showback_aggregate (Phase 11 wire `e020ad0` 의 showback_query EXTENSION) + (2) anomaly_aggregate (Phase 12 wire `f3c0e63` 의 anomaly_detection baseline EXTENSION) + (3) forecast_aggregate (Phase 13 wire `8b98030` 의 capacity_headroom + budget_burnrate EXTENSION) + (4) optimization_aggregate (Phase 14 wire `e904485` 의 optimization_accuracy_tracker EXTENSION) + (5) tag_compliance_aggregate (Phase 15 wire `1b800d9` 의 allocation_audit compliance scoring EXTENSION) 결정 wire.
- **F32.1.3** `monthly_cost_growth_pct` 계산 결정 wire = `(current_period_cost_krw - previous_period_cost_krw) / previous_period_cost_krw * 100` 결정 wire + previous_period 결정 wire = period_key 의 previous month 결정 wire + Phase 11 wire 의 12-period comparison EXTENSION 정합 + 음수 시 0 floor 처리 결정 wire.
- **F32.1.4** `cost_per_employee_krw` 계산 결정 wire = `total_monthly_cost_krw / active_employee_count` 결정 wire + active_employee_count 결정 wire = `tenant_settings.active_employee_count` INTEGER (Phase 12 budget_alert 의 4 industries baseline EXTENSION) 결정 wire + tenant_settings.active_employee_count NULL 시 cost_per_employee_krw = NULL (strict reject 없음, 표시 시 ⚠️ 배지) 결정 wire.
- **F32.1.5** `cost_anomaly_count_30d` 계산 결정 wire = Phase 12 wire `f3c0e63` 의 `phase_12_finops_anomaly` table 에서 confirmed anomaly (z_score > 3.0) count for last 30d 결정 wire + tenant_id + period_key JOIN 결정 wire + index hint `idx_phase_12_anomaly_tenant_period` 결정 wire.
- **F32.1.6** `forecast_deviation_pct` 계산 결정 wire = Phase 13 wire `8b98030` 의 `forecast_accuracy` table 에서 predicted_amount vs actual_amount deviation_pct 결정 wire + Period key JOIN + last forecast_definition_id EXTENSION 결정 wire.
- **F32.1.7** `idle_cost_monthly_krw` 계산 결정 wire = Phase 14 wire `e904485` 의 `phase_14_idle_resource` table 에서 current_monthly_cost_krw SUM for current period 결정 wire + idle_status = 'detected' filter 결정 wire.
- **F32.1.8** `tag_compliance_pct` 계산 결정 wire = Phase 15 wire `1b800d9` 의 `phase_15_compliance_report` table 에서 latest compliance score (last 30d rolling) 결정 wire + tenant_id scope JOIN 결정 wire.
- **F32.1.9** `optimization_realized_savings_krw` 계산 결정 wire = Phase 14 wire `e904485` 의 `phase_14_optimization_accuracy` table 에서 SUM(realized_savings_krw) for current period 결정 wire + tenant_id scope JOIN 결정 wire.
- **F32.1.10** `cost_per_revenue_krw` 계산 결정 wire = `total_monthly_cost_krw / tenant_settings.monthly_revenue_krw` 결정 wire + tenant_settings.monthly_revenue_krw NUMERIC(20,2) (per-tenant override EXTENSION) 결정 wire + NULL 시 cost_per_revenue_krw = NULL (표시 시 ⚠️ 배지) 결정 wire.
- **F32.1.11** `kpi_metadata` JSONB 5-module source attribution 결정 wire = `{"showback_source": "phase_11_finops_showback", "anomaly_source": "phase_12_finops_anomaly", "forecast_source": "phase_13_finops_forecast", "optimization_source": "phase_14_finops_optimization", "tag_governance_source": "phase_15_finops_tag_governance"}` 결정 wire + per-tenant override EXTENSION 결정 wire.
- **F32.1.12** audit-first INSERT `executive_dashboard_viewed` 결정 wire (executive rollup dashboard 진입 시 tenant_id + user_id + actor_role + view_timestamp 1행 INSERT, CR 1-1 verbatim) + cache key = `(tenant_id, period_key, executive_rollup_hash)` 결정 wire + NFR11 P95 ≤ 30s SLO 결정 wire.

### F32.2 cross-module KPI selector (8 NEW KPI, A455 결정)

- **F32.2.1** 8 NEW KPI TypedDict 결정 wire = (1) `total_monthly_cost_krw` (F32.1.1 의 NUMERIC(20,2) field) + (2) `monthly_cost_growth_pct` (F32.1.3 의 calculation result) + (3) `cost_per_employee_krw` (F32.1.4 의 calculation result) + (4) `cost_anomaly_count_30d` (F32.1.5 의 calculation result) + (5) `forecast_deviation_pct` (F32.1.6 의 calculation result) + (6) `idle_cost_monthly_krw` (F32.1.7 의 calculation result) + (7) `tag_compliance_pct` (F32.1.8 의 calculation result) + (8) `optimization_realized_savings_krw` (F32.1.9 의 calculation result) 결정 wire + 1 optional KPI = `cost_per_revenue_krw` (F32.1.10 의 calculation result, tenant_settings.monthly_revenue_krw NULL 시 제외).
- **F32.2.2** `apps/api/modules/finops/cross_module_kpi.py` NEW ~+180 LOC 결정 wire + `calculate_cross_module_kpi(tenant_id, period_key, kpi_set: List[str]) -> KPIResult` TypedDict 11 fields (kpi_result_id UUID PK + tenant_id UUID + period_key TEXT + kpi_set JSONB + kpi_values JSONB 8 KPI results + calculation_metadata JSONB (last 5-module rollup hash + cache hit/miss + computation_ms) + computed_at TIMESTAMPTZ + trace_id TEXT) 결정 wire + 8 KPI selector 결정 wire.
- **F32.2.3** `kpi_set` JSONB 결정 wire = `["total_monthly_cost_krw", "monthly_cost_growth_pct", "cost_anomaly_count_30d", "tag_compliance_pct", "optimization_realized_savings_krw"]` default 5 KPI (mandatory) + 3 optional = `[cost_per_employee_krw, forecast_deviation_pct, idle_cost_monthly_krw]` 결정 wire + tenant override EXTENSION 결정 wire.
- **F32.2.4** `kpi_values` JSONB format 결정 wire = `{"total_monthly_cost_krw": 12345678.90, "monthly_cost_growth_pct": 5.32, ...}` 결정 wire + per-KPI 형식 정의 결정 wire (numeric = NUMERIC(20,2) / integer = INTEGER / percentage = NUMERIC(5,2) with % suffix) + audit-first INSERT 결정 wire.
- **F32.2.5** `calculation_metadata.cache_hit` boolean 결정 wire + true 면 computation_ms ≤ 100ms (cache sub-100ms per Phase 12 cache hit pattern verbatim) + false 면 cold compute NFR11 SLO ≤ 30s 결정 wire.
- **F32.2.6** KPI alert threshold 결정 wire = (1) `tag_compliance_pct < 80.0` → red badge "⚠️ 태그 컴플라이언스 미달" 결정 wire + (2) `cost_anomaly_count_30d > 5` → red badge "🚨 이상 패턴 다수 발생" 결정 wire + (3) `monthly_cost_growth_pct > 20.0` → yellow badge "📈 비용 급증" 결정 wire + (4) 4 industries baseline INT4 EXTENSION 결정 wire (manufacturing ≤ 8.0% / service ≤ 12.0% / manufacturing_service ≤ 10.0% / manufacturing_service_other ≤ 10.0%) + per-tenant override EXTENSION 결정 wire.
- **F32.2.7** `cost_per_employee_krw` calculation source attribution 결정 wire = `tenant_settings.active_employee_count` 로 결정 + Phase 11 wire `e020ad0` 의 tenant_settings 의 employees JSONB (per-tenant EXTENSION) 결정 wire + employee count 변경 시 AD-25 cache invalidation publish 결정 wire.
- **F32.2.8** 4 industries KPI baseline 결정 wire (Phase 11~15 baseline EXTENSION) = (1) manufacturing : `total_monthly_cost_krw` percentile + `monthly_cost_growth_pct ≤ 8.0%` + `tag_compliance_pct ≥ 90.0` + (2) service : `monthly_cost_growth_pct ≤ 12.0%` + `tag_compliance_pct ≥ 85.0` + (3) manufacturing_service : both (1)(2) mixed + (4) manufacturing_service_other : per-tenant baseline + override EXTENSION 결정 wire.
- **F32.2.9** `kpi_set` 의 strict validation 결정 wire = kpi_names NOT IN (8 KPI + 1 optional) → strict reject + CR 11-4 D-PARITY-01 verbatim 적용 결정 wire + Phase 12 wire `f3c0e63` 의 source_kind strict reject EXTENSION 정합.
- **F32.2.10** cache key 결정 wire = `(tenant_id, period_key, kpi_set_hash, kpi_values_hash)` 결정 wire + cache TTL 1h (executive dashboard 의 sub-100ms hit 보장) 결정 wire + cache eviction LRU (Phase 13 cache pattern verbatim) 결정 wire.
- **F32.2.11** audit-first INSERT `executive_kpi_refreshed` 결정 wire (KPI refresh 시 tenant_id + user_id + kpi_set + period_key 1행 INSERT) + NFR11 P95 ≤ 30s SLO 결정 wire.
- **F32.2.12** cross-module rollup 5 module source aggregation sequence 결정 wire (sequential 5 steps) = (1) showback aggregate + 50ms + (2) anomaly aggregate + 30ms + (3) forecast aggregate + 80ms + (4) optimization aggregate + 40ms + (5) tag_governance aggregate + 50ms = total ≤ 250ms NFR11 cold compute 결정 wire.

### F32.3 executive report generation engine (PDF + CSV + Excel, A456 결정)

- **F32.3.1** `apps/api/modules/finops/executive_report_generator.py` NEW ~+220 LOC 결정 wire + `generate_executive_report(tenant_id, period_key, report_format, schedule_id) -> ExecutiveReport` TypedDict 14 fields (executive_report_id UUID PK + tenant_id UUID + period_key TEXT + report_format enum pdf/csv/excel + report_period enum monthly/quarterly/annual + computed_rollup_id UUID FK + report_file_path TEXT + report_file_size_bytes INTEGER + generation_duration_ms INTEGER + kpi_summary JSONB + dispatch_ready boolean + trace_id TEXT + generated_at TIMESTAMPTZ + generated_by_user_id UUID) 결정 wire + 3 format 옵션 PDF/CSV/Excel 3종 결정 wire.
- **F32.3.2** 3 NEW format generator 결정 wire = (1) **PDF generator** (`report_pdf_generator.py` NEW ~+150 LOC) - Phase 9 wire 의 ReportGenerator EXTENSION + A4 portrait + ko-KR 폰트 Pretendard + 8 KPI 차트 + 5-module source data appendix 1-line ref + owner-only watermark 결정 wire + (2) **CSV generator** (`report_csv_generator.py` NEW ~+80 LOC) - 8 KPI + 5-module source attribution row + tenant_id + period_key 결정 wire + (3) **Excel generator** (`report_excel_generator.py` NEW ~+120 LOC) - 8 KPI sheet + 5-module raw data sheet 5장 + chart sheet 결정 wire + openpyxl 3.1.2 AD-14 stack pin EXTENSION 결정 wire.
- **F32.3.3** `report_period` 3 옵션 결정 wire = (1) **monthly** (current month) + (2) **quarterly** (last 3 months) + (3) **annual** (last 12 months) 결정 wire + Phase 11 wire `e020ad0` 의 showback period selector EXTENSION 정합.
- **F32.3.4** `kpi_summary` JSONB 결정 wire = `{"total_monthly_cost_krw": ..., "monthly_cost_growth_pct": ..., "8 KPI values": ..., "5_module_source_attribution": {...}}` 결정 wire + Phase 16 F32.1.11 kpi_metadata EXTENSION.
- **F32.3.5** `dispatch_ready` boolean 결정 wire = true 면 scheduled dispatch 가능 + false 면 generation only (on-demand) 결정 wire + AD-22 owner-only RBAC bind (executive report on-demand = owner-only + Epic 12 2FA 챌린지 보존).
- **F32.3.6** PDF 디자인 결정 wire = page 1 = cover (tenant_id + period_key + 8 KPI big-number 8 rows) + page 2 = monthly trend chart 12-month (Phase 13 forecast baseline EXTENSION) + page 3 = 5-module breakdown table 5 rows + page 4 = 8 KPI bar chart 8 + page 5 = tag_compliance_pct trend + page 6 = footer (generated_at + generated_by + ko-KR 1-line 한글 disclaimer "이 보고서는 내부 의사결정 목적이며 외부 공개 시 owner 승인이 필요합니다") 결정 wire + ARIA labels WCAG 2.1 AA 적용 결정 wire.
- **F32.3.7** 4 industries baseline period 결정 wire = (1) manufacturing : monthly default + quarterly on-request + annual December only + (2) service : monthly + quarterly + annual December + (3) manufacturing_service : both (1)(2) + (4) manufacturing_service_other : per-tenant override EXTENSION.
- **F32.3.8** report dispatch fallback 결정 wire = generation timeout 시 retry 1회 (Phase 13 retry pattern EXTENSION) + 2회 실패 시 fallback monthly summary 5-line (no report file) + audit-first INSERT `executive_report_failed` 결정 wire.
- **F32.3.9** `report_file_path` 결정 wire = `apps/api/storage/executive_reports/{tenant_id}/{period_key}/{executive_report_id}.{ext}` 결정 wire + RLS 자동 적용 CR 0-2 verbatim + storage bucket S3-호환 결정 wire + Phase 5 wire `f093f8c` S3 storage EXTENSION 정합.
- **F32.3.10** `dispatch_ready` 결정 wire = generation 성공 시 true + Tenant RBAC owner-only 호출 결정 wire (owner-only on-demand generation = Epic 12 2FA 챌린지 보존 + audit-first INSERT 결정 wire).
- **F32.3.11** audit-first INSERT `executive_report_generated` 2행 결정 wire (executive report file generate 시 1행 + 5-module source attribution 1행 = 2행 append, CR 1-1 verbatim) + NFR8 audit retention_period 5-year append-only 결정 wire.
- **F32.3.12** 3 format 동시 generation 결정 wire = true 면 PDF + CSV + Excel 3 format 일괄 생성 + 각각 report_file_path 별도 + executive_report_id 동일 결정 wire + generation_duration_ms SUM ≤ 30s NFR11 SLO 결정 wire.

### F32.4 scheduled dispatch KST cron (A457 결정)

- **F32.4.1** `apps/api/modules/finops/scheduled_executive_dispatch.py` NEW ~+180 LOC 결정 wire + `ScheduledDispatch` TypedDict 12 fields (scheduled_dispatch_id UUID PK + tenant_id UUID + scheduled_period enum monthly/quarterly/annual + scheduled_cron TEXT + recipient_emails JSONB (List[str]) + dispatch_format enum pdf/csv/excel/all_3 + dispatch_status enum active/paused/expired + last_dispatch_at TIMESTAMPTZ + last_dispatch_status enum success/failed/not_run + next_dispatch_at TIMESTAMPTZ + audit_first_insert boolean default true + trace_id TEXT) 결정 wire.
- **F32.4.2** KST cron 4 NEW cadence 결정 wire = (1) **weekly** (매주 월요일 09:00 KST) + (2) **monthly** (매월 1일 09:00 KST) + (3) **quarterly** (분기별 1일 09:00 KST) + (4) **annual** (매년 1월 1일 09:00 KST) 결정 wire + Phase 11 wire `e020ad0` 의 12-period comparison EXTENSION 정합.
- **F32.4.3** `apps/api/jobs/scheduled_executive_dispatch_job.py` NEW ~+150 LOC 결정 wire + APScheduler (3.10.4 AD-14 stack pin EXTENSION) cron job 결정 wire + next_dispatch_at 계산 (Phase 12 wire `f3c0e63` 의 scheduled_alert_job pattern verbatim EXTENSION) 결정 wire + 4 cadence 모두 wire 결정 wire + L4 service-layer 결정 wire.
- **F32.4.4** recipient_emails 결정 wire = JSONB array of validated email addresses 결정 wire + max 50 recipients (per tenant) + STRICT email format validation (RFC 5322 EXTENSION) + no internal email (audit_first_insert 결정 wire + 1-row sender actor record 결정 wire).
- **F32.4.5** `dispatch_status` 결정 wire = 3 옵션 (active + paused + expired) + paused → scheduled_dispatch_id 의 pause 결정 wire (owner-only 1-click) + expired → next_dispatch_at < NOW() - 90d 결정 wire (auto-expire 결정 wire + audit-first INSERT `scheduled_dispatch_expired` 결정 wire).
- **F32.4.6** `last_dispatch_status` 3 옵션 결정 wire = (1) success (executive_report_generated 정상 발송) + (2) failed (executive_report_failed 발송 실패) + (3) not_run (next_dispatch_at > NOW() 미실행) 결정 wire + audit-first INSERT 결정 wire.
- **F32.4.7** dispatch email content 결정 wire = (1) tenant_name (Phase 11 tenant_settings.name) + (2) report_period (period_key) + (3) download_url (signed S3 URL 24h expiration 결정 wire + Phase 5 wire S3 signed URL EXTENSION) + (4) ko-KR 1-line summary "월간 비용 보고서가 준비되었습니다." 결정 wire + (5) ARIA labels WCAG 2.1 AA 적용 결정 wire + 2-step unsubscribe 결정 wire (CR GDPR opt-out EXTENSION 결정 wire).
- **F32.4.8** `dispatch_format` 결정 wire = 4 옵션 (pdf OR csv OR excel OR all_3 동시) 결정 wire + all_3 동시 시 3 file attachment 결정 wire (max 10MB per file 결정 wire + larger 시 S3 link only 결정 wire).
- **F32.4.9** retry 정책 결정 wire = dispatch 실패 시 exponential backoff 결정 wire (Phase 12 wire retry pattern EXTENSION) = retry 3회 (1s + 5s + 30s) + 3회 실패 시 fail + audit-first INSERT 결정 wire.
- **F32.4.10** `audit_first_insert` boolean 결정 wire = true 면 dispatch 시 audit-first INSERT 1행 (CR 1-1 verbatim 결정 wire) + dispatch_status 변화마다 INSERT 결정 wire (created + paused + resumed + expired) + NFR8 5-year append-only 결정 wire.
- **F32.4.11** audit-first INSERT 2 NEW 결정 wire = (1) `executive_report_dispatched` (dispatch 성공 시 1행) + (2) `executive_scheduled_dispatch_evaluated` (cron 평가 시 1행, success/failed/not_run) 결정 wire + AD-22 owner-only RBAC bind 결정 wire.
- **F32.4.12** Phase 11~15 cross-rollup 보존 결정 wire = F32 의 모든 scheduled dispatch 는 Phase 11~15 의 5 module source data 를 aggregate 한 executive rollup 결과를 발송 + per-tenant scope EXTENSION + 4 industries baseline EXTENSION + dry-run 모드 (`--finops-executive-dispatch-dry-run` 1 NEW CLI flag) 결정 wire.

### F32.5 tenant-scoped executive role RBAC (A458 결정)

- **F32.5.1** `Capability.FINOPS_REPORTING` 1 NEW enum 결정 wire = `apps/api/core/capability.py` MODIFIED + `Capability.FINOPS_REPORTING = "finops_reporting"` 1 NEW 결정 wire + 4 industries grants 결정 wire (manufacturing + service + manufacturing_service + manufacturing_service_other 모두 ✅/✅/✅/✅ CR 12-1 L4 industry-agnostic precedent verbatim) + Phase 11~15 FinOps pattern verbatim EXTENSION 결정 wire.
- **F32.5.2** executive role 결정 wire = tenant_settings.executive_role_enabled boolean default false + Epic 12 2FA 챌린지 보존 + AD-22 owner-only RBAC EXTENSION 결정 wire + Epic 16 wire 의 tenant_idp_admin EXTENSION 정합.
- **F32.5.3** `apps/api/dependencies/capability.py` MODIFIED + `require_finops_reporting` 1 NEW dep 결정 wire + 4 industries grants EXTENSION 결정 wire + pattern Phase 11~15 verbatim 미러 결정 wire.
- **F32.5.4** executive role hierarchy 결정 wire = (1) tenant_owner (Epic 12 2FA + owner-only) + (2) tenant_admin (read-only executive dashboard) + (3) tenant_analyst (read-only cross-module KPI) + (4) tenant_viewer (only summary stats) 결정 wire + per-tenant EXTENSION decision wire + 5 roles hierarchy 보존 결정 wire.
- **F32.5.5** Epic 12 2FA 챌린지 보존 결정 wire = executive report generation + scheduled dispatch 모두 Epic 12 2FA 챌린지 통과 후 호출 가능 + AD-22 owner-only RBAC EXTENSION 결정 wire + 2FA 미통과 시 strict reject 결정 wire (CR 12-5 D-14 envelope EXACT 적용 결정 wire).
- **F32.5.6** cross-tenant contamination 방어 결정 wire (CR 12-5 D-PARITY-01 verbatim) = tenant_id scope 자동 적용 + executive dashboard 진입 시 `require_finops_reporting(tenant_id)` 호출 결정 wire + 다른 tenant_id 호출 시 strict reject + 1-row counter increment 결정 wire.
- **F32.5.7** cross-industry consistency 검증 결정 wire = 4 industries 모두 F32 capability grant + 동일 RBAC pattern 적용 + industry_agnostic = true (CR 12-1 L4 EXTENSION) 결정 wire + drift detector `tests/integration/test_capability_matrix_v1_42_drift.py` 8 NEW pytest cases EXTENSION 결정 wire.
- **F32.5.8** audit-first INSERT 결정 wire = executive dashboard 진입 + KPI refresh + report generate + dispatch 모두 audit-first INSERT (CR 1-1 verbatim) + NFR8 5-year retention 결정 wire.
- **F32.5.9** Frontend RBAC 결정 wire = ko-KR.json `finops_reporting.role.*` namespace EXTENSION 결정 wire + `finops_reporting.role.owner.label = "대표 (2FA 인증)"` + `finops_reporting.role.owner.description = "2FA 인증된 대표만 Executive Report 생성 가능"` 결정 wire + role badge ARIA labels WCAG 2.1 AA 적용 결정 wire.
- **F32.5.10** capability drift detector EXTENSION 결정 wire = phase 16 wire 진입 시점에 `tests/integration/test_capability_matrix_v1_42_drift.py` NEW + 8 NEW pytest cases (4 industry × 2 grant-type 결정 wire) 결정 wire + Phase 15 wire pattern verbatim 미러 (FINOPS_TAG_GOVERNANCE 8 cases EXTENSION).
- **F32.5.11** `FinopsReportingAction` Literal 결정 wire = `apps/api/core/audit_action.py` MODIFIED + ActionClass.FINOPS_REPORTING 1 NEW enum 결정 wire + 8 NEW Literal values (`executive_report_generated` + `executive_dashboard_viewed` + `executive_kpi_refreshed` + `executive_report_exported` + `executive_report_dispatched` + `executive_scheduled_dispatch_evaluated` + `finops_reporting_dry_run_executed` + `cross_module_kpi_calculated`) 결정 wire + Phase 11~15 audit action EXTENSION 정합.
- **F32.5.12** `apps/api/core/errors.py` MODIFIED + 10 NEW typed exception classes 결정 wire (CR 12-5 D-14 envelope) = `ExecutiveRollupCalculationError(500)` + `CrossModuleKPIError(500)` + `ExecutiveReportGenerationError(500)` + `ExecutiveReportExportError(500)` + `ScheduledDispatchInvalidCronError(400)` + `ScheduledDispatchRecipientInvalidError(400)` + `ScheduledDispatchExpiredError(400)` + `ExecutiveDispatchEmailInvalidError(400)` + `FinopsReportingUnsupportedFormatError(400)` + `FinopsReportingCapabilityDeniedError(403)` 결정 wire + Phase 11~15 typed exception pattern verbatim EXTENSION.

### F32.6 executive dashboard UI (10 sub-ACs, A458 결정)

- **F32.6.1** `apps/web/app/[locale]/(dashboard)/admin/finops/reporting/page.tsx` NEW ~+180 LOC 결정 wire + 5 sub-components 결정 wire (ExecutiveDashboardAggregator + CrossModuleKPISelector + ExecutiveReportGeneratorPanel + ScheduledDispatchConfigPanel + ComplianceTrendMiniChart) 결정 wire + owner-only RBAC AD-22 verbatim + Epic 12 2FA 챌린지 보존 결정 wire.
- **F32.6.2** `apps/web/components/finops/FinopsReportingDashboardPanel.tsx` Client 5 sub-component 결정 wire + Recharts 2.12.7 AD-14 stack pin EXTENSION 결정 wire + Phase 11~15 finops dashboard pattern verbatim EXTENSION 결정 wire.
- **F32.6.3** ExecutiveDashboardAggregator component 결정 wire = scope selector (tenant + department + cost_center + product_line 4 NEW 옵션) + period selector (current/previous/last 3/6/12 months) + 8 KPI big-number 8 rows + 5-module source attribution chip 5 NEW chip 결정 wire + Phase 11 wire `e020ad0` 의 showback dashboard EXTENSION 정합.
- **F32.6.4** CrossModuleKPISelector component 결정 wire = 5 mandatory KPI 선택 + 3 optional KPI 선택 = total 8 KPI 결정 wire + 선택된 KPI 의 kpi_set JSONB 생성 결정 wire + CR 11-4 D-005 unknown state reject EXACT 적용 결정 wire.
- **F32.6.5** ExecutiveReportGeneratorPanel component 결정 wire = period selector (monthly + quarterly + annual 3) + format selector (PDF + CSV + Excel 3 NEW checkbox) + on-demand generation trigger button + 2FA 챌린지 모달 결정 wire (Epic 12 EXTENSION 결정 wire) + progress bar 결정 wire + generated report download link 결정 wire (signed URL 24h expiration EXTENSION 결정 wire).
- **F32.6.6** ScheduledDispatchConfigPanel component 결정 wire = cadence selector (weekly + monthly + quarterly + annual 4) + format selector + recipients input (multi-email + max 50) + scheduled_dispatch_id 생성 후 일시정지/재개 버튼 + last_dispatch_status 표시 + next_dispatch_at 표시 결정 wire + 4 cadence wire 결정 wire.
- **F32.6.7** ComplianceTrendMiniChart component 결정 wire = last 12-month tag_compliance_pct trend (Phase 15 wire `1b800d9` 의 compliance scoring EXTENSION 정합) + 95% threshold reference line 결정 wire + Recharts LineChart (Phase 12~15 pattern verbatim) + ARIA labels WCAG 2.1 AA 결정 wire.
- **F32.6.8** toast notification 결정 wire = `executive_report_generated` 시 toast (success → green + 5초) + `executive_report_failed` 시 toast (failed → red + 10초) + `executive_report_dispatched` 시 toast (dispatched → blue + 5초) + `finops_reporting_dry_run_executed` 시 toast 결정 wire + Phase 11~15 toast notification EXTENSION 정합.
- **F32.6.9** Vitest RTL render discipline CR 11-4 D-003 verbatim 결정 wire = 7 NEW vitest cases (ExecutiveDashboardAggregator 1 + CrossModuleKPISelector 1 + ExecutiveReportGeneratorPanel 1 + ScheduledDispatchConfigPanel 1 + ComplianceTrendMiniChart 1 + ko-KR SSOT 2) 결정 wire + Phase 11~15 vitest pattern verbatim EXTENSION.
- **F32.6.10** ko-KR.json `finops_reporting.*` namespace EXTENSION ~30 keys 결정 wire + `finops_reporting.title = "Executive Dashboard"` + `finops_reporting.kpi.total_monthly_cost = "이번 달 총 비용"` + `finops_reporting.kpi.monthly_cost_growth = "전월 대비 증가율"` + ... 결정 wire + ARIA labels WCAG 2.1 AA 결정 wire + CR 11-4 D-002 verbatim SSOT 결정 wire.

### F32.7 Capability matrix v1.42 EXTENSION FINOPS_REPORTING (12 sub-ACs, A458 결정)

- **F32.7.1** capability matrix v1.41 → v1.42 EXTENSION FINOPS_REPORTING 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅ 결정 wire + Phase 11~15 FINOPS_* pattern verbatim EXTENSION + CR 12-1 L4 precedent 미러.
- **F32.7.2** `apps/api/core/capability.py` MODIFIED + `Capability.FINOPS_REPORTING = "finops_reporting"` 1 NEW enum 결정 wire + 4 industries EXTENSION 결정 wire + Phase 11~15 verbatim EXTENSION 결정 wire.
- **F32.7.3** `apps/api/dependencies/capability.py` MODIFIED + `require_finops_reporting` 1 NEW dep 결정 wire + 4 industries grants EXTENSION 결정 wire + Phase 11~15 verbatim EXTENSION 결정 wire.
- **F32.7.4** ActionClass.FINOPS_REPORTING 1 NEW enum 결정 wire + 8 NEW Literal values (F32.5.11 결정) + audit-first INSERT 8 NEW 결정 wire + CR 1-1 verbatim 적용 결정 wire.
- **F32.7.5** 4-industry grant parity 정합 (manufacturing + service + manufacturing_service + manufacturing_service_other 모두 ✅) 결정 wire + industry_agnostic = true 결정 wire.
- **F32.7.6** drift detector `tests/integration/test_capability_matrix_v1_42_drift.py` NEW 8 NEW pytest cases 결정 wire + 4 industries × 2 grant-type (Capability + Dependency) EXTENSION.
- **F32.7.7** `apps/api/alembic/versions/0048_phase_16_finops_reporting.py` NEW ~+180 LOC + 5 NEW tables 결정 wire (executive_rollup + cross_module_kpi + executive_report + scheduled_dispatch + executive_report_attempt) + 2 NEW preview tables (`phase_16_finops_executive_rollup_preview` + `phase_16_finops_scheduled_dispatch_preview`) 결정 wire + RLS 자동 적용 CR 0-2 verbatim + CHECK + UNIQUE + indexes 결정 wire + audit-first INSERT 결정 wire.
- **F32.7.8** `apps/api/jobs/scheduled_executive_dispatch_job.py` NEW APScheduler 4 cadence cron 결정 wire (F32.4 결정) + 4 cadence 모두 wire 결정 wire (weekly + monthly + quarterly + annual) 결정 wire.
- **F32.7.9** ALLOWED_SERVICE_SUBMODULES (CR 11-3 lesson) 즉시 sweep EXTENSION 결정 wire = `m16_finops_reporting` 신규 submodule 등록 + Phase 11~15 verbatim EXTENSION (Phase 15 m15_finops_tag_governance 패턴 보존).
- **F32.7.10** NFR11 (P95 ≤ 30s) 결정 wire = executive dashboard view NFR11 cold compute ≤ 30s + KPI refresh cache hit sub-100ms EXTENSION + report generation PDF+CSV+Excel 동시 ≤ 30s EXTENSION + scheduled dispatch NFR15 0분 손실 보장 EXTENSION.
- **F32.7.11** NFR8 (감사로그 5년 append-only) 결정 wire = 8 NEW audit log entries (`executive_report_generated` + `executive_dashboard_viewed` + `executive_kpi_refreshed` + `executive_report_exported` + `executive_report_dispatched` + `executive_scheduled_dispatch_evaluated` + `finops_reporting_dry_run_executed` + `cross_module_kpi_calculated`) + 5-year retention EXTENSION.
- **F32.7.12** AD-43 신규 결정 진입 (a)~(g) 7 sub-decisions 결정 wire + 부록 A architectural decisions table 1 NEW row EXTENSION + Phase 12~15 AD-39~AD-42 verbatim EXTENSION 결정 wire.

### F32.8 dry-run + Tests + wire scope T1~T8 (10 sub-ACs, A458 결정)

- **F32.8.1** dry-run mode 결정 wire = `--finops-executive-rollup-dry-run` + `--finops-cross-module-kpi-dry-run` + `--finops-executive-report-dry-run` + `--finops-scheduled-dispatch-dry-run` + `--finops-reporting-export-dry-run` 5 NEW CLI flag + dry-run 시 actual `executive_dashboard_viewed` + `executive_kpi_refreshed` + `executive_report_generated` + `executive_report_dispatched` + `executive_scheduled_dispatch_evaluated` audit-first INSERT skip 결정 wire + Phase 11~15 dry-run pattern verbatim EXTENSION.
- **F32.8.2** dry-run preview 결과 결정 wire = `phase_16_finops_executive_rollup_preview` + `phase_16_finops_scheduled_dispatch_preview` 2 table 결정 wire + preview_id UUID PK + tenant_id UUID + preview_type enum + period_key TEXT + preview_data JSONB + computed_at TIMESTAMPTZ DEFAULT NOW() + trace_id TEXT + RLS 자동 적용 CR 0-2 verbatim + audit-first INSERT `finops_reporting_dry_run_executed` 결정 wire.
- **F32.8.3** dry-run CLI flag 5 NEW 결정 wire + Phase 11~15 dry-run pattern verbatim EXTENSION + 5 CLI flag 모두 wire 결정 wire + dry-run 결과 preview 결정 wire.
- **F32.8.4** tests 결정 wire = ~+56 NEW pytest PASS (executive_dashboard_aggregator 8 + cross_module_kpi 8 + executive_report_generator 7 + scheduled_executive_dispatch 6 + alembic 0048 5 + audit action 8 + capability matrix v1.42 8 + dry-run 6 = ~+56 NEW pytest PASS) 결정 wire + Phase 11~15 test pattern verbatim EXTENSION.
- **F32.8.5** vitest tests ~+7 NEW vitest PASS 결정 wire (ExecutiveDashboardAggregator 1 + CrossModuleKPISelector 1 + ExecutiveReportGeneratorPanel 1 + ScheduledDispatchConfigPanel 1 + ComplianceTrendMiniChart 1 + ko-KR SSOT 2 + finops reporting dashboard parity CR 12-5 D-PARITY-01 결정 wire) + Phase 11~15 vitest pattern verbatim EXTENSION.
- **F32.8.6** ruff + tsc 0 NEW + SDR drift gate 결정 wire + A36 SDR 검증 4-step 자동 적용 결정 wire + 0 NEW ruff + 0 NEW tsc + 0 regressions + SDR drift gate PASS 결정 wire.
- **F32.8.7** wire scope T1~T8 결정 wire = T1 executive_dashboard_aggregator + cross_module_kpi 결정 wire + T2 executive_report_generator + 3 format generators (PDF + CSV + Excel) 결정 wire + T3 scheduled_executive_dispatch + APScheduler 4 cadence cron 결정 wire + T4 alembic 0048 phase_16_finops_reporting 5 tables + RLS 5 tables + 2 preview tables 결정 wire + T5 audit action EXTENSION 8 NEW + 10 NEW typed exception classes 결정 wire + T6 capability v1.42 EXTENSION + frontend finops reporting dashboard 결정 wire + T7 scheduled_dispatch_job wire 결정 wire + T8 3중 게이트 FINAL CLEAN atomic commit 결정 wire + estimated ~+33-38 files estimate = ~+21 NEW + ~+12 MODIFIED 결정 wire + Phase 15 wire `1b800d9` 의 ~19 files pattern verbatim EXTENSION 결정 wire + Phase 11~15 wire 의 ~25-30 files pattern verbatim EXTENSION.
- **F32.8.8** A19 cohesion pattern 9 surface EXTENSION PASS 결정 wire (FinOps Reporting & Executive Dashboard surface NEW = F32.1~F32.8 결정 wire) + Phase 15 wire `1b800d9` 의 FinOps Tag Governance surface NEW = F31.1~F31.8 pattern verbatim 미러 + Phase 14 wire `e904485` 의 FinOps Optimization surface NEW = F30.1~F30.8 pattern verbatim 미러 + Phase 13 wire `8b98030` 의 FinOps Forecasting surface NEW = F29.1~F29.8 pattern verbatim 미러 + Phase 12 wire `f3c0e63` 의 FinOps Anomaly surface NEW = F28.1~F28.8 pattern verbatim 미러 + Phase 11 wire `e020ad0` 의 FinOps Showback surface NEW = F27.1~F27.7 pattern verbatim 미러 결정 wire.
- **F32.8.9** CR lessons applied 14종 결정 wire (CR 0-2 RLS lesson ✅ APPLIED + CR 1-1 audit-first INSERT ✅ APPLIED (8 NEW audit log entries + 5-module source attribution 2행 append = 10 NEW effective audit events) + CR 4-3/4-4 lessons carry ✅ APPLIED + CR 1-1 ContextVar lesson ✅ APPLIED + CR 1-1 RSC boundary lesson ✅ APPLIED + CR 9-6 commit message discipline ✅ APPLIED + CR 11-3 honest-DEFER discipline ✅ APPLIED (125번째 epic 연속 정직 회복) + CR 11-4 D-001~D-005 + P-015 ✅ APPLIED + CR 12-1 L4 ✅ APPLIED + CR 12-5 D-14 typed exception envelope ✅ APPLIED (10 NEW typed exception classes) + CR 12-5 D-PARITY-01 inversion ✅ APPLIED + CR 12-5 D-GATE-01 inversion ✅ APPLIED + A36 SDR 검증 4-step 자동 적용 ✅ + AD-14 stack pin ✅ APPLIED + AD-22 owner-only RBAC ✅ APPLIED + NFR4 PII minimization ✅ PRESERVED) 결정 wire.
- **F32.8.10** D-DEFER-* honestly 결정 wire (CR 11-3 125번째 epic 연속 정직 회복 결정 wire 보존) = D-1-1-DEFER-1/2/3 RESOLVED 보존 60~125번째 + D-EPIC-16-REVIEW-DEFER-2~6 ✅ RESOLVED 보존 78~125번째 + D-PHASE-4-DR-DEFER-1/2 ✅ RESOLVED 보존 73~125번째 + D-EPIC-17-WIRE-DEFER-T2-T3-UI ✅ RESOLVED 보존 83~125번째 + D-RETENTION-1 ✅ RESOLVED 보존 85~125번째 + D-OBSERVABILITY-1 ✅ RESOLVED 보존 89~125번째 + D-PERFORMANCE-1 ✅ RESOLVED 보존 93~125번째 + D-CHAOS-1 ✅ RESOLVED 보존 100~125번째 + D-SLO-1 ✅ RESOLVED 보존 101~125번째 + D-FINOPS-1 ✅ RESOLVED 보존 105~125번째 + D-FINOPS-2 ✅ RESOLVED 보존 109~125번째 + D-FINOPS-3 ✅ RESOLVED 보존 113~125번째 + D-FINOPS-4 ✅ RESOLVED 보존 117~125번째 + D-FINOPS-5 ✅ honestly DEFER 보존 121~125번째 결정 wire + Epic 1 ~ Epic 17 + Phase 3 ~ Phase 15 + 1st release cycle 정합 보존 결정 wire (cj-style 125번째 epic 연속 정직 회복 pre-flight 정합 sweep) + 결정 wire 일자: 2026-08-25 (KST) + **next**: 옵션 (a) Phase 16 bmad-create-story spec entry 진입 (cj-style 126번째 wire 진입 시점) / 옵션 (b) Phase 16 bmad-dev-story atomic wire T1~T8 진입 (cj-style 127번째) / 옵션 (c) Phase 16 close-out retro 진입 (cj-style 128번째) / 옵션 (d) Epic 18+ 진입 / 옵션 (e) D-DEFER-* follow-up 결정 wire 보류 결정 wire.

---

## F33. FinOps Sustainability & Carbon Reporting (Phase 17 territory 신규)

> **§F33 결정 wire 진입** — Phase 16 close-out retro `26fd530` (cj-style 128번째 wire entry) 직후 다음 옵션 5종 중 **사용자 권장 결정 = 옵션 (a) Phase 17+ 진입 + 옵션 (a) FinOps Sustainability & Carbon Reporting (Recommended) (carbon_emissions_aggregator 6-module cross-rollup aggregator + sustainability_kpi_selector 8 NEW KPI calculations (carbon_intensity_kgco2e_per_krw + cloud_carbon_emissions_kgco2e + data_center_pue + renewable_energy_pct + scope1_emissions_kgco2e + scope2_emissions_kgco2e + scope3_emissions_kgco2e + carbon_offset_kgco2e) + sustainability_report_generation_engine PDF + CSV + Excel + scheduled_sustainability_dispatch KST cron 4 cadence + tenant_scoped_sustainability_role RBAC owner-only + sustainability_dashboard UI 5 sub-components + ko-KR.json `finops_sustainability.*` namespace EXTENSION ~30 keys + audit-first INSERT 8 NEW + Capability FINOPS_SUSTAINABILITY 1 NEW + dry-run mode + tests + wire scope T1~T8) = Phase 17 territory 결정 wire 진입**. rationale 5종: ① Phase 16 wire `81ae00a` FinOps Reporting & Executive Dashboard territory 의 natural backend SUSTAINABILITY ROLLUP LAYER EXTENSION 결정 wire 진입 (6 FinOps modules 의 data aggregate → sustainability-grade carbon rollup view + cross-module sustainability KPI 8개 + scheduled sustainability report export PDF+CSV+Excel monthly/quarterly/annual + EU CSRD/IFRS S2/KSSB compliance report) ② cj-style discipline 회피 위험 방지 (49~128번째 누적 80-entry-point cycle + 128번째 Phase 16 close-out retro + 127번째 atomic wire + 126번째 spec entry + 125번째 PRD entry 패턴 모두 wire DONE 진입 정합 보존 후 즉시 Phase 17 진입 = 1-day atomic sprint discipline) ③ 비즈니스 우선순위 = EU CSRD (Corporate Sustainability Reporting Directive, 2024~2027 단계 시행) + SEC Climate Disclosure Rules (2026 강화) + EU Taxonomy Regulation + IFRS S2 Climate-related Disclosures + 한국 KSSB (Korean Sustainability Standards Board, 2026 신설) 규제 driver + enterprise 고객 onboarding 시 sustainability-grade carbon reporting + 6-module cross-rollup KPI dashboard + scheduled sustainability report dispatch territory 필수 (Phase 11 showback department breakdown EXTENSION + Phase 12 anomaly detection baseline EXTENSION + Phase 13 forecast 12-month horizon EXTENSION + Phase 14 optimization accuracy tracking EXTENSION + Phase 15 tag compliance scoring EXTENSION + Phase 16 executive rollup cross-join EXTENSION) ④ Phase 16 wire `81ae00a` 의 5-module cross-join (Phase 11 showback + Phase 12 anomaly + Phase 13 forecast + Phase 14 optimization + Phase 15 tag_governance) + Phase 14 wire `e904485` 의 idle resource detection (carbon attribution: idle_compute → scope2 carbon) + commitment recommender + Phase 13 wire `8b98030` 의 capacity headroom (carbon projection: forecast utilization → projected carbon) + budget burn-rate + Phase 15 wire `1b800d9` 의 tag compliance (resource efficiency → renewable energy attribution) 의 자연스러운 carry-over chain = Phase 17 sustainability rollup 의 aggregate_function (6 modules cross-join with carbon attribution key from Phase 14 idle_resource + Phase 15 tag_governance + Phase 16 executive rollup) + KPI calculation (Phase 11 cost → carbon intensity + Phase 13 forecast → carbon projection + Phase 14 idle cost → idle carbon + Phase 15 tag compliance → resource efficiency + Phase 16 executive rollup → executive carbon view) + scheduled dispatch (KST cron weekly/monthly/quarterly/annual) + EU CSRD/IFRS S2 compliance report EXTENSION 정합 ⑤ 1st release close-out retro + Epic 17 close-out retro + Phase 5~16 close-out retro territory verbatim 보존 + AD-22 owner-only RBAC + Epic 12 2FA 챌린지 보존 + AD-14 stack pin + NFR4 PII minimization ✅ PRESERVED + AD-44 FinOps Sustainability & Carbon Reporting 신규 결정 ((a)~(g) 7 sub-decisions 모두 결정 wire 진입).

### F33.1 carbon_emissions_aggregator + 6 FinOps modules cross-join EXTENSION (A474 결정)

- **F33.1.1** `apps/api/modules/finops/carbon_emissions_aggregator.py` NEW ~+200 LOC 결정 wire + `aggregate_carbon_emissions(tenant_id, period_key, scope) -> CarbonEmissionsRollup` TypedDict 17 fields (carbon_rollup_id UUID PK + tenant_id UUID + period_key TEXT + scope enum tenant/department/cost_center/product_line + computed_at TIMESTAMPTZ + total_carbon_emissions_kgco2e NUMERIC(20,4) + scope1_emissions_kgco2e NUMERIC(18,4) + scope2_emissions_kgco2e NUMERIC(18,4) + scope3_emissions_kgco2e NUMERIC(18,4) + carbon_intensity_kgco2e_per_krw NUMERIC(18,8) + data_center_pue NUMERIC(5,3) + renewable_energy_pct NUMERIC(5,2) + carbon_offset_kgco2e NUMERIC(18,4) + scope_breakdown JSONB + cloud_provider_breakdown JSONB + trace_id TEXT + carbon_metadata JSONB 6-module source attribution) 결정 wire + 6-module cross-join EXTENSION (Phase 11 showback + Phase 12 anomaly + Phase 13 forecast + Phase 14 optimization + Phase 15 tag_governance + Phase 16 executive_reporting) 결정 wire + 4 scope 옵션 (tenant + department + cost_center + product_line) 결정 wire.
- **F33.1.2** `aggregate_function` 6 functions 결정 wire = (1) `showback_carbon_aggregate` (Phase 11 wire `e020ad0` 의 showback_query EXTENSION + cost → carbon conversion using cloud_provider_carbon_intensity_table) + (2) `anomaly_carbon_aggregate` (Phase 12 wire `f3c0e63` 의 anomaly_detection baseline EXTENSION + carbon_anomaly threshold) + (3) `forecast_carbon_aggregate` (Phase 13 wire `8b98030` 의 capacity_headroom EXTENSION + carbon_projection) + (4) `optimization_carbon_aggregate` (Phase 14 wire `e904485` 의 idle_resource_detector EXTENSION + idle_compute → scope2 carbon attribution) + (5) `tag_compliance_carbon_aggregate` (Phase 15 wire `1b800d9` 의 allocation_audit EXTENSION + renewable energy attribution per tagged resource) + (6) `executive_reporting_carbon_aggregate` (Phase 16 wire `81ae00a` 의 executive_dashboard_aggregator EXTENSION + executive carbon rollup) 결정 wire.
- **F33.1.3** `total_carbon_emissions_kgco2e` 계산 결정 wire = `scope1_emissions + scope2_emissions + scope3_emissions - carbon_offset_kgco2e` 결정 wire + GHG Protocol Corporate Standard (Revised Edition, 2024 ISO 14064-1) scope classification EXTENSION 결정 wire + 6-module cross-join RLS 자동 적용 CR 0-2 verbatim + Redis cache 24h TTL EXTENSION 결정 wire.
- **F33.1.4** `scope1_emissions_kgco2e` 계산 결정 wire = direct emissions from owned/controlled sources (data center fuel combustion + on-premise cooling + on-prem servers) 결정 wire + `tenant_settings.on_premise_carbon_intensity_kgco2e_per_kwh` NUMERIC(10,4) per-tenant override EXTENSION 결정 wire + on-prem 면제 시 scope1 = 0 (no on-prem) 결정 wire.
- **F33.1.5** `scope2_emissions_kgco2e` 계산 결정 wire = indirect emissions from purchased electricity (cloud provider data center electricity consumption × grid carbon intensity) 결정 wire + cloud_provider_carbon_intensity_table 4 providers (AWS ap-northeast-2 0.4436 kgCO2e/kWh + GCP asia-northeast3 0.4521 kgCO2e/kWh + Azure koreacentral 0.4436 kgCO2e/kWh + on-prem default 0.4715 kgCO2e/kWh) 결정 wire + monthly cloud electricity consumption kWh from cloud billing API EXTENSION 결정 wire.
- **F33.1.6** `scope3_emissions_kgco2e` 계산 결정 wire = value chain emissions (cloud provider upstream emissions: data center construction + hardware manufacturing + supply chain transportation) 결정 wire + scope3_cat1_purchased_goods + scope3_cat11_use_of_sold_products + scope3_cat15_investments 카테고리 결정 wire + SaaS (Slack + SendGrid + AWS S3) 카테고리 11 적용 EXTENSION 결정 wire.
- **F33.1.7** `carbon_intensity_kgco2e_per_krw` 계산 결정 wire = `total_carbon_emissions_kgco2e / total_monthly_cost_krw` 결정 wire + Phase 11 cost 데이터 활용 EXTENSION 결정 wire + 4 industries baseline (manufacturing ≤ 0.0008 kgCO2e/KRW + service ≤ 0.0004 kgCO2e/KRW + manufacturing_service ≤ 0.0006 kgCO2e/KRW + manufacturing_service_other ≤ 0.0007 kgCO2e/KRW) EXTENSION 결정 wire.
- **F33.1.8** `data_center_pue` 계산 결정 wire = Power Usage Effectiveness = `total_facility_power / it_equipment_power` 결정 wire + `tenant_settings.data_center_pue_override` NUMERIC(5,3) per-tenant override EXTENSION 결정 wire + AWS Seoul region average PUE 1.45 EXTENSION 결정 wire + Korea MSIT 2024 data center PUE 평균 1.5 EXTENSION 결정 wire.
- **F33.1.9** `renewable_energy_pct` 계산 결정 wire = percentage of electricity from renewable sources (solar + wind + hydro + nuclear) 결정 wire + cloud_provider renewable energy disclosure (AWS Seoul 100% renewable 2025) + GCP asia-northeast3 90% renewable + Azure koreacentral 70% renewable EXTENSION 결정 wire + tenant override EXTENSION 결정 wire.
- **F33.1.10** `carbon_offset_kgco2e` 계산 결정 wire = voluntary carbon offset purchased (verified carbon units VCU + certified emission reductions CER) 결정 wire + `tenant_settings.carbon_offset_amount_kgco2e` NUMERIC(18,4) per-tenant override EXTENSION 결정 wire + offset registry provider (Verra VCS + Gold Standard + KCU) EXTENSION 결정 wire.
- **F33.1.11** `scope_breakdown` JSONB 결정 wire = `{"scope1_pct": 5.2, "scope2_pct": 62.5, "scope3_pct": 32.3}` 결정 wire + GHG Protocol EXTENSION 결정 wire.
- **F33.1.12** `cloud_provider_breakdown` JSONB 결정 wire = `{"aws_emissions_pct": 65.0, "gcp_emissions_pct": 25.0, "azure_emissions_pct": 10.0}` 결정 wire + per-tenant override EXTENSION 결정 wire.
- **F33.1.13** `carbon_metadata` JSONB 6-module source attribution 결정 wire = `{"showback_carbon_source": "phase_11_finops_showback", "anomaly_carbon_source": "phase_12_finops_anomaly", "forecast_carbon_source": "phase_13_finops_forecast", "optimization_carbon_source": "phase_14_finops_optimization", "tag_governance_carbon_source": "phase_15_finops_tag_governance", "executive_reporting_carbon_source": "phase_16_finops_executive_reporting"}` 결정 wire + per-tenant override EXTENSION 결정 wire.
- **F33.1.14** audit-first INSERT `sustainability_dashboard_viewed` 결정 wire (carbon emissions dashboard 진입 시 tenant_id + user_id + actor_role + view_timestamp 1행 INSERT, CR 1-1 verbatim) + cache key = `(tenant_id, period_key, carbon_rollup_hash)` 결정 wire + NFR11 P95 ≤ 30s SLO 결정 wire + EU CSRD report generation 5-year retention 결정 wire.

### F33.2 sustainability_kpi_selector (8 NEW KPI, A475 결정)

- **F33.2.1** 8 NEW sustainability KPI TypedDict 결정 wire = (1) `total_carbon_emissions_kgco2e` (F33.1.3 의 calculation result) + (2) `scope1_emissions_kgco2e` (F33.1.4 의 calculation result) + (3) `scope2_emissions_kgco2e` (F33.1.5 의 calculation result) + (4) `scope3_emissions_kgco2e` (F33.1.6 의 calculation result) + (5) `carbon_intensity_kgco2e_per_krw` (F33.1.7 의 calculation result) + (6) `data_center_pue` (F33.1.8 의 calculation result) + (7) `renewable_energy_pct` (F33.1.9 의 calculation result) + (8) `carbon_offset_kgco2e` (F33.1.10 의 calculation result) 결정 wire.
- **F33.2.2** `apps/api/modules/finops/sustainability_kpi_selector.py` NEW ~+180 LOC 결정 wire + `calculate_sustainability_kpi(tenant_id, period_key, kpi_set: List[str]) -> SustainabilityKPIResult` TypedDict 11 fields (sustainability_kpi_id UUID PK + tenant_id UUID + period_key TEXT + kpi_set JSONB + kpi_values JSONB 8 sustainability KPI results + calculation_metadata JSONB (last carbon_rollup_hash + cache hit/miss + computation_ms) + computed_at TIMESTAMPTZ + trace_id TEXT) 결정 wire + 8 KPI selector 결정 wire.
- **F33.2.3** `kpi_set` JSONB 결정 wire = `["total_carbon_emissions_kgco2e", "scope1_emissions_kgco2e", "scope2_emissions_kgco2e", "scope3_emissions_kgco2e", "carbon_intensity_kgco2e_per_krw", "renewable_energy_pct"]` default 6 KPI (mandatory) + 2 optional = `[data_center_pue, carbon_offset_kgco2e]` 결정 wire + tenant override EXTENSION 결정 wire.
- **F33.2.4** `kpi_values` JSONB format 결정 wire = `{"total_carbon_emissions_kgco2e": 1234.5678, "scope1_emissions_kgco2e": 64.2, "scope2_emissions_kgco2e": 771.6, "scope3_emissions_kgco2e": 398.8, "carbon_intensity_kgco2e_per_krw": 0.0004567, "renewable_energy_pct": 87.5, ...}` 결정 wire + per-KPI 형식 정의 결정 wire (carbon_emissions = NUMERIC(18,4) / carbon_intensity = NUMERIC(18,8) / percentage = NUMERIC(5,2) with % suffix) + audit-first INSERT 결정 wire.
- **F33.2.5** `calculation_metadata.cache_hit` boolean 결정 wire + true 면 computation_ms ≤ 100ms (cache sub-100ms per Phase 12 cache hit pattern verbatim) + false 면 cold compute NFR11 SLO ≤ 30s 결정 wire.
- **F33.2.6** KPI alert threshold 결정 wire = (1) `carbon_intensity_kgco2e_per_krw > 0.0008` → red badge "🚨 탄소집약도 임계 초과 (제조업 baseline)" 결정 wire + (2) `renewable_energy_pct < 50.0` → yellow badge "⚠️ 재생에너지 비율 미달" 결정 wire + (3) `data_center_pue > 1.7` → yellow badge "⚠️ 데이터센터 PUE 비효율" 결정 wire + (4) 4 industries baseline INT4 EXTENSION 결정 wire (manufacturing carbon_intensity ≤ 0.0008 / service carbon_intensity ≤ 0.0004 / manufacturing_service ≤ 0.0006 / manufacturing_service_other ≤ 0.0007) + per-tenant override EXTENSION 결정 wire.
- **F33.2.7** `scope2_emissions_kgco2e` calculation source attribution 결정 wire = `tenant_settings.cloud_provider_monthly_electricity_kwh` JSONB 로 결정 + cloud_provider별 분리 (AWS + GCP + Azure) 결정 wire + Phase 11 wire `e020ad0` 의 tenant_settings 의 cloud_provider JSONB (per-tenant EXTENSION) 결정 wire + cloud provider 변경 시 AD-25 cache invalidation publish 결정 wire.
- **F33.2.8** 4 industries sustainability KPI baseline 결정 wire (Phase 11~16 baseline EXTENSION) = (1) manufacturing : carbon_intensity ≤ 0.0008 kgCO2e/KRW + renewable_energy_pct ≥ 70.0 + (2) service : carbon_intensity ≤ 0.0004 kgCO2e/KRW + renewable_energy_pct ≥ 80.0 + (3) manufacturing_service : both (1)(2) mixed + (4) manufacturing_service_other : per-tenant baseline + override EXTENSION 결정 wire.
- **F33.2.9** `kpi_set` 의 strict validation 결정 wire = kpi_names NOT IN (8 KPI) → strict reject + CR 11-4 D-PARITY-01 verbatim 적용 결정 wire + Phase 12 wire `f3c0e63` 의 source_kind strict reject EXTENSION 정합.
- **F33.2.10** cache key 결정 wire = `(tenant_id, period_key, kpi_set_hash, kpi_values_hash)` 결정 wire + cache TTL 1h (sustainability dashboard 의 sub-100ms hit 보장) 결정 wire + cache eviction LRU (Phase 13 cache pattern verbatim) 결정 wire.
- **F33.2.11** audit-first INSERT `sustainability_kpi_refreshed` 결정 wire (KPI refresh 시 tenant_id + user_id + kpi_set + period_key 1행 INSERT) + NFR11 P95 ≤ 30s SLO 결정 wire.
- **F33.2.12** cross-module carbon rollup 6 module source aggregation sequence 결정 wire (sequential 6 steps) = (1) showback carbon aggregate + 60ms + (2) anomaly carbon aggregate + 35ms + (3) forecast carbon aggregate + 90ms + (4) optimization carbon aggregate + 50ms + (5) tag_governance carbon aggregate + 60ms + (6) executive_reporting carbon aggregate + 70ms = total ≤ 365ms NFR11 cold compute 결정 wire.

### F33.3 sustainability_report_generation_engine (PDF + CSV + Excel, A476 결정)

- **F33.3.1** `apps/api/modules/finops/sustainability_report_generator.py` NEW ~+220 LOC 결정 wire + `generate_sustainability_report(tenant_id, period_key, report_format, schedule_id) -> SustainabilityReport` TypedDict 14 fields (sustainability_report_id UUID PK + tenant_id UUID + period_key TEXT + report_format enum pdf/csv/excel + report_period enum monthly/quarterly/annual + computed_carbon_rollup_id UUID FK + report_file_path TEXT + report_file_size_bytes INTEGER + generation_duration_ms INTEGER + kpi_summary JSONB + csrd_compliance_summary JSONB + dispatch_ready boolean + trace_id TEXT + generated_at TIMESTAMPTZ + generated_by_user_id UUID) 결정 wire + 3 format 옵션 PDF/CSV/Excel 3종 결정 wire.
- **F33.3.2** 3 NEW format generator 결정 wire = (1) **PDF generator** (`sustainability_report_pdf_generator.py` NEW ~+150 LOC) - Phase 9 wire 의 ReportGenerator EXTENSION + A4 portrait + ko-KR 폰트 Pretendard + 8 sustainability KPI 차트 + 6-module source data appendix 1-line ref + EU CSRD/IFRS S2/KSSB compliance template + owner-only watermark + greenhouse_gas_protocol_compliance_badge 결정 wire + (2) **CSV generator** (`sustainability_report_csv_generator.py` NEW ~+80 LOC) - 8 KPI + 6-module source attribution row + tenant_id + period_key + scope1/2/3 breakdown + cloud_provider breakdown 결정 wire + (3) **Excel generator** (`sustainability_report_excel_generator.py` NEW ~+120 LOC) - 8 KPI sheet + 6-module raw data sheet 6장 + chart sheet 결정 wire + openpyxl 3.1.2 AD-14 stack pin EXTENSION 결정 wire.
- **F33.3.3** `report_period` 3 옵션 결정 wire = (1) **monthly** (current month) + (2) **quarterly** (last 3 months) + (3) **annual** (last 12 months) 결정 wire + Phase 11 wire `e020ad0` 의 showback period selector EXTENSION 정합 + EU CSRD fiscal year (January-December) EXTENSION 정합.
- **F33.3.4** `kpi_summary` JSONB 결정 wire = `{"total_carbon_emissions_kgco2e": ..., "scope1_emissions_kgco2e": ..., "scope2_emissions_kgco2e": ..., "scope3_emissions_kgco2e": ..., "8 sustainability KPI values": ..., "6_module_source_attribution": {...}, "scope_breakdown": {...}, "cloud_provider_breakdown": {...}}` 결정 wire + Phase 17 F33.1.13 carbon_metadata EXTENSION.
- **F33.3.5** `csrd_compliance_summary` JSONB 결정 wire = EU CSRD (Corporate Sustainability Reporting Directive) ESRS E1 Climate Change disclosure summary 결정 wire = `{"esrs_e1_paragraph_12_gross_scope1": ..., "esrs_e1_paragraph_13_gross_scope2": ..., "esrs_e1_paragraph_14_gross_scope3": ..., "esrs_e1_paragraph_16_energy_consumption_mwh": ..., "esrs_e1_paragraph_18_renewable_energy_pct": ..., "esrs_e1_paragraph_65_financed_emissions": ..., "reporting_period": ..., "compliance_status": "COMPLIANT"}` 결정 wire + IFRS S2 (Industry-based disclosure) EXTENSION 결정 wire + KSSB (Korean Sustainability Standards Board, 2026 신설) climate disclosure EXTENSION 결정 wire.
- **F33.3.6** `dispatch_ready` boolean 결정 wire = true 면 scheduled dispatch 가능 + false 면 generation only (on-demand) 결정 wire + AD-22 owner-only RBAC bind (sustainability report on-demand = owner-only + Epic 12 2FA 챌린지 보존).
- **F33.3.7** PDF 디자인 결정 wire = page 1 = cover (tenant_id + period_key + 8 KPI big-number 8 rows) + page 2 = monthly trend chart 12-month (Phase 13 forecast baseline EXTENSION) + page 3 = 6-module breakdown table 6 rows + page 4 = 8 KPI bar chart 8 + page 5 = renewable_energy_pct trend + page 6 = EU CSRD ESRS E1 disclosure summary + page 7 = footer (generated_at + generated_by + ko-KR 1-line 한글 disclaimer "이 보고서는 ESG 공시용이며 CSRD/IFRS S2/KSSB compliance 검증 후에만 외부 공시에 사용 가능합니다") 결정 wire + ARIA labels WCAG 2.1 AA 적용 결정 wire.
- **F33.3.8** 4 industries baseline period 결정 wire = (1) manufacturing : monthly default + quarterly on-request + annual December only + (2) service : monthly + quarterly + annual December + (3) manufacturing_service : both (1)(2) + (4) manufacturing_service_other : per-tenant override EXTENSION.
- **F33.3.9** report dispatch fallback 결정 wire = generation timeout 시 retry 1회 (Phase 13 retry pattern EXTENSION) + 2회 실패 시 fallback monthly summary 5-line (no report file) + audit-first INSERT `sustainability_report_failed` 결정 wire.
- **F33.3.10** `report_file_path` 결정 wire = `apps/api/storage/sustainability_reports/{tenant_id}/{period_key}/{sustainability_report_id}.{ext}` 결정 wire + RLS 자동 적용 CR 0-2 verbatim + storage bucket S3-호환 결정 wire + Phase 5 wire `f093f8c` S3 storage EXTENSION 정합.
- **F33.3.11** `dispatch_ready` 결정 wire = generation 성공 시 true + Tenant RBAC owner-only 호출 결정 wire (owner-only on-demand generation = Epic 12 2FA 챌린지 보존 + audit-first INSERT 결정 wire).
- **F33.3.12** audit-first INSERT `sustainability_report_generated` 2행 결정 wire (sustainability report file generate 시 1행 + 6-module source attribution 1행 = 2행 append, CR 1-1 verbatim) + NFR8 audit retention_period 5-year append-only 결정 wire + EU CSRD 5-year retention EXTENSION 결정 wire.
- **F33.3.13** 3 format 동시 generation 결정 wire = true 면 PDF + CSV + Excel 3 format 일괄 생성 + 각각 report_file_path 별도 + sustainability_report_id 동일 결정 wire + generation_duration_ms SUM ≤ 30s NFR11 SLO 결정 wire.

### F33.4 scheduled_sustainability_dispatch KST cron (A477 결정)

- **F33.4.1** `apps/api/modules/finops/scheduled_sustainability_dispatch.py` NEW ~+180 LOC 결정 wire + `ScheduledSustainabilityDispatch` TypedDict 12 fields (scheduled_sustainability_dispatch_id UUID PK + tenant_id UUID + scheduled_period enum monthly/quarterly/annual + scheduled_cron TEXT + recipient_emails JSONB (List[str]) + dispatch_format enum pdf/csv/excel/all_3 + dispatch_status enum active/paused/expired + last_dispatch_at TIMESTAMPTZ + last_dispatch_status enum success/failed/not_run + next_dispatch_at TIMESTAMPTZ + audit_first_insert boolean default true + trace_id TEXT) 결정 wire.
- **F33.4.2** KST cron 4 NEW cadence 결정 wire = (1) **weekly** (매주 월요일 09:00 KST) + (2) **monthly** (매월 1일 09:00 KST) + (3) **quarterly** (분기별 1일 09:00 KST) + (4) **annual** (매년 1월 1일 09:00 KST) 결정 wire + Phase 16 wire `81ae00a` 의 4 cadence EXTENSION 정합 + EU CSRD annual reporting (March 31 deadline) 정합.
- **F33.4.3** `apps/api/jobs/scheduled_sustainability_dispatch_job.py` NEW ~+150 LOC 결정 wire + APScheduler (3.10.4 AD-14 stack pin EXTENSION) cron job 결정 wire + next_dispatch_at 계산 (Phase 12 wire `f3c0e63` 의 scheduled_alert_job pattern verbatim EXTENSION) 결정 wire + 4 cadence 모두 wire 결정 wire + L4 service-layer 결정 wire.
- **F33.4.4** recipient_emails 결정 wire = JSONB array of validated email addresses 결정 wire + max 50 recipients (per tenant) + STRICT email format validation (RFC 5322 EXTENSION) + no internal email (audit_first_insert 결정 wire + 1-row sender actor record 결정 wire).
- **F33.4.5** `dispatch_status` 결정 wire = 3 옵션 (active + paused + expired) + paused → scheduled_sustainability_dispatch_id 의 pause 결정 wire (owner-only 1-click) + expired → next_dispatch_at < NOW() - 90d 결정 wire (auto-expire 결정 wire + audit-first INSERT `scheduled_sustainability_dispatch_expired` 결정 wire).
- **F33.4.6** `last_dispatch_status` 3 옵션 결정 wire = (1) success (sustainability_report_generated 정상 발송) + (2) failed (sustainability_report_failed 발송 실패) + (3) not_run (next_dispatch_at > NOW() 미실행) 결정 wire + audit-first INSERT 결정 wire.
- **F33.4.7** dispatch email content 결정 wire = (1) tenant_name (Phase 11 tenant_settings.name) + (2) report_period (period_key) + (3) download_url (signed S3 URL 24h expiration 결정 wire + Phase 5 wire S3 signed URL EXTENSION) + (4) ko-KR 1-line summary "월간 탄소배출 보고서가 준비되었습니다." 결정 wire + (5) ARIA labels WCAG 2.1 AA 적용 결정 wire + 2-step unsubscribe 결정 wire (CR GDPR opt-out EXTENSION 결정 wire).
- **F33.4.8** `dispatch_format` 결정 wire = 4 옵션 (pdf OR csv OR excel OR all_3 동시) 결정 wire + all_3 동시 시 3 file attachment 결정 wire (max 10MB per file 결정 wire + larger 시 S3 link only 결정 wire).
- **F33.4.9** retry 정책 결정 wire = dispatch 실패 시 exponential backoff 결정 wire (Phase 12 wire retry pattern EXTENSION) = retry 3회 (1s + 5s + 30s) + 3회 실패 시 fail + audit-first INSERT 결정 wire.
- **F33.4.10** `audit_first_insert` boolean 결정 wire = true 면 dispatch 시 audit-first INSERT 1행 (CR 1-1 verbatim 결정 wire) + dispatch_status 변화마다 INSERT 결정 wire (created + paused + resumed + expired) + NFR8 5-year append-only 결정 wire.
- **F33.4.11** audit-first INSERT 2 NEW 결정 wire = (1) `sustainability_report_dispatched` (dispatch 성공 시 1행) + (2) `sustainability_scheduled_dispatch_evaluated` (cron 평가 시 1행, success/failed/not_run) 결정 wire + AD-22 owner-only RBAC bind 결정 wire.
- **F33.4.12** Phase 11~16 cross-rollup 보존 결정 wire = F33 의 모든 scheduled dispatch 는 Phase 11~16 의 6 module source data 를 aggregate 한 carbon rollup 결과를 발송 + per-tenant scope EXTENSION + 4 industries baseline EXTENSION + dry-run 모드 (`--finops-sustainability-dispatch-dry-run` 1 NEW CLI flag) 결정 wire.

### F33.5 tenant-scoped sustainability role RBAC (A478 결정)

- **F33.5.1** `Capability.FINOPS_SUSTAINABILITY` 1 NEW enum 결정 wire = `apps/api/core/capability.py` MODIFIED + `Capability.FINOPS_SUSTAINABILITY = "finops_sustainability"` 1 NEW 결정 wire + 4 industries grants 결정 wire (manufacturing + service + manufacturing_service + manufacturing_service_other 모두 ✅/✅/✅/✅ CR 12-1 L4 industry-agnostic precedent verbatim) + Phase 11~16 FinOps pattern verbatim EXTENSION 결정 wire.
- **F33.5.2** sustainability role 결정 wire = tenant_settings.sustainability_role_enabled boolean default false + Epic 12 2FA 챌린지 보존 + AD-22 owner-only RBAC EXTENSION 결정 wire + Epic 16 wire 의 tenant_idp_admin EXTENSION 정합.
- **F33.5.3** `apps/api/dependencies/capability.py` MODIFIED + `require_finops_sustainability` 1 NEW dep 결정 wire + 4 industries grants EXTENSION 결정 wire + pattern Phase 11~16 verbatim 미러 결정 wire.
- **F33.5.4** sustainability role hierarchy 결정 wire = (1) tenant_owner (Epic 12 2FA + owner-only) + (2) tenant_admin (read-only sustainability dashboard) + (3) tenant_analyst (read-only cross-module sustainability KPI) + (4) tenant_viewer (only summary stats) 결정 wire + per-tenant EXTENSION decision wire + 5 roles hierarchy 보존 결정 wire.
- **F33.5.5** Epic 12 2FA 챌린지 보존 결정 wire = sustainability report generation + scheduled dispatch 모두 Epic 12 2FA 챌린지 통과 후 호출 가능 + AD-22 owner-only RBAC EXTENSION 결정 wire + 2FA 미통과 시 strict reject 결정 wire (CR 12-5 D-14 envelope EXACT 적용 결정 wire).
- **F33.5.6** cross-tenant contamination 방어 결정 wire (CR 12-5 D-PARITY-01 verbatim) = tenant_id scope 자동 적용 + sustainability dashboard 진입 시 `require_finops_sustainability(tenant_id)` 호출 결정 wire + 다른 tenant_id 호출 시 strict reject + 1-row counter increment 결정 wire.
- **F33.5.7** cross-industry consistency 검증 결정 wire = 4 industries 모두 F33 capability grant + 동일 RBAC pattern 적용 + industry_agnostic = true (CR 12-1 L4 EXTENSION) 결정 wire + drift detector `tests/integration/test_capability_matrix_v1_43_drift.py` 8 NEW pytest cases EXTENSION 결정 wire.
- **F33.5.8** audit-first INSERT 결정 wire = sustainability dashboard 진입 + KPI refresh + report generate + dispatch 모두 audit-first INSERT (CR 1-1 verbatim) + NFR8 5-year retention 결정 wire + EU CSRD 5-year retention EXTENSION 결정 wire.
- **F33.5.9** Frontend RBAC 결정 wire = ko-KR.json `finops_sustainability.role.*` namespace EXTENSION 결정 wire + `finops_sustainability.role.owner.label = "대표 (2FA 인증)"` + `finops_sustainability.role.owner.description = "2FA 인증된 대표만 Sustainability Report 생성 가능"` 결정 wire + role badge ARIA labels WCAG 2.1 AA 적용 결정 wire.
- **F33.5.10** capability drift detector EXTENSION 결정 wire = phase 17 wire 진입 시점에 `tests/integration/test_capability_matrix_v1_43_drift.py` NEW + 8 NEW pytest cases (4 industry × 2 grant-type 결정 wire) 결정 wire + Phase 16 wire pattern verbatim 미러 (FINOPS_REPORTING 8 cases EXTENSION).
- **F33.5.11** `FinopsSustainabilityAction` Literal 결정 wire = `apps/api/core/audit_action.py` MODIFIED + ActionClass.FINOPS_SUSTAINABILITY 1 NEW enum 결정 wire + 8 NEW Literal values (`sustainability_report_generated` + `sustainability_dashboard_viewed` + `sustainability_kpi_refreshed` + `sustainability_report_exported` + `sustainability_report_dispatched` + `sustainability_scheduled_dispatch_evaluated` + `finops_sustainability_dry_run_executed` + `cross_module_sustainability_kpi_calculated`) 결정 wire + Phase 11~16 audit action EXTENSION 정합.
- **F33.5.12** `apps/api/core/errors.py` MODIFIED + 10 NEW typed exception classes 결정 wire (CR 12-5 D-14 envelope) = `CarbonRollupCalculationError(500)` + `SustainabilityKPIError(500)` + `SustainabilityReportGenerationError(500)` + `SustainabilityReportExportError(500)` + `ScheduledSustainabilityDispatchInvalidCronError(400)` + `ScheduledSustainabilityDispatchRecipientInvalidError(400)` + `ScheduledSustainabilityDispatchExpiredError(400)` + `SustainabilityDispatchEmailInvalidError(400)` + `FinopsSustainabilityUnsupportedFormatError(400)` + `FinopsSustainabilityCapabilityDeniedError(403)` 결정 wire + Phase 11~16 typed exception pattern verbatim EXTENSION.

### F33.6 sustainability dashboard UI (5 sub-components, A478 결정)

- **F33.6.1** `apps/web/app/[locale]/(dashboard)/admin/finops/sustainability/page.tsx` NEW ~+180 LOC 결정 wire + 5 sub-components 결정 wire (SustainabilityDashboardAggregator + SustainabilityKPISelector + SustainabilityReportGeneratorPanel + ScheduledSustainabilityDispatchConfigPanel + SustainabilityTrendMiniChart) 결정 wire + owner-only RBAC AD-22 verbatim + Epic 12 2FA 챌린지 보존 결정 wire.
- **F33.6.2** `apps/web/components/finops/FinopsSustainabilityDashboardPanel.tsx` Client 5 sub-component 결정 wire + Recharts 2.12.7 AD-14 stack pin EXTENSION 결정 wire + Phase 11~16 finops dashboard pattern verbatim EXTENSION 결정 wire.
- **F33.6.3** SustainabilityDashboardAggregator component 결정 wire = scope selector (tenant + department + cost_center + product_line 4 NEW 옵션) + period selector (current/previous/last 3/6/12 months) + 8 sustainability KPI big-number 8 rows + 6-module source attribution chip 6 NEW chip + EU CSRD compliance badge + GHG Protocol scope1/2/3 breakdown chart 결정 wire + Phase 16 wire `81ae00a` 의 executive dashboard EXTENSION 정합.
- **F33.6.4** SustainabilityKPISelector component 결정 wire = 6 mandatory sustainability KPI 선택 + 2 optional sustainability KPI 선택 = total 8 sustainability KPI 결정 wire + 선택된 KPI 의 kpi_set JSONB 생성 결정 wire + CR 11-4 D-005 unknown state reject EXACT 적용 결정 wire.
- **F33.6.5** SustainabilityReportGeneratorPanel component 결정 wire = period selector (monthly + quarterly + annual 3) + format selector (PDF + CSV + Excel 3 NEW checkbox) + on-demand generation trigger button + 2FA 챌린지 모달 결정 wire (Epic 12 EXTENSION 결정 wire) + progress bar 결정 wire + generated report download link 결정 wire (signed URL 24h expiration EXTENSION 결정 wire) + EU CSRD ESRS E1 + IFRS S2 + KSSB compliance summary section 결정 wire.
- **F33.6.6** ScheduledSustainabilityDispatchConfigPanel component 결정 wire = cadence selector (weekly + monthly + quarterly + annual 4) + format selector + recipients input (multi-email + max 50) + scheduled_sustainability_dispatch_id 생성 후 일시정지/재개 버튼 + last_dispatch_status 표시 + next_dispatch_at 표시 결정 wire + 4 cadence wire 결정 wire + EU CSRD annual reporting deadline (March 31) highlight 결정 wire.
- **F33.6.7** SustainabilityTrendMiniChart component 결정 wire = last 12-month renewable_energy_pct trend (Phase 16 wire `81ae00a` 의 ComplianceTrendMiniChart EXTENSION 정합) + 50% threshold reference line 결정 wire + Recharts LineChart (Phase 12~16 pattern verbatim) + ARIA labels WCAG 2.1 AA 결정 wire + carbon_intensity_kgco2e_per_krw trend 동시 표시 결정 wire.
- **F33.6.8** toast notification 결정 wire = `sustainability_report_generated` 시 toast (success → green + 5초) + `sustainability_report_failed` 시 toast (failed → red + 10초) + `sustainability_report_dispatched` 시 toast (dispatched → blue + 5초) + `finops_sustainability_dry_run_executed` 시 toast 결정 wire + Phase 11~16 toast notification EXTENSION 정합.
- **F33.6.9** Vitest RTL render discipline CR 11-4 D-003 verbatim 결정 wire = 7 NEW vitest cases (SustainabilityDashboardAggregator 1 + SustainabilityKPISelector 1 + SustainabilityReportGeneratorPanel 1 + ScheduledSustainabilityDispatchConfigPanel 1 + SustainabilityTrendMiniChart 1 + ko-KR SSOT 2) 결정 wire + Phase 11~16 vitest pattern verbatim EXTENSION.
- **F33.6.10** ko-KR.json `finops_sustainability.*` namespace EXTENSION ~30 keys 결정 wire + `finops_sustainability.title = "지속가능성 대시보드"` + `finops_sustainability.kpi.total_carbon = "이번 달 총 탄소배출량"` + `finops_sustainability.kpi.scope1 = "Scope 1 직접배출"` + `finops_sustainability.kpi.scope2 = "Scope 2 간접배출 (전력)"` + `finops_sustainability.kpi.scope3 = "Scope 3 가치사슬배출"` + `finops_sustainability.kpi.carbon_intensity = "탄소집약도"` + `finops_sustainability.kpi.renewable_energy = "재생에너지 비율"` + `finops_sustainability.kpi.data_center_pue = "데이터센터 PUE"` + `finops_sustainability.kpi.carbon_offset = "탄소상쇄량"` + ... 결정 wire + ARIA labels WCAG 2.1 AA 결정 wire + CR 11-4 D-002 verbatim SSOT 결정 wire.

### F33.7 Capability matrix v1.43 EXTENSION FINOPS_SUSTAINABILITY (12 sub-ACs, A478 결정)

- **F33.7.1** capability matrix v1.42 → v1.43 EXTENSION FINOPS_SUSTAINABILITY 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅ 결정 wire + Phase 11~16 FINOPS_* pattern verbatim EXTENSION + CR 12-1 L4 precedent 미러.
- **F33.7.2** `apps/api/core/capability.py` MODIFIED + `Capability.FINOPS_SUSTAINABILITY = "finops_sustainability"` 1 NEW enum 결정 wire + 4 industries EXTENSION 결정 wire + Phase 11~16 verbatim EXTENSION 결정 wire.
- **F33.7.3** `apps/api/dependencies/capability.py` MODIFIED + `require_finops_sustainability` 1 NEW dep 결정 wire + 4 industries grants EXTENSION 결정 wire + Phase 11~16 verbatim EXTENSION 결정 wire.
- **F33.7.4** ActionClass.FINOPS_SUSTAINABILITY 1 NEW enum 결정 wire + 8 NEW Literal values (F33.5.11 결정) + audit-first INSERT 8 NEW 결정 wire + CR 1-1 verbatim 적용 결정 wire.
- **F33.7.5** 4-industry grant parity 정합 (manufacturing + service + manufacturing_service + manufacturing_service_other 모두 ✅) 결정 wire + industry_agnostic = true 결정 wire.
- **F33.7.6** drift detector `tests/integration/test_capability_matrix_v1_43_drift.py` NEW 8 NEW pytest cases 결정 wire + 4 industries × 2 grant-type (Capability + Dependency) EXTENSION.
- **F33.7.7** `apps/api/alembic/versions/0049_phase_17_finops_sustainability.py` NEW ~+180 LOC + 6 NEW tables 결정 wire (carbon_emissions_rollup + sustainability_kpi + sustainability_report + scheduled_sustainability_dispatch + sustainability_viewer + sustainability_offset_registry) + 2 NEW preview tables (`phase_17_finops_carbon_emissions_rollup_preview` + `phase_17_finops_sustainability_kpi_preview`) 결정 wire + RLS 자동 적용 CR 0-2 verbatim + CHECK + UNIQUE + indexes 결정 wire + audit-first INSERT 결정 wire.
- **F33.7.8** `apps/api/jobs/scheduled_sustainability_dispatch_job.py` NEW APScheduler 4 cadence cron 결정 wire (F33.4 결정) + 4 cadence 모두 wire 결정 wire (weekly + monthly + quarterly + annual) 결정 wire.
- **F33.7.9** ALLOWED_SERVICE_SUBMODULES (CR 11-3 lesson) 즉시 sweep EXTENSION 결정 wire = `m17_finops_sustainability` 신규 submodule 등록 + Phase 11~16 verbatim EXTENSION (Phase 16 m16_finops_reporting 패턴 보존).
- **F33.7.10** NFR11 (P95 ≤ 30s) 결정 wire = sustainability dashboard view NFR11 cold compute ≤ 30s + KPI refresh cache hit sub-100ms EXTENSION + report generation PDF+CSV+Excel 동시 ≤ 30s EXTENSION + scheduled dispatch NFR15 0분 손실 보장 EXTENSION.
- **F33.7.11** NFR8 (감사로그 5년 append-only) 결정 wire = 8 NEW audit log entries (`sustainability_report_generated` + `sustainability_dashboard_viewed` + `sustainability_kpi_refreshed` + `sustainability_report_exported` + `sustainability_report_dispatched` + `sustainability_scheduled_dispatch_evaluated` + `finops_sustainability_dry_run_executed` + `cross_module_sustainability_kpi_calculated`) + 5-year retention EXTENSION + EU CSRD 5-year retention EXTENSION.
- **F33.7.12** AD-44 신규 결정 진입 (a)~(g) 7 sub-decisions 결정 wire + 부록 A architectural decisions table 1 NEW row EXTENSION + Phase 12~16 AD-39~AD-43 verbatim EXTENSION 결정 wire.

### F33.8 dry-run + Tests + wire scope T1~T8 (10 sub-ACs, A478 결정)

- **F33.8.1** dry-run mode 결정 wire = `--finops-carbon-rollup-dry-run` + `--finops-sustainability-kpi-dry-run` + `--finops-sustainability-report-dry-run` + `--finops-scheduled-sustainability-dispatch-dry-run` + `--finops-sustainability-export-dry-run` 5 NEW CLI flag + dry-run 시 actual `sustainability_dashboard_viewed` + `sustainability_kpi_refreshed` + `sustainability_report_generated` + `sustainability_report_dispatched` + `sustainability_scheduled_dispatch_evaluated` audit-first INSERT skip 결정 wire + Phase 11~16 dry-run pattern verbatim EXTENSION.
- **F33.8.2** dry-run preview 결과 결정 wire = `phase_17_finops_carbon_emissions_rollup_preview` + `phase_17_finops_sustainability_kpi_preview` 2 table 결정 wire + preview_id UUID PK + tenant_id UUID + preview_type enum + period_key TEXT + preview_data JSONB + computed_at TIMESTAMPTZ DEFAULT NOW() + trace_id TEXT + RLS 자동 적용 CR 0-2 verbatim + audit-first INSERT `finops_sustainability_dry_run_executed` 결정 wire.
- **F33.8.3** dry-run CLI flag 5 NEW 결정 wire + Phase 11~16 dry-run pattern verbatim EXTENSION + 5 CLI flag 모두 wire 결정 wire + dry-run 결과 preview 결정 wire.
- **F33.8.4** tests 결정 wire = ~+56 NEW pytest PASS (carbon_emissions_aggregator 8 + sustainability_kpi_selector 8 + sustainability_report_generator 7 + scheduled_sustainability_dispatch 6 + alembic 0049 5 + audit action 8 + capability matrix v1.43 8 + dry-run 6 = ~+56 NEW pytest PASS) 결정 wire + Phase 11~16 test pattern verbatim EXTENSION.
- **F33.8.5** vitest tests ~+7 NEW vitest PASS 결정 wire (SustainabilityDashboardAggregator 1 + SustainabilityKPISelector 1 + SustainabilityReportGeneratorPanel 1 + ScheduledSustainabilityDispatchConfigPanel 1 + SustainabilityTrendMiniChart 1 + ko-KR SSOT 2 + finops sustainability dashboard parity CR 12-5 D-PARITY-01 결정 wire) + Phase 11~16 vitest pattern verbatim EXTENSION.
- **F33.8.6** ruff + tsc 0 NEW + SDR drift gate 결정 wire + A36 SDR 검증 4-step 자동 적용 결정 wire + 0 NEW ruff + 0 NEW tsc + 0 regressions + SDR drift gate PASS 결정 wire.
- **F33.8.7** wire scope T1~T8 결정 wire = T1 carbon_emissions_aggregator + sustainability_kpi_selector 결정 wire + T2 sustainability_report_generator + 3 format generators (PDF + CSV + Excel) 결정 wire + T3 scheduled_sustainability_dispatch + APScheduler 4 cadence cron 결정 wire + T4 alembic 0049 phase_17_finops_sustainability 6 tables + RLS 6 tables + 2 preview tables 결정 wire + T5 audit action EXTENSION 8 NEW + 10 NEW typed exception classes 결정 wire + T6 capability v1.43 EXTENSION + frontend finops sustainability dashboard 결정 wire + T7 scheduled_sustainability_dispatch_job wire 결정 wire + T8 3중 게이트 FINAL CLEAN atomic commit 결정 wire + estimated ~+33-38 files estimate = ~+21 NEW + ~+12 MODIFIED 결정 wire + Phase 16 wire `81ae00a` 의 ~28 files pattern verbatim EXTENSION 결정 wire + Phase 11~16 wire 의 ~25-30 files pattern verbatim EXTENSION.
- **F33.8.8** A19 cohesion pattern 9 surface EXTENSION PASS 결정 wire (FinOps Sustainability & Carbon Reporting surface NEW = F33.1~F33.8 결정 wire) + Phase 16 wire `81ae00a` 의 FinOps Reporting & Executive Dashboard surface NEW = F32.1~F32.8 pattern verbatim 미러 + Phase 15 wire `1b800d9` 의 FinOps Tag Governance surface NEW = F31.1~F31.8 pattern verbatim 미러 + Phase 14 wire `e904485` 의 FinOps Optimization surface NEW = F30.1~F30.8 pattern verbatim 미러 + Phase 13 wire `8b98030` 의 FinOps Forecasting surface NEW = F29.1~F29.8 pattern verbatim 미러 + Phase 12 wire `f3c0e63` 의 FinOps Anomaly surface NEW = F28.1~F28.8 pattern verbatim 미러 + Phase 11 wire `e020ad0` 의 FinOps Showback surface NEW = F27.1~F27.7 pattern verbatim 미러 결정 wire.
- **F33.8.9** CR lessons applied 18종 결정 wire (CR 0-2 RLS lesson ✅ APPLIED + CR 1-1 audit-first INSERT ✅ APPLIED (8 NEW audit log entries + 6-module source attribution 2행 append = 10 NEW effective audit events) + CR 4-3/4-4 lessons carry ✅ APPLIED + CR 1-1 ContextVar lesson ✅ APPLIED + CR 1-1 RSC boundary lesson ✅ APPLIED + CR 9-6 commit message discipline ✅ APPLIED + CR 11-3 honest-DEFER discipline ✅ APPLIED (129번째 epic 연속 정직 회복) + CR 11-4 D-001~D-005 + P-015 ✅ APPLIED + CR 12-1 L4 ✅ APPLIED + CR 12-5 D-14 typed exception envelope ✅ APPLIED (10 NEW typed exception classes) + CR 12-5 D-PARITY-01 inversion ✅ APPLIED + CR 12-5 D-GATE-01 inversion ✅ APPLIED + A36 SDR 검증 4-step 자동 적용 ✅ + AD-14 stack pin ✅ APPLIED + AD-22 owner-only RBAC ✅ APPLIED + NFR4 PII minimization ✅ PRESERVED) 결정 wire.
- **F33.8.10** D-DEFER-* honestly 결정 wire (CR 11-3 129번째 epic 연속 정직 회복 결정 wire 보존) = D-1-1-DEFER-1/2/3 RESOLVED 보존 60~129번째 + D-LAUNCH-1-DEFER-1 honestly preserved 65~129번째 + D-EPIC-16-REVIEW-DEFER-2~6 ✅ RESOLVED 보존 78~129번째 + D-PHASE-4-DR-DEFER-1/2 ✅ RESOLVED 보존 73~129번째 + D-EPIC-17-WIRE-DEFER-T2-T3-UI ✅ RESOLVED 보존 83~129번째 + D-RETENTION-1 ✅ RESOLVED 보존 85~129번째 + D-OBSERVABILITY-1 ✅ RESOLVED 보존 89~129번째 + D-PERFORMANCE-1 ✅ RESOLVED 보존 93~129번째 + D-CHAOS-1 ✅ RESOLVED 보존 100~129번째 + D-SLO-1 ✅ RESOLVED 보존 101~129번째 + D-FINOPS-1 ✅ RESOLVED 보존 105~129번째 + D-FINOPS-2 ✅ RESOLVED 보존 109~129번째 + D-FINOPS-3 ✅ RESOLVED 보존 113~129번째 + D-FINOPS-4 ✅ RESOLVED 보존 117~129번째 + D-FINOPS-5 ✅ RESOLVED 보존 121~129번째 + D-FINOPS-6 ✅ honestly DEFER 보존 125~129번째 결정 wire + Epic 1 ~ Epic 17 + Phase 3 ~ Phase 16 + 1st release cycle 정합 보존 결정 wire (cj-style 129번째 epic 연속 정직 회복 pre-flight 정합 sweep) + 결정 wire 일자: 2026-08-25 (KST) + **next**: 옵션 (a) Phase 17 bmad-create-story spec entry 진입 (cj-style 130번째 wire 진입 시점) / 옵션 (b) Phase 17 bmad-dev-story atomic wire T1~T8 진입 (cj-style 131번째) / 옵션 (c) Phase 17 close-out retro 진입 (cj-style 132번째) / 옵션 (d) Epic 18+ 진입 / 옵션 (e) D-DEFER-* follow-up 결정 wire 보류 결정 wire.

## F34. FinOps Cloud Commitment Management (Phase 18 territory 신규)

> **§F34 결정 wire 진입** — Phase 17 close-out retro `de009fe` (cj-style 132번째 wire entry) 직후 다음 옵션 5종 중 **사용자 권장 결정 = 옵션 (a) Phase 18+ 진입 + 옵션 (a) FinOps Cloud Commitment Management (RIs/SPs/CUDs) (Recommended) (commitment_inventory_aggregator 5 cloud provider (AWS EC2 RI + RDS RI + EC2 SP + S3 SP + Redshift SP + DynamoDB SP + Azure reservations + GCP CUDs + Naver Cloud + KT Cloud) cross-rollup + commitment_kpi_selector 8 NEW KPI calculations (total_commitment_value_krw + coverage_pct + utilization_pct + expiring_commitments_30d + recommended_purchase_krw + savings_realized_krw + idle_commitment_krw + renewal_decision_score) + commitment_report_generation_engine PDF + CSV + Excel + scheduled_commitment_dispatch KST cron 4 cadence + tenant_scoped_commitment_role RBAC owner-only + commitment_dashboard UI 5 sub-components + ko-KR.json `finops_commitment.*` namespace EXTENSION ~30 keys + audit-first INSERT 8 NEW via ActionClass.FINOPS_COMMITMENT + Capability FINOPS_COMMITMENT 1 NEW + dry-run mode 5 NEW CLI flags + tests + wire scope T1~T8) = Phase 18 territory 결정 wire 진입**. rationale 5종: ① Phase 17 wire `97cfe4e` FinOps Sustainability & Carbon Reporting territory (carbon_emissions_aggregator 6-module cross-rollup + sustainability_kpi_selector 8 NEW KPI selector + sustainability report generation engine PDF + CSV + Excel + scheduled sustainability dispatch KST cron + tenant-scoped sustainability role RBAC owner-only) 의 natural backend CLOUD COMMITMENT MANAGEMENT LAYER EXTENSION 결정 wire 진입 (7 FinOps modules 의 data aggregate → commitment-grade cloud cost optimization view + cross-module commitment KPI 8개 + scheduled commitment report export PDF+CSV+Excel weekly/monthly/quarterly/annual + commitment_inventory cross-rollup + coverage_pct + utilization_pct + expiring_commitments_30d + purchase_recommendation + ROI scoring + risk scoring + renewal_workflow + AWS / Azure / GCP / Naver Cloud / KT Cloud 멀티 cloud commitment 지원) ② cj-style discipline 회피 위험 방지 (49~132번째 누적 84-entry-point cycle + 132번째 Phase 17 close-out retro + 131번째 atomic wire + 130번째 spec entry + 129번째 PRD entry 패턴 모두 wire DONE 진입 정합 보존 후 즉시 Phase 18 진입 = 1-day atomic sprint discipline) ③ 비즈니스 우선순위 = enterprise 고객 onboarding 시 commitment 기반 cloud cost optimization (RIs/SPs/CUDs purchase recommendation + coverage_pct + utilization_pct + expiring_commitments_30d + ROI scoring + risk scoring + renewal workflow) territory 필수 (Phase 14 optimization의 RI/SP commitment_recommender 1y/3y break-even → Phase 18 commitment_inventory cross-join + coverage_pct EXTENSION + Phase 17 sustainability의 renewable_energy_pct + carbon_offset → carbon-aware commitment 권고 EXTENSION + Phase 13 forecast의 12-month horizon → expiring_commitments_30d + utilization_forecast EXTENSION + Phase 11 showback의 cost center breakdown → cost center commitment coverage EXTENSION + Phase 12 anomaly의 z-score 기반 → commitment anomaly detection EXTENSION + Phase 15 tag_governance의 allocation rule → commitment allocation rule EXTENSION) ④ Phase 17 wire `97cfe4e` 의 6-module cross-rollup (Phase 11 showback + Phase 12 anomaly + Phase 13 forecast + Phase 14 optimization + Phase 15 tag_governance + Phase 16 executive + Phase 17 sustainability) + Phase 14 wire `e904485` 의 commitment_recommender 1y/3y break-even + Phase 13 wire `8b98030` 의 12-month forecast + Phase 11 wire `e020ad0` 의 cost center cost allocation + Phase 12 wire `f3c0e63` 의 anomaly detection baseline 의 자연스러운 carry-over chain = Phase 18 commitment 의 commitment_inventory (EC2 RI + RDS RI + EC2 SP + S3 SP + Redshift SP + DynamoDB SP + Azure reservations + GCP CUDs + Naver Cloud + KT Cloud) + coverage_pct (commitment covered cost / total cost) + utilization_pct (commitment used hours / commitment total hours) + expiring_commitments_30d (next 30-day expiration) + purchase_recommendation (Phase 14 break-even EXTENSION) + ROI scoring (savings / cost) + risk scoring (low + medium + high) + renewal_workflow (auto + manual + hold) EXTENSION 정합 ⑤ 1st release close-out retro + Epic 17 close-out retro + Phase 5~17 close-out retro territory verbatim 보존 + AD-22 owner-only RBAC + Epic 12 2FA 챌린지 보존 + AD-14 stack pin + NFR4 PII minimization ✅ PRESERVED + AD-45 FinOps Cloud Commitment Management 신규 결정 ((a)~(g) 7 sub-decisions 모두 결정 wire 진입).

### F34.1 commitment_inventory_aggregator + 5 cloud provider cross-rollup EXTENSION (A494 결정)

- **F34.1.1** `apps/api/modules/finops/commitment_inventory_aggregator.py` NEW ~+200 LOC 결정 wire + `aggregate_commitment_inventory(tenant_id, period_key, cloud_provider) -> CommitmentInventory` TypedDict 18 fields (commitment_id UUID PK + tenant_id UUID + period_key TEXT + cloud_provider enum aws/azure/gcp/naver/kt + commitment_type enum ec2_ri/rds_ri/ec2_sp/s3_sp/redshift_sp/dynamodb_sp/azure_reservation/gcp_cud/naver_commitment/kt_commitment + commitment_term enum 1_year/3_year + commitment_status enum active/expired/expiring_soon/pending_renewal + start_date DATE + end_date DATE + committed_amount_krw NUMERIC(20,4) + utilized_amount_krw NUMERIC(20,4) + utilization_pct NUMERIC(5,2) + coverage_pct NUMERIC(5,2) + savings_realized_krw NUMERIC(20,4) + instance_type TEXT + region TEXT + trace_id TEXT + commitment_metadata JSONB 5-cloud-provider source attribution) 결정 wire + 5 cloud provider cross-rollup EXTENSION (AWS + Azure + GCP + Naver Cloud + KT Cloud) 결정 wire.
- **F34.1.2** `aggregate_function` 5 functions 결정 wire = (1) `aws_commitment_aggregate` (EC2 RI + RDS RI + EC2 SP + S3 SP + Redshift SP + DynamoDB SP via AWS Cost Explorer API) + (2) `azure_commitment_aggregate` (Azure reservations via Azure Consumption API) + (3) `gcp_commitment_aggregate` (GCP CUDs via GCP Cloud Billing API) + (4) `naver_commitment_aggregate` (Naver Cloud commitment discounts via Naver Cloud billing API) + (5) `kt_commitment_aggregate` (KT Cloud commitment discounts via KT Cloud billing API) 결정 wire.
- **F34.1.3** `commitment_inventory_id` UUID PK 결정 wire + tenant-scoped RLS 자동 적용 CR 0-2 verbatim + CHECK + UNIQUE + indexes 결정 wire + Phase 17 carbon_emissions_rollup_id pattern verbatim EXTENSION.
- **F34.1.4** `coverage_pct` 계산 결정 wire = `commitment_covered_cost / total_tenant_cost` 결정 wire + Phase 11 showback cost data 활용 EXTENSION 결정 wire + 4 industries baseline (manufacturing coverage ≥ 70% + service coverage ≥ 80% + manufacturing_service coverage ≥ 75% + manufacturing_service_other coverage ≥ 65%) EXTENSION 결정 wire.
- **F34.1.5** `utilization_pct` 계산 결정 wire = `commitment_used_hours / commitment_total_hours` 결정 wire + utilization_under_threshold (utilization < 70%) → idle_commitment_krw EXTENSION 결정 wire + Phase 14 optimization accuracy tracking EXTENSION 결정 wire.
- **F34.1.6** `expiring_commitments_30d` 계산 결정 wire = `commitments with end_date - current_date <= 30 days` 결정 wire + KST timezone('Asia/Seoul') EXTENSION + 30-day rolling window + Phase 13 forecast 12-month horizon EXTENSION 결정 wire.
- **F34.1.7** `total_commitment_value_krw` 계산 결정 wire = `SUM(committed_amount_krw)` across all active commitments 결정 wire + AWS / Azure / GCP / Naver / KT currency conversion (KRW) EXTENSION 결정 wire + KRW 환율 일일 update 결정 wire.
- **F34.1.8** 5 cloud provider coverage matrix EXTENSION = AWS coverage ≥ 70% baseline + Azure coverage ≥ 60% baseline + GCP coverage ≥ 50% baseline + Naver Cloud coverage ≥ 40% baseline + KT Cloud coverage ≥ 30% baseline 결정 wire + per-tenant override EXTENSION 결정 wire + tenant_owner_id 의 delegated commitment viewer 권한 OPTIONAL EXTENSION 결정 wire.
- **F34.1.9** `commitment_metadata` JSONB 결정 wire = `{provider: aws/azure/gcp/naver/kt, term: 1_year/3_year, payment_option: all_upfront/partial_upfront/no_upfront, offering_class: standard/convertible, scope: region/Zonal, instance_family: ec2/rds/...}` 결정 wire + AWS RI marketplace metadata EXTENSION 결정 wire.
- **F34.1.10** commitment inventory cross-rollup 결정 wire = 7 FinOps modules (Phase 11 showback cost + Phase 12 anomaly baseline + Phase 13 forecast 12-month + Phase 14 optimization commitment + Phase 15 tag_governance allocation + Phase 16 executive rollup + Phase 17 sustainability carbon) EXTENSION + commitment inventory RLS 자동 적용 CR 0-2 verbatim + Redis cache 24h TTL EXTENSION 결정 wire.
- **F34.1.11** `trace_id` UUID 결정 wire + audit-first INSERT `commitment_inventory_aggregated` CR 1-1 verbatim 결정 wire + Phase 11~17 inventory aggregator pattern verbatim EXTENSION.

### F34.2 commitment_kpi_selector + 8 NEW KPI calculations (A495 결정)

- **F34.2.1** `apps/api/modules/finops/commitment_kpi_selector.py` NEW ~+220 LOC 결정 wire + `select_commitment_kpi(tenant_id, period_key, kpi_type) -> CommitmentKPI` TypedDict 16 fields (kpi_id UUID PK + tenant_id UUID + period_key TEXT + kpi_type enum total_commitment_value_krw/coverage_pct/utilization_pct/expiring_commitments_30d/recommended_purchase_krw/savings_realized_krw/idle_commitment_krw/renewal_decision_score + kpi_value NUMERIC(20,4) + kpi_unit TEXT + target_value NUMERIC(20,4) + variance_pct NUMERIC(5,2) + computed_at TIMESTAMPTZ + trace_id TEXT + kpi_metadata JSONB + industry_baseline NUMERIC(20,4)) 결정 wire + 8 NEW KPI calculations EXTENSION 결정 wire.
- **F34.2.2** `total_commitment_value_krw` 계산 결정 wire = `SUM(committed_amount_krw)` of active commitments per period_key 결정 wire + KRW currency unit EXTENSION + industry baseline per-tenant override EXTENSION 결정 wire.
- **F34.2.3** `coverage_pct` 계산 결정 wire = `commitment_covered_cost / total_tenant_cost * 100` 결정 wire + 4 industries baseline (manufacturing ≥ 70% + service ≥ 80% + manufacturing_service ≥ 75% + manufacturing_service_other ≥ 65%) EXTENSION + coverage_alert (coverage < baseline → warning) 결정 wire.
- **F34.2.4** `utilization_pct` 계산 결정 wire = `SUM(utilized_amount_krw) / SUM(committed_amount_krw) * 100` 결정 wire + Phase 14 optimization accuracy tracking EXTENSION + utilization_alert (utilization < 70% → idle_commitment_flag) 결정 wire.
- **F34.2.5** `expiring_commitments_30d` 계산 결정 wire = `COUNT(commitments with end_date - current_date <= 30 days)` 결정 wire + renewal_workflow trigger EXTENSION + Phase 13 forecast 12-month horizon EXTENSION + KST timezone('Asia/Seoul') EXTENSION 결정 wire.
- **F34.2.6** `recommended_purchase_krw` 계산 결정 wire = Phase 14 commitment_recommender 1y/3y break-even EXTENSION + coverage_gap * predicted_on_demand_cost * (1 - expected_discount_pct) 결정 wire + Phase 13 forecast 12-month horizon EXTENSION + savings_estimation EXTENSION 결정 wire.
- **F34.2.7** `savings_realized_krw` 계산 결정 wire = `SUM(on_demand_cost - commitment_cost)` for all utilized commitments 결정 wire + Phase 14 optimization realized_savings EXTENSION + savings_per_commitment_type aggregation EXTENSION 결정 wire.
- **F34.2.8** `idle_commitment_krw` 계산 결정 wire = `SUM(committed_amount_krw * (1 - utilization_pct / 100))` for utilization < 70% commitments 결정 wire + Phase 14 idle resource detection EXTENSION + remediation recommendation EXTENSION 결정 wire.
- **F34.2.9** `renewal_decision_score` 계산 결정 wire = weighted score (0~100) based on (utilization_pct * 0.4 + coverage_pct * 0.3 + savings_realized_krw_normalized * 0.2 + expiring_urgency * 0.1) 결정 wire + decision: auto_renew (score >= 80) / manual_review (40 <= score < 80) / do_not_renew (score < 40) 결정 wire + Phase 14 optimization accuracy tracking EXTENSION.
- **F34.2.10** 8 KPI 7-module cross-join EXTENSION (Phase 11 cost + Phase 12 anomaly + Phase 13 forecast + Phase 14 optimization + Phase 15 tag_governance + Phase 16 executive + Phase 17 sustainability) 결정 wire + per-tenant override EXTENSION + industry_baseline comparison EXTENSION 결정 wire.
- **F34.2.11** `kpi_metadata` JSONB 결정 wire = `{calculation_method, source_modules, data_freshness, baseline_period, target_threshold}` EXTENSION + 7-module index hints 결정 wire + Redis cache 24h TTL EXTENSION + audit-first INSERT `commitment_kpi_calculated` CR 1-1 verbatim 결정 wire.

### F34.3 commitment_report_generation_engine + PDF + CSV + Excel + 3 cadence (A496 결정)

- **F34.3.1** `apps/api/modules/finops/commitment_report_generator.py` NEW ~+250 LOC 결정 wire + `generate_commitment_report(tenant_id, period_key, cadence, export_format) -> CommitmentReport` TypedDict 14 fields (report_id UUID PK + tenant_id UUID + period_key TEXT + cadence enum weekly/monthly/quarterly/annual + export_format enum pdf/csv/excel + coverage_pct NUMERIC(5,2) + utilization_pct NUMERIC(5,2) + expiring_commitments_30d INTEGER + recommended_purchase_krw NUMERIC(20,4) + savings_realized_krw NUMERIC(20,4) + idle_commitment_krw NUMERIC(20,4) + generated_at TIMESTAMPTZ + trace_id TEXT + report_metadata JSONB) 결정 wire.
- **F34.3.2** PDF generation 결정 wire = reportlab==4.0.7 (AD-14 stack pin) + Korean font registration (Pretendard) + 8-section template (executive summary + commitment inventory + coverage analysis + utilization analysis + expiration schedule + purchase recommendation + savings realized + renewal decisions) + AWS / Azure / GCP / Naver / KT 다국어 support EXTENSION 결정 wire.
- **F34.3.3** CSV generation 결정 wire = pandas==2.1.4 (AD-14 stack pin) + openpyxl==3.1.2 (AD-14 stack pin) + UTF-8-BOM encoding + 14 columns (commitment_id + cloud_provider + commitment_type + term + start_date + end_date + committed_amount_krw + utilized_amount_krw + utilization_pct + coverage_pct + savings_realized_krw + status + trace_id + period_key) 결정 wire.
- **F34.3.4** Excel generation 결정 wire = xlsxwriter==3.1.9 (AD-14 stack pin) + 3 sheets (Commitment Inventory + KPI Summary + Renewal Decisions) + 차트 (coverage trend + utilization trend + savings realized) + conditional formatting (utilization < 70% → red highlight) EXTENSION 결정 wire.
- **F34.3.5** 3 cadence EXTENSION = weekly (current week commitments) + monthly (current month commitments) + quarterly (Q1/Q2/Q3/Q4) + annual (current year YTD) 결정 wire + Phase 17 sustainability_report_generation_engine pattern verbatim EXTENSION 결정 wire.
- **F34.3.6** 5-framework support EXTENSION = FinOps Foundation Framework (commitment optimization pillar) + AWS Cost Optimization Pillar (RI/SP best practices) + Azure Cost Optimization (reserved instances) + GCP Cost Optimization (CUDs) + 한국 조달청 클라우드 commitment 가이드라인 EXTENSION 결정 wire.
- **F34.3.7** `report_metadata` JSONB 결정 wire = `{generation_method, framework_version, kpi_count, commitment_count, total_savings_krw, next_action: auto_renew/manual_review/do_not_renew}` EXTENSION 결정 wire.
- **F34.3.8** `expiring_commitments_30d` 강조 EXTENSION = report PDF 첫 페이지에 expiring commitments summary 박스 EXTENSION 결정 wire + renewal_decision_score 기반 색상 coding (red < 40 / yellow 40~80 / green >= 80) EXTENSION 결정 wire.
- **F34.3.9** `recommended_purchase_krw` 권고 EXTENSION = Phase 14 commitment_recommender 1y/3y break-even EXTENSION + coverage gap analysis + 1-year vs 3-year trade-off (3-year savings 30%+ → 3-year 추천, 그 외 1-year 추천) EXTENSION 결정 wire.
- **F34.3.10** `audit-first INSERT 8 NEW` 결정 wire = `commitment_report_generated` + `commitment_report_exported` + `commitment_kpi_refreshed` + `commitment_dashboard_viewed` + `commitment_report_dispatched` + `commitment_scheduled_dispatch_evaluated` + `finops_commitment_dry_run_executed` + `cross_module_commitment_kpi_calculated` via ActionClass.FINOPS_COMMITMENT EXTENSION + 5-year retention NFR8 EXTENSION 결정 wire.
- **F34.3.11** `trace_id` UUID 결정 wire + 7-module cross-join EXTENSION + 4-industry baseline comparison EXTENSION + audit-first INSERT `commitment_report_generated` CR 1-1 verbatim 결정 wire.

### F34.4 scheduled_commitment_dispatch + 4 cron schedules KST (A496 결정)

- **F34.4.1** `apps/api/modules/finops/scheduled_commitment_dispatch.py` NEW ~+200 LOC 결정 wire + `schedule_commitment_dispatch(tenant_id, cadence, recipients) -> ScheduledCommitmentDispatch` TypedDict 11 fields (dispatch_id UUID PK + tenant_id UUID + cadence enum weekly/monthly/quarterly/annual + cron_expression TEXT + recipients JSONB + commitment_types JSONB + next_run_at TIMESTAMPTZ + last_run_at TIMESTAMPTZ + last_status enum success/failed/skipped + created_at TIMESTAMPTZ + trace_id TEXT) 결정 wire.
- **F34.4.2** 4 cron schedules EXTENSION = (1) weekly Mon 09:00 KST (`0 9 * * 1` pytz timezone('Asia/Seoul')) + (2) monthly 1st-day 09:00 KST (`0 9 1 * *`) + (3) quarterly 1st-day 09:00 KST (`0 9 1 */3 *`) + (4) annual Jan-1 09:00 KST (`0 9 1 1 *`) 결정 wire + APScheduler 3.10.4 (AD-14 stack pin) EXTENSION 결정 wire.
- **F34.4.3** recipient resolver 결정 wire = Slack (Slack webhook URL) + Email (SMTP) + S3 archive (commitment_report_{period_key}.pdf upload) + MS Teams webhook EXTENSION + per-tenant recipient list EXTENSION + role-based recipient (owner-only AD-22 verbatim + Epic 12 2FA 챌린지 보존) EXTENSION 결정 wire.
- **F34.4.4** `commitment_types` JSONB 결정 wire = `{ec2_ri: true, rds_ri: true, ec2_sp: true, s3_sp: true, redshift_sp: true, dynamodb_sp: true, azure_reservation: true, gcp_cud: true, naver_commitment: false, kt_commitment: false}` EXTENSION + per-tenant override EXTENSION 결정 wire.
- **F34.4.5** `cron_expression` 결정 wire = apscheduler cron format EXTENSION + next_run_at 계산 (croniter 기반) + last_run_at 추적 EXTENSION + idempotency_key (commitment_dispatch_{tenant_id}_{cadence}_{period_key}) EXTENSION 결정 wire.
- **F34.4.6** `audit-first INSERT` 결정 wire = `commitment_scheduled_dispatch_evaluated` (cron 매 tick 마다) + `commitment_report_dispatched` (dispatch 성공 시) + 5-year retention NFR8 EXTENSION + Phase 17 scheduled_sustainability_dispatch pattern verbatim EXTENSION 결정 wire.
- **F34.4.7** Slack 메시지 format 결정 wire = 5-section message (commitment summary + coverage alert + expiring commitments + utilization alert + recommended purchase) + Block Kit JSON EXTENSION + Korean language default (ko-KR NFR18 SSOT) EXTENSION 결정 wire.
- **F34.4.8** Email format 결정 wire = multipart/alternative (HTML + plain text) + Korean charset UTF-8 + attachment commitment_report_{period_key}.pdf EXTENSION + unsubscribe link (CR 12-1 compliance) EXTENSION 결정 wire.
- **F34.4.9** S3 archive 결정 wire = commitment_report_{tenant_id}_{period_key}_{cadence}.pdf + lifecycle policy (90-day → Glacier IR) + KMS encryption (NFR6 AES-256-GCM) EXTENSION + audit-first INSERT `commitment_report_archived` EXTENSION 결정 wire.
- **F34.4.10** MS Teams webhook EXTENSION = Adaptive Card 1.4 format + 5-section card (commitment summary + coverage + expiring + utilization + purchase) + per-tenant webhook URL EXTENSION 결정 wire.
- **F34.4.11** `last_status` enum EXTENSION = success / failed / skipped / dry_run 결정 wire + 실패 시 retry 3회 (exponential backoff) + alert routing (owner Slack DM) EXTENSION 결정 wire.

### F34.5 tenant-scoped commitment role RBAC + owner-only (A497 결정)

- **F34.5.1** `apps/api/core/role.py` MODIFIED 결정 wire + `Role.COMMITMENT_VIEWER` 1 NEW enum EXTENSION + commitment dashboard / report / KPI / scheduled dispatch 모두 owner-only RBAC AD-22 verbatim 보존 결정 wire + Phase 17 `Role.SUSTAINABILITY_VIEWER` 패턴 verbatim EXTENSION 결정 wire.
- **F34.5.2** `apps/api/dependencies/capability.py` MODIFIED 결정 wire + `require_commitment_role()` Dependency 1 NEW wire EXTENSION + Phase 17 `require_sustainability_role` 패턴 verbatim EXTENSION + 미허용 tenant 의 commitment dashboard 진입 차단 결정 wire.
- **F34.5.3** tenant-level override EXTENSION = tenant_owner_id 의 delegated commitment viewer 권한 OPTIONAL 결정 wire + Epic 12 2FA 챌린지 보존 결정 wire + Phase 17 sustainability RBAC 패턴 verbatim EXTENSION 결정 wire.
- **F34.5.4** `apps/api/core/capability.py` MODIFIED 결정 wire + `Capability.FINOPS_COMMITMENT = "finops_commitment"` 1 NEW enum EXTENSION + Phase 17 `Capability.FINOPS_SUSTAINABILITY` 패턴 verbatim EXTENSION + 4-industry grants ✅/✅/✅/✅ industry-agnostic per CR 12-1 L4 verbatim EXTENSION 결정 wire.
- **F34.5.5** `apps/api/dependencies/capability.py` MODIFIED 결정 wire + `require_finops_commitment` 1 NEW dep EXTENSION + Phase 17 `require_finops_sustainability` 패턴 verbatim EXTENSION 결정 wire.
- **F34.5.6** commitment dashboard 진입 차단 EXTENSION = role check 실패 시 HTTPException(403) `CommitmentRolePermissionError` 1 NEW typed exception EXTENSION + audit-first INSERT `commitment_role_violation` 결정 wire.
- **F34.5.7** `CommitmentTenantScopeViolationError` 1 NEW typed exception EXTENSION = tenant_id mismatch 시 HTTPException(403) EXTENSION + audit-first INSERT `commitment_tenant_scope_violation` 결정 wire + Phase 17 `SustainabilityTenantScopeViolationError` 패턴 verbatim EXTENSION 결정 wire.
- **F34.5.8** `CommitmentCapabilityGateViolationError` 1 NEW typed exception EXTENSION = Capability.FINOPS_COMMITMENT gate 실패 시 HTTPException(403) EXTENSION + audit-first INSERT `commitment_capability_gate_violation` 결정 wire.
- **F34.5.9** `commitment_viewer` 테이블 NEW 결정 wire + (commitment_viewer_id UUID PK + tenant_id UUID + user_id UUID + role enum owner/delegate/viewer + granted_by UUID + granted_at TIMESTAMPTZ + revoked_at TIMESTAMPTZ NULL + trace_id TEXT) 결정 wire + RLS 자동 적용 CR 0-2 verbatim EXTENSION 결정 wire.
- **F34.5.10** commitment viewer grant workflow EXTENSION = owner가 delegate 위임 가능 + Epic 12 2FA 챌린지 mandatory + revoke 가능 + audit log full trail EXTENSION 결정 wire + Phase 17 `sustainability_viewer` 테이블 패턴 verbatim EXTENSION 결정 wire.
- **F34.5.11** `commitment_role_permission` audit log EXTENSION = 5-year retention NFR8 + role grant/revoke/check 모두 audit-first INSERT + CR 1-1 verbatim EXTENSION + NFR4 PII minimization ✅ PRESERVED 결정 wire.

### F34.6 commitment dashboard UI + 5 sub-components (A497 결정)

- **F34.6.1** `apps/web/app/[locale]/(dashboard)/admin/finops/commitment/page.tsx` NEW ~+180 LOC 결정 wire + 5 sub-components (CommitmentInventoryAggregator + CommitmentKPISelector + CommitmentReportGeneratorPanel + ScheduledCommitmentDispatchConfigPanel + CommitmentTrendMiniChart) + owner-only RBAC AD-22 verbatim 보존 + Epic 12 2FA 챌린지 보존 EXTENSION 결정 wire.
- **F34.6.2** `apps/web/components/finops/FinopsCommitmentDashboardPanel.tsx` NEW Client component ~+200 LOC 결정 wire + 5-tab layout (Inventory / KPI / Reports / Dispatch / Trends) + Recharts 2.12.7 (AD-14 stack pin) 차트 EXTENSION + Phase 17 FinopsSustainabilityDashboardPanel 패턴 verbatim EXTENSION 결정 wire.
- **F34.6.3** `CommitmentInventoryAggregator` sub-component EXTENSION = 5 cloud provider tab (AWS / Azure / GCP / Naver / KT) + commitment list (sortable + filterable) + coverage_pct progress bar + utilization_pct progress bar + expiring_commitments_30d badge EXTENSION 결정 wire.
- **F34.6.4** `CommitmentKPISelector` sub-component EXTENSION = 8 KPI card (total_commitment_value_krw + coverage_pct + utilization_pct + expiring_commitments_30d + recommended_purchase_krw + savings_realized_krw + idle_commitment_krw + renewal_decision_score) + industry baseline 비교 + per-tenant override EXTENSION 결정 wire.
- **F34.6.5** `CommitmentReportGeneratorPanel` sub-component EXTENSION = cadence selector (weekly/monthly/quarterly/annual) + export_format selector (PDF/CSV/Excel) + generate button + download link + audit log entry EXTENSION 결정 wire.
- **F34.6.6** `ScheduledCommitmentDispatchConfigPanel` sub-component EXTENSION = 4 cadence toggle (weekly/monthly/quarterly/annual) + recipient list editor (Slack + Email + S3 + MS Teams) + cron_expression display + next_run_at display + last_run_at display + last_status badge EXTENSION 결정 wire.
- **F34.6.7** `CommitmentTrendMiniChart` sub-component EXTENSION = Recharts LineChart + coverage_pct trend (12-period) + utilization_pct trend (12-period) + savings_realized_krw trend (12-period) + recommended_purchase_krw trend (12-period) + Recharts 2.12.7 AD-14 stack pin EXTENSION 결정 wire.
- **F34.6.8** `apps/web/messages/ko-KR.json` MODIFIED 결정 wire + `finops_commitment.*` namespace EXTENSION ~30 keys (CR 11-4 D-002 verbatim SSOT) EXTENSION 결정 wire + Phase 17 `finops_sustainability.*` 패턴 verbatim EXTENSION 결정 wire.
- **F34.6.9** ARIA labels WCAG 2.1 AA EXTENSION = role="region" + aria-label="클라우드 commitment 관리" + aria-live="polite" + 키보드 navigation 지원 EXTENSION 결정 wire.
- **F34.6.10** `apps/web/lib/finops/commitment-types.ts` NEW 결정 wire + TypeScript mirror of backend TypedDict 16 fields (CR 12-5 D-PARITY-01 inversion) EXTENSION + Phase 17 `sustainability-types.ts` 패턴 verbatim EXTENSION 결정 wire.

### F34.7 Capability matrix v1.44 EXTENSION FINOPS_COMMITMENT (A497 결정)

- **F34.7.1** `docs/capability-matrix.md` MODIFIED 결정 wire + v1.43 → v1.44 EXTENSION + FINOPS_COMMITMENT 1 NEW row after FINOPS_SUSTAINABILITY industry-agnostic 4-industry grants ✅/✅/✅/✅ CR 12-1 L4 precedent verbatim 미러 EXTENSION + FINOPS_SUSTAINABILITY Phase 17 wire + FINOPS_REPORTING Phase 16 wire + FINOPS_TAG_GOVERNANCE Phase 15 wire + FINOPS_OPTIMIZATION Phase 14 wire + FINOPS_FORECASTING_CAPACITY_PLANNING Phase 13 wire + FINOPS_ANOMALY_DETECTION + FINOPS_BUDGET_ALERT Phase 12 wire + FINOPS_SHOWBACK + FINOPS_CHARGEBACK Phase 11 wire + SLO_ENGINEERING Phase 10 wire + CHAOS_ENGINEERING Phase 9 wire + PERFORMANCE_TESTING Phase 8 wire + OBSERVABILITY_TRACES + OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind EXTENSION 결정 wire.
- **F34.7.2** `apps/api/core/capability.py` MODIFIED 결정 wire + `Capability.FINOPS_COMMITMENT = "finops_commitment"` 1 NEW enum EXTENSION 결정 wire.
- **F34.7.3** `apps/api/dependencies/capability.py` MODIFIED 결정 wire + `require_finops_commitment` 1 NEW dep EXTENSION 결정 wire.
- **F34.7.4** `apps/api/core/audit_action.py` MODIFIED 결정 wire + `ActionClass.FINOPS_COMMITMENT = "finops_commitment"` 1 NEW enum EXTENSION 결정 wire + `FinopsCommitmentAction` 8 NEW Literal (`commitment_dashboard_viewed` + `cross_module_commitment_kpi_calculated` + `commitment_report_generated` + `commitment_report_exported` + `commitment_report_dispatched` + `commitment_scheduled_dispatch_evaluated` + `finops_commitment_dry_run_executed` + `commitment_kpi_refreshed`) EXTENSION 결정 wire.
- **F34.7.5** `apps/api/core/errors.py` MODIFIED 결정 wire + 10 NEW typed exception classes CR 12-5 D-14 envelope (CommitmentInventoryAggregationError(500) + CommitmentInventoryScopeError(404) + CommitmentInventoryPeriodError(422) + CommitmentCrossModuleJoinError(500) + CommitmentKPIError(500) + CommitmentReportGenerationError(500) + CommitmentReportExportError(500) + CommitmentReportArchiveError(500) + ScheduledCommitmentDispatchError(500) + CommitmentCronExpressionInvalidError(400) + CommitmentRecipientResolverError(404) + CommitmentDispatchIdempotencyViolationError(422) + CommitmentRolePermissionError(403) + CommitmentTenantScopeViolationError(403) + CommitmentCapabilityGateViolationError(403) + CommitmentAccuracyDegradationError(500)) EXTENSION 결정 wire.
- **F34.7.6** drift detector `tests/integration/test_capability_matrix_v1_44_drift.py` NEW 8 NEW pytest cases 결정 wire + 4 industries × 2 grant-type (Capability + Dependency) EXTENSION.
- **F34.7.7** `apps/api/alembic/versions/0050_phase_18_finops_commitment.py` NEW ~+180 LOC + 6 NEW tables 결정 wire (commitment_inventory + commitment_kpi + commitment_report + scheduled_commitment_dispatch + commitment_viewer + commitment_metadata) + 2 NEW preview tables (`phase_18_finops_commitment_inventory_preview` + `phase_18_finops_commitment_kpi_preview`) 결정 wire + RLS 자동 적용 CR 0-2 verbatim + CHECK + UNIQUE + indexes 결정 wire + audit-first INSERT 결정 wire.
- **F34.7.8** `apps/api/jobs/scheduled_commitment_dispatch_job.py` NEW APScheduler 4 cadence cron 결정 wire (F34.4 결정) + 4 cadence 모두 wire 결정 wire (weekly + monthly + quarterly + annual) 결정 wire.
- **F34.7.9** ALLOWED_SERVICE_SUBMODULES (CR 11-3 lesson) 즉시 sweep EXTENSION 결정 wire = `m18_finops_commitment` 신규 submodule 등록 + Phase 11~17 verbatim EXTENSION (Phase 17 m17_finops_sustainability 패턴 보존).
- **F34.7.10** NFR11 (P95 ≤ 30s) 결정 wire = commitment dashboard view NFR11 cold compute ≤ 30s + KPI refresh cache hit sub-100ms EXTENSION + report generation PDF+CSV+Excel 동시 ≤ 30s EXTENSION + scheduled dispatch NFR15 0분 손실 보장 EXTENSION.
- **F34.7.11** NFR8 (감사로그 5년 append-only) 결정 wire = 8 NEW audit log entries (`commitment_report_generated` + `commitment_dashboard_viewed` + `commitment_kpi_refreshed` + `commitment_report_exported` + `commitment_report_dispatched` + `commitment_scheduled_dispatch_evaluated` + `finops_commitment_dry_run_executed` + `cross_module_commitment_kpi_calculated`) + 5-year retention EXTENSION + NFR8 보존 EXTENSION.
- **F34.7.12** AD-45 신규 결정 진입 (a)~(g) 7 sub-decisions 결정 wire + 부록 A architectural decisions table 1 NEW row EXTENSION + Phase 12~17 AD-39~AD-44 verbatim EXTENSION 결정 wire.

### F34.8 dry-run + Tests + wire scope T1~T8 (A498 결정)

- **F34.8.1** dry-run mode 결정 wire = `--finops-commitment-inventory-dry-run` + `--finops-commitment-kpi-dry-run` + `--finops-commitment-report-dry-run` + `--finops-scheduled-commitment-dispatch-dry-run` + `--finops-commitment-export-dry-run` 5 NEW CLI flag + dry-run 시 actual `commitment_dashboard_viewed` + `commitment_kpi_refreshed` + `commitment_report_generated` + `commitment_report_dispatched` + `commitment_scheduled_dispatch_evaluated` audit-first INSERT skip 결정 wire + Phase 11~17 dry-run pattern verbatim EXTENSION.
- **F34.8.2** dry-run preview 결과 결정 wire = `phase_18_finops_commitment_inventory_preview` + `phase_18_finops_commitment_kpi_preview` 2 table 결정 wire + preview_id UUID PK + tenant_id UUID + preview_type enum + period_key TEXT + preview_data JSONB + computed_at TIMESTAMPTZ DEFAULT NOW() + trace_id TEXT + RLS 자동 적용 CR 0-2 verbatim + audit-first INSERT `finops_commitment_dry_run_executed` 결정 wire.
- **F34.8.3** dry-run CLI flag 5 NEW 결정 wire + Phase 11~17 dry-run pattern verbatim EXTENSION + 5 CLI flag 모두 wire 결정 wire + dry-run 결과 preview 결정 wire.
- **F34.8.4** tests 결정 wire = ~+62 NEW pytest PASS (commitment_inventory_aggregator 8 + commitment_kpi_selector 8 + commitment_report_generator 8 + scheduled_commitment_dispatch 6 + alembic 0050 6 + audit action 8 + capability matrix v1.44 8 + dry-run 6 + 5 cloud provider integration 6 = ~+62 NEW pytest PASS) 결정 wire + Phase 11~17 test pattern verbatim EXTENSION.
- **F34.8.5** vitest tests ~+7 NEW vitest PASS 결정 wire (CommitmentInventoryAggregator 1 + CommitmentKPISelector 1 + CommitmentReportGeneratorPanel 1 + ScheduledCommitmentDispatchConfigPanel 1 + CommitmentTrendMiniChart 1 + ko-KR SSOT 2 + finops commitment dashboard parity CR 12-5 D-PARITY-01 결정 wire) + Phase 11~17 vitest pattern verbatim EXTENSION.
- **F34.8.6** ruff + tsc 0 NEW + SDR drift gate 결정 wire + A36 SDR 검증 4-step 자동 적용 결정 wire + 0 NEW ruff + 0 NEW tsc + 0 regressions + SDR drift gate PASS 결정 wire.
- **F34.8.7** wire scope T1~T8 결정 wire = T1 commitment_inventory_aggregator + commitment_kpi_selector 결정 wire + T2 commitment_report_generator + 3 format generators (PDF + CSV + Excel) 결정 wire + T3 scheduled_commitment_dispatch + APScheduler 4 cadence cron 결정 wire + T4 alembic 0050 phase_18_finops_commitment 6 tables + RLS 6 tables + 2 preview tables 결정 wire + T5 audit action EXTENSION 8 NEW + 16 NEW typed exception classes 결정 wire + T6 capability v1.44 EXTENSION + frontend finops commitment dashboard 결정 wire + T7 scheduled_commitment_dispatch_job wire 결정 wire + T8 3중 게이트 FINAL CLEAN atomic commit 결정 wire + estimated ~+28-33 files estimate = ~+21 NEW + ~+12 MODIFIED 결정 wire + Phase 17 wire `97cfe4e` 의 ~28 files pattern verbatim EXTENSION 결정 wire + Phase 11~17 wire 의 ~25-30 files pattern verbatim EXTENSION.
- **F34.8.8** A19 cohesion pattern 9 surface EXTENSION PASS 결정 wire (FinOps Cloud Commitment Management surface NEW = F34.1~F34.8 결정 wire) + Phase 17 wire `97cfe4e` 의 FinOps Sustainability & Carbon Reporting surface NEW = F33.1~F33.8 pattern verbatim 미러 + Phase 16 wire `81ae00a` 의 FinOps Reporting & Executive Dashboard surface NEW = F32.1~F32.8 pattern verbatim 미러 + Phase 15 wire `1b800d9` 의 FinOps Tag Governance surface NEW = F31.1~F31.8 pattern verbatim 미러 + Phase 14 wire `e904485` 의 FinOps Optimization surface NEW = F30.1~F30.8 pattern verbatim 미러 + Phase 13 wire `8b98030` 의 FinOps Forecasting surface NEW = F29.1~F29.8 pattern verbatim 미러 + Phase 12 wire `f3c0e63` 의 FinOps Anomaly surface NEW = F28.1~F28.8 pattern verbatim 미러 + Phase 11 wire `e020ad0` 의 FinOps Showback surface NEW = F27.1~F27.7 pattern verbatim 미러 결정 wire.
- **F34.8.9** CR lessons applied 18종 결정 wire (CR 0-2 RLS lesson ✅ APPLIED + CR 1-1 audit-first INSERT ✅ APPLIED (8 NEW audit log entries + 5-cloud-provider source attribution 2행 append = 10 NEW effective audit events) + CR 4-3/4-4 lessons carry ✅ APPLIED + CR 1-1 ContextVar lesson ✅ APPLIED + CR 1-1 RSC boundary lesson ✅ APPLIED + CR 9-6 commit message discipline ✅ APPLIED + CR 11-3 honest-DEFER discipline ✅ APPLIED (133번째 epic 연속 정직 회복) + CR 11-4 D-001~D-005 + P-015 ✅ APPLIED + CR 12-1 L4 ✅ APPLIED + CR 12-5 D-14 typed exception envelope ✅ APPLIED (16 NEW typed exception classes) + CR 12-5 D-PARITY-01 inversion ✅ APPLIED + CR 12-5 D-GATE-01 inversion ✅ APPLIED + A36 SDR 검증 4-step 자동 적용 ✅ + AD-14 stack pin ✅ APPLIED + AD-22 owner-only RBAC ✅ APPLIED + NFR4 PII minimization ✅ PRESERVED) 결정 wire.
- **F34.8.10** D-DEFER-* honestly 결정 wire (CR 11-3 133번째 epic 연속 정직 회복 결정 wire 보존) = D-1-1-DEFER-1/2/3 RESOLVED 보존 60~133번째 + D-LAUNCH-1-DEFER-1 honestly preserved 65~133번째 + D-EPIC-16-REVIEW-DEFER-2~6 ✅ RESOLVED 보존 78~133번째 + D-PHASE-4-DR-DEFER-1/2 ✅ RESOLVED 보존 73~133번째 + D-EPIC-17-WIRE-DEFER-T2-T3-UI ✅ RESOLVED 보존 83~133번째 + D-RETENTION-1 ✅ RESOLVED 보존 85~133번째 + D-OBSERVABILITY-1 ✅ RESOLVED 보존 89~133번째 + D-PERFORMANCE-1 ✅ RESOLVED 보존 93~133번째 + D-CHAOS-1 ✅ RESOLVED 보존 100~133번째 + D-SLO-1 ✅ RESOLVED 보존 101~133번째 + D-FINOPS-1 ✅ RESOLVED 보존 105~133번째 + D-FINOPS-2 ✅ RESOLVED 보존 109~133번째 + D-FINOPS-3 ✅ RESOLVED 보존 113~133번째 + D-FINOPS-4 ✅ RESOLVED 보존 117~133번째 + D-FINOPS-5 ✅ RESOLVED 보존 121~133번째 + D-FINOPS-6 ✅ RESOLVED 보존 125~133번째 + D-FINOPS-7 ✅ RESOLVED 보존 129~133번째 + **D-FINOPS-8 신규 honestly DEFER 보존 = Phase 18 PRD entry 진입 시점에 carry-over chain 정직 회복 결정 wire 진입: Phase 17 close-out retro `de009fe` §13 + Phase 16 close-out retro `26fd530` §13 + Phase 15 close-out retro `102f370` §13 + Phase 14 close-out retro `5b367d9` §13 + Phase 13 close-out retro `850b4f8` §13 + Phase 12 close-out retro `3354e83` §13 + Phase 11 close-out retro `80df15b` §12 + Phase 10 close-out retro `733d428` §10 + Phase 9 close-out retro `634427d` §10 + Phase 8 close-out retro `ab495a8` §10 + Phase 7 close-out retro `326fa9f` §10 + Phase 6 close-out retro `f9f006c` §13 + Epic 17 close-out retro `be8f3bd` §11 "FinOps Cloud Commitment Management 결정 wire 보류, Phase 18+ 진입 시점" verbatim 해소**) + Epic 1 ~ Epic 17 + Phase 3 ~ Phase 17 + 1st release cycle 정합 보존 결정 wire (cj-style 133번째 epic 연속 정직 회복 pre-flight 정합 sweep) + 결정 wire 일자: 2026-08-25 (KST) + **next**: 옵션 (a) Phase 18 bmad-create-story spec entry 진입 (cj-style 134번째 wire 진입 시점) / 옵션 (b) Phase 18 bmad-dev-story atomic wire T1~T8 진입 (cj-style 135번째) / 옵션 (c) Phase 18 close-out retro 진입 (cj-style 136번째) / 옵션 (d) Epic 19+ 진입 / 옵션 (e) D-DEFER-* follow-up 결정 wire 보류 결정 wire.

---

## F35. FinOps Pricing, Rate Card & TCO Modeling (Phase 19 territory 신규)

> **§F35 결정 wire 진입** — Phase 18 close-out retro `de72f50` (cj-style 136번째 wire entry) 직후 다음 옵션 4종 중 **사용자 권장 결정 = 옵션 (a) Phase 19+ 진입 + 옵션 (a) FinOps Pricing, Rate Card & TCO Modeling (Recommended) (rate_card_aggregator 5 cloud provider (AWS EDP + Azure EA + GCP CUD Pricing + Naver Cloud Volume Tier + KT Cloud Volume Tier) cross-rollup + tco_modeling_selector 8 NEW KPI calculations (total_blended_rate_krw_per_hour + effective_discount_pct + tco_1year_commitment_krw + tco_3year_commitment_krw + tco_on_demand_krw + cost_per_user_krw + cost_per_transaction_krw + unit_economics_score) + pricing_report_generation_engine PDF + CSV + Excel + scheduled_pricing_dispatch KST cron 4 cadence + tenant_scoped_pricing_role RBAC owner-only + pricing_dashboard UI 5 sub-components + ko-KR.json `finops_pricing.*` namespace EXTENSION ~30 keys + audit-first INSERT 8 NEW via ActionClass.FINOPS_PRICING + Capability FINOPS_PRICING 1 NEW + dry-run mode 5 NEW CLI flags + tests + wire scope T1~T8) = Phase 19 territory 결정 wire 진입**. rationale 5종: ① Phase 18 wire `67059cf` FinOps Cloud Commitment Management territory (commitment_inventory_aggregator 5 cloud provider cross-rollup + commitment_kpi_selector 8 NEW KPI selector + commitment report generation engine PDF + CSV + Excel + scheduled commitment dispatch KST cron + tenant-scoped commitment role RBAC owner-only) 의 natural backend PRICING & TCO MODELING LAYER EXTENSION 결정 wire 진입 (8 FinOps modules 의 data aggregate → pricing-grade TCO view + cross-module pricing KPI 8개 + scheduled pricing report export PDF+CSV+Excel weekly/monthly/quarterly/annual + rate_card cross-rollup + blended_rate_per_hour + effective_discount_pct + TCO 1y/3y 비교 + unit economics + AWS EDP + Azure EA + GCP CUD Pricing + Naver Cloud + KT Cloud 멀티 cloud pricing 지원) ② cj-style discipline 회피 위험 방지 (49~136번째 누적 88-entry-point cycle + 136번째 Phase 18 close-out retro + 135번째 atomic wire + 134번째 spec entry + 133번째 PRD entry 패턴 모두 wire DONE 진입 정합 보존 후 즉시 Phase 19 진입 = 1-day atomic sprint discipline) ③ 비즈니스 우선순위 = enterprise 고객 onboarding 시 pricing 기반 TCO modeling (rate card 관리 + 1y vs 3y vs on-demand vs Savings Plans 비교 + blended/unblended rate 추적 + unit economics (cost per user, cost per transaction) + 한국 공공 조달 가격 가이드라인 정합) territory 필수 (Phase 18 commitment의 RIs/SPs/CUDs 구매 권고 → Phase 19 TCO modeling EXTENSION + Phase 17 sustainability의 renewable_energy_pct + carbon_offset → carbon-aware pricing 권고 EXTENSION + Phase 16 reporting의 executive rollup → pricing KPI rollup EXTENSION + Phase 15 tag_governance의 allocation rule → pricing allocation rule EXTENSION + Phase 14 optimization의 rightsizing + commitment_recommender → TCO break-even EXTENSION + Phase 13 forecast의 12-month horizon → pricing forecast EXTENSION + Phase 12 anomaly의 z-score 기반 → pricing anomaly detection EXTENSION + Phase 11 showback의 cost center breakdown → cost center pricing breakdown EXTENSION + AWS EDP + Azure EA + GCP CUD Pricing + Naver Cloud + KT Cloud 멀티 cloud rate card 지원) ④ Phase 18 wire `67059cf` 의 5 cloud provider commitment cross-rollup (AWS EC2 RI + RDS RI + EC2 SP + S3 SP + Redshift SP + DynamoDB SP + Azure reservations + GCP CUDs + Naver Cloud + KT Cloud) + Phase 14 wire `e904485` 의 commitment_recommender 1y/3y break-even + Phase 13 wire `8b98030` 의 12-month forecast + Phase 11 wire `e020ad0` 의 cost center cost allocation + Phase 12 wire `f3c0e63` 의 anomaly detection baseline 의 자연스러운 carry-over chain = Phase 19 pricing 의 rate_card_inventory (AWS EDP + Azure EA + GCP CUD Pricing + Naver Cloud Volume Tier + KT Cloud Volume Tier + marketplace) + blended_rate_per_hour (effective rate after discount) + effective_discount_pct (1 - blended/on_demand) + TCO 1y vs 3y vs on_demand (Phase 14 break-even EXTENSION) + cost_per_user_krw (unit economics) + cost_per_transaction_krw (unit economics) + unit_economics_score (composite KPI) + renewal_workflow (auto + manual + hold) EXTENSION 정합 ⑤ 1st release close-out retro + Epic 17 close-out retro + Phase 5~18 close-out retro territory verbatim 보존 + AD-22 owner-only RBAC + Epic 12 2FA 챌린지 보존 + AD-14 stack pin + NFR4 PII minimization ✅ PRESERVED + AD-46 FinOps Pricing, Rate Card & TCO Modeling 신규 결정 ((a)~(g) 7 sub-decisions 모두 결정 wire 진입).

### F35.1 rate_card_aggregator + 5 cloud provider cross-rollup EXTENSION (A514 결정)

- **F35.1.1** `apps/api/modules/finops/pricing/rate_card_aggregator.py` NEW ~+200 LOC, `aggregate_rate_cards(tenant_id, period_key, cloud_provider) -> RateCardInventory` TypedDict 18 fields 결정 wire = `tenant_id` UUID + `period_key` TEXT + `cloud_provider` enum (aws + azure + gcp + naver + kt) + `rate_card_type` enum (edp + ea + cud_pricing + naver_volume + kt_volume + marketplace) + `service_code` TEXT (예: EC2 m5.large, RDS MySQL db.t3.medium, S3 Standard) + `region` TEXT (예: ap-northeast-2) + `on_demand_rate_krw_per_hour` Decimal(18,6) + `negotiated_rate_krw_per_hour` Decimal(18,6) + `effective_rate_krw_per_hour` Decimal(18,6) 결정 wire + `volume_tier` enum (tier_1 + tier_2 + tier_3 + tier_4 + custom) + `committed_use_discount_pct` Decimal(5,2) (예: 40.00%) + `rate_card_effective_from` DATE + `rate_card_effective_to` DATE + `rate_card_source` TEXT (예: aws_pricing_api, azure_retail_prices_api, gcp_cloud_pricing_api) + `last_synced_at` TIMESTAMPTZ + `record_hash` TEXT(64) 결정 wire.
- **F35.1.2** rate_card_aggregator `aggregate_rate_cards(tenant_id, period_key, cloud_provider)` 시그니처 결정 wire = `tenant_id: UUID` + `period_key: str` (예: "2026-08") + `cloud_provider: Literal["aws", "azure", "gcp", "naver", "kt"] | None` (None = 5 cloud 모두) 결정 wire + `-> RateCardInventory` TypedDict 18 fields 반환 결정 wire + Phase 11 wire `e020ad0` 의 `aggregate_showback(tenant_id, period_key, department_id, group_by)` + Phase 12 wire `f3c0e63` 의 `aggregate_anomaly(tenant_id, period_key, severity)` + Phase 13 wire `8b98030` 의 `forecast_capacity(tenant_id, period_key, horizon_months)` + Phase 14 wire `e904485` 의 `aggregate_optimization(tenant_id, period_key, resource_type)` + Phase 15 wire `1b800d9` 의 `aggregate_tag_governance(tenant_id, period_key)` + Phase 16 wire `81ae00a` 의 `aggregate_executive_rollup(tenant_id, period_key, scope)` + Phase 17 wire `97cfe4e` 의 `aggregate_carbon_emissions(tenant_id, period_key, scope)` + Phase 18 wire `67059cf` 의 `aggregate_commitment_inventory(tenant_id, period_key, cloud_provider)` pattern verbatim EXTENSION 결정 wire.
- **F35.1.3** rate_card_aggregator RLS 자동 적용 결정 wire = `tenant_id = current_setting('app.tenant_id')::uuid` 필터 CR 0-2 verbatim 결정 wire + 4 industries baseline (manufacturing + retail + service + saas) industry-agnostic 결정 wire + Phase 11~18 wire 의 RLS 자동 적용 pattern verbatim EXTENSION 결정 wire.
- **F35.1.4** rate_card_aggregator `RateCardInventory` TypedDict 18 fields 결정 wire = `tenant_id` + `period_key` + `cloud_provider` + `rate_card_type` + `service_code` + `region` + `on_demand_rate_krw_per_hour` + `negotiated_rate_krw_per_hour` + `effective_rate_krw_per_hour` + `volume_tier` + `committed_use_discount_pct` + `rate_card_effective_from` + `rate_card_effective_to` + `rate_card_source` + `last_synced_at` + `record_hash` + `inventory_count` + `last_aggregated_at` 결정 wire.
- **F35.1.5** rate_card_aggregator audit-first INSERT 결정 wire = `pricing_dashboard_viewed` 1 NEW via ActionClass.FINOPS_PRICING EXTENSION + CR 1-1 verbatim 적용 결정 wire + 5-year retention NFR8 EXTENSION 결정 wire.
- **F35.1.6** rate_card_aggregator 5 cloud provider integration 결정 wire = AWS Pricing API (PricingClient.get_products) + Azure Retail Prices API (GET /subscriptions/{subscriptionId}/providers/Microsoft.Capacity/prices) + GCP Cloud Pricing API (cloudbilling.googleapis.com/v1.services) + Naver Cloud Volume Tier API (internal endpoint) + KT Cloud Volume Tier API (internal endpoint) 결정 wire + 5 cloud provider 모두 wire 결정 wire.
- **F35.1.7** rate_card_aggregator Unit economics 계산 결정 wire = `cost_per_user_krw = total_cost_krw / active_user_count` + `cost_per_transaction_krw = total_cost_krw / transaction_count` 결정 wire + Phase 11 showback의 cost center breakdown EXTENSION 결정 wire.
- **F35.1.8** rate_card_aggregator V8 byte-identical determinism 결정 wire = `record_hash` SHA-256 of normalized rate card data 결정 wire + Phase 11~18 V8 byte-identical determinism pattern verbatim EXTENSION 결정 wire.
- **F35.1.9** rate_card_aggregator 5-framework support 결정 wire = FinOps Foundation Pricing Principles + AWS Pricing Models (On-Demand + Reserved + Savings Plans + Spot) + Azure Pricing Calculator + GCP Pricing Calculator + 한국 공공 조달 가격 가이드라인 (조달청 나라장터 클라우드 가격) 결정 wire.
- **F35.1.10** rate_card_aggregator multi-period support 결정 wire = last_30d + last_90d + last_180d + last_365d + period_custom 5 옵션 결정 wire + Phase 14~18 wire 의 multi-period pattern verbatim EXTENSION 결정 wire.
- **F35.1.11** rate_card_aggregator error envelope 결정 wire = `RateCardAggregationError(500)` + `RateCardScopeError(404)` + `RateCardPeriodError(422)` + `RateCardProviderError(502)` 4 NEW typed exception 결정 wire + CR 12-5 D-14 envelope verbatim EXTENSION 결정 wire + `apps/api/main.py` envelope handlers 4 NEW EXTENSION 결정 wire + Phase 11~18 wire 의 error envelope pattern verbatim EXTENSION 결정 wire.
- **F35.1.12** AD-46 신규 결정 진입 (a)~(g) 7 sub-decisions 결정 wire + 부록 A architectural decisions table 1 NEW row EXTENSION + Phase 12~18 AD-39~AD-45 verbatim EXTENSION 결정 wire.

### F35.2 tco_modeling_selector + 8 NEW KPI calculations (A515 결정)

- **F35.2.1** `apps/api/modules/finops/pricing/tco_modeling_selector.py` NEW ~+220 LOC 결정 wire + 8 NEW TCO KPI TypedDict 결정 wire = `total_blended_rate_krw_per_hour` (Decimal 18,6) + `effective_discount_pct` (Decimal 5,2, range 0.00~100.00) + `tco_1year_commitment_krw` (Decimal 18,2) + `tco_3year_commitment_krw` (Decimal 18,2) + `tco_on_demand_krw` (Decimal 18,2) + `cost_per_user_krw` (Decimal 18,2) + `cost_per_transaction_krw` (Decimal 18,2) + `unit_economics_score` (Decimal 5,2, range 0.00~100.00) 결정 wire + 6 mandatory KPI (total_blended + effective_discount + tco_1y + tco_3y + cost_per_user + cost_per_transaction) + 2 optional KPI (tco_on_demand + unit_economics_score) 결정 wire.
- **F35.2.2** tco_modeling_selector KPI calculation logic 결정 wire = `total_blended_rate_krw_per_hour = sum(effective_rate * usage_hours) / sum(usage_hours)` (가중 평균) + `effective_discount_pct = (1 - total_blended_rate / total_on_demand_rate) * 100` + `tco_1year_commitment_krw = upfront_fee_1y + monthly_rate_1y * 12` + `tco_3year_commitment_krw = upfront_fee_3y + monthly_rate_3y * 36` + `tco_on_demand_krw = on_demand_rate * 8760` (1년 시간) + `cost_per_user_krw = total_cost / active_user_count` + `cost_per_transaction_krw = total_cost / transaction_count` + `unit_economics_score = (revenue_per_unit / cost_per_unit) * 100` 결정 wire.
- **F35.2.3** tco_modeling_selector per-tenant override EXTENSION 결정 wire + 4 industries baseline (manufacturing + retail + service + saas) industry-agnostic 결정 wire + policy evaluation precedence tenant override > industry baseline > system default 결정 wire + Phase 11~18 wire 의 per-tenant override pattern verbatim EXTENSION 결정 wire.
- **F35.2.4** tco_modeling_selector KPI selector signature 결정 wire = `select_tco_kpis(tenant_id, period_key, cloud_provider, kpi_set) -> TCOKPIBundle` TypedDict 10 fields (8 KPI + bundle_id + computed_at) 결정 wire + Phase 11~18 wire 의 KPI selector pattern verbatim EXTENSION 결정 wire.
- **F35.2.5** tco_modeling_selector audit-first INSERT 결정 wire = `pricing_kpi_refreshed` 1 NEW via ActionClass.FINOPS_PRICING EXTENSION + CR 1-1 verbatim 적용 결정 wire.
- **F35.2.6** tco_modeling_selector Phase 18 commitment KPI EXTENSION 결정 wire = commitment_recommender 1y/3y break-even EXTENSION + commitment_coverage_pct → effective_discount_pct EXTENSION + commitment_utilization_pct → TCO accuracy EXTENSION 결정 wire + Phase 18 wire `67059cf` 의 commitment_kpi_selector pattern verbatim EXTENSION 결정 wire.
- **F35.2.7** tco_modeling_selector Phase 17 sustainability KPI EXTENSION 결정 wire = renewable_energy_pct → carbon-aware pricing_krw EXTENSION + carbon_offset → green pricing discount EXTENSION 결정 wire + Phase 17 wire `97cfe4e` 의 sustainability_kpi_selector pattern verbatim EXTENSION 결정 wire.
- **F35.2.8** tco_modeling_selector 4 industries baseline 결정 wire = manufacturing: cost_per_user_krw baseline 50000, cost_per_transaction_krw baseline 200, unit_economics_score baseline 75 / retail: cost_per_user_krw baseline 30000, cost_per_transaction_krw baseline 150, unit_economics_score baseline 80 / service: cost_per_user_krw baseline 40000, cost_per_transaction_krw baseline 100, unit_economics_score baseline 85 / saas: cost_per_user_krw baseline 15000, cost_per_transaction_krw baseline 50, unit_economics_score baseline 90 결정 wire + per-tenant override EXTENSION 결정 wire.
- **F35.2.9** tco_modeling_selector error envelope 결정 wire = `TCOModelingError(500)` + `TCOScopeError(404)` + `TCOPeriodError(422)` + `TCOBaselineError(500)` 4 NEW typed exception 결정 wire + CR 12-5 D-14 envelope verbatim EXTENSION 결정 wire.
- **F35.2.10** tco_modeling_selector TCO 1y vs 3y 비교 로직 결정 wire = `break_even_months_1y = upfront_fee_1y / (on_demand_cost_1y - commitment_cost_1y)` + `break_even_months_3y = upfront_fee_3y / (on_demand_cost_3y - commitment_cost_3y)` + `recommendation = "1y" if break_even_months_1y < 8 else "3y" if break_even_months_3y < 18 else "on_demand"` 결정 wire + Phase 14 wire `e904485` 의 commitment_recommender break-even EXTENSION 결정 wire.
- **F35.2.11** tco_modeling_selector cache invalidation 결정 wire = 6-month rolling window cache + commit_id 기반 idempotent 결정 wire + Phase 18 wire `67059cf` 의 cache pattern verbatim EXTENSION 결정 wire.
- **F35.2.12** AD-46 신규 결정 진입 (a)~(g) 7 sub-decisions 결정 wire + 부록 A architectural decisions table 1 NEW row EXTENSION + Phase 12~18 AD-39~AD-45 verbatim EXTENSION 결정 wire.

### F35.3 pricing_report_generation_engine + PDF + CSV + Excel + 3 cadence (A516 결정)

- **F35.3.1** `apps/api/modules/finops/pricing/pricing_report_generator.py` NEW ~+250 LOC 결정 wire + 3 NEW format generator (PDF + CSV + Excel) 결정 wire + Phase 9 wire ReportGenerator EXTENSION + Phase 16 wire `81ae00a` 의 executive_report_generator + Phase 17 wire `97cfe4e` 의 sustainability_report_generator + Phase 18 wire `67059cf` 의 commitment_report_generator pattern verbatim EXTENSION 결정 wire.
- **F35.3.2** pricing_report_generator PDF format 결정 wire = reportlab 4.0.7 AD-14 stack pin EXTENSION + 6 pages 결정 wire = cover page (tenant_id + period_key + generated_at) + rate card summary table + TCO comparison chart (bar chart) + cost per unit economics dashboard + renewal recommendations + appendix (data sources + methodology) 결정 wire.
- **F35.3.3** pricing_report_generator CSV format 결정 wire = 14 columns 결정 wire = tenant_id + period_key + cloud_provider + service_code + region + on_demand_rate + negotiated_rate + effective_rate + volume_tier + commitment_discount_pct + tco_1y_krw + tco_3y_krw + tco_on_demand_krw + cost_per_unit_krw 결정 wire + utf-8 encoding + RFC 4180 EXTENSION 결정 wire.
- **F35.3.4** pricing_report_generator Excel format 결정 wire = openpyxl 3.1.2 + xlsxwriter 3.1.9 AD-14 stack pin EXTENSION 결정 wire + 5 sheets 결정 wire = Summary (8 KPI) + Rate Cards (5 cloud provider) + TCO Comparison (1y vs 3y vs on_demand) + Unit Economics (cost per user/transaction) + Methodology (data sources + assumptions) 결정 wire.
- **F35.3.5** pricing_report_generator 3 cadence option 결정 wire = monthly + quarterly + annual 결정 wire + Phase 18 wire `67059cf` 의 commitment_report_generator 의 3 cadence pattern verbatim EXTENSION 결정 wire.
- **F35.3.6** pricing_report_generator 5-framework support 결정 wire = FinOps Foundation Pricing Principles + AWS Pricing Models + Azure Pricing Calculator + GCP Pricing Calculator + 한국 공공 조달 가격 가이드라인 EXTENSION 결정 wire.
- **F35.3.7** pricing_report_generator audit-first INSERT 결정 wire = `pricing_report_generated` 2행 결정 wire (PDF + Excel 2행 = 동일 phase 19 wire 진입 시점) EXTENSION + CR 1-1 verbatim 적용 결정 wire + 5-year retention NFR8 EXTENSION 결정 wire.
- **F35.3.8** pricing_report_generator report_id 결정 wire = UUID PK + tenant_id + period_key + report_type enum (monthly + quarterly + annual) + format enum (pdf + csv + excel) + generated_at TIMESTAMPTZ DEFAULT NOW() + trace_id TEXT + RLS 자동 적용 CR 0-2 verbatim 결정 wire.
- **F35.3.9** pricing_report_generator stream response 결정 wire = `StreamingResponse` FastAPI 응답 + 3 format 모두 wire 결정 wire + Phase 11~18 wire 의 stream response pattern verbatim EXTENSION 결정 wire.
- **F35.3.10** pricing_report_generator chart generation 결정 wire = Recharts 2.12.7 AD-14 stack pin EXTENSION + 4 chart type (rate_card_bar + tco_comparison_line + unit_economics_scatter + renewal_timeline_gantt) 결정 wire + Phase 16~18 wire 의 chart pattern verbatim EXTENSION 결정 wire.
- **F35.3.11** pricing_report_generator error envelope 결정 wire = `PricingReportGenerationError(500)` + `PricingReportExportError(500)` + `PricingReportArchiveError(500)` 3 NEW typed exception 결정 wire + CR 12-5 D-14 envelope verbatim EXTENSION 결정 wire.
- **F35.3.12** AD-46 신규 결정 진입 (a)~(g) 7 sub-decisions 결정 wire + 부록 A architectural decisions table 1 NEW row EXTENSION + Phase 12~18 AD-39~AD-45 verbatim EXTENSION 결정 wire.

### F35.4 scheduled_pricing_dispatch + 4 cron schedules KST (A516 결정)

- **F35.4.1** `apps/api/modules/finops/pricing/scheduled_pricing_dispatch.py` NEW ~+200 LOC 결정 wire + `apps/api/jobs/scheduled_pricing_dispatch_job.py` NEW ~+150 LOC 결정 wire + APScheduler 3.10.4 AD-14 stack pin EXTENSION 결정 wire + KST cron 4 NEW cadence (weekly Mon 09:00 + monthly 1st-day 09:00 + quarterly 1st-day 09:00 + annual Jan-1 09:00 KST pytz timezone('Asia/Seoul')) 결정 wire + Phase 16~18 wire 의 APScheduler pattern verbatim EXTENSION 결정 wire.
- **F35.4.2** scheduled_pricing_dispatch recipients JSONB max 50 결정 wire + 4 channel 결정 wire = Slack + Email + S3 archive + MS Teams webhook (Phase 18 wire 의 MS Teams 채널 신규 도입 후 보존) 결정 wire + Phase 16~18 wire 의 4 channel pattern verbatim EXTENSION 결정 wire.
- **F35.4.3** scheduled_pricing_dispatch audit-first INSERT 결정 wire = `pricing_report_dispatched` + `pricing_scheduled_dispatch_evaluated` 2 NEW via ActionClass.FINOPS_PRICING EXTENSION 결정 wire + CR 1-1 verbatim 적용 결정 wire.
- **F35.4.4** scheduled_pricing_dispatch cron configuration 결정 wire = `weekly_pricing_cron = "0 9 * * 1"` (Mon 09:00 KST) + `monthly_pricing_cron = "0 9 1 * *"` (1st day 09:00 KST) + `quarterly_pricing_cron = "0 9 1 */3 *"` (1st day of quarter 09:00 KST) + `annual_pricing_cron = "0 9 1 1 *"` (Jan 1 09:00 KST) 결정 wire + Phase 16~18 wire 의 cron configuration pattern verbatim EXTENSION 결정 wire.
- **F35.4.5** scheduled_pricing_dispatch recipient_resolver 결정 wire = `resolve_pricing_recipients(tenant_id, period_key) -> list[Recipient]` 4 source 결정 wire = tenant_settings.pricing_alert_emails (max 50) + tenant_settings.slack_webhook_url + tenant_settings.ms_teams_webhook_url + tenant_settings.s3_archive_bucket 결정 wire + Phase 16~18 wire 의 recipient_resolver pattern verbatim EXTENSION 결정 wire.
- **F35.4.6** scheduled_pricing_dispatch idempotency 결정 wire = `dispatch_idempotency_key = hash(tenant_id + period_key + dispatch_cron)` 결정 wire + 동일 idempotency_key 중복 dispatch 시 skip 결정 wire + Phase 16~18 wire 의 idempotency pattern verbatim EXTENSION 결정 wire.
- **F35.4.7** scheduled_pricing_dispatch dry-run mode 결정 wire = `--finops-scheduled-pricing-dispatch-dry-run` CLI flag + dry-run 시 actual `pricing_report_dispatched` + `pricing_scheduled_dispatch_evaluated` audit-first INSERT skip 결정 wire + Phase 16~18 wire 의 dry-run pattern verbatim EXTENSION 결정 wire.
- **F35.4.8** scheduled_pricing_dispatch error envelope 결정 wire = `ScheduledPricingDispatchError(500)` + `PricingCronExpressionInvalidError(400)` + `PricingRecipientResolverError(404)` + `PricingDispatchIdempotencyViolationError(422)` 4 NEW typed exception 결정 wire + CR 12-5 D-14 envelope verbatim EXTENSION 결정 wire.
- **F35.4.9** scheduled_pricing_dispatch timezone 처리 결정 wire = APScheduler timezone='Asia/Seoul' 설정 + KST-aware cron expression 검증 + UTC 자동 변환 결정 wire + Phase 16~18 wire 의 timezone pattern verbatim EXTENSION 결정 wire.
- **F35.4.10** scheduled_pricing_dispatch S3 archive 결정 wire = s3://costmgr-pricing-archive/{tenant_id}/{YYYY-MM-DD}/pricing-report-{period_key}.pdf 결정 wire + AES-256-GCM NFR6 encryption EXTENSION 결정 wire.
- **F35.4.11** scheduled_pricing_dispatch retry policy 결정 wire = 3회 retry + exponential backoff (1s + 2s + 4s) + Slack/Email 채널 우선 retry + MS Teams webhook fallback 결정 wire.
- **F35.4.12** AD-46 신규 결정 진입 (a)~(g) 7 sub-decisions 결정 wire + 부록 A architectural decisions table 1 NEW row EXTENSION + Phase 12~18 AD-39~AD-45 verbatim EXTENSION 결정 wire.

### F35.5 tenant-scoped pricing role RBAC + owner-only (A517 결정)

- **F35.5.1** `apps/api/core/role.py` MODIFIED 결정 wire + `Role.PRICING_VIEWER` 1 NEW enum 결정 wire + Phase 18 wire `67059cf` 의 `Role.COMMITMENT_VIEWER` pattern verbatim EXTENSION 결정 wire.
- **F35.5.2** `apps/api/core/capability.py` MODIFIED 결정 wire + `Capability.FINOPS_PRICING = "finops_pricing"` 1 NEW enum EXTENSION 결정 wire + Phase 18 `Capability.FINOPS_COMMITMENT` 패턴 verbatim EXTENSION 결정 wire + 4-industry grants ✅/✅/✅/✅ industry-agnostic per CR 12-1 L4 verbatim EXTENSION 결정 wire.
- **F35.5.3** `apps/api/dependencies/capability.py` MODIFIED 결정 wire + `require_finops_pricing` 1 NEW dep + `require_pricing_role()` 1 NEW dep 결정 wire + Phase 18 `require_commitment_role()` pattern verbatim EXTENSION 결정 wire.
- **F35.5.4** Epic 12 2FA 챌린지 mandatory 보존 EXTENSION 결정 wire + Phase 18 wire `67059cf` 의 Epic 12 2FA 챌린지 pattern verbatim EXTENSION 결정 wire + pricing dashboard 진입 시 2FA 챌린지 mandatory 결정 wire.
- **F35.5.5** AD-22 owner-only RBAC EXTENSION 결정 wire = `Role.PRICING_VIEWER` 는 owner-only dashboard view + admin-only 수정 권한 결정 wire + Phase 16~18 wire 의 AD-22 owner-only pattern verbatim EXTENSION 결정 wire.
- **F35.5.6** `apps/api/core/capability.py` MODIFIED 결정 wire + `Capability.FINOPS_PRICING` 4-industry grants ✅/✅/✅/✅ industry-agnostic per CR 12-1 L4 precedent verbatim EXTENSION 결정 wire.
- **F35.5.7** `PricingRolePermissionError` 1 NEW typed exception EXTENSION 결정 wire = Role.PRICING_VIEWER 권한 부족 시 HTTPException(403) EXTENSION + audit-first INSERT `pricing_role_permission_violation` 결정 wire.
- **F35.5.8** `apps/api/core/audit_action.py` MODIFIED 결정 wire + `ActionClass.FINOPS_PRICING = "finops_pricing"` 1 NEW enum EXTENSION 결정 wire + `FinopsPricingAction` 8 NEW Literal 결정 wire = `pricing_dashboard_viewed` + `cross_module_pricing_kpi_calculated` + `pricing_report_generated` + `pricing_report_exported` + `pricing_report_dispatched` + `pricing_scheduled_dispatch_evaluated` + `finops_pricing_dry_run_executed` + `pricing_kpi_refreshed` EXTENSION 결정 wire + Phase 18 wire `67059cf` 의 ActionClass.FINOPS_COMMITMENT pattern verbatim EXTENSION 결정 wire.
- **F35.5.9** `PricingTenantScopeViolationError(403)` 1 NEW typed exception EXTENSION = RLS 자동 적용 CR 0-2 verbatim + tenant_id mismatch 시 HTTPException(403) EXTENSION 결정 wire.
- **F35.5.10** `PricingCapabilityGateViolationError(403)` 1 NEW typed exception EXTENSION = Capability.FINOPS_PRICING gate 실패 시 HTTPException(403) EXTENSION + audit-first INSERT `pricing_capability_gate_violation` 결정 wire.
- **F35.5.11** NFR8 (감사로그 5년 append-only) 결정 wire = 8 NEW audit log entries (`pricing_report_generated` + `pricing_dashboard_viewed` + `pricing_kpi_refreshed` + `pricing_report_exported` + `pricing_report_dispatched` + `pricing_scheduled_dispatch_evaluated` + `finops_pricing_dry_run_executed` + `cross_module_pricing_kpi_calculated`) + 5-year retention EXTENSION + NFR8 보존 EXTENSION.
- **F35.5.12** AD-46 신규 결정 진입 (a)~(g) 7 sub-decisions 결정 wire + 부록 A architectural decisions table 1 NEW row EXTENSION + Phase 12~18 AD-39~AD-45 verbatim EXTENSION 결정 wire.

### F35.6 pricing dashboard UI + 5 sub-components (A517 결정)

- **F35.6.1** `apps/web/app/[locale]/(dashboard)/admin/finops/pricing/page.tsx` NEW ~+180 LOC 결정 wire + 5 sub-components 결정 wire = `RateCardAggregatorPanel` (5 cloud provider rate card view) + `TCOModelingSelectorPanel` (8 NEW TCO KPI selector) + `PricingReportGeneratorPanel` (3 format generator) + `ScheduledPricingDispatchConfigPanel` (4 cadence cron config) + `PricingTrendMiniChart` (12-month TCO trend) 결정 wire + Phase 16~18 wire 의 dashboard UI pattern verbatim EXTENSION 결정 wire.
- **F35.6.2** `apps/web/components/finops/FinopsPricingDashboardPanel.tsx` NEW Client component ~+200 LOC 결정 wire + 5-tab layout 결정 wire = Rate Cards / TCO / Reports / Dispatch / Trends 결정 wire + Phase 18 wire `67059cf` 의 `FinopsCommitmentDashboardPanel.tsx` pattern verbatim EXTENSION 결정 wire.
- **F35.6.3** `apps/web/messages/ko-KR.json` MODIFIED 결정 wire + `finops_pricing.*` namespace EXTENSION ~30 keys 결정 wire = `finops_pricing.dashboard.title` + `finops_pricing.rate_cards.title` + `finops_pricing.rate_cards.aws` + `finops_pricing.rate_cards.azure` + `finops_pricing.rate_cards.gcp` + `finops_pricing.rate_cards.naver` + `finops_pricing.rate_cards.kt` + `finops_pricing.tco.title` + `finops_pricing.tco.comparison_1y` + `finops_pricing.tco.comparison_3y` + `finops_pricing.tco.comparison_on_demand` + `finops_pricing.unit_economics.title` + `finops_pricing.unit_economics.cost_per_user` + `finops_pricing.unit_economics.cost_per_transaction` + `finops_pricing.reports.title` + `finops_pricing.reports.monthly` + `finops_pricing.reports.quarterly` + `finops_pricing.reports.annual` + `finops_pricing.reports.format_pdf` + `finops_pricing.reports.format_csv` + `finops_pricing.reports.format_excel` + `finops_pricing.dispatch.title` + `finops_pricing.dispatch.weekly` + `finops_pricing.dispatch.monthly` + `finops_pricing.dispatch.quarterly` + `finops_pricing.dispatch.annual` + `finops_pricing.trends.title` + `finops_pricing.error.aggregation` + `finops_pricing.error.kpi_calculation` + `finops_pricing.error.report_generation` 결정 wire + CR 11-4 D-002 verbatim SSOT 결정 wire + Phase 18 wire `67059cf` 의 ko-KR.json `finops_commitment.*` namespace EXTENSION pattern verbatim EXTENSION 결정 wire.
- **F35.6.4** `apps/web/lib/finops/pricing-types.ts` NEW TypeScript mirror 결정 wire = `RateCardInventory` 18 fields + `TCOKPIBundle` 10 fields + `PricingReportRequest` 5 fields + `ScheduledPricingDispatchConfig` 6 fields TypedDict 결정 wire + CR 12-5 D-PARITY-01 inversion TypeScript mirror 결정 wire + Phase 18 wire `67059cf` 의 `commitment-types.ts` pattern verbatim EXTENSION 결정 wire.
- **F35.6.5** owner-only RBAC AD-22 verbatim 결정 wire + Epic 12 2FA 챌린지 보존 결정 wire + Phase 18 wire `67059cf` 의 owner-only pattern verbatim EXTENSION 결정 wire.
- **F35.6.6** ARIA labels WCAG 2.1 AA 결정 wire + Phase 16~18 wire 의 WCAG 2.1 AA pattern verbatim EXTENSION 결정 wire + 모든 pricing dashboard 컴포넌트 aria-label 결정 wire.
- **F35.6.7** vitest RTL render discipline CR 11-4 D-003 verbatim 결정 wire + Phase 16~18 wire 의 vitest pattern verbatim EXTENSION 결정 wire.
- **F35.6.8** cross-language drift detector EXTENSION 결정 wire + Phase 16~18 wire 의 cross-language drift detector pattern verbatim EXTENSION 결정 wire + TypeScript mirror 결정 wire.
- **F35.6.9** pricing dashboard 5-tab navigation 결정 wire = Rate Cards (default) / TCO / Reports / Dispatch / Trends 결정 wire + Phase 18 wire `67059cf` 의 5-tab layout verbatim EXTENSION 결정 wire.
- **F35.6.10** pricing dashboard error boundary 결정 wire + pricing_dashboard_viewed audit-first INSERT 에러 시 fail-soft 결정 wire + Phase 18 wire `67059cf` 의 error boundary pattern verbatim EXTENSION 결정 wire.
- **F35.6.11** pricing dashboard 2FA 챌린지 통합 결정 wire = Phase 12 wire 의 Epic 12 2FA 챌린지 보존 EXTENSION + pricing dashboard 진입 시 2FA 챌린지 mandatory 결정 wire.
- **F35.6.12** AD-46 신규 결정 진입 (a)~(g) 7 sub-decisions 결정 wire + 부록 A architectural decisions table 1 NEW row EXTENSION + Phase 12~18 AD-39~AD-45 verbatim EXTENSION 결정 wire.

### F35.7 Capability matrix v1.45 EXTENSION FINOPS_PRICING (A517 결정)

- **F35.7.1** `docs/capability-matrix.md` MODIFIED 결정 wire + v1.44 → v1.45 EXTENSION + FINOPS_PRICING 1 NEW row after FINOPS_COMMITMENT industry-agnostic 4-industry grants ✅/✅/✅/✅ CR 12-1 L4 precedent verbatim 미러 EXTENSION + FINOPS_COMMITMENT Phase 18 wire + FINOPS_SUSTAINABILITY Phase 17 wire + FINOPS_REPORTING Phase 16 wire + FINOPS_TAG_GOVERNANCE Phase 15 wire + FINOPS_OPTIMIZATION Phase 14 wire + FINOPS_FORECASTING_CAPACITY_PLANNING Phase 13 wire + FINOPS_ANOMALY_DETECTION + FINOPS_BUDGET_ALERT Phase 12 wire + FINOPS_SHOWBACK + FINOPS_CHARGEBACK Phase 11 wire + SLO_ENGINEERING Phase 10 wire + CHAOS_ENGINEERING Phase 9 wire + PERFORMANCE_TESTING Phase 8 wire + OBSERVABILITY_TRACES + OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind EXTENSION 결정 wire.
- **F35.7.2** `apps/api/core/capability.py` MODIFIED 결정 wire + `Capability.FINOPS_PRICING = "finops_pricing"` 1 NEW enum EXTENSION 결정 wire.
- **F35.7.3** `apps/api/dependencies/capability.py` MODIFIED 결정 wire + `require_finops_pricing` 1 NEW dep EXTENSION 결정 wire.
- **F35.7.4** `apps/api/core/audit_action.py` MODIFIED 결정 wire + `ActionClass.FINOPS_PRICING = "finops_pricing"` 1 NEW enum EXTENSION 결정 wire + `FinopsPricingAction` 8 NEW Literal EXTENSION 결정 wire.
- **F35.7.5** `tests/integration/test_capability_matrix_v1_45_drift.py` NEW 8 NEW pytest cases EXTENSION 결정 wire = FINOPS_PRICING row presence + 4-industry grants ✅/✅/✅/✅ + Phase 11~18 FINOPS_* capability rows preserved + AD-22 owner-only pattern + CR 12-1 L4 verbatim + drift detector EXTENSION + Phase 18 wire `67059cf` 의 test_capability_matrix_v1_44_drift.py pattern verbatim EXTENSION 결정 wire.
- **F35.7.6** `tests/integration/test_audit_action_v1_45_drift.py` NEW 6 NEW pytest cases EXTENSION 결정 wire = ActionClass.FINOPS_PRICING enum presence + FinopsPricingAction 8 Literal + __all__ EXTENSION + Phase 11~18 ActionClass.FINOPS_* enum preserved + drift detector EXTENSION 결정 wire.
- **F35.7.7** `apps/api/modules/finops/__init__.py` MODIFIED 결정 wire = pricing submodule export EXTENSION + commitment_recommender reuse from Phase 18 wire `67059cf` `apps/api/modules/finops/commitment/__init__.py` pattern verbatim EXTENSION 결정 wire.
- **F35.7.8** ko-KR.json `finops_pricing.*` namespace EXTENSION ~30 keys 결정 wire + ARIA labels WCAG 2.1 AA 결정 wire + Phase 18 wire `67059cf` 의 ko-KR.json EXTENSION pattern verbatim EXTENSION 결정 wire.
- **F35.7.9** `apps/web/lib/finops/pricing-types.ts` NEW TypeScript mirror 결정 wire + Phase 18 wire `67059cf` 의 `commitment-types.ts` pattern verbatim EXTENSION 결정 wire + CR 12-5 D-PARITY-01 inversion TypeScript mirror 결정 wire.
- **F35.7.10** `apps/web/lib/finops/pricing-client.ts` NEW TypeScript client 결정 wire + `fetchRateCards()` + `fetchTCOKPIs()` + `generatePricingReport()` + `schedulePricingDispatch()` 4 methods EXTENSION + Phase 18 wire `67059cf` 의 `commitment-client.ts` pattern verbatim EXTENSION 결정 wire.
- **F35.7.11** NFR8 (감사로그 5년 append-only) 결정 wire = 8 NEW audit log entries + 5-year retention EXTENSION + NFR8 보존 EXTENSION.
- **F35.7.12** AD-46 신규 결정 진입 (a)~(g) 7 sub-decisions 결정 wire + 부록 A architectural decisions table 1 NEW row EXTENSION + Phase 12~18 AD-39~AD-45 verbatim EXTENSION 결정 wire.

### F35.8 dry-run + Tests + wire scope T1~T8 (A518 결정)

- **F35.8.1** dry-run mode 결정 wire = `--finops-pricing-rate-card-dry-run` + `--finops-pricing-tco-dry-run` + `--finops-pricing-report-dry-run` + `--finops-scheduled-pricing-dispatch-dry-run` + `--finops-pricing-export-dry-run` 5 NEW CLI flag + dry-run 시 actual `pricing_dashboard_viewed` + `pricing_kpi_refreshed` + `pricing_report_generated` + `pricing_report_dispatched` + `pricing_scheduled_dispatch_evaluated` audit-first INSERT skip 결정 wire + Phase 11~18 dry-run pattern verbatim EXTENSION.
- **F35.8.2** dry-run preview 결과 결정 wire = `phase_19_finops_pricing_rate_card_preview` + `phase_19_finops_pricing_tco_preview` 2 table 결정 wire + preview_id UUID PK + tenant_id UUID + preview_type enum + period_key TEXT + preview_data JSONB + computed_at TIMESTAMPTZ DEFAULT NOW() + trace_id TEXT + RLS 자동 적용 CR 0-2 verbatim + audit-first INSERT `finops_pricing_dry_run_executed` 결정 wire.
- **F35.8.3** dry-run CLI flag 5 NEW 결정 wire + Phase 11~18 dry-run pattern verbatim EXTENSION + 5 CLI flag 모두 wire 결정 wire + dry-run 결과 preview 결정 wire.
- **F35.8.4** tests 결정 wire = ~+62 NEW pytest PASS (rate_card_aggregator 8 + tco_modeling_selector 8 + pricing_report_generator 8 + scheduled_pricing_dispatch 6 + alembic 0051 6 + audit action 8 + capability matrix v1.45 8 + dry-run 6 + 5 cloud provider integration 6 = ~+62 NEW pytest PASS) 결정 wire + Phase 11~18 test pattern verbatim EXTENSION.
- **F35.8.5** vitest tests ~+7 NEW vitest PASS 결정 wire (RateCardAggregatorPanel 1 + TCOModelingSelectorPanel 1 + PricingReportGeneratorPanel 1 + ScheduledPricingDispatchConfigPanel 1 + PricingTrendMiniChart 1 + ko-KR SSOT 2 + finops pricing dashboard parity CR 12-5 D-PARITY-01 결정 wire) + Phase 11~18 vitest pattern verbatim EXTENSION.
- **F35.8.6** ruff + tsc 0 NEW + SDR drift gate 결정 wire + A36 SDR 검증 4-step 자동 적용 결정 wire + 0 NEW ruff + 0 NEW tsc + 0 regressions + SDR drift gate PASS 결정 wire.
- **F35.8.7** wire scope T1~T8 결정 wire = T1 rate_card_aggregator + tco_modeling_selector 결정 wire + T2 pricing_report_generator + 3 format generators (PDF + CSV + Excel) 결정 wire + T3 scheduled_pricing_dispatch + APScheduler 4 cadence cron 결정 wire + T4 alembic 0051 phase_19_finops_pricing 6 tables + RLS 6 tables + 2 preview tables 결정 wire + T5 audit action EXTENSION 8 NEW + 16 NEW typed exception classes 결정 wire + T6 capability v1.45 EXTENSION + frontend finops pricing dashboard 결정 wire + T7 scheduled_pricing_dispatch_job wire 결정 wire + T8 3중 게이트 FINAL CLEAN atomic commit 결정 wire + estimated ~+28-33 files estimate = ~+21 NEW + ~+12 MODIFIED 결정 wire + Phase 18 wire `67059cf` 의 ~28 files pattern verbatim EXTENSION 결정 wire + Phase 11~18 wire 의 ~25-30 files pattern verbatim EXTENSION.
- **F35.8.8** A19 cohesion pattern 9 surface EXTENSION PASS 결정 wire (FinOps Pricing, Rate Card & TCO Modeling surface NEW = F35.1~F35.8 결정 wire) + Phase 18 wire `67059cf` 의 FinOps Cloud Commitment Management surface NEW = F34.1~F34.8 pattern verbatim 미러 + Phase 17 wire `97cfe4e` 의 FinOps Sustainability & Carbon Reporting surface NEW = F33.1~F33.8 pattern verbatim 미러 + Phase 16 wire `81ae00a` 의 FinOps Reporting & Executive Dashboard surface NEW = F32.1~F32.8 pattern verbatim 미러 + Phase 15 wire `1b800d9` 의 FinOps Tag Governance surface NEW = F31.1~F31.8 pattern verbatim 미러 + Phase 14 wire `e904485` 의 FinOps Optimization surface NEW = F30.1~F30.8 pattern verbatim 미러 + Phase 13 wire `8b98030` 의 FinOps Forecasting surface NEW = F29.1~F29.8 pattern verbatim 미러 + Phase 12 wire `f3c0e63` 의 FinOps Anomaly surface NEW = F28.1~F28.8 pattern verbatim 미러 + Phase 11 wire `e020ad0` 의 FinOps Showback surface NEW = F27.1~F27.7 pattern verbatim 미러 결정 wire.
- **F35.8.9** CR lessons applied 18종 결정 wire (CR 0-2 RLS lesson ✅ APPLIED + CR 1-1 audit-first INSERT ✅ APPLIED (8 NEW audit log entries + 5-cloud-provider source attribution 2행 append = 10 NEW effective audit events) + CR 4-3/4-4 lessons carry ✅ APPLIED + CR 1-1 ContextVar lesson ✅ APPLIED + CR 1-1 RSC boundary lesson ✅ APPLIED + CR 9-6 commit message discipline ✅ APPLIED + CR 11-3 honest-DEFER discipline ✅ APPLIED (137번째 epic 연속 정직 회복) + CR 11-4 D-001~D-005 + P-015 ✅ APPLIED + CR 12-1 L4 ✅ APPLIED + CR 12-5 D-14 typed exception envelope ✅ APPLIED (16 NEW typed exception classes) + CR 12-5 D-PARITY-01 inversion ✅ APPLIED + CR 12-5 D-GATE-01 inversion ✅ APPLIED + A36 SDR 검증 4-step 자동 적용 ✅ + AD-14 stack pin ✅ APPLIED + AD-22 owner-only RBAC ✅ APPLIED + NFR4 PII minimization ✅ PRESERVED) 결정 wire.
- **F35.8.10** D-DEFER-* honestly 결정 wire (CR 11-3 137번째 epic 연속 정직 회복 결정 wire 보존) = D-1-1-DEFER-1/2/3 RESOLVED 보존 60~137번째 + D-LAUNCH-1-DEFER-1 honestly preserved 65~137번째 + D-EPIC-16-REVIEW-DEFER-2~6 ✅ RESOLVED 보존 78~137번째 + D-PHASE-4-DR-DEFER-1/2 ✅ RESOLVED 보존 73~137번째 + D-EPIC-17-WIRE-DEFER-T2-T3-UI ✅ RESOLVED 보존 83~137번째 + D-RETENTION-1 ✅ RESOLVED 보존 85~137번째 + D-OBSERVABILITY-1 ✅ RESOLVED 보존 89~137번째 + D-PERFORMANCE-1 ✅ RESOLVED 보존 93~137번째 + D-CHAOS-1 ✅ RESOLVED 보존 100~137번째 + D-SLO-1 ✅ RESOLVED 보존 101~137번째 + D-FINOPS-1 ✅ RESOLVED 보존 105~137번째 + D-FINOPS-2 ✅ RESOLVED 보존 109~137번째 + D-FINOPS-3 ✅ RESOLVED 보존 113~137번째 + D-FINOPS-4 ✅ RESOLVED 보존 117~137번째 + D-FINOPS-5 ✅ RESOLVED 보존 121~137번째 + D-FINOPS-6 ✅ RESOLVED 보존 125~137번째 + D-FINOPS-7 ✅ RESOLVED 보존 129~137번째 + D-FINOPS-8 ✅ RESOLVED 보존 133~137번째 + **D-FINOPS-9 신규 honestly DEFER 보존 = Phase 19 PRD entry 진입 시점에 carry-over chain 정직 회복 결정 wire 진입: Phase 18 close-out retro `de72f50` §13 + Phase 17 close-out retro `de009fe` §13 + Phase 16 close-out retro `26fd530` §13 + Phase 15 close-out retro `102f370` §13 + Phase 14 close-out retro `5b367d9` §13 + Phase 13 close-out retro `850b4f8` §13 + Phase 12 close-out retro `3354e83` §13 + Phase 11 close-out retro `80df15b` §12 + Phase 10 close-out retro `733d428` §10 + Phase 9 close-out retro `634427d` §10 + Phase 8 close-out retro `ab495a8` §10 + Phase 7 close-out retro `326fa9f` §10 + Phase 6 close-out retro `f9f006c` §13 + Epic 17 close-out retro `be8f3bd` §11 "FinOps Pricing, Rate Card & TCO Modeling 결정 wire 보류, Phase 19+ 진입 시점" verbatim 해소**) + Epic 1 ~ Epic 17 + Phase 3 ~ Phase 18 + 1st release cycle 정합 보존 결정 wire (cj-style 137번째 epic 연속 정직 회복 pre-flight 정합 sweep) + 결정 wire 일자: 2026-08-25 (KST) + **next**: 옵션 (a) Phase 19 bmad-create-story spec entry 진입 (cj-style 138번째 wire 진입 시점) / 옵션 (b) Phase 19 bmad-dev-story atomic wire T1~T8 진입 (cj-style 139번째) / 옵션 (c) Phase 19 close-out retro 진입 (cj-style 140번째) / 옵션 (d) Epic 20+ 진입 / 옵션 (e) D-DEFER-* follow-up 결정 wire 보류 결정 wire.

---

## F36. FinOps Multi-Cloud Cost Unified Reconciliation (Phase 20 territory 신규)

### F36.1 multi_cloud_rate_card_reconciliation_aggregator + 5 cloud provider cross-rollup EXTENSION (A539 결정)

- **F36.1.1** `apps/api/modules/finops/multi_cloud/rate_card_reconciliation_aggregator.py` NEW ~+220 LOC + `reconcile_multi_cloud_rate_cards(tenant_id, period_key, scope_chain) -> MultiCloudRateCardReconciliation` TypedDict 18 fields (rate_card_reconciliation_id + tenant_id + period_key + scope enum tenant/department/cost_center/product_line + scope_chain value_jsonb 9-module source attribution + cloud_provider enum aws_azure_gcp_naver_kt + reconciled_rate_krw_per_hour NUMERIC(18,6) + variance_rate_krw_per_hour NUMERIC(18,6) + variance_pct NUMERIC(5,2) + source_count int + primary_source enum rate_card_api/contract_file/manual/negotiation/audit + last_negotiated_at + computed_at + trace_id) + 9-module cross-join EXTENSION (Phase 11 showback + Phase 12 anomaly + Phase 13 forecast + Phase 14 optimization + Phase 15 tag_governance + Phase 16 executive + Phase 17 sustainability + Phase 18 commitment + Phase 19 pricing) + 5 cloud provider cross-rollup EXTENSION (AWS EDP + Azure EA + GCP CUD Pricing + Naver Cloud Volume Tier + KT Cloud Volume Tier) + audit-first INSERT `multi_cloud_rate_card_reconciled` CR 1-1 verbatim 결정 wire.
- **F36.1.2** 5 cloud provider rate card source priority chain 결정 wire = (1) negotiation override (AWS EDP 자동 negotiation bot + Azure EA consumption commit reconciliation + GCP CUD break-even optimization 결과 최우선) + (2) contract file (negotiated_rate_krw_per_hour from AWS EDP / Azure EA / GCP CUD Pricing / Naver Cloud Volume Tier / KT Cloud Volume Tier SaaS 계약서 PDF 파싱) + (3) rate_card_api (provider official pricing API → on_demand_rate_krw_per_hour + marketplace list_price_krw_per_hour) + (4) manual (tenant_admin이 직접 입력한 custom_rate_krw_per_hour) + (5) audit (과거 audit log 에서 recovered_rate_krw_per_hour) 결정 wire + Phase 19 wire `8db3cfc` 의 rate_card_aggregator 5-tier priority (negotiation + contract + on_demand + marketplace + audit) 패턴 verbatim EXTENSION 결정 wire.
- **F36.1.3** rate card reconciliation algorithm 결정 wire = for each (cloud_provider, service_code, region) tuple: (a) collect all available rate sources from 5-tier priority chain / (b) select primary_rate = highest-priority non-null rate source / (c) compute variance_rate = max_rate - min_rate among all sources / (d) compute variance_pct = (max_rate - min_rate) / min_rate × 100 / (e) flag if variance_pct > 5.0% (alert "rate discrepancy detected" → audit-first INSERT `multi_cloud_rate_card_reconciled`) / (f) persist to phase_20_multi_cloud_rate_card_reconciliation table 결정 wire + Phase 18 wire `67059cf` commitment_inventory_aggregator 의 source priority algorithm 패턴 verbatim EXTENSION 결정 wire.
- **F36.1.4** reconciliation scope 결정 wire = 4 scope (tenant + department + cost_center + product_line) + per-tenant override EXTENSION + per-scope reconciliation (각 scope 마다 independent rate_card_reconciliation_id 생성) + RLS 자동 적용 CR 0-2 verbatim + tenant_id = current_setting('app.tenant_id')::uuid 결정 wire + Phase 18 wire `67059cf` 의 4 scope + per-tenant override 패턴 verbatim EXTENSION.
- **F36.1.5** 5 cloud provider rate card format normalization 결정 wire = AWS EDP JSON format + Azure EA REST API format + GCP CUD Pricing BigQuery export format + Naver Cloud Volume Tier XML format + KT Cloud Volume Tier CSV format 모두 unified MultiCloudRateCardReconciliation TypedDict 으로 normalize + parsing_error 발생 시 fallback to manual entry + audit-first INSERT `multi_cloud_rate_card_reconciled` with `parsing_error=true` flag 결정 wire + Phase 19 wire `8db3cfc` 의 5 cloud provider rate_card_aggregator format normalization 패턴 verbatim EXTENSION.
- **F36.1.6** `apps/api/modules/finops/multi_cloud/__init__.py` MODIFIED 결정 wire = rate_card_reconciliation submodule export + Phase 19 wire `8db3cfc` 의 `apps/api/modules/finops/pricing/__init__.py` pattern verbatim EXTENSION + ALLOWED_SERVICE_SUBMODULES (CR 11-3 lesson) 즉시 sweep EXTENSION = m20_finops_multi_cloud 신규 submodule 등록 + Phase 11~19 verbatim EXTENSION (Phase 19 m19_finops_pricing 패턴 보존) 결정 wire.
- **F36.1.7** audit-first INSERT 결정 wire = `multi_cloud_dashboard_viewed` + `multi_cloud_rate_card_reconciled` + `multi_cloud_cost_reconciled` + `negotiation_bot_triggered` + `marketplace_saas_pricing_integrated` + `multi_cloud_dry_run_executed` + `multi_cloud_kpi_refreshed` + `blended_unblended_tracked` 8 NEW CR 1-1 verbatim 적용 결정 wire + Phase 19 wire `8db3cfc` 의 8 NEW audit actions 패턴 verbatim EXTENSION.
- **F36.1.8** 5 cloud provider rate card integration tests 결정 wire = AWS EDP mock fixture (negotiated_rate 1500 + on_demand 2500 + variance_pct 40%) + Azure EA mock fixture (consumption_commit_reconciled 1800 + on_demand 2400 + variance_pct 25%) + GCP CUD mock fixture (flexible_tier 1600 + fixed_tier 1400 + variance_pct 12.5%) + Naver Cloud mock fixture (volume_tier_3 1200 + on_demand 2000 + variance_pct 40%) + KT Cloud mock fixture (volume_tier_2 1100 + on_demand 1800 + variance_pct 38.9%) 5 NEW pytest cases 결정 wire + Phase 19 wire `8db3cfc` 의 5 cloud provider rate_card integration tests 패턴 verbatim EXTENSION.
- **F36.1.9** rate card reconciliation KPI 결정 wire = `reconciliation_match_rate_pct` (matched / total × 100, target ≥ 95%) + `reconciliation_drift_count_30d` (variance_pct > 5% last 30d count, target ≤ 5) + `reconciliation_avg_variance_pct` (avg variance_pct last 30d, target ≤ 2%) + 4 industries baseline EXTENSION + per-tenant override EXTENSION + NFR11 P95 ≤ 30s SLO 결정 wire + Phase 19 wire `8db3cfc` 의 pricing_kpi_selector 패턴 verbatim EXTENSION 결정 wire.
- **F36.1.10** reconciliation freshness tracking 결정 wire = last_reconciled_at TIMESTAMPTZ + staleness_threshold_minutes (default 60) + auto_alert if now - last_reconciled_at > staleness_threshold → Slack DM to owner + audit-first INSERT `multi_cloud_rate_card_reconciled` with staleness=true flag 결정 wire + Phase 18 wire `67059cf` 의 commitment_inventory_aggregator freshness tracking 패턴 verbatim EXTENSION.
- **F36.1.11** rate card reconciliation audit log 결정 wire = phase_20_multi_cloud_rate_card_audit 테이블 NEW 결정 wire + audit_id UUID PK + tenant_id UUID + cloud_provider enum + service_code TEXT + region TEXT + reconciled_rate_krw_per_hour NUMERIC(18,6) + source enum rate_card_api/contract_file/manual/negotiation/audit + reconciled_at TIMESTAMPTZ DEFAULT NOW() + trace_id TEXT + RLS 자동 적용 CR 0-2 verbatim + 5-year retention 결정 wire + Phase 19 wire `8db3cfc` 의 pricing_audit_log 패턴 verbatim EXTENSION + NFR8 audit log 5년 append-only 보존 결정 wire.
- **F36.1.12** AD-47 신규 결정 진입 (a)~(g) 7 sub-decisions 결정 wire + 부록 A architectural decisions table 1 NEW row EXTENSION + Phase 19 AD-46 verbatim EXTENSION 결정 wire.

### F36.2 multi_cloud_cost_reconciliation_aggregator + unified source of truth (A539 결정)

- **F36.2.1** `apps/api/modules/finops/multi_cloud/cost_reconciliation_aggregator.py` NEW ~+220 LOC + `reconcile_multi_cloud_costs(tenant_id, period_key, scope_chain) -> MultiCloudCostReconciliation` TypedDict 19 fields (cost_reconciliation_id + tenant_id + period_key + scope enum tenant/department/cost_center/product_line + scope_chain value_jsonb 9-module source attribution + cloud_provider enum aws_azure_gcp_naver_kt + service_code TEXT + region TEXT + blended_cost_krw NUMERIC(20,2) + unblended_cost_krw NUMERIC(20,2) + cost_variance_krw NUMERIC(20,2) + cost_variance_pct NUMERIC(5,2) + cost_source_count int + primary_cost_source enum billing_api/invoice_pdf/contract_estimated/manual/audit + last_reconciled_at + computed_at + trace_id + source_attribution_jsonb) + 9-module cross-join EXTENSION (Phase 11 showback + Phase 12 anomaly + Phase 13 forecast + Phase 14 optimization + Phase 15 tag_governance + Phase 16 executive + Phase 17 sustainability + Phase 18 commitment + Phase 19 pricing) + 5 cloud provider cost cross-rollup EXTENSION (AWS Cost Explorer + Azure Cost Management + GCP Billing + Naver Cloud Billing + KT Cloud Billing) + audit-first INSERT `multi_cloud_cost_reconciled` CR 1-1 verbatim 결정 wire.
- **F36.2.2** multi-cloud unified source of truth 패턴 결정 wire = 단일 unified MultiCloudCostReconciliation TypedDict 이 5 cloud provider 의 cost 를 normalize → (1) 모든 dashboard (executive + finops + cost-center) 가 이 unified view 만 참조 / (2) 모든 KPI selector 가 이 unified view 에서 join / (3) 모든 report generator 가 이 unified view 에서 export / (4) 모든 reconciliation step 이 이 unified view 에서 consume / (5) 모든 drill-down (tenant → department → cost_center → product_line) 이 이 unified view 에서 4-scope cross-join EXTENSION 결정 wire + Phase 16 wire `81ae00a` 의 executive_rollup unified source of truth 패턴 verbatim EXTENSION.
- **F36.2.3** 5 cloud provider cost source priority chain 결정 wire = (1) billing_api (provider official cost API → AWS Cost Explorer GetCostAndUsage + Azure Cost Management Query + GCP Billing BigQuery export + Naver Cloud Billing REST API + KT Cloud Billing REST API, 가장 신뢰도 높음) + (2) invoice_pdf (provider monthly invoice PDF parse → textract OCR for AWS / Azure / GCP / Naver Cloud / KT Cloud invoice, 중간 신뢰도) + (3) contract_estimated (Phase 18 commitment_inventory + Phase 19 pricing negotiated_rate 기반으로 estimated cost 계산, Phase 19 wire `8db3cfc` 의 TCO modeling 결과 활용) + (4) manual (tenant_admin이 직접 입력한 custom_cost_krw) + (5) audit (과거 audit log 에서 recovered_cost_krw) 결정 wire + Phase 18 wire `67059cf` 의 commitment_kpi_priority_chain 패턴 verbatim EXTENSION.
- **F36.2.4** cost reconciliation algorithm 결정 wire = for each (cloud_provider, service_code, region, period_key) tuple: (a) collect all available cost sources from 5-tier priority chain / (b) select primary_cost = highest-priority non-null cost source / (c) compute cost_variance = max_cost - min_cost among all sources / (d) compute cost_variance_pct = (max_cost - min_cost) / min_cost × 100 / (e) flag if cost_variance_pct > 3.0% (alert "cost reconciliation drift detected" → audit-first INSERT `multi_cloud_cost_reconciled`) / (f) persist to phase_20_multi_cloud_cost_reconciliation table 결정 wire + Phase 19 wire `8db3cfc` 의 rate_card_reconciliation algorithm 패턴 verbatim EXTENSION.
- **F36.2.5** cross-tenant + cross-period cost continuity 결정 wire = current_period_cost + previous_period_cost + cost_growth_pct ((current - previous) / previous × 100) + 4 industries baseline EXTENSION (manufacturing ≤ 15% / service ≤ 25% / manufacturing_service ≤ 18% / saas ≤ 35%) + per-tenant override EXTENSION + cost_growth_pct > baseline + 10% → alert "cost spike detected" 결정 wire + Phase 12 wire `f3c0e63` anomaly detection baseline EXTENSION + Phase 11 wire `e020ad0` showback period selector 패턴 verbatim EXTENSION 결정 wire.
- **F36.2.6** 5 cloud provider cost reconciliation drill-down UI 결정 wire = (1) Cloud Cost Drill-Down Panel (5 cloud provider 별 cost breakdown chart) + (2) Service Cost Heatmap (service_code × region 별 cost heatmap) + (3) Cost Variance Tracker (variance_pct 추이 line chart) + (4) Cost Source Inspector (각 cost 의 primary_source + secondary_source + tertiary_source 분해 view) + (5) Cost Reconciliation Alert Feed (cost_variance_pct > 3% 알림 feed) 5 NEW sub-components 결정 wire + Phase 19 wire `8db3cfc` 의 pricing dashboard 5 sub-components 패턴 verbatim EXTENSION 결정 wire.
- **F36.2.7** cost reconciliation cross-tenant invalidation 결정 wire = LISTEN/NOTIFY channel `phase_20_multi_cloud_cost_reconciled` 신규 + LISTEN trigger after INSERT on phase_20_multi_cloud_cost_reconciliation table → broadcast to all tenant workers → all RSC page revalidate (Phase 13 wire `8b98030` + Phase 14 wire `e904485` 의 LISTEN/NOTIFY consume trigger EXTENSION 패턴 verbatim) 결정 wire + CR 0-2 RLS + CR 1-1 audit-first INSERT verbatim EXTENSION.
- **F36.2.8** cost reconciliation KPI 결정 wire = `total_multi_cloud_cost_krw` (Σ(blended_cost) across 5 cloud provider, target = sum of 5 individual cloud costs) + `cost_variance_total_krw` (Σ(cost_variance), target ≤ 1% of total) + `cost_variance_avg_pct` (avg cost_variance_pct, target ≤ 1.5%) + `reconciliation_freshness_minutes` (now - last_reconciled_at, target ≤ 60) + `cost_source_coverage_pct` (covered / total × 100, target ≥ 98%) + 4 industries baseline EXTENSION + per-tenant override EXTENSION + audit-first INSERT `multi_cloud_kpi_refreshed` CR 1-1 verbatim 적용 결정 wire + Phase 19 wire `8db3cfc` 의 pricing_kpi_selector 패턴 verbatim EXTENSION.
- **F36.2.9** cost reconciliation RLS + cross-tenant safety 결정 wire = RLS 자동 적용 CR 0-2 verbatim + tenant_id = current_setting('app.tenant_id')::uuid + cross-tenant query 시 tenant_id mismatch 자동 reject + Epic 12 2FA 챌린지 보존 + audit-first INSERT `multi_cloud_cost_reconciled` with `cross_tenant_attempt=true` flag if attempted 결정 wire + Phase 18 wire `67059cf` 의 commitment_inventory_aggregator RLS 패턴 verbatim EXTENSION.
- **F36.2.10** cost reconciliation forecast EXTENSION 결정 wire = Phase 13 wire `8b98030` forecast + Phase 19 wire `8db3cfc` pricing + Phase 20 multi_cloud 통합 → next_period_cost_forecast_krw = current_period_cost × (1 + cost_growth_pct) + forecast_deviation_pct = abs(forecast - actual) / actual × 100 + alert if forecast_deviation_pct > 20% + 4 industries baseline (manufacturing ≤ 10% / service ≤ 15% / manufacturing_service ≤ 12% / saas ≤ 18%) + per-tenant override EXTENSION 결정 wire + Phase 13 wire `8b98030` 의 forecast_deviation_pct 패턴 verbatim EXTENSION.
- **F36.2.11** cost reconciliation competitive benchmark 결정 wire = 5 cloud provider 별 cost_per_unit_krw benchmark + (tenant cost - benchmark) / benchmark × 100 = cost_vs_benchmark_pct + 4 industries baseline EXTENSION + per-tenant override EXTENSION + cost_vs_benchmark_pct > 10% → alert "above benchmark" + cost_vs_benchmark_pct < -10% → alert "below benchmark (verify accuracy)" 결정 wire + Phase 19 wire `8db3cfc` 의 tco_benchmark 패턴 verbatim EXTENSION 결정 wire.
- **F36.2.12** AD-47 신규 결정 진입 (a)~(g) 7 sub-decisions 결정 wire + 부록 A architectural decisions table 1 NEW row EXTENSION + Phase 19 AD-46 verbatim EXTENSION 결정 wire.

### F36.3 negotiation_bot (AWS EDP + Azure EA + GCP CUD break-even) (A540 결정)

- **F36.3.1** `apps/api/modules/finops/multi_cloud/negotiation_bot.py` NEW ~+250 LOC + `run_negotiation_bot(tenant_id, cloud_provider, scope) -> NegotiationRecommendation` TypedDict 16 fields (negotiation_recommendation_id + tenant_id + cloud_provider enum aws_azure_gcp + scope enum tenant/department/cost_center/product_line + current_rate_krw_per_hour NUMERIC(18,6) + recommended_rate_krw_per_hour NUMERIC(18,6) + recommended_commitment_term enum 1_year/3_year + estimated_savings_pct NUMERIC(5,2) + estimated_savings_krw_per_year NUMERIC(20,2) + payback_period_months int + break_even_utilization_pct NUMERIC(5,2) + confidence_score NUMERIC(5,2) + risk_score enum low/medium/high + negotiation_strategy enum aggressive/moderate/conservative + auto_negotiate_enabled boolean + computed_at + trace_id) + 3 cloud provider support 결정 wire (AWS EDP 자동 negotiation + Azure EA consumption commit reconciliation + GCP CUD flexible/fixed tier break-even optimization) + audit-first INSERT `negotiation_bot_triggered` CR 1-1 verbatim 결정 wire.
- **F36.3.2** AWS EDP 자동 negotiation bot 결정 wire = (1) analyze AWS Cost Explorer last 12 months usage pattern (P95 utilization across all EC2 / RDS / ElastiCache / Redshift / DynamoDB) / (2) recommend EDP commit tier (1-year 5% discount + 3-year 12% discount + 5-year 18% discount) based on P95 utilization stability / (3) generate auto-negotiation email template (AWS account manager contact + EDP tier recommendation + projected savings + risk assessment) / (4) track negotiation outcome (accepted / rejected / counter-offered / no-response) / (5) persist to phase_20_negotiation_outcome table + audit-first INSERT `negotiation_bot_triggered` 결정 wire + Phase 14 wire `e904485` 의 commitment_recommender 1y/3y break-even 패턴 verbatim EXTENSION + Phase 18 wire `67059cf` 의 commitment_inventory_aggregator 패턴 verbatim EXTENSION.
- **F36.3.3** Azure EA consumption commit reconciliation bot 결정 wire = (1) analyze Azure Consumption API last 12 months usage (compute hours + storage GB + bandwidth GB + DB DTUs) / (2) compute committed spend vs actual spend variance = consumption_commitment_variance_pct / (3) recommend EA tier adjustment (over-commit: actual > 110% of commit → recommend tier increase / under-commit: actual < 70% of commit → recommend tier decrease / optimal: 70~110% → recommend tier hold) / (4) generate Azure EA tier adjustment recommendation report / (5) auto-trigger EA enrollment renegotiation webhook (if configured) / (6) persist to phase_20_ea_recommendation table + audit-first INSERT `negotiation_bot_triggered` 결정 wire + Phase 18 wire `67059cf` 의 commitment_inventory_aggregator + Phase 19 wire `8db3cfc` 의 pricing TCO modeling 패턴 verbatim EXTENSION.
- **F36.3.4** GCP CUD flexible/fixed tier break-even optimization bot 결정 wire = (1) analyze GCP BigQuery billing export last 12 months resource usage (vCPU hours + memory GB-hours + storage GB) / (2) compute flexible tier vs fixed tier cost comparison for 1-year and 3-year horizons / (3) determine break-even utilization_pct per tier (flexible tier break-even ~50%, fixed tier break-even ~70%) / (4) recommend CUD tier (flexible_1y if P95 utilization < 50% + flexible_3y if 50~70% + fixed_3y if > 70%) / (5) compute projected savings (recommended tier cost - on_demand cost) / (6) persist to phase_20_cud_recommendation table + audit-first INSERT `negotiation_bot_triggered` 결정 wire + Phase 14 wire `e904485` 의 commitment_recommender 1y/3y break-even 패턴 verbatim EXTENSION + Phase 18 wire `67059cf` 의 commitment_inventory_aggregator 패턴 verbatim EXTENSION.
- **F36.3.5** negotiation bot guard 결정 wire = `MINIMUM_SAVINGS_PCT = 5.0` (5% 미만 skip recommendation) + `MINIMUM_SAVINGS_KRW = 1_000_000` (연 100만원 미만 skip recommendation) + `MAX_NEGOTIATIONS_PER_MONTH = 3` (월 3회 초과 skip) + `MAX_AUTO_TRIGGER_PER_DAY = 1` (일 1회 자동 trigger 한정) + audit-first INSERT `negotiation_bot_triggered` with `skipped=true` flag if guard violated 결정 wire + Phase 14 wire `e904485` 의 commitment_recommender guard 패턴 verbatim EXTENSION.
- **F36.3.6** Naver/KT public pricing API stability 검증 결정 wire = (1) P1 priority : Naver Cloud / KT Cloud pricing API 의 uptime / response_time / data_freshness 모니터링 (uptime ≥ 99.0% target / P95 response_time ≤ 2s / data_freshness ≤ 24h target) / (2) P2 priority : Naver/KT public pricing API 가 downtime 발생 시 fallback to manual rate entry + Slack DM to owner alert + audit-first INSERT `multi_cloud_rate_card_reconciled` with `api_unavailable=true` flag / (3) P2 priority : Naver/KT pricing API 가 rate_limited 발생 시 exponential backoff (60s → 120s → 240s) + audit-first INSERT `multi_cloud_rate_card_reconciled` with `rate_limited=true` flag 결정 wire + Phase 19 wire `8db3cfc` 의 pricing_reconciliation freshness tracking 패턴 verbatim EXTENSION.
- **F36.3.7** negotiation bot confidence + risk scoring 결정 wire = (1) confidence_score 계산 = utilization_stability_score × 0.6 + historical_accuracy_score × 0.4 (range 0~100) / (2) risk_score 계산 = savings_pct × 0.4 + commitment_term × 0.3 + commitment_flexibility × 0.3 (low / medium / high) / (3) confidence_score < 60 → recommendation_status = `low_confidence` + skip auto-trigger / (4) confidence_score ≥ 80 + risk_score = low → recommendation_status = `auto_negotiate_ready` + auto-trigger enabled / (5) intermediate → recommendation_status = `manual_review_required` 결정 wire + Phase 14 wire `e904485` 의 commitment_recommender confidence/risk scoring 패턴 verbatim EXTENSION.
- **F36.3.8** negotiation bot integration tests 결정 wire = (1) AWS EDP auto-negotiation bot 8 NEW pytest cases (EC2 P95 utilization analysis + RDS utilization + recommendation tier + email template generation + outcome tracking) / (2) Azure EA consumption commit reconciliation bot 8 NEW pytest cases (consumption variance analysis + tier adjustment recommendation + EA enrollment webhook trigger) / (3) GCP CUD flexible/fixed tier break-even optimization bot 8 NEW pytest cases (BigQuery export analysis + tier break-even calculation + projected savings) / (4) Naver/KT public pricing API stability 검증 6 NEW pytest cases (uptime monitoring + P95 response_time + rate_limited fallback + data_freshness) = ~30 NEW pytest cases 결정 wire + Phase 14 wire `e904485` 의 commitment_recommender tests 패턴 verbatim EXTENSION.
- **F36.3.9** negotiation bot audit log 결정 wire = phase_20_negotiation_bot_audit 테이블 NEW 결정 wire + audit_id UUID PK + tenant_id UUID + cloud_provider enum + recommendation_id UUID + action enum bot_triggered/recommendation_generated/recommendation_accepted/recommendation_rejected/auto_negotiate_completed + audit_metadata JSONB + created_at TIMESTAMPTZ DEFAULT NOW() + trace_id TEXT + RLS 자동 적용 CR 0-2 verbatim + 5-year retention 결정 wire + Phase 19 wire `8db3cfc` 의 pricing_audit_log 패턴 verbatim EXTENSION + NFR8 audit log 5년 append-only 보존 결정 wire.
- **F36.3.10** negotiation bot owner approval flow 결정 wire = (1) bot generates recommendation → recommendation_status = `manual_review_required` / (2) bot sends Slack DM to tenant_owner with recommendation summary / (3) tenant_owner approves via Slack button or admin UI → recommendation_status = `approved` / (4) bot triggers actual negotiation (email send / webhook / etc) / (5) audit-first INSERT `negotiation_bot_triggered` with approval_chain JSONB 결정 wire + AD-22 owner-only RBAC EXTENSION + Epic 12 2FA 챌린지 보존 + Phase 14 wire `e904485` 의 commitment_recommender approval flow 패턴 verbatim EXTENSION 결정 wire.
- **F36.3.11** negotiation bot idempotency 결정 wire = idempotency_key = (tenant_id, cloud_provider, scope, recommendation_period_key) + duplicate trigger 시 동일 idempotency_key 발견 → skip + audit-first INSERT `negotiation_bot_triggered` with `duplicate_skipped=true` flag 결정 wire + Phase 19 wire `8db3cfc` 의 pricing_scheduled_dispatch_idempotency 패턴 verbatim EXTENSION 결정 wire.
- **F36.3.12** AD-47 신규 결정 진입 (a)~(g) 7 sub-decisions 결정 wire + 부록 A architectural decisions table 1 NEW row EXTENSION + Phase 19 AD-46 verbatim EXTENSION 결정 wire.

### F36.4 blended_unblended_tracker + Naver/KT public pricing API stability (A541 결정)

- **F36.4.1** `apps/api/modules/finops/multi_cloud/blended_unblended_tracker.py` NEW ~+180 LOC + `track_blended_unblended_diff(tenant_id, period_key, cloud_provider) -> BlendedUnblendedDiff` TypedDict 14 fields (diff_id + tenant_id + period_key + cloud_provider enum aws_azure_gcp + scope enum tenant/department/cost_center/product_line + blended_rate_krw_per_hour NUMERIC(18,6) + unblended_rate_krw_per_hour NUMERIC(18,6) + rate_diff_krw_per_hour NUMERIC(18,6) + rate_diff_pct NUMERIC(5,2) + service_count int + resource_count int + tracking_status enum real_time/near_real_time/drift_detected/manual / + last_tracked_at + trace_id) + AWS + Azure + GCP 3 cloud provider support + audit-first INSERT `blended_unblended_tracked` CR 1-1 verbatim 결정 wire.
- **F36.4.2** blended vs unblended 실시간 차이 추적 결정 wire = (1) AWS Cost Explorer GetCostAndUsage API → blended_cost (전체 average) + unblended_cost (per-resource) + rate_diff 계산 / (2) Azure Cost Management Query API → amortized_cost (RI/SP amortized) + actual_cost (on-demand equivalent) + rate_diff 계산 / (3) GCP BigQuery billing export → blended_cost (committed use discount 적용) + unblended_cost (list price) + rate_diff 계산 / (4) 매 1시간 마다 cron refresh (real_time) + 실시간 drift 감지 (rate_diff_pct > 5% → alert) / (5) persist to phase_20_blended_unblended_diff table 결정 wire + Phase 19 wire `8db3cfc` 의 pricing_freshness tracking 패턴 verbatim EXTENSION 결정 wire.
- **F36.4.3** blended_unblended_tracker KPI 결정 wire = `blended_unblended_diff_pct` (rate_diff / unblended × 100, target ≤ 5%) + `blended_unblended_drift_count_30d` (drift > 5% last 30d count, target ≤ 3) + `blended_unblended_avg_diff_krw_per_hour` (avg rate_diff, target ≤ 50 KRW) + 4 industries baseline EXTENSION (manufacturing ≤ 100 + service ≤ 50 + manufacturing_service ≤ 80 + saas ≤ 30 KRW/hour) + per-tenant override EXTENSION 결정 wire + Phase 19 wire `8db3cfc` 의 pricing_kpi_selector 패턴 verbatim EXTENSION.
- **F36.4.4** Naver/KT public pricing API stability 검증 결정 wire = (1) Naver Cloud pricing API endpoint health check (`https://www.ncloud.com/api/v1/pricing` P95 ≤ 2s) + 24-hour data freshness check + uptime monitoring (≥ 99.0% target) / (2) KT Cloud pricing API endpoint health check (`https://cloud.kt.com/api/pricing/v1` P95 ≤ 2s) + 24-hour data freshness check + uptime monitoring / (3) API downtime 발생 시 fallback to manual rate entry (admin UI) + Slack DM to owner + audit-first INSERT `multi_cloud_rate_card_reconciled` with `api_unavailable=true` flag / (4) rate_limited 발생 시 exponential backoff (60s → 120s → 240s) + audit-first INSERT with `rate_limited=true` flag / (5) status monitoring dashboard (uptime timeline + P95 response time chart + data freshness gauge) 결정 wire + Phase 19 wire `8db3cfc` 의 pricing_freshness tracking 패턴 verbatim EXTENSION 결정 wire.
- **F36.4.5** Naver/KT pricing API data accuracy 검증 결정 wire = (1) Naver Cloud pricing API 의 volume tier 별 rate 가 manual entry 와 일치하는지 4-week rolling sample validation (target ≥ 95% match) / (2) KT Cloud pricing API 의 volume tier 별 rate 가 manual entry 와 일치하는지 4-week rolling sample validation / (3) accuracy < 95% → alert "Naver/KT pricing API accuracy degraded" → manual override recommended → audit-first INSERT `multi_cloud_rate_card_reconciled` with `accuracy_degraded=true` flag / (4) accuracy ≥ 98% → status = `verified_realtime` / (5) accuracy 95~98% → status = `verified_near_realtime` / (6) accuracy < 95% → status = `drift_detected` 결정 wire + Phase 19 wire `8db3cfc` 의 pricing_accuracy_degradation_error 패턴 verbatim EXTENSION 결정 wire.
- **F36.4.6** Naver/KT pricing API 4-tier rate card format 결정 wire = (1) tier_1 (default rate, 0~100 instances) + (2) tier_2 (volume discount 5%, 100~500 instances) + (3) tier_3 (volume discount 10%, 500~1000 instances) + (4) tier_4 (custom contract rate, 1000+ instances) 결정 wire + Phase 19 wire `8db3cfc` 의 volume_tier enum 패턴 verbatim EXTENSION 결정 wire.
- **F36.4.7** blended_unblended_tracker + Naver/KT stability audit log 결정 wire = phase_20_blended_unblended_audit 테이블 NEW + audit_id UUID PK + tenant_id UUID + cloud_provider enum + period_key TEXT + blended_rate_krw_per_hour NUMERIC(18,6) + unblended_rate_krw_per_hour NUMERIC(18,6) + drift_pct NUMERIC(5,2) + api_uptime_pct NUMERIC(5,2) + tracked_at TIMESTAMPTZ DEFAULT NOW() + trace_id TEXT + RLS 자동 적용 CR 0-2 verbatim + 5-year retention 결정 wire + Phase 19 wire `8db3cfc` 의 pricing_audit_log 패턴 verbatim EXTENSION + NFR8 audit log 5년 append-only 보존 결정 wire.
- **F36.4.8** blended_unblended_tracker + Naver/KT stability tests 결정 wire = (1) blended_unblended_tracker 6 NEW pytest cases (AWS + Azure + GCP 3 cloud provider + drift detection + KPI calculation + freshness tracking) / (2) Naver/KT public pricing API stability 검증 6 NEW pytest cases (uptime monitoring + P95 response_time + rate_limited fallback + data_freshness + accuracy degradation detection + 4-tier rate card format) = ~12 NEW pytest cases 결정 wire + Phase 19 wire `8db3cfc` 의 pricing_freshness tests 패턴 verbatim EXTENSION 결정 wire.
- **F36.4.9** real-time tracking strategy 결정 wire = Phase 20 wire 진입 시점에 initial strategy = `near_real_time` (1-hour cron refresh, target ≤ 5min latency) + future enhancement = `real_time` (5-min cron refresh via LISTEN/NOTIFY consume trigger) 결정 wire + Phase 19 wire `8db3cfc` 의 pricing_freshness tracking strategy 패턴 verbatim EXTENSION 결정 wire.
- **F36.4.10** tracking threshold + alert routing 결정 wire = rate_diff_pct > 5% → Slack DM to tenant_owner (severity warning) + email to tenant_owner (severity info) / rate_diff_pct > 10% → Slack DM + Slack channel alert (severity critical) + PagerDuty integration (severity critical) / rate_diff_pct > 20% → Slack DM + Slack channel alert + PagerDuty + audit-first INSERT `multi_cloud_dashboard_viewed` with `cost_spike_alert=true` flag 결정 wire + Phase 12 wire `f3c0e63` anomaly alert routing 패턴 verbatim EXTENSION 결정 wire.
- **F36.4.11** blended_unblended_tracker LISTEN/NOTIFY integration 결정 wire = LISTEN/NOTIFY channel `phase_20_blended_unblended_diff` 신규 + LISTEN trigger after INSERT on phase_20_blended_unblended_diff table → broadcast to all tenant workers → all RSC page revalidate 결정 wire + CR 0-2 RLS + CR 1-1 audit-first INSERT verbatim EXTENSION + Phase 13 wire `8b98030` LISTEN/NOTIFY 패턴 verbatim EXTENSION 결정 wire.
- **F36.4.12** AD-47 신규 결정 진입 (a)~(g) 7 sub-decisions 결정 wire + 부록 A architectural decisions table 1 NEW row EXTENSION + Phase 19 AD-46 verbatim EXTENSION 결정 wire.

### F36.5 marketplace_saas_pricing_integrator (A542 결정)

- **F36.5.1** `apps/api/modules/finops/multi_cloud/marketplace_saas_pricing_integrator.py` NEW ~+200 LOC + `integrate_marketplace_saas_pricing(tenant_id, period_key, marketplace_source) -> MarketplaceSaaSPricingRollup` TypedDict 16 fields (marketplace_pricing_id + tenant_id + period_key + marketplace_source enum aws_marketplace/azure_marketplace/gcp_marketplace/naver_marketplace/kt_marketplace + vendor_name TEXT + product_name TEXT + sku TEXT + list_price_krw_per_unit NUMERIC(18,6) + negotiated_price_krw_per_unit NUMERIC(18,6) + effective_price_krw_per_unit NUMERIC(18,6) + unit enum per_user/per_transaction/per_request/per_gb/per_hour + saas_category enum crm/erp/devops/security/analytics/other + pricing_model enum subscription/per_use/metered + integration_status enum active/paused/expired/manual + last_synced_at + trace_id) + 5 marketplace source support (AWS Marketplace + Azure Marketplace + GCP Marketplace + Naver Marketplace + KT Marketplace) + audit-first INSERT `marketplace_saas_pricing_integrated` CR 1-1 verbatim 결정 wire.
- **F36.5.2** marketplace SaaS pricing 파편화 통합 패턴 결정 wire = (1) 모든 marketplace SaaS pricing 의 unified source = MarketplaceSaaSPricingRollup TypedDict (단일 schema) / (2) 5 marketplace source 별 adapter pattern (각 marketplace 별 parsing logic 격리) / (3) unified pricing view 1 NEW decision = 모든 pricing dashboard 가 이 unified view 참조 / (4) unified TCO calculation (negotiated_price + subscription_period → total_cost) / (5) unified recommendation (cheapest alternative suggestion across 5 marketplace) 결정 wire + Phase 19 wire `8db3cfc` 의 pricing_aggregator 5 cloud provider cross-rollup 패턴 verbatim EXTENSION 결정 wire.
- **F36.5.3** 5 marketplace adapter pattern 결정 wire = (1) AWS Marketplace adapter (https://aws.amazon.com/marketplace/pricing/ REST API + product_code + offer_id) / (2) Azure Marketplace adapter (https://marketplace.azure.com/api/pricing/ REST API + offer_id + plan_id) / (3) GCP Marketplace adapter (https://console.cloud.google.com/marketplace/api/pricing REST API + asset_name + service_id) / (4) Naver Marketplace adapter (https://www.ncloud.com/marketplace/api/pricing REST API + product_id) / (5) KT Marketplace adapter (https://cloud.kt.com/marketplace/api/pricing REST API + product_id) → 모두 unified MarketplaceSaaSPricingRollup TypedDict 으로 normalize 결정 wire + Phase 19 wire `8db3cfc` 의 5 cloud provider rate_card_aggregator format normalization 패턴 verbatim EXTENSION.
- **F36.5.4** marketplace pricing freshness tracking 결정 wire = (1) last_synced_at TIMESTAMPTZ + staleness_threshold_hours (default 24) / (2) now - last_synced_at > staleness_threshold → alert "marketplace pricing stale" + audit-first INSERT `marketplace_saas_pricing_integrated` with `stale=true` flag / (3) auto-sync trigger: LISTEN/NOTIFY channel `phase_20_marketplace_pricing_refreshed` + 4-hour cron refresh + on-demand refresh via admin UI 결정 wire + Phase 19 wire `8db3cfc` 의 pricing_freshness tracking 패턴 verbatim EXTENSION 결정 wire.
- **F36.5.5** marketplace pricing alternative suggestion 결정 wire = (1) 동일한 SaaS category 내에서 cheapest 3개 alternative 추천 / (2) alternative 의 effective_price + subscription_period + integration_effort 종합 비교 / (3) recommendation_status = `recommended` if savings_pct > 10% / (4) recommendation_status = `manual_review` if savings_pct 5~10% / (5) recommendation_status = `skip` if savings_pct < 5% 결정 wire + Phase 14 wire `e904485` 의 commitment_recommender guard 패턴 verbatim EXTENSION 결정 wire.
- **F36.5.6** marketplace pricing KPI 결정 wire = `total_marketplace_spend_krw` (Σ(effective_price × usage_count) across all marketplace SaaS, target = sum of 5 individual marketplace spends) + `marketplace_pricing_freshness_hours` (now - last_synced_at, target ≤ 24) + `marketplace_pricing_coverage_pct` (covered / total × 100, target ≥ 90%) + `marketplace_alternative_savings_krw_per_year` (Σ(alternative_recommended_cost - current_cost), target ≥ 1M KRW) + 4 industries baseline EXTENSION + per-tenant override EXTENSION + audit-first INSERT `multi_cloud_kpi_refreshed` CR 1-1 verbatim 적용 결정 wire + Phase 19 wire `8db3cfc` 의 pricing_kpi_selector 패턴 verbatim EXTENSION.
- **F36.5.7** marketplace pricing audit log 결정 wire = phase_20_marketplace_pricing_audit 테이블 NEW + audit_id UUID PK + tenant_id UUID + marketplace_source enum + vendor_name TEXT + product_name TEXT + effective_price_krw_per_unit NUMERIC(18,6) + alternative_recommended TEXT + integration_status enum + audited_at TIMESTAMPTZ DEFAULT NOW() + trace_id TEXT + RLS 자동 적용 CR 0-2 verbatim + 5-year retention 결정 wire + Phase 19 wire `8db3cfc` 의 pricing_audit_log 패턴 verbatim EXTENSION + NFR8 audit log 5년 append-only 보존 결정 wire.
- **F36.5.8** marketplace pricing tests 결정 wire = (1) 5 marketplace adapter 8 NEW pytest cases (AWS + Azure + GCP + Naver + KT parsing logic) / (2) marketplace pricing freshness tracking 4 NEW pytest cases (staleness detection + auto-sync trigger + alert routing) / (3) marketplace alternative suggestion 4 NEW pytest cases (cheapest alternative + recommendation_status guard + savings calculation) = ~16 NEW pytest cases 결정 wire + Phase 19 wire `8db3cfc` 의 pricing tests 패턴 verbatim EXTENSION 결정 wire.
- **F36.5.9** marketplace pricing RLS + cross-tenant safety 결정 wire = RLS 자동 적용 CR 0-2 verbatim + tenant_id = current_setting('app.tenant_id')::uuid + cross-tenant query 시 tenant_id mismatch 자동 reject + Epic 12 2FA 챌린지 보존 + audit-first INSERT `marketplace_saas_pricing_integrated` with `cross_tenant_attempt=true` flag if attempted 결정 wire + Phase 18 wire `67059cf` 의 commitment_inventory_aggregator RLS 패턴 verbatim EXTENSION.
- **F36.5.10** marketplace pricing LISTEN/NOTIFY integration 결정 wire = LISTEN/NOTIFY channel `phase_20_marketplace_pricing_refreshed` 신규 + LISTEN trigger after INSERT on phase_20_marketplace_saas_pricing table → broadcast to all tenant workers → all RSC page revalidate + 4-hour cron refresh 자동 trigger 결정 wire + CR 0-2 RLS + CR 1-1 audit-first INSERT verbatim EXTENSION + Phase 13 wire `8b98030` LISTEN/NOTIFY 패턴 verbatim EXTENSION 결정 wire.
- **F36.5.11** marketplace pricing tenant override + on-demand refresh 결정 wire = (1) per-tenant marketplace subscription override (tenant_admin can disable specific marketplace SaaS subscription) / (2) on-demand refresh via admin UI (admin can trigger immediate sync for specific marketplace source) / (3) refresh progress indicator (admin can monitor sync status) / (4) audit-first INSERT `marketplace_saas_pricing_integrated` with `manual_refresh=true` flag if on-demand triggered 결정 wire + Phase 19 wire `8db3cfc` 의 pricing_tenant_override 패턴 verbatim EXTENSION 결정 wire.
- **F36.5.12** AD-47 신규 결정 진입 (a)~(g) 7 sub-decisions 결정 wire + 부록 A architectural decisions table 1 NEW row EXTENSION + Phase 19 AD-46 verbatim EXTENSION 결정 wire.

### F36.6 multi_cloud dashboard UI + 5 sub-components (A542 결정)

- **F36.6.1** `apps/web/app/[locale]/(dashboard)/admin/finops/multi-cloud/page.tsx` NEW ~+220 LOC + 5 sub-components 결정 wire = (1) MultiCloudRateCardReconciliationPanel (5 cloud provider rate card reconciliation view + variance tracking + source priority chain visualization) + (2) MultiCloudCostReconciliationPanel (5 cloud provider unified cost view + cost_variance tracker + cross-tenant drill-down) + (3) NegotiationBotConfigPanel (AWS EDP + Azure EA + GCP CUD bot configuration + auto-trigger settings + approval workflow) + (4) BlendedUnblendedTrackerPanel + NaverKTStabilityPanel (real-time blended/unblended diff tracker + Naver/KT API stability monitoring) + (5) MarketplaceSaaSPricingPanel (5 marketplace source pricing view + alternative suggestion + freshness tracking) 결정 wire + Phase 19 wire `8db3cfc` 의 pricing dashboard 5 sub-components 패턴 verbatim EXTENSION 결정 wire.
- **F36.6.2** `apps/web/components/finops/FinopsMultiCloudDashboardPanel.tsx` NEW Client component ~+250 LOC + 5-tab layout (Rate Cards / Costs / Negotiation / Tracking / Marketplace) + 5-tab 별 Recharts visualization (AreaChart for variance trend + BarChart for 5 cloud provider cost breakdown + LineChart for blended/unblended diff + HeatMap for marketplace pricing + PieChart for source priority chain) 결정 wire + Phase 19 wire `8db3cfc` 의 FinopsPricingDashboardPanel.tsx 패턴 verbatim EXTENSION 결정 wire.
- **F36.6.3** `apps/web/app/[locale]/(dashboard)/admin/finops/multi-cloud/layout.tsx` NEW ~+100 LOC + owner-only RBAC AD-22 verbatim + Epic 12 2FA 챌린지 mandatory + ko-KR.json `finops_multi_cloud.*` namespace EXTENSION ~30 keys (CR 11-4 D-002 verbatim SSOT) + ARIA labels WCAG 2.1 AA + `(dashboard)` route group 보호 + 미허용 tenant 의 multi_cloud dashboard 진입 차단 결정 wire + Phase 19 wire `8db3cfc` 의 pricing dashboard layout 패턴 verbatim EXTENSION 결정 wire.
- **F36.6.4** `apps/web/lib/finops/multi-cloud-types.ts` NEW TypeScript mirror 결정 wire + `MultiCloudRateCardReconciliation` + `MultiCloudCostReconciliation` + `NegotiationRecommendation` + `BlendedUnblendedDiff` + `MarketplaceSaaSPricingRollup` 5 NEW TypeScript interfaces 결정 wire + CR 12-5 D-PARITY-01 inversion TypeScript mirror 결정 wire + Phase 19 wire `8db3cfc` 의 `pricing-types.ts` pattern verbatim EXTENSION.
- **F36.6.5** `apps/web/lib/finops/multi-cloud-client.ts` NEW TypeScript client 결정 wire + `fetchRateCardReconciliations()` + `fetchCostReconciliations()` + `triggerNegotiationBot()` + `trackBlendedUnblended()` + `integrateMarketplacePricing()` 5 NEW methods EXTENSION 결정 wire + Phase 19 wire `8db3cfc` 의 `pricing-client.ts` pattern verbatim EXTENSION.
- **F36.6.6** `apps/web/messages/ko-KR.json` MODIFIED EXTENSION ~30 keys 결정 wire + `finops_multi_cloud.dashboard.title` = "멀티클라우드 통합 정합" + `finops_multi_cloud.rate_card.title` = "5종 클라우드 요율카드 정합" + `finops_multi_cloud.cost.title` = "5종 클라우드 비용 정합" + `finops_multi_cloud.negotiation.title` = "자동 협상 봇" + `finops_multi_cloud.tracking.title` = "블렌디드/언블렌디드 추적" + `finops_multi_cloud.marketplace.title` = "마켓플레이스 SaaS 가격 통합" + ~30 keys EXTENSION + NFR18 ko-KR SSOT 보존 결정 wire + CR 11-4 D-002 verbatim EXTENSION.
- **F36.6.7** multi_cloud dashboard vitest tests 결정 wire = (1) MultiCloudRateCardReconciliationPanel 1 + (2) MultiCloudCostReconciliationPanel 1 + (3) NegotiationBotConfigPanel 1 + (4) BlendedUnblendedTrackerPanel 1 + (5) MarketplaceSaaSPricingPanel 1 + (6) ko-KR SSOT 2 + (7) finops multi_cloud dashboard parity CR 12-5 D-PARITY-01 = ~7 NEW vitest PASS 결정 wire + Phase 19 wire `8db3cfc` 의 pricing dashboard vitest pattern verbatim EXTENSION.
- **F36.6.8** multi_cloud dashboard Recharts 2.12.7 AD-14 stack pin EXTENSION 결정 wire + 5 NEW charts (AreaChart + BarChart + LineChart + HeatMap + PieChart) + 4 industries baseline (manufacturing + service + manufacturing_service + saas) visualization 차이 EXTENSION + Phase 19 wire `8db3cfc` 의 pricing dashboard Recharts 패턴 verbatim EXTENSION 결정 wire.
- **F36.6.9** multi_cloud dashboard owner approval flow 결정 wire = NegotiationBotConfigPanel 진입 시 owner-only RBAC AD-22 verbatim + Epic 12 2FA 챌린지 mandatory + auto-trigger enable 시 tenant_owner_id 의 2nd factor 인증 required + audit-first INSERT `multi_cloud_dashboard_viewed` with `approval_required=true` flag 결정 wire + Phase 14 wire `e904485` 의 commitment_recommender approval flow 패턴 verbatim EXTENSION 결정 wire.
- **F36.6.10** multi_cloud dashboard export + dispatch EXTENSION 결정 wire = (1) Multi-cloud unified cost dashboard export PDF (reportlab 4.0.7 + Korean font registration Pretendard-Regular.ttf) + CSV (pandas 2.1.4 + openpyxl 3.1.2) + Excel (xlsxwriter 3.1.9) 3 format / (2) scheduled dispatch KST cron weekly Mon 09:00 + monthly 1st-day 09:00 + quarterly 1st-day 09:00 + annual Jan-1 09:00 / (3) Slack + Email + S3 archive + MS Teams webhook 4 recipients / (4) AD-22 owner-only RBAC + Epic 12 2FA 챌린지 + audit-first INSERT `multi_cloud_dashboard_viewed` with `dispatch_triggered=true` flag 결정 wire + Phase 19 wire `8db3cfc` 의 pricing_report_generation + scheduled_pricing_dispatch 패턴 verbatim EXTENSION 결정 wire.
- **F36.6.11** multi_cloud dashboard dry-run mode EXTENSION 결정 wire = (1) NegotiationBotConfigPanel 진입 시 dry-run mode toggle (default: dry-run) + (2) dry-run 모드에서 bot trigger 시 actual `negotiation_bot_triggered` audit-first INSERT skip + (3) dry-run 결과 preview = phase_20_negotiation_bot_preview + phase_20_rate_card_reconciliation_preview + phase_20_cost_reconciliation_preview 3 table 결정 wire + Phase 19 wire `8db3cfc` 의 pricing dry-run mode 패턴 verbatim EXTENSION 결정 wire.
- **F36.6.12** AD-47 신규 결정 진입 (a)~(g) 7 sub-decisions 결정 wire + 부록 A architectural decisions table 1 NEW row EXTENSION + Phase 19 AD-46 verbatim EXTENSION 결정 wire.

### F36.7 Capability matrix v1.46 EXTENSION FINOPS_MULTI_CLOUD_UNIFIED_RECONCILIATION (A543 결정)

- **F36.7.1** `docs/capability-matrix.md` MODIFIED 결정 wire + v1.45 → v1.46 EXTENSION + FINOPS_MULTI_CLOUD_UNIFIED_RECONCILIATION 1 NEW row after FINOPS_PRICING industry-agnostic 4-industry grants ✅/✅/✅/✅ CR 12-1 L4 precedent verbatim 미러 EXTENSION + FINOPS_PRICING Phase 19 wire + FINOPS_COMMITMENT Phase 18 wire + FINOPS_SUSTAINABILITY Phase 17 wire + FINOPS_REPORTING Phase 16 wire + FINOPS_TAG_GOVERNANCE Phase 15 wire + FINOPS_OPTIMIZATION Phase 14 wire + FINOPS_FORECASTING_CAPACITY_PLANNING Phase 13 wire + FINOPS_ANOMALY_DETECTION + FINOPS_BUDGET_ALERT Phase 12 wire + FINOPS_SHOWBACK + FINOPS_CHARGEBACK Phase 11 wire + SLO_ENGINEERING Phase 10 wire + CHAOS_ENGINEERING Phase 9 wire + PERFORMANCE_TESTING Phase 8 wire + OBSERVABILITY_TRACES + OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind EXTENSION 결정 wire.
- **F36.7.2** `apps/api/core/capability.py` MODIFIED 결정 wire + `Capability.FINOPS_MULTI_CLOUD_UNIFIED_RECONCILIATION = "finops_multi_cloud_unified_reconciliation"` 1 NEW enum EXTENSION 결정 wire.
- **F36.7.3** `apps/api/dependencies/capability.py` MODIFIED 결정 wire + `require_finops_multi_cloud` 1 NEW dep EXTENSION 결정 wire.
- **F36.7.4** `apps/api/core/audit_action.py` MODIFIED 결정 wire + `ActionClass.FINOPS_MULTI_CLOUD_UNIFIED_RECONCILIATION = "finops_multi_cloud_unified_reconciliation"` 1 NEW enum EXTENSION 결정 wire + `FinopsMultiCloudAction` 8 NEW Literal EXTENSION 결정 wire (multi_cloud_dashboard_viewed + multi_cloud_rate_card_reconciled + multi_cloud_cost_reconciled + negotiation_bot_triggered + marketplace_saas_pricing_integrated + multi_cloud_dry_run_executed + multi_cloud_kpi_refreshed + blended_unblended_tracked).
- **F36.7.5** `tests/integration/test_capability_matrix_v1_46_drift.py` NEW 8 NEW pytest cases EXTENSION 결정 wire = FINOPS_MULTI_CLOUD_UNIFIED_RECONCILIATION row presence + 4-industry grants ✅/✅/✅/✅ + Phase 11~19 FINOPS_* capability rows preserved + AD-22 owner-only pattern + CR 12-1 L4 verbatim + drift detector EXTENSION + Phase 19 wire `8db3cfc` 의 test_capability_matrix_v1_45_drift.py pattern verbatim EXTENSION 결정 wire.
- **F36.7.6** `tests/integration/test_audit_action_v1_46_drift.py` NEW 6 NEW pytest cases EXTENSION 결정 wire = ActionClass.FINOPS_MULTI_CLOUD_UNIFIED_RECONCILIATION enum presence + FinopsMultiCloudAction 8 Literal + __all__ EXTENSION + Phase 11~19 ActionClass.FINOPS_* enum preserved + drift detector EXTENSION 결정 wire.
- **F36.7.7** `apps/api/modules/finops/__init__.py` MODIFIED 결정 wire = multi_cloud submodule export EXTENSION + Phase 19 wire `8db3cfc` 의 pricing submodule export pattern verbatim EXTENSION 결정 wire.
- **F36.7.8** ko-KR.json `finops_multi_cloud.*` namespace EXTENSION ~30 keys 결정 wire + ARIA labels WCAG 2.1 AA 결정 wire + Phase 19 wire `8db3cfc` 의 ko-KR.json EXTENSION pattern verbatim EXTENSION 결정 wire.
- **F36.7.9** `apps/web/lib/finops/multi-cloud-types.ts` NEW TypeScript mirror 결정 wire + Phase 19 wire `8db3cfc` 의 `pricing-types.ts` pattern verbatim EXTENSION 결정 wire + CR 12-5 D-PARITY-01 inversion TypeScript mirror 결정 wire.
- **F36.7.10** `apps/web/lib/finops/multi-cloud-client.ts` NEW TypeScript client 결정 wire + `fetchRateCardReconciliations()` + `fetchCostReconciliations()` + `triggerNegotiationBot()` + `trackBlendedUnblended()` + `integrateMarketplacePricing()` 5 methods EXTENSION + Phase 19 wire `8db3cfc` 의 `pricing-client.ts` pattern verbatim EXTENSION 결정 wire.
- **F36.7.11** NFR8 (감사로그 5년 append-only) 결정 wire = 8 NEW audit log entries + 5-year retention EXTENSION + NFR8 보존 EXTENSION.
- **F36.7.12** AD-47 신규 결정 진입 (a)~(g) 7 sub-decisions 결정 wire + 부록 A architectural decisions table 1 NEW row EXTENSION + Phase 19 AD-46 verbatim EXTENSION 결정 wire.

### F36.8 dry-run + Tests + wire scope T1~T8 (A543 결정)

- **F36.8.1** dry-run mode 결정 wire = `--finops-multi-cloud-rate-card-dry-run` + `--finops-multi-cloud-cost-dry-run` + `--finops-multi-cloud-negotiation-bot-dry-run` + `--finops-multi-cloud-blended-unblended-dry-run` + `--finops-multi-cloud-marketplace-dry-run` 5 NEW CLI flag + dry-run 시 actual `multi_cloud_dashboard_viewed` + `multi_cloud_rate_card_reconciled` + `multi_cloud_cost_reconciled` + `negotiation_bot_triggered` + `marketplace_saas_pricing_integrated` + `multi_cloud_dry_run_executed` + `multi_cloud_kpi_refreshed` + `blended_unblended_tracked` audit-first INSERT skip 결정 wire + Phase 11~19 dry-run pattern verbatim EXTENSION.
- **F36.8.2** dry-run preview 결과 결정 wire = `phase_20_multi_cloud_rate_card_preview` + `phase_20_multi_cloud_cost_preview` + `phase_20_negotiation_bot_preview` 3 table 결정 wire + preview_id UUID PK + tenant_id UUID + preview_type enum + period_key TEXT + preview_data JSONB + computed_at TIMESTAMPTZ DEFAULT NOW() + trace_id TEXT + RLS 자동 적용 CR 0-2 verbatim + audit-first INSERT `multi_cloud_dry_run_executed` 결정 wire.
- **F36.8.3** dry-run CLI flag 5 NEW 결정 wire + Phase 11~19 dry-run pattern verbatim EXTENSION + 5 CLI flag 모두 wire 결정 wire + dry-run 결과 preview 결정 wire.
- **F36.8.4** tests 결정 wire = ~+80 NEW pytest PASS (rate_card_reconciliation_aggregator 10 + cost_reconciliation_aggregator 10 + negotiation_bot 30 + blended_unblended_tracker 6 + marketplace_saas_pricing_integrator 8 + alembic 0052 6 + audit action 8 + capability matrix v1.46 8 + dry-run 6 + 5 cloud provider integration 8 = ~+92 NEW pytest PASS) 결정 wire + Phase 11~19 test pattern verbatim EXTENSION.
- **F36.8.5** vitest tests ~+7 NEW vitest PASS 결정 wire (MultiCloudRateCardReconciliationPanel 1 + MultiCloudCostReconciliationPanel 1 + NegotiationBotConfigPanel 1 + BlendedUnblendedTrackerPanel 1 + MarketplaceSaaSPricingPanel 1 + ko-KR SSOT 2 + finops multi_cloud dashboard parity CR 12-5 D-PARITY-01 결정 wire) + Phase 11~19 vitest pattern verbatim EXTENSION.
- **F36.8.6** ruff + tsc 0 NEW + SDR drift gate 결정 wire + A36 SDR 검증 4-step 자동 적용 결정 wire + 0 NEW ruff + 0 NEW tsc + 0 regressions + SDR drift gate PASS 결정 wire.
- **F36.8.7** wire scope T1~T8 결정 wire = T1 rate_card_reconciliation_aggregator + cost_reconciliation_aggregator + negotiation_bot 결정 wire + T2 blended_unblended_tracker + Naver/KT stability 검증 + marketplace_saas_pricing_integrator 결정 wire + T3 scheduled_dispatch KST cron 4 cadence 결정 wire + T4 alembic 0052 phase_20_multi_cloud_unified_reconciliation 8 tables + RLS 8 tables + 3 preview tables 결정 wire + T5 audit action EXTENSION 8 NEW + 16 NEW typed exception classes 결정 wire + T6 capability v1.46 EXTENSION + frontend finops multi_cloud dashboard 결정 wire + T7 scheduled_dispatch_job wire 결정 wire + T8 3중 게이트 FINAL CLEAN atomic commit 결정 wire + estimated ~+30-35 files estimate = ~+22 NEW + ~+13 MODIFIED 결정 wire + Phase 19 wire `8db3cfc` 의 ~28 files pattern verbatim EXTENSION 결정 wire + Phase 11~19 wire 의 ~25-30 files pattern verbatim EXTENSION.
- **F36.8.8** A19 cohesion pattern 9 surface EXTENSION PASS 결정 wire (FinOps Multi-Cloud Cost Unified Reconciliation surface NEW = F36.1~F36.8 결정 wire) + Phase 19 wire `8db3cfc` 의 FinOps Pricing, Rate Card & TCO Modeling surface NEW = F35.1~F35.8 pattern verbatim 미러 + Phase 18 wire `67059cf` 의 FinOps Cloud Commitment Management surface NEW = F34.1~F34.8 pattern verbatim 미러 + Phase 17 wire `97cfe4e` 의 FinOps Sustainability & Carbon Reporting surface NEW = F33.1~F33.8 pattern verbatim 미러 + Phase 16 wire `81ae00a` 의 FinOps Reporting & Executive Dashboard surface NEW = F32.1~F32.8 pattern verbatim 미러 + Phase 15 wire `1b800d9` 의 FinOps Tag Governance surface NEW = F31.1~F31.8 pattern verbatim 미러 + Phase 14 wire `e904485` 의 FinOps Optimization surface NEW = F30.1~F30.8 pattern verbatim 미러 + Phase 13 wire `8b98030` 의 FinOps Forecasting surface NEW = F29.1~F29.8 pattern verbatim 미러 + Phase 12 wire `f3c0e63` 의 FinOps Anomaly surface NEW = F28.1~F28.8 pattern verbatim 미러 + Phase 11 wire `e020ad0` 의 FinOps Showback surface NEW = F27.1~F27.7 pattern verbatim 미러 결정 wire.
- **F36.8.9** CR lessons applied 18종 결정 wire (CR 0-2 RLS lesson ✅ APPLIED + CR 1-1 audit-first INSERT ✅ APPLIED (8 NEW audit log entries + 9-module source attribution 2행 append = 10 NEW effective audit events) + CR 4-3/4-4 lessons carry ✅ APPLIED + CR 1-1 ContextVar lesson ✅ APPLIED + CR 1-1 RSC boundary lesson ✅ APPLIED + CR 9-6 commit message discipline ✅ APPLIED + CR 11-3 honest-DEFER discipline ✅ APPLIED (142번째 epic 연속 정직 회복) + CR 11-4 D-001~D-005 + P-015 ✅ APPLIED + CR 12-1 L4 ✅ APPLIED + CR 12-5 D-14 typed exception envelope ✅ APPLIED (16 NEW typed exception classes) + CR 12-5 D-PARITY-01 inversion ✅ APPLIED + CR 12-5 D-GATE-01 inversion ✅ APPLIED + A36 SDR 검증 4-step 자동 적용 ✅ + AD-14 stack pin ✅ APPLIED + AD-22 owner-only RBAC ✅ APPLIED + NFR4 PII minimization ✅ PRESERVED) 결정 wire.
- **F36.8.10** D-DEFER-* honestly 결정 wire (CR 11-3 142번째 epic 연속 정직 회복 결정 wire 보존) = D-1-1-DEFER-1/2/3 RESOLVED 보존 60~142번째 + D-LAUNCH-1-DEFER-1 honestly preserved 65~142번째 + D-EPIC-16-REVIEW-DEFER-2~6 ✅ RESOLVED 보존 78~142번째 + D-PHASE-4-DR-DEFER-1/2 ✅ RESOLVED 보존 73~142번째 + D-EPIC-17-WIRE-DEFER-T2-T3-UI ✅ RESOLVED 보존 83~142번째 + D-RETENTION-1 ✅ RESOLVED 보존 85~142번째 + D-OBSERVABILITY-1 ✅ RESOLVED 보존 89~142번째 + D-PERFORMANCE-1 ✅ RESOLVED 보존 93~142번째 + D-CHAOS-1 ✅ RESOLVED 보존 100~142번째 + D-SLO-1 ✅ RESOLVED 보존 101~142번째 + D-FINOPS-1 ✅ RESOLVED 보존 105~142번째 + D-FINOPS-2 ✅ RESOLVED 보존 109~142번째 + D-FINOPS-3 ✅ RESOLVED 보존 113~142번째 + D-FINOPS-4 ✅ RESOLVED 보존 117~142번째 + D-FINOPS-5 ✅ RESOLVED 보존 121~142번째 + D-FINOPS-6 ✅ RESOLVED 보존 125~142번째 + D-FINOPS-7 ✅ RESOLVED 보존 129~142번째 + D-FINOPS-8 ✅ RESOLVED 보존 133~142번째 + **D-FINOPS-9 honestly DEFER 보존 + Phase 19.5 carry-over 결정 wire `b2fb1d8` 의 AD-47 신규 (a)~(g) 7 sub-decisions 모두 결정 wire 진입 = Phase 20 PRD entry 진입 시점에 carry-over chain 정직 회복 결정 wire 진입: Phase 19.5 carry-over 결정 wire `b2fb1d8` §F35.5~§F35.13 + Phase 19 close-out retro `18ca1ae` §13 + Phase 19 wire `8db3cfc` §13 + Phase 18 close-out retro `de72f50` §13 + Phase 17 close-out retro `de009fe` §13 + Phase 16 close-out retro `26fd530` §13 + Phase 15 close-out retro `102f370` §13 + Phase 14 close-out retro `5b367d9` §13 + Phase 13 close-out retro `850b4f8` §13 + Phase 12 close-out retro `3354e83` §13 + Phase 11 close-out retro `80df15b` §12 + Phase 10 close-out retro `733d428` §10 + Phase 9 close-out retro `634427d` §10 + Phase 8 close-out retro `ab495a8` §10 + Phase 7 close-out retro `326fa9f` §10 + Phase 6 close-out retro `f9f006c` §13 + Epic 17 close-out retro `be8f3bd` §11 "FinOps Multi-Cloud Cost Unified Reconciliation 결정 wire 보류, Phase 20+ 진입 시점" verbatim 해소**) + Epic 1 ~ Epic 17 + Phase 3 ~ Phase 19 + 1st release cycle 정합 보존 결정 wire (cj-style 142번째 epic 연속 정직 회복 pre-flight 정합 sweep) + 결정 wire 일자: 2026-08-25 (KST) + **next**: 옵션 (a) Phase 20 bmad-create-story spec entry 진입 (cj-style 143번째 wire 진입 시점) / 옵션 (b) Phase 20 bmad-dev-story atomic wire T1~T8 진입 (cj-style 144번째) / 옵션 (c) Phase 20 close-out retro 진입 (cj-style 145번째) / 옵션 (d) Epic 21+ 진입 / 옵션 (e) D-DEFER-* follow-up 결정 wire 보류 결정 wire.

---

## F37. FinOps Reserved Capacity Planning (Phase 21 territory 신규)

> Phase 21 PRD entry DONE 2026-08-26 (cj-style Phase 21 1번째 진입점 = cj-style 149번째 epic 연속 정직 회복 atomic docs-only wire). master PRD v6.0 → v7.0 atomic edit (docs only). Phase 21 territory = FinOps Reserved Capacity Planning = (1) **demand_forecast_aggregator** / (2) **capacity_planning_aggregator** / (3) **commitment_recommendation_engine** / (4) **reserved_capacity_orchestrator** / (5) **reserved_capacity dashboard UI** / (6) **Capability matrix v1.47 EXTENSION FINOPS_RESERVED_CAPACITY_PLANNING** / (7) **dry-run + Tests + wire scope T1~T8** = **§F37.1 ~ §F37.8 신규 결정 wire 진입**.

### F37.1 demand_forecast_aggregator + 5 FinOps modules cross-join EXTENSION (A574 결정)

- **F37.1.1** `apps/api/modules/finops/reserved_capacity/demand_forecast_aggregator.py` NEW ~+220 LOC + `aggregate_demand_forecast(tenant_id, period_key, scope_chain) -> ReservedCapacityDemandForecast` TypedDict 16 fields (demand_forecast_id + tenant_id + period_key + scope enum tenant/department/cost_center/product_line + scope_chain value_jsonb 5-module source attribution + forecast_horizon_months int + forecasted_demand_krw NUMERIC(20,2) + confidence_interval_low_krw NUMERIC(20,2) + confidence_interval_high_krw NUMERIC(20,2) + seasonal_factor NUMERIC(5,2) + growth_rate_pct NUMERIC(5,2) + baseline_demand_krw NUMERIC(20,2) + demand_signal enum growth/stable/declining/volatile + last_calculated_at + trace_id) + 5 FinOps modules cross-join EXTENSION (Phase 13 forecast + Phase 14 optimization + Phase 18 commitment + Phase 19 pricing + Phase 20 multi_cloud) + audit-first INSERT `demand_forecast_calculated` CR 1-1 verbatim 결정 wire + Phase 13 wire `8b98030` 의 forecasting_aggregator 5-source reconciliation 패턴 verbatim EXTENSION 결정 wire.
- **F37.1.2** demand forecast composition 패턴 결정 wire = (1) Phase 13 forecasting_aggregator 12-month horizon 결과 baseline_demand_krw 으로 입력 / (2) Phase 14 optimization rightsizing 권고의 idle_cost_reduction_krw 차감 / (3) Phase 18 commitment_inventory 의 expiring_commitments_30d 의 renewal_required_krw 가산 / (4) Phase 19 pricing tco_3year_commitment_krw 의 unit_economics_score 가중치 적용 / (5) Phase 20 multi_cloud reconciliation 의 cost_variance_avg_pct 의 volatility 조정 + 5 module 의 가중 평균 = single forecasted_demand_krw 결정 wire + Phase 20 wire `52dad7f` 의 9-module cross-rollup 패턴 verbatim EXTENSION.
- **F37.1.3** forecast confidence interval 계산 결정 wire = (a) confidence_interval_low_krw = forecasted_demand_krw × (1 - growth_rate_pct × 0.5) / (b) confidence_interval_high_krw = forecasted_demand_krw × (1 + growth_rate_pct × 0.5) / (c) confidence interval width = (high - low) / forecast × 100 → width > 30% → demand_signal = `volatile` / width 15~30% → `growth` or `declining` / width ≤ 15% → `stable` 결정 wire + Phase 13 wire `8b98030` 의 forecast_confidence_interval 패턴 verbatim EXTENSION.
- **F37.1.4** seasonal factor + growth rate 계산 결정 wire = (1) seasonal_factor = Σ(monthly_cost_variance_pct last 12 months) / 12 / 100 + 1.0 / (2) growth_rate_pct = (current_month_cost - same_month_last_year_cost) / same_month_last_year_cost × 100 / (3) 4 industries baseline EXTENSION (manufacturing ≤ 8% / service ≤ 12% / manufacturing_service ≤ 10% / saas ≤ 18%) / (4) per-tenant override EXTENSION / (5) growth_rate_pct > baseline × 1.5 → alert "demand spike detected" + audit-first INSERT `demand_forecast_calculated` with `growth_alert=true` flag 결정 wire + Phase 11 wire `e020ad0` 의 showback period selector 패턴 verbatim EXTENSION.
- **F37.1.5** demand forecast cross-tenant invalidation 결정 wire = LISTEN/NOTIFY channel `phase_21_demand_forecast_calculated` 신규 + LISTEN trigger after INSERT on phase_21_reserved_capacity_demand_forecast table → broadcast to all tenant workers → all RSC page revalidate (Phase 13 wire `8b98030` + Phase 14 wire `e904485` 의 LISTEN/NOTIFY consume trigger EXTENSION 패턴 verbatim) 결정 wire + CR 0-2 RLS + CR 1-1 audit-first INSERT verbatim EXTENSION.
- **F37.1.6** demand forecast KPI 결정 wire = `reserved_capacity_demand_forecast_krw` (forecasted_demand_krw Σ across 5 module cross-join, target = sum of 5 individual module forecasts) + `forecast_confidence_pct` (100 - width × 0.5, target ≥ 70%) + `demand_growth_rate_pct` (growth_rate_pct, target ≤ industry baseline) + `demand_volatility_count_30d` (volatile signal last 30d count, target ≤ 3) + 4 industries baseline EXTENSION + per-tenant override EXTENSION + audit-first INSERT `reserved_capacity_kpi_refreshed` CR 1-1 verbatim 적용 결정 wire + Phase 20 wire `52dad7f` 의 multi_cloud_kpi_refreshed 패턴 verbatim EXTENSION.
- **F37.1.7** demand forecast audit log 결정 wire = phase_21_demand_forecast_audit 테이블 NEW + audit_id UUID PK + tenant_id UUID + scope enum + forecast_horizon_months int + forecasted_demand_krw NUMERIC(20,2) + confidence_interval_low/high_krw NUMERIC(20,2) + seasonal_factor NUMERIC(5,2) + growth_rate_pct NUMERIC(5,2) + demand_signal enum + forecast_calculated_at TIMESTAMPTZ DEFAULT NOW() + trace_id TEXT + RLS 자동 적용 CR 0-2 verbatim + 5-year retention 결정 wire + Phase 18 wire `67059cf` 의 commitment_inventory_audit_log 패턴 verbatim EXTENSION + NFR8 audit log 5년 append-only 보존 결정 wire.
- **F37.1.8** demand forecast integration tests 결정 wire = (1) 5-module cross-join 8 NEW pytest cases (Phase 13 + Phase 14 + Phase 18 + Phase 19 + Phase 20 input fixtures + composition logic) / (2) confidence interval calculation 6 NEW pytest cases (volatile + growth + declining + stable) / (3) seasonal factor + growth rate 6 NEW pytest cases (4 industries baseline + per-tenant override) / (4) cross-tenant invalidation 4 NEW pytest cases (LISTEN/NOTIFY) = ~24 NEW pytest cases 결정 wire + Phase 20 wire `52dad7f` 의 multi_cloud_demand_forecast tests 패턴 verbatim EXTENSION.
- **F37.1.9** demand forecast RLS + cross-tenant safety 결정 wire = RLS 자동 적용 CR 0-2 verbatim + tenant_id = current_setting('app.tenant_id')::uuid + cross-tenant query 시 tenant_id mismatch 자동 reject + Epic 12 2FA 챌린지 보존 + audit-first INSERT `demand_forecast_calculated` with `cross_tenant_attempt=true` flag if attempted 결정 wire + Phase 20 wire `52dad7f` 의 multi_cloud_cost_reconciliation RLS 패턴 verbatim EXTENSION.
- **F37.1.10** demand forecast idempotency 결정 wire = idempotency_key = (tenant_id, period_key, scope) + duplicate trigger 시 동일 idempotency_key 발견 → skip + audit-first INSERT `demand_forecast_calculated` with `duplicate_skipped=true` flag 결정 wire + Phase 20 wire `52dad7f` 의 negotiation_bot_idempotency 패턴 verbatim EXTENSION 결정 wire.
- **F37.1.11** demand forecast forecasting_aggregator integration 결정 wire = Phase 13 wire `8b98030` 의 forecasting_aggregator 의 12-month forecast 결과를 직접 import + Phase 14 wire `e904485` 의 rightsizing_aggregator 의 idle_cost_reduction_krw 를 직접 import + Phase 18 wire `67059cf` 의 commitment_inventory_aggregator 의 expiring_commitments_30d 를 직접 import + Phase 19 wire `8db3cfc` 의 pricing_aggregator 의 tco_3year_commitment_krw 를 직접 import + Phase 20 wire `52dad7f` 의 multi_cloud_cost_reconciliation_aggregator 의 cost_variance_avg_pct 를 직접 import → 5 module cross-join 후 가중 평균 계산 = composition layer only, 신규 backend aggregator 작성 최소화 결정 wire + A19 cohesion 9 surface EXTENSION PASS 보존 결정 wire.
- **F37.1.12** AD-49 신규 결정 진입 (a)~(g) 7 sub-decisions 결정 wire + 부록 A architectural decisions table 1 NEW row EXTENSION + Phase 20 AD-47 verbatim EXTENSION 결정 wire.

### F37.2 capacity_planning_aggregator + reserved capacity tier calculation (A575 결정)

- **F37.2.1** `apps/api/modules/finops/reserved_capacity/capacity_planning_aggregator.py` NEW ~+220 LOC + `plan_reserved_capacity(tenant_id, period_key, demand_forecast_id, scope) -> ReservedCapacityPlan` TypedDict 18 fields (capacity_plan_id + tenant_id + period_key + demand_forecast_id UUID FK + scope enum tenant/department/cost_center/product_line + forecasted_demand_krw NUMERIC(20,2) + current_capacity_krw NUMERIC(20,2) + capacity_gap_krw NUMERIC(20,2) + recommended_reserved_capacity_krw NUMERIC(20,2) + reserved_capacity_tier enum 1_year_no_upfront/1_year_partial_upfront/1_year_all_upfront/3_year_no_upfront/3_year_partial_upfront/3_year_all_upfront + estimated_savings_pct NUMERIC(5,2) + estimated_savings_krw_per_year NUMERIC(20,2) + payback_period_months int + break_even_utilization_pct NUMERIC(5,2) + capacity_headroom_pct NUMERIC(5,2) + planning_status enum auto_planning/manual_review_required/skipped + computed_at + trace_id) + audit-first INSERT `capacity_planning_recommended` CR 1-1 verbatim 결정 wire.
- **F37.2.2** capacity gap calculation 결정 wire = (1) current_capacity_krw = Phase 18 commitment_inventory_aggregator 의 total_commitment_value_krw / (2) capacity_gap_krw = forecasted_demand_krw - current_capacity_krw / (3) if capacity_gap_krw > 0 → recommended_reserved_capacity_krw = capacity_gap_krw + safety_buffer (10% of forecasted_demand_krw) / (4) if capacity_gap_krw ≤ 0 → recommended_reserved_capacity_krw = 0 + planning_status = `skipped` (over-provisioned) / (5) capacity_headroom_pct = (current_capacity_krw + recommended_reserved_capacity_krw - forecasted_demand_krw) / forecasted_demand_krw × 100 → target 10~20% headroom 결정 wire + Phase 18 wire `67059cf` 의 commitment_inventory_aggregator 의 coverage_pct 계산 패턴 verbatim EXTENSION.
- **F37.2.3** reserved capacity tier 선택 알고리즘 결정 wire = (1) compute break_even_utilization_pct for each tier (1_year_no_upfront ~70% / 1_year_partial_upfront ~60% / 1_year_all_upfront ~50% / 3_year_no_upfront ~80% / 3_year_partial_upfront ~65% / 3_year_all_upfront ~55%) / (2) compute estimated_savings_pct = tier_discount_pct - break_even_risk_premium (3_year_~5% / 1_year_~2%) / (3) select tier with max estimated_savings_pct where break_even_utilization_pct ≤ forecasted_utilization_pct / (4) tier selection guard = MINIMUM_SAVINGS_PCT = 5.0 (5% 미만 skip recommendation) + MINIMUM_SAVINGS_KRW = 1_000_000 (연 100만원 미만 skip recommendation) / (5) capacity_planning_recommended with `skipped=true` flag if guard violated 결정 wire + Phase 20 wire `52dad7f` 의 negotiation_bot guard 패턴 verbatim EXTENSION.
- **F37.2.4** 4 industries baseline EXTENSION 결정 wire = manufacturing / service / manufacturing_service / saas 별 (recommended_reserved_capacity_krw + estimated_savings_pct + capacity_headroom_pct + break_even_utilization_pct) baseline 4-set + per-tenant override EXTENSION + 4-industry grants industry-agnostic ✅/✅/✅/✅ CR 12-1 L4 precedent 미러 결정 wire + Phase 20 wire `52dad7f` 의 4 industries baseline EXTENSION 패턴 verbatim EXTENSION.
- **F37.2.5** capacity planning cross-tenant invalidation 결정 wire = LISTEN/NOTIFY channel `phase_21_capacity_planning_recommended` 신규 + LISTEN trigger after INSERT on phase_21_reserved_capacity_plan table → broadcast to all tenant workers → all RSC page revalidate + CR 0-2 RLS + CR 1-1 audit-first INSERT verbatim EXTENSION + Phase 13 wire `8b98030` LISTEN/NOTIFY 패턴 verbatim EXTENSION 결정 wire.
- **F37.2.6** capacity planning KPI 결정 wire = `reserved_capacity_recommended_krw` (Σ recommended_reserved_capacity_krw across scope, target = sum of 5 module demand) + `reserved_capacity_savings_pct_avg` (avg estimated_savings_pct, target ≥ 15%) + `capacity_headroom_pct_avg` (avg capacity_headroom_pct, target 10~20%) + `reserved_capacity_plans_active` (active plan count, target ≥ 1 per scope) + 4 industries baseline EXTENSION + per-tenant override EXTENSION + audit-first INSERT `reserved_capacity_kpi_refreshed` CR 1-1 verbatim 적용 결정 wire + Phase 20 wire `52dad7f` 의 multi_cloud_kpi_refreshed 패턴 verbatim EXTENSION.
- **F37.2.7** capacity planning audit log 결정 wire = phase_21_capacity_planning_audit 테이블 NEW + audit_id UUID PK + tenant_id UUID + capacity_plan_id UUID FK + recommended_reserved_capacity_krw NUMERIC(20,2) + reserved_capacity_tier enum + estimated_savings_pct NUMERIC(5,2) + planning_status enum + computed_at TIMESTAMPTZ DEFAULT NOW() + trace_id TEXT + RLS 자동 적용 CR 0-2 verbatim + 5-year retention 결정 wire + Phase 19 wire `8db3cfc` 의 pricing_audit_log 패턴 verbatim EXTENSION + NFR8 audit log 5년 append-only 보존 결정 wire.
- **F37.2.8** capacity planning tests 결정 wire = (1) capacity gap calculation 8 NEW pytest cases (positive gap + zero gap + negative gap + safety buffer) / (2) reserved capacity tier selection 10 NEW pytest cases (6 tier + break-even + guard) / (3) 4 industries baseline 8 NEW pytest cases (manufacturing + service + manufacturing_service + saas + per-tenant override) / (4) cross-tenant invalidation 4 NEW pytest cases = ~30 NEW pytest cases 결정 wire + Phase 18 wire `67059cf` 의 commitment_kpi_selector tests 패턴 verbatim EXTENSION.
- **F37.2.9** capacity planning RLS + cross-tenant safety 결정 wire = RLS 자동 적용 CR 0-2 verbatim + tenant_id = current_setting('app.tenant_id')::uuid + cross-tenant query 시 tenant_id mismatch 자동 reject + Epic 12 2FA 챌린지 보존 + audit-first INSERT `capacity_planning_recommended` with `cross_tenant_attempt=true` flag if attempted 결정 wire + Phase 18 wire `67059cf` 의 commitment_inventory_aggregator RLS 패턴 verbatim EXTENSION.
- **F37.2.10** capacity planning idempotency 결정 wire = idempotency_key = (tenant_id, period_key, demand_forecast_id, scope) + duplicate trigger 시 동일 idempotency_key 발견 → skip + audit-first INSERT `capacity_planning_recommended` with `duplicate_skipped=true` flag 결정 wire + Phase 19 wire `8db3cfc` 의 pricing_scheduled_dispatch_idempotency 패턴 verbatim EXTENSION 결정 wire.
- **F37.2.11** capacity planning tenant override + on-demand recompute 결정 wire = (1) per-tenant reserved_capacity_tier override (tenant_admin can manually select specific tier) / (2) on-demand recompute via admin UI (admin can trigger immediate recompute) / (3) per-scope reserved_capacity_krw override (tenant_admin can adjust recommended_reserved_capacity_krw per scope) / (4) recompute progress indicator (admin can monitor recompute status) / (5) audit-first INSERT `capacity_planning_recommended` with `manual_recompute=true` flag if on-demand triggered 결정 wire + Phase 19 wire `8db3cfc` 의 pricing_tenant_override 패턴 verbatim EXTENSION 결정 wire.
- **F37.2.12** AD-49 신규 결정 진입 (a)~(g) 7 sub-decisions 결정 wire + 부록 A architectural decisions table 1 NEW row EXTENSION + Phase 20 AD-47 verbatim EXTENSION 결정 wire.

### F37.3 commitment_recommendation_engine + reserved capacity tier execution (A576 결정)

- **F37.3.1** `apps/api/modules/finops/reserved_capacity/commitment_recommendation_engine.py` NEW ~+250 LOC + `generate_commitment_recommendation(tenant_id, capacity_plan_id, reserved_capacity_tier) -> CommitmentRecommendation` TypedDict 17 fields (recommendation_id + tenant_id + capacity_plan_id UUID FK + reserved_capacity_tier enum + recommended_commitment_term enum 1_year/3_year + recommended_quantity_krw NUMERIC(20,2) + estimated_savings_pct NUMERIC(5,2) + estimated_savings_krw_per_year NUMERIC(20,2) + payback_period_months int + break_even_utilization_pct NUMERIC(5,2) + confidence_score NUMERIC(5,2) + risk_score enum low/medium/high + execution_strategy enum auto_execute_ready/manual_review_required/owner_approval_required/low_confidence + auto_execute_enabled boolean + owner_approval_required boolean + computed_at + trace_id) + audit-first INSERT `commitment_recommendation_generated` CR 1-1 verbatim 결정 wire.
- **F37.3.2** commitment recommendation composition 패턴 결정 wire = (1) Phase 14 wire `e904485` commitment_recommender 1y/3y break-even 결과 입력 / (2) Phase 18 wire `67059cf` commitment_inventory 의 coverage_pct + utilization_pct 가중치 적용 / (3) Phase 19 wire `8db3cfc` pricing 의 tco_1year_commitment_krw + tco_3year_commitment_krw 비교 / (4) Phase 20 wire `52dad7f` multi_cloud 의 5 cloud provider (AWS EDP + Azure EA + GCP CUD + Naver Cloud + KT Cloud) cross-rollup / (5) Phase 21 F37.2 capacity_planning_aggregator 의 recommended_reserved_capacity_krw = composition layer only, 5 module 의 가중 평균 = single recommendation_id 결정 wire + Phase 20 wire `52dad7f` 의 9-module cross-rollup 패턴 verbatim EXTENSION.
- **F37.3.3** confidence + risk scoring 결정 wire = (1) confidence_score = utilization_stability_score × 0.4 + historical_accuracy_score × 0.3 + demand_forecast_confidence_pct × 0.3 (range 0~100) / (2) risk_score = savings_pct × 0.4 + commitment_term × 0.3 + commitment_flexibility × 0.3 (low / medium / high) / (3) confidence_score < 60 → execution_strategy = `low_confidence` + skip auto-execute / (4) confidence_score ≥ 80 + risk_score = low → execution_strategy = `auto_execute_ready` + auto-trigger enabled / (5) intermediate → execution_strategy = `manual_review_required` / (6) high-value (≥ 10M KRW/year savings) → execution_strategy = `owner_approval_required` + Epic 12 2FA 챌린지 mandatory + audit-first INSERT `commitment_recommendation_generated` with `owner_approval_required=true` flag 결정 wire + Phase 20 wire `52dad7f` 의 negotiation_bot confidence/risk scoring 패턴 verbatim EXTENSION.
- **F37.3.4** commitment recommendation owner approval flow 결정 wire = (1) recommendation_status = `owner_approval_required` / (2) bot sends Slack DM to tenant_owner with recommendation summary / (3) tenant_owner approves via Slack button or admin UI → recommendation_status = `approved` / (4) tenant_owner rejects → recommendation_status = `rejected` / (5) audit-first INSERT `commitment_recommendation_generated` with approval_chain JSONB 결정 wire + AD-22 owner-only RBAC EXTENSION + Epic 12 2FA 챌린지 보존 + Phase 20 wire `52dad7f` 의 negotiation_bot owner approval flow 패턴 verbatim EXTENSION 결정 wire.
- **F37.3.5** commitment recommendation execution 결정 wire = (1) auto_execute_ready → trigger Phase 18 wire `67059cf` commitment_inventory 의 purchase_recommendation (1-year or 3-year commitment) → generate purchase order / (2) manual_review_required → tenant_admin 이 admin UI 에서 review 후 approve / (3) owner_approval_required → tenant_owner 가 Slack button 으로 2FA 챌린지 통과 후 approve / (4) audit-first INSERT `commitment_recommendation_generated` with execution_status (pending/executed/rejected/skipped) 결정 wire + Phase 18 wire `67059cf` 의 commitment_purchase_recommendation 패턴 verbatim EXTENSION.
- **F37.3.6** commitment recommendation idempotency 결정 wire = idempotency_key = (tenant_id, capacity_plan_id, reserved_capacity_tier) + duplicate trigger 시 동일 idempotency_key 발견 → skip + audit-first INSERT `commitment_recommendation_generated` with `duplicate_skipped=true` flag 결정 wire + Phase 20 wire `52dad7f` 의 negotiation_bot_idempotency 패턴 verbatim EXTENSION 결정 wire.
- **F37.3.7** commitment recommendation guard 결정 wire = MINIMUM_SAVINGS_PCT = 5.0 (5% 미만 skip recommendation) + MINIMUM_SAVINGS_KRW = 1_000_000 (연 100만원 미만 skip recommendation) + MAX_RECOMMENDATIONS_PER_MONTH = 3 (월 3회 초과 skip) + MAX_AUTO_EXECUTE_PER_DAY = 1 (일 1회 자동 execute 한정) + audit-first INSERT `commitment_recommendation_generated` with `skipped=true` flag if guard violated 결정 wire + Phase 20 wire `52dad7f` 의 negotiation_bot guard 패턴 verbatim EXTENSION.
- **F37.3.8** commitment recommendation audit log 결정 wire = phase_21_commitment_recommendation_audit 테이블 NEW + audit_id UUID PK + tenant_id UUID + capacity_plan_id UUID FK + recommended_commitment_term enum + recommended_quantity_krw NUMERIC(20,2) + execution_strategy enum + confidence_score NUMERIC(5,2) + risk_score enum + approval_chain JSONB + recommended_at TIMESTAMPTZ DEFAULT NOW() + trace_id TEXT + RLS 자동 적용 CR 0-2 verbatim + 5-year retention 결정 wire + Phase 19 wire `8db3cfc` 의 pricing_audit_log 패턴 verbatim EXTENSION + NFR8 audit log 5년 append-only 보존 결정 wire.
- **F37.3.9** commitment recommendation tests 결정 wire = (1) 5-module composition 8 NEW pytest cases (Phase 13 + 14 + 18 + 19 + 20 + 21 input fixtures) / (2) confidence + risk scoring 6 NEW pytest cases (low_confidence + auto_execute_ready + manual_review_required + owner_approval_required threshold) / (3) owner approval flow 4 NEW pytest cases (Slack DM + 2FA + approval_chain + rejection) / (4) idempotency + guard 6 NEW pytest cases = ~24 NEW pytest cases 결정 wire + Phase 20 wire `52dad7f` 의 negotiation_bot tests 패턴 verbatim EXTENSION.
- **F37.3.10** commitment recommendation RLS + cross-tenant safety 결정 wire = RLS 자동 적용 CR 0-2 verbatim + tenant_id = current_setting('app.tenant_id')::uuid + cross-tenant query 시 tenant_id mismatch 자동 reject + Epic 12 2FA 챌린지 보존 + audit-first INSERT `commitment_recommendation_generated` with `cross_tenant_attempt=true` flag if attempted 결정 wire + Phase 18 wire `67059cf` 의 commitment_inventory_aggregator RLS 패턴 verbatim EXTENSION.
- **F37.3.11** commitment recommendation cross-tenant invalidation 결정 wire = LISTEN/NOTIFY channel `phase_21_commitment_recommendation_generated` 신규 + LISTEN trigger after INSERT on phase_21_commitment_recommendation table → broadcast to all tenant workers → all RSC page revalidate + CR 0-2 RLS + CR 1-1 audit-first INSERT verbatim EXTENSION + Phase 13 wire `8b98030` LISTEN/NOTIFY 패턴 verbatim EXTENSION 결정 wire.
- **F37.3.12** AD-49 신규 결정 진입 (a)~(g) 7 sub-decisions 결정 wire + 부록 A architectural decisions table 1 NEW row EXTENSION + Phase 20 AD-47 verbatim EXTENSION 결정 wire.

### F37.4 reserved_capacity_orchestrator + composition layer (A577 결정)

- **F37.4.1** `apps/api/modules/finops/reserved_capacity/reserved_capacity_orchestrator.py` NEW ~+250 LOC + `orchestrate_reserved_capacity(tenant_id, period_key, scope) -> ReservedCapacityOrchestration` TypedDict 19 fields (orchestration_id + tenant_id + period_key + scope enum tenant/department/cost_center/product_line + demand_forecast_id UUID FK + capacity_plan_id UUID FK + commitment_recommendation_id UUID FK + orchestration_status enum planning_pending/planning_active/recommendation_pending/recommendation_approved/recommendation_executed/recommendation_rejected/low_confidence_skipped + total_recommended_reserved_capacity_krw NUMERIC(20,2) + total_estimated_savings_krw_per_year NUMERIC(20,2) + total_estimated_savings_pct NUMERIC(5,2) + composition_step_chain enum[] [demand_forecast → capacity_planning → commitment_recommendation] + execution_step_chain enum[] [demand_forecast_executed → capacity_planning_executed → commitment_recommendation_executed] + approval_step_chain JSONB + auto_execute_enabled boolean + computed_at + trace_id) + audit-first INSERT `reserved_capacity_orchestrator_triggered` CR 1-1 verbatim 결정 wire.
- **F37.4.2** orchestrator composition 패턴 결정 wire = (1) Step 1: call F37.1 demand_forecast_aggregator.aggregate_demand_forecast() → demand_forecast_id / (2) Step 2: call F37.2 capacity_planning_aggregator.plan_reserved_capacity(demand_forecast_id) → capacity_plan_id / (3) Step 3: call F37.3 commitment_recommendation_engine.generate_commitment_recommendation(capacity_plan_id) → commitment_recommendation_id / (4) Step 4: if execution_strategy = auto_execute_ready → trigger Phase 18 wire `67059cf` commitment_inventory_aggregator.purchase_recommendation() / (5) Step 5: persist orchestration record + composition_step_chain + execution_step_chain / (6) audit-first INSERT `reserved_capacity_orchestrator_triggered` with composition_step_chain + execution_step_chain 결정 wire + Phase 20 wire `52dad7f` 의 multi_cloud reconciliation composition 패턴 verbatim EXTENSION.
- **F37.4.3** orchestrator 4 cadence schedule 결정 wire = (1) daily cron at 02:00 KST (compute demand forecast + capacity plan + commitment recommendation for next 30 days) / (2) weekly cron Monday 03:00 KST (re-evaluate with weekly trend data) / (3) monthly cron 1st day 04:00 KST (re-evaluate with monthly actuals + carry-over chain) / (4) quarterly cron 1st day 05:00 KST (re-evaluate with quarterly actuals + commitment expiration check) = 4 NEW cron schedule + pytz timezone('Asia/Seoul') EXTENSION 결정 wire + Phase 20 wire `52dad7f` 의 multi_cloud_scheduled_dispatch 4 cadence 패턴 verbatim EXTENSION.
- **F37.4.4** orchestrator dry-run mode 결정 wire = (1) dry-run mode 시 actual `reserved_capacity_orchestrator_triggered` audit-first INSERT skip / (2) dry-run 결과 preview = phase_21_orchestration_preview 테이블 NEW + preview_id UUID PK + tenant_id UUID + period_key TEXT + orchestration_data JSONB + computed_at TIMESTAMPTZ DEFAULT NOW() + trace_id TEXT + RLS 자동 적용 CR 0-2 verbatim + audit-first INSERT `reserved_capacity_dry_run_executed` 결정 wire / (3) dry-run CLI flag = `--finops-reserved-capacity-orchestrator-dry-run` 1 NEW CLI flag + dry-run 시 actual reserved_capacity_orchestrator_triggered audit-first INSERT skip 결정 wire + Phase 20 wire `52dad7f` 의 negotiation_bot dry-run 패턴 verbatim EXTENSION.
- **F37.4.5** orchestrator approval workflow 결정 wire = (1) high-value (≥ 10M KRW/year savings) → execution_strategy = `owner_approval_required` + Epic 12 2FA 챌린지 mandatory / (2) low_confidence → execution_strategy = `low_confidence_skipped` + alert Slack DM to tenant_owner / (3) auto_execute_ready → trigger Phase 18 commitment purchase_recommendation / (4) manual_review_required → tenant_admin 이 admin UI 에서 review 후 approve / (5) audit-first INSERT `reserved_capacity_orchestrator_triggered` with approval_chain JSONB 결정 wire + AD-22 owner-only RBAC EXTENSION + Epic 12 2FA 챌린지 보존 + Phase 20 wire `52dad7f` 의 negotiation_bot owner approval flow 패턴 verbatim EXTENSION 결정 wire.
- **F37.4.6** orchestrator idempotency 결정 wire = idempotency_key = (tenant_id, period_key, scope) + duplicate trigger 시 동일 idempotency_key 발견 → skip + audit-first INSERT `reserved_capacity_orchestrator_triggered` with `duplicate_skipped=true` flag 결정 wire + Phase 19 wire `8db3cfc` 의 pricing_scheduled_dispatch_idempotency 패턴 verbatim EXTENSION 결정 wire.
- **F37.4.7** orchestrator audit log 결정 wire = phase_21_orchestration_audit 테이블 NEW + audit_id UUID PK + tenant_id UUID + orchestration_id UUID FK + composition_step_chain enum[] + execution_step_chain enum[] + approval_step_chain JSONB + orchestration_status enum + orchestrated_at TIMESTAMPTZ DEFAULT NOW() + trace_id TEXT + RLS 자동 적용 CR 0-2 verbatim + 5-year retention 결정 wire + Phase 19 wire `8db3cfc` 의 pricing_audit_log 패턴 verbatim EXTENSION + NFR8 audit log 5년 append-only 보존 결정 wire.
- **F37.4.8** orchestrator tests 결정 wire = (1) composition step chain 8 NEW pytest cases (Step 1+2+3+4+5 all executed + partial + skipped) / (2) 4 cadence schedule 4 NEW pytest cases (daily + weekly + monthly + quarterly) / (3) dry-run mode 6 NEW pytest cases (skip audit + preview table + CLI flag) / (4) approval workflow 8 NEW pytest cases (owner_approval_required + low_confidence + auto_execute_ready + manual_review_required) / (5) idempotency + RLS 4 NEW pytest cases = ~30 NEW pytest cases 결정 wire + Phase 20 wire `52dad7f` 의 multi_cloud_orchestration tests 패턴 verbatim EXTENSION.
- **F37.4.9** orchestrator RLS + cross-tenant safety 결정 wire = RLS 자동 적용 CR 0-2 verbatim + tenant_id = current_setting('app.tenant_id')::uuid + cross-tenant query 시 tenant_id mismatch 자동 reject + Epic 12 2FA 챌린지 보존 + audit-first INSERT `reserved_capacity_orchestrator_triggered` with `cross_tenant_attempt=true` flag if attempted 결정 wire + Phase 18 wire `67059cf` 의 commitment_inventory_aggregator RLS 패턴 verbatim EXTENSION.
- **F37.4.10** orchestrator tenant override + on-demand orchestration 결정 wire = (1) per-tenant auto_execute_enabled override (tenant_admin can disable auto-execute) / (2) on-demand orchestration via admin UI (admin can trigger immediate orchestration for specific scope) / (3) per-scope reserved_capacity_tier override (tenant_admin can manually select tier per scope) / (4) orchestration progress indicator (admin can monitor orchestration status) / (5) audit-first INSERT `reserved_capacity_orchestrator_triggered` with `manual_orchestration=true` flag if on-demand triggered 결정 wire + Phase 19 wire `8db3cfc` 의 pricing_tenant_override 패턴 verbatim EXTENSION 결정 wire.
- **F37.4.11** orchestrator cross-tenant invalidation 결정 wire = LISTEN/NOTIFY channel `phase_21_reserved_capacity_orchestrated` 신규 + LISTEN trigger after INSERT on phase_21_reserved_capacity_orchestration table → broadcast to all tenant workers → all RSC page revalidate + CR 0-2 RLS + CR 1-1 audit-first INSERT verbatim EXTENSION + Phase 13 wire `8b98030` LISTEN/NOTIFY 패턴 verbatim EXTENSION 결정 wire.
- **F37.4.12** AD-49 신규 결정 진입 (a)~(g) 7 sub-decisions 결정 wire + 부록 A architectural decisions table 1 NEW row EXTENSION + Phase 20 AD-47 verbatim EXTENSION 결정 wire.

### F37.5 reserved_capacity dashboard UI + 5 sub-components (A577 결정)

- **F37.5.1** `apps/web/app/[locale]/(dashboard)/admin/finops/reserved-capacity/page.tsx` NEW ~+220 LOC + 5 sub-components 결정 wire = (1) DemandForecastPanel (5 FinOps modules cross-rollup + confidence interval visualization + seasonal factor gauge) + (2) CapacityPlanningPanel (capacity gap + reserved capacity tier selection + 4 industries baseline visualization) + (3) CommitmentRecommendationPanel (recommendation queue + confidence + risk score + owner approval flow) + (4) ReservedCapacityOrchestratorPanel (composition step chain visualization + 4 cadence schedule + dry-run toggle + auto-execute settings) + (5) ReservedCapacityTrendMiniChart (3-month reserved_capacity_krw + savings trend + headroom trend) 5 NEW sub-components 결정 wire + Phase 20 wire `52dad7f` 의 multi_cloud dashboard 5 sub-components 패턴 verbatim EXTENSION 결정 wire.
- **F37.5.2** `apps/web/components/finops/FinopsReservedCapacityDashboardPanel.tsx` NEW Client component ~+250 LOC + 5-tab layout (Demand Forecast / Capacity Planning / Commitment Recommendation / Orchestrator / Trend) + 5-tab 별 Recharts visualization (AreaChart for demand forecast confidence interval + BarChart for capacity gap by scope + LineChart for savings trend + GanttChart for 4 cadence schedule timeline + PieChart for tier distribution) 결정 wire + Phase 20 wire `52dad7f` 의 FinopsMultiCloudDashboardPanel.tsx 패턴 verbatim EXTENSION 결정 wire.
- **F37.5.3** `apps/web/app/[locale]/(dashboard)/admin/finops/reserved-capacity/layout.tsx` NEW ~+100 LOC + owner-only RBAC AD-22 verbatim + Epic 12 2FA 챌린지 mandatory + ko-KR.json `finops_reserved_capacity.*` namespace EXTENSION ~30 keys (CR 11-4 D-002 verbatim SSOT) + ARIA labels WCAG 2.1 AA + `(dashboard)` route group 보호 + 미허용 tenant 의 reserved_capacity dashboard 진입 차단 결정 wire + Phase 20 wire `52dad7f` 의 multi_cloud dashboard layout 패턴 verbatim EXTENSION 결정 wire.
- **F37.5.4** `apps/web/lib/finops/reserved-capacity-types.ts` NEW TypeScript mirror 결정 wire + `ReservedCapacityDemandForecast` + `ReservedCapacityPlan` + `CommitmentRecommendation` + `ReservedCapacityOrchestration` 4 NEW TypeScript interfaces 결정 wire + CR 12-5 D-PARITY-01 inversion TypeScript mirror 결정 wire + Phase 20 wire `52dad7f` 의 `multi-cloud-types.ts` pattern verbatim EXTENSION.
- **F37.5.5** `apps/web/lib/finops/reserved-capacity-client.ts` NEW TypeScript client 결정 wire + `aggregateDemandForecast()` + `planReservedCapacity()` + `generateCommitmentRecommendation()` + `orchestrateReservedCapacity()` 4 NEW methods EXTENSION 결정 wire + Phase 20 wire `52dad7f` 의 `multi-cloud-client.ts` pattern verbatim EXTENSION.
- **F37.5.6** `apps/web/messages/ko-KR.json` MODIFIED EXTENSION ~30 keys 결정 wire + `finops_reserved_capacity.dashboard.title` = "예약 용량 계획" + `finops_reserved_capacity.demand_forecast.title` = "수요 예측 (5 모듈 크로스롤업)" + `finops_reserved_capacity.capacity_planning.title` = "예약 용량 등급 선택" + `finops_reserved_capacity.commitment_recommendation.title` = "약정 추천 엔진" + `finops_reserved_capacity.orchestrator.title` = "예약 용량 오케스트레이터" + ~30 keys EXTENSION + NFR18 ko-KR SSOT 보존 결정 wire + CR 11-4 D-002 verbatim EXTENSION.
- **F37.5.7** reserved_capacity dashboard vitest tests 결정 wire = (1) DemandForecastPanel 1 + (2) CapacityPlanningPanel 1 + (3) CommitmentRecommendationPanel 1 + (4) ReservedCapacityOrchestratorPanel 1 + (5) ReservedCapacityTrendMiniChart 1 + (6) ko-KR SSOT 2 + (7) finops reserved_capacity dashboard parity CR 12-5 D-PARITY-01 = ~7 NEW vitest PASS 결정 wire + Phase 20 wire `52dad7f` 의 multi_cloud dashboard vitest pattern verbatim EXTENSION.
- **F37.5.8** reserved_capacity dashboard Recharts 2.12.7 AD-14 stack pin EXTENSION 결정 wire + 5 NEW charts (AreaChart + BarChart + LineChart + GanttChart + PieChart) + 4 industries baseline (manufacturing + service + manufacturing_service + saas) visualization 차이 EXTENSION + Phase 20 wire `52dad7f` 의 multi_cloud dashboard Recharts 패턴 verbatim EXTENSION 결정 wire.
- **F37.5.9** reserved_capacity dashboard owner approval flow 결정 wire = CommitmentRecommendationPanel 진입 시 owner-only RBAC AD-22 verbatim + Epic 12 2FA 챌린지 mandatory + auto-execute enable 시 tenant_owner_id 의 2nd factor 인증 required + audit-first INSERT `commitment_recommendation_generated` with `approval_required=true` flag 결정 wire + Phase 20 wire `52dad7f` 의 negotiation_bot approval flow 패턴 verbatim EXTENSION 결정 wire.
- **F37.5.10** reserved_capacity dashboard export + dispatch EXTENSION 결정 wire = (1) Reserved capacity plan dashboard export PDF (reportlab 4.0.7 + Korean font registration Pretendard-Regular.ttf) + CSV (pandas 2.1.4 + openpyxl 3.1.2) + Excel (xlsxwriter 3.1.9) 3 format / (2) scheduled dispatch KST cron weekly Mon 09:00 + monthly 1st-day 09:00 + quarterly 1st-day 09:00 + annual Jan-1 09:00 / (3) Slack + Email + S3 archive + MS Teams webhook 4 recipients / (4) AD-22 owner-only RBAC + Epic 12 2FA 챌린지 + audit-first INSERT `reserved_capacity_dashboard_viewed` with `dispatch_triggered=true` flag 결정 wire + Phase 20 wire `52dad7f` 의 multi_cloud dashboard export 패턴 verbatim EXTENSION 결정 wire.
- **F37.5.11** reserved_capacity dashboard dry-run mode EXTENSION 결정 wire = (1) ReservedCapacityOrchestratorPanel 진입 시 dry-run mode toggle (default: dry-run) + (2) dry-run 모드에서 orchestrator trigger 시 actual `reserved_capacity_orchestrator_triggered` audit-first INSERT skip / (3) dry-run 결과 preview = phase_21_orchestration_preview 1 table 결정 wire + Phase 20 wire `52dad7f` 의 multi_cloud dashboard dry-run 패턴 verbatim EXTENSION 결정 wire.
- **F37.5.12** AD-49 신규 결정 진입 (a)~(g) 7 sub-decisions 결정 wire + 부록 A architectural decisions table 1 NEW row EXTENSION + Phase 20 AD-47 verbatim EXTENSION 결정 wire.

### F37.6 Capability matrix v1.47 EXTENSION FINOPS_RESERVED_CAPACITY_PLANNING (A578 결정)

- **F37.6.1** `docs/capability-matrix.md` MODIFIED 결정 wire + v1.46 → v1.47 EXTENSION + FINOPS_RESERVED_CAPACITY_PLANNING 1 NEW row after FINOPS_MULTI_CLOUD_UNIFIED_RECONCILIATION industry-agnostic 4-industry grants ✅/✅/✅/✅ CR 12-1 L4 precedent verbatim 미러 EXTENSION + FINOPS_MULTI_CLOUD_UNIFIED_RECONCILIATION Phase 20 wire + FINOPS_PRICING Phase 19 wire + FINOPS_COMMITMENT Phase 18 wire + FINOPS_SUSTAINABILITY Phase 17 wire + FINOPS_REPORTING Phase 16 wire + FINOPS_TAG_GOVERNANCE Phase 15 wire + FINOPS_OPTIMIZATION Phase 14 wire + FINOPS_FORECASTING_CAPACITY_PLANNING Phase 13 wire + FINOPS_ANOMALY_DETECTION + FINOPS_BUDGET_ALERT Phase 12 wire + FINOPS_SHOWBACK + FINOPS_CHARGEBACK Phase 11 wire + SLO_ENGINEERING Phase 10 wire + CHAOS_ENGINEERING Phase 9 wire + PERFORMANCE_TESTING Phase 8 wire + OBSERVABILITY_TRACES + OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind EXTENSION 결정 wire.
- **F37.6.2** `apps/api/core/capability.py` MODIFIED 결정 wire + `Capability.FINOPS_RESERVED_CAPACITY_PLANNING = "finops_reserved_capacity_planning"` 1 NEW enum EXTENSION 결정 wire.
- **F37.6.3** `apps/api/dependencies/capability.py` MODIFIED 결정 wire + `require_finops_reserved_capacity` 1 NEW dep EXTENSION 결정 wire.
- **F37.6.4** `apps/api/core/audit_action.py` MODIFIED 결정 wire + `ActionClass.FINOPS_RESERVED_CAPACITY_PLANNING = "finops_reserved_capacity_planning"` 1 NEW enum EXTENSION 결정 wire + `FinopsReservedCapacityAction` 8 NEW Literal EXTENSION 결정 wire (reserved_capacity_dashboard_viewed + demand_forecast_calculated + capacity_planning_recommended + commitment_recommendation_generated + reserved_capacity_dry_run_executed + reserved_capacity_kpi_refreshed + reserved_capacity_commitment_executed + reserved_capacity_orchestrator_triggered).
- **F37.6.5** `apps/api/core/errors.py` MODIFIED 결정 wire + 16 NEW typed exception classes EXTENSION 결정 wire (ReservedCapacityDemandForecastError(500) + ReservedCapacityDemandForecastScopeError(404) + ReservedCapacityDemandForecastPeriodError(422) + ReservedCapacityDemandForecastModuleError(502) + ReservedCapacityPlanningError(500) + ReservedCapacityPlanningScopeError(404) + ReservedCapacityPlanningTierError(422) + ReservedCapacityPlanningGuardError(500) + ReservedCapacityRecommendationError(500) + ReservedCapacityRecommendationConfidenceError(500) + ReservedCapacityRecommendationApprovalError(500) + ReservedCapacityRecommendationExecutionError(500) + ReservedCapacityOrchestratorError(500) + ReservedCapacityOrchestratorStepError(500) + ReservedCapacityDryRunError(500) + ReservedCapacityIdempotencyError(500)) CR 12-5 D-14 envelope.
- **F37.6.6** `apps/api/modules/finops/__init__.py` MODIFIED 결정 wire = reserved_capacity submodule export + Phase 20 wire `52dad7f` 의 multi_cloud submodule export pattern verbatim EXTENSION + ALLOWED_SERVICE_SUBMODULES (CR 11-3 lesson) 즉시 sweep EXTENSION = m21_finops_reserved_capacity 신규 submodule 등록 + Phase 11~20 verbatim EXTENSION (Phase 20 m20_finops_multi_cloud 패턴 보존) 결정 wire.
- **F37.6.7** alembic 0053 phase_21_reserved_capacity_planning.py NEW 8 NEW tables 결정 wire = phase_21_reserved_capacity_demand_forecast + phase_21_demand_forecast_audit + phase_21_reserved_capacity_plan + phase_21_capacity_planning_audit + phase_21_commitment_recommendation + phase_21_commitment_recommendation_audit + phase_21_reserved_capacity_orchestration + phase_21_orchestration_audit + phase_21_orchestration_preview + RLS 8 tables + 1 preview table 결정 wire + Phase 20 wire `52dad7f` 의 alembic 0052 8 tables + RLS 8 tables + 3 preview tables 패턴 verbatim EXTENSION 결정 wire.
- **F37.6.8** `tests/integration/test_capability_matrix_v1_47_drift.py` NEW 8 NEW pytest cases EXTENSION 결정 wire = FINOPS_RESERVED_CAPACITY_PLANNING row presence + 4-industry grants ✅/✅/✅/✅ + Phase 11~20 FINOPS_* capability rows preserved + AD-22 owner-only pattern + CR 12-1 L4 verbatim + drift detector EXTENSION + Phase 20 wire `52dad7f` 의 test_capability_matrix_v1_46_drift.py pattern verbatim EXTENSION 결정 wire.
- **F37.6.9** `tests/integration/test_audit_action_v1_47_drift.py` NEW 6 NEW pytest cases EXTENSION 결정 wire = ActionClass.FINOPS_RESERVED_CAPACITY_PLANNING enum presence + FinopsReservedCapacityAction 8 Literal + __all__ EXTENSION + Phase 11~20 ActionClass.FINOPS_* enum preserved + drift detector EXTENSION 결정 wire.
- **F37.6.10** `apps/web/lib/finops/reserved-capacity-types.ts` NEW TypeScript mirror 결정 wire + Phase 20 wire `52dad7f` 의 `multi-cloud-types.ts` pattern verbatim EXTENSION + CR 12-5 D-PARITY-01 inversion TypeScript mirror 결정 wire.
- **F37.6.11** `apps/web/lib/finops/reserved-capacity-client.ts` NEW TypeScript client 결정 wire + `aggregateDemandForecast()` + `planReservedCapacity()` + `generateCommitmentRecommendation()` + `orchestrateReservedCapacity()` 4 NEW methods EXTENSION 결정 wire + Phase 20 wire `52dad7f` 의 `multi-cloud-client.ts` pattern verbatim EXTENSION.
- **F37.6.12** AD-49 신규 결정 진입 (a)~(g) 7 sub-decisions 결정 wire + 부록 A architectural decisions table 1 NEW row EXTENSION + Phase 20 AD-47 verbatim EXTENSION 결정 wire.

### F37.7 audit action EXTENSION 8 NEW + 16 NEW typed exception classes (A578 결정)

- **F37.7.1** audit action EXTENSION 8 NEW 결정 wire = `reserved_capacity_dashboard_viewed` + `demand_forecast_calculated` + `capacity_planning_recommended` + `commitment_recommendation_generated` + `reserved_capacity_dry_run_executed` + `reserved_capacity_kpi_refreshed` + `reserved_capacity_commitment_executed` + `reserved_capacity_orchestrator_triggered` 8 NEW via ActionClass.FINOPS_RESERVED_CAPACITY_PLANNING 결정 wire + CR 1-1 verbatim 적용 + Phase 20 wire `52dad7f` 의 8 NEW audit actions 패턴 verbatim EXTENSION.
- **F37.7.2** 16 NEW typed exception classes 결정 wire = ReservedCapacityDemandForecastError(500) + ReservedCapacityDemandForecastScopeError(404) + ReservedCapacityDemandForecastPeriodError(422) + ReservedCapacityDemandForecastModuleError(502) + ReservedCapacityPlanningError(500) + ReservedCapacityPlanningScopeError(404) + ReservedCapacityPlanningTierError(422) + ReservedCapacityPlanningGuardError(500) + ReservedCapacityRecommendationError(500) + ReservedCapacityRecommendationConfidenceError(500) + ReservedCapacityRecommendationApprovalError(500) + ReservedCapacityRecommendationExecutionError(500) + ReservedCapacityOrchestratorError(500) + ReservedCapacityOrchestratorStepError(500) + ReservedCapacityDryRunError(500) + ReservedCapacityIdempotencyError(500) 결정 wire + CR 12-5 D-14 envelope 적용 + Phase 20 wire `52dad7f` 의 16 NEW typed exceptions 패턴 verbatim EXTENSION.
- **F37.7.3** ko-KR.json EXTENSION ~30 keys 결정 wire + `finops_reserved_capacity.*` namespace EXTENSION + ARIA labels WCAG 2.1 AA + NFR18 ko-KR SSOT 보존 + CR 11-4 D-002 verbatim EXTENSION.
- **F37.7.4** reserved_capacity wire T1~T8 결정 wire = T1 demand_forecast_aggregator + capacity_planning_aggregator + commitment_recommendation_engine + reserved_capacity_orchestrator 결정 wire + T2 reserved_capacity_dashboard 5 NEW sub-components + Clients component + TypeScript mirror + ko-KR.json extension 결정 wire + T3 alembic 0053 phase_21_reserved_capacity_planning 8 tables + RLS 8 tables + 1 preview table 결정 wire + T4 audit action EXTENSION 8 NEW + 16 NEW typed exception classes 결정 wire + T5 capability v1.47 EXTENSION + frontend finops reserved_capacity dashboard 결정 wire + T6 scheduled_dispatch_job wire 결정 wire + T7 dry-run mode + 1 NEW CLI flag 결정 wire + T8 3중 게이트 FINAL CLEAN atomic commit 결정 wire + estimated ~+25-30 files estimate = ~+18 NEW + ~+12 MODIFIED 결정 wire + Phase 20 wire `52dad7f` 의 ~28 files pattern verbatim EXTENSION 결정 wire + Phase 11~20 wire 의 ~25-30 files pattern verbatim EXTENSION 결정 wire.

### F37.8 dry-run + Tests + wire scope T1~T8 (A578 결정)

- **F37.8.1** dry-run mode 결정 wire = `--finops-reserved-capacity-orchestrator-dry-run` 1 NEW CLI flag + dry-run 시 actual `reserved_capacity_orchestrator_triggered` audit-first INSERT skip + Phase 20 wire `52dad7f` 의 5 NEW CLI flags 패턴 verbatim EXTENSION 결정 wire.
- **F37.8.2** dry-run preview 결과 결정 wire = `phase_21_orchestration_preview` 1 table 결정 wire + preview_id UUID PK + tenant_id UUID + period_key TEXT + orchestration_data JSONB + computed_at TIMESTAMPTZ DEFAULT NOW() + trace_id TEXT + RLS 자동 적용 CR 0-2 verbatim + audit-first INSERT `reserved_capacity_dry_run_executed` 결정 wire + Phase 20 wire `52dad7f` 의 3 preview tables 패턴 verbatim EXTENSION.
- **F37.8.3** dry-run CLI flag 1 NEW 결정 wire + Phase 20 wire `52dad7f` 의 5 NEW CLI flags 패턴 verbatim EXTENSION + 1 CLI flag 모두 wire 결정 wire + dry-run 결과 preview 결정 wire.
- **F37.8.4** tests 결정 wire = ~+108 NEW pytest PASS (demand_forecast_aggregator 24 + capacity_planning_aggregator 30 + commitment_recommendation_engine 24 + reserved_capacity_orchestrator 30 = ~108 NEW pytest PASS) 결정 wire + Phase 20 wire `52dad7f` 의 ~92 NEW pytest PASS 패턴 verbatim EXTENSION.
- **F37.8.5** vitest tests ~+7 NEW vitest PASS 결정 wire (DemandForecastPanel 1 + CapacityPlanningPanel 1 + CommitmentRecommendationPanel 1 + ReservedCapacityOrchestratorPanel 1 + ReservedCapacityTrendMiniChart 1 + ko-KR SSOT 2 + finops reserved_capacity dashboard parity CR 12-5 D-PARITY-01 결정 wire) + Phase 20 wire `52dad7f` 의 ~7 NEW vitest PASS 패턴 verbatim EXTENSION.
- **F37.8.6** ruff + tsc 0 NEW + SDR drift gate 결정 wire + A36 SDR 검증 4-step 자동 적용 결정 wire + 0 NEW ruff + 0 NEW tsc + 0 regressions + SDR drift gate PASS 결정 wire.
- **F37.8.7** wire scope T1~T8 결정 wire = T1 demand_forecast_aggregator + capacity_planning_aggregator + commitment_recommendation_engine + reserved_capacity_orchestrator 결정 wire + T2 reserved_capacity_dashboard 5 NEW sub-components + Clients component + TypeScript mirror + ko-KR.json extension 결정 wire + T3 alembic 0053 phase_21_reserved_capacity_planning 8 tables + RLS 8 tables + 1 preview table 결정 wire + T4 audit action EXTENSION 8 NEW + 16 NEW typed exception classes 결정 wire + T5 capability v1.47 EXTENSION + frontend finops reserved_capacity dashboard 결정 wire + T6 scheduled_dispatch_job wire 결정 wire + T7 dry-run mode + 1 NEW CLI flag 결정 wire + T8 3중 게이트 FINAL CLEAN atomic commit 결정 wire + estimated ~+25-30 files estimate = ~+18 NEW + ~+12 MODIFIED 결정 wire + Phase 20 wire `52dad7f` 의 ~30-35 files pattern verbatim EXTENSION 결정 wire + Phase 11~20 wire 의 ~25-30 files pattern verbatim EXTENSION.
- **F37.8.8** A19 cohesion pattern 9 surface EXTENSION PASS 결정 wire (FinOps Reserved Capacity Planning surface NEW = F37.1~F37.8 결정 wire) + Phase 20 wire `52dad7f` 의 FinOps Multi-Cloud Cost Unified Reconciliation surface NEW = F36.1~F36.8 pattern verbatim 미러 + Phase 19 wire `8db3cfc` 의 FinOps Pricing, Rate Card & TCO Modeling surface NEW = F35.1~F35.8 pattern verbatim 미러 + Phase 18 wire `67059cf` 의 FinOps Cloud Commitment Management surface NEW = F34.1~F34.8 pattern verbatim 미러 + Phase 17 wire `97cfe4e` 의 FinOps Sustainability & Carbon Reporting surface NEW = F33.1~F33.8 pattern verbatim 미러 + Phase 16 wire `81ae00a` 의 FinOps Reporting & Executive Dashboard surface NEW = F32.1~F32.8 pattern verbatim 미러 + Phase 15 wire `1b800d9` 의 FinOps Tag Governance surface NEW = F31.1~F31.8 pattern verbatim 미러 + Phase 14 wire `e904485` 의 FinOps Optimization surface NEW = F30.1~F30.8 pattern verbatim 미러 + Phase 13 wire `8b98030` 의 FinOps Forecasting surface NEW = F29.1~F29.8 pattern verbatim 미러 + Phase 12 wire `f3c0e63` 의 FinOps Anomaly surface NEW = F28.1~F28.8 pattern verbatim 미러 + Phase 11 wire `e020ad0` 의 FinOps Showback surface NEW = F27.1~F27.7 pattern verbatim 미러 결정 wire.
- **F37.8.9** CR lessons applied 18종 결정 wire (CR 0-2 RLS lesson ✅ APPLIED + CR 1-1 audit-first INSERT ✅ APPLIED (8 NEW audit log entries + 5-module source attribution 5행 append = 13 NEW effective audit events) + CR 4-3/4-4 lessons carry ✅ APPLIED + CR 1-1 ContextVar lesson ✅ APPLIED + CR 1-1 RSC boundary lesson ✅ APPLIED + CR 9-6 commit message discipline ✅ APPLIED + CR 11-3 honest-DEFER discipline ✅ APPLIED (149번째 epic 연속 정직 회복) + CR 11-4 D-001~D-005 + P-015 ✅ APPLIED + CR 12-1 L4 ✅ APPLIED + CR 12-5 D-14 typed exception envelope ✅ APPLIED (16 NEW typed exception classes) + CR 12-5 D-PARITY-01 inversion ✅ APPLIED + CR 12-5 D-GATE-01 inversion ✅ APPLIED + A36 SDR 검증 4-step 자동 적용 ✅ + AD-14 stack pin ✅ APPLIED + AD-22 owner-only RBAC ✅ APPLIED + NFR4 PII minimization ✅ PRESERVED) 결정 wire.
- **F37.8.10** D-DEFER-* honestly 결정 wire (CR 11-3 149번째 epic 연속 정직 회복 결정 wire 보존) = D-1-1-DEFER-1/2/3 RESOLVED 보존 60~149번째 + D-LAUNCH-1-DEFER-1 honestly preserved 65~149번째 + D-EPIC-16-REVIEW-DEFER-2~6 ✅ RESOLVED 보존 78~149번째 + D-PHASE-4-DR-DEFER-1/2 ✅ RESOLVED 보존 73~149번째 + D-EPIC-17-WIRE-DEFER-T2-T3-UI ✅ RESOLVED 보존 83~149번째 + D-RETENTION-1 ✅ RESOLVED 보존 85~149번째 + D-OBSERVABILITY-1 ✅ RESOLVED 보존 89~149번째 + D-PERFORMANCE-1 ✅ RESOLVED 보존 93~149번째 + D-CHAOS-1 ✅ RESOLVED 보존 100~149번째 + D-SLO-1 ✅ RESOLVED 보존 101~149번째 + D-FINOPS-1 ✅ RESOLVED 보존 105~149번째 + D-FINOPS-2 ✅ RESOLVED 보존 109~149번째 + D-FINOPS-3 ✅ RESOLVED 보존 113~149번째 + D-FINOPS-4 ✅ RESOLVED 보존 117~149번째 + D-FINOPS-5 ✅ RESOLVED 보존 121~149번째 + D-FINOPS-6 ✅ RESOLVED 보존 125~149번째 + D-FINOPS-7 ✅ RESOLVED 보존 129~149번째 + D-FINOPS-8 ✅ RESOLVED 보존 133~149번째 + D-FINOPS-9 ✅ RESOLVED 보존 137~149번째 + **D-FINOPS-10 신규 honestly DEFER 보존 + Phase 21 carry-over 결정 wire 의 AD-49 신규 (a)~(g) 7 sub-decisions 모두 결정 wire 진입 = Phase 21 PRD entry 진입 시점에 carry-over chain 정직 회복 결정 wire 진입: Phase 20.5 close-out retro `8505d98` §13 + Phase 20 close-out retro `f361016` §13 + Phase 20 wire `52dad7f` §13 + Phase 19 close-out retro `18ca1ae` §13 + Phase 19 wire `8db3cfc` §13 + Phase 18 close-out retro `de72f50` §13 + Phase 17 close-out retro `de009fe` §13 + Phase 16 close-out retro `26fd530` §13 + Phase 15 close-out retro `102f370` §13 + Phase 14 close-out retro `5b367d9` §13 + Phase 13 close-out retro `850b4f8` §13 + Phase 12 close-out retro `3354e83` §13 + Phase 11 close-out retro `80df15b` §12 + Phase 10 close-out retro `733d428` §10 + Phase 9 close-out retro `634427d` §10 + Phase 8 close-out retro `ab495a8` §10 + Phase 7 close-out retro `326fa9f` §10 + Phase 6 close-out retro `f9f006c` §13 + Epic 17 close-out retro `be8f3bd` §11 "FinOps Reserved Capacity Planning 결정 wire 보류, Phase 21+ 진입 시점" verbatim 해소**) + Epic 1 ~ Epic 17 + Phase 3 ~ Phase 20.5 + 1st release cycle 정합 보존 결정 wire (cj-style 149번째 epic 연속 정직 회복 pre-flight 정합 sweep) + 결정 wire 일자: 2026-08-26 (KST) + **next**: 옵션 (a) Phase 21 bmad-create-story spec entry 진입 (cj-style 150번째 wire 진입 시점) / 옵션 (b) Phase 21 bmad-dev-story atomic wire T1~T8 진입 (cj-style 151번째) / 옵션 (c) Phase 21 close-out retro 진입 (cj-style 152번째) / 옵션 (d) Phase 20.6 Layer 2 P1 + Layer 3 P2 carry-over sprint 진입 / 옵션 (e) audit-fixes sprint 진입 / 옵션 (f) Epic 21+ 진입 결정 wire / 옵션 (g) D-DEFER-* follow-up 결정 wire 보류.

---

# Phase 22 territory 신규 (cj-style 158 PRD entry 결정 wire 진입)

## F38. FinOps Chargeback Settlement (Phase 22 territory 신규)

**Phase 22 결정 wire 진입 rationale (5종)**:
1. **자연스러운 FinOps 확장**: Phase 11/16/17/18/19/20/21 의 FinOps territory chain (보고 → 최적화 → 탄소 → 약정 → 가격 → 멀티클라우드 → reserved capacity) 의 자연스러운 정착 단계 — 비용 정산/배분 자동화 → 직접적 ROI 제공
2. **비즈니스 가치 최고**: chargeback settlement = 비용 정산/배분 자동화 → 비용 회수 직접 발생 → SaaS ARPU 증가
3. **reuse 최대화**: Phase 11 chargeback_engine + Phase 18 commitment + Phase 19 pricing + Phase 20 multi_cloud + Phase 21 reserved_capacity 5 module 의 ledger data 활용 → 새 backend infra 불필요
4. **risk 최소화**: ledger data 기반 정산 계산은 pure function (외부 의존성 없음) → 정합성 보존 용이
5. **A19 cohesion**: Phase 11~21 11-module FinOps territory chain 의 정착 phase, 9 surface EXTENSION 자연스러움

### F38.1 settlement_rules engine + 5 module cross-join EXTENSION (A619 결정)

- **F38.1.1** `apps/api/modules/finops/chargeback_settlement/__init__.py` NEW 결정 wire = m22_finops_chargeback_settlement 신규 submodule 등록 결정 wire + ALLOWED_SERVICE_SUBMODULES 즉시 sweep EXTENSION 결정 wire (CR 11-2 verbatim) + Phase 21 wire `f7d1f41` 의 `m21_finops_reserved_capacity` submodule pattern verbatim EXTENSION 결정 wire.
- **F38.1.2** `apps/api/modules/finops/chargeback_settlement/serializers.py` NEW ~+260 LOC 결정 wire = 4 NEW enums (`SettlementRuleType` flat_fee/proportional_allocation/metered_volume/tag_weighted + `SettlementStatus` draft/pending_approval/approved/invoiced/reconciled + `AllocationDimension` cost_center/department/business_unit/tag/tenant + `InvoiceFormat` pdf/xlsx/csv) + 4 NEW TypedDicts (`SettlementRule` 12 fields + `SettlementResult` 16 fields + `AllocationLine` 10 fields + `ReconciliationResult` 12 fields) + `FIVE_MODULE_WEIGHTS` (Phase 11 chargeback_engine 0.30 + Phase 18 commitment 0.20 + Phase 19 pricing 0.20 + Phase 20 multi_cloud 0.15 + Phase 21 reserved_capacity 0.15) + `SETTLEMENT_CADENCE_HOURS_KST` (monthly 1일 02:00 + quarterly 1일 03:00) + `SETTLEMENT_RECIPIENT_TEMPLATES` (manager_email + finance_team_email + tenant_owner_email) + `SETTLEMENT_DEFAULTS` (settlement_period_monthly + invoice_format_pdf + reconciliation_tolerance_pct=1.0 + high_value_threshold_krw=10M).
- **F38.1.3** `apps/api/modules/finops/chargeback_settlement/settlement_rules.py` NEW ~+220 LOC 결정 wire + `SettlementRule` TypedDict 12 fields (rule_id UUID PK + tenant_id UUID + rule_name TEXT + rule_type enum + rule_scope enum tenant/department/cost_center/business_unit/tag + allocation_basis enum + markup_pct default 0.0 + tax_pct default 0.10 + effective_from DATE + effective_to DATE nullable + created_at + updated_at + trace_id) 결정 wire + `create_settlement_rule(tenant_id, rule_data) -> SettlementRule` 함수 결정 wire + `update_settlement_rule(rule_id, rule_data) -> SettlementRule` 함수 결정 wire + `list_settlement_rules(tenant_id, scope) -> List[SettlementRule]` 함수 결정 wire (CR 12-5 D-14 envelope 결정 wire + 1 NEW error class SettlementRuleNotFoundError(404) + 1 NEW error class SettlementRuleInvalidError(400) + 1 NEW error class SettlementRuleOverlapError(409) CR 12-5 D-14 envelope 결정 wire).
- **F38.1.4** `apps/api/modules/finops/chargeback_settlement/settlement_engine.py` NEW ~+280 LOC 결정 wire + `compute_settlement(tenant_id, period_key, scope) -> SettlementResult` 함수 결정 wire (5-module cross-join EXTENSION: Phase 11 chargeback_engine ledger + Phase 18 commitment cost + Phase 19 pricing rate × usage + Phase 20 multi_cloud cost variance + Phase 21 reserved_capacity commitment cost) + `SettlementResult` TypedDict 16 fields (settlement_id UUID PK + tenant_id + period_key + rule_id + rule_type + total_base_amount + total_markup_amount + total_tax_amount + total_settlement_amount + currency_code + allocation_lines + status enum draft + computed_at + trace_id + actor_id + approved_at nullable) 결정 wire (CR 12-5 D-PARITY-01 verbatim + CR 12-5 D-14 envelope) + Decimal precision 결정 (소수점 2자리, banker's rounding 적용 CR 5-1 verbatim) + per-tenant currency_code 적용 결정 wire.
- **F38.1.5** settlement 의 monthly + quarterly cadence KST 결정 wire = cron KST 매월 1일 02:00 UTC 17:00 (previous month 기준) 결정 wire + cron KST 매분기 1일 03:00 UTC 18:00 (previous quarter 기준) 결정 wire + Phase 11 wire `e020ad0` 의 chargeback_period_monthly cron KST 매월 1일 00:00 패턴 verbatim EXTENSION 결정 wire + audit-first INSERT `settlement_calculated` CR 1-1 verbatim 결정 wire.
- **F38.1.6** settlement 의 multi-region aggregation 결정 wire (Phase 5 wire `f093f8c` `phase_5_replication_lag` table 정합 + primary Seoul + secondary Tokyo replica 결정 wire EXTENSION + cross-region settlement cost variance detection EXTENSION + region_weight_map default `{seoul: 0.6, tokyo: 0.3, singapore: 0.1}` 결정 wire + audit-first INSERT `settlement_calculated_multi_region` CR 1-1 verbatim 결정 wire).
- **F38.1.7** settlement 의 per-tenant override EXTENSION 결정 wire = `tenant_settings.settlement_overrides` JSONB TypedDict 결정 (rule_type override + markup_pct override + tax_pct override + settlement_period override + invoice_format override + recipient_emails override) + policy evaluation precedence tenant override > industry baseline > system default 결정 wire + Phase 11 wire `e020ad0` 의 `tenant_settings.chargeback_overrides` JSONB pattern verbatim 미러 결정 wire.
- **F38.1.8** settlement 의 dry-run mode 결정 wire (`--finops-settlement-dry-run` CLI flag + compute_settlement dry-run parameter + dry-run 시 actual settlement_calculated INSERT skip + dry_run=True default for prod deploys) + dry-run 결과 preview 결정 wire (`phase_22_settlement_preview` 1 NEW table alembic 0054 신규 + tenant_id + period_key + rule_type + preview_amount + preview_currency + preview_recipient_count + computed_at + trace_id) + audit-first INSERT `settlement_dry_run_executed` CR 1-1 verbatim 결정 wire.
- **F38.1.9** test 결정 wire = ~+24 NEW pytest PASS (settlement_rules 8 + settlement_engine 12 + dry-run preview 4 = ~24 NEW pytest PASS) 결정 wire + Phase 21 wire `f7d1f41` 의 ~108 NEW pytest PASS 패턴 verbatim EXTENSION 결정 wire.
- **F38.1.10** vitest tests ~+1 NEW vitest PASS 결정 wire (SettlementRulesForm 1 + ko-KR SSOT 결정 wire + finops settlement parity CR 12-5 D-PARITY-01 결정 wire) + Phase 21 wire `f7d1f41` 의 ~7 NEW vitest PASS 패턴 verbatim EXTENSION 결정 wire.

### F38.2 allocation_engine + 5-dimension weighted allocation (A620 결정)

- **F38.2.1** `apps/api/modules/finops/chargeback_settlement/allocation_engine.py` NEW ~+260 LOC 결정 wire + `allocate_settlement(tenant_id, settlement_id, dimensions) -> List[AllocationLine]` 함수 결정 wire (5-dimension allocation: cost_center + department + business_unit + tag + tenant) + `AllocationLine` TypedDict 10 fields (allocation_id UUID PK + tenant_id + settlement_id + dimension enum + dimension_value TEXT + allocated_amount + currency_code + allocation_pct + created_at + trace_id) 결정 wire (CR 12-5 D-PARITY-01 verbatim + CR 12-5 D-14 envelope) + 5-dimension weight default `{cost_center: 0.30, department: 0.25, business_unit: 0.20, tag: 0.15, tenant: 0.10}` 결정 wire.
- **F38.2.2** 5-dimension allocation 의 precedence 결정 wire = tenant override dimension weight > industry baseline weight > system default weight 결정 wire + Phase 11 wire `e020ad0` 의 tenant_settings chargeback_overrides precedence pattern verbatim EXTENSION 결정 wire.
- **F38.2.3** allocation 의 total verification 결정 wire = sum of allocated_amount across all allocation_lines == settlement.total_settlement_amount (정합성 검증, ±0.01 KRW tolerance) + 불일치 시 1 NEW error class AllocationMismatchError(500) CR 12-5 D-14 envelope 결정 wire + audit-first INSERT `allocation_verified` CR 1-1 verbatim 결정 wire.
- **F38.2.4** allocation 의 zero-amount handling 결정 wire = dimension_value 의 allocated_amount == 0 인 경우 skip (allocation_line 미생성) 결정 wire + negative-amount handling 결정 wire (refund/adjustment 의 경우 allocation_pct 적용 후 음수 유지) 결정 wire + currency_code per-allocation 결정 wire (Phase 11 chargeback_engine 의 per-tenant currency_code EXTENSION verbatim 미러) 결정 wire.
- **F38.2.5** test 결정 wire = ~+18 NEW pytest PASS (allocation_engine 12 + zero-amount handling 3 + negative-amount handling 3 = ~18 NEW pytest PASS) 결정 wire.
- **F38.2.6** vitest tests ~+1 NEW vitest PASS 결정 wire (AllocationTable 1 + ko-KR SSOT 결정 wire + finops settlement parity CR 12-5 D-PARITY-01 결정 wire).

### F38.3 invoice_generation + PDF/XLSX template (A621 결정)

- **F38.3.1** `apps/api/modules/finops/chargeback_settlement/invoice_generator.py` NEW ~+220 LOC 결정 wire + `generate_settlement_invoice(settlement_id, format) -> bytes` 함수 결정 wire + 3 format 지원 (PDF + XLSX + CSV) 결정 wire + per-tenant currency_code 적용 결정 wire + owner-only RBAC AD-22 verbatim 결정 wire + `require_role("owner")` FastAPI Dependency 결정 wire + Epic 12 2FA 챌린지 보존 결정 wire.
- **F38.3.2** PDF generation 결정 wire = reportlab 기반 PDF generation 결정 wire + company logo (Phase 1 wire 의 branding assets 정합) + department breakdown 차트 PDF 임베드 (Recharts → matplotlib → PDF 변환 또는 reportlab.graphics 결정 wire) + page layout 결정 wire (title + period + tenant_slug + settlement summary + allocation breakdown + recipient list + footer + page numbers) + reportlab AD-14 stack pin 결정 wire (`reportlab==4.0.7` 결정) + xlsxwriter AD-14 stack pin 결정 wire (`xlsxwriter==3.1.9` 결정).
- **F38.3.3** PDF/XLSX 의 font 결정 wire = 한글 폰트 NOTO Sans CJK KR 결정 wire + `noto-sans-cjk-kr` AD-14 stack pin 결정 wire + page size A4 결정 wire + orientation landscape 결정 wire (allocation breakdown wide table) 결정 wire + filename 결정 `settlement-{tenant_slug}-{period_key}.{ext}` 결정 wire.
- **F38.3.4** invoice recipient list 결정 wire = tenant_settings.settlement_overrides.recipient_emails override > SETTLEMENT_RECIPIENT_TEMPLATES (manager_email + finance_team_email + tenant_owner_email) fallback 결정 wire + recipient 가 0명 인 경우 1 NEW error class SettlementRecipientMissingError(400) CR 12-5 D-14 envelope 결정 wire.
- **F38.3.5** invoice 의 audit-first INSERT `settlement_invoice_generated` 결정 wire (CR 1-1 verbatim, action_class='FINOPS' + action='settlement_invoice_generated' + actor_id + tenant_id + period_key + invoice_format enum pdf/xlsx/csv + recipient_count + total_settlement_amount + file_size_bytes + trace_id 정합 + per-tenant RLS 자동 적용 결정 wire + 누가 언제 어떤 format 으로 invoice 생성했는지 추적 결정 wire + Phase 11 wire `e020ad0` 의 chargeback_exported audit pattern verbatim 미러 결정 wire).
- **F38.3.6** invoice 의 rate limit 결정 wire (Phase 10 wire `ac5d6c5` 의 SLO breach rate limit EXTENSION pattern verbatim 미러) = owner 1 invoice / minute (default) + rate limit 초과 시 429 status 결정 wire + `Retry-After` header 결정 wire + 1 NEW error class SettlementInvoiceRateLimitedError(429) CR 12-5 D-14 envelope 결정 wire + audit-first INSERT `settlement_invoice_rate_limited` CR 1-1 verbatim 결정 wire.
- **F38.3.7** test 결정 wire = ~+18 NEW pytest PASS (invoice_generator 12 + rate limit 3 + recipient override 3 = ~18 NEW pytest PASS) 결정 wire.
- **F38.3.8** vitest tests ~+1 NEW vitest PASS 결정 wire (InvoicePreviewPanel 1 + ko-KR SSOT 결정 wire + finops settlement parity CR 12-5 D-PARITY-01 결정 wire).

### F38.4 reconciliation 3-way match + 3-way diff (A622 결정)

- **F38.4.1** `apps/api/modules/finops/chargeback_settlement/reconciliation.py` NEW ~+260 LOC 결정 wire + `reconcile_settlement(settlement_id, tolerance_pct=1.0) -> ReconciliationResult` 함수 결정 wire (3-way match: settlement ↔ invoice ↔ allocation 합계 비교) + `ReconciliationResult` TypedDict 12 fields (reconciliation_id UUID PK + tenant_id + settlement_id + settlement_amount + invoice_amount + allocation_amount + diff_settlement_invoice + diff_settlement_allocation + reconciliation_status enum matched/mismatched/within_tolerance + tolerance_pct + reconciled_at + trace_id) 결정 wire (CR 12-5 D-PARITY-01 verbatim + CR 12-5 D-14 envelope).
- **F38.4.2** 3-way match tolerance 결정 wire = default 1.0% tolerance 결정 wire + per-tenant override EXTENSION (`tenant_settings.settlement_overrides.reconciliation_tolerance_pct` JSONB) 결정 wire + tolerance_pct range 0~10 + 0.01 step granularity 결정 wire + tolerance 초과 시 reconciliation_status='mismatched' 결정 wire + 자동 alert 발송 결정 wire (Phase 12 wire `f3c0e63` 의 budget_alert pattern verbatim EXTENSION).
- **F38.4.3** reconciliation 의 auto-retry 결정 wire = mismatched 상태 시 3회 자동 retry (5분 간격) 결정 wire + retry 모두 실패 시 1 NEW error class SettlementReconciliationFailedError(500) CR 12-5 D-14 envelope 결정 wire + admin email 알림 결정 wire.
- **F38.4.4** reconciliation 의 approval flow 결정 wire = high-value settlement (≥ 10M KRW/year) → Epic 12 2FA 챌린지 mandatory + tenant_owner approval chain 결정 wire (Slack DM + 2FA + approval_chain) + Phase 21 wire `f7d1f41` 의 reserved_capacity_orchestrator owner approval flow pattern verbatim EXTENSION 결정 wire.
- **F38.4.5** reconciliation 의 audit-first INSERT `settlement_reconciled` 결정 wire (CR 1-1 verbatim, action_class='FINOPS' + action='settlement_reconciled' + actor_id + tenant_id + period_key + settlement_amount + invoice_amount + allocation_amount + reconciliation_status + tolerance_pct + trace_id 정합 + per-tenant RLS 자동 적용 결정 wire).
- **F38.4.6** test 결정 wire = ~+18 NEW pytest PASS (reconciliation 12 + auto-retry 3 + approval flow 3 = ~18 NEW pytest PASS) 결정 wire.
- **F38.4.7** vitest tests ~+1 NEW vitest PASS 결정 wire (ReconciliationPanel 1 + ko-KR SSOT 결정 wire + finops settlement parity CR 12-5 D-PARITY-01 결정 wire).

### F38.5 chargeback settlement dashboard UI + 5 sub-components (A623 결정)

- **F38.5.1** `apps/web/app/[locale]/(dashboard)/admin/finops/chargeback-settlement/page.tsx` NEW ~+220 LOC 결정 wire + 5 sub-components (SettlementRulesCard + AllocationBreakdownPanel + InvoicePreviewPanel + ReconciliationStatusPanel + SettlementTrendMiniChart) + `apps/web/components/finops/FinopsChargebackSettlementDashboardPanel.tsx` NEW Client component ~+250 LOC 결정 wire + 5-tab layout 결정 wire (Rules + Allocation + Invoice + Reconciliation + History) + Recharts 2.12.7 AD-14 stack pin 결정 wire + owner-only RBAC AD-22 결정 wire + Epic 12 2FA 챌린지 보존 결정 wire + ko-KR.json `finops_chargeback_settlement.*` namespace EXTENSION ~30 keys 결정 wire (NFR18 ko-KR SSOT 결정 wire).
- **F38.5.2** SettlementRulesCard 결정 wire = tenant 의 settlement_rules 목록 (rule_id + rule_name + rule_type + effective_from + effective_to + status) + CRUD action (create/update/delete) + dry-run preview action + ko-KR.json `finops_chargeback_settlement.rules.*` namespace EXTENSION 결정 wire.
- **F38.5.3** AllocationBreakdownPanel 결정 wire = tenant 의 settlement 의 5-dimension allocation_breakdown (cost_center + department + business_unit + tag + tenant) + 각 dimension 의 allocated_amount + allocation_pct 시각화 (Recharts pie chart) + ko-KR.json `finops_chargeback_settlement.allocation.*` namespace EXTENSION 결정 wire.
- **F38.5.4** InvoicePreviewPanel 결정 wire = tenant 의 settlement 의 invoice 미리보기 (PDF preview iframe + XLSX preview table + CSV preview text) + download action + rate limit indicator (1 invoice / minute) + ko-KR.json `finops_chargeback_settlement.invoice.*` namespace EXTENSION 결정 wire.
- **F38.5.5** ReconciliationStatusPanel 결정 wire = tenant 의 settlement 의 reconciliation_status (matched/mismatched/within_tolerance) + 3-way diff 시각화 (settlement_amount ↔ invoice_amount ↔ allocation_amount) + tolerance_pct 표시 + auto-retry counter + ko-KR.json `finops_chargeback_settlement.reconciliation.*` namespace EXTENSION 결정 wire.
- **F38.5.6** SettlementTrendMiniChart 결정 wire = 최근 12개월 settlement_amount 추이 (Recharts line chart) + high-value settlement 표시 (≥ 10M KRW 빨강 표시) + Epic 12 2FA 챌린지 발동 indicator + ko-KR.json `finops_chargeback_settlement.trend.*` namespace EXTENSION 결정 wire.
- **F38.5.7** TypeScript mirror EXTENSION 결정 wire = Phase 11 chargeback_engine TS mirror + Phase 21 reserved_capacity TS mirror pattern verbatim EXTENSION 결정 wire + CR 12-5 D-PARITY-01 inversion 적용 보존 결정 wire + drift detector EXTENSION 결정 wire (cj-style 154 의 Phase 11~20 audit-fixes structural test pattern verbatim EXTENSION).
- **F38.5.8** test 결정 wire = ~+24 NEW vitest PASS 결정 wire (SettlementRulesCard 6 + AllocationBreakdownPanel 5 + InvoicePreviewPanel 5 + ReconciliationStatusPanel 5 + SettlementTrendMiniChart 3 = ~24 NEW vitest PASS) 결정 wire + Phase 21 wire `f7d1f41` 의 ~7 NEW vitest PASS 패턴 verbatim EXTENSION 결정 wire (대시보드 5 sub-components 전체 parity 검증).

### F38.6 Capability matrix v1.48 EXTENSION FINOPS_CHARGEBACK_SETTLEMENT (A624 결정)

- **F38.6.1** `apps/api/core/capability.py` MODIFIED 결정 wire + `Capability.FINOPS_CHARGEBACK_SETTLEMENT = "finops_chargeback_settlement"` 1 NEW enum EXTENSION 결정 wire.
- **F38.6.2** `apps/api/dependencies/capability.py` MODIFIED 결정 wire + `require_finops_chargeback_settlement()` 1 NEW dependency 결정 wire + `__all__` EXTENSION 결정 wire + 미허용 tenant 의 FinOps settlement 진입 차단 결정 wire + SSOT RED→GREEN EXTENSION 결정 wire (capability matrix v1.47 → v1.48 EXTENSION 결정 wire).
- **F38.6.3** `docs/capability-matrix.md` v1.47 → v1.48 EXTENSION 결정 wire = 1 NEW row `FINOPS_CHARGEBACK_SETTLEMENT` (Phase 22) industry-agnostic 4-industry grants ✅/✅/✅/✅ 결정 wire (manufacturing + service + manufacturing_service + manufacturing_service_other 모두 허용) + Phase 21 wire `f7d1f41` 의 `FINOPS_RESERVED_CAPACITY_PLANNING` industry-agnostic 4-industry grants pattern verbatim EXTENSION 결정 wire.
- **F38.6.4** `tests/integration/test_capability_matrix_v1_48_drift.py` NEW 8 NEW pytest cases EXTENSION 결정 wire = FINOPS_CHARGEBACK_SETTLEMENT row presence + 4-industry grants ✅/✅/✅/✅ + Phase 11~22 FINOPS_* capability rows preserved (FINOPS_SHOWBACK + FINOPS_CHARGEBACK + FINOPS_ANOMALY_DETECTION + FINOPS_BUDGET_ALERT + FINOPS_FORECASTING_CAPACITY_PLANNING + FINOPS_OPTIMIZATION + FINOPS_TAG_GOVERNANCE + FINOPS_REPORTING + FINOPS_SUSTAINABILITY + FINOPS_COMMITMENT + FINOPS_PRICING + FINOPS_MULTI_CLOUD_UNIFIED_RECONCILIATION + FINOPS_RESERVED_CAPACITY_PLANNING) + AD-22 owner-only pattern + CR 12-1 L4 verbatim + drift detector EXTENSION + Phase 21 wire `f7d1f41` 의 test_capability_matrix_v1_47_drift.py pattern verbatim EXTENSION 결정 wire.
- **F38.6.5** `tests/integration/test_audit_action_v1_48_drift.py` NEW 6 NEW pytest cases EXTENSION 결정 wire = ActionClass.FINOPS_CHARGEBACK_SETTLEMENT enum presence + FinopsChargebackSettlementAction 8 Literal + __all__ EXTENSION + Phase 11~22 ActionClass.FINOPS_* enum preserved + drift detector EXTENSION 결정 wire.
- **F38.6.6** capability gate 의 fail-closed 결정 wire = 미허용 tenant 의 FinOps settlement 진입 시 403 Forbidden 결정 wire + capability matrix v1.48 row 부재 시 fail-closed 결정 wire + `Capability.FINOPS_CHARGEBACK_SETTLEMENT` enum 부재 시 fail-closed 결정 wire + AD-22 owner-only RBAC 정합 결정 wire + Epic 12 2FA 챌린지 보존 결정 wire + NFR4 PII minimization 정합 보존 결정 wire.

### F38.7 audit action EXTENSION 8 NEW + 16 NEW typed exception classes (A624 결정)

- **F38.7.1** `apps/api/core/audit_action.py` MODIFIED 결정 wire + `ActionClass.FINOPS_CHARGEBACK_SETTLEMENT` 1 NEW enum value EXTENSION 결정 wire + `FinopsChargebackSettlementAction` Literal 8 NEW actions EXTENSION 결정 wire (`settlement_rule_created` + `settlement_rule_updated` + `settlement_calculated` + `allocation_verified` + `settlement_invoice_generated` + `settlement_reconciled` + `settlement_dry_run_executed` + `settlement_approval_required`) + `_ActionRegistry._REGISTRY` 1 NEW entry EXTENSION 결정 wire (FINOPS_CHARGEBACK_SETTLEMENT → "audit_logs" + frozenset of 8 actions) + `AuditAction` Union EXTENSION 결정 wire + `__all__` EXTENSION 결정 wire.
- **F38.7.2** 16 NEW typed exception classes EXTENSION 결정 wire (CR 12-5 D-14 envelope) = `FinopsChargebackSettlementError` base class + `SettlementRuleNotFoundError(404)` + `SettlementRuleInvalidError(400)` + `SettlementRuleOverlapError(409)` + `SettlementCalculationError(500)` + `SettlementRecipientMissingError(400)` + `SettlementInvoiceRateLimitedError(429)` + `SettlementInvoiceGenerationError(500)` + `AllocationMismatchError(500)` + `AllocationDimensionInvalidError(400)` + `AllocationZeroAmountSkipError(200)` (warning not error) + `SettlementReconciliationFailedError(500)` + `SettlementApprovalRequiredError(403)` + `SettlementApprovalTimeoutError(408)` + `SettlementDryRunFailedError(500)` + `SettlementPreviewInvalidError(400)` 결정 wire.
- **F38.7.3** audit-first INSERT 의 8 NEW actions 결정 wire (CR 1-1 verbatim) = `settlement_rule_created` + `settlement_rule_updated` + `settlement_calculated` + `allocation_verified` + `settlement_invoice_generated` + `settlement_reconciled` + `settlement_dry_run_executed` + `settlement_approval_required` 결정 wire + per-tenant RLS 자동 적용 결정 wire + 누가 언제 어떤 settlement action 발생시켰는지 추적 결정 wire.

### F38.8 dry-run + Tests + wire scope T1~T8 (A624 결정)

- **F38.8.1** dry-run mode 결정 wire = `--finops-chargeback-settlement-dry-run` 1 NEW CLI flag + dry-run 시 actual `settlement_calculated` audit-first INSERT skip + Phase 21 wire `f7d1f41` 의 `--finops-reserved-capacity-orchestrator-dry-run` 1 NEW CLI flag 패턴 verbatim EXTENSION 결정 wire.
- **F38.8.2** dry-run preview 결과 결정 wire = `phase_22_settlement_preview` 1 table 결정 wire + preview_id UUID PK + tenant_id UUID + period_key TEXT + settlement_data JSONB + computed_at TIMESTAMPTZ DEFAULT NOW() + trace_id TEXT + RLS 자동 적용 CR 0-2 verbatim + audit-first INSERT `settlement_dry_run_executed` 결정 wire + Phase 21 wire `f7d1f41` 의 1 preview table 패턴 verbatim EXTENSION.
- **F38.8.3** dry-run CLI flag 1 NEW 결정 wire + Phase 21 wire `f7d1f41` 의 1 NEW CLI flag 패턴 verbatim EXTENSION + 1 CLI flag 모두 wire 결정 wire + dry-run 결과 preview 결정 wire.
- **F38.8.4** tests 결정 wire = ~+78 NEW pytest PASS (settlement_rules 24 + allocation_engine 18 + invoice_generator 18 + reconciliation 18 = ~78 NEW pytest PASS) 결정 wire + Phase 21 wire `f7d1f41` 의 ~108 NEW pytest PASS 패턴 verbatim EXTENSION 결정 wire (전체 4 module 의 24+18+18+18 = ~78 NEW pytest PASS 분배).
- **F38.8.5** vitest tests ~+24 NEW vitest PASS 결정 wire (SettlementRulesCard 6 + AllocationBreakdownPanel 5 + InvoicePreviewPanel 5 + ReconciliationStatusPanel 5 + SettlementTrendMiniChart 3 = ~24 NEW vitest PASS) 결정 wire + Phase 21 wire `f7d1f41` 의 ~7 NEW vitest PASS 패턴 verbatim EXTENSION 결정 wire (5 sub-components parity 검증 대폭 확대).
- **F38.8.6** ruff + tsc 0 NEW + SDR drift gate 결정 wire + A36 SDR 검증 4-step 자동 적용 결정 wire + 0 NEW ruff + 0 NEW tsc + 0 regressions + SDR drift gate PASS 결정 wire.
- **F38.8.7** wire scope T1~T8 결정 wire = T1 settlement_rules + settlement_engine + allocation_engine + invoice_generator + reconciliation 5 NEW backend modules 결정 wire + T2 settlement_dashboard 5 NEW sub-components + Clients component + TypeScript mirror + ko-KR.json extension 결정 wire + T3 alembic 0054 phase_22_chargeback_settlement 9 tables (settlement_rules + settlement_results + allocation_lines + settlement_invoices + reconciliation_results + 4 preview tables) + RLS 9 tables 결정 wire + T4 audit action EXTENSION 8 NEW + 16 NEW typed exception classes 결정 wire + T5 capability v1.48 EXTENSION + frontend finops settlement dashboard 결정 wire + T6 scheduled_dispatch_job wire 결정 wire + T7 dry-run mode + 1 NEW CLI flag 결정 wire + T8 3중 게이트 FINAL CLEAN atomic commit 결정 wire + estimated ~+28-32 files estimate = ~+20 NEW + ~+12 MODIFIED 결정 wire + Phase 21 wire `f7d1f41` 의 ~30 files pattern verbatim EXTENSION 결정 wire.
- **F38.8.8** A19 cohesion pattern 9 surface EXTENSION PASS 결정 wire (FinOps Chargeback Settlement surface NEW = F38.1~F38.8 결정 wire) + Phase 21 wire `f7d1f41` 의 FinOps Reserved Capacity Planning surface NEW = F37.1~F37.8 pattern verbatim 미러 + Phase 20 wire `52dad7f` 의 FinOps Multi-Cloud Cost Unified Reconciliation surface NEW = F36.1~F36.8 pattern verbatim 미러 + Phase 19 wire `8db3cfc` 의 FinOps Pricing surface NEW = F35.1~F35.8 pattern verbatim 미러 + Phase 18 wire `67059cf` 의 FinOps Cloud Commitment Management surface NEW = F34.1~F34.8 pattern verbatim 미러 + Phase 17 wire `97cfe4e` 의 FinOps Sustainability surface NEW = F33.1~F33.8 pattern verbatim 미러 + Phase 16 wire `81ae00a` 의 FinOps Reporting surface NEW = F32.1~F32.8 pattern verbatim 미러 + Phase 15 wire `1b800d9` 의 FinOps Tag Governance surface NEW = F31.1~F31.8 pattern verbatim 미러 + Phase 14 wire `e904485` 의 FinOps Optimization surface NEW = F30.1~F30.8 pattern verbatim 미러 + Phase 13 wire `8b98030` 의 FinOps Forecasting surface NEW = F29.1~F29.8 pattern verbatim 미러 + Phase 12 wire `f3c0e63` 의 FinOps Anomaly surface NEW = F28.1~F28.8 pattern verbatim 미러 + Phase 11 wire `e020ad0` 의 FinOps Showback surface NEW = F27.1~F27.7 pattern verbatim 미러 결정 wire.
- **F38.8.9** CR lessons applied 19종 결정 wire (CR 0-2 RLS lesson ✅ APPLIED + CR 1-1 audit-first INSERT ✅ APPLIED (8 NEW audit log entries) + CR 4-3/4-4 lessons carry ✅ APPLIED + CR 1-1 ContextVar lesson ✅ APPLIED + CR 1-1 RSC boundary lesson ✅ APPLIED + CR 9-6 commit message discipline ✅ APPLIED + CR 11-3 honest-DEFER discipline ✅ APPLIED (158번째 epic 연속 정직 회복) + CR 11-4 D-001~D-005 + P-015 ✅ APPLIED + CR 12-1 L4 ✅ APPLIED + CR 12-5 D-14 typed exception envelope ✅ APPLIED (16 NEW typed exception classes) + CR 12-5 D-PARITY-01 inversion ✅ APPLIED + CR 12-5 D-GATE-01 inversion ✅ APPLIED + A36 SDR 검증 4-step 자동 적용 ✅ + AD-14 stack pin ✅ APPLIED (reportlab 4.0.7 + xlsxwriter 3.1.9) + AD-22 owner-only RBAC ✅ APPLIED + AD-49 (a)~(g) 7 sub-decisions ✅ APPLIED + NFR4 PII minimization ✅ PRESERVED) 결정 wire.
- **F38.8.10** D-DEFER-* honestly 결정 wire (CR 11-3 158번째 epic 연속 정직 회복 결정 wire 보존) = D-FINOPS-1~D-FINOPS-10 ✅ RESOLVED 보존 105~157번째 + **D-FINOPS-11 신규 honestly DEFER 보존 + Phase 22 carry-over 결정 wire 의 AD-50 신규 (a)~(g) 7 sub-decisions 모두 결정 wire 진입 = Phase 22 PRD entry 진입 시점에 carry-over chain 정직 회복 결정 wire 진입** + Epic 1 ~ Epic 17 + Phase 3 ~ Phase 21 + Phase 19.5 + Phase 20.5 + 1st release cycle 정합 보존 결정 wire (cj-style 158번째 epic 연속 정직 회복 pre-flight 정합 sweep) + 결정 wire 일자: 2026-08-27 (KST) + **next**: 옵션 (a) Phase 22 bmad-create-story spec entry 진입 (cj-style 159번째 wire 진입 시점) / 옵션 (b) Phase 22 bmad-dev-story atomic wire T1~T8 진입 (cj-style 160번째) / 옵션 (c) Phase 22 close-out retro 진입 (cj-style 161번째) / 옵션 (d) Layer 2 P1 + Layer 3 P2 carry-over sprint 진입 / 옵션 (e) audit-fixes sprint 진입 / 옵션 (f) Epic 22+ 진입 결정 wire / 옵션 (g) D-DEFER-* follow-up 결정 wire 보류.

---

# 13. 화면·디자인·기술

## 13.1 화면 원칙
- 좌측 사이드바 내비게이션, PC 그리드 입력 / 모바일 폼 입력, 완전반응형
- 대시보드: 월 체크리스트(순차입력 안내)·TOP5/WORST5·12개월 추이
- 디자인: 클리어블루 + 옐로우 포인트 + 화이트, Pretendard, 음수 (1,234) 빨강 표기
- 거래처 입력 안내 문구: "거래처 정보를 입력하면, 거래처별로 정교한 판매전략을 수립하는 데 도움이 됩니다"
- **AI 배지 (F10.2-(a))** — `source_kind='auto_analysis'` 파란 배지 "📊 자동 분석" + `source_kind='ai_reference'` 보라 배지 "🤖 AI 참고(검증 필요)" + tooltip 한국어 only [NFR18 / §13.1 ko-KR 정합]. 2차 로드맵: 다국어 [§14.B NON-GOAL #5]

## 13.2 기술 스택
| 계층 | 선택 | 비고 |
|------|------|------|
| 프론트 | Next.js + Tailwind + shadcn/ui + TanStack Table + next-intl + Recharts | 완전반응형 |
| 백엔드 | FastAPI + **순수 Python 원가엔진** | 엑셀 1원 단위 대조 테스트(V8) 필수 |
| DB | Supabase PostgreSQL + RLS(멀티테넌트) | append-only 수불 원장, 마감잠금 트리거 |
| 결제 | Stripe | 월 구독 |
| 배포 | Vercel(프론트) + Railway(백엔드) | 월 예산 10만원 내 |
| AI | Claude API (Vision 포함) | M10 |
| 배제 | Celery 등 복잡 인프라 | G2 "새벽에 혼자 고칠 수 있는 시스템" |

## 13.3 보안·운영
2FA, 저장 데이터 암호화, 일 1회 자동 백업 + 셀프 백업 다운로드, 해지 시 보관일수 고지+삭제 동의 문구, 운영자 콘솔·대리접속(동의+읽기전용), 공지 테이블.

---

# 14. 비기능 요구·제약

- 데이터 규모: 소형 여유 설계(제품 수백·자재 수천 무리 없음 — 원본 슬롯 한계 철폐). **1차 정량 한계**: 테넌트 ≤ 100, 제품 ≤ 500, 자재 ≤ 2,000, 월 거래건수 ≤ 50,000, 월 입력 동시 사용자 ≤ 10.
- 성능: 월 계산 수 초 내(소형 데이터 전제), 계산 버튼 방식으로 예측 가능성 확보. **응답시간 SLO**: 단일 테넌트 월 계산 P95 ≤ 5초, 보고서 조회 P95 ≤ 3초, AI 추출 응답 P95 ≤ 30초.
- 마이그레이션: 원본 엑셀 5종 가져오기(유통품 자기참조 BOM → merchandise 자동 변환 포함)
- 법적 문서(약관·개인정보처리방침)는 프로젝트 마무리 단계에 초안 작성 (예정 과업)
- 파일럿 투입은 개발 중반 시점에 재논의 (예정 과업) / 서비스명 '비즈업'은 가칭, 확정 재논의

**비기능 정량 한계 (NFR 표)**

| 항목 | 1차 목표 | 2차 확장 | 측정 방법 |
|------|----------|----------|---------|
| 가용성 | 99.5% (월 4h 다운 허용) | 99.9% | PostgreSQL ping + 모니터링 |
| RPO (Recovery Point Objective) | 24h (일 1회 백업) | 1h | 백업 시각 vs 장애 시각 |
| RTO (Recovery Time Objective) | 4h (1인 운영자 수동 복구) | 1h | 장애 선언 → 복구 완료 |
| 백업 보관 | 30일 (자동), 1년 (분기) | 1년 (자동) | 보관 정책 |
| 감사로그 보존 | 5년 (append-only [A8]) | 5년 | DB retention |
| 보안 — 전송 | TLS 1.3 | TLS 1.3 | Cert 검증 |
| 보안 — 저장 | AES-256 at rest, KMS 관리 | 동일 | KMS audit |
| 인증 | 2FA 강제 (M12-a) | SSO 추가 | M12 |
| 동시 사용자 (테넌트당) | 10 | 50 | 동시접속 카운터 |
| 데이터 볼륨 (테넌트당) | 제품 500, 자재 2,000, 월 50K 트랜잭션 | 10× | DB row count |
| 인프라 페이로드 | Supabase Free → Pro 승격 트리거: 동시 30 테넌트 또는 월 10K 트랜잭션 | — | 운영자 콘솔 알림 |
| **언어 (UI 라벨)** | **한국어 only** (1차), [§14.B NON-GOAL #5] 정합 | 다국어 (en-US + ja-JP 우선) | `apps/web/messages/ko-KR.json` SSOT 1권 강제 + `apps/web/lib/i18n/*` `notFound` 시 ko-KR fallback + ESLint rule forbid-non-ko-KR-keys |
| **AI 추출 응답** | P95 ≤ 30초 (M10-c + 10-1 wire) | 동일 | Anthropic Claude Vision 응답 시간 측정 + NFR11 SLO |
| **AI 인사이트 cache hit** | sub-100ms (10-2 wire) | 동일 | in-memory + DB lookup 시간 측정 |
| **AI 배지 reject counter** | **target 0건** (10-3 wire, [AD-7 verbatim]) | 동일 | `audit_logs.action_name='ai_badge_source_kind_rejected'` count |

**용어**: RPO/RTO 정의는 §부록 C 참조. 본 표 임계값은 IR·architecture 단계에서 재검증될 수 있다.

---

# 14.B 비목표 (Non-Goals for MVP)

본 섹션은 1차 출시에서 **명시적으로 구축하지 않을** 기능이다. 후속 로드맵(§15) 항목과 1:1 대응하며, 각 항목은 의도된 미구현임을 표시한다. Downstream(architecture, stories)에서 본 목록에 없는 기능을 MVP 범위로 가정해서는 안 된다.

**[NON-GOAL for MVP #1] — 제조 부문의 ABC 엔진.** 전통 개별원가 엔진(§6)·TDABC(§7.2)·Classic ABC(§7.1)는 **서비스 부문 전용**이며, 제조 부문의 활동기준원가는 1차에서 미구현. 겸영 기업의 경우 제조는 전통 엔진, 서비스는 ABC 엔진으로 자동 분기(Q-I). 2차 이후 통합 검토.

**[NON-GOAL for MVP #2] — 복수 예산 시나리오.** §10 예산 시나리오는 1차에서 시나리오 1개(M8-a). 2개 이상 동시 보유·비교는 2차. trigger: ≥ 5 테넌트 요청 시.

**[NON-GOAL for MVP #3] — A×B×C×D 예산 편성 엔진.** §부록 B에 명세만 보존, 1차 비구현. 예산 입력은 사용자 직접 입력(금액)으로 한정. trigger: ≥ 5 테넌트 요청 시.

**[NON-GOAL for MVP #4] — CPA (Cost-Per-Activity) 정밀 분석.** Classic ABC의 동인 단가는 1차에서 단순 합산(원가/총건수), 회귀·계절성 분석은 2차.

**[NON-GOAL for MVP #5] — 다국어·다통화 자동 환산.** KRW/USD 동시 표시(§8.1 M5-b)만 지원, 환율 자동 수집·시점 잠금은 2차. trigger: 외국인 투자자 round 진입 시.

**[NON-GOAL for MVP #6] — 멀티에이전트 원가분석 위원회.** AI 인사이트는 단일 모델의 고정 템플릿(M10-b)만. 다중 에이전트 협의·합의 알고리즘은 3차.

**[NON-GOAL for MVP #7] — 환경·지속가능성 원가.** §15 장기 검토 항목. 1차·2차 의제 아님.

**[NON-GOAL for MVP #8] — 모바일 네이티브 앱.** 완전반응형 웹(§13.1)만 제공. iOS/Android 네이티브는 별도 의사결정 후.

**[NON-GOAL for MVP #9] — 부채·자금 관리.** 매출·인건비·경비 입력과 비용 흐름만 추적. 차입금·상환계획·현금흐름표는 별도 모듈(M13+ 후보).

**[NON-GOAL for MVP #10] — ERP 양방향 동기화.** 다양한 ERP(더존·경동나비·SAP)와 양방향 API는 3차. 1차 입력은 엑셀 업로드 + 직접 입력만.

*본 비목표는 §15 로드맵의 2차·3차 항목과 동기화되어 있다. 2차 pull-forward trigger는 OQ-7에서 관리.*

---

# 14.A 미해결 질문 (Open Questions)

PRD 본문에서 단언되었으나 확정 전 재논의가 필요한 결정. 각 항목은 owner와 목표 마일스톤을 명시한다.

| # | 미해결 항목 | 현 상태 | Owner | 목표 마일스톤 |
|---|------------|---------|-------|---------------|
| OQ-1 | 서비스명 '비즈업' 확정 | 가칭. 도메인·상표 사용 가능 여부 미검증 | PM | UX 진입 전 |
| OQ-2 | 가격 정책(1만원 단일 vs 단계형) | §2 단일 요금제로 단언되었으나, 동종 SaaS 벤치마크·고객 가격 민감도 미수행 (검수 medium) | PM + 영업 | 파일럿 시작 전 (M5 완료 후) |
| OQ-3 | 파일럿 투입 시점 | "개발 중반 시점"은 모호 → **trigger: M0–M6 완성 후 1주**로 고정 (검수 low) | 운영자 | M6 완성 시 |
| OQ-4 | 법적 문서(약관·개인정보처리방침) | 작성 일정 미확정 | 운영자 + 법무 자문 | 파일럿 시작 전 |
| OQ-5 | Q-G report 파일 26 보고서 surface | 구조 분석으로 갈음하나, §9 14+7종으로의 커버리지 매핑 미수행 (검수 low) | 운영자 | M5 완료 후 |
| OQ-6 | [NOTE FOR PM] §6.1 (5) — 제품재고조정 UX 부호 처리 | UX 와이어프레임 단계에서 해소 예정 | UX designer | UX 단계 |
| OQ-7 | 2차 로드맵 항목 trigger 기준 | A×B×C×D 엔진·복수 예산·CPA의 pull-forward 기준 미정 (검수 medium) | PM | 1차 출시 후 6개월 시점 |

각 항목은 해소 시점에 memlog에 `event: "OQ-N resolved"`로 기록하고 본 표에서 제거한다.

---

# 15. 로드맵 (2026-07 최신 이론 근거 포함 — 결정 Q-J)

## 1차 (첫 출시 — 본 PRD 범위)
12모듈 + ABC/TDABC + AI 3종 + 검증 체계. 출시 후 파일럿 1~2곳 무료.

## 2차
- A×B×C×D 예산 편성 엔진 (제10장 보존 산식 구현)
- 복수 예산 시나리오, 활동별 classic/TDABC 혼용(method_override 활성화)
- 고객수익성 분석(CPA) — 판매지역 축을 고객 축으로 확장
- 다국어 확장(중·일), 원가 이상감지 알림(AI 예측 기반)

## 3차
- **제조 부문 ABC 개방** (부문-엔진 매핑 해제, 병행 분석 뷰)
- 멀티에이전트 원가분석 위원회 (AI 에이전트 협의체가 월 마감 데이터를 다각 분석)

## Epic 13 (✅ DONE, 2026-08-20 — cj-style 1~4번째 진입점 모두 wire DONE: PRD entry + 13-1 atomic + post-wire handoff + Epic 13 close-out retro)

- **LISTEN/NOTIFY Consume Trigger EXTENSION** — AD-25 cache invalidation trigger EXTENSION for close/reopen (4-channel publisher EXTENSION: `ai_cache` / `cost_engine_cache` / `fiscal_period_cache` / `closing_snapshot_cache`). **✅ DONE 2026-08-20** — atomic single sprint T1~T8 wire (commit `f2ea2f6`, 17 files = 12 NEW + 5 MODIFIED): alembic 0033 (NOTIFY trigger `cache_invalidation_log_notify_trg` + `cache_invalidation_log_notify()` PL/pgSQL function with 6-key alphabetical JSON payload) + LISTEN daemon `CacheInvalidationListener` (`apps/api/core/cache_invalidation_listener.py` ~620 LOC, stdlib-only pure async kernel + reconnect/backoff exponential base 1s factor 2 + jitter ±20% + max 30s + circuit breaker 5 failures → 60s cool-down) + FastAPI lifespan EXTENSION (`apps/api/main.py` 4 NEW functions + 2 NEW exception handlers CR 12-5 D-14 envelope) + 4-channel cache eviction handlers EXTENSION (M10/M3/M11 — `M10AIInvalidationAdapter` + `M3CostEngineInvalidationAdapter` + `M11FiscalPeriodInvalidationAdapter` + `M11ClosingSnapshotInvalidationAdapter` in `cache_invalidation_listener_adapters.py` ~220 LOC, F10.1-(d) verbatim cross-channel contamination 방어) + V8 determinism byte-identical test NEW (`tests/regression_v8/test_listen_notify_v8_determinism.py` ~11 cases) + CR 12-5 cross-lang drift detector EXTENSION (`apps/web/lib/cache-invalidation-listener.ts` ~150 LOC TS mirror + `tests/web/test_cache_invalidation_listener_parity.py` ~14 cases + 1-line ko-KR reject) + capability gate `LISTEN_NOTIFY` (capability matrix v1.22, industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-5 D-GATE-01 inversion 적용). D-10-2-DEFER-3 ✅ RESOLVED. A19 cohesion pattern 8 surface 8/8 PASS. 3중 게이트 FINAL CLEAN (backend ruff scoped 0 NEW, capability matrix v1.22 SSOT RED→GREEN, AD-25 verbatim bind EXTENSION + AD-22 + AD-4 cross-ref + CR 12-5 D-GATE-01 + D-PARITY-01 inversion 적용). ~107 NEW pytest PASS + 0 NEW ruff + 0 regressions (existing tests: 474 passed, 88 skipped DB-backed). **A36 SDR 검증 4-step 자동 적용 PASS** (commit prefix lint + sprint-status structure + vitest file count drift + commit consistency). **3 honestly DEFER preserved (CR 11-3 22~42번째 epic 연속)**: D-13-1-DEFER-1 ✅ RESOLVED (master PRD v2.3 §F13 verbatim = 본 A54) / D-13-1-DEFER-2 preserved (LISTEN/NOTIFY 실측 evidence 정합 sweep = 1차 출시 후 진입 시점) / D-13-1-DEFER-3 preserved (separate epic LISTEN/NOTIFY consume 2nd batch = cross-tenant invalidation fan-out + multi-process coordination = Epic 13 후속 story 진입 결정, A53 결정 시점). **A53+A54+A55+A56 결정 wire (cj-style Epic 13 5번째 진입점 = cj-style Epic 13 carry-over 17번째 docs only 권장)**: A54 = 본 master PRD v2.3 edit (D-13-1-DEFER-1 해소). PRD §F13 verbatim 13-1 wire 정합 + §8.1 M10-(d)·§F10.1-(d) EXTENSION 진입 wire.

## Phase 3 (✅ DONE, 2026-08-22 — cj-style Phase 3 1~3번째 진입점 모두 wire DONE: PRD entry + Phase 3-0 atomic sprint + Phase 3-1 atomic wire T1~T8 + close-out retro)

- **Auth Foundation (로그인/회원가입 UI + auth middleware = Epic 1 완성 territory close-out 결정)** — Phase 3 PRD entry DONE 2026-08-20 (cj-style Phase 3 1번째 진입점 = cj-style 49번째 epic 연속 정직 회복, master PRD v2.5 → v3.0 atomic edit) + Phase 3-0 atomic sprint `1db21d2` DONE 2026-08-21 (cj-style Phase 3 carry-over 1번째 "fix" 종류 = P0 3종 ALL RESOLVED ✅: ① GUC name split / ② custom_access_token_hook enabled / ③ signup path) + Phase 3-1 bmad-create-story spec entry + Phase 3-1 bmad-dev-story atomic wire T1~T8 `d3e7454` DONE 2026-08-21 (cj-style Phase 3 2번째 진입점 = cj-style 50번째 epic 연속 정직 회복, 41 files atomic = 33 NEW + 8 MODIFIED, 97 NEW test cases = 66 vitest + 31 pytest, 3중 게이트 FINAL CLEAN, SDR 갱신 3737→3855) + Phase 3 close-out retro `handoff-2026-08-22-phase-3-close-out-done` DONE 2026-08-22 (cj-style Phase 3 3번째 진입점 = cj-style 51~52번째 epic 연속 정직 회복 wire DONE, A65~A75 결정 wire + D-1-1-DEFER-1/2/3 honestly preserved 50번째 검증 + 3중 게이트 FINAL CLEAN + A19 cohesion 9 surface EXTENSION PASS). 결정 wire 일자: 2026-08-22 (KST).

## Phase 4 (in-progress, 2026-08-22 — cj-style Phase 4 1번째 진입점 = Phase 4 PRD entry DONE 진입 wire = cj-style 53번째 epic 연속 정직 회복, Phase 4 wire 진입 대기 = cj-style Phase 4 2번째 진입점 = cj-style 54번째 epic 연속 정직 회복 진입 대기)

- **Deployment config + Dockerfile + health check + observability + database backup territory 진입 (옵션 (a) Phase 4 진입, A73 결정 wire)** — Phase 4 PRD entry DONE 2026-08-22 (cj-style Phase 4 1번째 진입점 = cj-style 53번째 epic 연속 정직 회복, master PRD v3.0 → v3.1 atomic edit). Phase 4 territory = Vercel frontend (vercel.json + framework=nextjs + regions=[icn1] + buildCommand=`pnpm --filter web build` + env mapping NEXT_PUBLIC_SUPABASE_URL/ANON_KEY/API_BASE_URL + headers CSP+X-Frame-Options+HSTS + redirects legacy `/ko-KR/*` → `/ko/*`) + Railway backend (railway.toml + builder=DOCKERFILE + dockerfilePath=`apps/api/Dockerfile` + healthcheckPath=`/api/v1/health` + restartPolicyType=ON_FAILURE + env mapping DATABASE_URL/SUPABASE_JWT_SECRET/SUPABASE_URL/ANON_KEY/SERVICE_ROLE_KEY/SENTRY_DSN) + Supabase production PostgreSQL (PITR 7일 자동 + Supabase Storage 결정 wire 보류) 결정 wire + per-app Dockerfile 분리 (`apps/web/Dockerfile` Next.js standalone output + `apps/api/Dockerfile` FastAPI uvicorn, AD-14 stack pin by @sha256: digest 결정) + Health check + observability wire (`GET /api/v1/health` FastAPI endpoint + `GET /api/health` Next.js route handler + liveness/readiness 분리 결정 + Sentry browser SSR-safe init + Sentry FastAPI server integration + tracesSampleRate=0.1) + Database backup strategy (alembic 0036 phase_4_backup_strategy table + Supabase PITR 7일 자동 + 수동 export 보완 + RPO 5분 / RTO 1시간 결정 + SHA-256 checksum validation) + capability matrix v1.24 → v1.25 EXTENSION (DEPLOYMENT_PROD + DEPLOYMENT_STAGING + DEPLOYMENT_DATABASE_BACKUP + DEPLOYMENT_HEALTH_CHECK 4 NEW rows, industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러) + AD-27 Deployment 신규 결정 (Vercel frontend + Railway backend + Supabase PostgreSQL + Sentry observability 결정 wire). Epic 12 wire 정합 (audit-first INSERT 보존) + Epic 13/14 wire 정합 (PostgreSQL LISTEN/NOTIFY multi-process coordination 결정 wire 보존). **Phase 4 PRD entry wire scope (master PRD v3.1 atomic edit)**: (1) front matter title v3.0 → v3.1 + changelog v3.1 entry / (2) §F16 신규 (F16.1 vercel.json Vercel frontend deployment config / F16.2 railway.toml Railway backend deployment config / F16.3 apps/web/Dockerfile + apps/api/Dockerfile per-app Dockerfile 분리 / F16.4 docs/deployment.md production deployment runbook 12 sections / F16.5 health check + observability + monitoring / F16.6 database backup strategy / F16.7 tests + wire scope T1~T8 결정) / (3) §8.1 M0-(g) production deployment 결정 wire 진입 / (4) §15 로드맵 Phase 4 row status 백로그 → in-progress (PRD entry DONE 진입 wire) / (5) §부록 A A73 + A74 + A76 + A77 + A78 신규 결정 표 / (6) AD-27 Deployment 신규 결정 (Vercel + Railway + Supabase + Sentry 결정 wire) / (7) capability matrix v1.24 → v1.25 EXTENSION DEPLOYMENT_PROD + DEPLOYMENT_STAGING + DEPLOYMENT_DATABASE_BACKUP + DEPLOYMENT_HEALTH_CHECK 4 NEW rows (industry-agnostic 4-industry grants ✅/✅/✅/✅). **Phase 4 진입 flow (cj-style 1~3번째 진입점 결정 보존)**: (1) cj-style Phase 4 1번째 진입점 = Phase 4 PRD entry (cj-style 53번째): ✅ DONE 2026-08-22 (master PRD v3.1 atomic edit) / (2) cj-style Phase 4 2번째 진입점 = bmad-create-story spec (cj-style 54번째): Phase 4 wire spec (T1~T8) 결정 wire 진입 대기 / (3) cj-style Phase 4 3번째 진입점 = bmad-dev-story atomic wire (cj-style 55번째): Phase 4 본체 wire T1~T8 atomic single sprint (T1 Vercel config + T2 Railway config + T3 per-app Dockerfile + T4 deployment runbook + T5 health check + observability + T6 database backup strategy + T7 capability v1.25 + T8 tests + 3중 게이트 FINAL CLEAN atomic commit). A19 cohesion pattern 9 surface EXTENSION PASS 결정 (deployment surface NEW). estimated ~40 NEW pytest PASS + ~20 NEW vitest PASS + 0 NEW ruff + 0 regressions. 결정 wire 일자: 2026-08-22 (KST).

## Phase 7 (in-progress, 2026-08-22 — cj-style Phase 7 1번째 진입점 = Phase 7 PRD entry DONE 진입 wire = cj-style 89번째 epic 연속 정직 회복, Phase 7 wire 진입 대기 = cj-style Phase 7 2번째 진입점 = cj-style 90번째 epic 연속 정직 회복 진입 대기)

- **Observability Stack 강화 territory 진입 (옵션 (a) Phase 7+ 진입, A223 결정 wire 진입)** — Phase 7 PRD entry DONE 2026-08-22 (cj-style Phase 7 1번째 진입점 = cj-style 89번째 epic 연속 정직 회복, master PRD v3.7 → v3.8 atomic edit). Phase 7 territory = (1) **OpenTelemetry distributed tracing** (`apps/api/core/tracing.py` NEW ~+200 LOC, OTLP HTTP exporter 결정 + W3C Trace Context propagation `traceparent` + `tracestate` header + span enrichment `tenant_id` + `user_id` + `trace_id` + auto-instrumentation FastAPI + SQLAlchemy + httpx + asyncpg 4 instrumentors 결정 + head_based sampler ratio 1.0 dev + 0.1 prod 결정 + Phase 4 wire `71a033a` Sentry `tracesSampleRate=0.1` 정합 + Phase 5 wire `f093f8c` multi-region observability carry-over) / (2) **Prometheus custom metrics + Grafana dashboards** (`apps/api/core/metrics.py` NEW ~+180 LOC, Counter + Histogram + Summary + Gauge 4 metric types + 7 NEW business metrics: signups + logins + calculations + cost_engine_duration Histogram + audit_log_purge + active_tenants + ai_extraction Histogram + `/metrics` endpoint Prometheus exposition format + `prometheus-client==0.20.0` AD-14 stack pin + `docs/grafana-dashboards.md` NEW 4 dashboards: business-signups + cost-engine-performance + auth-flow + audit-log-purge 결정 + Phase 5 wire `f093f8c` `phase_5_replication_lag` table 정합) / (3) **Alerting system** (`apps/api/core/alerting.py` NEW ~+120 LOC + `config/alert_rules.yaml` NEW 5 NEW alert rules 결정: `HighErrorRate` (5xx > 5% for 5m, severity=critical) + `SlowCalc` (p99 > 5s for 10m, severity=warning) + `FailoverStuck` (replication_lag_seconds > 30 for 5m Phase 5 wire 정합, severity=critical) + `RetentionPurgeFailed` (audit_log_purge_last_success_timestamp > 26h Phase 6 wire 정합, severity=warning) + `MultiRegionDown` (primary + secondary all down for 1m, severity=critical) + Prometheus AlertManager integration + Sentry alert routing EXTENSION + Slack webhook integration `#bizup-alerts` channel + PagerDuty integration owner-only manual trigger + audit-first INSERT `alert_fired` CR 1-1 verbatim action_class='OBSERVABILITY' 결정) / (4) **Frontend performance tracing (Browser RUM)** (`apps/web/lib/tracing.ts` NEW ~+150 LOC + `@opentelemetry/sdk-trace-web` + `@opentelemetry/exporter-trace-otlp-http` AD-14 stack pin + W3C Trace Context propagation server → client through `traceparent` header + Web Vitals auto-collection LCP + FID + CLS + INP + TTFB + custom span attributes `user.tenant_id` + `user.role` + `user.industry` + `route.path` + `route.locale` + `apps/web/lib/api-fetch.ts` MODIFIED trace context propagation + `apps/web/instrumentation.ts` NEW Next.js instrumentation hook + NFR18 ko-KR 정합 결정) / (5) **audit-first INSERT 2 NEW actions** (ActionClass.OBSERVABILITY 신규 정의 + 2 NEW AuditAction Literal values: `alert_fired` (severity + alert_name + tenant_id) + `trace_sampled` (decision + tenant_id + sampling_ratio) + apps/api/core/audit_action.py MODIFIED AuditAction Literal EXTENSION 2 NEW + _ActionRegistry OBSERVABILITY entry 신규 2개 등록 + __all__ EXTENSION + emit_audit_typed BEFORE alerting trace CR 1-1 verbatim 적용 + Phase 6 wire `24e1cd7` 의 5 NEW AuditAction Literal EXTENSION pattern verbatim 적용) / (6) **Capability gates OBSERVABILITY_TRACES + OBSERVABILITY_METRICS** (capability matrix v1.31 → v1.32 EXTENSION OBSERVABILITY_TRACES + OBSERVABILITY_METRICS 2 NEW rows industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러, AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind + `apps/api/core/capability.py` MODIFIED + Capability.OBSERVABILITY_TRACES + Capability.OBSERVABILITY_METRICS 2 NEW enum + drift detector `tests/integration/test_capability_matrix_v1_32_drift.py` NEW 8 NEW pytest cases) / (7) **Tests + wire scope T1~T8** 결정 (~+30 NEW pytest PASS (tracing module 6 + metrics module 6 + alerting module 4 + grafana dashboards 2 + audit action 2 NEW + capability matrix v1.32 8 + integration e2e 2 = ~+30 NEW pytest PASS) + ~+10 NEW vitest PASS (browser RUM tracing 3 + API fetch trace propagation 2 + SSOT drift 2 + ko-KR SSOT 검증 3 = ~+10 NEW vitest PASS) + 0 NEW ruff + 0 regressions + T1 OpenTelemetry tracing module + T2 Prometheus metrics module + T3 Alerting module + T4 Frontend tracing module + T5 capability v1.32 EXTENSION 2 NEW rows + T6 audit action EXTENSION 2 NEW + T7 tests + T8 3중 게이트 FINAL CLEAN atomic commit) + AD-34 Observability Stack 강화 신규 결정 ((a)~(g) 7 sub-decisions 모두 결정 wire 진입). Phase 6 close-out retro `f9f006c` (cj-style 88번째) + Phase 6 atomic wire `24e1cd7` (cj-style 87번째) + Phase 6 spec entry `f5c14c9` (cj-style 86번째) + Phase 6 PRD entry `e84a281` (cj-style 85번째) 결정 verbatim preserve + EXTENSION (Phase 6 territory DONE 정합) + Epic 17 close-out retro `be8f3bd` (cj-style 84번째) + Epic 17 T2+T3 UI wire `bb92879` (cj-style 83번째) + Epic 17 bmad-dev-story atomic wire T1~T8 backend `2ada2ec` (cj-style 82번째) + Epic 17 spec entry `f4b2b58` (cj-style 81번째) + Epic 17 PRD entry `40a9c41` (cj-style 80번째) + Epic 16 close-out retro `f1ead9a` (cj-style 72번째) + Epic 16 T4 admin UI follow-up sprint `ff5c3b5` (cj-style 71번째) + Epic 16 review follow-up sprint `963079c` (cj-style 70번째) + Epic 16 atomic wire `e117e09` (cj-style 69번째) + Epic 16 PRD entry `08bfca5` (cj-style 67번째) + Phase 5 close-out retro `b843565` (cj-style 76~77번째) + Phase 5 atomic wire `f093f8c` (cj-style 75번째) + Phase 5 PRD entry `93d852b` (cj-style 73번째) + 1st release cycle cj-style 62~66번째 모두 wire DONE 진입 + Epic 15 cycle cj-style 58~61번째 모두 wire DONE 진입 + Phase 4 cycle cj-style 53~57번째 모두 wire DONE 진입 + Phase 3 cycle cj-style 49~52번째 모두 wire DONE 진입 결정 wire 모두 보존. **Phase 7 PRD entry wire scope (master PRD v3.8 atomic edit)**: (1) front matter title v3.7 → v3.8 + changelog v3.8 entry prepend / (2) §F23 신규 (F23.1 OpenTelemetry distributed tracing + F23.2 Prometheus custom metrics + F23.3 Alerting system + F23.4 Frontend performance tracing + F23.5 audit-first INSERT 2 NEW actions + F23.6 capability gate OBSERVABILITY_TRACES + OBSERVABILITY_METRICS + F23.7 tests + wire scope T1~T8 결정) / (3) §8.1 M0-(p) observability stack 강화 AC 신규 / (4) §15 로드맵 Phase 7 row status 백로그 → in-progress (PRD entry DONE 진입 wire) / (5) §부록 A A223+A224+A225+A226+A227 신규 결정 표 / (6) AD-34 Observability Stack 강화 신규 결정 (observability stack 강화 architecture 결정) / (7) capability matrix v1.31 → v1.32 EXTENSION OBSERVABILITY_TRACES + OBSERVABILITY_METRICS 2 NEW rows industry-agnostic 4-industry grants ✅/✅/✅/✅. **Phase 7 진입 flow (cj-style 1~4번째 진입점 결정 보존)**: (1) cj-style Phase 7 1번째 진입점 = Phase 7 PRD entry (cj-style 89번째): ✅ DONE 2026-08-22 (master PRD v3.8 atomic edit) / (2) cj-style Phase 7 2번째 진입점 = bmad-create-story spec (cj-style 90번째): Phase 7 wire spec (T1~T8) 결정 wire 진입 대기 / (3) cj-style Phase 7 3번째 진입점 = bmad-dev-story atomic wire (cj-style 91번째): Phase 7 본체 wire T1~T8 atomic single sprint (T1 OpenTelemetry tracing module + T2 Prometheus metrics module + T3 Alerting module + T4 Frontend tracing module + T5 capability v1.32 EXTENSION 2 NEW rows + T6 audit action EXTENSION 2 NEW + T7 tests + T8 3중 게이트 FINAL CLEAN atomic commit) / (4) cj-style Phase 7 4번째 진입점 = Phase 7 close-out retro (cj-style 92번째): A223~A227 close-out retro + A19 cohesion 9 surface EXTENSION PASS 검증 (observability surface NEW = F23.1~F23.6 observability stack 강화 territory + Phase 4 Sentry observability carry-over + Phase 5 multi-region observability carry-over chain 정합). A19 cohesion pattern 9 surface EXTENSION PASS 결정 (observability surface NEW). estimated ~+30 NEW pytest PASS + ~+10 NEW vitest PASS + 0 NEW ruff + 0 regressions. 결정 wire 일자: 2026-08-22 (KST).

## Epic 16 (in-progress, 2026-08-22 — cj-style Epic 16 1번째 진입점 = Epic 16 PRD entry DONE 진입 wire = cj-style 67번째 epic 연속 정직 회복, Epic 16 wire 진입 대기 = cj-style Epic 16 2번째 진입점 = cj-style 68번째 epic 연속 정직 회복 진입 대기)

- **Tenant IdP admin management territory 진입 (옵션 (a) Epic 16 진입, A92 결정 wire 진입)** — Epic 16 PRD entry DONE 2026-08-22 (cj-style Epic 16 1번째 진입점 = cj-style 67번째 epic 연속 정직 회복, master PRD v3.3 → v3.4 atomic edit). Epic 16 territory = (1) `tenant_idps` table (alembic 0038 + 13 columns + unique constraint `(tenant_id, idp_entity_id)` + RLS policy `tenant_id = current_setting('app.tenant_id')` 결정 wire 진입, CR 0-2 RLS lesson 적용) / (2) IdP metadata XML validation service (`apps/api/modules/auth/sso/idp_metadata_validator.py` NEW ~120 LOC, 8 validation steps 결정 wire) / (3) Tenant IdP CRUD API 5 routes (FastAPI `apps/api/modules/auth/sso/idp_admin_routes.py` NEW, owner/admin role required + RLS 자동 적용 + audit-first INSERT 4 NEW: `tenant_idp_created` / `tenant_idp_updated` / `tenant_idp_deleted` / `tenant_idp_tested`, CR 1-1 verbatim 적용) / (4) Tenant IdP admin UI (Next.js `apps/web/app/[locale]/(dashboard)/settings/sso/page.tsx` NEW ~150 LOC + 4 components + ko-KR.json `settings.sso.*` namespace EXTENSION 12 keys + (dashboard) 보호) / (5) Per-tenant IdP routing EXTENSION (Epic 15 SAML routes `saml_routes.py` 정합: `tenant_slug` → `tenant_idps` lookup → `idp_sso_url` redirect + ACS `idp_x509_cert` 동적 로딩 + backward compatibility `acme` hardcoded tenant 보존) / (6) Capability gate TENANT_IDP_MANAGEMENT (capability matrix v1.27 → v1.28 EXTENSION 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러) + AD-30 Tenant IdP admin management 신규 결정 ((a)~(f) 6 sub-decisions 모두 결정 wire 진입). Epic 15 SSO enterprise SAML territory carry-over chain (Epic 15 cj-style 58~61번째 wire DONE 진입 + docs/sso-enterprise.md §4.1 step 3 'TODO Epic 16' forward-reference 해결 + Epic 15 SAML response validation + JIT provisioning + multi-tenant isolation 정합) + Epic 15 close-out retro `729b223` 결정 verbatim preserve + EXTENSION (Epic 15 territory DONE 정합). Phase 3-0 + Phase 3-1 + Phase 4 + Epic 13/14 wire + Epic 15 wire + 1st release 5-entry-point pattern 모두 wire DONE 진입 (62~66번째) + Epic 15 4-entry-point pattern 모두 wire DONE 진입 (58~61번째) + Phase 4 3-entry-point pattern 모두 wire DONE 진입 (53~56번째) + Phase 3 3-entry-point pattern 모두 wire DONE 진입 (49~52번째) 결정 wire 모두 보존. **Epic 16 PRD entry wire scope (master PRD v3.4 atomic edit)**: (1) front matter title v3.3 → v3.4 + changelog v3.4 entry / (2) §F19 신규 (F19.1 tenant_idps table + F19.2 IdP metadata validation + F19.3 CRUD API + F19.4 admin UI + F19.5 per-tenant routing EXTENSION + F19.6 capability gate + F19.7 tests + wire scope T1~T8 결정) / (3) §8.1 M0-(l) tenant IdP admin AC 신규 / (4) §15 로드맵 Epic 16 row status 백로그 → in-progress (PRD entry DONE 진입 wire) / (5) §부록 A A92+A93+A94+A95+A96 신규 결정 표 / (6) AD-30 Tenant IdP admin management 신규 결정 (tenant_idps architecture 결정) / (7) capability matrix v1.27 → v1.28 EXTENSION TENANT_IDP_MANAGEMENT 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅. **Epic 16 진입 flow (cj-style 1~5번째 진입점 결정 보존)**: (1) cj-style Epic 16 1번째 진입점 = Epic 16 PRD entry (cj-style 67번째): ✅ DONE 2026-08-22 (master PRD v3.4 atomic edit) / (2) cj-style Epic 16 2번째 진입점 = bmad-create-story spec (cj-style 68번째): Epic 16 wire spec (T1~T8) 결정 wire 진입 대기 / (3) cj-style Epic 16 3번째 진입점 = bmad-dev-story atomic wire (cj-style 69번째): Epic 16 본체 wire T1~T8 atomic single sprint (T1 tenant_idps table alembic 0038 + T2 IdP metadata validator + T3 CRUD API 5 routes + T4 admin UI + T5 per-tenant routing EXTENSION + T6 capability v1.28 + T7 tests + T8 3중 게이트 FINAL CLEAN atomic commit) / (4) cj-style Epic 16 4번째 진입점 = bmad-code-review (cj-style 70번째): review follow-up sprint 진입 결정 (Epic 15 review follow-up 패턴 미러) / (5) cj-style Epic 16 5번째 진입점 = Epic 16 close-out retro (cj-style 71번째): A92~A96 close-out retro + A19 cohesion 9 surface EXTENSION PASS 검증 (Epic 15 SSO enterprise surface EXTENSION = F19.1~F19.5 tenant IdP admin territory). A19 cohesion pattern 9 surface EXTENSION PASS 결정 (Epic 15 SSO enterprise surface EXTENSION). estimated ~25 NEW pytest PASS + ~5 NEW vitest PASS + 0 NEW ruff + 0 regressions. 결정 wire 일자: 2026-08-22 (KST).

## Phase 5 (in-progress, 2026-08-22 — cj-style Phase 5 1번째 진입점 = Phase 5 PRD entry DONE 진입 wire = cj-style 73번째 epic 연속 정직 회복, Phase 5 wire 진입 대기 = cj-style Phase 5 2번째 진입점 = cj-style 74번째 epic 연속 정직 회복 진입 대기)

- **Multi-Region Backup & Disaster Recovery territory 진입 (옵션 (a) Phase 5 진입, A124 결정 wire 진입)** — Phase 5 PRD entry DONE 2026-08-22 (cj-style Phase 5 1번째 진입점 = cj-style 73번째 epic 연속 정직 회복, master PRD v3.4 → v3.5 atomic edit). Phase 5 territory = (1) **Cross-region read replica + WAL archiving** (alembic 0039 phase_5_replication_lag table + replica_region enum seoul/tokyo/singapore/frankfurt/virginia/oregon/sao_paulo + primary_region enum + lag_bytes BIGINT + lag_seconds INTEGER + last_synced_lsn TEXT PG_LSN + replication_status enum syncing/replicating/lagged/disconnected/failed + audit-first INSERT `replica_status_changed` + 3 indexes + 2 CHECK constraints + `apps/api/alembic/versions/0039_phase_5_multi_region_backup.py` + `docs/cross-region-replication.md` 결정 wire) / (2) **Cross-region failover automation** (`apps/api/jobs/failover_orchestrator.py` + primary → secondary health probe 5-second interval + 3 consecutive failures trigger + automatic promotion via Supabase API + DNS update via Supabase custom domain redirect + 30s RTO target + audit-first INSERT `failover_initiated` + `failover_completed` + FastAPI lifespan hook startup/shutdown + GRACEFUL_SHUTDOWN_TIMEOUT=30s + owner-only manual trigger `POST /api/v1/admin/failover` AD-22 RBAC + 2FA 챌린지 Epic 12 정합 결정) / (3) **DR drill + automated quarterly test** (`apps/api/jobs/dr_drill.py` + cron KST 1st Sunday 03:00 UTC 18:00 + actual failover drill test in staging + 6 drill steps: health check + secondary promote + write test + application health + DNS update + restore trigger + RPO/RTO measurement + phase_5_dr_drill_results table + Q1/Q2/Q3/Q4 quarterly schedule + audit-first INSERT `dr_drill_completed` 결정) / (4) **Cross-region backup strategy** (`docs/database-backup.md` EXTENSION 10 sections → 12 sections + cross-region PITR primary Seoul + secondary Tokyo + 30일 hot + 90일 cold + 365일 archive regional + RPO 1h / RTO 4h SLA post-multi-region wire DONE 진입 결정 wire + Phase 4 single-region RPO 5min/RTO 1h honest-extreme risk = Seoul region disaster 시 backup restoration 불가 verbatim 해소 결정) / (5) **Multi-region health observability** (`apps/api/core/health.py` EXTENSION `/api/v1/health/multi-region` endpoint + primary + secondary status array + CR 12-5 D-14 envelope `{status, primary: {region, status, lag_bytes, lag_seconds, last_synced_at}, secondary: {...}, timestamp}` + `apps/api/core/observability.py` EXTENSION Sentry breadcrumb failover + Grafana multi-region dashboard + `apps/web/app/api/health/multi-region/route.ts` NEW Next.js Edge Runtime 결정 wire) + capability matrix v1.28 → v1.29 EXTENSION (MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER 2 NEW rows, industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러) + AD-31 Multi-Region Backup & Disaster Recovery 신규 결정 ((a)~(f) 6 sub-decisions 모두 결정 wire 진입). Phase 4 close-out retro §6 disaster recovery verbatim carry-over (Phase 4 docs/database-backup.md §7 "multi-region backup 결정 wire 보류, Phase 5+ 진입 시점" honestly carry-over + Phase 5 PRD entry 진입 시점에 정직 회복 결정 wire 완료) + Epic 16 close-out retro `f1ead9a` 결정 verbatim preserve + EXTENSION (Epic 16 territory DONE 정합) + 1st release close-out retro + Phase 4 close-out retro + Phase 3 close-out retro cycle 정직 회복 결정 wire 모두 보존 + Phase 3-0 + Phase 3-1 + Phase 4 + Epic 13/14 wire + Epic 15 wire + 1st release + Epic 16 6-entry-point pattern 모두 wire DONE 진입 (67~72번째) + 1st release 5-entry-point pattern 모두 wire DONE 진입 (62~66번째) + Epic 15 4-entry-point pattern 모두 wire DONE 진입 (58~61번째) + Phase 4 4-entry-point pattern 모두 wire DONE 진입 (53~57번째) + Phase 3 4-entry-point pattern 모두 wire DONE 진입 (49~52번째) 결정 wire 모두 보존. **Phase 5 PRD entry wire scope (master PRD v3.5 atomic edit)**: (1) front matter title v3.4 → v3.5 + changelog v3.5 entry / (2) §F20 신규 (F20.1 Cross-region read replica + WAL archiving / F20.2 Cross-region failover automation / F20.3 DR drill + automated quarterly test / F20.4 Cross-region backup strategy / F20.5 Multi-region health observability / F20.6 capability matrix v1.29 EXTENSION 2 NEW rows / F20.7 tests + wire scope T1~T8 결정) / (3) §8.1 M0-(m) multi-region backup AC 신규 / (4) §15 로드맵 Phase 5 row status 백로그 → in-progress (PRD entry DONE 진입 wire) / (5) §부록 A A124+A125+A126+A127+A128 신규 결정 표 / (6) AD-31 Multi-Region Backup & Disaster Recovery 신규 결정 (cross-region architecture 결정) / (7) capability matrix v1.28 → v1.29 EXTENSION MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER 2 NEW rows industry-agnostic 4-industry grants ✅/✅/✅/✅. **Phase 5 진입 flow (cj-style 1~3번째 진입점 결정 보존)**: (1) cj-style Phase 5 1번째 진입점 = Phase 5 PRD entry (cj-style 73번째): ✅ DONE 2026-08-22 (master PRD v3.5 atomic edit) / (2) cj-style Phase 5 2번째 진입점 = bmad-create-story spec (cj-style 74번째): Phase 5 wire spec (T1~T8) 결정 wire 진입 대기 / (3) cj-style Phase 5 3번째 진입점 = bmad-dev-story atomic wire (cj-style 75번째): Phase 5 본체 wire T1~T8 atomic single sprint (T1 alembic 0039 phase_5_replication_lag + phase_5_dr_drill_results tables + T2 failover_orchestrator + T3 dr_drill + T4 cross-region backup strategy docs EXTENSION + T5 multi-region health observability + T6 capability v1.29 + T7 tests + T8 3중 게이트 FINAL CLEAN atomic commit) / (4) cj-style Phase 5 4번째 진입점 = Phase 5 close-out retro (cj-style 76~77번째): A124~A128 close-out retro + A19 cohesion 9 surface EXTENSION PASS 검증 (multi-region surface NEW = F20.1~F20.5 multi-region backup & disaster recovery territory) + D-PHASE-4-DR-DEFER-1/2 ✅ RESOLVE 검증 + D-LAUNCH-1-DEFER-1 honestly preserved + D-EPIC-16-REVIEW-DEFER-2~6 honestly DEFERRED 5 OPEN preserved 보존 + D-1-1-DEFER-1/2/3 grep guard 73~76~77번째 epic 연속 정직 회복 검증. A19 cohesion pattern 9 surface EXTENSION PASS 결정 (multi-region surface NEW). estimated ~50 NEW pytest PASS + ~10 NEW vitest PASS + 0 NEW ruff + 0 regressions. 결정 wire 일자: 2026-08-22 (KST).

## Phase 6 (in-progress, 2026-08-22 — cj-style Phase 6 1번째 진입점 = Phase 6 PRD entry DONE 진입 wire = cj-style 85번째 epic 연속 정직 회복, Phase 6 wire 진입 대기 = cj-style Phase 6 2번째 진입점 = cj-style 86번째 epic 연속 정직 회복 진입 대기)

- **Audit Log Retention Policy territory 진입 (옵션 (a) Phase 6 진입, A193 결정 wire 진입)** — Phase 6 PRD entry DONE 2026-08-22 (cj-style Phase 6 1번째 진입점 = cj-style 85번째 epic 연속 정직 회복, master PRD v3.6 → v3.7 atomic edit). Phase 6 territory = (1) **retention policy DSL** (`apps/api/modules/audit/audit_retention_policy.py` NEW ~+120 LOC, 4 retention classes: AUDIT 2557일 7년 SOX hold + AUTH 30일 + INFRA 90일 + DATA 365일 + CONFIG 180일 결정 wire + 4 industries baseline + per-tenant override EXTENSION + policy evaluation precedence tenant override > industry baseline > system default + ActionClass 별 retention rule + `DEFAULT_RETENTION_DAYS` constants 결정 wire) / (2) **automatic purge job** (`apps/api/jobs/audit_purge.py` NEW ~+150 LOC + cron KST 매일 03:00 UTC 18:00 + tenant 별 retention policy 평가 + dry-run mode `--dry-run` flag + purge algorithm 6 steps: phase_6_audit_purge_log INSERT + retention policy evaluate + dry-run + archive + mask_pii + delete + 5 NEW actions audit-first INSERT `audit_log_purged` + `audit_log_archived` + `audit_log_pii_masked` + `audit_log_cold_archived` + `audit_log_personal_data_erased` CR 1-1 verbatim 적용 + `apps/api/jobs/audit_cold_archive.py` NEW ~+120 LOC + cron KST 1st Sunday 04:00 UTC 19:00 monthly cold archive + alembic 0040 phase_6_audit_retention + RLS 자동 적용 CR 0-2 verbatim) / (3) **archive storage + 365일 cold archive** (`audit_log_archive` table alembic 0040 NEW + immutable append-only trigger `audit_log_archive_immutable_trg` BEFORE UPDATE OR DELETE raise exception + SHA-256 hash chain linkage `hash_chain` column + 이전 row hash + 현재 row payload SHA-256 = current.hash_chain + tamper detection C2 도입 + 4 NEW indexes + cross-region archive EXTENSION Phase 5 wire `f093f8c` `phase_5_replication_lag` table + Supabase multi-region primary Seoul + secondary Tokyo replica EXTENSION + 365일 cold archive + audit_log_archive 의 cross-region replication + Phase 5 wire 정합) / (4) **GDPR/NFR4 PII minimization + Article 17 erasure EXTENSION** (`email_mask(email) -> "[REDACTED:sha256(email)]"` + `ip_mask(ip) -> "[REDACTED:ip]/24"` + GDPR Article 17 erasure endpoint `DELETE /api/v1/audit/personal-data?email=` owner-only RBAC AD-22 verbatim 보존 + audit-first INSERT `audit_log_personal_data_erased` 5th NEW action + audit_log table 의 해당 email 의 PII 컬럼 전체 `[REDACTED]` UPDATE + audit_log_archive 에 archive_event INSERT + NFR4 PII minimization 정합 + 1st release close-out retro §6 + Epic 17 close-out retro §11 "audit log retention policy 결정 wire 보류, Phase 6+ 진입 시점" verbatim 해소) / (5) **audit-first INSERT on deletion** (5 NEW actions audit-first INSERT: `audit_log_purged` + `audit_log_archived` + `audit_log_pii_masked` + `audit_log_cold_archived` + `audit_log_personal_data_erased` CR 1-1 verbatim 적용 + apps/api/core/audit_action.py MODIFIED AuditAction Literal 5 values EXTENSION + _ActionRegistry AUDIT entry 신규 5개 등록 + __all__ EXTENSION + audit-first INSERT BEFORE actual mutation CR 1-1 verbatim discipline + immutable audit log table + 별도 archive table 동시 지원 territory) / (6) **Capability gate AUDIT_LOG_RETENTION** (capability matrix v1.30 → v1.31 EXTENSION AUDIT_LOG_RETENTION 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러, AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind + `apps/api/core/capability.py` MODIFIED + Capability.AUDIT_LOG_RETENTION = "audit_log_retention" 1 NEW enum + drift detector `tests/integration/test_capability_matrix_v1_31_drift.py` NEW 8 NEW pytest cases) / (7) **Tests + wire scope T1~T8** 결정 (~+46 NEW pytest PASS (audit_retention_policy 8 + audit_purge 12 + audit_cold_archive 4 + alembic 4 + PII redaction 5 + capability matrix v1.31 8 + audit action 5 신규) + ~+5 NEW vitest PASS (dry-run UI test 3 + SSOT drift 2 + ko-KR SSOT 검증) + 0 NEW ruff + 0 regressions + T1 retention policy module + T2 automatic purge job + T3 alembic 0040 archive + T4 PII redaction + T5 capability v1.31 + T6 dry-run mode UI + T7 tests + T8 atomic commit) + AD-33 Audit Log Retention Policy 신규 결정 ((a)~(g) 7 sub-decisions 모두 결정 wire 진입). Epic 17 close-out retro `be8f3bd` (cj-style 84번째) + Epic 17 T2+T3 UI wire `bb92879` (cj-style 83번째) + Epic 17 bmad-dev-story atomic wire T1~T8 backend `2ada2ec` (cj-style 82번째) + Epic 17 spec entry `f4b2b58` (cj-style 81번째) + Epic 17 PRD entry `40a9c41` (cj-style 80번째) 결정 verbatim preserve + EXTENSION (Epic 17 territory DONE 정합) + Epic 16 close-out retro `f1ead9a` (cj-style 72번째) + Epic 16 T4 admin UI follow-up sprint `ff5c3b5` (cj-style 71번째) + Epic 16 review follow-up sprint `963079c` (cj-style 70번째) + Epic 16 atomic wire `e117e09` (cj-style 69번째) + Epic 16 spec entry (cj-style 68번째) + Epic 16 PRD entry `08bfca5` (cj-style 67번째) + 1st release cycle cj-style 62~66번째 모두 wire DONE 진입 + Epic 15 cycle cj-style 58~61번째 모두 wire DONE 진입 + Phase 5 cycle cj-style 73~77번째 모두 wire DONE 진입 + Phase 4 cycle cj-style 53~57번째 모두 wire DONE 진입 + Phase 3 cycle cj-style 49~52번째 모두 wire DONE 진입 결정 wire 모두 보존. **Phase 6 PRD entry wire scope (master PRD v3.7 atomic edit)**: (1) front matter title v3.6 → v3.7 + changelog v3.7 entry prepend / (2) §F22 신규 (F22.1 retention policy DSL + F22.2 automatic purge job + F22.3 archive storage + F22.4 GDPR/NFR4 PII compliance + F22.5 audit-first INSERT on deletion + F22.6 capability gate AUDIT_LOG_RETENTION + F22.7 tests + wire scope T1~T8 결정) / (3) §8.1 M0-(o) audit log retention AC 신규 / (4) §15 로드맵 Phase 6 row status 백로그 → in-progress (PRD entry DONE 진입 wire) / (5) §부록 A A193+A194+A195+A196+A197 신규 결정 표 / (6) AD-33 Audit Log Retention Policy 신규 결정 (audit log retention architecture 결정) / (7) capability matrix v1.30 → v1.31 EXTENSION AUDIT_LOG_RETENTION 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅. **Phase 6 진입 flow (cj-style 1~4번째 진입점 결정 보존)**: (1) cj-style Phase 6 1번째 진입점 = Phase 6 PRD entry (cj-style 85번째): ✅ DONE 2026-08-22 (master PRD v3.7 atomic edit) / (2) cj-style Phase 6 2번째 진입점 = bmad-create-story spec (cj-style 86번째): Phase 6 wire spec (T1~T8) 결정 wire 진입 대기 / (3) cj-style Phase 6 3번째 진입점 = bmad-dev-story atomic wire (cj-style 87번째): Phase 6 본체 wire T1~T8 atomic single sprint (T1 retention policy module + T2 automatic purge job + T3 alembic 0040 archive + T4 PII redaction + T5 capability v1.31 + T6 dry-run mode UI + T7 tests + T8 3중 게이트 FINAL CLEAN atomic commit) / (4) cj-style Phase 6 4번째 진입점 = Phase 6 close-out retro (cj-style 88번째): A193~A197 close-out retro + A19 cohesion 9 surface EXTENSION PASS 검증 (audit retention surface NEW = F22.1~F22.6 audit log retention territory + audit_log_archive carry-over chain). A19 cohesion pattern 9 surface EXTENSION PASS 결정 (audit retention surface NEW). estimated ~+46 NEW pytest PASS + ~+5 NEW vitest PASS + 0 NEW ruff + 0 regressions. 결정 wire 일자: 2026-08-22 (KST).

## Epic 17 (in-progress, 2026-08-22 — cj-style Epic 17 1번째 진입점 = Epic 17 PRD entry DONE 진입 wire = cj-style 80번째 epic 연속 정직 회복, Epic 17 wire 진입 대기 = cj-style Epic 17 2번째 진입점 = cj-style 81번째 epic 연속 정직 회복 진입 대기)

- **Audit Log Viewer & Activity Stream territory 진입 (옵션 (a) Epic 17 진입, A153 결정 wire 진입)** — Epic 17 PRD entry DONE 2026-08-22 (cj-style Epic 17 1번째 진입점 = cj-style 80번째 epic 연속 정직 회복, master PRD v3.5 → v3.6 atomic edit). Epic 17 territory = (1) **audit log query API** (`apps/api/modules/audit/audit_log_query.py` NEW ~+180 LOC, 4 functions: query_audit_log + count_audit_log + get_audit_log_entry + query_activity_stream + AuditLogQueryFilters + AuditLogEntry + AuditLogPage + ActivityStreamGroup TypedDict + RLS 자동 적용 CR 0-2 verbatim + owner/admin role required + capability gate AUDIT_LOG_VIEW 결정 wire) / (2) **audit log viewer UI** (`apps/web/app/[locale]/(dashboard)/audit-log/page.tsx` NEW ~+200 LOC + 5 components: AuditLogFilterPanel + AuditLogTable + AuditLogPagination + AuditLogExportButton + AuditLogDetailModal + ko-KR.json `audit_log.*` namespace EXTENSION 14 keys + (dashboard) 보호 + vitest RTL render discipline CR 11-4 D-003 verbatim 결정 wire) / (3) **activity stream UI** (`apps/web/app/[locale]/(dashboard)/activity/page.tsx` NEW ~+150 LOC + 3 components: ActivityStreamTimeline + ActivityStreamEntry + ActivityStreamWindowSelector + ko-KR.json `activity.*` namespace EXTENSION 8 keys + all tenant members 권한 결정 wire) / (4) **cross-region audit log visibility** (Phase 5 multi-region read replica 통한 cross-region audit query + read-only routing + lag threshold 정합 lag_bytes ≤ 100MB + lag_seconds ≤ 30s + Sentry breadcrumb 결정 wire) / (5) **CSV export** (`apps/api/modules/audit/audit_log_export.py` NEW ~+120 LOC + streaming response + UTF-8 BOM + Excel-compatible comma-separated + audit-first INSERT `audit_log_exported` CR 1-1 verbatim action_class='AUDIT' + action='audit_log_exported' + actor_id + tenant_id + filters_json + row_count + trace_id + CR 12-5 D-14 envelope 결정 wire) / (6) **Capability gate AUDIT_LOG_VIEW** (capability matrix v1.29 → v1.30 EXTENSION 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러, MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind 결정 wire) / (7) **Tests + wire scope T1~T8** 결정 (~+50 NEW pytest PASS + ~+15 NEW vitest PASS + 0 NEW ruff + 0 regressions + T1 audit_log_query module + T2 audit_log_export module + T3 audit_log_viewer page + T4 activity_stream page + T5 ko-KR.json SSOT EXTENSION + T6 capability.py + capability matrix v1.30 + T7 tests + 3중 게이트 FINAL CLEAN + T8 atomic commit) + AD-32 Audit Log Viewer & Activity Stream 신규 결정 ((a)~(g) 7 sub-decisions 모두 결정 wire 진입). Sidebar/MenuProvider hot-fix `01a06e4` (cj-style 79번째) + D-EPIC-16-REVIEW-DEFER-2~6 RESOLVE sprint `512ed6a` (cj-style 78번째) + Phase 5 close-out retro `b843565` (cj-style 76~77번째) + Phase 5 atomic wire `f093f8c` (cj-style 75번째) 결정 verbatim preserve + EXTENSION (Phase 5 territory DONE 정합) + Epic 16 close-out retro `f1ead9a` (cj-style 72번째) + Epic 16 T4 admin UI follow-up sprint `ff5c3b5` (cj-style 71번째) + Epic 16 review follow-up sprint `963079c` (cj-style 70번째) + Epic 16 atomic wire `e117e09` (cj-style 69번째) + Epic 16 PRD entry `08bfca5` (cj-style 67번째) + 1st release cycle cj-style 62~66번째 모두 wire DONE 진입 + Epic 15 cycle cj-style 58~61번째 모두 wire DONE 진입 + Phase 4 cycle cj-style 53~57번째 모두 wire DONE 진입 + Phase 3 cycle cj-style 49~52번째 모두 wire DONE 진입 결정 wire 모두 보존. **Epic 17 PRD entry wire scope (master PRD v3.6 atomic edit)**: (1) front matter title v3.5 → v3.6 + changelog v3.6 entry / (2) §F21 신규 (F21.1 audit log query API + F21.2 audit log viewer UI + F21.3 activity stream UI + F21.4 cross-region audit log visibility + F21.5 CSV export + F21.6 capability gate AUDIT_LOG_VIEW + F21.7 tests + wire scope T1~T8 결정) / (3) §8.1 M0-(n) audit log viewer AC 신규 / (4) §15 로드맵 Epic 17 row status 백로그 → in-progress (PRD entry DONE 진입 wire) / (5) §부록 A A153+A154+A155+A156+A157 신규 결정 표 / (6) AD-32 Audit Log Viewer & Activity Stream 신규 결정 (audit log viewer architecture 결정) / (7) capability matrix v1.29 → v1.30 EXTENSION AUDIT_LOG_VIEW 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅. **Epic 17 진입 flow (cj-style 1~5번째 진입점 결정 보존)**: (1) cj-style Epic 17 1번째 진입점 = Epic 17 PRD entry (cj-style 80번째): ✅ DONE 2026-08-22 (master PRD v3.6 atomic edit) / (2) cj-style Epic 17 2번째 진입점 = bmad-create-story spec (cj-style 81번째): Epic 17 wire spec (T1~T8) 결정 wire 진입 대기 / (3) cj-style Epic 17 3번째 진입점 = bmad-dev-story atomic wire (cj-style 82번째): Epic 17 본체 wire T1~T8 atomic single sprint (T1 audit_log_query module + T2 audit_log_export module + T3 audit_log_viewer page + T4 activity_stream page + T5 ko-KR.json SSOT EXTENSION + T6 capability v1.30 + T7 tests + T8 3중 게이트 FINAL CLEAN atomic commit) / (4) cj-style Epic 17 4번째 진입점 = bmad-code-review (cj-style 83번째): review follow-up sprint 진입 결정 (Epic 16 wire 의 review follow-up 패턴 미러) / (5) cj-style Epic 17 5번째 진입점 = Epic 17 close-out retro (cj-style 84번째): A153~A157 close-out retro + A19 cohesion 9 surface EXTENSION PASS 검증 (audit viewer surface NEW = F21.1~F21.5 audit log viewer & activity stream territory). A19 cohesion pattern 9 surface EXTENSION PASS 결정 (audit viewer surface NEW). estimated ~50 NEW pytest PASS + ~15 NEW vitest PASS + 0 NEW ruff + 0 regressions. 결정 wire 일자: 2026-08-22 (KST).

## 1st release launch (in-progress, 2026-08-22 — cj-style 62번째 epic 연속 정직 회복 = 1st release launch PRD entry DONE 진입 wire, Epic 15 close-out retro §12 옵션 (d) 결정 wire 진입)

- **1st release launch territory 진입 (옵션 (d) 1차 출시 진입, A83 결정 wire)** — 1st release launch PRD entry DONE 2026-08-22 (cj-style 62번째 epic 연속 정직 회복 atomic docs-only wire, master PRD v3.2 → v3.3 atomic edit). 1st release territory = (1) Marketing landing page (`/landing` route + LandingHero + LandingFeatures + LandingPricing + LandingCTA + ko-KR inline copy EXTENSION, §F18.1) / (2) ToS + Privacy Policy (`docs/terms-of-service.md` + `docs/privacy-policy.md` 한국 PIPA + GDPR 정합, §F18.2) / (3) Onboarding user guide (`docs/onboarding-guide.md` 8 sections + OnboardingTooltip + first-run wizard EXTENSION Epic 1 partial scaffold `d182d7d` 정합, §F18.3) / (4) Customer support channels (`docs/support.md` + email `support@bizup.kr` + HelpWidget + FAQ `docs/faq.md`, §F18.4) / (5) Production launch verification (smoke test RE-RUN 정직 결정 + backup drill 0036 PITR quarterly + Sentry alert wiring production + RPO 4h/RTO 24h SLA verification, §F18.5) / (6) Public launch communications (`docs/launch-announcement.md` + press kit + og/assets + in-app banner, §F18.6). launch checklist 6 conditions ALL PASS 진입 시점에 1st release official launch 결정 wire 보존. capability matrix v1.26 → v1.27 EXTENSION 4 NEW rows (LAUNCH_LANDING + LAUNCH_TOS + LAUNCH_SUPPORT + LAUNCH_MONITORING, industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러) + AD-29 1st release launch 신규 결정 ((a)~(f) 6 sub-decisions 모두 결정 wire 진입). Epic 15 close-out retro `729b223` 결정 verbatim preserve + EXTENSION (Epic 15 territory DONE 정합). Phase 3-0 + Phase 3-1 + Phase 4 + Epic 13/14 wire 보존 진입 (Supabase SSR + sb-access-token + Vercel + Railway + Supabase + LISTEN/NOTIFY + Magic link + OAuth + SSO + 2FA + observability + backup 결정 wire 보존). **1st release PRD entry wire scope (master PRD v3.3 atomic edit)**: (1) front matter title v3.2 → v3.3 + changelog v3.3 entry / (2) §F18 신규 (F18.1 Marketing landing + F18.2 ToS/Privacy + F18.3 Onboarding guide + F18.4 Support channels + F18.5 Production verification + F18.6 Launch comms + F18.7 capability v1.27 EXTENSION 4 NEW rows + F18.8 tests + wire scope T1~T8 결정) / (3) §8.1 M0-(k) 1st release launch 결정 wire 진입 / (4) §15 로드맵 1st release row status 백로그 → in-progress (PRD entry DONE 진입 wire) / (5) §부록 A A83+A84+A85+A86+A87 신규 결정 표 / (6) AD-29 1st release launch 신규 결정 (Marketing landing + ToS/Privacy + Onboarding + Support + Verification + Comms 6 sub-decisions) / (7) capability matrix v1.26 → v1.27 EXTENSION 4 NEW rows industry-agnostic 4-industry grants ✅/✅/✅/✅. **1st release 진입 flow (cj-style 1~4번째 진입점 결정 보존)**: (1) cj-style 1st release 1번째 진입점 = 1st release PRD entry (cj-style 62번째): ✅ DONE 2026-08-22 (master PRD v3.3 atomic edit) / (2) cj-style 1st release 2번째 진입점 = bmad-create-story spec (cj-style 63번째): 1st release wire spec (T1~T8) 결정 wire 진입 대기 / (3) cj-style 1st release 3번째 진입점 = bmad-dev-story atomic wire (cj-style 64번째): 1st release 본체 wire T1~T8 atomic single sprint (T1 Landing page + T2 ToS/Privacy + T3 Onboarding guide + T4 Support channels + T5 Production verification + T6 Capability v1.27 + T7 Tests + T8 Launch comms + 3중 게이트 FINAL CLEAN atomic commit) / (4) cj-style 1st release 4번째 진입점 = 1st release close-out retro (cj-style 65번째): A83~A87 close-out retro + A19 cohesion 9 surface EXTENSION PASS 검증 (launch surface EXTENSION) + launch checklist 6 conditions ALL PASS 검증. A19 cohesion pattern 9 surface EXTENSION PASS 결정 (launch surface EXTENSION = F18.1~F18.6 launch territory). estimated ~30 NEW pytest PASS + ~20 NEW vitest PASS + 0 NEW ruff + 0 regressions. 결정 wire 일자: 2026-08-22 (KST).

## Epic 15 (in-progress, 2026-08-22 — cj-style Epic 15 1번째 진입점 = Epic 15 PRD entry DONE 진입 wire = cj-style 58번째 epic 연속 정직 회복, Epic 15 wire 진입 대기 = cj-style Epic 15 2번째 진입점 = cj-style 59번째 epic 연속 정직 회복 진입 대기)

- **Magic link + Social OAuth + SSO enterprise SAML territory 진입 (옵션 (a) Epic 15 진입, A70+A71+A72 결정 wire 진입)** — Epic 15 PRD entry DONE 2026-08-22 (cj-style Epic 15 1번째 진입점 = cj-style 58번째 epic 연속 정직 회복, master PRD v3.1 → v3.2 atomic edit). Epic 15 territory = Magic link login (Supabase `signInWithOtp` + 5회 cool-down + email 존재 여부 노출 방지 + audit-first INSERT `magic_link_sent`) + Social OAuth Google/Naver/Kakao (Supabase `signInWithOAuth` + provider whitelist + 3회 cool-down + audit-first INSERT `social_oauth_initiated` + OAuth callback handler) + SSO enterprise SAML (`python3-saml==1.16.0` AD-14 stack pin + SAML response validation + JIT user provisioning + multi-tenant isolation RLS + audit-first INSERT `sso_identity_linked`) 결정 wire + ko-KR.json SSOT EXTENSION (auth.magic_link.* + auth.social.* + auth.sso.* namespace) + alembic 0037 `external_identities` table (multi-tenant RLS policy) + capability matrix v1.25 → v1.26 EXTENSION 5 NEW rows (MAGIC_LINK + SOCIAL_OAUTH_GOOGLE + SOCIAL_OAUTH_NAVER + SOCIAL_OAUTH_KAKAO + SSO_ENTERPRISE, industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러) + AD-28 Magic link + Social OAuth + SSO enterprise SAML 신규 결정. Epic 1 carry-over D-1-1-DEFER-1/2/3 honestly ✅ RESOLVED 58번째 epic 연속 정직 회복 (CR 11-3 discipline). Phase 3-1 wire (`d3e7454`) 의 Supabase SSR + sb-access-token cookie + auth route group (auth) + dashboard route group (dashboard) + auth middleware EXTENSION + Epic 12 2FA 게이트 보존 + Phase 4 wire (`71a033a`) 의 Vercel + Railway + Supabase + Sentry + observability 결정 wire 보존 + Epic 13/14 wire 의 LISTEN/NOTIFY multi-process coordination 결정 wire 보존. **Epic 15 PRD entry wire scope (master PRD v3.2 atomic edit)**: (1) front matter title v3.1 → v3.2 + changelog v3.2 entry / (2) §F17 신규 (F17.1 Magic link / F17.2 Social OAuth / F17.3 SSO enterprise SAML / F17.4 ko-KR.json SSOT EXTENSION / F17.5 capability matrix v1.26 EXTENSION 5 NEW rows / F17.6 tests + wire scope T1~T8 결정) / (3) §8.1 M0-(h) Magic link + M0-(i) Social OAuth + M0-(j) SSO enterprise SAML 3 NEW 인수 불릿 / (4) §15 로드맵 Epic 15 row status 백로그 → in-progress (PRD entry DONE 진입 wire) / (5) §부록 A A70+A71+A72 ✅ done + A75 preserved + A79+A80+A81+A82 신규 결정 표 / (6) AD-28 Magic link + Social OAuth + SSO enterprise SAML 신규 결정 (Supabase `signInWithOtp` + `signInWithOAuth` + `python3-saml==1.16.0` AD-14 stack pin + JIT user provisioning + multi-tenant isolation CR 0-2 RLS lesson + audit-first INSERT 3 NEW) / (7) capability matrix v1.25 → v1.26 EXTENSION 5 NEW rows industry-agnostic 4-industry grants ✅/✅/✅/✅. **Epic 15 진입 flow (cj-style 1~4번째 진입점 결정 보존)**: (1) cj-style Epic 15 1번째 진입점 = Epic 15 PRD entry (cj-style 58번째): ✅ DONE 2026-08-22 (master PRD v3.2 atomic edit) / (2) cj-style Epic 15 2번째 진입점 = bmad-create-story spec (cj-style 59번째): Epic 15 wire spec (T1~T8) 결정 wire 진입 대기 / (3) cj-style Epic 15 3번째 진입점 = bmad-dev-story atomic wire (cj-style 60번째): Epic 15 본체 wire T1~T8 atomic single sprint (T1 Magic link wrapper + T2 Magic link UI + T3 Social OAuth wrapper + T4 OAuth callback + T5 SSO SAML backend + T6 SSO UI + T7 capability v1.26 + T8 tests + 3중 게이트 FINAL CLEAN atomic commit) / (4) cj-style Epic 15 4번째 진입점 = Epic 15 close-out retro (cj-style 61~62번째): A70+A71+A72 honestly RESOLVE 검증 + A19 cohesion 9 surface EXTENSION PASS 검증 (auth surface EXTENSION) + D-1-1-DEFER-1/2/3 grep guard 58~61~62번째 epic 연속 정직 회복 검증. A19 cohesion pattern 9 surface EXTENSION PASS 결정 (auth surface EXTENSION = F17.1~F17.3 magic link + social OAuth + SSO enterprise territory). estimated ~60 NEW pytest PASS + ~50 NEW vitest PASS + 0 NEW ruff + 0 regressions. 결정 wire 일자: 2026-08-22 (KST).

## Epic 14 (in-progress, 2026-08-20 — cj-style Epic 14 1번째 진입점 = Epic 14 PRD entry DONE 진입 wire = cj-style 45번째 epic 연속 정직 회복, 14-1 atomic wire 진입 대기 = cj-style Epic 14 2번째 진입점 = cj-style 46번째 epic 연속 정직 회복 진입 대기)

- **LISTEN/NOTIFY Consume 2nd Batch** — D-13-1-DEFER-3 separate epic territory 결정 wire 진입 (옵션 (a) Epic 14 진입). **Epic 14 PRD entry DONE 2026-08-20** (cj-style Epic 14 1번째 진입점 = cj-style 45번째 epic 연속 정직 회복, master PRD v2.4 → v2.5 atomic edit). Epic 14 territory = cross-tenant invalidation fan-out (multi-tenant isolation 검증 + tenant-level subscription routing) + multi-process coordination (multi-worker 환경 listener process-per-pod, **PostgreSQL LISTEN/NOTIFY multi-process coordination 결정 wire 진입** = A58 결정, Option 1 verbatim 보존 결정, Option 2 Redis pub/sub rejected rationale: G2 "새벽에 혼자 고칠 수 있는 시스템" 정합 — 인프라 최소화). Epic 13 close-out retro §7 A53 결정 verbatim wire (cj-style Epic 13 5번째 진입점 = cj-style Epic 13 carry-over 17번째 docs only). A45+A46 결정 wire 진입 = 옵션 (a) Epic 14 follow-up sprint 진입 결정 (bundled into ONE Epic 14 carry-over sprint = cj-style Epic 14 3번째 진입점 진입 시점). **Epic 14 PRD entry wire scope (master PRD v2.5 atomic edit)**: (1) front matter title v2.4 → v2.5 + changelog v2.5 entry / (2) §F14 신규 (F14.1 cross-tenant invalidation fan-out 토폴로지 + F14.2 multi-process coordination leader/follower + F14.3 V8 determinism + cross-language drift detector EXTENSION + F14.4 tests + wire scope T1~T9 결정) / (3) §8.1 M10-(d)·§F10.1-(d) cross-tenant fan-out EXTENSION 결정 wire 진입 / (4) §15 로드맵 Epic 14 row status 백로그 → in-progress (PRD entry DONE 진입 wire) / (5) §부록 A A57+A58+A59 신규 결정 표 / (6) AD-25 EXTENSION 4-channel → 5+ channels 결정 wire (cross_tenant_fanout channel 추가) / (7) capability matrix v1.22 → v1.23 신규 2 rows (`LISTEN_NOTIFY_TENANT_FANOUT` + `LISTEN_NOTIFY_MULTIPROCESS`, industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러). **Epic 14 진입 flow (cj-style 1~3번째 진입점 결정 보존)**: (1) cj-style Epic 14 1번째 진입점 = Epic 14 PRD entry (cj-style 45번째): ✅ DONE 2026-08-20 (master PRD v2.5 atomic edit) / (2) cj-style Epic 14 2번째 진입점 = Story 14.1 bmad-dev-story atomic wire (cj-style 46번째): cross-tenant invalidation fan-out + multi-process coordination 본체 wire (T1~T9 atomic single sprint) 진입 대기 / (3) cj-style Epic 14 3번째 진입점 = Epic 14 close-out retro (cj-style 47번째): A45+A46 follow-up sprint 진입 결정 (bundled into ONE Epic 14 carry-over sprint). Epic 14 보류 결정 (cj-style Epic 14 진입 시점에 동시 결정 wire): A55 LISTEN/NOTIFY 실측 evidence 정합 sweep (D-13-1-DEFER-2 ✅ RESOLVE) + A56 A42 A36 SDR 검증 4-step 보존 + Epic 14+ 적용 ✅ done + preserved. **Story 14-1 wire scope 결정 보존** (cj-style Epic 14 2번째 진입점 진입 시점에 wire): T1 alembic 0034 + T2 listener EXTENSION multi-process coordination + T3 main.py lifespan EXTENSION leader election + T4 cross-tenant fan-out + multi-process dispatch adapters + T5 capability 2 NEW rows v1.23 + T6 V8 determinism EXTENSION + T7 cross-lang drift EXTENSION + T8 multi-process coordination tests + T9 3중 게이트 FINAL CLEAN atomic commit. estimated ~140 NEW pytest PASS (across 9 test files) + 0 NEW ruff + 0 regressions.

## Phase 10 (DONE, 2026-08-24 — cj-style Phase 10 4-entry-point pattern 모두 wire DONE 진입: Phase 10 PRD entry 101번째 + Phase 10 spec entry 102번째 + Phase 10 atomic wire T1~T8 103번째 + Phase 10 close-out retro 104번째 = 4-entry-point ALL DONE. Phase 10 territory DONE 정합)

## Phase 11 (in-progress, 2026-08-24 — cj-style Phase 11 1번째 진입점 = Phase 11 PRD entry DONE 진입 wire = cj-style 105번째 epic 연속 정직 회복, Phase 11 wire 진입 대기 = cj-style Phase 11 2번째 진입점 = cj-style 106번째 epic 연속 정직 회복 진입 대기)

- **FinOps Showback / Chargeback territory 진입 (옵션 (a) Phase 11+ 진입 + 옵션 (d) FinOps Showback / Chargeback (Recommended), A318 결정 wire 진입)** — Phase 11 PRD entry DONE 2026-08-24 (cj-style Phase 11 1번째 진입점 = cj-style 105번째 epic 연속 정직 회복, master PRD v4.1 → v4.2 atomic edit). Phase 11 territory = (1) **showback DSL + period selector + comparison view** (`apps/api/modules/finops/showback_dsl.py` NEW ~+150 LOC + `showback(tenant_id, period_key, department_id, group_by)` builder + 5 group_by 옵션 결정 + 6 period selector 모드 결정 + comparison view + 4 industries baseline + per-tenant override EXTENSION + audit-first INSERT `showback_generated`) + `showback_query.py` NEW ~+120 LOC + DepartmentBreakdown + ComparisonView TypedDict CR 12-5 D-PARITY-01 verbatim / (2) **chargeback cost allocation engine** (`apps/api/modules/finops/chargeback_engine.py` NEW ~+180 LOC + chargeback rule 3종 정액제/비율배분/usage-based + markup + tax + cost_allocation_method enum + 2 NEW error class `ChargebackRuleInvalidError(400)` + `ChargebackCalculationError(500)`) + `chargeback_rule_evaluator.py` NEW ~+100 LOC / (3) **department cost center mapping** (`apps/api/modules/finops/department_mapping.py` NEW ~+120 LOC + `tenant_settings.cost_center_mapping` JSONB TypedDict + UNIQUE constraint + RLS policy CR 0-2 verbatim + audit-first INSERT `department_mapping_updated`) / (4) **showback dashboard UI** (`apps/web/app/[locale]/(dashboard)/admin/finops/page.tsx` NEW ~+150 LOC + 4 components `ShowbackPeriodSelector` + `ShowbackDepartmentBreakdownChart` + `ShowbackComparisonView` + `ShowbackCSVExportButton` + owner-only RBAC AD-22 + Epic 12 2FA 챌린지 보존 + ko-KR.json `finops.*` namespace EXTENSION ~25 keys CR 11-4 D-002 verbatim SSOT) / (5) **chargeback CSV/PDF export** (`apps/api/modules/finops/chargeback_export.py` NEW ~+150 LOC + `export_chargeback_csv` StreamingResponse + `export_chargeback_pdf` bytes + reportlab AD-14 stack pin + audit-first INSERT `chargeback_exported` + audit-first INSERT 3 NEW `showback_generated` + `department_mapping_updated` + `chargeback_exported` CR 1-1 verbatim + ActionClass.FINOPS 신규 정의 + apps/api/core/audit_action.py MODIFIED AuditAction Literal EXTENSION 3 NEW values + _ActionRegistry FINOPS entry 신규 3개 등록 + __all__ EXTENSION) / (6) **Capability gate FINOPS_SHOWBACK + FINOPS_CHARGEBACK** (capability matrix v1.35 → v1.36 EXTENSION 2 NEW rows industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러, SLO_ENGINEERING Phase 10 wire + CHAOS_ENGINEERING Phase 9 wire + PERFORMANCE_TESTING Phase 8 wire + OBSERVABILITY_TRACES + OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind + `apps/api/core/capability.py` MODIFIED + Capability.FINOPS_SHOWBACK = "finops_showback" 1 NEW enum + Capability.FINOPS_CHARGEBACK = "finops_chargeback" 1 NEW enum + `apps/api/dependencies/capability.py` MODIFIED require_finops_showback + require_finops_chargeback 2 NEW deps + drift detector `tests/integration/test_capability_matrix_v1_36_drift.py` NEW 10 NEW pytest cases) + AD-38 FinOps Showback / Chargeback 신규 결정 ((a)~(g) 7 sub-decisions 모두 결정 wire 진입). Phase 10 close-out retro `733d428` (cj-style 104번째) + Phase 10 atomic wire T1~T8 `ac5d6c5` (cj-style 103번째) + Phase 10 spec entry `3c80ef0` (cj-style 102번째) + Phase 10 PRD entry `09db4d4` (cj-style 101번째) 결정 verbatim preserve + EXTENSION (Phase 10 territory DONE 정합) + Phase 9 close-out retro `634427d` (cj-style 100번째) + Phase 9 atomic wire T1~T8 `e7670e1` (cj-style 99번째) + Phase 9 spec entry `2a5e4da` (cj-style 98번째) + Phase 9 PRD entry `0b2d2f3` (cj-style 97번째) + Phase 8 close-out retro `ab495a8` (cj-style 96번째) + Phase 8 atomic wire T1~T8 `60d4ea1` (cj-style 95번째) + Phase 8 spec entry `5ae0f4e` (cj-style 94번째) + Phase 8 PRD entry `ced452f` (cj-style 93번째) + build-fixes sprint `eaee198` + Phase 7 close-out retro `326fa9f` (cj-style 92번째) + Phase 7 atomic wire T1~T8 `59b56cd` (cj-style 91번째) + Phase 7 spec entry `749381e` (cj-style 90번째) + Phase 7 PRD entry `916a541` (cj-style 89번째) + Phase 6 close-out retro `f9f006c` (cj-style 88번째) + Phase 6 atomic wire T1~T8 `24e1cd7` (cj-style 87번째) + Phase 6 spec entry `f5c14c9` (cj-style 86번째) + Phase 6 PRD entry `e84a281` (cj-style 85번째) + Epic 17 close-out retro `be8f3bd` (cj-style 84번째) + Epic 17 T2+T3 UI wire `bb92879` (cj-style 83번째) + Epic 17 wire `2ada2ec` (cj-style 82번째) + Epic 17 spec entry `f4b2b58` (cj-style 81번째) + Epic 17 PRD entry `40a9c41` (cj-style 80번째) + Sidebar/MenuProvider hot-fix `01a06e4` (cj-style 79번째) + D-EPIC-16-REVIEW-DEFER-2~6 RESOLVE sprint `512ed6a` (cj-style 78번째) + Phase 5 close-out retro `b843565` (cj-style 76~77번째) + Phase 5 wire `f093f8c` (cj-style 75번째) + Phase 5 spec entry (cj-style 74번째) + Phase 5 PRD entry `93d852b` (cj-style 73번째) + Epic 16 close-out retro (cj-style 72번째) + Epic 16 T4 admin UI follow-up `ff5c3b5` (cj-style 71번째) + Epic 16 review follow-up `963079c` (cj-style 70번째) + Epic 16 atomic wire `e117e09` (cj-style 69번째) + Epic 16 PRD entry `08bfca5` (cj-style 67번째) + 1st release cycle cj-style 62~66번째 모두 wire DONE 진입 + Epic 15 cycle cj-style 58~61번째 모두 wire DONE 진입 + Phase 4 cycle cj-style 53~57번째 모두 wire DONE 진입 + Phase 3 cycle cj-style 49~52번째 모두 wire DONE 진입 결정 wire 모두 보존. **Phase 11 PRD entry wire scope (master PRD v4.2 atomic edit)**: (1) front matter title v4.1 → v4.2 + changelog v4.2 entry prepend / (2) §F27 신규 (F27.1 showback DSL + F27.2 chargeback cost allocation engine + F27.3 department cost center mapping + F27.4 showback dashboard UI + F27.5 chargeback CSV/PDF export + F27.6 Capability gate FINOPS_SHOWBACK + FINOPS_CHARGEBACK + F27.7 dry-run + Tests + wire scope T1~T8 결정) / (3) §8.1 M0-(t) FinOps Showback / Chargeback AC 신규 / (4) §15 로드맵 Phase 11 row status 백로그 → in-progress (PRD entry DONE 진입 wire) / (5) §부록 A A313+A314+A315+A316+A317+A318+A319+A320+A321+A322+A323 신규 결정 표 / (6) AD-38 FinOps Showback / Chargeback 신규 결정 (FinOps architecture 7 sub-decisions) / (7) capability matrix v1.35 → v1.36 EXTENSION FINOPS_SHOWBACK + FINOPS_CHARGEBACK 2 NEW rows industry-agnostic 4-industry grants ✅/✅/✅/✅. **Phase 11 진입 flow (cj-style 1~3번째 진입점 결정 보존)**: (1) cj-style Phase 11 1번째 진입점 = Phase 11 PRD entry (cj-style 105번째): ✅ DONE 2026-08-24 (master PRD v4.2 atomic edit) / (2) cj-style Phase 11 2번째 진입점 = Phase 11 bmad-create-story spec (cj-style 106번째): Phase 11 wire spec (T1~T8) 결정 wire 진입 대기 / (3) cj-style Phase 11 3번째 진입점 = Phase 11 bmad-dev-story atomic wire T1~T8 (cj-style 107번째): Phase 11 본체 wire T1~T8 atomic single sprint (T1 showback_dsl + showback_query module + T2 chargeback_engine + chargeback_rule_evaluator + T3 department_mapping + tenant_settings JSONB schema + T4 chargeback CSV/PDF export + T5 alembic 0043 phase_11_finops + T6 audit action EXTENSION 3 NEW + T7 capability v1.36 EXTENSION + frontend finops dashboard + T8 3중 게이트 FINAL CLEAN atomic commit) / (4) cj-style Phase 11 4번째 진입점 = Phase 11 close-out retro (cj-style 108번째): A313~A323 close-out retro + A19 cohesion 9 surface EXTENSION PASS 검증 (FinOps showback/chargeback surface NEW = F27.1~F27.7 FinOps Showback / Chargeback territory + Epic 7~10 ABC/TDABC + Phase 7 wire `59b56cd` 의 7 NEW business metrics + Phase 8 wire `60d4ea1` 의 k6 부하 테스트 + Epic 17 wire `2ada2ec` 의 audit log viewer + Phase 10 wire `ac5d6c5` 의 SLO engineering carry-over chain 정합). A19 cohesion pattern 9 surface EXTENSION PASS 결정 (FinOps showback/chargeback surface NEW). estimated ~+46 NEW pytest PASS + ~+5 NEW vitest PASS + 0 NEW ruff + 0 regressions. 결정 wire 일자: 2026-08-24 (KST).

- **SLO Engineering / Error Budget Management territory 진입 (옵션 (a) Phase 10+ 진입, A303 결정 wire 진입)** — Phase 10 PRD entry DONE 2026-08-24 (cj-style Phase 10 1번째 진입점 = cj-style 101번째 epic 연속 정직 회복, master PRD v4.0 → v4.1 atomic edit). Phase 10 territory = (1) **SLO definition DSL** (`apps/api/modules/slo/slo_definition_dsl.py` NEW ~+150 LOC, syntax: `slo(service, indicator, target, window, comparator)` builder + 6 SLI types (availability + latency_p50 + latency_p95 + latency_p99 + latency_p999 + throughput) + 5 evaluation windows (1h + 6h + 24h + 7d + 30d) + 4 comparators (lt + lte + gt + gte) + 4 industries baseline + per-tenant override EXTENSION + policy evaluation precedence tenant override > industry baseline > system default + `SLO_TARGET_DEFAULTS = {'availability': 0.999, 'latency_p95_seconds': 1.0, 'latency_p99_seconds': 5.0, 'throughput_rps': 100.0}` constants) / (2) **multi-window burn-rate evaluation** (`apps/api/modules/slo/slo_burn_rate.py` NEW ~+180 LOC, Google SRE Workbook multi-window multi-burn-rate criteria verbatim — (1h, 14.4×) + (6h, 6×) + (24h, 3×) + (7d, 1×) 4 window/burn-rate pairs 결정 wire + burn_rate = error_rate / (1 - SLO_target) calculation + burn_rate_alert fast_burn + slow_burn classification + SLOViolationError typed exception CR 12-5 D-14 envelope) / (3) **error budget tracker** (`apps/api/modules/slo/slo_error_budget.py` NEW ~+150 LOC + ErrorBudget TypedDict 6 fields (slo_id + window + total_budget_minutes + consumed_minutes + remaining_minutes + burn_rate_pct) + monthly budget reset KST 1일 00:00 + 30d rolling calculation + per-tenant error_budget_remaining visualization + AD-22 owner-only view + 4 industries baseline industry-agnostic) / (4) **multi-region SLO aggregation** (`apps/api/modules/slo/slo_multi_region.py` NEW ~+120 LOC + MultiRegionSLOAggregator 정합 Phase 5 wire `f093f8c` `phase_5_replication_lag` table + primary Seoul + secondary Tokyo replica 의 SLI 데이터 aggregate + worst-region SLO = primary aggregation strategy + SLOViolationMultiRegionError envelope + cross-region observability carry-over) / (5) **tenant-scoped SLO override + audit-first INSERT** (`apps/api/modules/slo/slo_tenant_scoped.py` NEW ~+100 LOC + RLS 자동 적용 CR 0-2 verbatim + per-tenant SLO target override EXTENSION + tenant_settings.slo_overrides TypedDict + audit-first INSERT 3 NEW actions: `slo_target_updated` + `slo_budget_exhausted` + `slo_violation_detected` CR 1-1 verbatim 적용 + ActionClass.SLO_ENGINEERING 신규 정의 + apps/api/core/audit_action.py MODIFIED AuditAction Literal EXTENSION 3 NEW + _ActionRegistry SLO_ENGINEERING entry 신규 3개 등록 + __all__ EXTENSION + emit_audit_typed BEFORE SLO update CR 1-1 verbatim 적용 + Phase 9 wire `e7670e1` 의 4 NEW AuditAction Literal EXTENSION pattern verbatim 적용) / (6) **SLO governance review + auto-rollback trigger** (`apps/api/modules/slo/slo_governance.py` NEW ~+120 LOC + weekly review cron KST 매주 일요일 02:00 UTC 17:00 + monthly review KST 1일 01:00 UTC 16:00 + SLO review document generation `docs/slo-review-{YYYY-MM-DD}.md` + SLOViolation → auto-rollback trigger Phase 9 chaos_experiment 정합 (auto_rollback module integration) + SLO dashboard `apps/web/app/[locale]/(dashboard)/admin/slo/page.tsx` NEW ~+150 LOC + 4 components (SloStatusCard + SloBurnRateChart + SloErrorBudgetGauge + SloViolationTimeline) + ko-KR.json `slo.*` namespace EXTENSION 10 keys CR 11-4 D-002 verbatim SSOT + vitest RTL render discipline CR 11-4 D-003 verbatim 결정 wire) / (7) **Capability gate SLO_ENGINEERING** (capability matrix v1.34 → v1.35 EXTENSION SLO_ENGINEERING 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러, CHAOS_ENGINEERING Phase 9 wire + PERFORMANCE_TESTING Phase 8 wire + OBSERVABILITY_TRACES + OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind + `apps/api/core/capability.py` MODIFIED + Capability.SLO_ENGINEERING = "slo_engineering" 1 NEW enum + `apps/api/dependencies/capability.py` MODIFIED require_slo_engineering + drift detector `tests/integration/test_capability_matrix_v1_35_drift.py` NEW 8 NEW pytest cases) + AD-37 SLO Engineering / Error Budget Management 신규 결정 ((a)~(g) 7 sub-decisions 모두 결정 wire 진입). Phase 9 close-out retro `634427d` (cj-style 100번째) + Phase 9 atomic wire T1~T8 `e7670e1` (cj-style 99번째) + Phase 9 spec entry `2a5e4da` (cj-style 98번째) + Phase 9 PRD entry `0b2d2f3` (cj-style 97번째) 결정 verbatim preserve + EXTENSION (Phase 9 territory DONE 정합) + Phase 8 close-out retro `ab495a8` (cj-style 96번째) + Phase 8 atomic wire T1~T8 `60d4ea1` (cj-style 95번째) + Phase 8 spec entry `5ae0f4e` (cj-style 94번째) + Phase 8 PRD entry `ced452f` (cj-style 93번째) + Phase 7 close-out retro `326fa9f` (cj-style 92번째) + Phase 7 atomic wire T1~T8 `59b56cd` (cj-style 91번째) + Phase 7 spec entry `749381e` (cj-style 90번째) + Phase 7 PRD entry `916a541` (cj-style 89번째) + Phase 6 close-out retro `f9f006c` (cj-style 88번째) + Phase 6 atomic wire T1~T8 `24e1cd7` (cj-style 87번째) + Phase 6 spec entry `f5c14c9` (cj-style 86번째) + Phase 6 PRD entry `e84a281` (cj-style 85번째) + Epic 17 close-out retro `be8f3bd` (cj-style 84번째) + Epic 17 T2+T3 UI wire `bb92879` (cj-style 83번째) + Epic 17 wire `2ada2ec` (cj-style 82번째) + Epic 17 spec entry `f4b2b58` (cj-style 81번째) + Epic 17 PRD entry `40a9c41` (cj-style 80번째) + Sidebar/MenuProvider hot-fix `01a06e4` (cj-style 79번째) + D-EPIC-16-REVIEW-DEFER-2~6 RESOLVE sprint `512ed6a` (cj-style 78번째) + Phase 5 close-out retro `b843565` (cj-style 76~77번째) + Phase 5 wire `f093f8c` (cj-style 75번째) + Phase 5 spec entry (cj-style 74번째) + Phase 5 PRD entry `93d852b` (cj-style 73번째) + Epic 16 close-out retro (cj-style 72번째) + Epic 16 T4 admin UI follow-up `ff5c3b5` (cj-style 71번째) + Epic 16 review follow-up `963079c` (cj-style 70번째) + Epic 16 atomic wire `e117e09` (cj-style 69번째) + Epic 16 spec entry (cj-style 68번째) + Epic 16 PRD entry `08bfca5` (cj-style 67번째) + 1st release cycle cj-style 62~66번째 모두 wire DONE 진입 + Epic 15 cycle cj-style 58~61번째 모두 wire DONE 진입 + Phase 4 cycle cj-style 53~57번째 모두 wire DONE 진입 + Phase 3 cycle cj-style 49~52번째 모두 wire DONE 진입 결정 wire 모두 보존. **Phase 10 PRD entry wire scope (master PRD v4.1 atomic edit)**: (1) front matter title v4.0 → v4.1 + changelog v4.1 entry prepend / (2) §F26 신규 (F26.1 SLO definition DSL + F26.2 multi-window burn-rate evaluator + F26.3 error budget tracker + F26.4 multi-region SLO aggregation + F26.5 tenant-scoped SLO override + F26.6 SLO governance review + auto-rollback trigger + F26.7 Capability gate SLO_ENGINEERING + F26.8 tests + wire scope T1~T8 결정) / (3) §8.1 M0-(s) SLO Engineering AC 신규 / (4) §15 로드맵 Phase 10 row status 백로그 → in-progress (PRD entry DONE 진입 wire) / (5) §부록 A A303+A304+A305+A306+A307 신규 결정 표 / (6) AD-37 SLO Engineering / Error Budget Management 신규 결정 (SLO engineering architecture 7 sub-decisions) / (7) capability matrix v1.34 → v1.35 EXTENSION SLO_ENGINEERING 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅. **Phase 10 진입 flow (cj-style 1~3번째 진입점 결정 보존)**: (1) cj-style Phase 10 1번째 진입점 = Phase 10 PRD entry (cj-style 101번째): ✅ DONE 2026-08-24 (master PRD v4.1 atomic edit) / (2) cj-style Phase 10 2번째 진입점 = Phase 10 bmad-create-story spec (cj-style 102번째): Phase 10 wire spec (T1~T8) 결정 wire 진입 대기 / (3) cj-style Phase 10 3번째 진입점 = Phase 10 bmad-dev-story atomic wire T1~T8 (cj-style 103번째): Phase 10 본체 wire T1~T8 atomic single sprint (T1 SLO definition DSL module + T2 multi-window burn-rate evaluator + T3 error budget tracker + T4 multi-region SLO aggregator + T5 tenant-scoped SLO override + audit-first INSERT 3 NEW + T6 SLO governance + auto-rollback trigger + T7 capability v1.35 EXTENSION + T8 3중 게이트 FINAL CLEAN atomic commit) / (4) cj-style Phase 10 4번째 진입점 = Phase 10 close-out retro (cj-style 104번째): A303~A307 close-out retro + A19 cohesion 9 surface EXTENSION PASS 검증 (SLO engineering surface NEW = F26.1~F26.6 SLO Engineering / Error Budget Management territory + Phase 8 SLO/SLI 정의 + Phase 9 chaos_experiment baseline carry-over chain 정합). A19 cohesion pattern 9 surface EXTENSION PASS 결정 (SLO engineering surface NEW). estimated ~+37 NEW pytest PASS + ~+5 NEW vitest PASS + 0 NEW ruff + 0 regressions. 결정 wire 일자: 2026-08-24 (KST).

## Phase 14 (in-progress, 2026-08-25 — cj-style Phase 14 1번째 진입점 = Phase 14 PRD entry DONE 진입 wire = cj-style 117번째 epic 연속 정직 회복, Phase 14 wire 진입 대기 = cj-style Phase 14 2번째 진입점 = cj-style 118번째 epic 연속 정직 회복 진입 대기)

- **FinOps Optimization & Rightsizing territory 진입 (옵션 (a) Phase 14+ 진입 + 옵션 (a) FinOps Optimization & Rightsizing (Recommended), A414 결정 wire 진입)** — Phase 14 PRD entry DONE 2026-08-25 (cj-style Phase 14 1번째 진입점 = cj-style 117번째 epic 연속 정직 회복, master PRD v4.4 → v4.5 atomic edit). Phase 14 territory = (1) **optimization definition DSL** (`apps/api/modules/finops/optimization_definition.py` NEW ~+150 LOC, syntax: `define_optimization(tenant_id, resource_type, optimization_strategy, target_metric, baseline_period)` builder + AST 5 levels + 5 resource_type 옵션 (compute + storage + database + network + container) + 7 optimization_strategy 옵션 (rightsize_down + rightsize_up + idle_terminate + commit_1y + commit_3y + storage_tier_down + composite) + 4 target_metric 옵션 (cost_saving_pct + cost_saving_amount + utilization_target + commit_break_even_months) + 5 baseline_period 옵션 (last_7d + last_30d + last_90d + last_180d + last_365d) + 4 industries baseline + per-tenant override EXTENSION + policy evaluation precedence tenant override > industry baseline > system default + `OPTIMIZATION_DEFAULTS = {'resource_type': 'compute', 'optimization_strategy': 'composite', 'target_metric': 'cost_saving_pct', 'baseline_period': 'last_30d', 'idle_cpu_threshold_pct': 5.0, 'idle_detection_window_days': 30, 'min_savings_amount_krw': 10000}` constants + audit-first INSERT `optimization_definition_updated` CR 1-1 verbatim) / (2) **rightsizing engine** (`apps/api/modules/finops/rightsizing_engine.py` NEW ~+200 LOC + 5 resource types (compute EC2 instance type 한 단계 downsize/upsize + storage tier downgrade S3 Standard → IA → Glacier + database RDS instance class 한 단계 downsize + network EIP/NAT/LB + container EKS node group) + Phase 13 wire `8b98030` capacity_headroom_report 의 90일 lookahead forecast EXTENSION + 80+ AWS EC2 instance type mapping + 3 storage tier mapping + RightsizingRecommendation TypedDict 14 fields (recommendation_id + tenant_id + resource_id + resource_type + current_instance_type + recommended_instance_type + current_cost_krw + recommended_cost_krw + projected_savings_pct + projected_savings_amount_krw + confidence_score + severity enum + model_version + generated_at) + audit-first INSERT `recommendation_generated` CR 1-1 verbatim) / (3) **idle resource detection** (`apps/api/modules/finops/idle_resource_detector.py` NEW ~+180 LOC + z-score < -2.0 기반 + compute idle (CPU < 5% for 30d) + storage idle (S3 last_accessed > 30d + EBS unattached + snapshot > 90d) + database idle (RDS connection_count_p95 = 0 for 30d) + network idle (EIP associated=false + NAT gateway bytes=0 + LB request < 100/day) + container idle (EKS desired_count × utilization < 30%) + IdleResource TypedDict 13 fields + audit-first INSERT `idle_resource_detected` CR 1-1 verbatim) / (4) **RI/SP commitment recommendations** (`apps/api/modules/finops/commitment_recommender.py` NEW ~+150 LOC + commitment_type enum (ec2_ri + rds_ri + ec2_sp + s3_sp + redshift_sp + dynamodb_sp) + commitment_term enum (1_year + 3_year) + break-even calculation `break_even_months = upfront_cost / monthly_savings` + 1y ≤ 8mo / 3y ≤ 18mo break-even threshold + utilization_forecast 기반 권고 + Phase 13 wire `8b98030` forecast 12-month baseline EXTENSION + CommitmentRecommendation TypedDict 12 fields + ROI calculation + per-tenant override + audit-first INSERT `commitment_recommended` CR 1-1 verbatim) / (5) **optimization accuracy tracking** (`apps/api/modules/finops/optimization_accuracy_tracker.py` NEW ~+120 LOC + Phase 13 wire `8b98030` forecast_accuracy_tracker EXTENSION + precision (TP / (TP + FP)) + recall (TP / (TP + FN)) + realized_savings vs projected_savings + model performance degradation detection (accuracy_score < 70% for 3 consecutive months → retraining trigger) + retraining cron KST 매주 일요일 04:00 UTC 19:00 + OptimizationAccuracyReport TypedDict 10 fields + audit-first INSERT `optimization_recommended_action` + `optimization_accuracy_degraded` + `optimization_retraining_triggered` EXTENSION) / (6) **optimization dashboard UI** (`apps/web/app/[locale]/(dashboard)/admin/finops/optimization/page.tsx` NEW ~+150 LOC + 5 components (OptimizationStrategySelector + RightsizingRecommendationTable + IdleResourcePanel + CommitmentRecommendationPanel + OptimizationAccuracyPanel) + owner-only RBAC AD-22 verbatim + Epic 12 2FA 챌린지 보존 + ko-KR.json `finops_optimization.*` namespace EXTENSION ~30 keys CR 11-4 D-002 verbatim SSOT 결정 wire) / (7) **Capability matrix v1.40 EXTENSION FINOPS_OPTIMIZATION** (capability matrix v1.39 → v1.40 EXTENSION 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅ CR 12-1 L4 precedent 미러, FINOPS_FORECASTING_CAPACITY_PLANNING Phase 13 wire + FINOPS_ANOMALY_DETECTION + FINOPS_BUDGET_ALERT Phase 12 wire + FINOPS_SHOWBACK + FINOPS_CHARGEBACK Phase 11 wire + SLO_ENGINEERING Phase 10 wire + CHAOS_ENGINEERING Phase 9 wire + PERFORMANCE_TESTING Phase 8 wire + OBSERVABILITY_TRACES + OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind + `apps/api/core/capability.py` MODIFIED + Capability.FINOPS_OPTIMIZATION = "finops_optimization" 1 NEW enum + `apps/api/dependencies/capability.py` MODIFIED require_finops_optimization 1 NEW dep + drift detector `tests/integration/test_capability_matrix_v1_40_drift.py` NEW 8 NEW pytest cases) / (8) **dry-run + tests + wire scope T1~T8** (~+56 NEW pytest PASS (optimization_definition 6 + rightsizing_engine 8 + idle_resource_detector 8 + commitment_recommender 8 + optimization_accuracy_tracker 5 + alembic 0046 5 + audit action 8 + capability matrix v1.40 8 = ~+56 NEW pytest PASS) + ~+6 NEW vitest PASS (OptimizationStrategySelector + RightsizingRecommendationTable owner-only ack prompt AD-22 verbatim + IdleResourcePanel RTL render + CommitmentRecommendationPanel + ko-KR SSOT 2 + finops optimization dashboard parity CR 12-5 D-PARITY-01 결정 wire) + 0 NEW ruff + 0 regressions + T1 optimization_definition + optimization_dsl module + T2 rightsizing_engine + 5 resource types + T3 idle_resource_detector + z-score detection + T4 commitment_recommender + 1y/3y break-even + Phase 13 forecast accuracy EXTENSION + T5 alembic 0046 phase_14_optimization 5 tables + RLS 5 tables + T6 audit action EXTENSION 6 NEW + T7 capability v1.40 EXTENSION + frontend finops optimization dashboard 5 NEW components + T8 3중 게이트 FINAL CLEAN atomic commit) + AD-41 FinOps Optimization & Rightsizing 신규 결정 ((a)~(g) 7 sub-decisions 모두 결정 wire 진입). Phase 13 close-out retro `850b4f8` (cj-style 116번째) + Phase 13 atomic wire T1~T8 `8b98030` (cj-style 115번째) + Phase 13 spec entry `77ed55f` (cj-style 114번째) + Phase 13 PRD entry `d31dfc8` (cj-style 113번째) 결정 verbatim preserve + EXTENSION (Phase 13 territory DONE 정합) + Phase 12 close-out retro `3354e83` (cj-style 112번째) + Phase 12 atomic wire T1~T8 `f3c0e63` (cj-style 111번째) + Phase 12 spec entry `8c5f374` (cj-style 110번째) + Phase 12 PRD entry `344c7eb` (cj-style 109번째) + Phase 11 close-out retro `80df15b` (cj-style 108번째) + Phase 11 atomic wire T1~T8 `e020ad0` (cj-style 107번째) + Phase 11 spec entry `82c93a8` (cj-style 106번째) + Phase 11 PRD entry `16d7698` (cj-style 105번째) + Phase 10 close-out retro `733d428` (cj-style 104번째) + Phase 10 atomic wire T1~T8 `ac5d6c5` (cj-style 103번째) + Phase 10 spec entry `3c80ef0` (cj-style 102번째) + Phase 10 PRD entry `09db4d4` (cj-style 101번째) 결정 wire 모두 보존. **Phase 14 PRD entry wire scope (master PRD v4.5 atomic edit)**: (1) front matter title v4.4 → v4.5 + changelog v4.5 entry prepend / (2) §F30 신규 (F30.1 optimization definition DSL + F30.2 rightsizing engine + F30.3 idle resource detection + F30.4 RI/SP commitment recommendations + F30.5 optimization accuracy tracking + F30.6 optimization dashboard UI + F30.7 Capability gate FINOPS_OPTIMIZATION + F30.8 dry-run + Tests + wire scope T1~T8 결정) / (3) §8.1 M0-(w) FinOps Optimization & Rightsizing AC 신규 / (4) §15 로드맵 Phase 14 row status 백로그 → in-progress (PRD entry DONE 진입 wire) / (5) §부록 A A414+A415+A416+A417+A418 신규 결정 표 / (6) AD-41 FinOps Optimization & Rightsizing 신규 결정 (FinOps optimization architecture 7 sub-decisions) / (7) capability matrix v1.39 → v1.40 EXTENSION FINOPS_OPTIMIZATION 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅. **Phase 14 진입 flow (cj-style 1~4번째 진입점 결정 보존)**: (1) cj-style Phase 14 1번째 진입점 = Phase 14 PRD entry (cj-style 117번째): ✅ DONE 2026-08-25 (master PRD v4.5 atomic edit) / (2) cj-style Phase 14 2번째 진입점 = Phase 14 bmad-create-story spec (cj-style 118번째): Phase 14 wire spec (T1~T8) 결정 wire 진입 대기 / (3) cj-style Phase 14 3번째 진입점 = Phase 14 bmad-dev-story atomic wire T1~T8 (cj-style 119번째): Phase 14 본체 wire T1~T8 atomic single sprint (T1 optimization_definition + optimization_dsl module + T2 rightsizing_engine + 5 resource types + T3 idle_resource_detector + z-score detection + T4 commitment_recommender + 1y/3y break-even + Phase 13 forecast accuracy EXTENSION + optimization_accuracy_tracker module + T5 alembic 0046 phase_14_optimization 5 tables + RLS 5 tables + T6 audit action EXTENSION 6 NEW + T7 capability v1.40 EXTENSION + frontend finops optimization dashboard 5 NEW components + T8 3중 게이트 FINAL CLEAN atomic commit) / (4) cj-style Phase 14 4번째 진입점 = Phase 14 close-out retro (cj-style 120번째): A414~A418 close-out retro + A19 cohesion 9 surface EXTENSION PASS 검증 (FinOps optimization surface NEW = F30.1~F30.8 FinOps Optimization & Rightsizing territory + Phase 13 forecasting + Phase 12 anomaly detection + Phase 11 showback carry-over chain 정합). A19 cohesion pattern 9 surface EXTENSION PASS 결정 (FinOps optimization surface NEW). estimated ~+56 NEW pytest PASS + ~+6 NEW vitest PASS + 0 NEW ruff + 0 regressions. 결정 wire 일자: 2026-08-25 (KST).

## Phase 15 (in-progress, 2026-08-25 — cj-style Phase 15 1번째 진입점 = Phase 15 PRD entry DONE 진입 wire = cj-style 121번째 epic 연속 정직 회복, Phase 15 wire 진입 대기 = cj-style Phase 15 2번째 진입점 = cj-style 122번째 epic 연속 정직 회복 진입 대기)

- **FinOps Tag Governance & Cost Allocation territory 진입 (옵션 (a) Phase 15+ 진입 + 옵션 (a) FinOps Tag Governance & Cost Allocation (Recommended), A434 결정 wire 진입)** — Phase 15 PRD entry DONE 2026-08-25 (cj-style Phase 15 1번째 진입점 = cj-style 121번째 epic 연속 정직 회복, master PRD v4.5 → v4.6 atomic edit). Phase 15 territory = (1) **tag policy DSL** (`apps/api/modules/finops/tag_policy_dsl.py` NEW ~+150 LOC, syntax: `define_tag_policy(tenant_id, resource_type, tag_key, enforcement_level, default_value)` builder + AST 5 levels + 6 resource_type 옵션 (ec2 + rds + s3 + lambda + eks + vpc) + 4 enforcement_level 옵션 (required + recommended + optional + blocked) + tag_key validation (AWS 표준 + reserved prefix `aws:` 차단) + 4 default_value 옵션 (string + env_based + conditional + inherit) + 4 industries baseline + per-tenant override EXTENSION + policy evaluation precedence tenant override > industry baseline > system default + `TAG_POLICY_DEFAULTS = {'enforcement_level': 'recommended', 'default_value': 'untagged', 'compliance_threshold_pct': 95.0}` constants + audit-first INSERT `tag_policy_updated` CR 1-1 verbatim) / (2) **untagged resource detector** (`apps/api/modules/finops/untagged_resource_detector.py` NEW ~+200 LOC + 6 resource types (EC2 instance + RDS db instance + S3 bucket + Lambda function + EKS cluster + VPC endpoint/NAT/security group) + Phase 14 wire `e904485` 의 idle_resource_detector EXTENSION + 4 remediation_status (pending + notified + remediated + expired) + UntaggedResource TypedDict 14 fields + audit-first INSERT `untagged_resource_detected` CR 1-1 verbatim) / (3) **allocation rules engine** (`apps/api/modules/finops/allocation_rules_engine.py` NEW ~+220 LOC + 5 rule types (tag_match + percentage_split + weighted + conditional + fallback) + rule precedence tag_match > percentage_split > weighted > conditional > fallback + Phase 11 wire `e020ad0` 의 chargeback engine EXTENSION + AllocationRule TypedDict 13 fields + AllocatedCost TypedDict 11 fields + audit-first INSERT `allocation_rule_evaluated` + `allocation_rule_updated` 2 NEW CR 1-1 verbatim) / (4) **allocation audit + compliance** (`apps/api/modules/finops/allocation_audit.py` NEW ~+150 LOC + compliance score calculation + compliance threshold 95% default + allocation accuracy 99% threshold + allocation coverage 95% threshold + ComplianceReport TypedDict 12 fields + monthly compliance_report cron KST 1st day of month 04:00 + 3 export format PDF/CSV/JSON + compliance trend analysis last 12-month + compliance alert routing Slack + PagerDuty + Email + compliance remediation workflow 5 steps + audit-first INSERT `compliance_report_generated` + `compliance_alert_sent` + `compliance_remediation_initiated` 3 NEW CR 1-1 verbatim) / (5) **chargeback allocation reconciliation** (`apps/api/modules/finops/chargeback_allocation_reconciliation.py` NEW ~+180 LOC + 3 reconciliation strategy (chargeback_only + tag_allocation_only + hybrid_blended default) + variance calculation + ReconciliationReport TypedDict 13 fields + unmatched allocation handling + cross-period reconciliation 13-period rolling + reconciliation investigation workflow + reconciliation approval workflow owner-only RBAC AD-22 + Epic 12 2FA 챌린지 보존 + audit-first INSERT 5 NEW `reconciliation_initiated` + `reconciliation_report_generated` + `reconciliation_investigation_triggered` + `reconciliation_approved` + `reconciliation_resolved` CR 1-1 verbatim) / (6) **tag governance dashboard UI** (`apps/web/app/[locale]/(dashboard)/admin/finops/tag-governance/page.tsx` NEW ~+150 LOC + 5 components (TagPolicyEditor + UntaggedResourceDetectorPanel + AllocationRuleBuilder + ComplianceReportPanel + ReconciliationReportPanel) + owner-only RBAC AD-22 verbatim + Epic 12 2FA 챌린지 보존 + ko-KR.json `finops_tag_governance.*` namespace EXTENSION ~30 keys CR 11-4 D-002 verbatim SSOT 결정 wire) / (7) **Capability matrix v1.41 EXTENSION FINOPS_TAG_GOVERNANCE** (capability matrix v1.40 → v1.41 EXTENSION 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅ CR 12-1 L4 precedent 미러, FINOPS_OPTIMIZATION Phase 14 wire + FINOPS_FORECASTING_CAPACITY_PLANNING Phase 13 wire + FINOPS_ANOMALY_DETECTION + FINOPS_BUDGET_ALERT Phase 12 wire + FINOPS_SHOWBACK + FINOPS_CHARGEBACK Phase 11 wire + SLO_ENGINEERING Phase 10 wire + CHAOS_ENGINEERING Phase 9 wire + PERFORMANCE_TESTING Phase 8 wire + OBSERVABILITY_TRACES + OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind + `apps/api/core/capability.py` MODIFIED + Capability.FINOPS_TAG_GOVERNANCE = "finops_tag_governance" 1 NEW enum + `apps/api/dependencies/capability.py` MODIFIED require_finops_tag_governance 1 NEW dep + drift detector `tests/integration/test_capability_matrix_v1_41_drift.py` NEW 8 NEW pytest cases) / (8) **dry-run + tests + wire scope T1~T8** (~+56 NEW pytest PASS (tag_policy_dsl 6 + untagged_resource_detector 8 + allocation_rules_engine 8 + allocation_audit 6 + chargeback_allocation_reconciliation 7 + alembic 0047 5 + audit action 8 + capability matrix v1.41 8 = ~+56 NEW pytest PASS) + ~+7 NEW vitest PASS (TagPolicyEditor + UntaggedResourceDetectorPanel owner-only ack prompt AD-22 verbatim + AllocationRuleBuilder RTL render + ComplianceReportPanel + ReconciliationReportPanel + ko-KR SSOT 2 + finops tag governance dashboard parity CR 12-5 D-PARITY-01 결정 wire) + 0 NEW ruff + 0 regressions + T1 tag_policy_dsl + 4 enforcement_level + T2 untagged_resource_detector + 6 resource types + T3 allocation_rules_engine + 5 rule types + T4 allocation_audit + compliance report + chargeback_allocation_reconciliation + T5 alembic 0047 phase_15_tag_governance + T6 audit action EXTENSION 8 NEW + T7 capability v1.41 EXTENSION + frontend finops tag governance dashboard 5 NEW components + T8 3중 게이트 FINAL CLEAN atomic commit) + AD-42 FinOps Tag Governance & Cost Allocation 신규 결정 ((a)~(g) 7 sub-decisions 모두 결정 wire 진입). Phase 14 close-out retro `5b367d9` (cj-style 120번째) + Phase 14 atomic wire T1~T8 `e904485` (cj-style 119번째) + Phase 14 spec entry `30637f6` (cj-style 118번째) + Phase 14 PRD entry `0e3f8d9` (cj-style 117번째) 결정 verbatim preserve + EXTENSION (Phase 14 territory DONE 정합) + Phase 13 close-out retro `850b4f8` (cj-style 116번째) + Phase 13 atomic wire T1~T8 `8b98030` (cj-style 115번째) + Phase 13 spec entry `77ed55f` (cj-style 114번째) + Phase 13 PRD entry `d31dfc8` (cj-style 113번째) 결정 wire 모두 보존. **Phase 15 PRD entry wire scope (master PRD v4.6 atomic edit)**: (1) front matter title v4.5 → v4.6 + changelog v4.6 entry prepend / (2) §F31 신규 (F31.1 tag policy DSL + F31.2 untagged resource detector + F31.3 allocation rules engine + F31.4 allocation audit + compliance + F31.5 chargeback allocation reconciliation + F31.6 tag governance dashboard UI + F31.7 Capability gate FINOPS_TAG_GOVERNANCE + F31.8 dry-run + Tests + wire scope T1~T8 결정) / (3) §8.1 M0-(x) FinOps Tag Governance & Cost Allocation AC 신규 / (4) §15 로드맵 Phase 15 row status 백로그 → in-progress (PRD entry DONE 진입 wire) / (5) §부록 A A434+A435+A436+A437+A438 신규 결정 표 / (6) AD-42 FinOps Tag Governance & Cost Allocation 신규 결정 (FinOps tag governance architecture 7 sub-decisions) / (7) capability matrix v1.40 → v1.41 EXTENSION FINOPS_TAG_GOVERNANCE 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅. **Phase 15 진입 flow (cj-style 1~4번째 진입점 결정 보존)**: (1) cj-style Phase 15 1번째 진입점 = Phase 15 PRD entry (cj-style 121번째): ✅ DONE 2026-08-25 (master PRD v4.6 atomic edit) / (2) cj-style Phase 15 2번째 진입점 = Phase 15 bmad-create-story spec (cj-style 122번째): Phase 15 wire spec (T1~T8) 결정 wire 진입 대기 / (3) cj-style Phase 15 3번째 진입점 = Phase 15 bmad-dev-story atomic wire T1~T8 (cj-style 123번째): Phase 15 본체 wire T1~T8 atomic single sprint (T1 tag_policy_dsl + 4 enforcement_level + T2 untagged_resource_detector + 6 resource types + T3 allocation_rules_engine + 5 rule types + T4 allocation_audit + compliance report + chargeback_allocation_reconciliation + T5 alembic 0047 phase_15_tag_governance + T6 audit action EXTENSION 8 NEW + T7 capability v1.41 EXTENSION + frontend finops tag governance dashboard 5 NEW components + T8 3중 게이트 FINAL CLEAN atomic commit) / (4) cj-style Phase 15 4번째 진입점 = Phase 15 close-out retro (cj-style 124번째): A434~A438 close-out retro + A19 cohesion 9 surface EXTENSION PASS 검증 (FinOps tag governance surface NEW = F31.1~F31.8 FinOps Tag Governance & Cost Allocation territory + Phase 14 optimization + Phase 13 forecasting + Phase 12 anomaly detection + Phase 11 showback carry-over chain 정합). A19 cohesion pattern 9 surface EXTENSION PASS 결정 (FinOps tag governance surface NEW). estimated ~+56 NEW pytest PASS + ~+7 NEW vitest PASS + 0 NEW ruff + 0 regressions. 결정 wire 일자: 2026-08-25 (KST).

## Phase 16 (in-progress, 2026-08-25 — cj-style Phase 16 1번째 진입점 = Phase 16 PRD entry DONE 진입 wire = cj-style 125번째 epic 연속 정직 회복, Phase 16 wire 진입 대기 = cj-style Phase 16 2번째 진입점 = cj-style 126번째 epic 연속 정직 회복 진입 대기)

- **FinOps Reporting & Executive Dashboard territory 진입 (옵션 (a) Phase 16+ 진입 + 옵션 (a) FinOps Reporting & Executive Dashboard (Recommended), A454 결정 wire 진입)** — Phase 16 PRD entry DONE 2026-08-25 (cj-style Phase 16 1번째 진입점 = cj-style 125번째 epic 연속 정직 회복, master PRD v4.6 → v4.7 atomic edit). Phase 16 territory = (1) **executive dashboard aggregator** (`apps/api/modules/finops/executive_dashboard_aggregator.py` NEW ~+200 LOC, `aggregate_executive_rollup(tenant_id, period_key, scope) -> ExecutiveRollup` TypedDict 16 fields + 5-module cross-join EXTENSION (Phase 11 showback + Phase 12 anomaly + Phase 13 forecast + Phase 14 optimization + Phase 15 tag_governance) + 4 scope 옵션 (tenant + department + cost_center + product_line) + per-tenant override EXTENSION + audit-first INSERT `executive_dashboard_viewed` CR 1-1 verbatim) / (2) **cross-module KPI selector** (`apps/api/modules/finops/cross_module_kpi.py` NEW ~+180 LOC + 8 NEW KPI TypedDict 결정 wire = total_monthly_cost_krw + monthly_cost_growth_pct + cost_per_employee_krw + cost_anomaly_count_30d + forecast_deviation_pct + idle_cost_monthly_krw + tag_compliance_pct + optimization_realized_savings_krw + 5 mandatory + 3 optional + 4 industries baseline EXTENSION + per-tenant override EXTENSION + audit-first INSERT `executive_kpi_refreshed` CR 1-1 verbatim) / (3) **executive report generation engine** (`apps/api/modules/finops/executive_report_generator.py` NEW ~+220 LOC + 3 NEW format generator (PDF + CSV + Excel) 결정 wire + Phase 9 wire ReportGenerator EXTENSION + openpyxl 3.1.2 AD-14 stack pin EXTENSION + 3 format option 결정 wire (monthly + quarterly + annual) + audit-first INSERT `executive_report_generated` 2행 결정 wire) / (4) **scheduled dispatch** (`apps/api/modules/finops/scheduled_executive_dispatch.py` NEW ~+180 LOC + `apps/api/jobs/scheduled_executive_dispatch_job.py` NEW ~+150 LOC + APScheduler 3.10.4 AD-14 stack pin EXTENSION + KST cron 4 NEW cadence (weekly + monthly + quarterly + annual) 결정 wire + recipient_emails JSONB max 50 + audit-first INSERT `executive_report_dispatched` + `executive_scheduled_dispatch_evaluated` 2 NEW 결정 wire) / (5) **executive role RBAC owner-only** (`apps/api/core/capability.py` MODIFIED + `Capability.FINOPS_REPORTING = "finops_reporting"` 1 NEW enum + `apps/api/dependencies/capability.py` MODIFIED + `require_finops_reporting` 1 NEW dep 결정 wire + 4 industries grants ✅/✅/✅/✅ industry-agnostic CR 12-1 L4 precedent 미러 + Epic 12 2FA 챌린지 보존 + AD-22 owner-only RBAC EXTENSION + drift detector `tests/integration/test_capability_matrix_v1_42_drift.py` NEW 8 NEW pytest cases) / (6) **executive dashboard UI** (`apps/web/app/[locale]/(dashboard)/admin/finops/reporting/page.tsx` NEW ~+180 LOC + 5 sub-components (ExecutiveDashboardAggregator + CrossModuleKPISelector + ExecutiveReportGeneratorPanel + ScheduledDispatchConfigPanel + ComplianceTrendMiniChart) 결정 wire + owner-only RBAC AD-22 verbatim + Epic 12 2FA 챌린지 보존 + ko-KR.json `finops_reporting.*` namespace EXTENSION ~30 keys CR 11-4 D-002 verbatim SSOT 결정 wire) / (7) **Capability matrix v1.42 EXTENSION FINOPS_REPORTING** (capability matrix v1.41 → v1.42 EXTENSION 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅ CR 12-1 L4 precedent 미러, FINOPS_TAG_GOVERNANCE Phase 15 wire + FINOPS_OPTIMIZATION Phase 14 wire + FINOPS_FORECASTING_CAPACITY_PLANNING Phase 13 wire + FINOPS_ANOMALY_DETECTION + FINOPS_BUDGET_ALERT Phase 12 wire + FINOPS_SHOWBACK + FINOPS_CHARGEBACK Phase 11 wire + SLO_ENGINEERING Phase 10 wire + CHAOS_ENGINEERING Phase 9 wire + PERFORMANCE_TESTING Phase 8 wire + OBSERVABILITY_TRACES + OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind + `apps/api/core/capability.py` MODIFIED + Capability.FINOPS_REPORTING = "finops_reporting" 1 NEW enum + `apps/api/dependencies/capability.py` MODIFIED require_finops_reporting 1 NEW dep + drift detector `tests/integration/test_capability_matrix_v1_42_drift.py` NEW 8 NEW pytest cases) / (8) **dry-run + tests + wire scope T1~T8** (~+56 NEW pytest PASS (executive_dashboard_aggregator 8 + cross_module_kpi 8 + executive_report_generator 7 + scheduled_executive_dispatch 6 + alembic 0048 5 + audit action 8 + capability matrix v1.42 8 + dry-run 6 = ~+56 NEW pytest PASS) + ~+7 NEW vitest PASS (ExecutiveDashboardAggregator + CrossModuleKPISelector owner-only ack prompt AD-22 verbatim + ExecutiveReportGeneratorPanel RTL render + ScheduledDispatchConfigPanel + ComplianceTrendMiniChart + ko-KR SSOT 2 + finops reporting dashboard parity CR 12-5 D-PARITY-01 결정 wire) + 0 NEW ruff + 0 regressions + T1 executive_dashboard_aggregator + cross_module_kpi + T2 executive_report_generator + 3 format generators + T3 scheduled_executive_dispatch + APScheduler 4 cadence + T4 alembic 0048 phase_16_finops_reporting 5 tables + RLS 5 tables + 2 preview tables + T5 audit action EXTENSION 8 NEW + 10 NEW typed exception classes + T6 capability v1.42 EXTENSION + frontend finops reporting dashboard 5 NEW components + T7 scheduled_dispatch_job wire + T8 3중 게이트 FINAL CLEAN atomic commit) + AD-43 FinOps Reporting & Executive Dashboard 신규 결정 ((a)~(g) 7 sub-decisions 모두 결정 wire 진입). Phase 15 close-out retro `102f370` (cj-style 124번째) + Phase 15 atomic wire T1~T8 `1b800d9` (cj-style 123번째) + Phase 15 spec entry `69c29df` (cj-style 122번째) + Phase 15 PRD entry `87393b4` (cj-style 121번째) 결정 verbatim preserve + EXTENSION (Phase 15 territory DONE 정합) + Phase 14 close-out retro `5b367d9` (cj-style 120번째) + Phase 14 atomic wire T1~T8 `e904485` (cj-style 119번째) + Phase 14 spec entry `30637f6` (cj-style 118번째) + Phase 14 PRD entry `0e3f8d9` (cj-style 117번째) + Phase 13 close-out retro `850b4f8` (cj-style 116번째) + Phase 13 atomic wire T1~T8 `8b98030` (cj-style 115번째) + Phase 13 spec entry `77ed55f` (cj-style 114번째) + Phase 13 PRD entry `d31dfc8` (cj-style 113번째) + Phase 12 close-out retro `3354e83` (cj-style 112번째) + Phase 12 atomic wire T1~T8 `f3c0e63` (cj-style 111번째) + Phase 12 spec entry `8c5f374` (cj-style 110번째) + Phase 12 PRD entry `344c7eb` (cj-style 109번째) + Phase 11 close-out retro `80df15b` (cj-style 108번째) + Phase 11 atomic wire T1~T8 `e020ad0` (cj-style 107번째) + Phase 11 spec entry `82c93a8` (cj-style 106번째) + Phase 11 PRD entry `16d7698` (cj-style 105번째) 결정 wire 모두 보존. **Phase 16 PRD entry wire scope (master PRD v4.7 atomic edit)**: (1) front matter title v4.6 → v4.7 + changelog v4.7 entry prepend / (2) §F32 신규 (F32.1 cross-FinOps-module rollup aggregator 5 modules cross-join EXTENSION + F32.2 cross-module KPI 8 KPI selector + F32.3 executive report generation engine PDF + CSV + Excel + F32.4 scheduled dispatch KST cron + F32.5 tenant-scoped executive role RBAC owner-only + F32.6 executive dashboard UI 5 sub-components + F32.7 Capability gate FINOPS_REPORTING + F32.8 dry-run + Tests + wire scope T1~T8 결정) / (3) §15 로드맵 Phase 16 row status 백로그 → in-progress (PRD entry DONE 진입 wire) / (4) §부록 A A454+A455+A456+A457+A458 신규 결정 표 / (5) AD-43 FinOps Reporting & Executive Dashboard 신규 결정 (FinOps reporting architecture 7 sub-decisions) / (6) capability matrix v1.41 → v1.42 EXTENSION FINOPS_REPORTING 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅. **Phase 16 진입 flow (cj-style 1~4번째 진입점 결정 보존)**: (1) cj-style Phase 16 1번째 진입점 = Phase 16 PRD entry (cj-style 125번째): ✅ DONE 2026-08-25 (master PRD v4.7 atomic edit) / (2) cj-style Phase 16 2번째 진입점 = Phase 16 bmad-create-story spec (cj-style 126번째): Phase 16 wire spec (T1~T8) 결정 wire 진입 대기 / (3) cj-style Phase 16 3번째 진입점 = Phase 16 bmad-dev-story atomic wire T1~T8 (cj-style 127번째): Phase 16 본체 wire T1~T8 atomic single sprint (T1 executive_dashboard_aggregator + cross_module_kpi + T2 executive_report_generator + 3 format generators + T3 scheduled_executive_dispatch + APScheduler 4 cadence + T4 alembic 0048 phase_16_finops_reporting 5 tables + RLS 5 tables + 2 preview tables + T5 audit action EXTENSION 8 NEW + 10 NEW typed exception classes + T6 capability v1.42 EXTENSION + frontend finops reporting dashboard 5 NEW components + T7 scheduled_dispatch_job wire + T8 3중 게이트 FINAL CLEAN atomic commit) / (4) cj-style Phase 16 4번째 진입점 = Phase 16 close-out retro (cj-style 128번째): A454~A458 close-out retro + A19 cohesion 9 surface EXTENSION PASS 검증 (FinOps reporting surface NEW = F32.1~F32.8 FinOps Reporting & Executive Dashboard territory + Phase 15 tag governance + Phase 14 optimization + Phase 13 forecasting + Phase 12 anomaly detection + Phase 11 showback carry-over chain 정합). A19 cohesion pattern 9 surface EXTENSION PASS 결정 (FinOps reporting surface NEW). estimated ~+56 NEW pytest PASS + ~+7 NEW vitest PASS + 0 NEW ruff + 0 regressions. 결정 wire 일자: 2026-08-25 (KST).

## Phase 17 (in-progress, 2026-08-25 — cj-style Phase 17 1번째 진입점 = Phase 17 PRD entry DONE 진입 wire = cj-style 129번째 epic 연속 정직 회복, Phase 17 wire 진입 대기 = cj-style Phase 17 2번째 진입점 = cj-style 130번째 epic 연속 정직 회복 진입 대기)

- **FinOps Sustainability & Carbon Reporting territory 진입 (옵션 (a) Phase 17+ 진입 + 옵션 (a) FinOps Sustainability & Carbon Reporting (Recommended), A474 결정 wire 진입)** — Phase 17 PRD entry DONE 2026-08-25 (cj-style Phase 17 1번째 진입점 = cj-style 129번째 epic 연속 정직 회복, master PRD v4.7 → v4.8 atomic edit). Phase 17 territory = (1) **carbon emissions aggregator** (`apps/api/modules/finops/carbon_emissions_aggregator.py` NEW ~+200 LOC, `aggregate_carbon_emissions(tenant_id, period_key, scope) -> CarbonEmissionsRollup` TypedDict 17 fields + 6-module cross-join EXTENSION (Phase 11 showback + Phase 12 anomaly + Phase 13 forecast + Phase 14 optimization + Phase 15 tag_governance + Phase 16 executive_reporting) + 4 scope 옵션 (tenant + department + cost_center + product_line) + GHG Protocol scope1/2/3 breakdown + audit-first INSERT `sustainability_dashboard_viewed` CR 1-1 verbatim) / (2) **sustainability KPI selector** (`apps/api/modules/finops/sustainability_kpi_selector.py` NEW ~+180 LOC + 8 NEW sustainability KPI TypedDict 결정 wire = total_carbon_emissions_kgco2e + scope1_emissions_kgco2e + scope2_emissions_kgco2e + scope3_emissions_kgco2e + carbon_intensity_kgco2e_per_krw + data_center_pue + renewable_energy_pct + carbon_offset_kgco2e + 6 mandatory + 2 optional + 4 industries baseline EXTENSION + per-tenant override EXTENSION + audit-first INSERT `sustainability_kpi_refreshed` CR 1-1 verbatim) / (3) **sustainability report generation engine** (`apps/api/modules/finops/sustainability_report_generator.py` NEW ~+220 LOC + 3 NEW format generator (PDF + CSV + Excel) 결정 wire + Phase 9 wire ReportGenerator EXTENSION + openpyxl 3.1.2 AD-14 stack pin EXTENSION + EU CSRD ESRS E1 + IFRS S2 + KSSB compliance summary + 3 format option 결정 wire (monthly + quarterly + annual) + audit-first INSERT `sustainability_report_generated` 2행 결정 wire) / (4) **scheduled sustainability dispatch** (`apps/api/modules/finops/scheduled_sustainability_dispatch.py` NEW ~+180 LOC + `apps/api/jobs/scheduled_sustainability_dispatch_job.py` NEW ~+150 LOC + APScheduler 3.10.4 AD-14 stack pin EXTENSION + KST cron 4 NEW cadence (weekly + monthly + quarterly + annual) 결정 wire + recipient_emails JSONB max 50 + audit-first INSERT `sustainability_report_dispatched` + `sustainability_scheduled_dispatch_evaluated` 2 NEW 결정 wire) / (5) **sustainability role RBAC owner-only** (`apps/api/core/capability.py` MODIFIED + `Capability.FINOPS_SUSTAINABILITY = "finops_sustainability"` 1 NEW enum + `apps/api/dependencies/capability.py` MODIFIED + `require_finops_sustainability` 1 NEW dep 결정 wire + 4 industries grants ✅/✅/✅/✅ industry-agnostic CR 12-1 L4 precedent 미러 + Epic 12 2FA 챌린지 보존 + AD-22 owner-only RBAC EXTENSION + drift detector `tests/integration/test_capability_matrix_v1_43_drift.py` NEW 8 NEW pytest cases) / (6) **sustainability dashboard UI** (`apps/web/app/[locale]/(dashboard)/admin/finops/sustainability/page.tsx` NEW ~+180 LOC + 5 sub-components (SustainabilityDashboardAggregator + SustainabilityKPISelector + SustainabilityReportGeneratorPanel + ScheduledSustainabilityDispatchConfigPanel + SustainabilityTrendMiniChart) 결정 wire + owner-only RBAC AD-22 verbatim + Epic 12 2FA 챌린지 보존 + ko-KR.json `finops_sustainability.*` namespace EXTENSION ~30 keys CR 11-4 D-002 verbatim SSOT 결정 wire) / (7) **Capability matrix v1.43 EXTENSION FINOPS_SUSTAINABILITY** (capability matrix v1.42 → v1.43 EXTENSION 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅ CR 12-1 L4 precedent 미러, FINOPS_REPORTING Phase 16 wire + FINOPS_TAG_GOVERNANCE Phase 15 wire + FINOPS_OPTIMIZATION Phase 14 wire + FINOPS_FORECASTING_CAPACITY_PLANNING Phase 13 wire + FINOPS_ANOMALY_DETECTION + FINOPS_BUDGET_ALERT Phase 12 wire + FINOPS_SHOWBACK + FINOPS_CHARGEBACK Phase 11 wire + SLO_ENGINEERING Phase 10 wire + CHAOS_ENGINEERING Phase 9 wire + PERFORMANCE_TESTING Phase 8 wire + OBSERVABILITY_TRACES + OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind + `apps/api/core/capability.py` MODIFIED + Capability.FINOPS_SUSTAINABILITY = "finops_sustainability" 1 NEW enum + `apps/api/dependencies/capability.py` MODIFIED require_finops_sustainability 1 NEW dep + drift detector `tests/integration/test_capability_matrix_v1_43_drift.py` NEW 8 NEW pytest cases) / (8) **dry-run + tests + wire scope T1~T8** (~+56 NEW pytest PASS (carbon_emissions_aggregator 8 + sustainability_kpi_selector 8 + sustainability_report_generator 7 + scheduled_sustainability_dispatch 6 + alembic 0049 5 + audit action 8 + capability matrix v1.43 8 + dry-run 6 = ~+56 NEW pytest PASS) + ~+7 NEW vitest PASS (SustainabilityDashboardAggregator + SustainabilityKPISelector owner-only ack prompt AD-22 verbatim + SustainabilityReportGeneratorPanel RTL render + ScheduledSustainabilityDispatchConfigPanel + SustainabilityTrendMiniChart + ko-KR SSOT 2 + finops sustainability dashboard parity CR 12-5 D-PARITY-01 결정 wire) + 0 NEW ruff + 0 regressions + T1 carbon_emissions_aggregator + sustainability_kpi_selector + T2 sustainability_report_generator + 3 format generators (PDF + CSV + Excel) + T3 scheduled_sustainability_dispatch + APScheduler 4 cadence + T4 alembic 0049 phase_17_finops_sustainability 6 tables + RLS 6 tables + 2 preview tables + T5 audit action EXTENSION 8 NEW + 10 NEW typed exception classes + T6 capability v1.43 EXTENSION + frontend finops sustainability dashboard 5 NEW components + T7 scheduled_sustainability_dispatch_job wire + T8 3중 게이트 FINAL CLEAN atomic commit) + AD-44 FinOps Sustainability & Carbon Reporting 신규 결정 ((a)~(g) 7 sub-decisions 모두 결정 wire 진입). Phase 16 close-out retro `26fd530` (cj-style 128번째) + Phase 16 atomic wire T1~T8 `81ae00a` (cj-style 127번째) + Phase 16 spec entry `69c29df` (cj-style 126번째) + Phase 16 PRD entry `4f11d03` (cj-style 125번째) 결정 verbatim preserve + EXTENSION (Phase 16 territory DONE 정합) + Phase 15 close-out retro `102f370` (cj-style 124번째) + Phase 15 atomic wire T1~T8 `1b800d9` (cj-style 123번째) + Phase 15 spec entry `69c29df` (cj-style 122번째) + Phase 15 PRD entry `87393b4` (cj-style 121번째) + Phase 14 close-out retro `5b367d9` (cj-style 120번째) + Phase 14 atomic wire T1~T8 `e904485` (cj-style 119번째) + Phase 14 spec entry `30637f6` (cj-style 118번째) + Phase 14 PRD entry `0e3f8d9` (cj-style 117번째) + Phase 13 close-out retro `850b4f8` (cj-style 116번째) + Phase 13 atomic wire T1~T8 `8b98030` (cj-style 115번째) + Phase 13 spec entry `77ed55f` (cj-style 114번째) + Phase 13 PRD entry `d31dfc8` (cj-style 113번째) + Phase 12 close-out retro `3354e83` (cj-style 112번째) + Phase 12 atomic wire T1~T8 `f3c0e63` (cj-style 111번째) 결정 wire 모두 보존. **Phase 17 PRD entry wire scope (master PRD v4.8 atomic edit)**: (1) front matter title v4.7 → v4.8 + changelog v4.8 entry prepend / (2) §F33 신규 (F33.1 carbon_emissions_aggregator 6-module cross-join EXTENSION + F33.2 sustainability_kpi_selector 8 NEW KPI selector + F33.3 sustainability_report_generation_engine PDF + CSV + Excel + F33.4 scheduled_sustainability_dispatch KST cron + F33.5 tenant_scoped_sustainability_role RBAC owner-only + F33.6 sustainability_dashboard UI 5 sub-components + F33.7 Capability gate FINOPS_SUSTAINABILITY + F33.8 dry-run + Tests + wire scope T1~T8 결정) / (3) §15 로드맵 Phase 17 row status 백로그 → in-progress (PRD entry DONE 진입 wire) / (4) §부록 A A474+A475+A476+A477+A478 신규 결정 표 / (5) AD-44 FinOps Sustainability & Carbon Reporting 신규 결정 (FinOps sustainability architecture 7 sub-decisions) / (6) capability matrix v1.42 → v1.43 EXTENSION FINOPS_SUSTAINABILITY 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅. **Phase 17 진입 flow (cj-style 1~4번째 진입점 결정 보존)**: (1) cj-style Phase 17 1번째 진입점 = Phase 17 PRD entry (cj-style 129번째): ✅ DONE 2026-08-25 (master PRD v4.8 atomic edit) / (2) cj-style Phase 17 2번째 진입점 = Phase 17 bmad-create-story spec (cj-style 130번째): Phase 17 wire spec (T1~T8) 결정 wire 진입 대기 / (3) cj-style Phase 17 3번째 진입점 = Phase 17 bmad-dev-story atomic wire T1~T8 (cj-style 131번째): Phase 17 본체 wire T1~T8 atomic single sprint (T1 carbon_emissions_aggregator + sustainability_kpi_selector + T2 sustainability_report_generator + 3 format generators + T3 scheduled_sustainability_dispatch + APScheduler 4 cadence + T4 alembic 0049 phase_17_finops_sustainability 6 tables + RLS 6 tables + 2 preview tables + T5 audit action EXTENSION 8 NEW + 10 NEW typed exception classes + T6 capability v1.43 EXTENSION + frontend finops sustainability dashboard 5 NEW components + T7 scheduled_sustainability_dispatch_job wire + T8 3중 게이트 FINAL CLEAN atomic commit) / (4) cj-style Phase 17 4번째 진입점 = Phase 17 close-out retro (cj-style 132번째): A474~A478 close-out retro + A19 cohesion 9 surface EXTENSION PASS 검증 (FinOps sustainability surface NEW = F33.1~F33.8 FinOps Sustainability & Carbon Reporting territory + Phase 16 reporting + Phase 15 tag governance + Phase 14 optimization + Phase 13 forecasting + Phase 12 anomaly detection + Phase 11 showback carry-over chain 정합). A19 cohesion pattern 9 surface EXTENSION PASS 결정 (FinOps sustainability surface NEW). estimated ~+56 NEW pytest PASS + ~+7 NEW vitest PASS + 0 NEW ruff + 0 regressions. 결정 wire 일자: 2026-08-25 (KST). D-FINOPS-7 신규 honestly DEFER 보존 = Phase 17 territory 의 backend extension 진입 시점에 deferred 결정 wire (예: KSSB 2026 신설 표준 detail, ESRS Sector-specific 표준 detail, IFRS S2 Industry-based detail, asset-level carbon footprint detail, scope4 emissions future 정의 detail — 모두 단일 sprint `wire` 진입이 아닌 docs-only entry 에서 honestly defer 결정 wire 보존).

## Phase 18 (in-progress, 2026-08-25 — cj-style Phase 18 1번째 진입점 = Phase 18 PRD entry DONE 진입 wire = cj-style 133번째 epic 연속 정직 회복, Phase 18 wire 진입 대기 = cj-style Phase 18 2번째 진입점 = cj-style 134번째 epic 연속 정직 회복 진입 대기)

- **FinOps Cloud Commitment Management territory 진입 (옵션 (a) Phase 18+ 진입 + 옵션 (a) FinOps Cloud Commitment Management (RIs/SPs/CUDs) (Recommended), A494 결정 wire 진입)** — Phase 18 PRD entry DONE 2026-08-25 (cj-style Phase 18 1번째 진입점 = cj-style 133번째 epic 연속 정직 회복, master PRD v4.8 → v4.9 atomic edit). Phase 18 territory = (1) **commitment inventory aggregator** (`apps/api/modules/finops/commitment_inventory_aggregator.py` NEW ~+200 LOC, `aggregate_commitment_inventory(tenant_id, period_key, cloud_provider) -> CommitmentInventory` TypedDict 18 fields + 5 cloud provider cross-rollup EXTENSION (AWS EC2 RI + RDS RI + EC2 SP + S3 SP + Redshift SP + DynamoDB SP + Azure reservations + GCP CUDs + Naver Cloud commitments + KT Cloud commitments) + commitment_type enum (ec2_ri + rds_ri + ec2_sp + s3_sp + redshift_sp + dynamodb_sp + azure_reservation + gcp_cud + naver_commitment + kt_commitment) + audit-first INSERT `commitment_dashboard_viewed` CR 1-1 verbatim) / (2) **commitment KPI selector** (`apps/api/modules/finops/commitment_kpi_selector.py` NEW ~+220 LOC + 8 NEW commitment KPI TypedDict 결정 wire = total_commitment_value_krw + coverage_pct + utilization_pct + expiring_commitments_30d + recommended_purchase_krw + savings_realized_krw + idle_commitment_krw + renewal_decision_score + 6 mandatory + 2 optional + 4 industries baseline EXTENSION + per-tenant override EXTENSION + audit-first INSERT `commitment_kpi_refreshed` CR 1-1 verbatim) / (3) **commitment report generation engine** (`apps/api/modules/finops/commitment_report_generator.py` NEW ~+250 LOC + 3 NEW format generator (PDF + CSV + Excel) 결정 wire + Phase 9 wire ReportGenerator EXTENSION + openpyxl 3.1.2 AD-14 stack pin EXTENSION + 3 cadence option 결정 wire (weekly + monthly + quarterly + annual) + 5-framework support (FinOps Foundation + AWS Cost Optimization Pillar + Azure Cost Optimization + GCP Cost Optimization + 한국 조달청 클라우드 commitment 가이드라인) + audit-first INSERT `commitment_report_generated` 2행 결정 wire) / (4) **scheduled commitment dispatch** (`apps/api/modules/finops/scheduled_commitment_dispatch.py` NEW ~+200 LOC + `apps/api/jobs/scheduled_commitment_dispatch_job.py` NEW ~+150 LOC + APScheduler 3.10.4 AD-14 stack pin EXTENSION + KST cron 4 NEW cadence (weekly + monthly + quarterly + annual) 결정 wire + recipients JSONB max 50 (Slack + Email + S3 archive + MS Teams webhook) + audit-first INSERT `commitment_report_dispatched` + `commitment_scheduled_dispatch_evaluated` 2 NEW 결정 wire) / (5) **commitment role RBAC owner-only** (`apps/api/core/role.py` MODIFIED + `Role.COMMITMENT_VIEWER` 1 NEW enum + `apps/api/core/capability.py` MODIFIED + `Capability.FINOPS_COMMITMENT = "finops_commitment"` 1 NEW enum + `apps/api/dependencies/capability.py` MODIFIED + `require_finops_commitment` 1 NEW dep + `require_commitment_role()` 1 NEW dep 결정 wire + 4 industries grants ✅/✅/✅/✅ industry-agnostic CR 12-1 L4 precedent 미러 + Epic 12 2FA 챌린지 보존 + AD-22 owner-only RBAC EXTENSION + drift detector `tests/integration/test_capability_matrix_v1_44_drift.py` NEW 8 NEW pytest cases) / (6) **commitment dashboard UI** (`apps/web/app/[locale]/(dashboard)/admin/finops/commitment/page.tsx` NEW ~+180 LOC + 5 sub-components (CommitmentInventoryAggregator + CommitmentKPISelector + CommitmentReportGeneratorPanel + ScheduledCommitmentDispatchConfigPanel + CommitmentTrendMiniChart) 결정 wire + `apps/web/components/finops/FinopsCommitmentDashboardPanel.tsx` NEW Client component ~+200 LOC + 5-tab layout (Inventory / KPI / Reports / Dispatch / Trends) + owner-only RBAC AD-22 verbatim + Epic 12 2FA 챌린지 보존 + ko-KR.json `finops_commitment.*` namespace EXTENSION ~30 keys CR 11-4 D-002 verbatim SSOT 결정 wire + `apps/web/lib/finops/commitment-types.ts` NEW TypeScript mirror 결정 wire (CR 12-5 D-PARITY-01 inversion)) / (7) **Capability matrix v1.44 EXTENSION FINOPS_COMMITMENT** (capability matrix v1.43 → v1.44 EXTENSION 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅ CR 12-1 L4 precedent 미러, FINOPS_SUSTAINABILITY Phase 17 wire + FINOPS_REPORTING Phase 16 wire + FINOPS_TAG_GOVERNANCE Phase 15 wire + FINOPS_OPTIMIZATION Phase 14 wire + FINOPS_FORECASTING_CAPACITY_PLANNING Phase 13 wire + FINOPS_ANOMALY_DETECTION + FINOPS_BUDGET_ALERT Phase 12 wire + FINOPS_SHOWBACK + FINOPS_CHARGEBACK Phase 11 wire + SLO_ENGINEERING Phase 10 wire + CHAOS_ENGINEERING Phase 9 wire + PERFORMANCE_TESTING Phase 8 wire + OBSERVABILITY_TRACES + OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind + `apps/api/core/capability.py` MODIFIED + Capability.FINOPS_COMMITMENT = "finops_commitment" 1 NEW enum + `apps/api/dependencies/capability.py` MODIFIED require_finops_commitment 1 NEW dep + drift detector `tests/integration/test_capability_matrix_v1_44_drift.py` NEW 8 NEW pytest cases) / (8) **dry-run + tests + wire scope T1~T8** (~+62 NEW pytest PASS (commitment_inventory_aggregator 8 + commitment_kpi_selector 8 + commitment_report_generator 8 + scheduled_commitment_dispatch 6 + alembic 0050 6 + audit action 8 + capability matrix v1.44 8 + dry-run 6 + 5 cloud provider integration 6 = ~+62 NEW pytest PASS) + ~+7 NEW vitest PASS (CommitmentInventoryAggregator + CommitmentKPISelector owner-only ack prompt AD-22 verbatim + CommitmentReportGeneratorPanel RTL render + ScheduledCommitmentDispatchConfigPanel + CommitmentTrendMiniChart + ko-KR SSOT 2 + finops commitment dashboard parity CR 12-5 D-PARITY-01 결정 wire) + 0 NEW ruff + 0 regressions + T1 commitment_inventory_aggregator + commitment_kpi_selector + T2 commitment_report_generator + 3 format generators + T3 scheduled_commitment_dispatch + APScheduler 4 cadence + T4 alembic 0050 phase_18_finops_commitment 6 tables + RLS 6 tables + 2 preview tables + T5 audit action EXTENSION 8 NEW + 16 NEW typed exception classes + T6 capability v1.44 EXTENSION + frontend finops commitment dashboard 5 NEW components + T7 scheduled_commitment_dispatch_job wire + T8 3중 게이트 FINAL CLEAN atomic commit) + AD-45 FinOps Cloud Commitment Management 신규 결정 ((a)~(g) 7 sub-decisions 모두 결정 wire 진입). Phase 17 close-out retro `de009fe` (cj-style 132번째) + Phase 17 atomic wire T1~T8 `97cfe4e` (cj-style 131번째) + Phase 17 spec entry `4be3120` (cj-style 130번째) + Phase 17 PRD entry `e0778ed` (cj-style 129번째) 결정 verbatim preserve + EXTENSION (Phase 17 territory DONE 정합) + Phase 16 close-out retro `26fd530` (cj-style 128번째) + Phase 16 atomic wire T1~T8 `81ae00a` (cj-style 127번째) + Phase 16 spec entry `69c29df` (cj-style 126번째) + Phase 16 PRD entry `4f11d03` (cj-style 125번째) + Phase 15 close-out retro `102f370` (cj-style 124번째) + Phase 15 atomic wire T1~T8 `1b800d9` (cj-style 123번째) + Phase 15 spec entry `69c29df` (cj-style 122번째) + Phase 15 PRD entry `87393b4` (cj-style 121번째) + Phase 14 close-out retro `5b367d9` (cj-style 120번째) + Phase 14 atomic wire T1~T8 `e904485` (cj-style 119번째) + Phase 14 spec entry `30637f6` (cj-style 118번째) + Phase 14 PRD entry `0e3f8d9` (cj-style 117번째) + Phase 13 close-out retro `850b4f8` (cj-style 116번째) + Phase 13 atomic wire T1~T8 `8b98030` (cj-style 115번째) + Phase 13 spec entry `77ed55f` (cj-style 114번째) + Phase 13 PRD entry `d31dfc8` (cj-style 113번째) + Phase 12 close-out retro `3354e83` (cj-style 112번째) + Phase 12 atomic wire T1~T8 `f3c0e63` (cj-style 111번째) 결정 wire 모두 보존. **Phase 18 PRD entry wire scope (master PRD v4.9 atomic edit)**: (1) front matter title v4.8 → v4.9 + changelog v4.9 entry prepend / (2) §F34 신규 (F34.1 commitment_inventory_aggregator 5 cloud provider cross-rollup EXTENSION + F34.2 commitment_kpi_selector 8 NEW KPI selector + F34.3 commitment_report_generation_engine PDF + CSV + Excel + F34.4 scheduled_commitment_dispatch KST cron + F34.5 tenant_scoped_commitment_role RBAC owner-only + F34.6 commitment dashboard UI 5 sub-components + F34.7 Capability gate FINOPS_COMMITMENT + F34.8 dry-run + Tests + wire scope T1~T8 결정) / (3) §15 로드맵 Phase 18 row status 백로그 → in-progress (PRD entry DONE 진입 wire) / (4) §부록 A A494+A495+A496+A497+A498 신규 결정 표 / (5) AD-45 FinOps Cloud Commitment Management 신규 결정 (FinOps commitment architecture 7 sub-decisions) / (6) capability matrix v1.43 → v1.44 EXTENSION FINOPS_COMMITMENT 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅. **Phase 18 진입 flow (cj-style 1~4번째 진입점 결정 보존)**: (1) cj-style Phase 18 1번째 진입점 = Phase 18 PRD entry (cj-style 133번째): ✅ DONE 2026-08-25 (master PRD v4.9 atomic edit) / (2) cj-style Phase 18 2번째 진입점 = Phase 18 bmad-create-story spec (cj-style 134번째): Phase 18 wire spec (T1~T8) 결정 wire 진입 대기 / (3) cj-style Phase 18 3번째 진입점 = Phase 18 bmad-dev-story atomic wire T1~T8 (cj-style 135번째): Phase 18 본체 wire T1~T8 atomic single sprint (T1 commitment_inventory_aggregator + commitment_kpi_selector + T2 commitment_report_generator + 3 format generators + T3 scheduled_commitment_dispatch + APScheduler 4 cadence + T4 alembic 0050 phase_18_finops_commitment 6 tables + RLS 6 tables + 2 preview tables + T5 audit action EXTENSION 8 NEW + 16 NEW typed exception classes + T6 capability v1.44 EXTENSION + frontend finops commitment dashboard 5 NEW components + T7 scheduled_commitment_dispatch_job wire + T8 3중 게이트 FINAL CLEAN atomic commit) / (4) cj-style Phase 18 4번째 진입점 = Phase 18 close-out retro (cj-style 136번째): A494~A498 close-out retro + A19 cohesion 9 surface EXTENSION PASS 검증 (FinOps commitment surface NEW = F34.1~F34.8 FinOps Cloud Commitment Management territory + Phase 17 sustainability + Phase 16 reporting + Phase 15 tag governance + Phase 14 optimization + Phase 13 forecasting + Phase 12 anomaly detection + Phase 11 showback carry-over chain 정합). A19 cohesion pattern 9 surface EXTENSION PASS 결정 (FinOps commitment surface NEW). estimated ~+62 NEW pytest PASS + ~+7 NEW vitest PASS + 0 NEW ruff + 0 regressions. 결정 wire 일자: 2026-08-25 (KST). D-FINOPS-8 신규 honestly DEFER 보존 = Phase 18 territory 의 backend extension 진입 시점에 deferred 결정 wire (예: AWS RI marketplace cross-account 2nd-hand RI 거래 detail, GCP CUD flexibile/fixed tier 최적화 detail, Naver Cloud / KT Cloud commitment API stability 검증 detail, commitment auto-renewal webhook integration detail, 5 cloud provider unified cost reconciliation detail — 모두 단일 sprint `wire` 진입이 아닌 docs-only entry 에서 honestly defer 결정 wire 보존).

## Phase 19 (in-progress, 2026-08-25 — cj-style Phase 19 1번째 진입점 = Phase 19 PRD entry DONE 진입 wire = cj-style 137번째 epic 연속 정직 회복, Phase 19 wire 진입 대기 = cj-style Phase 19 2번째 진입점 = cj-style 138번째 epic 연속 정직 회복 진입 대기)

- **FinOps Pricing, Rate Card & TCO Modeling territory 진입 (옵션 (a) Phase 19+ 진입 + 옵션 (a) FinOps Pricing, Rate Card & TCO Modeling (Recommended), A514 결정 wire 진입)** — Phase 19 PRD entry DONE 2026-08-25 (cj-style Phase 19 1번째 진입점 = cj-style 137번째 epic 연속 정직 회복, master PRD v4.9 → v5.0 atomic edit). Phase 19 territory = (1) **rate card aggregator** (`apps/api/modules/finops/pricing/rate_card_aggregator.py` NEW ~+200 LOC, `aggregate_rate_cards(tenant_id, period_key, cloud_provider) -> RateCardInventory` TypedDict 18 fields + 5 cloud provider cross-rollup EXTENSION (AWS EDP + Azure EA + GCP CUD Pricing + Naver Cloud Volume Tier + KT Cloud Volume Tier) + rate_card_type enum (edp + ea + cud_pricing + naver_volume + kt_volume + marketplace) + audit-first INSERT `pricing_dashboard_viewed` CR 1-1 verbatim) / (2) **TCO modeling selector** (`apps/api/modules/finops/pricing/tco_modeling_selector.py` NEW ~+220 LOC + 8 NEW TCO KPI TypedDict 결정 wire = total_blended_rate_krw_per_hour + effective_discount_pct + tco_1year_commitment_krw + tco_3year_commitment_krw + tco_on_demand_krw + cost_per_user_krw + cost_per_transaction_krw + unit_economics_score + 6 mandatory + 2 optional + 4 industries baseline EXTENSION + per-tenant override EXTENSION + audit-first INSERT `pricing_kpi_refreshed` CR 1-1 verbatim) / (3) **pricing report generation engine** (`apps/api/modules/finops/pricing/pricing_report_generator.py` NEW ~+250 LOC + 3 NEW format generator (PDF + CSV + Excel) 결정 wire + Phase 9 wire ReportGenerator EXTENSION + openpyxl 3.1.2 AD-14 stack pin EXTENSION + 3 cadence option 결정 wire (monthly + quarterly + annual) + 5-framework support (FinOps Foundation Pricing + AWS Pricing Models + Azure Pricing Calculator + GCP Pricing Calculator + 한국 공공 조달 가격 가이드라인) + audit-first INSERT `pricing_report_generated` 2행 결정 wire) / (4) **scheduled pricing dispatch** (`apps/api/modules/finops/pricing/scheduled_pricing_dispatch.py` NEW ~+200 LOC + `apps/api/jobs/scheduled_pricing_dispatch_job.py` NEW ~+150 LOC + APScheduler 3.10.4 AD-14 stack pin EXTENSION + KST cron 4 NEW cadence (weekly + monthly + quarterly + annual) 결정 wire + recipients JSONB max 50 (Slack + Email + S3 archive + MS Teams webhook) + audit-first INSERT `pricing_report_dispatched` + `pricing_scheduled_dispatch_evaluated` 2 NEW 결정 wire) / (5) **pricing role RBAC owner-only** (`apps/api/core/role.py` MODIFIED + `Role.PRICING_VIEWER` 1 NEW enum + `apps/api/core/capability.py` MODIFIED + `Capability.FINOPS_PRICING = "finops_pricing"` 1 NEW enum + `apps/api/dependencies/capability.py` MODIFIED + `require_finops_pricing` 1 NEW dep + `require_pricing_role()` 1 NEW dep 결정 wire + 4 industries grants ✅/✅/✅/✅ industry-agnostic CR 12-1 L4 precedent 미러 + Epic 12 2FA 챌린지 보존 + AD-22 owner-only RBAC EXTENSION + drift detector `tests/integration/test_capability_matrix_v1_45_drift.py` NEW 8 NEW pytest cases) / (6) **pricing dashboard UI** (`apps/web/app/[locale]/(dashboard)/admin/finops/pricing/page.tsx` NEW ~+180 LOC + 5 sub-components (RateCardAggregatorPanel + TCOModelingSelectorPanel + PricingReportGeneratorPanel + ScheduledPricingDispatchConfigPanel + PricingTrendMiniChart) 결정 wire + `apps/web/components/finops/FinopsPricingDashboardPanel.tsx` NEW Client component ~+200 LOC + 5-tab layout (Rate Cards / TCO / Reports / Dispatch / Trends) + owner-only RBAC AD-22 verbatim + Epic 12 2FA 챌린지 보존 + ko-KR.json `finops_pricing.*` namespace EXTENSION ~30 keys CR 11-4 D-002 verbatim SSOT 결정 wire + `apps/web/lib/finops/pricing-types.ts` NEW TypeScript mirror 결정 wire (CR 12-5 D-PARITY-01 inversion)) / (7) **Capability matrix v1.45 EXTENSION FINOPS_PRICING** (capability matrix v1.44 → v1.45 EXTENSION 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅ CR 12-1 L4 precedent 미러, FINOPS_COMMITMENT Phase 18 wire + FINOPS_SUSTAINABILITY Phase 17 wire + FINOPS_REPORTING Phase 16 wire + FINOPS_TAG_GOVERNANCE Phase 15 wire + FINOPS_OPTIMIZATION Phase 14 wire + FINOPS_FORECASTING_CAPACITY_PLANNING Phase 13 wire + FINOPS_ANOMALY_DETECTION + FINOPS_BUDGET_ALERT Phase 12 wire + FINOPS_SHOWBACK + FINOPS_CHARGEBACK Phase 11 wire + SLO_ENGINEERING Phase 10 wire + CHAOS_ENGINEERING Phase 9 wire + PERFORMANCE_TESTING Phase 8 wire + OBSERVABILITY_TRACES + OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind + `apps/api/core/capability.py` MODIFIED + Capability.FINOPS_PRICING = "finops_pricing" 1 NEW enum + `apps/api/dependencies/capability.py` MODIFIED require_finops_pricing 1 NEW dep + drift detector `tests/integration/test_capability_matrix_v1_45_drift.py` NEW 8 NEW pytest cases) / (8) **dry-run + tests + wire scope T1~T8** (~+62 NEW pytest PASS (rate_card_aggregator 8 + tco_modeling_selector 8 + pricing_report_generator 8 + scheduled_pricing_dispatch 6 + alembic 0051 6 + audit action 8 + capability matrix v1.45 8 + dry-run 6 + 5 cloud provider integration 6 = ~+62 NEW pytest PASS) + ~+7 NEW vitest PASS (RateCardAggregatorPanel + TCOModelingSelectorPanel owner-only ack prompt AD-22 verbatim + PricingReportGeneratorPanel RTL render + ScheduledPricingDispatchConfigPanel + PricingTrendMiniChart + ko-KR SSOT 2 + finops pricing dashboard parity CR 12-5 D-PARITY-01 결정 wire) + 0 NEW ruff + 0 regressions + T1 rate_card_aggregator + tco_modeling_selector + T2 pricing_report_generator + 3 format generators + T3 scheduled_pricing_dispatch + APScheduler 4 cadence + T4 alembic 0051 phase_19_finops_pricing 6 tables + RLS 6 tables + 2 preview tables + T5 audit action EXTENSION 8 NEW + 16 NEW typed exception classes + T6 capability v1.45 EXTENSION + frontend finops pricing dashboard 5 NEW components + T7 scheduled_pricing_dispatch_job wire + T8 3중 게이트 FINAL CLEAN atomic commit) + AD-46 FinOps Pricing, Rate Card & TCO Modeling 신규 결정 ((a)~(g) 7 sub-decisions 모두 결정 wire 진입). Phase 18 close-out retro `de72f50` (cj-style 136번째) + Phase 18 atomic wire T1~T8 `67059cf` (cj-style 135번째) + Phase 18 spec entry `bdc7997` (cj-style 134번째) + Phase 18 PRD entry `5eded22` (cj-style 133번째) 결정 verbatim preserve + EXTENSION (Phase 18 territory DONE 정합) + Phase 17 close-out retro `de009fe` (cj-style 132번째) + Phase 17 atomic wire T1~T8 `97cfe4e` (cj-style 131번째) + Phase 17 spec entry `4be3120` (cj-style 130번째) + Phase 17 PRD entry `e0778ed` (cj-style 129번째) + Phase 16 close-out retro `26fd530` (cj-style 128번째) + Phase 16 atomic wire T1~T8 `81ae00a` (cj-style 127번째) + Phase 16 spec entry `69c29df` (cj-style 126번째) + Phase 16 PRD entry `4f11d03` (cj-style 125번째) + Phase 15 close-out retro `102f370` (cj-style 124번째) + Phase 15 atomic wire T1~T8 `1b800d9` (cj-style 123번째) + Phase 15 spec entry `69c29df` (cj-style 122번째) + Phase 15 PRD entry `87393b4` (cj-style 121번째) + Phase 14 close-out retro `5b367d9` (cj-style 120번째) + Phase 14 atomic wire T1~T8 `e904485` (cj-style 119번째) + Phase 14 spec entry `30637f6` (cj-style 118번째) + Phase 14 PRD entry `0e3f8d9` (cj-style 117번째) + Phase 13 close-out retro `850b4f8` (cj-style 116번째) + Phase 13 atomic wire T1~T8 `8b98030` (cj-style 115번째) + Phase 13 spec entry `77ed55f` (cj-style 114번째) + Phase 13 PRD entry `d31dfc8` (cj-style 113번째) + Phase 12 close-out retro `3354e83` (cj-style 112번째) + Phase 12 atomic wire T1~T8 `f3c0e63` (cj-style 111번째) 결정 wire 모두 보존. **Phase 19 PRD entry wire scope (master PRD v5.0 atomic edit)**: (1) front matter title v4.9 → v5.0 + changelog v5.0 entry prepend / (2) §F35 신규 (F35.1 rate_card_aggregator 5 cloud provider cross-rollup EXTENSION + F35.2 tco_modeling_selector 8 NEW KPI selector + F35.3 pricing_report_generation_engine PDF + CSV + Excel + F35.4 scheduled_pricing_dispatch KST cron + F35.5 tenant_scoped_pricing_role RBAC owner-only + F35.6 pricing dashboard UI 5 sub-components + F35.7 Capability gate FINOPS_PRICING + F35.8 dry-run + Tests + wire scope T1~T8 결정) / (3) §15 로드맵 Phase 19 row status 백로그 → in-progress (PRD entry DONE 진입 wire) / (4) §부록 A A514+A515+A516+A517+A518 신규 결정 표 / (5) AD-46 FinOps Pricing, Rate Card & TCO Modeling 신규 결정 (FinOps pricing architecture 7 sub-decisions) / (6) capability matrix v1.44 → v1.45 EXTENSION FINOPS_PRICING 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅. **Phase 19 진입 flow (cj-style 1~4번째 진입점 결정 보존)**: (1) cj-style Phase 19 1번째 진입점 = Phase 19 PRD entry (cj-style 137번째): ✅ DONE 2026-08-25 (master PRD v5.0 atomic edit) / (2) cj-style Phase 19 2번째 진입점 = Phase 19 bmad-create-story spec (cj-style 138번째): Phase 19 wire spec (T1~T8) 결정 wire 진입 대기 / (3) cj-style Phase 19 3번째 진입점 = Phase 19 bmad-dev-story atomic wire T1~T8 (cj-style 139번째): Phase 19 본체 wire T1~T8 atomic single sprint (T1 rate_card_aggregator + tco_modeling_selector + T2 pricing_report_generator + 3 format generators + T3 scheduled_pricing_dispatch + APScheduler 4 cadence + T4 alembic 0051 phase_19_finops_pricing 6 tables + RLS 6 tables + 2 preview tables + T5 audit action EXTENSION 8 NEW + 16 NEW typed exception classes + T6 capability v1.45 EXTENSION + frontend finops pricing dashboard 5 NEW components + T7 scheduled_pricing_dispatch_job wire + T8 3중 게이트 FINAL CLEAN atomic commit) / (4) cj-style Phase 19 4번째 진입점 = Phase 19 close-out retro (cj-style 140번째): A514~A518 close-out retro + A19 cohesion 9 surface EXTENSION PASS 검증 (FinOps pricing surface NEW = F35.1~F35.8 FinOps Pricing, Rate Card & TCO Modeling territory + Phase 18 commitment + Phase 17 sustainability + Phase 16 reporting + Phase 15 tag governance + Phase 14 optimization + Phase 13 forecasting + Phase 12 anomaly detection + Phase 11 showback carry-over chain 정합). A19 cohesion pattern 9 surface EXTENSION PASS 결정 (FinOps pricing surface NEW). estimated ~+62 NEW pytest PASS + ~+7 NEW vitest PASS + 0 NEW ruff + 0 regressions. 결정 wire 일자: 2026-08-25 (KST). D-FINOPS-9 신규 honestly DEFER 보존 = Phase 19 territory 의 backend extension 진입 시점에 deferred 결정 wire (예: AWS EDP 자동 negotiation bot detail, Azure EA consumption commit reconciliation detail, GCP CUD flexible/fixed tier break-even optimization detail, Naver Cloud / KT Cloud pricing API stability 검증 detail, 5 cloud provider unified cost reconciliation detail, blended vs unblended 실시간 차이 추적 detail, marketplace SaaS pricing 파편화 통합 detail — 모두 단일 sprint `wire` 진입이 아닌 docs-only entry 에서 honestly defer 결정 wire 보존).

## Phase 20 (in-progress, 2026-08-25 — cj-style Phase 20 1번째 진입점 = Phase 20 PRD entry DONE 진입 wire = cj-style 142번째 epic 연속 정직 회복, Phase 20 wire 진입 대기 = cj-style Phase 20 2번째 진입점 = cj-style 143번째 epic 연속 정직 회복 진입 대기)

- **FinOps Multi-Cloud Cost Unified Reconciliation territory 진입 (옵션 (a) Phase 20+ 진입 + 옵션 (a) FinOps Multi-Cloud Cost Unified Reconciliation (Recommended), A539 결정 wire 진입)** — Phase 20 PRD entry DONE 2026-08-25 (cj-style Phase 20 1번째 진입점 = cj-style 142번째 epic 연속 정직 회복, master PRD v5.0 → v6.0 atomic edit). Phase 20 territory = (1) **multi_cloud_rate_card_reconciliation_aggregator** (`apps/api/modules/finops/multi_cloud/rate_card_reconciliation_aggregator.py` NEW ~+220 LOC, `reconcile_multi_cloud_rate_cards(tenant_id, period_key, scope_chain) -> MultiCloudRateCardReconciliation` TypedDict 18 fields + 9-module cross-join EXTENSION (Phase 11 showback + Phase 12 anomaly + Phase 13 forecast + Phase 14 optimization + Phase 15 tag_governance + Phase 16 executive + Phase 17 sustainability + Phase 18 commitment + Phase 19 pricing) + 5 cloud provider rate card cross-rollup EXTENSION (AWS EDP + Azure EA + GCP CUD Pricing + Naver Cloud Volume Tier + KT Cloud Volume Tier) + 5-tier source priority chain (negotiation + contract + rate_card_api + manual + audit) + variance_pct > 5.0% alert + audit-first INSERT `multi_cloud_rate_card_reconciled` CR 1-1 verbatim) / (2) **multi_cloud_cost_reconciliation_aggregator** (`apps/api/modules/finops/multi_cloud/cost_reconciliation_aggregator.py` NEW ~+220 LOC + `reconcile_multi_cloud_costs(tenant_id, period_key, scope_chain) -> MultiCloudCostReconciliation` TypedDict 19 fields + 9-module cross-join EXTENSION + 5 cloud provider cost cross-rollup EXTENSION (AWS Cost Explorer + Azure Cost Management + GCP Billing + Naver Cloud Billing + KT Cloud Billing) + unified source of truth (단일 unified view 모든 dashboard/KPI/report/reconciliation/drill-down 참조) + 5-tier cost source priority chain (billing_api + invoice_pdf + contract_estimated + manual + audit) + cost_variance_pct > 3.0% alert + audit-first INSERT `multi_cloud_cost_reconciled` CR 1-1 verbatim) / (3) **negotiation_bot** (`apps/api/modules/finops/multi_cloud/negotiation_bot.py` NEW ~+250 LOC + `run_negotiation_bot(tenant_id, cloud_provider, scope) -> NegotiationRecommendation` TypedDict 16 fields + 3 cloud provider support (AWS EDP 자동 negotiation + Azure EA consumption commit reconciliation + GCP CUD flexible/fixed tier break-even optimization) + MINIMUM_SAVINGS_PCT=5.0 + MINIMUM_SAVINGS_KRW=1M + MAX_NEGOTIATIONS_PER_MONTH=3 + MAX_AUTO_TRIGGER_PER_DAY=1 + confidence_score (utilization_stability × 0.6 + historical_accuracy × 0.4) + risk_score (savings_pct × 0.4 + commitment_term × 0.3 + commitment_flexibility × 0.3) + auto-negotiate_ready / manual_review_required / low_confidence 3 status + Naver/KT public pricing API stability 검증 P2 (uptime ≥ 99.0% + P95 ≤ 2s + data_freshness ≤ 24h + rate_limited exponential backoff + accuracy ≥ 95% match) + audit-first INSERT `negotiation_bot_triggered` CR 1-1 verbatim + owner approval flow (Slack DM + 2FA + approval_chain) + idempotency_key = (tenant_id, cloud_provider, scope, recommendation_period_key) 결정 wire) / (4) **blended_unblended_tracker** (`apps/api/modules/finops/multi_cloud/blended_unblended_tracker.py` NEW ~+180 LOC + `track_blended_unblended_diff(tenant_id, period_key, cloud_provider) -> BlendedUnblendedDiff` TypedDict 14 fields + 3 cloud provider support (AWS + Azure + GCP) + 1-hour cron refresh + real_time drift 감지 (rate_diff_pct > 5% → alert) + 4-tier rate card format (tier_1 default + tier_2 5% discount + tier_3 10% discount + tier_4 custom) + Naver/KT public pricing API stability 검증 P2 (uptime + P95 + data_freshness + accuracy degradation detection) + audit-first INSERT `blended_unblended_tracked` CR 1-1 verbatim) / (5) **marketplace_saas_pricing_integrator** (`apps/api/modules/finops/multi_cloud/marketplace_saas_pricing_integrator.py` NEW ~+200 LOC + `integrate_marketplace_saas_pricing(tenant_id, period_key, marketplace_source) -> MarketplaceSaaSPricingRollup` TypedDict 16 fields + 5 marketplace source support (AWS Marketplace + Azure Marketplace + GCP Marketplace + Naver Marketplace + KT Marketplace) + 5 marketplace adapter pattern + unified SaaS pricing view (단일 schema + cheapest alternative suggestion across 5 marketplace) + freshness tracking (staleness_threshold_hours=24 + 4-hour cron refresh + on-demand refresh) + audit-first INSERT `marketplace_saas_pricing_integrated` CR 1-1 verbatim) / (6) **multi_cloud dashboard UI** (`apps/web/app/[locale]/(dashboard)/admin/finops/multi-cloud/page.tsx` NEW ~+220 LOC + 5 sub-components (MultiCloudRateCardReconciliationPanel + MultiCloudCostReconciliationPanel + NegotiationBotConfigPanel + BlendedUnblendedTrackerPanel + MarketplaceSaaSPricingPanel) 결정 wire + `apps/web/components/finops/FinopsMultiCloudDashboardPanel.tsx` NEW Client component ~+250 LOC + 5-tab layout (Rate Cards / Costs / Negotiation / Tracking / Marketplace) + Recharts 2.12.7 AD-14 stack pin EXTENSION + owner-only RBAC AD-22 verbatim + Epic 12 2FA 챌린지 mandatory + ko-KR.json `finops_multi_cloud.*` namespace EXTENSION ~30 keys CR 11-4 D-002 verbatim SSOT 결정 wire + `apps/web/lib/finops/multi-cloud-types.ts` NEW TypeScript mirror 결정 wire (CR 12-5 D-PARITY-01 inversion)) / (7) **Capability matrix v1.46 EXTENSION FINOPS_MULTI_CLOUD_UNIFIED_RECONCILIATION** (capability matrix v1.45 → v1.46 EXTENSION 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅ CR 12-1 L4 precedent 미러, FINOPS_PRICING Phase 19 wire + FINOPS_COMMITMENT Phase 18 wire + FINOPS_SUSTAINABILITY Phase 17 wire + FINOPS_REPORTING Phase 16 wire + FINOPS_TAG_GOVERNANCE Phase 15 wire + FINOPS_OPTIMIZATION Phase 14 wire + FINOPS_FORECASTING_CAPACITY_PLANNING Phase 13 wire + FINOPS_ANOMALY_DETECTION + FINOPS_BUDGET_ALERT Phase 12 wire + FINOPS_SHOWBACK + FINOPS_CHARGEBACK Phase 11 wire + SLO_ENGINEERING Phase 10 wire + CHAOS_ENGINEERING Phase 9 wire + PERFORMANCE_TESTING Phase 8 wire + OBSERVABILITY_TRACES + OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind + `apps/api/core/capability.py` MODIFIED + Capability.FINOPS_MULTI_CLOUD_UNIFIED_RECONCILIATION = "finops_multi_cloud_unified_reconciliation" 1 NEW enum + `apps/api/dependencies/capability.py` MODIFIED require_finops_multi_cloud 1 NEW dep + drift detector `tests/integration/test_capability_matrix_v1_46_drift.py` NEW 8 NEW pytest cases) / (8) **dry-run + tests + wire scope T1~T8** (~+92 NEW pytest PASS (rate_card_reconciliation_aggregator 10 + cost_reconciliation_aggregator 10 + negotiation_bot 30 + blended_unblended_tracker 6 + marketplace_saas_pricing_integrator 8 + alembic 0052 6 + audit action 8 + capability matrix v1.46 8 + dry-run 6 + 5 cloud provider integration 8 = ~+92 NEW pytest PASS) + ~+7 NEW vitest PASS (MultiCloudRateCardReconciliationPanel + MultiCloudCostReconciliationPanel owner-only ack prompt AD-22 verbatim + NegotiationBotConfigPanel RTL render + BlendedUnblendedTrackerPanel + MarketplaceSaaSPricingPanel + ko-KR SSOT 2 + finops multi_cloud dashboard parity CR 12-5 D-PARITY-01 결정 wire) + 0 NEW ruff + 0 regressions + T1 rate_card_reconciliation_aggregator + cost_reconciliation_aggregator + T2 negotiation_bot + 3 cloud provider + T3 blended_unblended_tracker + Naver/KT stability 검증 + marketplace_saas_pricing_integrator + T4 alembic 0052 phase_20_multi_cloud_unified_reconciliation 8 tables + RLS 8 tables + 3 preview tables + T5 audit action EXTENSION 8 NEW + 16 NEW typed exception classes + T6 capability v1.46 EXTENSION + frontend finops multi_cloud dashboard 5 NEW components + T7 scheduled_dispatch_job wire + T8 3중 게이트 FINAL CLEAN atomic commit) + AD-47 FinOps Multi-Cloud Cost Unified Reconciliation 신규 결정 ((a)~(g) 7 sub-decisions 모두 결정 wire 진입). Phase 19.5 carry-over 결정 wire `b2fb1d8` (cj-style 141번째) + Phase 19 close-out retro `18ca1ae` (cj-style 140번째) + Phase 19 atomic wire T1~T8 `8db3cfc` (cj-style 139번째) + Phase 19 spec entry `59d15fb` (cj-style 138번째) + Phase 19 PRD entry `ff8a797` (cj-style 137번째) 결정 verbatim preserve + EXTENSION (Phase 19 territory DONE 정합 + Phase 19.5 carry-over 결정 wire DONE 진입 정합) + Phase 18 close-out retro `de72f50` (cj-style 136번째) + Phase 18 atomic wire T1~T8 `67059cf` (cj-style 135번째) + Phase 18 spec entry `bdc7997` (cj-style 134번째) + Phase 18 PRD entry `5eded22` (cj-style 133번째) + Phase 17 close-out retro `de009fe` (cj-style 132번째) + Phase 17 atomic wire T1~T8 `97cfe4e` (cj-style 131번째) + Phase 17 spec entry `4be3120` (cj-style 130번째) + Phase 17 PRD entry `e0778ed` (cj-style 129번째) + Phase 16 close-out retro `26fd530` (cj-style 128번째) + Phase 16 atomic wire T1~T8 `81ae00a` (cj-style 127번째) + Phase 16 spec entry `69c29df` (cj-style 126번째) + Phase 16 PRD entry `4f11d03` (cj-style 125번째) + Phase 15 close-out retro `102f370` (cj-style 124번째) + Phase 15 atomic wire T1~T8 `1b800d9` (cj-style 123번째) + Phase 15 spec entry `69c29df` (cj-style 122번째) + Phase 15 PRD entry `87393b4` (cj-style 121번째) + Phase 14 close-out retro `5b367d9` (cj-style 120번째) + Phase 14 atomic wire T1~T8 `e904485` (cj-style 119번째) + Phase 14 spec entry `30637f6` (cj-style 118번째) + Phase 14 PRD entry `0e3f8d9` (cj-style 117번째) + Phase 13 close-out retro `850b4f8` (cj-style 116번째) + Phase 13 atomic wire T1~T8 `8b98030` (cj-style 115번째) + Phase 13 spec entry `77ed55f` (cj-style 114번째) + Phase 13 PRD entry `d31dfc8` (cj-style 113번째) + Phase 12 close-out retro `3354e83` (cj-style 112번째) + Phase 12 atomic wire T1~T8 `f3c0e63` (cj-style 111번째) 결정 wire 모두 보존. **Phase 20 PRD entry wire scope (master PRD v6.0 atomic edit)**: (1) front matter title v5.0 → v6.0 + changelog v6.0 entry prepend / (2) §F36 신규 (F36.1 multi_cloud_rate_card_reconciliation_aggregator 5 cloud provider cross-rollup EXTENSION + F36.2 multi_cloud_cost_reconciliation_aggregator unified source of truth + F36.3 negotiation_bot AWS EDP + Azure EA + GCP CUD break-even + F36.4 blended_unblended_tracker + Naver/KT public pricing API stability 검증 + F36.5 marketplace_saas_pricing_integrator + F36.6 multi_cloud dashboard UI 5 sub-components + F36.7 Capability matrix v1.46 EXTENSION FINOPS_MULTI_CLOUD_UNIFIED_RECONCILIATION + F36.8 dry-run + Tests + wire scope T1~T8 결정) / (3) §15 로드맵 Phase 20 row status 백로그 → in-progress (PRD entry DONE 진입 wire) / (4) §부록 A A539+A540+A541+A542+A543 신규 결정 표 / (5) AD-47 FinOps Multi-Cloud Cost Unified Reconciliation 신규 결정 (FinOps multi-cloud architecture 7 sub-decisions) / (6) capability matrix v1.45 → v1.46 EXTENSION FINOPS_MULTI_CLOUD_UNIFIED_RECONCILIATION 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅. **Phase 20 진입 flow (cj-style 1~4번째 진입점 결정 보존)**: (1) cj-style Phase 20 1번째 진입점 = Phase 20 PRD entry (cj-style 142번째): ✅ DONE 2026-08-25 (master PRD v6.0 atomic edit) / (2) cj-style Phase 20 2번째 진입점 = Phase 20 bmad-create-story spec (cj-style 143번째): Phase 20 wire spec (T1~T8) 결정 wire 진입 대기 / (3) cj-style Phase 20 3번째 진입점 = Phase 20 bmad-dev-story atomic wire T1~T8 (cj-style 144번째): Phase 20 본체 wire T1~T8 atomic single sprint (T1 rate_card_reconciliation_aggregator + cost_reconciliation_aggregator + T2 negotiation_bot + 3 cloud provider + T3 blended_unblended_tracker + Naver/KT stability 검증 + marketplace_saas_pricing_integrator + T4 alembic 0052 phase_20_multi_cloud_unified_reconciliation 8 tables + RLS 8 tables + 3 preview tables + T5 audit action EXTENSION 8 NEW + 16 NEW typed exception classes + T6 capability v1.46 EXTENSION + frontend finops multi_cloud dashboard 5 NEW components + T7 scheduled_dispatch_job wire + T8 3중 게이트 FINAL CLEAN atomic commit) / (4) cj-style Phase 20 4번째 진입점 = Phase 20 close-out retro (cj-style 145번째): A539~A543 close-out retro + A19 cohesion 9 surface EXTENSION PASS 검증 (FinOps multi_cloud surface NEW = F36.1~F36.8 FinOps Multi-Cloud Cost Unified Reconciliation territory + Phase 19 pricing + Phase 18 commitment + Phase 17 sustainability + Phase 16 reporting + Phase 15 tag governance + Phase 14 optimization + Phase 13 forecasting + Phase 12 anomaly detection + Phase 11 showback carry-over chain 정합). A19 cohesion pattern 9 surface EXTENSION PASS 결정 (FinOps multi_cloud surface NEW). estimated ~+92 NEW pytest PASS + ~+7 NEW vitest PASS + 0 NEW ruff + 0 regressions. 결정 wire 일자: 2026-08-25 (KST). D-FINOPS-9 honestly DEFER 보존 + Phase 19.5 carry-over 결정 wire `b2fb1d8` 의 AD-47 신규 (a)~(g) 7 sub-decisions 모두 결정 wire 진입 = Phase 20 territory 의 backend extension 진입 시점에 deferred 결정 wire (예: 5 cloud provider unified rate card reconciliation backend detail, AWS EDP 자동 negotiation bot backend detail, Azure EA consumption commit reconciliation backend detail, GCP CUD flexible/fixed tier break-even optimization backend detail, Naver/KT public pricing API stability 검증 backend detail, 5 cloud provider unified cost reconciliation backend detail, blended vs unblended 실시간 차이 추적 backend detail, marketplace SaaS pricing 파편화 통합 backend detail — 모두 단일 sprint `wire` 진입이 아닌 docs-only entry 에서 honestly defer 결정 wire 보존).

## Phase 21 (in-progress, 2026-08-26 — cj-style Phase 21 1번째 진입점 = Phase 21 PRD entry DONE 진입 wire = cj-style 149번째 epic 연속 정직 회복, Phase 21 wire 진입 대기 = cj-style Phase 21 2번째 진입점 = cj-style 150번째 epic 연속 정직 회복 진입 대기)

- **FinOps Reserved Capacity Planning territory 진입 (옵션 (a) Phase 21+ 진입 + 옵션 (a) FinOps Reserved Capacity Planning (Recommended), A574 결정 wire 진입)** — Phase 21 PRD entry DONE 2026-08-26 (cj-style Phase 21 1번째 진입점 = cj-style 149번째 epic 연속 정직 회복, master PRD v6.0 → v7.0 atomic edit). Phase 21 territory = (1) **demand_forecast_aggregator** (`apps/api/modules/finops/reserved_capacity/demand_forecast_aggregator.py` NEW ~+220 LOC + `aggregate_demand_forecast(tenant_id, period_key, scope_chain) -> ReservedCapacityDemandForecast` TypedDict 16 fields + 5-module cross-join EXTENSION (Phase 13 forecast + Phase 14 optimization + Phase 18 commitment + Phase 19 pricing + Phase 20 multi_cloud) + confidence interval + seasonal_factor + growth_rate_pct 4 industries baseline + audit-first INSERT `demand_forecast_calculated` CR 1-1 verbatim) / (2) **capacity_planning_aggregator** (`apps/api/modules/finops/reserved_capacity/capacity_planning_aggregator.py` NEW ~+220 LOC + `plan_reserved_capacity` TypedDict 18 fields + 6 reserved_capacity_tier enum + break_even_utilization_pct + capacity_headroom_pct 10~20% + audit-first INSERT `capacity_planning_recommended` CR 1-1 verbatim) / (3) **commitment_recommendation_engine** (`apps/api/modules/finops/reserved_capacity/commitment_recommendation_engine.py` NEW ~+250 LOC + `generate_commitment_recommendation` TypedDict 17 fields + confidence_score + risk_score + execution_strategy 4 enum + owner approval flow + audit-first INSERT `commitment_recommendation_generated` CR 1-1 verbatim) / (4) **reserved_capacity_orchestrator** (`apps/api/modules/finops/reserved_capacity/reserved_capacity_orchestrator.py` NEW ~+250 LOC + `orchestrate_reserved_capacity` TypedDict 19 fields + composition_step_chain 5 step + 4 cadence schedule (daily + weekly + monthly + quarterly KST pytz) + dry-run + audit-first INSERT `reserved_capacity_orchestrator_triggered` CR 1-1 verbatim) / (5) **reserved_capacity dashboard UI** (`apps/web/app/[locale]/(dashboard)/admin/finops/reserved-capacity/page.tsx` NEW ~+220 LOC + 5 sub-components (DemandForecastPanel + CapacityPlanningPanel + CommitmentRecommendationPanel + ReservedCapacityOrchestratorPanel + ReservedCapacityTrendMiniChart) + `apps/web/components/finops/FinopsReservedCapacityDashboardPanel.tsx` NEW Client component ~+250 LOC + 5-tab layout + Recharts 2.12.7 AD-14 stack pin EXTENSION + owner-only RBAC AD-22 verbatim + Epic 12 2FA 챌린지 mandatory + ko-KR.json `finops_reserved_capacity.*` namespace EXTENSION ~30 keys CR 11-4 D-002 verbatim SSOT) / (6) **Capability matrix v1.47 EXTENSION FINOPS_RESERVED_CAPACITY_PLANNING** (capability matrix v1.46 → v1.47 EXTENSION 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅ CR 12-1 L4 precedent 미러 + `apps/api/core/capability.py` MODIFIED Capability.FINOPS_RESERVED_CAPACITY_PLANNING 1 NEW enum + `apps/api/dependencies/capability.py` MODIFIED require_finops_reserved_capacity 1 NEW dep + drift detector `tests/integration/test_capability_matrix_v1_47_drift.py` NEW 8 NEW pytest cases) / (7) **dry-run + tests + wire scope T1~T8** (~+108 NEW pytest PASS + ~+7 NEW vitest PASS + 0 NEW ruff + 0 regressions + T1 4 backend aggregators + T2 dashboard 5 sub-components + T3 alembic 0053 8 tables + RLS 8 tables + 1 preview table + T4 audit action EXTENSION 8 NEW + 16 NEW typed exception classes + T5 capability v1.47 EXTENSION + T6 scheduled_dispatch_job wire + T7 dry-run mode + 1 NEW CLI flag + T8 3중 게이트 FINAL CLEAN atomic commit) + AD-49 FinOps Reserved Capacity Planning 신규 결정 ((a)~(g) 7 sub-decisions 모두 결정 wire 진입). Phase 20.5 close-out retro `8505d98` (cj-style 148번째) + Phase 20.5 atomic wire T1~T3 `46ddcc5` (cj-style 147번째) + Phase 20.5 spec entry `e23141d` (cj-style 146번째) + Phase 20 close-out retro `f361016` (cj-style 145번째) + Phase 20 atomic wire T1~T8 `52dad7f` (cj-style 144번째) + Phase 20 spec entry `bdc7997` (cj-style 143번째) + Phase 20 PRD entry `ff8a797` (cj-style 142번째) 결정 verbatim preserve + EXTENSION + Phase 19 close-out retro `18ca1ae` (140번째) + Phase 19 atomic wire `8db3cfc` (139번째) + Phase 19 PRD entry `ff8a797` (137번째) + Phase 19.5 carry-over 결정 wire `b2fb1d8` (141번째) + Phase 18 close-out retro `de72f50` (136번째) + Phase 18 atomic wire `67059cf` (135번째) + Phase 17 close-out retro `de009fe` (132번째) + Phase 16 close-out retro `26fd530` (128번째) + Phase 15 close-out retro `102f370` (124번째) + Phase 14 close-out retro `5b367d9` (120번째) + Phase 13 close-out retro `850b4f8` (116번째) + Phase 12 close-out retro `3354e83` (112번째) 결정 wire 모두 보존. **Phase 21 PRD entry wire scope (master PRD v7.0 atomic edit)**: (1) front matter title v6.0 → v7.0 + changelog v7.0 entry prepend / (2) §F37 신규 (F37.1 demand_forecast_aggregator + F37.2 capacity_planning_aggregator + F37.3 commitment_recommendation_engine + F37.4 reserved_capacity_orchestrator + F37.5 reserved_capacity dashboard UI + F37.6 Capability matrix v1.47 EXTENSION + F37.7 audit action EXTENSION 8 NEW + 16 NEW typed exception classes + F37.8 dry-run + Tests + wire scope T1~T8) / (3) §15 로드맵 Phase 21 row status 백로그 → in-progress / (4) §부록 A A574+A575+A576+A577+A578 신규 결정 표 / (5) AD-49 신규 결정 / (6) capability matrix v1.46 → v1.47 EXTENSION. **Phase 21 진입 flow (cj-style 1~4번째 진입점 결정 보존)**: (1) cj-style Phase 21 1번째 진입점 = Phase 21 PRD entry (cj-style 149번째): ✅ DONE 2026-08-26 / (2) cj-style Phase 21 2번째 진입점 = Phase 21 spec (cj-style 150번째): 진입 대기 / (3) cj-style Phase 21 3번째 진입점 = Phase 21 atomic wire T1~T8 (cj-style 151번째): 진입 대기 / (4) cj-style Phase 21 4번째 진입점 = Phase 21 close-out retro (cj-style 152번째): 진입 대기. A19 cohesion pattern 9 surface EXTENSION PASS 결정 (FinOps reserved_capacity surface NEW). estimated ~+108 NEW pytest PASS + ~+7 NEW vitest PASS + 0 NEW ruff + 0 regressions. 결정 wire 일자: 2026-08-26 (KST). D-FINOPS-10 신규 honestly DEFER 보존.

## 이론 근거 (2026-07 웹 검증 요지)
- **AI-driven ABC**: AI 결합 ABC가 원가 배부 오류를 크게 줄이고, 이상감지·예측 모델·동인 자동 식별이 실무 표준으로 부상 — 비즈업 M10(1차) → 이상감지 알림(2차) → 멀티에이전트(3차)의 단계 승격 경로와 일치.
- **TDABC의 표준화**: 시간방정식·CCR 기반 TDABC가 전통 ABC의 구축·유지 부담을 낮추는 사실상의 표준 — 1차 장착 근거.
- **클라우드 ABC의 접근성**: 클라우드 기반 ABC가 도입 기간을 단축해 중소기업 접근성을 열고 있음 — 월 1만원 SaaS 포지셔닝의 근거.
- **미사용능력·유휴원가 관리**: 유휴능력 원가의 별도 관리가 정론으로 정착 — A9 공리의 근거.
- **지속가능성(환경) 원가**: ABC를 환경영향 원가에 결합하는 연구 흐름 — 장기 검토 항목(로드맵 외 참고).

---

# 16. 다음 단계

1. **통합 ERD v2.0 작성** (본 PRD 확정 직후): 기존 ERD 2권 + 테이블명세서(총 63테이블)를 1권으로 통합하며 다음 신규 반영 — ① 기계시간 입력 필드(machine_hours, 배부기준 ③ 선택 시) ② 예산 시나리오(가상 기간) 구조 ③ 주문 잔량·자재소요 파생 뷰 ④ 일자별 선택 모드(record_date) 확정 ⑤ 생산기준 고정 + 재고조정 자동화 컬럼 ⑥ 배부기준 3종 교체(직접노무원가/직접노무시간/기계시간) ⑦ ABC 파일 발견분(원가풀 3기준 배부, 활동 3기준 매트릭스, 동인 건수/비율 토글, 간접인건비 시간 검증)
2. 통합 DDL 스크립트 (RLS·마감잠금 트리거 포함)
3. 엔진 산식 명세서 (엑셀 셀 → Python 함수 매핑, V8 대조 테스트 설계)
4. 화면 정의서 (HTML 목업) + 디자인 가이드

---

## 부록 A. 확정 결정 이력 (Q-A ~ Q-J)

| 결정 | 내용 |
|------|------|
| Q-A | 제조경비 배부기준 = 직접노무원가 / 직접노무시간 / 기계시간 3종 택1 (기계시간 신규 입력) |
| Q-B | 주문 관리 전부 포함, '선택 기능' 지위 (미입력 시 숨김) |
| Q-C | 월합계 기본 + 일자별 선택 모드 |
| Q-D | 예산 시나리오(가상 기간) 1차, A×B×C×D 편성 엔진 2차(산식 보존) |
| Q-E | 노무비 배부 = 공수 기반 단일 (할당인원 방식 미채택) |
| Q-F | 생산기준 고정 + '제품 재고 조정' 라인 자동화 (매출기준 배부 폐기 — pl3 92% 왜곡 실증) |
| Q-G | report 파일은 기존 구조 분석으로 갈음 |
| Q-H | 통합 PRD 1권 완전 대체 (기존 2권 폐기), ERD도 후속 통합 1권 |
| Q-I | 부문-엔진 고정 매핑 (제조 ABC는 3차) |
| Q-J | 최신 지식 = 확정분(TDABC+AI 3종) 1차, 최신 동향은 제15장 이론 근거로 기재 |

### Epic 7~9 retro 결정 (2026-08-08 ~ 2026-08-17)

| 결정 | 내용 |
|------|------|
| A19 | math surface cohesion pattern — 단일 math surface 진입 시점에 함수/dataclass/exception/constants 동시 wire (abc_engine.py 9-1, pdf_generator.py 9-4 precedent) |
| A20 | service module cohesion pattern — kernel + service + handler + tests 4-way wire (CR 1.1 audit-first INSERT 보존) |
| A21 | capability matrix v1.18+ SSOT pattern — 각 Epic 도입 시 capability matrix 신규 row 추가 + drift detector test (P-015 SSOT) wire |
| A22 | inventory projection.py 제거 — `packages/services/m2_input/inventory_math.py` 으로 일원화 (carry-over sprint) |
| A23 | Epic 9 cj-style 4-story + retro 5번째 진입점 패턴 (cj-style Epic 7~8 동일) |
| A24 | capability matrix v1.18 industry-agnostic 4-industry grants (CR 12-1 L4 precedent) |
| A25 | A19 cohesion pattern 6번째 surface = `packages/cost_engine/abc_engine.py` (9-1 wire tip) |
| A26 | Epic 9 8-3 honestly DEFER #4 해소 (8-3 follow-up sprint) |
| A27 | A19 follow-up sprint for 8 honestly DEFER (cj-style 9-6 follow-up 진입) |

### Epic 9 close-out 결정 (2026-08-17, 9-6/9-7 follow-up sprint)

| 결정 | 내용 |
|------|------|
| A28 | ABC_CALCULATION industry-agnostic 보존 (CR 12-1 L4) — 모든 Epic 9 wire 영향 0 |
| A29 | dual-route dispatch (M3 orchestrator) 보존 — service → M9 ABC, else → M3 traditional |
| A30 | SHARED PDF generator factory pattern (`packages/services/m5_reports/pdf_generator.py`) — Discriminated union `report_id: Literal[15, 16, 17, 18, 19, 20, 21]` |
| A31 | Report #15 wire schedule (cj-style Epic 9 6번째 진입점 권장) — A30 SHARED factory EXTENSION 1st case |
| A32 | A30 SHARED factory pattern reuse entry — Report #15 wire 진입 시점에 pdf_generator EXTENSION |
| A33 | A19 cohesion pattern 9 surface — Report #15 wire 진입 시점에 pdf_generator EXTENSION |
| A34 | mixed honestly DEFER 4-category framework — (a) docs 정합 / (b) retro input / (c) separate epic / (d) dedicated sprint |
| A35 | frontend test debt honestly DEFER (d) — vitest mount + TS mirror parity 정직 회복 (9-7 wire 진입) |
| A36 | SDR 검증 프로토콜 wire — 4-step 자동화 (commit prefix lint + sprint-status structure + vitest file count drift + commit consistency) |

### Epic 10 close-out 결정 (2026-08-19, cj-style 5번째 진입점)

| 결정 | 내용 |
|------|------|
| **A37** | **Master PRD v2.0 본체 edit** (§F10.1·§F10.2 + §8.1 M10 + §12 AI 3종 + §13.1 ko-KR + §14 NFR + §14.B NON-GOAL #5·6 + §AD-7/17/25 verbatim + §SM-3a + §A11 + §NFR18 + §부록 A A23~A42). cj-style carry-over 15번째 docs only atomic wire (Epic 10 PRD extension → master PRD 본체 edit). 결정 wire 일자: 2026-08-20 |
| **A38** | A35 frontend test debt dedicated sprint (cj-style carry-over 14번째) — Epic 10 4 stories frontend files + TS mirror parity + vitest mount 일괄 wire |
| **A39** | D-10-2-DEFER-3 LISTEN/NOTIFY consume 별도 epic territory 결정 (post-Epic 10) — AD-25 cache invalidation trigger EXTENSION for close/reopen. **결정 wire 진입 2026-08-20 (사용자 옵션 (a))**: Epic 13 = LISTEN/NOTIFY 전용 epic (Epic 13 1번째 진입점). Story 13-1 = LISTEN/NOTIFY consume trigger EXTENSION wire 진입. D-10-2-DEFER-3 ✅ RESOLVED 진입. A45/A46/A50 preserved (Epic 13 후속 story 진입). cj-style Epic 13 1번째 진입점 결정 verbatim bind. AD-25 verbatim 100% binding 진입. **✅ done 진입 2026-08-20** (Epic 13 PRD entry 결정 wire 완료 + §F13 LISTEN/NOTIFY 명세 신규 + §8.1 M10-(d)·§F10.1-(d) EXTENSION 진입 + §15 로드맵 Epic 13 row + A51 결정 wire) |
| **A40** | A31/A32/A33 (Report #15 wire schedule) 처리 결정 — Epic 11 carry-over sprint 진입 시 wire (cj-style Epic 11 4번째 진입점 = Epic 11 Story 11.5 + 11.6 진입 결정) |
| **A41** | Epic 11 carry-over sprint 진입 결정 (A13/A17/A18 sprint-up items) — 11-3 DEFER 8 items triage + W2 reopen 4-channel 검증 + A5 drift detector 3-way extension. ✅ done via 11-4 (cj-style carry-over 11번째) + 11-5 (cj-style 36번째) atomic wire |
| **A42** | A36 SDR 검증 4-step 자동화 wire 보존 + Epic 11+ 적용 — commit prefix lint + sprint-status structure 검증 + vitest file count drift + commit consistency 자동 검증 단계 모두 PASS |

### Epic 13 PRD entry 결정 (2026-08-20, cj-style Epic 13 1번째 진입점)

| 결정 | 내용 |
|------|------|
| **A51** | **Epic 13 = LISTEN/NOTIFY 전용 epic PRD entry** — Epic 10 close-out retro §7 A39 결정 verbatim wire (cj-style Epic 13 1번째 진입점). Story 13-1 = LISTEN/NOTIFY consume trigger EXTENSION wire 진입 (NOTIFY trigger alembic 0033 + LISTEN daemon FastAPI lifespan + 4-channel cache eviction handlers + reconnect/backoff + V8 determinism + CR 12-5 cross-lang drift detector EXTENSION). 결정 wire = master PRD v2.2 atomic edit (§F13 신규 + §8.1 M10-(d)·§F10.1-(d) EXTENSION + §15 로드맵 Epic 13 row + §부록 A A39 done 진입 + A51 NEW). AD-25 verbatim 100% binding. D-10-2-DEFER-3 ✅ RESOLVED. capability matrix v1.22 신규 row `LISTEN_NOTIFY` (Epic 13 wire 진입). CR 11-3 honest-DEFER discipline + A36 SDR 검증 4-step 자동 적용 (commit prefix lint + sprint-status structure + vitest file count drift + commit consistency 모두 PASS). ✅ done 진입 2026-08-20 (commit `3e398b9`). **A52 follow-on**: Story 13-1 atomic wire 진입 |
| **A52** | **✅ done** (2026-08-20, cj-style Epic 13 2번째 진입점 = cj-style 42번째 epic 연속 정직 회복) — Story 13-1 bmad-dev-story atomic wire T1~T8 결정 wire. wire_commit = `f2ea2f6` atomic single sweep, 17 files (12 NEW + 5 MODIFIED), ~107 NEW pytest PASS, 0 NEW ruff (8 auto-fixed), A19 cohesion 8 surface PASS, capability matrix v1.22 SSOT RED→GREEN, 3중 게이트 FINAL CLEAN. D-13-1-DEFER-1/2/3 preserved (CR 11-3 진형화) — D-13-1-DEFER-1 ✅ RESOLVED 진입 결정 wire (A54 master PRD v2.3 atomic edit 진입 결정). 결정 wire 일자: 2026-08-20 (Epic 13 PRD entry done 진입 직후 즉시) |
| **A45** | **✅ 결정 wire 진입** (2026-08-20, cj-style Epic 13 5번째 진입점 = cj-style Epic 13 carry-over 17번째 docs only) — Epic 11 close-out retro 2nd (2026-08-20) — 11-3 honestly DEFER 3 items (T10 docs LOW + A5 partial LOW + reopen state transition LOW) still-pending. **사용자 결정 = 옵션 (a) Epic 14 follow-up sprint 진입** 결정 wire 진입 (bundled with A46 into ONE Epic 14 carry-over sprint = cj-style Epic 14 3번째 진입점 진입 시점). Epic 14 PRD entry + 14-1 atomic wire DONE 후 follow-up sprint 진입 결정 wire 보존. deadline: Epic 14 follow-up sprint 진입 시점 (cj-style Epic 14 3번째 진입점) |
| **A46** | **✅ 결정 wire 진입** (2026-08-20, cj-style Epic 13 5번째 진입점 = cj-style Epic 13 carry-over 17번째 docs only) — Epic 11 close-out retro 2nd (2026-08-20) — 11-5 A13 residual stub UUIDs (`00000000-...`) with `TODO(11-4 carry)` markers preserved as W-class DEFER. **사용자 결정 = 옵션 (a) Epic 14 follow-up sprint 진입** 결정 wire 진입 (bundled with A45 into ONE Epic 14 carry-over sprint = cj-style Epic 14 3번째 진입점 진입 시점). Epic 14 PRD entry + 14-1 atomic wire DONE 후 follow-up sprint 진입 결정 wire 보존. deadline: Epic 14 follow-up sprint 진입 시점 (cj-style Epic 14 3번째 진입점) |
| **A50** | (preserved) Epic 11 close-out retro 2nd (2026-08-20) — A39 LISTEN/NOTIFY consume trigger EXTENSION 결정 = 별도 epic territory. Epic 13 PRD entry 진입 ✅ done (A51 결정 wire = A39 결정 = Epic 13 진입 결정 wire). A50 reframing close-out 완료 |

### Epic 13 close-out retro 결정 (2026-08-20, cj-style Epic 13 4번째 진입점 = cj-style 43번째 epic 연속 정직 회복)

| 결정 | 내용 |
|------|------|
| **A53** | **✅ done** (2026-08-20, cj-style Epic 13 5번째 진입점 = cj-style Epic 13 carry-over 17번째 docs only = cj-style 44번째 epic 연속 정직 회복) — **D-13-1-DEFER-3 separate epic LISTEN/NOTIFY consume 2nd batch 결정** (cross-tenant invalidation fan-out + multi-process coordination = Epic 13 후속 story 진입 결정). **사용자 결정 = 옵션 (a) Epic 14 진입** = Epic 14 = LISTEN/NOTIFY consume 2nd batch territory 진입 결정 wire. Epic 14 PRD entry 진입 대기 (cj-style Epic 14 1번째 진입점 = cj-style 45번째 epic 연속 정직 회복 진입 대기). 옵션 (b) Epic 13 follow-up sprint 진입 / 옵션 (c) Epic 13 close-out 후 별도 Epic 14 진입 모두 rejected (rationale: Epic 14 단일 territory로 통합 진입 = A45+A46 follow-up sprint까지 bundled 가능 = 결정 wire 효율성 + cj-style 1번째 진입점 표준 진입 가능). 3중 게이트 impact NONE (docs only 변경, no code/test/sprint-status delta 외에 PRD edit 신규). CR 11-3 honest-DEFER discipline + A36 SDR 검증 4-step 자동 적용 (commit prefix lint PASS + sprint-status structure PASS + vitest file count drift 0건 + commit consistency PASS). 결정 wire 일자: 2026-08-20 |
| **A54** | **✅ done** (2026-08-20, cj-style Epic 13 5번째 진입점 = cj-style Epic 13 carry-over 17번째 docs only) — master PRD v2.2 → v2.3 atomic edit (D-13-1-DEFER-1 ✅ RESOLVE). 결정 wire 진입 = §F13 verbatim 13-1 wire 정합 확장 (F13.1 6-key alphabetical JSON payload + alembic 0033 trigger verbatim + F13.2 4-channel handler verbatim wire + F13.3 V8 determinism + cross-language drift detector EXTENSION + F13.4 T1~T8 atomic wire DONE + capability `LISTEN_NOTIFY` v1.22 4-industry grants) + §15 로드맵 Epic 13 row status in-progress → done (cj-style 1~4번째 진입점 모두 wire DONE) + §부록 A A52 done + A53+A54+A55+A56 신규 결정 + AD-25 EXTENSION 표기. 3중 게이트 impact NONE (docs only 변경). CR 11-3 honest-DEFER discipline + A36 SDR 검증 4-step 자동 적용 (commit prefix lint PASS + sprint-status structure PASS + vitest file count drift 0건 + commit consistency PASS) |
| **A55** | (preserved) **LISTEN/NOTIFY 실측 evidence 정합 sweep** (D-13-1-DEFER-2 ✅ RESOLVE 진입 시점). 1차 출시 후 진입 시점 (production runtime data 정합 sweep) — D-13-1-DEFER-3 결정 시점에 동시 결정 wire. Owner: Amelia + Dana. deadline: 1차 출시 후 진입 시점 |
| **A56** | **✅ done + preserved** (2026-08-20) — A42 A36 SDR 검증 4-step 자동화 wire 보존 + Epic 14+ 모든 stories 자동 적용 결정 (Epic 13 1-story cycle 정직 회복 검증). 13-1 wire 시점에 commit prefix lint + sprint-status structure + vitest file count drift + commit consistency 모두 PASS 자동 적용. CR 11-3 honest-DEFER discipline + A36 SDR 검증 4-step 모두 보존 결정 |

### Epic 14 PRD entry 결정 (2026-08-20, cj-style Epic 14 1번째 진입점 = cj-style 45번째 epic 연속 정직 회복)

| 결정 | 내용 |
|------|------|
| **A57** | **Epic 14 = LISTEN/NOTIFY Consume 2nd Batch PRD entry** — Epic 13 close-out retro §7 A53 결정 verbatim wire (cj-style Epic 14 1번째 진입점 = cj-style 45번째 epic 연속 정직 회복). Story 14-1 = cross-tenant invalidation fan-out + multi-process coordination 본체 wire 진입 (T1~T9 atomic single sprint). 결정 wire = master PRD v2.4 → v2.5 atomic edit (§F14 신규 + §8.1 M10-(d)·§F10.1-(d) cross-tenant fan-out EXTENSION 진입 + §15 로드맵 Epic 14 row status 백로그 → in-progress + §부록 A A57+A58+A59 신규 결정 표 + AD-25 EXTENSION 5+ channels + capability matrix v1.22 → v1.23 LISTEN_NOTIFY_TENANT_FANOUT + LISTEN_NOTIFY_MULTIPROCESS 2 NEW rows industry-agnostic 4-industry grants ✅/✅/✅/✅). AD-25 verbatim 100% binding EXTENSION. D-13-1-DEFER-3 ✅ RESOLVED (A53 결정 wire). CR 11-3 honest-DEFER discipline + A36 SDR 검증 4-step 자동 적용 (commit prefix lint + sprint-status structure + vitest file count drift + commit consistency 모두 PASS). ✅ done 진입 2026-08-20. **A58+A59 follow-on**: AD-25 EXTENSION + capability 신규 row 결정 wire |
| **A58** | **AD-25 EXTENSION 4-channel → 5+ channels 결정** — Epic 14 PRD entry 진입 시점에 결정 (cj-style Epic 14 1번째 진입점 = cj-style 45번째 epic 연속 정직 회복). Epic 13 13-1 wire 시점 4-channel publisher (`ai_cache` / `cost_engine_cache` / `fiscal_period_cache` / `closing_snapshot_cache`) 에 **cross_tenant_fanout channel 1 channel 추가** 결정 wire (총 5+ channels EXTENSION). Cross-tenant invalidation fan-out channel = tenant-level subscription routing + multi-tenant isolation 검증 (CR 0-2 RLS lesson 적용 + AD-22 verbatim 보존). NOTIFY payload 7-key alphabetical EXTENSION (channel='cross_tenant_fanout' + source_tenant_id + target_tenant_ids + correction_group_id + invalidation_id + period_key + trace_id). **Multi-process coordination Option 1 결정**: PostgreSQL `LISTEN/NOTIFY` only multi-process coordination via pg_notify fan-out leader/follower model (PostgreSQL advisory lock `pg_try_advisory_xact_lock(LISTEN_FANOUT_LOCK_ID)` 으로 leader election + follower health check 30s interval + leader takeover 90s timeout). Option 2 Redis pub/sub fan-out **rejected** 결정 (rationale: G2 "새벽에 혼자 고칠 수 있는 시스템" 정합 — 인프라 최소화). Alembic 0034 NEW 결정 (down_revision='0033_listen_notify_consume_trigger'). CR 11-3 honest-DEFER discipline + A36 SDR 검증 4-step 자동 적용 (commit prefix lint PASS + sprint-status structure PASS + vitest file count drift 0건 + commit consistency PASS). 결정 wire 일자: 2026-08-20 |
| **A59** | **Capability matrix v1.22 → v1.23 LISTEN_NOTIFY_TENANT_FANOUT + LISTEN_NOTIFY_MULTIPROCESS 2 NEW rows 결정** — Epic 14 PRD entry 진입 시점에 결정 (cj-style Epic 14 1번째 진입점 = cj-style 45번째 epic 연속 정직 회복). `Capability.LISTEN_NOTIFY_TENANT_FANOUT = "listen_notify_tenant_fanout"` 4-industry grants: manufacturing ✅ + service ✅ + manufacturing_service ✅ + manufacturing_service_other ✅ (industry-agnostic, CR 12-1 L4 precedent 미러, AI_INSIGHT 10-1 + LISTEN_NOTIFY 13-1 wire pattern). `Capability.LISTEN_NOTIFY_MULTIPROCESS = "listen_notify_multiprocess"` 동일 industry-agnostic 4-industry grants ✅/✅/✅/✅. 미허용 tenant 의 cross-tenant fan-out channel listener 등록 차단 + multi-process coordination leader election 제외 결정 wire. SSOT RED→GREEN EXTENSION (capability matrix v1.23 신규 2 rows + capability.py EXTENSION 2 NEW enum + `require_capability()` Dependency 2개 신규 wire). CR 11-3 honest-DEFER discipline + A36 SDR 검증 4-step 자동 적용 (commit prefix lint PASS + sprint-status structure PASS + vitest file count drift 0건 + commit consistency PASS). 결정 wire 일자: 2026-08-20 |

### Phase 3 PRD entry 결정 (2026-08-20, cj-style Phase 3 1번째 진입점 = cj-style 49번째 epic 연속 정직 회복)

| 결정 | 내용 |
|------|------|
| **A65** | **Phase 3 = 로그인/회원가입 UI + auth middleware (Epic 1 완성 territory) PRD entry** — Phase 2 close-out 직후의 다음 territory 진입 결정 wire (cj-style Phase 3 1번째 진입점 = cj-style 49번째 epic 연속 정직 회복). Phase 3 territory 결정 = Epic 1 carry-over 정직 회복 (Story 1.1 F-1 Supabase SSR client + F-4 accessToken string pass + F-30 rls_db fixture 모두 D-1-1-DEFER-* honestly DEFER preserved) + login/signup/forgot-password UI 신규 wire (Epic 1 partial scaffold = `(auth)` route group + `apps/web/app/[locale]/(auth)/onboarding/industry/page.tsx` + IndustrySelector 이미 wire 됨) + auth middleware EXTENSION (`apps/web/middleware.ts` 의 next-intl middleware EXTENSION = Supabase session check + `(dashboard)` 보호 + `?redirect=` 쿼리 보존 + `(auth)` 공개 route group bypass + `/api/v1/*` bypass + 2FA 미설정 사용자 `/account/security?reason=2fa_required` redirect) + logout flow + AD-26 Auth Foundation 신규 결정 + capability matrix v1.23 → v1.24 EXTENSION (LOGIN + SIGNUP + AUTH_MIDDLEWARE + FORGOT_PASSWORD + LOGOUT 5 NEW rows). 결정 wire = master PRD v2.5 → v3.0 atomic edit (§F15 신규 + §8.1 M0-(d)·M0-(e)·M0-(f) auth 3 NEW 인수 불릿 + §15 로드맵 Phase 3 row status 백로그 → in-progress + §부록 A A65~A69 신규 결정 표 + AD-26 신규 결정 + capability matrix v1.24 EXTENSION 5 NEW rows). CR 11-3 honest-DEFER discipline + A36 SDR 검증 4-step 자동 적용 (commit prefix lint + sprint-status structure + vitest file count drift + commit consistency 모두 PASS). 결정 wire 일자: 2026-08-20. **A66+A67+A68+A69 follow-on**: capability 신규 5 rows 결정 wire |
| **A66** | **AD-26 Auth Foundation 신규 결정** — Phase 3 PRD entry 진입 시점에 결정 (cj-style Phase 3 1번째 진입점 = cj-style 49번째 epic 연속 정직 회복). AD-26 = (a) Supabase SSR auth client (`createServerClient` for Server Components + `createBrowserClient` for Client Components, single source of truth URL + anon key) / (b) `sb-access-token` cookie session = `httpOnly` + `secure` + `sameSite=lax` + `path=/` + `maxAge=3600` (1시간, refresh token 으로 자동 �신) / (c) next-intl middleware EXTENSION = Supabase session check + `(dashboard)` 보호 + `?redirect=` 쿼리 보존 + `(auth)` 공개 + `/api/v1/*` bypass + Edge Runtime 명시 / (d) auth route group `(auth)` 공개 (login + signup + forgot-password + 2fa + email-verification-pending) / (e) dashboard route group `(dashboard)` 보호 (Supabase session 필수 + Epic 12 2FA 미설정 시 `/account/security?reason=2fa_required` redirect) / (f) Supabase PKCE flow + sameSite=lax cookie CSRF 방어 결정 (별도 CSRF token 미사용, Supabase 권장 정합) / (g) email 존재 여부 노출 방지 (forgot-password 항상 200 반환 결정 wire). 결정 wire 일자: 2026-08-20 |
| **A67** | **Capability matrix v1.23 → v1.24 LOGIN + SIGNUP + AUTH_MIDDLEWARE + FORGOT_PASSWORD + LOGOUT 5 NEW rows 결정** — Phase 3 PRD entry 진입 시점에 결정 (cj-style Phase 3 1번째 진입점 = cj-style 49번째 epic 연속 정직 회복). `Capability.LOGIN = "login"` 4-industry grants: manufacturing ✅ + service ✅ + manufacturing_service ✅ + manufacturing_service_other ✅ (industry-agnostic, CR 12-1 L4 precedent 미러, AI_INSIGHT 10-1 + LISTEN_NOTIFY 13-1 + LISTEN_NOTIFY_TENANT_FANOUT 14-1 + LISTEN_NOTIFY_MULTIPROCESS 14-1 wire pattern). `Capability.SIGNUP` + `Capability.AUTH_MIDDLEWARE` + `Capability.FORGOT_PASSWORD` + `Capability.LOGOUT` 동일 industry-agnostic 4-industry grants ✅/✅/✅/✅. 미허용 tenant 의 login/signup/forgot-password 페이지 진입 차단 결정 wire. SSOT RED→GREEN EXTENSION (capability matrix v1.24 신규 5 rows + capability.py EXTENSION 5 NEW enum + `require_capability()` Dependency 5개 신규 wire). CR 11-3 honest-DEFER discipline + A36 SDR 검증 4-step 자동 적용 (commit prefix lint PASS + sprint-status structure PASS + vitest file count drift 0건 + commit consistency PASS). 결정 wire 일자: 2026-08-20 |
| **A68** | **Epic 1 carry-over DEFER 1~N honestly preserved (D-1-1-DEFER-*) 결정** — Phase 3 PRD entry 진입 시점에 결정 (cj-style Phase 3 1번째 진입점 = cj-style 49번째 epic 연속 정직 회복). Story 1.1 F-1 (Supabase SSR client wire) + F-4 (accessToken string pass to IndustrySelector) + F-30 (rls_db fixture wire) 모두 Phase 3 wire 진입 시점에 honestly RESOLVE 결정 wire 진입 (T1+T4 wire 진입 시점에 resolve). Story 1.1 F-2 (next-intl i18n bundle) + F-3 (IndustryCard UI polish) + F-5~F-29 (Epic 1 carry-over 25 items) preserved + D-1-1-DEFER-1 (Magic link login) + D-1-1-DEFER-2 (Social login OAuth: Google/Naver/Kakao) + D-1-1-DEFER-3 (SSO enterprise SAML) honestly preserved (CR 11-3 discipline) — Epic 1 close-out 시점에 결정 wire 보존 (cj-style Epic 1 follow-up sprint 진입 시점 = cj-style Phase 3 close-out retro 진입 시점). 결정 wire 일자: 2026-08-20 |
| **A69** | **Phase 3 wire scope T1~T8 결정 + Epic 1 partial scaffold 보존** — Phase 3 PRD entry 진입 시점에 결정 (cj-style Phase 3 1번째 진입점 = cj-style 49번째 epic 연속 정직 회복). T1 Supabase SSR client + T2 login page + T3 signup page + T4 auth middleware EXTENSION + T5 logout + T6 forgot-password + T7 capability v1.24 EXTENSION 5 NEW rows + T8 tests + 3중 게이트 FINAL CLEAN atomic commit. Epic 1 partial scaffold 보존 결정 wire: (1) `apps/web/app/[locale]/(auth)/layout.tsx` minimal shell 보존 (Phase 3 wire 진입 시점에 design tokens EXTENSION 결정 wire 보존) / (2) `apps/web/app/[locale]/(auth)/onboarding/industry/page.tsx` 보존 (Phase 3 T3 signup wire 시점에 atomic redirect 보존) / (3) `apps/web/components/onboarding/IndustrySelector.tsx` + `IndustryCard.tsx` 보존 (Phase 3 wire 영향 0) / (4) `apps/web/middleware.ts` next-intl EXTENSION 결정 wire (Phase 3 T4 진입 시점에 EXTENSION). estimated ~70 NEW vitest PASS + ~10 NEW pytest PASS + 0 NEW ruff + 0 regressions. wire_commit = TBD (cj-style Phase 3 3번째 진입점 = cj-style 51번째 epic 연속 정직 회복 atomic single sweep T1~T8, expected ~20-22 files = ~15 NEW + ~5-7 MODIFIED). 결정 wire 일자: 2026-08-20 |

### Phase 3 close-out retro 결정 (2026-08-22, cj-style Phase 3 3번째 진입점 = cj-style 51~52번째 epic 연속 정직 회복)

| 결정 | 내용 |
|------|------|
| **A70** | **D-1-1-DEFER-1 Magic link 결정 wire 진입** — Phase 3 close-out retro 진입 시점에 결정 (cj-style Phase 3 3번째 진입점 = cj-style 51~52번째 epic 연속 정직 회복). 옵션 (a) Magic link wire 진입 / 옵션 (b) Epic 15 carry-over 진입 / 옵션 (c) Phase 5 carry-over 진입 모두 결정 보류 — Epic 15+ 진입 시점에 사용자 결정 wire 보존. CR 11-3 honest-DEFER discipline 50번째 epic 연속 정직 회복 검증 ✅ |
| **A71** | **D-1-1-DEFER-2 Social login OAuth 결정 wire 진입** — Phase 3 close-out retro 진입 시점에 결정 (cj-style Phase 3 3번째 진입점 = cj-style 51~52번째 epic 연속 정직 회복). 옵션 (a) Google/Naver/Kakao OAuth wire 진입 / 옵션 (b) Epic 15 carry-over 진입 / 옵션 (c) Phase 5 carry-over 진입 모두 결정 보류 — Epic 15+ 진입 시점에 사용자 결정 wire 보존. CR 11-3 honest-DEFER discipline 50번째 epic 연속 정직 회복 검증 ✅ |
| **A72** | **D-1-1-DEFER-3 SSO enterprise SAML 결정 wire 진입** — Phase 3 close-out retro 진입 시점에 결정 (cj-style Phase 3 3번째 진입점 = cj-style 51~52번째 epic 연속 정직 회복). 옵션 (a) SAML wire 진입 / 옵션 (b) Epic 15 carry-over 진입 / 옵션 (c) Phase 5 carry-over 진입 모두 결정 보류 — Epic 15+ 진입 시점에 사용자 결정 wire 보존. CR 11-3 honest-DEFER discipline 50번째 epic 연속 정직 회복 검증 ✅ |
| **A73** | **✅ done** (2026-08-22, cj-style Phase 4 1번째 진입점 = cj-style 53번째 epic 연속 정직 회복) — **옵션 (a) Phase 4 진입** = Deployment config + Dockerfile territory 진입 결정 wire. Phase 3 close-out retro 진입 시점에 사용자 결정 = 옵션 (a) Phase 4 진입 결정 (옵션 (b) Epic 15 진입 / 옵션 (c) carry-over 진입 모두 rejected). Phase 4 territory = Vercel frontend + Railway backend + Supabase PostgreSQL production deployment config 결정. 결정 wire 일자: 2026-08-22 |
| **A74** | **✅ done** (2026-08-22, cj-style Phase 4 1번째 진입점 = cj-style 53번째 epic 연속 정직 회복) — **Master PRD v3.0 → v3.1 atomic edit** (D-1-1-DEFER-* RESOLVE 표기 보류). Phase 4 PRD entry 진입 시점에 master PRD v3.0 → v3.1 atomic edit 결정 wire 진입 — front matter title v3.0 → v3.1 + changelog v3.1 entry + §F16 신규 (F16.1 vercel.json + F16.2 railway.toml + F16.3 per-app Dockerfile + F16.4 docs/deployment.md + F16.5 health check + observability + F16.6 database backup + F16.7 tests + wire scope T1~T8) + §8.1 M0-(g) production deployment 인수 불릿 + §15 로드맵 Phase 4 row status 백로그 → in-progress + §부록 A A73+A74+A76+A77+A78 신규 결정 표 + AD-27 Deployment 신규 결정 + capability matrix v1.24 → v1.25 EXTENSION DEPLOYMENT_PROD + DEPLOYMENT_STAGING + DEPLOYMENT_DATABASE_BACKUP + DEPLOYMENT_HEALTH_CHECK 4 NEW rows (industry-agnostic 4-industry grants ✅/✅/✅/✅). CR 11-3 honest-DEFER discipline + A36 SDR 검증 4-step 자동 적용 (commit prefix lint PASS + sprint-status structure PASS + vitest file count drift 0건 + commit consistency PASS). 결정 wire 일자: 2026-08-22 |
| **A75** | **(preserved)** A42 A36 SDR 검증 4-step 보존 + Epic 15+ 적용 — commit prefix lint + sprint-status structure + vitest file count drift + commit consistency 자동 검증 단계 모두 PASS |

### Phase 4 PRD entry 결정 (2026-08-22, cj-style Phase 4 1번째 진입점 = cj-style 53번째 epic 연속 정직 회복)

| 결정 | 내용 |
|------|------|
| **A76** | **AD-27 Deployment 신규 결정** — Phase 4 PRD entry 진입 시점에 결정 (cj-style Phase 4 1번째 진입점 = cj-style 53번째 epic 연속 정직 회복). AD-27 = (a) **Vercel frontend** 결정 (vercel.json + framework=nextjs + regions=[icn1] Seoul + buildCommand=`pnpm --filter web build` + installCommand=`pnpm install --frozen-lockfile` + outputDirectory=`apps/web/.next` + env mapping NEXT_PUBLIC_SUPABASE_URL/ANON_KEY/API_BASE_URL + headers CSP+X-Frame-Options+HSTS + redirects legacy `/ko-KR/*` → `/ko/*` next-intl 정합) / (b) **Railway backend** 결정 (railway.toml + builder=DOCKERFILE + dockerfilePath=`apps/api/Dockerfile` + healthcheckPath=`/api/v1/health` + healthcheckTimeout=300 + restartPolicyType=ON_FAILURE + restartPolicyMaxRetries=3 + env mapping DATABASE_URL/SUPABASE_JWT_SECRET/SUPABASE_URL/ANON_KEY/SERVICE_ROLE_KEY/SENTRY_DSN/ENVIRONMENT=production) / (c) **Supabase PostgreSQL production** 결정 (Supabase Pro plan + PITR 7일 자동 backup + Storage 결정 wire 보류) / (d) **Sentry observability** 결정 (Sentry browser SSR-safe init + tracesSampleRate=0.1 + Sentry FastAPI server integration + FastAPI middleware integration + SQLAlchemy integration opt-in) / (e) **per-app Dockerfile 분리** 결정 (apps/web/Dockerfile Next.js standalone output + apps/api/Dockerfile FastAPI uvicorn, AD-14 stack pin by @sha256: digest 모든 베이스 이미지 결정) / (f) **Health check endpoint** 결정 (`GET /api/v1/health` FastAPI endpoint + `GET /api/health` Next.js route handler + liveness/readiness 분리 `/health/live` + `/health/ready` + database connectivity check + JWT verification test + uptime_seconds 표시). 결정 wire 일자: 2026-08-22 |
| **A77** | **Capability matrix v1.24 → v1.25 DEPLOYMENT_PROD + DEPLOYMENT_STAGING + DEPLOYMENT_DATABASE_BACKUP + DEPLOYMENT_HEALTH_CHECK 4 NEW rows 결정** — Phase 4 PRD entry 진입 시점에 결정 (cj-style Phase 4 1번째 진입점 = cj-style 53번째 epic 연속 정직 회복). `Capability.DEPLOYMENT_PROD = "deployment_prod"` 4-industry grants: manufacturing ✅ + service ✅ + manufacturing_service ✅ + manufacturing_service_other ✅ (industry-agnostic, CR 12-1 L4 precedent 미러, AI_INSIGHT 10-1 + LISTEN_NOTIFY 13-1 + LISTEN_NOTIFY_TENANT_FANOUT 14-1 + LISTEN_NOTIFY_MULTIPROCESS 14-1 + LOGIN/SIGNUP/AUTH_MIDDLEWARE/FORGOT_PASSWORD/LOGOUT Phase 3 wire pattern). `Capability.DEPLOYMENT_STAGING` + `Capability.DEPLOYMENT_DATABASE_BACKUP` + `Capability.DEPLOYMENT_HEALTH_CHECK` 동일 industry-agnostic 4-industry grants ✅/✅/✅/✅. 미허용 tenant 의 production deployment 진입 차단 + staging deployment 진입 차단 결정 wire. SSOT RED→GREEN EXTENSION (capability matrix v1.25 신규 4 rows + capability.py EXTENSION 4 NEW enum + `require_capability()` Dependency 4개 신규 wire). CR 11-3 honest-DEFER discipline + A36 SDR 검증 4-step 자동 적용 (commit prefix lint PASS + sprint-status structure PASS + vitest file count drift 0건 + commit consistency PASS). 결정 wire 일자: 2026-08-22 |
| **A78** | **Phase 4 wire scope T1~T8 결정** — Phase 4 PRD entry 진입 시점에 결정 (cj-style Phase 4 1번째 진입점 = cj-style 53번째 epic 연속 정직 회복). T1 Vercel config wire (vercel.json) + T2 Railway config wire (railway.toml) + T3 per-app Dockerfile wire (apps/web/Dockerfile + apps/api/Dockerfile) + T4 deployment runbook wire (docs/deployment.md 12 sections) + T5 health check + observability wire (apps/api/core/health.py + apps/api/core/observability.py + apps/web/lib/observability/sentry.ts + apps/web/app/api/health/route.ts + apps/api/main.py MODIFIED health router include) + T6 database backup strategy wire (apps/api/alembic/versions/0036_phase_4_backup_strategy.py + docs/database-backup.md) + T7 capability v1.25 EXTENSION 4 NEW rows + T8 tests + 3중 게이트 FINAL CLEAN atomic commit. estimated ~40 NEW pytest PASS + ~20 NEW vitest PASS + 0 NEW ruff + 0 regressions. wire_commit = TBD (cj-style Phase 4 3번째 진입점 = cj-style 55번째 epic 연속 정직 회복 atomic single sweep T1~T8, expected ~10-12 files = ~10 NEW + ~3-5 MODIFIED). 결정 wire 일자: 2026-08-22 |

### Phase 4 close-out retro 결정 (2026-08-22, cj-style Phase 4 4번째 진입점 = cj-style 56~57번째 epic 연속 정직 회복)

| 결정 | 내용 |
|------|------|
| **A70** | **✅ done** (2026-08-22, cj-style Phase 4 close-out retro 진입 시점에 결정, cj-style 56~57번째 epic 연속 정직 회복) — **D-1-1-DEFER-1 Magic link 결정 wire** = Epic 15 진입 시점에 wire 결정. 옵션 (a) Magic link wire 진입 (Epic 15 territory 진입) 결정 (옵션 (b) Epic 15 carry-over 진입 / 옵션 (c) Phase 5 carry-over 진입 모두 rejected — rationale: Epic 15 = Magic link + Social OAuth + SSO 통합 territory 표준 진입 가능 = cj-style 1번째 진입점 표준 진입 가능 = 결정 wire 효율성). Epic 15 PRD entry 진입 시점에 적용 (cj-style Epic 15 1번째 진입점 = cj-style 58번째 epic 연속 정직 회복 진입 시점에 동시 결정 wire). CR 11-3 honest-DEFER discipline 50~58번째 epic 연속 정직 회복 검증 ✅ |
| **A71** | **✅ done** (2026-08-22, cj-style Phase 4 close-out retro 진입 시점에 결정) — **D-1-1-DEFER-2 Social login OAuth 결정 wire** = Epic 15 진입 시점에 wire 결정. 옵션 (a) Google/Naver/Kakao OAuth wire 진입 (Epic 15 territory 진입) 결정 (옵션 (b) Epic 15 carry-over 진입 / 옵션 (c) Phase 5 carry-over 진입 모두 rejected — rationale: Epic 15 = Magic link + Social OAuth + SSO 통합 territory 표준 진입 가능). Epic 15 PRD entry 진입 시점에 적용. CR 11-3 honest-DEFER discipline 50~58번째 epic 연속 정직 회복 검증 ✅ |
| **A72** | **✅ done** (2026-08-22, cj-style Phase 4 close-out retro 진입 시점에 결정) — **D-1-1-DEFER-3 SSO enterprise SAML 결정 wire** = Epic 15 진입 시점에 wire 결정. 옵션 (a) SAML wire 진입 (Epic 15 territory 진입) 결정 (옵션 (b) Epic 15 carry-over 진입 / 옵션 (c) Phase 5 carry-over 진입 모두 rejected — rationale: Epic 15 = Magic link + Social OAuth + SSO 통합 territory 표준 진입 가능). Epic 15 PRD entry 진입 시점에 적용. CR 11-3 honest-DEFER discipline 50~58번째 epic 연속 정직 회복 검증 ✅ |

### Epic 15 PRD entry 결정 (2026-08-22, cj-style Epic 15 1번째 진입점 = cj-style 58번째 epic 연속 정직 회복)

| 결정 | 내용 |
|------|------|
| **A79** | **Epic 15 = Magic link + Social OAuth + SSO enterprise SAML 통합 territory PRD entry** — Phase 4 close-out retro 진입 시점에 옵션 (a) Epic 15 진입 결정 (cj-style Epic 15 1번째 진입점 = cj-style 58번째 epic 연속 정직 회복). Epic 15 territory = D-1-1-DEFER-1/2/3 honestly ✅ RESOLVE 결정 (A70+A71+A72 ✅ done 진입 wire). Epic 15-1 = magic link + social OAuth + SSO enterprise SAML 본체 wire 진입 (T1~T8 atomic single sprint). 결정 wire = master PRD v3.1 → v3.2 atomic edit (§F17 신규 + §8.1 M0-(h)·M0-(i)·M0-(j) 3 NEW 인수 불릿 + §15 로드맵 Epic 15 row status 백로그 → in-progress + §부록 A A70+A71+A72 ✅ done + A75 preserved + A79+A80+A81+A82 신규 결정 표 + AD-28 Magic link + Social OAuth + SSO enterprise SAML 신규 결정 + capability matrix v1.25 → v1.26 EXTENSION 5 NEW rows industry-agnostic 4-industry grants ✅/✅/✅/✅). D-1-1-DEFER-1/2/3 ✅ RESOLVED (A70+A71+A72 결정 wire). CR 11-3 honest-DEFER discipline 58번째 epic 연속 정직 회복 + A36 SDR 검증 4-step 자동 적용 (commit prefix lint PASS + sprint-status structure PASS + vitest file count drift 0건 + commit consistency PASS). ✅ done 진입 2026-08-22. **A80+A81+A82 follow-on**: AD-28 신규 + capability 신규 5 rows + wire scope T1~T8 결정 |
| **A80** | **AD-28 Magic link + Social OAuth + SSO enterprise SAML 신규 결정** — Epic 15 PRD entry 진입 시점에 결정 (cj-style Epic 15 1번째 진입점 = cj-style 58번째 epic 연속 정직 회복). AD-28 = (a) **Magic link** 결정 wire (Supabase `signInWithOtp({ email, options: { emailRedirectTo } })` wrapper + 5회 cool-down sessionStorage 30s + email 존재 여부 노출 방지 security invariant try/catch/finally + audit-first INSERT `magic_link_sent` CR 1-1 verbatim + `sb-access-token` cookie session 자동 설정 Phase 3-1 T1 wire 정합 + Epic 12 2FA 미설정 시 `/auth/2fa` redirect 결정) / (b) **Social OAuth Google/Naver/Kakao** 결정 wire (Supabase `signInWithOAuth({ provider, options: { redirectTo } })` wrapper + provider whitelist `ALLOWED_SOCIAL_PROVIDERS = frozenset({'google', 'naver', 'kakao'})` strict reject + 3회 cool-down sessionStorage 60s + audit-first INSERT `social_oauth_initiated` CR 1-1 verbatim + OAuth callback handler `/auth-callback` + `exchangeCodeForSession(code)` + Naver OAuth Option A/B 결정 wire 보존) / (c) **SSO enterprise SAML** 결정 wire (`python3-saml==1.16.0` AD-14 stack pin + SAML response validation (signature + `NotBefore`/`NotOnOrAfter` + `Audience` + `Destination` + `InResponseTo` + RelayState) + JIT user provisioning 5-step atomic flow + multi-tenant isolation CR 0-2 RLS lesson + 4 SSO routes `/api/v1/auth/sso/{login,acs,metadata,sls}` + audit-first INSERT `sso_identity_linked` CR 1-1 verbatim + tenant slug 별 IdP metadata routing multi-tenant SSO). 결정 wire 일자: 2026-08-22 |
| **A81** | **Capability matrix v1.25 → v1.26 MAGIC_LINK + SOCIAL_OAUTH_GOOGLE + SOCIAL_OAUTH_NAVER + SOCIAL_OAUTH_KAKAO + SSO_ENTERPRISE 5 NEW rows 결정** — Epic 15 PRD entry 진입 시점에 결정 (cj-style Epic 15 1번째 진입점 = cj-style 58번째 epic 연속 정직 회복). `Capability.MAGIC_LINK = "magic_link"` 4-industry grants: manufacturing ✅ + service ✅ + manufacturing_service ✅ + manufacturing_service_other ✅ (industry-agnostic, CR 12-1 L4 precedent 미러, LOGIN/SIGNUP/AUTH_MIDDLEWARE/FORGOT_PASSWORD/LOGOUT Phase 3-1 + LISTEN_NOTIFY/LISTEN_NOTIFY_TENANT_FANOUT/LISTEN_NOTIFY_MULTIPROCESS Epic 13/14 + DEPLOYMENT_PROD/DEPLOYMENT_STAGING/DEPLOYMENT_DATABASE_BACKUP/DEPLOYMENT_HEALTH_CHECK Phase 4 wire pattern). `Capability.SOCIAL_OAUTH_GOOGLE` + `Capability.SOCIAL_OAUTH_NAVER` + `Capability.SOCIAL_OAUTH_KAKAO` + `Capability.SSO_ENTERPRISE` 동일 industry-agnostic 4-industry grants ✅/✅/✅/✅. 미허용 tenant 의 magic link / social OAuth / SSO enterprise 진입 차단 결정 wire. SSOT RED→GREEN EXTENSION (capability matrix v1.26 신규 5 rows + capability.py EXTENSION 5 NEW enum + `require_capability()` Dependency 5개 신규 wire). CR 11-3 honest-DEFER discipline 58번째 epic 연속 정직 회복 + A36 SDR 검증 4-step 자동 적용 (commit prefix lint PASS + sprint-status structure PASS + vitest file count drift 0건 + commit consistency PASS). 결정 wire 일자: 2026-08-22 |
| **A82** | **Epic 15 wire scope T1~T8 결정** — Epic 15 PRD entry 진입 시점에 결정 (cj-style Epic 15 1번째 진입점 = cj-style 58번째 epic 연속 정직 회복). T1 Magic link wire (apps/web/lib/auth/magic-link.ts Supabase `signInWithOtp` wrapper) + T2 Magic link UI wire (MagicLinkForm.tsx + magic-link/page.tsx + magic-link-sent/page.tsx) + T3 Social OAuth wire (social.ts Supabase `signInWithOAuth` wrapper + SocialAuthButtons.tsx 3 provider buttons) + T4 OAuth callback wire (auth-callback/page.tsx `exchangeCodeForSession`) + T5 SSO SAML backend wire (saml_validator.py + saml_routes.py 4 routes + jit_provisioning.py + alembic 0037 external_identities table + apps/web/app/api/auth/sso/callback/route.ts) + T6 SSO UI wire (sso/[tenant_slug]/login/page.tsx) + T7 capability v1.26 EXTENSION 5 NEW rows + T8 tests + 3중 게이트 FINAL CLEAN atomic commit. estimated ~60 NEW pytest PASS + ~50 NEW vitest PASS + 0 NEW ruff + 0 regressions. wire_commit = TBD (cj-style Epic 15 3번째 진입점 = cj-style 60번째 epic 연속 정직 회복 atomic single sweep T1~T8, expected ~14-16 files = ~12 NEW + ~3-5 MODIFIED). 결정 wire 일자: 2026-08-22 |

### 1st release launch PRD entry 결정 (2026-08-22, cj-style 1st release launch 1번째 진입점 = cj-style 62번째 epic 연속 정직 회복)

| 결정 | 내용 |
|------|------|
| **A83** | **옵션 (d) 1st release launch 진입 결정** — Epic 15 close-out retro `729b223` §12 "Next unblocked 결정 wire 보류" 의 옵션 (a) Epic 16 / 옵션 (b) Phase 5 / 옵션 (c) carry-over / 옵션 (d) 1차 출시 중 **사용자 권장 결정 = 옵션 (d) 1st release launch 진입**. rationale 4종: (1) 모든 인프라 wire DONE = Auth Foundation (Epic 1 + Phase 3) + 2FA (Epic 12) + LISTEN/NOTIFY (Epic 13/14) + Deployment (Phase 4) + 인증 방법 4종 (Magic link + OAuth 3종 + SSO SAML = Epic 15) 모두 wire DONE / (2) D-1-1-DEFER-1/2/3 ✅ RESOLVED = Epic 15 wire 진입 시점에 honest-DEFER discipline 회복 완료 (60번째 epic 연속 정직 회복 검증) / (3) cj-style discipline 회피 위험 방지 = 1-day atomic sprint로 누적된 정직 회복 (49~61번째) 더 미루면 cycle 끊김 위험 / (4) 비즈니스 우선순위 = infrastructure 완성 → 실제 출시 가치 회수. 옵션 (a) Epic 16 / 옵션 (b) Phase 5 / 옵션 (c) carry-over 모두 rejected (rationale: 인프라 cycle 완료 + 출시 가치 회수 우선). Epic 15 territory DONE 정합 보존 (cj-style 58~61번째 epic 연속 정직 회복). 1st release launch PRD entry 진입 시점에 적용 (cj-style 62번째 epic 연속 정직 회복 진입 시점에 결정). ✅ done 진입 2026-08-22. **A84+A85+A86+A87 follow-on**: master PRD v3.2 → v3.3 atomic edit + AD-29 1st release launch 신규 + capability matrix v1.26 → v1.27 EXTENSION 4 NEW rows + 1st release wire scope T1~T8 결정 |
| **A84** | **Master PRD v3.2 → v3.3 atomic edit 결정** — 1st release launch PRD entry 진입 시점에 결정 (cj-style 62번째 epic 연속 정직 회복). master PRD v3.2 → v3.3 atomic edit (docs only, no code/test/sprint-status delta 외 PRD edit 신규). (1) front matter title v3.2 → v3.3 + changelog v3.3 entry 신규 (cj-style 62번째 epic 연속 정직 회복 진입 시점에 결정 verbatim 보존) / (2) §F18 신규 (F18.1 Marketing landing + F18.2 ToS/Privacy + F18.3 Onboarding guide + F18.4 Support channels + F18.5 Production verification + F18.6 Launch comms + F18.7 capability v1.27 EXTENSION 4 NEW rows + F18.8 tests + wire scope T1~T8 결정) / (3) §8.1 M0-(k) 1st release launch 결정 wire 진입 / (4) §15 로드맵 1st release row status 백로그 → in-progress (PRD entry DONE 진입 wire) / (5) §부록 A A83+A84+A85+A86+A87 신규 결정 표 / (6) AD-29 1st release launch 신규 결정 (Marketing landing + ToS/Privacy + Onboarding + Support + Verification + Comms 6 sub-decisions) / (7) capability matrix v1.26 → v1.27 EXTENSION 4 NEW rows industry-agnostic 4-industry grants ✅/✅/✅/✅. 결정 wire 일자: 2026-08-22 |
| **A85** | **AD-29 1st release launch 신규 결정** — 1st release launch PRD entry 진입 시점에 결정 (cj-style 62번째 epic 연속 정직 회복). AD-29 = (a) **Marketing landing page** 결정 wire (`/landing` public route + LandingHero + LandingFeatures + LandingPricing + LandingCTA + ko-KR inline copy EXTENSION, vercel.json public route EXTENSION, (public) route group 신규, 월 1만원 subscription + 14일 무료 체험 결정) / (b) **ToS + Privacy Policy** 결정 wire (`docs/terms-of-service.md` + `docs/privacy-policy.md` 한국 PIPA + GDPR 정합, 8+10 sections, versioned + changelog + effective date, signup flow EXTENSION (auth)/tos + (auth)/privacy 결정) / (c) **Onboarding user guide** 결정 wire (`docs/onboarding-guide.md` 8 sections + OnboardingTooltip + first-run wizard EXTENSION Epic 1 partial scaffold `d182d7d` 정합) / (d) **Customer support channels** 결정 wire (`docs/support.md` + email `support@bizup.kr` + HelpWidget + FAQ `docs/faq.md`) / (e) **Production launch verification** 결정 wire (smoke test RE-RUN 정직 결정 + backup drill 0036 PITR quarterly + Sentry alert wiring production + RPO 4h/RTO 24h SLA verification) / (f) **Public launch communications** 결정 wire (`docs/launch-announcement.md` + press kit + og/assets + in-app announcement banner). 결정 wire 일자: 2026-08-22 |
| **A86** | **Capability matrix v1.26 → v1.27 LAUNCH_LANDING + LAUNCH_TOS + LAUNCH_SUPPORT + LAUNCH_MONITORING 4 NEW rows 결정** — 1st release launch PRD entry 진입 시점에 결정 (cj-style 62번째 epic 연속 정직 회복). `Capability.LAUNCH_LANDING = "launch_landing"` 4-industry grants: manufacturing ✅ + service ✅ + manufacturing_service ✅ + manufacturing_service_other ✅ (industry-agnostic, CR 12-1 L4 precedent 미러, LOGIN/SIGNUP/AUTH_MIDDLEWARE/FORGOT_PASSWORD/LOGOUT Phase 3-1 + LISTEN_NOTIFY 13-1 + LISTEN_NOTIFY_TENANT_FANOUT/LISTEN_NOTIFY_MULTIPROCESS 14-1 + DEPLOYMENT_PROD/DEPLOYMENT_STAGING/DEPLOYMENT_DATABASE_BACKUP/DEPLOYMENT_HEALTH_CHECK Phase 4 + MAGIC_LINK/SOCIAL_OAUTH_GOOGLE/SOCIAL_OAUTH_NAVER/SOCIAL_OAUTH_KAKAO/SSO_ENTERPRISE Epic 15 wire pattern). `Capability.LAUNCH_TOS` + `Capability.LAUNCH_SUPPORT` + `Capability.LAUNCH_MONITORING` 동일 industry-agnostic 4-industry grants ✅/✅/✅/✅. 미허용 tenant 의 1st release launch 진입 차단 결정 wire. SSOT RED→GREEN EXTENSION (capability matrix v1.27 신규 4 rows + capability.py EXTENSION 4 NEW enum + `require_capability()` Dependency 4개 신규 wire). CR 11-3 honest-DEFER discipline 62번째 epic 연속 정직 회복 + A36 SDR 검증 4-step 자동 적용 (commit prefix lint PASS + sprint-status structure PASS + vitest file count drift 0건 + commit consistency PASS). 결정 wire 일자: 2026-08-22 |
| **A87** | **1st release wire scope T1~T8 결정** — 1st release launch PRD entry 진입 시점에 결정 (cj-style 62번째 epic 연속 정직 회복). T1 Landing page wire (apps/web/app/[locale]/(public)/landing/page.tsx + LandingHero.tsx + LandingFeatures.tsx + LandingPricing.tsx + LandingCTA.tsx + ko-KR.json landing.* namespace EXTENSION) + T2 ToS + Privacy wire (docs/terms-of-service.md + docs/privacy-policy.md + (auth)/tos/page.tsx + (auth)/privacy/page.tsx + signup flow EXTENSION) + T3 Onboarding guide wire (docs/onboarding-guide.md + OnboardingTooltip.tsx + (auth)/onboarding/page.tsx) + T4 Support channels wire (docs/support.md + docs/faq.md + docs/launch-announcement.md + HelpWidget.tsx) + T5 Production verification wire (apps/api/scripts/smoke_test.py RE-RUN + sentry-alerts.ts + sentry-alerts.py + docs/database-backup.md 0036 PITR drill quarterly EXTENSION) + T6 Capability v1.27 EXTENSION 4 NEW rows (capability.py + capability-matrix.md) + T7 Tests + 3중 게이트 FINAL CLEAN (~+30 NEW pytest PASS + ~+20 NEW vitest PASS + 5 NEW docs + 1 MODIFIED ko-KR.json) + T8 Launch comms wire (docs/launch-announcement.md + docs/press-kit.md + apps/web/public/og/ assets + (auth)/announcements/page.tsx). estimated ~30 NEW pytest PASS + ~20 NEW vitest PASS + 0 NEW ruff + 0 regressions. wire_commit = TBD (cj-style 1st release 3번째 진입점 = cj-style 64번째 epic 연속 정직 회복 atomic single sweep T1~T8, expected ~15-18 files = ~10 NEW + ~5-8 MODIFIED). 결정 wire 일자: 2026-08-22 |

### Epic 16 PRD entry 결정 (2026-08-22, cj-style Epic 16 1번째 진입점 = cj-style 67번째 epic 연속 정직 회복)

| 결정 | 내용 |
|------|------|
| **A92** | **옵션 (a) Epic 16 진입 결정** — 1st release close-out retro `25dccaf` §12 "Next unblocked 결정 wire 보류" 의 옵션 (a) Epic 16 / 옵션 (b) Phase 5 / 옵션 (c) carry-over / 옵션 (d) 추가 1st release 중 **사용자 권장 결정 = 옵션 (a) Epic 16 진입**. rationale 4종: (1) Epic 15 SSO enterprise SAML forward-reference 'TODO Epic 16' 해결 = docs/sso-enterprise.md §4.1 step 3 'Configure `tenant_idps` (TODO Epic 16)' verbatim — Epic 15 wire 의 natural carry-over / (2) Epic 15 territory carry-over chain (cj-style 58~61→67번째) = tenant IdP admin management 가 natural next territory / (3) cj-style discipline 회피 위험 방지 = 62~66번째 누적 cycle 더 미루면 cycle 끊김 위험 / (4) 비즈니스 우선순위 = 1차 출시 후 enterprise SSO onboarding 필수 (Epic 15 SSO enterprise SAML 은 response validation + JIT provisioning 까지 wire, tenant IdP config admin UI/API 가 Epic 16 territory 결정 wire 진입). 옵션 (b) Phase 5 / 옵션 (c) carry-over / 옵션 (d) 추가 1st release 모두 rejected (rationale: Epic 15 carry-over chain 우선 + 1-day atomic sprint discipline). Epic 15 territory DONE 정합 보존 (cj-style 58~61번째 epic 연속 정직 회복) + 1st release territory DONE 정합 보존 (cj-style 62~66번째 epic 연속 정직 회복). Epic 16 PRD entry 진입 시점에 적용 (cj-style 67번째 epic 연속 정직 회복 진입 시점에 결정). ✅ done 진입 2026-08-22. **A93+A94+A95+A96 follow-on**: master PRD v3.3 → v3.4 atomic edit + AD-30 Tenant IdP admin management 신규 + capability matrix v1.27 → v1.28 EXTENSION TENANT_IDP_MANAGEMENT 1 NEW row + Epic 16 wire scope T1~T8 결정 |
| **A93** | **Master PRD v3.3 → v3.4 atomic edit 결정** — Epic 16 PRD entry 진입 시점에 결정 (cj-style Epic 16 1번째 진입점 = cj-style 67번째 epic 연속 정직 회복). master PRD v3.3 → v3.4 atomic edit (docs only, no code/test/sprint-status delta 외 PRD edit 신규). (1) front matter title v3.3 → v3.4 + changelog v3.4 entry 신규 (cj-style 67번째 epic 연속 정직 회복 진입 시점에 결정 verbatim 보존) / (2) §F19 신규 (F19.1 tenant_idps table + F19.2 IdP metadata validation + F19.3 CRUD API 5 routes + F19.4 admin UI + F19.5 per-tenant routing EXTENSION + F19.6 capability gate + F19.7 tests + wire scope T1~T8 결정) / (3) §8.1 M0-(l) tenant IdP admin AC 신규 / (4) §15 로드맵 Epic 16 row status 백로그 → in-progress (PRD entry DONE 진입 wire) / (5) §부록 A A92+A93+A94+A95+A96 신규 결정 표 / (6) AD-30 Tenant IdP admin management 신규 결정 (tenant_idps architecture + IdP metadata validator + CRUD API + admin UI + per-tenant routing + audit-first INSERT 6 sub-decisions) / (7) capability matrix v1.27 → v1.28 EXTENSION TENANT_IDP_MANAGEMENT 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅. 결정 wire 일자: 2026-08-22 |
| **A94** | **AD-30 Tenant IdP admin management 신규 결정** — Epic 16 PRD entry 진입 시점에 결정 (cj-style Epic 16 1번째 진입점 = cj-style 67번째 epic 연속 정직 회복). AD-30 = (a) **`tenant_idps` table schema** 결정 wire (alembic `0038_epic_16_tenant_idps.py` NEW, 13 columns: id + tenant_id FK + idp_entity_id + idp_sso_url + idp_slo_url + idp_x509_cert + acs_url + name_id_format + enabled + created_at + updated_at + created_by + updated_by + unique constraint `(tenant_id, idp_entity_id)` + RLS policy `tenant_id = current_setting('app.tenant_id')` 결정, CR 0-2 RLS lesson 적용 + AD-22 verbatim 보존) / (b) **IdP metadata XML validation service** 결정 wire (`apps/api/modules/auth/sso/idp_metadata_validator.py` NEW ~120 LOC, Epic 15 `saml_validator.py` sibling module, 8 validation steps: XML well-formedness + EntityDescriptor root + entityID 추출 + IDPSSODescriptor 확인 + X509Certificate PEM wrap + SingleSignOnService Location https:// + SingleLogoutService Location https:// + tenant slug 매칭) / (c) **Tenant IdP CRUD API 5 routes** 결정 wire (`apps/api/modules/auth/sso/idp_admin_routes.py` NEW, FastAPI: `GET / POST / PUT / DELETE / TEST /api/v1/admin/tenant/{tenant_slug}/idp`, owner/admin role required `require_role("owner", "admin")` Dependency + capability gate `TENANT_IDP_MANAGEMENT` + RLS 자동 적용 + audit-first INSERT 4 NEW: `tenant_idp_created` / `tenant_idp_updated` / `tenant_idp_deleted` / `tenant_idp_tested`, CR 1-1 verbatim 적용) / (d) **Tenant IdP admin UI** 결정 wire (`apps/web/app/[locale]/(dashboard)/settings/sso/page.tsx` NEW ~150 LOC + 4 components: TenantIdPConfigForm + TenantIdPStatusBadge + TenantIdPTestResultModal + TenantIdPDeleteConfirmDialog + ko-KR.json `settings.sso.*` namespace EXTENSION 12 keys + (dashboard) 보호 + admin-idp-client.ts fetch wrapper) / (e) **Per-tenant IdP routing EXTENSION** 결정 wire (Epic 15 `apps/api/modules/auth/sso/saml_routes.py` EXTENSION: `tenant_slug` → `tenant_idps` lookup → `idp_sso_url` redirect (HTTP 302) + ACS `idp_x509_cert` 동적 로딩 + backward compatibility `acme` hardcoded tenant 보존 + alembic 0038 데이터 migration `acme` row 자동 seed 결정) / (f) **Audit-first INSERT 4 NEW + multi-tenant isolation** 결정 wire (CR 1-1 verbatim + CR 0-2 RLS lesson 적용, 모든 CRUD API endpoint 에 audit_log INSERT 결정, action_class='AUTH' + action='tenant_idp_*' + actor_id + tenant_id + payload_json 정합). 결정 wire 일자: 2026-08-22 |
| **A95** | **Capability matrix v1.27 → v1.28 TENANT_IDP_MANAGEMENT 1 NEW row 결정** — Epic 16 PRD entry 진입 시점에 결정 (cj-style Epic 16 1번째 진입점 = cj-style 67번째 epic 연속 정직 회복). `Capability.TENANT_IDP_MANAGEMENT = "tenant_idp_management"` 4-industry grants: manufacturing ✅ + service ✅ + manufacturing_service ✅ + manufacturing_service_other ✅ (industry-agnostic, CR 12-1 L4 precedent 미러, SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY 13-1 + LISTEN_NOTIFY_TENANT_FANOUT 14-1 + LISTEN_NOTIFY_MULTIPROCESS 14-1 + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire pattern). 미허용 tenant 의 tenant IdP admin 진입 차단 결정 wire. SSOT RED→GREEN EXTENSION (capability matrix v1.28 신규 1 row + capability.py EXTENSION 1 NEW enum + `require_capability()` Dependency 1개 신규 wire). CR 11-3 honest-DEFER discipline 67번째 epic 연속 정직 회복 + A36 SDR 검증 4-step 자동 적용 (commit prefix lint PASS + sprint-status structure PASS + vitest file count drift 0건 + commit consistency PASS). 결정 wire 일자: 2026-08-22 |
| **A96** | **Epic 16 wire scope T1~T8 결정** — Epic 16 PRD entry 진입 시점에 결정 (cj-style Epic 16 1번째 진입점 = cj-style 67번째 epic 연속 정직 회복). T1 `tenant_idps` table wire (alembic `0038_epic_16_tenant_idps.py` NEW + 13 columns + RLS policy + unique constraint + index + audit trigger) + T2 IdP metadata validator wire (`apps/api/modules/auth/sso/idp_metadata_validator.py` NEW ~120 LOC + 8 validation steps + IdPMetadata TypedDict) + T3 Tenant IdP CRUD API 5 routes wire (`apps/api/modules/auth/sso/idp_admin_routes.py` NEW + owner/admin Dependency + audit-first INSERT 4 NEW) + T4 Tenant IdP admin UI wire (`apps/web/app/[locale]/(dashboard)/settings/sso/page.tsx` NEW + 4 components + ko-KR.json `settings.sso.*` namespace EXTENSION 12 keys + `apps/web/lib/auth/admin-idp-client.ts` NEW) + T5 Per-tenant IdP routing EXTENSION wire (Epic 15 `saml_routes.py` MODIFIED + ACS `idp_x509_cert` 동적 로딩 + alembic 0038 `acme` 데이터 migration) + T6 Capability v1.28 EXTENSION 1 NEW row (capability.py MODIFIED + docs/capability-matrix.md v1.27 → v1.28) + T7 Tests + 3중 게이트 FINAL CLEAN (~+25 NEW pytest PASS + ~+5 NEW vitest PASS + 1 NEW integration drift + 4 NEW audit log verification) + T8 3중 게이트 FINAL CLEAN atomic commit. estimated ~25 NEW pytest PASS + ~5 NEW vitest PASS + 0 NEW ruff + 0 regressions. wire_commit = TBD (cj-style Epic 16 3번째 진입점 = cj-style 69번째 epic 연속 정직 회복 atomic single sweep T1~T8, expected ~10-12 files = ~9 NEW + ~2-3 MODIFIED). 결정 wire 일자: 2026-08-22 |

### Epic 16 close-out retro PRD entry 결정 (cj-style Epic 16 5번째 진입점 = cj-style 71번째 epic 연속 정직 회복) — Epic 16 T4 admin UI follow-up 후속 결정 wire 진입 — §F19.4 AC satisfied = T4 follow-up sprint `ff5c3b5` (cj-style 71번째) 프론트엔드 12 파일 atomic wire DONE 진입 시점에 Epic 16 territory 결정 wire 완료. (참고: A97~A100 Epic 16 spec entry 결정 + A101~A108 Epic 16 atomic wire 결정 + A109~A113 review follow-up 결정 + A114~A118 T4 follow-up 결정 + A119~A123 close-out retro 결정 모두 윗줄 결정 wire 진입 시점에 결정 완료, Epic 16 6-entry-point pattern 모두 wire DONE 진입 67~71번째.)

### Phase 5 PRD entry 결정 (2026-08-22, cj-style Phase 5 1번째 진입점 = cj-style 73번째 epic 연속 정직 회복)

| 결정 | 내용 |
|------|------|
| **A124** | **옵션 (a) Phase 5 진입 결정** — Epic 16 close-out retro `f1ead9a` §13 "Next unblocked 결정 wire 보류" 의 옵션 (a) Phase 5 / 옵션 (b) Epic 17 / 옵션 (c) carry-over / 옵션 (d) 1차 출시 follow-up / 옵션 (e) D-EPIC-16-REVIEW-DEFER-2~6 해소 중 **사용자 권장 결정 = 옵션 (a) Phase 5 진입**. rationale 4종: (1) **Phase 4 close-out retro §6 disaster recovery honestly-deferred 해소** = docs/database-backup.md §7 "multi-region backup 결정 wire 보류, Phase 5+ 진입 시점" verbatim — Phase 4 wire 진입 시점에 honestly-deferred carry-over territory 의 natural next wire 결정 / (2) **cj-style discipline 회피 위험 방지** = 49~72번째 누적 24-entry-point cycle 모두 wire DONE 진입 + A119+A120+A121+A122+A123 5/5 ALL DONE 결정 wire 후 next territory 진입 결정 = honest cycle 진행 정직 회복 / (3) **비즈니스 우선순위 + enterprise SLA 정합** = 1차 출시 후 enterprise 고객 유치 시 RPO 1h/RTO 4h SLA 요구 (Phase 4 single-region RPO 5min/RTO 1h 의 honest-extreme risk = Seoul region disaster 시 backup restoration 불가 verbatim 해소 결정 wire) / (4) **Phase 4 단일-region EXTENSION** = Supabase Seoul primary + PITR 7일 자동 의 multi-region EXTENSION (primary Seoul + secondary Tokyo + cross-region backup) 자연스러운 인프라 확장 결정 wire. 옵션 (b) Epic 17 / 옵션 (c) carry-over / 옵션 (d) 1차 출시 follow-up / 옵션 (e) D-EPIC-16-REVIEW-DEFER-2~6 해소 모두 rejected (rationale: Phase 4 honestly-deferred 해소 우선 + 다중 entry-point cycle discipline + enterprise SLA 정합). Epic 16 territory DONE 정합 보존 (cj-style 67~72번째 epic 연속 정직 회복) + 1st release territory DONE 정합 보존 (cj-style 62~66번째 epic 연속 정직 회복) + Epic 15 territory DONE 정합 보존 (cj-style 58~61번째 epic 연속 정직 회복) + Phase 4 territory DONE 정합 보존 (cj-style 53~57번째 epic 연속 정직 회복) + Phase 3 territory DONE 정합 보존 (cj-style 49~52번째 epic 연속 정직 회복) 결정 wire 모두 보존. Phase 5 PRD entry 진입 시점에 적용 (cj-style 73번째 epic 연속 정직 회복 진입 시점에 결정). ✅ done 진입 2026-08-22. **A125+A126+A127+A128 follow-on**: master PRD v3.4 → v3.5 atomic edit + AD-31 Multi-Region Backup & Disaster Recovery 신규 + capability matrix v1.28 → v1.29 EXTENSION MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER 2 NEW rows + Phase 5 wire scope T1~T8 결정 |
| **A125** | **Master PRD v3.4 → v3.5 atomic edit 결정** — Phase 5 PRD entry 진입 시점에 결정 (cj-style Phase 5 1번째 진입점 = cj-style 73번째 epic 연속 정직 회복). master PRD v3.4 → v3.5 atomic edit (docs only, no code/test/sprint-status delta 외 PRD edit 신규). (1) front matter title v3.4 → v3.5 + changelog v3.5 entry 신규 (cj-style 73번째 epic 연속 정직 회복 진입 시점에 결정 verbatim 보존) / (2) §F20 신규 (F20.1 Cross-region read replica + WAL archiving + F20.2 Cross-region failover automation + F20.3 DR drill + automated quarterly test + F20.4 Cross-region backup strategy + F20.5 Multi-region health observability + F20.6 capability matrix v1.29 EXTENSION 2 NEW rows + F20.7 tests + wire scope T1~T8 결정) / (3) §8.1 M0-(m) multi-region backup AC 신규 / (4) §15 로드맵 Phase 5 row status 백로그 → in-progress (PRD entry DONE 진입 wire) / (5) §부록 A A124+A125+A126+A127+A128 신규 결정 표 / (6) AD-31 Multi-Region Backup & Disaster Recovery 신규 결정 (cross-region architecture 6 sub-decisions) / (7) capability matrix v1.28 → v1.29 EXTENSION MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER 2 NEW rows industry-agnostic 4-industry grants ✅/✅/✅/✅. 결정 wire 일자: 2026-08-22 |
| **A126** | **AD-31 Multi-Region Backup & Disaster Recovery 신규 결정** — Phase 5 PRD entry 진입 시점에 결정 (cj-style Phase 5 1번째 진입점 = cj-style 73번째 epic 연속 정직 회복). AD-31 = (a) **Cross-region read replica + WAL archiving** 결정 wire = alembic `0039_phase_5_multi_region_backup.py` NEW `phase_5_replication_lag` table (BIGSERIAL id + replica_region TEXT enum seoul/tokyo/singapore/frankfurt/virginia/oregon/sao_paulo + primary_region TEXT enum + lag_bytes BIGINT + lag_seconds INTEGER + last_synced_lsn TEXT PG_LSN + last_synced_at TIMESTAMPTZ + replication_status TEXT enum syncing/replicating/lagged/disconnected/failed + created_at TIMESTAMPTZ DEFAULT NOW()) + 3 indexes (status + region+status+last_synced_at DESC + created_at) + 2 CHECK constraints (replication_status enum + replica_region enum) + audit-first INSERT `replica_status_changed` (CR 1-1 verbatim, action_class='INFRA' + action='replica_status_changed' + actor_id + region + previous_status + new_status + trace_id) 결정 wire 결정. WAL archiving 결정 wire = `postgresql.conf` `archive_mode = on` + `archive_command = 'pgbackrest --stanza=costmgr archive-push %p'` + `wal_level = replica` 결정 wire 보류 (Supabase managed 결정 wire). `docs/cross-region-replication.md` 결정 wire (cross-region replication setup + replica region 선택 Tokyo 1st choice latency Seoul-Tokyo ~50ms 결정 + replication lag monitoring lag_bytes threshold 100MB + lag_seconds threshold 30s alert 결정 + WAL archiving setup + Supabase pgbackrest 결정 wire 보류). / (b) **Cross-region failover automation** 결정 wire = `apps/api/jobs/failover_orchestrator.py` NEW ~200 LOC (primary → secondary health probe 5-second interval + 3 consecutive failures trigger 결정 + automatic promotion 결정: secondary region 의 PostgreSQL promote decision via Supabase API `POST /v1/projects/{ref}/database/promote` 결정 wire 보류 + read-only mode 해제 + connection pool redirect 결정) + DNS update 결정 wire (failover 결정 wire 진입 시점에 Supabase project URL 의 custom domain redirect 결정 wire + Supabase custom domain 결정 wire 보류) + RTO 30-second target 결정 wire + failover trigger 3종 결정 (a health probe 3 consecutive failures OR (b manual trigger via `POST /api/v1/admin/failover` owner-only AD-22 RBAC + 2FA 챌린지 Epic 12 정합 OR (c scheduled drill via `apps/api/jobs/dr_drill.py` cron 결정) + audit-first INSERT `failover_initiated` + `failover_completed` (CR 1-1 verbatim, action_class='INFRA' + action='failover_initiated' + actor_id + from_region + to_region + trace_id) + FastAPI lifespan hook startup/shutdown 결정 wire + GRACEFUL_SHUTDOWN_TIMEOUT=30s 결정 wire (in-flight requests 30s 대기 결정). / (c) **DR drill + automated quarterly test** 결정 wire = `apps/api/jobs/dr_drill.py` NEW ~150 LOC (cron KST 1st Sunday 03:00 = UTC 18:00 결정 wire + actual failover drill test in staging 결정 wire production 환경 직접 failover 위험 회피 결정 + 6 drill steps: staging primary health check + staging secondary promote trigger + staging database connection write test + staging application health check + staging DNS update test + staging primary restore trigger + RPO/RTO measurement decision drill 시작 시점 → drill 완료 시점 시간 측정 = RTO actual + drill 시작 전 마지막 transaction LSN → drill 후 secondary LSN 측정 = RPO actual + 결과 결정 wire `phase_5_dr_drill_results` table 신규 (BIGSERIAL id + drill_date DATE + rto_actual_seconds INTEGER + rpo_actual_bytes BIGINT + status TEXT enum pass/fail + notes TEXT + created_at TIMESTAMPTZ) + Q1/Q2/Q3/Q4 quarterly drill schedule 결정 wire (January + April + July + October 결정, docs/database-backup.md §9 quarterly drill pattern verbatim preserve) + audit-first INSERT `dr_drill_completed` (CR 1-1 verbatim, action_class='INFRA' + action='dr_drill_completed' + actor_id='system' + rto_actual_seconds + rpo_actual_bytes + status 결정) 결정. / (d) **Cross-region backup strategy** 결정 wire = `docs/database-backup.md` EXTENSION 10 sections → 12 sections (purpose + PITR strategy + RPO/RTO + restore procedure + disaster recovery + monitoring + retention + quarterly drill testing + cross-region backup strategy 신규 + cross-region failover runbook 신규 + RPO 1h / RTO 4h SLA 결정: Phase 4 single-region RPO 5min/RTO 1h 의 honest-extreme risk = Seoul region disaster 시 backup restoration 불가 verbatim 해소 결정 wire) + Cross-region backup vs single-region 결정 wire (Phase 4 single-region (Supabase Seoul primary + PITR 7일 자동) 의 honest-extreme risk 의 multi-region 해소 결정) + 30일 hot (primary) + 90일 cold (secondary) + 365일 archive (regional) retention decision. / (e) **Multi-region health observability** 결정 wire = `apps/api/core/health.py` EXTENSION `/api/v1/health/multi-region` endpoint 결정 wire (primary + secondary status array 결정 + CR 12-5 D-14 envelope `{status, primary: {region, status, lag_bytes, lag_seconds, last_synced_at}, secondary: {region, status, lag_bytes, lag_seconds, last_synced_at}, timestamp}` 결정 + JWT verification probe 결정 Supabase Auth health probe per-region 결정) + `apps/api/core/observability.py` EXTENSION Sentry breadcrumb failover_initiated 시 Sentry breadcrumb + alert decision (`sentry_sdk.capture_message(f"Failover initiated from {from_region} to {to_region}", level="warning")` + Sentry alert routing decision) + Grafana multi-region dashboard EXTENSION decision (primary + secondary region metrics + replication lag graph decision + failover event log decision) + `apps/web/app/api/health/multi-region/route.ts` NEW decision wire (~+30 LOC, atomic, Next.js Edge Runtime + force-dynamic + Vercel region decision + NextResponse.json envelope decision `{status, primary, secondary, build, region, timestamp}` decision wire). / (f) **Capability matrix v1.29 EXTENSION + 2 NEW rows** 결정 wire = `Capability.MULTI_REGION_BACKUP = "multi_region_backup"` 1 NEW row + `Capability.MULTI_REGION_FAILOVER = "multi_region_failover"` 1 NEW row (manufacturing ✅ + service ✅ + manufacturing_service ✅ + manufacturing_service_other ✅ industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러, DEPLOYMENT_PROD + DEPLOYMENT_DATABASE_BACKUP Phase 4 wire + SSO_ENTERPRISE Epic 15 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + LAUNCH_* 1st release wire + LISTEN_NOTIFY 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire pattern verbatim bind) + SSOT RED→GREEN EXTENSION (capability matrix v1.29 신규 2 rows + capability.py EXTENSION 2 NEW enum + `require_capability()` Dependency 2개 신규 wire) + drift detector `tests/integration/test_capability_matrix_v1_29_drift.py` NEW 결정 (Epic 16 wire 의 `tests/integration/test_capability_matrix_v1_28_drift.py` + Phase 4 wire 의 `tests/integration/test_capability_matrix_v1_25_drift.py` 패턴 verbatim). capability matrix v1.29 신규 2 rows (`MULTI_REGION_BACKUP` + `MULTI_REGION_FAILOVER`, industry-agnostic 4-industry grants ✅/✅/✅/✅) [§F20 verbatim, §8.1 M0-(m) EXTENSION 결정]. 결정 wire 일자: 2026-08-22 |
| **A127** | **Capability matrix v1.28 → v1.29 MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER 2 NEW rows 결정** — Phase 5 PRD entry 진입 시점에 결정 (cj-style Phase 5 1번째 진입점 = cj-style 73번째 epic 연속 정직 회복). `Capability.MULTI_REGION_BACKUP = "multi_region_backup"` 4-industry grants: manufacturing ✅ + service ✅ + manufacturing_service ✅ + manufacturing_service_other ✅ (industry-agnostic, CR 12-1 L4 precedent 미러, DEPLOYMENT_PROD + DEPLOYMENT_DATABASE_BACKUP Phase 4 wire pattern + SSO_ENTERPRISE Epic 15 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + LAUNCH_* 1st release wire pattern verbatim bind). `Capability.MULTI_REGION_FAILOVER` 동일 industry-agnostic 4-industry grants ✅/✅/✅/✅. 미허용 tenant 의 multi-region 진입 차단 결정 wire. SSOT RED→GREEN EXTENSION (capability matrix v1.29 신규 2 rows + capability.py EXTENSION 2 NEW enum + `require_capability()` Dependency 2개 신규 wire + drift detector `tests/integration/test_capability_matrix_v1_29_drift.py` NEW). CR 11-3 honest-DEFER discipline 73번째 epic 연속 정직 회복 (D-PHASE-4-DR-DEFER-1/2 ✅ RESOLVE 진입 wire 결정) + A36 SDR 검증 4-step 자동 적용 (commit prefix lint PASS + sprint-status structure PASS + vitest file count drift 0건 + commit consistency PASS). 결정 wire 일자: 2026-08-22 |
| **A128** | **Phase 5 wire scope T1~T8 결정** — Phase 5 PRD entry 진입 시점에 결정 (cj-style Phase 5 1번째 진입점 = cj-style 73번째 epic 연속 정직 회복). T1 alembic `0039_phase_5_multi_region_backup.py` NEW (~+120 LOC, phase_5_replication_lag table 결정 + phase_5_dr_drill_results table 결정 + replica_region enums + primary_region enum + replication_status enum 결정 + audit-first INSERT `replica_status_changed` CR 1-1 verbatim 결정 + 3 indexes + 2 CHECK constraints 결정) + T2 `apps/api/jobs/failover_orchestrator.py` NEW (~+200 LOC, primary → secondary health probe + automatic promotion + DNS update via Supabase API 결정 wire + RTO 30s target 결정 + audit-first INSERT `failover_initiated` + `failover_completed` CR 1-1 verbatim 결정 + FastAPI lifespan hook + GRACEFUL_SHUTDOWN_TIMEOUT=30s 결정 + owner-only manual trigger `POST /api/v1/admin/failover` AD-22 RBAC 결정) + T3 `apps/api/jobs/dr_drill.py` NEW (~+150 LOC, cron KST 1st Sunday 03:00 UTC 18:00 결정 + actual failover drill test in staging 결정 + 6 drill steps + RPO/RTO measurement + `phase_5_dr_drill_results` table 결정 + Q1/Q2/Q3/Q4 quarterly schedule 결정 + audit-first INSERT `dr_drill_completed` CR 1-1 verbatim 결정) + T4 cross-region backup strategy wire (`docs/database-backup.md` EXTENSION 10 sections → 12 sections + cross-region PITR primary Seoul + secondary Tokyo + 30일 hot + 90일 cold + 365일 archive regional + RPO 1h / RTO 4h SLA 결정 + `docs/cross-region-replication.md` 결정 wire) + T5 multi-region health observability wire (`apps/api/core/health.py` EXTENSION `/api/v1/health/multi-region` endpoint 결정 + CR 12-5 D-14 envelope 결정 + `apps/api/core/observability.py` EXTENSION Sentry breadcrumb failover 결정 + Grafana multi-region dashboard EXTENSION 결정 + `apps/web/app/api/health/multi-region/route.ts` NEW 결정) + T6 Capability v1.29 EXTENSION 2 NEW rows (capability.py MODIFIED + `tests/integration/test_capability_matrix_v1_29_drift.py` NEW decision + docs/capability-matrix.md v1.28 → v1.29) + T7 Tests + 3중 게이트 FINAL CLEAN (~+50 NEW pytest PASS + ~+10 NEW vitest PASS + 1 NEW integration drift decision + 4 NEW audit log verification) + T8 3중 게이트 FINAL CLEAN atomic commit decision. estimated ~50 NEW pytest PASS + ~10 NEW vitest PASS + 0 NEW ruff + 0 regressions. wire_commit = TBD (cj-style Phase 5 3번째 진입점 = cj-style 75번째 epic 연속 정직 회복 atomic single sweep T1~T8, expected ~12-15 files = ~10 NEW + ~2-5 MODIFIED). 결정 wire 일자: 2026-08-22 |

### Epic 17 PRD entry 결정 (2026-08-22, cj-style Epic 17 1번째 진입점 = cj-style 80번째 epic 연속 정직 회복)

| 결정 | 내용 |
|------|------|
| **A153** | **옵션 (a) Epic 17 진입 결정** — Sidebar/MenuProvider hot-fix `01a06e4` (cj-style 79번째 wire entry) + D-EPIC-16-REVIEW-DEFER-2~6 RESOLVE sprint `512ed6a` (cj-style 78번째) + Phase 5 close-out retro `b843565` (cj-style 76~77번째) wire DONE 진입 직후 next 옵션 5종 중 **사용자 권장 결정 = 옵션 (a) Epic 17 진입**. rationale 4종: (1) **Epic 1 ~ Epic 16 + Phase 3 + Phase 4 + Phase 5 모두 wire DONE (49~79번째 cumulative cycle)** → 모든 territory 의 audit-first INSERT (CR 1-1) 가 audit_log table 에 누적 → audit log viewer territory 자연스러운 next 진입 / (2) **cj-style discipline 회피 위험 방지** = 49~79번째 누적 31-entry-point cycle + 78~79번째 hot-fix + RESOLVE sprint 직후 즉시 Epic 17 진입 = 1-day atomic sprint discipline / (3) **비즈니스 우선순위 = enterprise 고객 onboarding 시 audit log viewer 필수** (PIPA + GDPR + SOX compliance = audit log 가시성 + filter + export 기능 요구) / (4) **Phase 5 multi-region wire 의 cross-region audit log visibility 자연스러운 carry-over** (audit_log table 은 cross-region primary 에 write → multi-region read replica 통한 cross-region audit visibility 제공). 옵션 (b) carry-over / 옵션 (c) 1st release 추가 follow-up / 옵션 (d) Phase 6 / 옵션 (e) D-PHASE-4-DR-DEFER follow-up 모두 rejected (rationale: Phase 5 territory DONE 정합 + audit log viewer territory 의 natural next 진입 = business priority + cross-region carry-over). Phase 5 territory DONE 정합 보존 (cj-style 73~77번째 epic 연속 정직 회복) + Epic 16 territory DONE 정합 보존 (cj-style 67~72번째 epic 연속 정직 회복) + 1st release territory DONE 정합 보존 (cj-style 62~66번째 epic 연속 정직 회복) + Epic 15 territory DONE 정합 보존 (cj-style 58~61번째 epic 연속 정직 회복) + Phase 4 territory DONE 정합 보존 (cj-style 53~57번째 epic 연속 정직 회복) + Phase 3 territory DONE 정합 보존 (cj-style 49~52번째 epic 연속 정직 회복) 결정 wire 모두 보존. Epic 17 PRD entry 진입 시점에 적용 (cj-style 80번째 epic 연속 정직 회복 진입 시점에 결정). ✅ done 진입 2026-08-22. **A154+A155+A156+A157 follow-on**: master PRD v3.5 → v3.6 atomic edit + AD-32 Audit Log Viewer & Activity Stream 신규 + capability matrix v1.29 → v1.30 EXTENSION AUDIT_LOG_VIEW 1 NEW row + Epic 17 wire scope T1~T8 결정 |
| **A154** | **Master PRD v3.5 → v3.6 atomic edit 결정** — Epic 17 PRD entry 진입 시점에 결정 (cj-style Epic 17 1번째 진입점 = cj-style 80번째 epic 연속 정직 회복). master PRD v3.5 → v3.6 atomic edit (docs only, no code/test/sprint-status delta 외 PRD edit 신규). (1) front matter title v3.5 → v3.6 + changelog v3.6 entry 신규 (cj-style 80번째 epic 연속 정직 회복 진입 시점에 결정 verbatim 보존) / (2) §F21 신규 (F21.1 audit log query API + F21.2 audit log viewer UI + F21.3 activity stream UI + F21.4 cross-region audit log visibility + F21.5 CSV export + F21.6 capability gate AUDIT_LOG_VIEW + F21.7 tests + wire scope T1~T8 결정) / (3) §8.1 M0-(n) audit log viewer AC 신규 / (4) §15 로드맵 Epic 17 row status 백로그 → in-progress (PRD entry DONE 진입 wire) / (5) §부록 A A153+A154+A155+A156+A157 신규 결정 표 / (6) AD-32 Audit Log Viewer & Activity Stream 신규 결정 (audit log viewer architecture 7 sub-decisions) / (7) capability matrix v1.29 → v1.30 EXTENSION AUDIT_LOG_VIEW 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅. 결정 wire 일자: 2026-08-22 |
| **A155** | **AD-32 Audit Log Viewer & Activity Stream 신규 결정** — Epic 17 PRD entry 진입 시점에 결정 (cj-style Epic 17 1번째 진입점 = cj-style 80번째 epic 연속 정직 회복). AD-32 = (a) **audit log query API** 결정 wire = `apps/api/modules/audit/audit_log_query.py` NEW ~180 LOC (4 functions: `query_audit_log(tenant_id, filters, page, page_size) -> AuditLogPage` + `count_audit_log(tenant_id, filters) -> int` + `get_audit_log_entry(tenant_id, entry_id) -> AuditLogEntry` + `query_activity_stream(tenant_id, window_days) -> list[ActivityStreamGroup]` 결정 + AuditLogQueryFilters TypedDict `{actor_id, action_class, action, period_key, payload_search, start_date, end_date}` 결정 + AuditLogEntry TypedDict `{id, tenant_id, actor_id, actor_email, action_class, action, payload_json, created_at, trace_id}` 결정 + AuditLogPage TypedDict `{entries, total_count, page, page_size, has_next}` 결정 + ActivityStreamGroup TypedDict `{date, entries, unique_actors}` 결정 + RLS `tenant_id = (SELECT current_setting('app.tenant_id'))::uuid` 자동 적용 (CR 0-2 RLS lesson 적용, AD-22 verbatim 보존) + owner/admin role required `require_role("owner", "admin")` FastAPI Dependency (Epic 12 2FA 게이트 보존) + capability gate `AUDIT_LOG_VIEW` 자동 적용 + pagination offset+limit (page_size=50 default, max 200) + sort `ORDER BY created_at DESC` 결정 + performance `audit_log` table 의 `(tenant_id, created_at DESC)` index 활용 + `payload_json` 의 GIN index (jsonb_path_ops) 결정 wire. / (b) **audit log viewer UI** 결정 wire = `apps/web/app/[locale]/(dashboard)/audit-log/page.tsx` NEW ~200 LOC + 5 components (`AuditLogFilterPanel` (filter form: actor email autocomplete + action_class dropdown + action text search + period_key + date range picker + payload_search input) + `AuditLogTable` (table view: created_at + actor_email + action_class chip + action text + payload summary + trace_id expandable) + `AuditLogPagination` (prev/next + page size selector + total count display) + `AuditLogExportButton` (CSV export trigger, calls F21.5 backend) + `AuditLogDetailModal` (full payload_json + trace_id expansion modal)) + ko-KR.json `audit_log.*` namespace EXTENSION 14 keys 결정 (CR 11-4 D-002 verbatim SSOT) + `(dashboard)` route group 보호 (Phase 3-1 T4 wire 정합) + `apps/web/lib/audit/audit-log-client.ts` NEW (fetch wrapper + auth cookie 자동 첨부 + typed error envelope) + vitest RTL render discipline 결정 (CR 11-4 D-003 verbatim). / (c) **activity stream UI** 결정 wire = `apps/web/app/[locale]/(dashboard)/activity/page.tsx` NEW ~150 LOC + 3 components (`ActivityStreamTimeline` (grouped by date: today / yesterday / N days ago) + `ActivityStreamEntry` (avatar + action_class icon + action text + actor name + relative time) + `ActivityStreamWindowSelector` (7일 / 30일 / 90일 window selector)) + ko-KR.json `activity.*` namespace EXTENSION 8 keys 결정 (title + description + window_7d + window_30d + window_90d + empty_message + today_label + yesterday_label) + all tenant members 권한 (`require_role("owner", "admin", "member", "viewer")`) + activity stream 은 tenant-wide 가시성 (owner/admin audit log viewer 와 분리). / (d) **cross-region audit log visibility** 결정 wire = Phase 5 multi-region wire `f093f8c` 의 `apps/api/alembic/versions/0039_phase_5_multi_region_backup.py` `phase_5_replication_lag` table + Supabase multi-region primary Seoul + secondary Tokyo replica 결정 wire EXTENSION (audit log query 시 secondary region 의 read replica 에서 query 가능 결정 wire + multi-region read replica 통한 cross-region audit visibility) + `/api/v1/audit-log` handler 에서 connection pool 의 read-only routing 결정 wire + 읽기 일관성 결정 wire (secondary region 의 replication lag 이 lag_bytes ≤ 100MB + lag_seconds ≤ 30s 시 read consistent (Phase 5 wire 의 lag threshold 정합) — lag 초과 시 primary region 으로 fallback + Sentry breadcrumb 결정 wire). / (e) **CSV export** 결정 wire = `apps/api/modules/audit/audit_log_export.py` NEW ~120 LOC + `export_audit_log_csv(tenant_id, filters, actor_id) -> StreamingResponse` 결정 (Excel-compatible UTF-8 BOM `﻿` + comma-separated + double-quote escape for payload_json) + columns `entry_id,created_at,actor_email,action_class,action,period_key,payload_json,trace_id` 결정 + streaming response `StreamingResponse(media_type='text/csv; charset=utf-8', headers={'Content-Disposition': f'attachment; filename="audit-log-{tenant_slug}-{yyyymmdd}.csv"'})` 결정 (large dataset 시 memory efficient) + audit-first INSERT `audit_log_exported` (CR 1-1 verbatim, action_class='AUDIT' + action='audit_log_exported' + actor_id + tenant_id + filters_json + row_count + trace_id) — 누가 언제 어떤 filter 로 export 했는지 추적 + CR 12-5 D-14 error envelope `{code, message_ko, details, trace_id}` 결정 (e.g. `AUDIT_LOG_EXPORT_FORBIDDEN_KO`, `AUDIT_LOG_EXPORT_TOO_LARGE_KO`). / (f) **audit-first INSERT 1 NEW + RLS 자동 적용** 결정 wire (CR 1-1 verbatim + CR 0-2 RLS lesson 적용, F21.5 `audit_log_exported` 1 NEW audit log entry 결정, action_class='AUDIT' + action='audit_log_exported' + actor_id + tenant_id + filters_json + row_count + trace_id + per-tenant RLS 자동 적용, multi-tenant isolation test 결정). / (g) **Capability matrix v1.30 EXTENSION + 1 NEW row** 결정 wire = `Capability.AUDIT_LOG_VIEW = "audit_log_view"` 신규 1 row (manufacturing ✅ + service ✅ + manufacturing_service ✅ + manufacturing_service_other ✅ industry-agnostic 4-industry grants, CR 12-1 L4 precedent 미러, MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind) + 미허용 tenant 의 audit log 진입 차단 결정 wire + SSOT RED→GREEN EXTENSION (capability matrix v1.30 신규 1 row + capability.py EXTENSION 1 NEW enum + `require_capability()` Dependency 1개 신규 wire) + drift detector `tests/integration/test_capability_matrix_v1_30_drift.py` NEW 결정 (Phase 5 wire 의 `tests/integration/test_capability_matrix_v1_29_drift.py` + Epic 16 wire 의 `tests/integration/test_capability_matrix_v1_28_drift.py` 패턴 verbatim). capability matrix v1.30 신규 1 row (`AUDIT_LOG_VIEW`, industry-agnostic 4-industry grants ✅/✅/✅/✅) [§F21 verbatim, §8.1 M0-(n) EXTENSION 결정]. 결정 wire 일자: 2026-08-22 |
| **A156** | **Capability matrix v1.29 → v1.30 AUDIT_LOG_VIEW 1 NEW row 결정** — Epic 17 PRD entry 진입 시점에 결정 (cj-style Epic 17 1번째 진입점 = cj-style 80번째 epic 연속 정직 회복). `Capability.AUDIT_LOG_VIEW = "audit_log_view"` 4-industry grants: manufacturing ✅ + service ✅ + manufacturing_service ✅ + manufacturing_service_other ✅ (industry-agnostic, CR 12-1 L4 precedent 미러, MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind). 미허용 tenant 의 audit log viewer 진입 차단 결정 wire. SSOT RED→GREEN EXTENSION (capability matrix v1.30 신규 1 row + capability.py EXTENSION 1 NEW enum + `require_capability()` Dependency 1개 신규 wire + drift detector `tests/integration/test_capability_matrix_v1_30_drift.py` NEW). CR 11-3 honest-DEFER discipline 80번째 epic 연속 정직 회복 (D-1-1-DEFER-1/2/3 RESOLVED 보존 + D-LAUNCH-1-DEFER-1 honestly preserved + D-EPIC-16-REVIEW-DEFER-2~6 ✅ RESOLVED 보존 + D-PHASE-4-DR-DEFER-1/2 ✅ RESOLVED 보존) + A36 SDR 검증 4-step 자동 적용 (commit prefix lint PASS + sprint-status structure PASS + vitest file count drift 0건 + commit consistency PASS). 결정 wire 일자: 2026-08-22 |
| **A157** | **Epic 17 wire scope T1~T8 결정** — Epic 17 PRD entry 진입 시점에 결정 (cj-style Epic 17 1번째 진입점 = cj-style 80번째 epic 연속 정직 회복). T1 `apps/api/modules/audit/audit_log_query.py` NEW (~+180 LOC, 4 functions: query_audit_log + count_audit_log + get_audit_log_entry + query_activity_stream + 4 TypedDict 결정 + RLS 자동 적용 + capability gate AUDIT_LOG_VIEW + owner/admin role required + pagination + sort) + T2 `apps/api/modules/audit/audit_log_export.py` NEW (~+120 LOC, streaming response + UTF-8 BOM + audit-first INSERT `audit_log_exported` CR 1-1 verbatim + CR 12-5 D-14 envelope) + T3 `apps/web/app/[locale]/(dashboard)/audit-log/page.tsx` NEW (~+200 LOC + 5 components 결정 + ko-KR.json `audit_log.*` namespace EXTENSION 14 keys + (dashboard) 보호) + T4 `apps/web/app/[locale]/(dashboard)/activity/page.tsx` NEW (~+150 LOC + 3 components 결정 + ko-KR.json `activity.*` namespace EXTENSION 8 keys + all tenant members 권한) + T5 ko-KR.json SSOT EXTENSION wire (`audit_log.*` 14 keys + `activity.*` 8 keys 결정 wire, CR 11-4 D-002 verbatim 적용) + T6 Capability v1.30 EXTENSION 1 NEW row (capability.py MODIFIED + `tests/integration/test_capability_matrix_v1_30_drift.py` NEW + docs/capability-matrix.md v1.29 → v1.30) + T7 Tests + 3중 게이트 FINAL CLEAN (~+50 NEW pytest PASS + ~+15 NEW vitest PASS + 1 NEW integration drift + audit_first INSERT verification + F21.4 cross-region lag threshold test + F21.5 CSV export test) + T8 3중 게이트 FINAL CLEAN atomic commit decision. estimated ~50 NEW pytest PASS + ~15 NEW vitest PASS + 0 NEW ruff + 0 regressions. wire_commit = TBD (cj-style Epic 17 3번째 진입점 = cj-style 82번째 epic 연속 정직 회복 atomic single sweep T1~T8, expected ~14-18 files = ~12 NEW + ~3-6 MODIFIED). 결정 wire 일자: 2026-08-22 |
| **A223** | **옵션 (a) Phase 7+ 진입 결정** — Phase 6 close-out retro `f9f006c` (cj-style 88번째 wire entry) + Phase 6 atomic wire T1~T8 `24e1cd7` (cj-style 87번째) + Phase 6 spec entry `f5c14c9` (cj-style 86번째) + Phase 6 PRD entry `e84a281` (cj-style 85번째) 결정 wire 모두 DONE 진입 정합 보존 후 다음 옵션 5종 (옵션 (a) Phase 7+ 진입 / 옵션 (b) Epic 18+ / 옵션 (c) carry-over / 옵션 (d) 1st release 추가 follow-up / 옵션 (e) D-DEFER-* carry-over follow-up) 중 **사용자 권장 결정 = 옵션 (a) Phase 7+ 진입** (AskUserQuestion 결정 verbatim bind). 옵션 (a) Phase 7+ 진입 후 옵션 5종 territory 후보 (옵션 (a) Observability stack 강화 (Recommended) / 옵션 (b) ABAC 정밀화 / 옵션 (c) Advanced analytics / 옵션 (d) Notification system / 옵션 (e) Phase 8 carry-over) 중 **사용자 권장 결정 = 옵션 (a) Observability stack 강화 = Phase 7 territory 결정 wire 진입**. rationale 5종: (1) **Phase 5 wire `f093f8c` 의 multi-region observability (§F20.5 multi-region health observability) 의 자연스러운 carry-over chain** = OpenTelemetry distributed tracing (cross-region primary + secondary read replica 의 request flow 가시화) + Prometheus custom metrics (multi-region Sentry breadcrumb + Grafana dashboard EXTENSION) territory 가 §F20.5 의 자연스러운 EXTENSION territory (Phase 7 = §F23 신규 territory) — Phase 5 wire 가 multi-region failover 가시성을 제공했다면, Phase 7 은 OpenTelemetry distributed tracing (요청 흐름 추적) + Prometheus custom metrics (SLA 측정) + Alerting (장애 감지 + Slack/PagerDuty) territory 의 natural next 진입 / (2) **cj-style discipline 회피 위험 방지** = 49~88번째 누적 40-entry-point cycle + 88번째 Phase 6 close-out retro + 87번째 atomic wire + 86번째 spec entry + 85번째 PRD entry 패턴 모두 wire DONE 진입 정합 보존 후 즉시 Phase 7 진입 = 1-day atomic sprint discipline / (3) **비즈니스 우선순위 = NFR4 + GDPR + SOX + observability stack 강화** = enterprise 고객 onboarding 시 alert + SLA 99.95% + multi-region failover 가시성 + distributed tracing 필수 / (4) **Phase 4 wire `71a033a` 의 Sentry observability (Sentry browser SSR-safe + Sentry FastAPI server integration + tracesSampleRate=0.1) 의 자연스러운 carry-over chain** = Sentry 의 OpenTelemetry distributed tracing EXTENSION territory + Sentry alert routing 결정 / (5) **1st release launch wire DONE 진입 후 enterprise SLA = RPO 1h/RTO 4h → multi-region failover + Alerting 으로 99.95% SLA 달성 territory**. 옵션 (b) ABAC 정밀화 / 옵션 (c) Advanced analytics / 옵션 (d) Notification system / 옵션 (e) Phase 8 carry-over 모두 rejected (rationale: cj-style 89번째 자연스러운 carry-over territory = Observability stack 강화 의 Phase 5 multi-region observability EXTENSION + Phase 4 Sentry observability EXTENSION carry-over chain 정합 + enterprise SLA 우선순위 정합). Phase 6 territory DONE 정합 보존 (cj-style 85~88번째 epic 연속 정직 회복) + Epic 17 territory DONE 정합 보존 (cj-style 80~84번째 epic 연속 정직 회복) + Epic 16 territory DONE 정합 보존 (cj-style 67~72번째 epic 연속 정직 회복) + 1st release territory DONE 정합 보존 (cj-style 62~66번째 epic 연속 정직 회복) + Epic 15 territory DONE 정합 보존 (cj-style 58~61번째 epic 연속 정직 회복) + Phase 4 territory DONE 정합 보존 (cj-style 53~57번째 epic 연속 정직 회복) + Phase 3 territory DONE 정합 보존 (cj-style 49~52번째 epic 연속 정직 회복) 결정 wire 모두 보존. Phase 7 PRD entry 진입 시점에 적용 (cj-style 89번째 epic 연속 정직 회복 진입 시점에 결정). ✅ done 진입 2026-08-22. **A224+A225+A226+A227 follow-on**: master PRD v3.7 → v3.8 atomic edit + AD-34 Observability Stack 강화 신규 결정 + capability matrix v1.31 → v1.32 EXTENSION OBSERVABILITY_TRACES + OBSERVABILITY_METRICS 2 NEW rows + Phase 7 wire scope T1~T8 결정 |
| **A224** | **Master PRD v3.7 → v3.8 atomic edit 결정** — Phase 7 PRD entry 진입 시점에 결정 (cj-style Phase 7 1번째 진입점 = cj-style 89번째 epic 연속 정직 회복). master PRD v3.7 → v3.8 atomic edit (docs only, no code/test/sprint-status delta 외 PRD edit 신규). (1) front matter title v3.7 → v3.8 + changelog v3.8 entry 신규 (cj-style 89번째 epic 연속 정직 회복 진입 시점에 결정 verbatim 보존) / (2) §F23 신규 (F23.1 OpenTelemetry distributed tracing + F23.2 Prometheus custom metrics + F23.3 Alerting system + F23.4 Frontend performance tracing (Browser RUM) + F23.5 audit-first INSERT 2 NEW actions + F23.6 capability gate OBSERVABILITY_TRACES + OBSERVABILITY_METRICS + F23.7 tests + wire scope T1~T8 결정) / (3) §8.1 M0-(p) observability stack 강화 AC 신규 / (4) §15 로드맵 Phase 7 row status 백로그 → in-progress (PRD entry DONE 진입 wire) / (5) §부록 A A223+A224+A225+A226+A227 신규 결정 표 / (6) AD-34 Observability Stack 강화 신규 결정 (observability stack 강화 architecture 7 sub-decisions (a)~(g)) / (7) capability matrix v1.31 → v1.32 EXTENSION OBSERVABILITY_TRACES + OBSERVABILITY_METRICS 2 NEW rows industry-agnostic 4-industry grants ✅/✅/✅/✅. 결정 wire 일자: 2026-08-22 |
| **A225** | **AD-34 Observability Stack 강화 신규 결정** — Phase 7 PRD entry 진입 시점에 결정 (cj-style Phase 7 1번째 진입점 = cj-style 89번째 epic 연속 정직 회복). AD-34 = (a) **OpenTelemetry distributed tracing** 결정 wire = `apps/api/core/tracing.py` NEW ~+200 LOC (OTLP HTTP exporter 결정 wire + W3C Trace Context propagation `traceparent` + `tracestate` HTTP header 추출/주입 + FastAPI middleware `TraceContextMiddleware` 결정 wire + 비동기 trace context 보존 CR 1-1 ContextVar 정합 + span enrichment `tenant_id` + `user_id` + `trace_id` + `request_id` + `client_ip` 자동 span attribute 추가 + auto-instrumentation `opentelemetry-instrumentation-fastapi` + `opentelemetry-instrumentation-sqlalchemy` + `opentelemetry-instrumentation-httpx` + `opentelemetry-instrumentation-asyncpg` 4 instrumentors 결정 wire + head_based sampler ratio 1.0 dev + 0.1 prod 결정 wire + AD-14 stack pin `opentelemetry-api==1.27.0` + `opentelemetry-sdk==1.27.0` + `opentelemetry-exporter-otlp-proto-http==1.27.0` 결정 wire + Phase 4 wire `71a033a` Sentry `tracesSampleRate=0.1` 정합 보존). / (b) **Prometheus custom metrics** 결정 wire = `apps/api/core/metrics.py` NEW ~+180 LOC (Counter + Histogram + Summary + Gauge 4 metric types + 7 NEW business metrics: `business_signups_total{industry,plan}` Counter + `business_logins_total{method,outcome}` Counter + `business_calculations_total{engine,outcome}` Counter + `business_cost_engine_duration_seconds{engine,tenant_size_bucket}` Histogram + `business_audit_log_purge_total{action_class}` Counter + `business_active_tenants_gauge` Gauge + `business_ai_extraction_duration_seconds{model,outcome}` Histogram + `prometheus-client==0.20.0` AD-14 stack pin 결정 wire + `/metrics` endpoint Prometheus exposition format + `docs/grafana-dashboards.md` NEW 4 dashboards: business-signups + cost-engine-performance + auth-flow + audit-log-purge + Phase 5 wire `f093f8c` `phase_5_replication_lag` table 정합 보존). / (c) **Alerting system** 결정 wire = `apps/api/core/alerting.py` NEW ~+120 LOC + `config/alert_rules.yaml` NEW 5 NEW alert rules (`HighErrorRate` 5xx > 5% for 5m severity=critical + `SlowCalc` p99 > 5s for 10m severity=warning + `FailoverStuck` replication_lag_seconds > 30 for 5m Phase 5 wire 정합 severity=critical + `RetentionPurgeFailed` audit_log_purge_last_success_timestamp > 26h Phase 6 wire 정합 severity=warning + `MultiRegionDown` primary + secondary all down for 1m severity=critical) + Prometheus AlertManager integration + Sentry alert routing `apps/api/core/observability.py` EXTENSION alert webhook handler + Slack webhook integration `#bizup-alerts` channel + PagerDuty integration owner-only manual trigger AD-22 RBAC 결정 wire + audit-first INSERT `alert_fired` CR 1-1 verbatim action_class='OBSERVABILITY' + action='alert_fired' + actor_id='system' + alert_name + severity + tenant_id + trace_id 결정). / (d) **Frontend performance tracing (Browser RUM)** 결정 wire = `apps/web/lib/tracing.ts` NEW ~+150 LOC + `@opentelemetry/sdk-trace-web` + `@opentelemetry/exporter-trace-otlp-http` AD-14 stack pin 결정 wire + W3C Trace Context propagation server → client through `traceparent` header (Phase 5 wire의 multi-region trace propagation EXTENSION 정합) + Web Vitals auto-collection LCP + FID + CLS + INP + TTFB 5 metrics + custom span attributes `user.tenant_id` + `user.role` + `user.industry` + `route.path` + `route.locale` + `apps/web/lib/api-fetch.ts` MODIFIED trace context propagation + `apps/web/instrumentation.ts` NEW Next.js instrumentation hook + NFR18 ko-KR 정합 + `web-vitals` AD-14 stack pin 결정). / (e) **audit-first INSERT 2 NEW actions** 결정 wire (CR 1-1 verbatim + ActionClass.OBSERVABILITY 신규 정의 + 2 NEW AuditAction Literal values: `alert_fired` (severity + alert_name + tenant_id) + `trace_sampled` (decision + tenant_id + sampling_ratio) + apps/api/core/audit_action.py MODIFIED AuditAction Literal EXTENSION 2 NEW values + _ActionRegistry OBSERVABILITY entry 신규 2개 등록 + __all__ EXTENSION + emit_audit_typed BEFORE alerting trace CR 1-1 verbatim 적용 + Phase 6 wire `24e1cd7` 의 5 NEW AuditAction Literal EXTENSION pattern verbatim 적용). / (f) **Capability matrix v1.32 EXTENSION + 2 NEW rows** 결정 wire = `Capability.OBSERVABILITY_TRACES = "observability_traces"` + `Capability.OBSERVABILITY_METRICS = "observability_metrics"` 2 NEW enum 추가 (manufacturing ✅ + service ✅ + manufacturing_service ✅ + manufacturing_service_other ✅ industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러, AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind) + 미허용 tenant 의 observability stack 진입 차단 결정 wire + SSOT RED→GREEN EXTENSION (capability matrix v1.32 신규 2 rows + capability.py EXTENSION 2 NEW enum + `require_capability()` Dependency 2개 신규 wire) + drift detector `tests/integration/test_capability_matrix_v1_32_drift.py` NEW 8 NEW pytest cases 결정 (Phase 6 wire 의 `tests/integration/test_capability_matrix_v1_31_drift.py` + Epic 17 wire 의 `tests/integration/test_capability_matrix_v1_30_drift.py` 패턴 verbatim). capability matrix v1.32 신규 2 rows (`OBSERVABILITY_TRACES` + `OBSERVABILITY_METRICS`, industry-agnostic 4-industry grants ✅/✅/✅/✅) [§F23 verbatim, §8.1 M0-(p) EXTENSION 결정]. / (g) **dry-run + Tests + wire scope T1~T8** 결정 wire = tracing dry-run mode (sampling disabled 시 trace propagation 검증) + tests backend 결정 wire (~+30 NEW pytest PASS tracing module 6 + metrics module 6 + alerting module 4 + grafana dashboards 2 + audit action 2 NEW + capability matrix v1.32 8 + integration e2e 2) + tests frontend 결정 wire (~+10 NEW vitest PASS browser RUM tracing 3 + API fetch trace propagation 2 + SSOT drift 2 + ko-KR SSOT 검증 3) + 0 NEW ruff + 0 regressions + T1 OpenTelemetry tracing module + T2 Prometheus metrics module + T3 Alerting module + T4 Frontend tracing module + T5 capability v1.32 EXTENSION 2 NEW rows + T6 audit action EXTENSION 2 NEW + T7 tests + T8 3중 게이트 FINAL CLEAN atomic commit 결정. 결정 wire 일자: 2026-08-22 |
| **A226** | **Capability matrix v1.31 → v1.32 OBSERVABILITY_TRACES + OBSERVABILITY_METRICS 2 NEW rows 결정** — Phase 7 PRD entry 진입 시점에 결정 (cj-style Phase 7 1번째 진입점 = cj-style 89번째 epic 연속 정직 회복). `Capability.OBSERVABILITY_TRACES = "observability_traces"` + `Capability.OBSERVABILITY_METRICS = "observability_metrics"` 4-industry grants: manufacturing ✅ + service ✅ + manufacturing_service ✅ + manufacturing_service_other ✅ (industry-agnostic, CR 12-1 L4 precedent 미러, AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind). 미허용 tenant 의 observability stack 진입 차단 결정 wire. SSOT RED→GREEN EXTENSION (capability matrix v1.32 신규 2 rows + capability.py EXTENSION 2 NEW enum + `require_capability()` Dependency 2개 신규 wire + drift detector `tests/integration/test_capability_matrix_v1_32_drift.py` NEW 8 NEW pytest cases). CR 11-3 honest-DEFER discipline 89번째 epic 연속 정직 회복 (D-1-1-DEFER-1/2/3 RESOLVED 보존 + D-LAUNCH-1-DEFER-1 honestly preserved + D-EPIC-16-REVIEW-DEFER-2~6 ✅ RESOLVED 보존 + D-PHASE-4-DR-DEFER-1/2 ✅ RESOLVED 보존 + D-EPIC-17-WIRE-DEFER-T2-T3-UI ✅ RESOLVED 보존 + D-RETENTION-1 ✅ RESOLVED 보존 + **D-OBSERVABILITY-1 honestly DEFER 보존 = Phase 7 PRD entry 진입 시점에 carry-over chain 정직 회복 결정 wire 진입: 1st release close-out retro + Epic 17 close-out retro + Phase 6 close-out retro "observability stack 강화 결정 wire 보류, Phase 7+ 진입 시점" verbatim 해소**) + A36 SDR 검증 4-step 자동 적용 (commit prefix lint PASS + sprint-status structure PASS + vitest file count drift 0건 + commit consistency PASS). 결정 wire 일자: 2026-08-22 |
| **A227** | **Phase 7 wire scope T1~T8 결정** — Phase 7 PRD entry 진입 시점에 결정 (cj-style Phase 7 1번째 진입점 = cj-style 89번째 epic 연속 정직 회복). T1 `apps/api/core/tracing.py` NEW (~+200 LOC, OpenTelemetry distributed tracing + OTLP HTTP exporter + W3C Trace Context propagation + span enrichment + auto-instrumentation FastAPI + SQLAlchemy + httpx + asyncpg + head_based sampling decision) + T2 `apps/api/core/metrics.py` NEW (~+180 LOC, Prometheus custom metrics + 7 NEW business metrics + Counter + Histogram + Summary + Gauge + `/metrics` endpoint + `prometheus-client==0.20.0` AD-14 stack pin + `docs/grafana-dashboards.md` NEW 4 dashboards 결정) + T3 `apps/api/core/alerting.py` NEW (~+120 LOC + `config/alert_rules.yaml` NEW 5 NEW alert rules + Prometheus AlertManager + Sentry alert routing + Slack webhook + PagerDuty owner-only AD-22 + audit-first INSERT `alert_fired` CR 1-1 verbatim + `apps/api/core/observability.py` EXTENSION) + T4 `apps/web/lib/tracing.ts` NEW (~+150 LOC, Browser RUM via OpenTelemetry web SDK + W3C Trace Context propagation server → client + Web Vitals auto-collection LCP + FID + CLS + INP + TTFB + `apps/web/lib/api-fetch.ts` MODIFIED trace context propagation + `apps/web/instrumentation.ts` NEW Next.js instrumentation hook + `@opentelemetry/sdk-trace-web` + `@opentelemetry/exporter-trace-otlp-http` + `web-vitals` AD-14 stack pin) + T5 ko-KR.json SSOT EXTENSION wire 결정 (`observability.*` namespace EXTENSION N keys 결정 wire, CR 11-4 D-002 verbatim 적용) + T6 Capability v1.32 EXTENSION 2 NEW rows (capability.py MODIFIED + `tests/integration/test_capability_matrix_v1_32_drift.py` NEW + docs/capability-matrix.md v1.31 → v1.32 + drift detector) + T7 Tests + 3중 게이트 FINAL CLEAN (~+30 NEW pytest PASS tracing module 6 + metrics module 6 + alerting module 4 + grafana dashboards 2 + audit action 2 NEW + capability matrix v1.32 8 + integration e2e 2 + ~+10 NEW vitest PASS browser RUM tracing 3 + API fetch trace propagation 2 + SSOT drift 2 + ko-KR SSOT 검증 3 + 1 NEW integration drift) + T8 3중 게이트 FINAL CLEAN atomic commit decision. estimated ~+30 NEW pytest PASS + ~+10 NEW vitest PASS + 0 NEW ruff + 0 regressions. wire_commit = TBD (cj-style Phase 7 3번째 진입점 = cj-style 91번째 epic 연속 정직 회복 atomic single sweep T1~T8, expected ~16-22 files = ~14 NEW + ~3-8 MODIFIED). 결정 wire 일자: 2026-08-22 |
| **A253** | **옵션 (a) Phase 8+ 진입 결정** — Phase 7 close-out retro `326fa9f` (cj-style 92번째 wire entry) + Phase 7 atomic wire T1~T8 `59b56cd` (cj-style 91번째) + Phase 7 spec entry (cj-style 90번째) + Phase 7 PRD entry `916a541` (cj-style 89번째) 결정 wire 모두 DONE 진입 정합 보존 후 옵션 5종 (옵션 (a) Phase 8+ 진입 / 옵션 (b) Epic 18+ / 옵션 (c) carry-over / 옵션 (d) 1st release 추가 follow-up / 옵션 (e) D-DEFER-* carry-over follow-up) 중 **사용자 권장 결정 = 옵션 (a) Phase 8+ 진입** (AskUserQuestion 결정 verbatim bind). 옵션 (a) Phase 8+ 진입 후 옵션 5종 territory 후보 (옵션 (a) Performance / Load Testing (Recommended) / 옵션 (b) ABAC 정밀화 / 옵션 (c) Advanced analytics / 옵션 (d) Notification system / 옵션 (e) Phase 9 carry-over) 중 **사용자 권장 결정 = 옵션 (a) Performance / Load Testing = Phase 8 territory 결정 wire 진입**. rationale 5종: (1) **Phase 7 wire `59b56cd` 의 Prometheus custom metrics + SlowCalc alert rule (`business_cost_engine_duration_seconds` Histogram + `SlowCalc` p99 > 5s for 10m severity=warning) 의 자연스러운 carry-over chain** = k6 부하 테스트 (5 scenarios: smoke 100 VUs + baseline 1000 VUs + stress 5000 VUs + soak 100 VUs 1h + spike 10000 VUs) + SLO/SLI 정의 (4 SLAs: cost engine p99 < 5s + signups success_rate > 99% + logins p99 < 1s + audit log purge success_rate > 99.9%) + p99 latency budget + Latency regression detector + Performance regression gate CI + Cost-engine benchmark V8 골든 fixture territory = §F23 의 natural next EXTENSION / (2) **cj-style discipline 회피 위험 방지** = 49~92번째 누적 44-entry-point cycle + 92번째 Phase 7 close-out retro + 91번째 atomic wire + 90번째 spec entry + 89번째 PRD entry 패턴 모두 wire DONE 진입 정합 보존 후 즉시 Phase 8 진입 = 1-day atomic sprint discipline / (3) **비즈니스 우선순위 = NFR18 enterprise SLA 99.95% + scale-out 검증** = Phase 5 multi-region failover RPO 1h/RTO 30s + Phase 7 observability stack 강화 territory 의 natural carry-over + Phase 8 = enterprise SLA 99.95% 검증 territory / (4) **1st release launch territory DONE 진입 후 NFR18 SLA 검증 = k6 부하 테스트 + SLO/SLI 정의 territory** / (5) **Epic 11 V8 determinism 골든 fixture 의 cost-engine benchmark V8 골든 EXTENSION territory**. 옵션 (b) ABAC 정밀화 / 옵션 (c) Advanced analytics / 옵션 (d) Notification system / 옵션 (e) Phase 9 carry-over 모두 rejected (rationale: cj-style 93번째 자연스러운 carry-over territory = Phase 7 wire 의 Prometheus metrics + SlowCalc alert + Phase 5 multi-region failover EXTENSION carry-over chain 정합 + enterprise SLA 우선순위 정합). Phase 7 territory DONE 정합 보존 (cj-style 89~92번째 epic 연속 정직 회복) + Phase 6 territory DONE 정합 보존 (cj-style 85~88번째 epic 연속 정직 회복) + Epic 17 territory DONE 정합 보존 (cj-style 80~84번째 epic 연속 정직 회복) + Epic 16 territory DONE 정합 보존 (cj-style 67~72번째 epic 연속 정직 회복) + 1st release territory DONE 정합 보존 (cj-style 62~66번째 epic 연속 정직 회복) + Epic 15 territory DONE 정합 보존 (cj-style 58~61번째 epic 연속 정직 회복) + Phase 4 territory DONE 정합 보존 (cj-style 53~57번째 epic 연속 정직 회복) + Phase 3 territory DONE 정합 보존 (cj-style 49~52번째 epic 연속 정직 회복) 결정 wire 모두 보존. Phase 8 PRD entry 진입 시점에 적용 (cj-style 93번째 epic 연속 정직 회복 진입 시점에 결정). ✅ done 진입 2026-08-24. **A254+A255+A256+A257 follow-on**: master PRD v3.8 → v3.9 atomic edit + AD-35 Performance / Load Testing 신규 결정 + capability matrix v1.32 → v1.33 EXTENSION PERFORMANCE_TESTING 1 NEW row + Phase 8 wire scope T1~T8 결정 |
| **A254** | **Master PRD v3.8 → v3.9 atomic edit 결정** — Phase 8 PRD entry 진입 시점에 결정 (cj-style Phase 8 1번째 진입점 = cj-style 93번째 epic 연속 정직 회복). master PRD v3.8 → v3.9 atomic edit (docs only, no code/test/sprint-status delta 외 PRD edit 신규). (1) front matter title v3.8 → v3.9 + changelog v3.9 entry 신규 (cj-style 93번째 epic 연속 정직 회복 진입 시점에 결정 verbatim 보존) / (2) §F24 신규 (F24.1 k6 부하 테스트 5 scenarios + F24.2 SLO/SLI 정의 4 metrics + F24.3 p99 latency budget 5s cost-engine + F24.4 Latency regression detector + F24.5 Performance regression gate (CI) + F24.6 Cost-engine benchmark V8 골든 + F24.7 tests + wire scope T1~T8 결정) / (3) §8.1 M0-(q) Performance / Load Testing AC 신규 / (4) §15 로드맵 Phase 8 row status 백로그 → in-progress (PRD entry DONE 진입 wire) / (5) §부록 A A253+A254+A255+A256+A257 신규 결정 표 / (6) AD-35 Performance / Load Testing 신규 결정 (performance / load testing architecture 7 sub-decisions (a)~(g)) / (7) capability matrix v1.32 → v1.33 EXTENSION PERFORMANCE_TESTING 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅. 결정 wire 일자: 2026-08-24 |
| **A255** | **AD-35 Performance / Load Testing 신규 결정** — Phase 8 PRD entry 진입 시점에 결정 (cj-style Phase 8 1번째 진입점 = cj-style 93번째 epic 연속 정직 회복). AD-35 = (a) **k6 부하 테스트 5 scenarios** 결정 wire = `apps/api/tests/performance/k6/` directory + 5 NEW k6 scripts: `scripts/smoke-test-100.js` (sanity 100 VUs 30s ramp) + `scripts/baseline-1000.js` (baseline 1000 VUs 5min steady) + `scripts/stress-5000.js` (stress 5000 VUs 10min ramp + steady + ramp-down) + `scripts/soak-100-1h.js` (soak 100 VUs 1h memory leak detection) + `scripts/spike-10000.js` (spike 10000 VUs 1min immediate spike decision) + `k6==0.45.0` AD-14 stack pin 결정 wire + `docs/load-testing.md` EXTENSION 12 sections runbook 결정 (prerequisites + install + run + interpret + teardown + cleanup + CI integration + baseline drift + memory analysis + report generation + SLO violation + escalation). / (b) **SLO/SLI 정의 4 metrics** 결정 wire = `docs/slo-sli-definition.md` NEW 8 sections + (1) `business_cost_engine_duration_seconds p99 < 5s` SLO 99% (Phase 7 wire 의 7 NEW business metrics 의 자연스러운 carry-over chain 정합) / (2) `business_signups_total success_rate > 99%` SLO 99% (Phase 7 wire `59b56cd` 의 `business_signups_total{industry,plan}` Counter 정합) / (3) `business_logins_total p99 < 1s` SLO 99.9% (Phase 3 wire + Phase 7 wire 의 `business_logins_total{method,outcome}` Counter 정합) / (4) `business_audit_log_purge_total success_rate > 99.9%` SLO 99.9% (Phase 6 wire `24e1cd7` 의 `business_audit_log_purge_total{action_class}` Counter 정합 + Phase 6 audit retention purge job 의 success rate 결정) + Phase 7 wire `59b56cd` 의 7 NEW business metrics 의 자연스러운 carry-over chain 결정 wire + 30d rolling window baseline + 1.5h/month error budget + error budget burn rate 알림 (G2 > 10x 1h burn + G1 > 2x 1h burn). / (c) **p99 latency budget 5s cost-engine** 결정 wire = `apps/api/core/performance.py` NEW + `LatencyBudget` TypedDict + `check_latency_budget(engine, p99_seconds) -> LatencyBudgetResult` + 4 industries baseline industry-agnostic (manufacturing 5000ms + service 4500ms + manufacturing_service 4800ms + manufacturing_service_other 4700ms) + per-tenant override EXTENSION (`tenant_settings.performance_overrides: dict[str, LatencyBudget]`) + CR 12-5 D-14 envelope `{engine, p99_seconds, threshold, breach, trace_id}` 결정 (e.g. `COST_ENGINE_LATENCY_BUDGET_EXCEEDED_KO`) + Phase 7 wire `59b56cd` SlowCalc alert rule 정합. / (d) **Latency regression detector** 결정 wire = `apps/api/jobs/latency_regression.py` NEW ~+150 LOC + cron KST 매일 03:30 UTC 18:30 + 매 24시간 p99 latency sample 수집 (Supabase ClickHouse 또는 in-memory 집계) + drift > 10% p99 vs golden baseline 자동 감지 + Sentry breadcrumb `LatencyRegressionDetected` + Slack alert `#bizup-alerts` channel + audit-first INSERT `latency_regression_detected` (CR 1-1 verbatim, action_class='PERFORMANCE' + action='latency_regression_detected' + actor_id='system' + engine + drift_pct + baseline_p99 + current_p99 + trace_id) + Phase 7 wire `59b56cd` 의 SlowCalc alert rule 정합 결정 wire + Epic 11 V8 determinism 골든 fixture EXTENSION 결정 wire + ActionClass.PERFORMANCE_TEST 신규 정의 + PerformanceTestAction Literal 4 NEW values: `latency_regression_detected` + `p99_budget_exceeded` + `k6_load_test_completed` + `cost_engine_benchmark_invalidated` 결정 wire + `apps/api/core/audit_action.py` MODIFIED AuditAction Literal EXTENSION 4 NEW values + _ActionRegistry PERFORMANCE_TEST entry 신규 4개 등록 + __all__ EXTENSION + emit_audit_typed BEFORE alerting CR 1-1 verbatim 적용 + Phase 7 wire `59b56cd` 의 2 NEW AuditAction Literal EXTENSION pattern verbatim 적용. / (e) **Performance regression gate (CI)** 결정 wire = `.github/workflows/ci-performance.yml` NEW + k6 부하 테스트 자동 실행 (GitHub Actions `ubuntu-latest` + k6 install via apt) + p99 budget 초과 시 CI 빌드 차단 결정 wire + `BASELINE_THRESHOLD=1.10` (10% drift) 결정 wire + NFR18 ko-KR 정합 결정 wire. / (f) **Cost-engine benchmark V8 골든** 결정 wire = `tests/performance/test_cost_engine_benchmark.py` NEW ~+250 LOC + Epic 11 V8 determinism 골든 fixture EXTENSION + cost-engine 12-period benchmark 결정 wire + `packages/cost_engine/traditional/calculator.py` + `packages/cost_engine/abc/calculator.py` + `packages/cost_engine/tdabc/calculator.py` 3 engines benchmark 결정 wire + `BENCHMARK_GOLDEN_BASELINE_MS = {traditional: 800, abc: 1200, tdabc: 1500}` 결정 wire + `BENCHMARK_DRIFT_THRESHOLD = 1.10` 결정 wire + CR 12-5 D-14 envelope `{engine, period_count, total_ms, drift, trace_id}` 결정 (e.g. `COST_ENGINE_BENCHMARK_DRIFT_KO`) + tenant-scoped result_hash CR 4-3/4-4 verbatim 결정 wire + Epic 11 wire 의 V8 determinism 골든 fixture EXTENSION 결정 wire. / (g) **Capability matrix v1.33 EXTENSION + 1 NEW row** 결정 wire = `Capability.PERFORMANCE_TESTING = "performance_testing"` 1 NEW enum 추가 (manufacturing ✅ + service ✅ + manufacturing_service ✅ + manufacturing_service_other ✅ industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러, OBSERVABILITY_TRACES + OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind) + 미허용 tenant 의 performance / load testing 진입 차단 결정 wire + SSOT RED→GREEN EXTENSION (capability matrix v1.33 신규 1 row + capability.py EXTENSION 1 NEW enum + `require_capability()` Dependency 1개 신규 wire) + drift detector `tests/integration/test_capability_matrix_v1_33_drift.py` NEW 6 NEW pytest cases 결정 (Phase 7 wire 의 `tests/integration/test_capability_matrix_v1_32_drift.py` + Phase 6 wire 의 `tests/integration/test_capability_matrix_v1_31_drift.py` 패턴 verbatim). capability matrix v1.33 신규 1 row (`PERFORMANCE_TESTING`, industry-agnostic 4-industry grants ✅/✅/✅/✅) [§F24 verbatim, §8.1 M0-(q) EXTENSION 결정]. 결정 wire 일자: 2026-08-24 |
| **A256** | **Capability matrix v1.32 → v1.33 PERFORMANCE_TESTING 1 NEW row 결정** — Phase 8 PRD entry 진입 시점에 결정 (cj-style Phase 8 1번째 진입점 = cj-style 93번째 epic 연속 정직 회복). `Capability.PERFORMANCE_TESTING = "performance_testing"` 4-industry grants: manufacturing ✅ + service ✅ + manufacturing_service ✅ + manufacturing_service_other ✅ (industry-agnostic, CR 12-1 L4 precedent 미러, OBSERVABILITY_TRACES + OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind). 미허용 tenant 의 performance / load testing 진입 차단 결정 wire. SSOT RED→GREEN EXTENSION (capability matrix v1.33 신규 1 row + capability.py EXTENSION 1 NEW enum + `require_capability()` Dependency 1개 신규 wire + drift detector `tests/integration/test_capability_matrix_v1_33_drift.py` NEW 6 NEW pytest cases). CR 11-3 honest-DEFER discipline 93번째 epic 연속 정직 회복 (D-1-1-DEFER-1/2/3 RESOLVED 보존 + D-LAUNCH-1-DEFER-1 honestly preserved + D-EPIC-16-REVIEW-DEFER-2~6 ✅ RESOLVED 보존 + D-PHASE-4-DR-DEFER-1/2 ✅ RESOLVED 보존 + D-EPIC-17-WIRE-DEFER-T2-T3-UI ✅ RESOLVED 보존 + D-RETENTION-1 ✅ RESOLVED 보존 + D-OBSERVABILITY-1 ✅ RESOLVED 보존 + **D-PERFORMANCE-1 honestly DEFER 보존 = Phase 8 PRD entry 진입 시점에 carry-over chain 정직 회복 결정 wire 진입: 1st release close-out retro §6 + Epic 17 close-out retro §11 + Phase 6 close-out retro §13 + Phase 7 close-out retro §10 "Performance / Load Testing territory 결정 wire 보류, Phase 8+ 진입 시점" verbatim 해소**) + A36 SDR 검증 4-step 자동 적용 (commit prefix lint PASS + sprint-status structure PASS + vitest file count drift 0건 + commit consistency PASS). 결정 wire 일자: 2026-08-24 |
| **A257** | **Phase 8 wire scope T1~T8 결정** — Phase 8 PRD entry 진입 시점에 결정 (cj-style Phase 8 1번째 진입점 = cj-style 93번째 epic 연속 정직 회복). T1 `apps/api/tests/performance/k6/` + 5 NEW k6 scripts + `k6==0.45.0` AD-14 stack pin + `docs/load-testing.md` EXTENSION 12 sections runbook 결정 + T2 `docs/slo-sli-definition.md` NEW 8 sections + 4 SLAs 결정 wire (cost engine p99 < 5s + signups success_rate > 99% + logins p99 < 1s + audit log purge success_rate > 99.9%) + T3 `apps/api/core/performance.py` NEW + `LatencyBudget` TypedDict + 4 industries baseline industry-agnostic + per-tenant override EXTENSION + CR 12-5 D-14 envelope 결정 wire + T4 `apps/api/jobs/latency_regression.py` NEW ~+150 LOC + cron KST 매일 03:30 UTC 18:30 + 매 24시간 p99 latency sample 수집 + drift > 10% p99 vs golden baseline 자동 감지 + Sentry breadcrumb + Slack alert + audit-first INSERT `latency_regression_detected` CR 1-1 verbatim + Epic 11 V8 determinism 골든 fixture EXTENSION 결정 + T5 `.github/workflows/ci-performance.yml` NEW + k6 부하 테스트 자동 실행 + p99 budget 초과 시 CI 빌드 차단 + `BASELINE_THRESHOLD=1.10` (10% drift) 결정 wire + T6 `tests/performance/test_cost_engine_benchmark.py` NEW ~+250 LOC + Epic 11 V8 determinism 골든 fixture EXTENSION + cost-engine 12-period benchmark + 3 engines benchmark + `BENCHMARK_GOLDEN_BASELINE_MS = {traditional: 800, abc: 1200, tdabc: 1500}` + `BENCHMARK_DRIFT_THRESHOLD = 1.10` 결정 wire + T7 Capability v1.33 EXTENSION 1 NEW row + audit action EXTENSION 4 NEW values + ko-KR.json EXTENSION 결정 wire + T8 Tests + 3중 게이트 FINAL CLEAN atomic commit decision. estimated ~+25 NEW pytest PASS + ~+5 NEW vitest PASS + 0 NEW ruff + 0 regressions. wire_commit = TBD (cj-style Phase 8 3번째 진입점 = cj-style 95번째 epic 연속 정직 회복 atomic single sweep T1~T8, expected ~22 NEW + ~13 MODIFIED = ~35 files atomic single sprint). 결정 wire 일자: 2026-08-24 |
| **A258** | **옵션 (a) Phase 8 spec entry 진입 결정** — Phase 8 PRD entry `ced452f` (cj-style 93번째) + A253~A257 결정 wire 모두 DONE 진입 정합 보존 후 옵션 5종 (옵션 (a) Phase 8 spec entry / 옵션 (b) Phase 8 wire / 옵션 (c) Phase 8 close-out retro / 옵션 (d) carry-over / 옵션 (e) D-DEFER-* follow-up) 중 **사용자 권장 결정 = 옵션 (a) Phase 8 spec entry 진입**. rationale 5종: (1) **cj-style discipline 회피 위험 방지** = 93번째 PRD entry 진입 직후 즉시 spec entry 진입 = 1-day atomic sprint discipline / (2) **Phase 8 PRD entry 진입 시점에 이미 wire scope T1~T8 결정** → spec entry 진입 시점에 7 ACs (§F24.1~§F24.7) → 78 sub-ACs + T1~T8 68 subtasks 분해 결정 / (3) **Phase 8 wire 진입 시점에 PRD entry + spec entry 모두 wire DONE 진입 정합 보존 후 atomic single sprint T1~T8** / (4) **Epic 1 ~ Epic 17 + Phase 3 ~ Phase 7 + 1st release cycle 정합 + 다음 territory spec entry 진입** / (5) **Performance / Load Testing territory 결정 wire = Phase 7 wire 의 Prometheus metrics + SlowCalc alert + Phase 5 multi-region failover EXTENSION carry-over chain 정합**. 옵션 (b) wire / 옵션 (c) close-out retro / 옵션 (d) carry-over / 옵션 (e) D-DEFER-* follow-up 모두 rejected (rationale: cj-style 94번째 자연스러운 carry-over territory = spec entry 진입 = PRD entry 직후의 natural next 진입 결정 wire 정합). Phase 8 PRD entry 진입 시점에 정합 보존 (cj-style 93번째 epic 연속 정직 회복). 결정 wire 일자: 2026-08-24 |
| **A259** | **Phase 8 spec entry 결정** — Phase 8 spec entry 진입 시점에 결정 (cj-style Phase 8 2번째 진입점 = cj-style 94번째 epic 연속 정직 회복). baseline_commit `ced452f` 결정 wire + 7 ACs (§F24.1~§F24.7) → 78 sub-ACs 분해 결정 + T1~T8 68 subtasks 분해 결정 wire + ~31 files estimate + ~25 NEW pytest + ~10 NEW vitest + 0 NEW ruff 결정 wire + docs/specs/phase-8-spec.md NEW ~+250 LOC 결정 wire. 결정 wire 일자: 2026-08-24 |
| **A260** | **옵션 (a) Phase 8 wire 진입 결정** — Phase 8 spec entry (cj-style 94번째) + Phase 8 PRD entry `ced452f` (cj-style 93번째) 결정 wire 모두 DONE 진입 정합 보존 후 옵션 5종 중 **사용자 권장 결정 = 옵션 (a) Phase 8 wire 진입**. rationale 5종: (1) **cj-style discipline 회피 위험 방지** = 94번째 spec entry 진입 직후 즉시 wire 진입 = 1-day atomic sprint discipline / (2) **Performance / Load Testing territory 결정 wire = Phase 7 wire `59b56cd` Prometheus metrics + SlowCalc alert + Phase 5 multi-region failover EXTENSION carry-over chain 정합** / (3) **Epic 1 ~ Epic 17 + Phase 3 ~ Phase 7 + 1st release cycle 정합 + 다음 territory wire 진입** / (4) **OpenTelemetry + Prometheus + Alerting + Frontend RUM wire + tests + atomic commit 결정 wire + Epic 12 2FA 챌린지 보존 + AD-22 owner-only RBAC 보존 + D-PERFORMANCE-1 ✅ RESOLVED 보존 진입** / (5) **atomic wire T1~T8 + frontend scope T7 honestly carry over + i18n SSOT + RTL tests + close-out retro 결정 wire + single sprint atomic wire 1 진입점**. 결정 wire 일자: 2026-08-24 |
| **A261** | **Phase 8 wire scope T1~T8 wire DONE 결정** — Phase 8 wire 진입 시점에 결정 (cj-style Phase 8 3번째 진입점 = cj-style 95번째 epic 연속 정직 회복). wire_commit `60d4ea1` (cj-style 95번째 wire entry) + 22 NEW + 13 MODIFIED = 35 files atomic single sprint 결정 wire + 31 NEW pytest CASES PASS + 10 NEW vitest CASES PASS + 0 NEW ruff + 0 regressions 결정 wire + AtomicCommit 6 NEW 결정 wire. 결정 wire 일자: 2026-08-24 |
| **A262** | **옵션 (a) Phase 8 close-out retro 진입 결정** — Phase 8 wire `60d4ea1` (cj-style 95번째) + Phase 8 spec entry (cj-style 94번째) + Phase 8 PRD entry `ced452f` (cj-style 93번째) 결정 wire 모두 DONE 진입 정합 보존 후 옵션 5종 중 **사용자 권장 결정 = 옵션 (a) Phase 8 close-out retro 진입**. rationale 5종: (1) **cj-style discipline 회피 위험 방지** = 95번째 wire 진입 직후 즉시 close-out retro 진입 = 1-day atomic sprint discipline / (2) **cj-style 정직 회복 (honest recovery) 패턴** = wire DONE 진입 후 close-out retro = retro_document + handoff + sprint-status v3.11 + MEMORY.md hook EXTENSION + commit-msg 5 files atomic docs-only / (3) **Epic 1 ~ Epic 17 + Phase 3 ~ Phase 7 + Phase 8 + 1st release cycle 정합 + 다음 territory 진입** / (4) **NFR18 ko-KR 정합 + Capability v1.33 EXTENSION PERFORMANCE_TESTING 정합 + ActionClass.PERFORMANCE_TEST EXTENSION 정합 + Epic 12 2FA 챌린지 보존 + AD-22 owner-only RBAC 보존 + D-PERFORMANCE-1 ✅ RESOLVED 보존 진입** / (5) **partial wire 시도 0건 + single sprint atomic docs-only wire 1 진입점**. 결정 wire 일자: 2026-08-24 |
| **A263** | **옵션 (a) Phase 8 wire 진입 결정** — Phase 8 PRD entry `ced452f` (cj-style 93번째) + Phase 8 spec entry (cj-style 94번째) 결정 wire 모두 DONE 진입 정합 보존 후 옵션 5종 중 **사용자 권장 결정 = 옵션 (a) Phase 8 wire 진입**. rationale 5종: (1) **cj-style discipline 회피 위험 방지** = 94번째 spec entry 진입 직후 즉시 wire 진입 = 1-day atomic sprint discipline / (2) **Performance / Load Testing territory 결정 wire = Phase 7 wire 의 Prometheus metrics + SlowCalc alert + Phase 5 multi-region failover EXTENSION carry-over chain 정합** / (3) **Epic 1 ~ Epic 17 + Phase 3 ~ Phase 7 + 1st release cycle 정합 + 다음 territory wire 진입** / (4) **OpenTelemetry + Prometheus + Alerting + Frontend RUM wire + tests + atomic commit 결정 wire + Epic 12 2FA 챌린지 보존 + AD-22 owner-only RBAC 보존 + D-PERFORMANCE-1 ✅ RESOLVED 보존 진입** / (5) **atomic wire T1~T8 + frontend scope T7 honestly carry over + i18n SSOT + RTL tests + close-out retro 결정 wire + single sprint atomic wire 1 진입점**. 결정 wire 일자: 2026-08-24 |
| **A264** | **Phase 8 wire DONE 진입 결정** — Phase 8 wire 진입 시점에 결정 (cj-style Phase 8 3번째 진입점 = cj-style 95번째 epic 연속 정직 회복). wire_commit `60d4ea1` + 7 ACs ALL PASS + capability matrix v1.33 EXTENSION PERFORMANCE_TESTING 1 NEW row + ActionClass.PERFORMANCE_TEST EXTENSION 4 NEW values + audit-first INSERT 4 NEW + 31 NEW pytest CASES PASS + 10 NEW vitest CASES PASS + 0 NEW ruff + 0 regressions + ko-KR.json `performance.*` EXTENSION ~25 keys + 3 GH Actions workflows + 5 k6 scripts + docs/slo-sli.md + cost-engine-v8.json golden fixture + AtomicCommit 6 NEW 결정 wire. 결정 wire 일자: 2026-08-24 |
| **A265** | **Capability matrix v1.33 PERFORMANCE_TESTING 1 NEW row 결정** — Phase 8 wire 진입 시점에 결정 (cj-style 95번째 epic 연속 정직 회복). v1.33 EXTENSION 결정 wire (industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent verbatim). 결정 wire 일자: 2026-08-24 |
| **A266** | **ActionClass.PERFORMANCE_TEST EXTENSION 4 NEW values 결정** — Phase 8 wire 진입 시점에 결정 (cj-style 95번째 epic 연속 정직 회복). PerformanceTestAction Literal 4 NEW values `performance_test_started` + `performance_test_completed` + `p99_regression_detected` + `cost_engine_benchmark_invalidated` 결정 wire (CR 1-1 audit-first INSERT verbatim). 결정 wire 일자: 2026-08-24 |
| **A267** | **load_test_runner.py + latency_budget.py + eslint/latency-budget-rule.js + 5 k6 scripts + 3 GH Actions workflows + slo-sli.md + cost-engine-v8.json golden fixture 결정** — Phase 8 wire 진입 시점에 결정 (cj-style 95번째 epic 연속 정직 회복). K6_VERSION=0.45.0 AD-14 stack pin + LatencyBudget TypedDict 5 keys + DEFAULT_LATENCY_BUDGETS 7 canonical endpoints + per-tenant JSONB override + LatencyRegressionThresholdExceededError(422) + LatencyBudgetMiddleware + set/get_current_trace_id CR 1-1 ContextVar verbatim + ESLint v9 rule KNOWN_ENDPOINTS 7 canonical endpoints + unmappedEndpoint messageId CR 12-5 D-14 envelope + 5 NEW k6 scripts + 3 GH Actions workflows load-test.yml + perf-regression.yml + cost-engine-benchmark.yml 결정 wire. 결정 wire 일자: 2026-08-24 |
| **A268** | **apps/api/main.py EXTENSION 결정** — Phase 8 wire 진입 시점에 결정 (cj-style 95번째 epic 연속 정직 회복). from apps.api.core.latency_budget import LatencyRegressionThresholdExceededError import + app.add_middleware(LatencyBudgetMiddleware) after TraceContextMiddleware + 1 NEW exception handler @app.exception_handler(LatencyRegressionThresholdExceededError) returning HTTP 422 with canonical error envelope 결정 wire. 결정 wire 일자: 2026-08-24 |
| **A269** | **apps/api/dependencies/capability.py EXTENSION 결정** — Phase 8 wire 진입 시점에 결정 (cj-style 95번째 epic 연속 정직 회복). require_performance_testing 1 NEW dep `require_performance_testing = require_capability(Capability.PERFORMANCE_TESTING)` + __all__ EXTENSION 결정 wire. 결정 wire 일자: 2026-08-24 |
| **A270** | **apps/web TS mirror + components + i18n 결정** — Phase 8 wire 진입 시점에 결정 (cj-style 95번째 epic 연속 정직 회복). apps/web/__tests__/slo-dashboard.test.tsx NEW SLOStatusBadge green/yellow/red + owner-only ack prompt AD-22 verbatim + apps/web/__tests__/latency-regression.test.tsx NEW LatencyRegressionBanner p99 regression alert + no render when below threshold + ko-KR.json `performance.*` namespace EXTENSION ~25 keys CR 11-4 D-002 verbatim SSOT + i18n SSOT drift detector 7 NEW vitest PASS 결정 wire + 10 NEW vitest CASES PASS 결정 wire CR 11-4 D-003 RTL render discipline verbatim + 0 NEW vitest drift from wire scope 결정 wire 보존 결정 wire. 결정 wire 일자: 2026-08-24 |
| **A271** | **T7a + T7b tests honestly FULFILLED 결정** — Phase 8 wire 진입 시점에 결정 (cj-style 95번째 epic 연속 정직 회복). 보존 (tracing module load test runner 8 + latency budget module 6 + ESLint rule 4 + SLO/SLI docs 4 + audit action 6 + capability matrix v1.33 3 = 31 NEW pytest CASES PASS 결정 wire + frontend SLO dashboard 3 + latency regression 2 + ko-KR SSOT 5 = 10 NEW vitest CASES PASS 결정 wire + 0 NEW ruff + 0 regressions 결정 wire 보존 결정 wire). 결정 wire 일자: 2026-08-24 |
| **A272** | **atomic commit via `git commit -F <file>` 결정** — Phase 8 wire 진입 시점에 결정 (cj-style 95번째 epic 연속 정직 회복). CR 9-6 D5 prevention + commit-msg file 신규 = `_bmad-output/implementation-artifacts/commit-msg-phase-8-wire.txt` 결정 wire + handoff memory 신규 = `memory/handoff-2026-08-24-phase-8-wire-done.md` 결정 wire + MEMORY.md hook index 신규 EXTENSION 결정 wire + sprint-status.yaml MODIFIED (`phase-8-wire: in-progress → done` + A263~A272 action_items 신규 block 10 entries + `last_updated_note` v3.10 Phase 8 wire entry prepend) 결정 wire. 결정 wire 일자: 2026-08-24 |
| **A273** | **옵션 (a) Phase 8 close-out retro 진입 결정** — Phase 8 wire `60d4ea1` (cj-style 95번째) + Phase 8 spec entry (cj-style 94번째) + Phase 8 PRD entry `ced452f` (cj-style 93번째) 결정 wire 모두 DONE 진입 정합 보존 후 옵션 5종 중 **사용자 권장 결정 = 옵션 (a) Phase 8 close-out retro 진입**. rationale 5종: (1) **cj-style discipline 회피 위험 방지** = 95번째 wire 진입 직후 즉시 close-out retro 진입 = 1-day atomic sprint discipline / (2) **cj-style 정직 회복 (honest recovery) 패턴** = wire DONE 진입 후 close-out retro = retro_document + handoff + sprint-status v3.11 + MEMORY.md hook EXTENSION + commit-msg 5 files atomic docs-only / (3) **Epic 1 ~ Epic 17 + Phase 3 ~ Phase 7 + Phase 8 + 1st release cycle 정합 + 다음 territory 진입** / (4) **NFR18 ko-KR 정합 + Capability v1.33 EXTENSION PERFORMANCE_TESTING 정합 + ActionClass.PERFORMANCE_TEST EXTENSION 정합 + Epic 12 2FA 챌린지 보존 + AD-22 owner-only RBAC 보존 + D-PERFORMANCE-1 ✅ RESOLVED 보존 진입** / (5) **partial wire 시도 0건 + single sprint atomic docs-only wire 1 진입점**. 결정 wire 일자: 2026-08-24 |
| **A274** | **Master PRD v3.9 → v3.10 atomic edit 결정** — Phase 8 close-out retro 진입 시점에 결정 (cj-style Phase 8 4번째 진입점 = cj-style 96번째 epic 연속 정직 회복). master PRD v3.9 → v3.10 atomic edit (docs only, no code/test/sprint-status delta 외 PRD edit 신규). (1) front matter title v3.9 → v3.10 + changelog v3.10 entry 신규 (cj-style 96번째 epic 연속 정직 회복 진입 시점에 결정 verbatim 보존) / (2) §부록 A A273~A282 신규 결정 표 / (3) AD-36 Performance / Load Testing close-out retro 신규 결정. 결정 wire 일자: 2026-08-24 |
| **A275** | **AD-36 Performance / Load Testing close-out retro 신규 결정** — Phase 8 close-out retro 진입 시점에 결정 (cj-style 96번째 epic 연속 정직 회복). retro_document `phase-8-close-out-2026-08-24.md` NEW ~+600 lines 14-section cj-style retro (Summary + 14 sections + Conclusions + References 결정 wire). 결정 wire 일자: 2026-08-24 |
| **A276** | **Capability matrix v1.33 close-out retro EXTENSION 결정** — Phase 8 close-out retro 진입 시점에 결정 (cj-style 96번째 epic 연속 정직 회복). PERFORMANCE_TESTING 1 NEW row 보존 결정 wire + v1.33 close-out EXTENSION 결정 wire. 결정 wire 일자: 2026-08-24 |
| **A277** | **Phase 8 close-out retro T1~T8 결정** — Phase 8 close-out retro 진입 시점에 결정 (cj-style 96번째 epic 연속 정직 회복). T1 retro_document `phase-8-close-out-2026-08-24.md` NEW ~+600 lines 14-section cj-style retro + T2 handoff memory 신규 = `memory/handoff-2026-08-24-phase-8-close-out-done.md` 결정 wire + T3 sprint-status.yaml MODIFIED (`phase-8-close-out: in-progress → done` + A273~A282 action_items 신규 block 10 entries + `last_updated_note` v3.11 Phase 8 close-out entry prepend) + T4 MEMORY.md hook index 신규 EXTENSION 결정 wire + T5 commit-msg file 신규 = `_bmad-output/implementation-artifacts/commit-msg-phase-8-close-out.txt` 결정 wire. 결정 wire 일자: 2026-08-24 |
| **A278** | **partial wire 시도 0건 결정** — Phase 8 close-out retro 진입 시점에 결정 (cj-style 96번째 epic 연속 정직 회복). 35 files atomic single sprint 결정 wire 보존 결정 wire. 결정 wire 일자: 2026-08-24 |
| **A279** | **CR 9-6 commit message discipline ✅ APPLIED 결정** — Phase 8 close-out retro 진입 시점에 결정 (cj-style 96번째 epic 연속 정직 회복). `git commit -F <file>` 사용, PowerShell here-string 회피, D5 prevention + this commit message 결정 wire. 결정 wire 일자: 2026-08-24 |
| **A280** | **CR 11-3 honest-DEFER discipline ✅ APPLIED 결정** — Phase 8 close-out retro 진입 시점에 결정 (cj-style 96번째 epic 연속 정직 회복). 95번째 epic 연속 정직 회복 결정 wire, D-1-1-DEFER-* ✅ RESOLVED 보존 + D-EPIC-16-REVIEW-DEFER-* ✅ RESOLVED 보존 + D-PHASE-4-DR-DEFER-* ✅ RESOLVED 보존 + D-EPIC-17-WIRE-DEFER-T2-T3-UI ✅ RESOLVED 보존 + D-RETENTION-1 ✅ RESOLVED 보존 + D-OBSERVABILITY-1 ✅ RESOLVED 보존 + D-PERFORMANCE-1 honestly DEFER 보존 1 NEW 결정 wire 결정 wire. 결정 wire 일자: 2026-08-24 |
| **A281** | **CR 12-5 D-PARITY-01 + D-GATE-01 inversion ✅ APPLIED 결정** — Phase 8 close-out retro 진입 시점에 결정 (cj-style 96번째 epic 연속 정직 회복). Python FastAPI backend latency_budget.py TypedDict ↔ TypeScript Next.js frontend slo-dashboard.tsx interface parity 결정 wire + vitest CR 12-5 D-PARITY-01 검증 결정 wire + PERFORMANCE_TESTING capability gate per-tenant on/off + owner-only RBAC AD-22 결정 wire + gate 적용 대상 명시 결정 wire. 결정 wire 일자: 2026-08-24 |
| **A282** | **A19 cohesion pattern 9 surface EXTENSION PASS 결정** — Phase 8 close-out retro 진입 시점에 결정 (cj-style 96번째 epic 연속 정직 회복). performance/load testing surface NEW = F24.1~F24.7 performance/load testing 강화 territory 결정 wire + spec surface EXTENSION 결정 wire + test surface EXTENSION 결정 wire + capability matrix surface EXTENSION 결정 wire. 결정 wire 일자: 2026-08-24 |
| **A283** | **옵션 (a) Phase 9+ 진입 결정** — Phase 8 close-out retro `ab495a8` (cj-style 96번째 wire entry) + Phase 8 atomic wire T1~T8 `60d4ea1` (cj-style 95번째) + Phase 8 spec entry (cj-style 94번째) + Phase 8 PRD entry `ced452f` (cj-style 93번째) 결정 wire 모두 DONE 진입 정합 보존 후 옵션 5종 (옵션 (a) Phase 9+ 진입 / 옵션 (b) Epic 18+ / 옵션 (c) carry-over / 옵션 (d) 1st release 추가 follow-up / 옵션 (e) D-DEFER-* carry-over follow-up) 중 **사용자 권장 결정 = 옵션 (a) Phase 9+ 진입** (AskUserQuestion 결정 verbatim bind). 옵션 (a) Phase 9+ 진입 후 옵션 5종 territory 후보 (옵션 (a) Chaos Engineering / Game Day (Recommended) / 옵션 (b) Supply chain security (SBOM + sigstore) / 옵션 (c) Cost allocation engine v2 / 옵션 (d) Multi-tenant analytics dashboard / 옵션 (e) Phase 10 carry-over) 중 **사용자 권장 결정 = 옵션 (a) Chaos Engineering / Game Day = Phase 9 territory 결정 wire 진입**. rationale 5종: (1) **Phase 8 wire `60d4ea1` 의 Performance / Load Testing territory (`business_cost_engine_duration_seconds` Histogram + `SlowCalc` alert + `LatencyRegressionDetector` + k6 부하 테스트 5 scenarios + SLO/SLI 정의 4 metrics) 의 자연스러운 carry-over chain** = Chaos Engineering (chaos_experiment + fault_injection 10 categories + game_day runbook + blast_radius_control 5 levels + continuous_chaos vs scheduled + auto-rollback + safety mechanisms 6 layers) territory 가 §F24 의 자연스러운 EXTENSION territory (Phase 9 = §F25 신규 territory) — Phase 8 wire 가 SLO/SLI 정의 + Latency regression detector territory 를 제공했다면, Phase 9 은 chaos_experiment 를 통한 fault injection + blast_radius_control + auto-rollback territory 의 natural next 진입 / (2) **cj-style discipline 회피 위험 방지** = 49~96번째 누적 48-entry-point cycle + 96번째 Phase 8 close-out retro + 95번째 atomic wire + 94번째 spec entry + 93번째 PRD entry 패턴 모두 wire DONE 진입 정합 보존 후 즉시 Phase 9 진입 = 1-day atomic sprint discipline / (3) **비즈니스 우선순위 = NFR18 enterprise SLA 99.95% + chaos engineering 검증 + multi-region failover 검증** = enterprise 고객 onboarding 시 resilience 검증 + fault injection 자동화 + game day 분기별 실행 territory 필수 / (4) **Phase 7 wire `59b56cd` 의 Prometheus custom metrics + Alerting system + Sentry alert routing 의 자연스러운 carry-over chain** = Chaos Engineering territory 의 steady_state metric 자동 검증 + auto-rollback 의 observability integration territory / (5) **Phase 5 wire `f093f8c` 의 multi-region failover + DR drill + cross-region backup 의 자연스러운 carry-over chain** = Chaos Engineering territory 의 multi-region blast radius L4/L5 + game day 분기별 실행 territory 정합. 옵션 (b) Supply chain security / 옵션 (c) Cost allocation engine v2 / 옵션 (d) Multi-tenant analytics dashboard / 옵션 (e) Phase 10 carry-over 모두 rejected (rationale: cj-style 97번째 자연스러운 carry-over territory = Chaos Engineering 의 Phase 8 performance observability EXTENSION + Phase 7 alerting EXTENSION + Phase 5 multi-region failover EXTENSION carry-over chain 정합 + enterprise resilience 우선순위 정합). Phase 8 territory DONE 정합 보존 (cj-style 93~96번째 epic 연속 정직 회복) + Phase 7 territory DONE 정합 보존 (cj-style 89~92번째 epic 연속 정직 회복) + Phase 6 territory DONE 정합 보존 (cj-style 85~88번째 epic 연속 정직 회복) + Epic 17 territory DONE 정합 보존 (cj-style 80~84번째 epic 연속 정직 회복) + Epic 16 territory DONE 정합 보존 (cj-style 67~72번째 epic 연속 정직 회복) + 1st release territory DONE 정합 보존 (cj-style 62~66번째 epic 연속 정직 회복) + Epic 15 territory DONE 정합 보존 (cj-style 58~61번째 epic 연속 정직 회복) + Phase 4 territory DONE 정합 보존 (cj-style 53~57번째 epic 연속 정직 회복) + Phase 3 territory DONE 정합 보존 (cj-style 49~52번째 epic 연속 정직 회복) 결정 wire 모두 보존. Phase 9 PRD entry 진입 시점에 적용 (cj-style 97번째 epic 연속 정직 회복 진입 시점에 결정). ✅ done 진입 2026-08-24. **A284+A285+A286+A287 follow-on**: master PRD v3.9 → v4.0 atomic edit + AD-36 Chaos Engineering / Game Day 신규 결정 + capability matrix v1.33 → v1.34 EXTENSION CHAOS_ENGINEERING 1 NEW row + Phase 9 wire scope T1~T8 결정 |
| **A284** | **Master PRD v3.9 → v4.0 atomic edit 결정** — Phase 9 PRD entry 진입 시점에 결정 (cj-style Phase 9 1번째 진입점 = cj-style 97번째 epic 연속 정직 회복). master PRD v3.9 → v4.0 atomic edit (docs only, no code/test/sprint-status delta 외 PRD edit 신규). (1) front matter title v3.9 → v4.0 + changelog v4.0 entry 신규 (cj-style 97번째 epic 연속 정직 회복 진입 시점에 결정 verbatim 보존) / (2) §F25 신규 (F25.1 chaos experiment definition + F25.2 fault injection types 10 categories + F25.3 game day runbook + blast radius control + F25.4 continuous chaos vs scheduled game day + F25.5 tenant-scoped + multi-region chaos + F25.6 auto-rollback + safety mechanisms + F25.7 tests + wire scope T1~T8 결정) / (3) §8.1 M0-(r) chaos engineering / game day AC 신규 / (4) §15 로드맵 Phase 9 row status 백로그 → in-progress (PRD entry DONE 진입 wire) / (5) §부록 A A283+A284+A285+A286+A287 신규 결정 표 / (6) AD-36 Chaos Engineering / Game Day 신규 결정 (chaos engineering architecture 7 sub-decisions (a)~(g)) / (7) capability matrix v1.33 → v1.34 EXTENSION CHAOS_ENGINEERING 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅. 결정 wire 일자: 2026-08-24 |
| **A285** | **AD-36 Chaos Engineering / Game Day 신규 결정** — Phase 9 PRD entry 진입 시점에 결정 (cj-style Phase 9 1번째 진입점 = cj-style 97번째 epic 연속 정직 회복). AD-36 = (a) **chaos experiment definition** 결정 wire = `apps/api/modules/chaos/chaos_experiment.py` NEW ~+150 LOC + `ChaosExperiment` TypedDict 12 fields (id, name, description, hypothesis, steady_state_metric, blast_radius enum L1/L2/L3/L4/L5, fault_type enum latency/error/resource/network_partition/disk_io/db_pool/cache/dns/process_kill/clock_skew, abort_condition enum steady_state_breach/error_rate/duration/manual, rollback_strategy enum automatic/manual/hybrid/scheduled_abort, target_service, target_tenant_id nullable, target_region nullable, duration_seconds, status, created_by, created_at, started_at nullable, completed_at nullable, trace_id) + 5 blast_radius levels (L1 single_request / L2 single_tenant / L3 all_tenants / L4 single_region / L5 multi_region) + 4 abort conditions (steady_state_metric > 1.5x baseline / error_rate > 5% / duration max / manual abort) + 4 rollback strategies (automatic rollback / manual rollback (owner-only + 2FA 챌린지 Epic 12 정합) / hybrid rollback / scheduled abort) 결정 wire + Phase 8 wire `60d4ea1` 의 `LatencyBudget` TypedDict EXTENSION chain 결정 wire. / (b) **fault injection types 10 categories** 결정 wire = `apps/api/modules/chaos/fault_injection.py` NEW ~+200 LOC + 10 fault types: latency injection (HTTP middleware `LatencyFaultMiddleware` + configurable delay ms + jitter) + error injection (`ErrorFaultMiddleware` + HTTP 500/503 + ratio) + resource exhaustion (CPU + memory via cgroups + Linux kernel limit) + network partition (Linux `tc netem` + iptables rule injection) + disk I/O stress (`fio` + read/write IOPS limit) + DB connection pool exhaustion (asyncpg pool max_size override) + cache failure (Supabase cache eviction + TTL=0 forced) + DNS failure (NXDOMAIN response injection via dnsmasq) + process kill (SIGKILL/SIGTERM target process) + clock skew (`libfaketime` library + time jump 결정 wire) + Phase 7 wire `59b56cd` 의 alerting system EXTENSION 결정 wire (chaos_experiment 자동 검증 시 alerting system 의 steady_state metric + Sentry breadcrumb + Slack alert 자동 publish) + Phase 8 wire `60d4ea1` 의 k6 부하 테스트 EXTENSION 결정 wire. / (c) **game day runbook + blast radius control** 결정 wire = `docs/chaos-engineering.md` NEW 14 sections runbook (purpose + prerequisites + safety checklist + blast radius matrix L1~L5 + 10 fault injection types + 8 game day steps + rollback procedures + post-mortem template + ABORT conditions + continuous chaos vs scheduled game day + tenant scoping + multi-region chaos + observability integration + escalation matrix) + `apps/api/jobs/chaos_game_day.py` NEW ~+150 LOC + cron KST 1st Sunday 03:00 = UTC 18:00 결정 wire + 8 game day steps: experiment selection (ChaosExperiment TypedDict 결정) + tenant scoping (L2 single_tenant blast radius 선택 시 specific tenant_id 결정) + blast radius confirmation (blast_radius level + target_service + target_region 검증) + steady state baseline 측정 (Phase 7 wire `59b56cd` 의 Prometheus custom metrics 자동 수집 + Phase 8 wire `60d4ea1` 의 k6 baseline 1000 VUs 5min scenario 자동 실행) + fault injection (fault_injection module 자동 호출) + observation (60s observation window + steady_state_metric 검증 + abort conditions 검증) + auto-rollback (rollback_strategy 결정 + 4 rollback strategies 자동 실행) + post-mortem report (`phase_9_chaos_experiments_results` table + audit_log_archive 결정 wire + Sentry capture + Slack thread summary 결정). / (d) **continuous chaos vs scheduled game day** 결정 wire = `apps/api/jobs/continuous_chaos.py` NEW ~+120 LOC + 4 production-safe experiment candidates (`cost-engine-latency-injection-100ms` + `auth-error-injection-1pct` + `audit-log-query-latency-injection-50ms` + `multi-region-replication-lag-injection`) + L1 single_request blast radius only + 5% traffic (production traffic 5% 만 chaos experiment 적용) + 60s max duration (production 안전 시간 상한) + Prometheus custom metrics `business_chaos_experiments_total{experiment_name,blast_radius,outcome}` Counter + `business_chaos_auto_rollback_total{experiment_name,trigger}` Counter + `business_chaos_observations_seconds` Histogram + Phase 8 wire `60d4ea1` 의 p99 latency budget EXTENSION 결정 wire + Phase 7 wire `59b56cd` 의 alerting system EXTENSION 결정 wire (chaos_experiment 결과 자동 publish). / (e) **tenant-scoped + multi-region chaos** 결정 wire (CR 0-2 RLS lesson + L2 single_tenant + L4 single_region + L5 multi_region + `apps/api/alembic/versions/0041_phase_9_chaos_engineering.py` NEW `phase_9_chaos_experiments` table 14 columns (id UUID PK + tenant_id UUID FK nullable for L3/L4 multi-tenant experiments + experiment_name TEXT NOT NULL + blast_radius TEXT enum L1~L5 NOT NULL + fault_type TEXT enum 10 values NOT NULL + target_service TEXT + target_region TEXT enum + steady_state_metric TEXT + abort_conditions JSONB + rollback_strategy TEXT enum 4 values + duration_seconds INTEGER + status TEXT enum pending/running/completed/aborted/failed + results_json JSONB + created_by UUID + created_at TIMESTAMPTZ DEFAULT NOW() + started_at TIMESTAMPTZ nullable + completed_at TIMESTAMPTZ nullable + trace_id UUID) + 3 indexes (status + tenant_id+status+created_at DESC + created_at DESC) + 2 CHECK constraints (blast_radius enum + status enum) + RLS policy `tenant_id = current_setting('app.tenant_id')::uuid` + Phase 5 wire `f093f8c` `phase_5_replication_lag` table 정합 결정 wire + Phase 5 wire 의 multi-region observability 정합 결정 wire. / (f) **auto-rollback + safety mechanisms 6 layers** 결정 wire (4 strategies: automatic / manual (owner-only + 2FA 챌린지 Epic 12 정합) / hybrid / scheduled abort + 6 safety layers: layer 1 abort conditions (4 conditions 자동 검증) + layer 2 blast radius control (5 levels + tenant scoping) + layer 3 owner-only RBAC AD-22 (Epic 12 2FA 챌린지 보존 결정 wire) + layer 4 dry-run mode default (production 진입 시 dry_run=True 기본) + layer 5 steady state verification (Phase 7 metrics 자동 검증) + layer 6 circuit breaker (concurrent chaos experiment max 1 + resource guardrail) + audit-first INSERT 4 NEW `chaos_experiment_started` + `chaos_experiment_completed` + `chaos_experiment_aborted` + `chaos_rollback_triggered` CR 1-1 verbatim + ActionClass.CHAOS_ENGINEERING 신규 정의 + apps/api/core/audit_action.py MODIFIED AuditAction Literal EXTENSION 4 NEW values + _ActionRegistry CHAOS_ENGINEERING entry 신규 4개 등록 + __all__ EXTENSION + emit_audit_typed BEFORE chaos_experiment 시작 CR 1-1 verbatim 적용 + Phase 7 wire `59b56cd` 의 2 NEW AuditAction Literal EXTENSION pattern verbatim 적용 + Phase 8 wire `60d4ea1` 의 4 NEW PerformanceTestAction EXTENSION pattern verbatim 적용). / (g) **Capability matrix v1.34 EXTENSION + 1 NEW row** 결정 wire = `Capability.CHAOS_ENGINEERING = "chaos_engineering"` 1 NEW enum 추가 (manufacturing ✅ + service ✅ + manufacturing_service ✅ + manufacturing_service_other ✅ industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러, PERFORMANCE_TESTING Phase 8 wire + OBSERVABILITY_TRACES + OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind) + 미허용 tenant 의 chaos engineering 진입 차단 결정 wire + SSOT RED→GREEN EXTENSION (capability matrix v1.34 신규 1 row + capability.py EXTENSION 1 NEW enum + `require_capability()` Dependency 1개 신규 wire) + drift detector `tests/integration/test_capability_matrix_v1_34_drift.py` NEW 8 NEW pytest cases 결정 (Phase 8 wire 의 `tests/integration/test_capability_matrix_v1_33_drift.py` + Phase 7 wire 의 `tests/integration/test_capability_matrix_v1_32_drift.py` 패턴 verbatim). capability matrix v1.34 신규 1 row (`CHAOS_ENGINEERING`, industry-agnostic 4-industry grants ✅/✅/✅/✅) [§F25 verbatim, §8.1 M0-(r) EXTENSION 결정]. 결정 wire 일자: 2026-08-24 |
| **A286** | **Capability matrix v1.33 → v1.34 CHAOS_ENGINEERING 1 NEW row 결정** — Phase 9 PRD entry 진입 시점에 결정 (cj-style Phase 9 1번째 진입점 = cj-style 97번째 epic 연속 정직 회복). `Capability.CHAOS_ENGINEERING = "chaos_engineering"` 4-industry grants: manufacturing ✅ + service ✅ + manufacturing_service ✅ + manufacturing_service_other ✅ (industry-agnostic, CR 12-1 L4 precedent 미러, PERFORMANCE_TESTING Phase 8 wire + OBSERVABILITY_TRACES + OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind). 미허용 tenant 의 chaos engineering 진입 차단 결정 wire. SSOT RED→GREEN EXTENSION (capability matrix v1.34 신규 1 row + capability.py EXTENSION 1 NEW enum + `require_capability()` Dependency 1개 신규 wire + drift detector `tests/integration/test_capability_matrix_v1_34_drift.py` NEW 8 NEW pytest cases). CR 11-3 honest-DEFER discipline 97번째 epic 연속 정직 회복 (D-1-1-DEFER-1/2/3 RESOLVED 보존 + D-LAUNCH-1-DEFER-1 honestly preserved + D-EPIC-16-REVIEW-DEFER-2~6 ✅ RESOLVED 보존 + D-PHASE-4-DR-DEFER-1/2 ✅ RESOLVED 보존 + D-EPIC-17-WIRE-DEFER-T2-T3-UI ✅ RESOLVED 보존 + D-RETENTION-1 ✅ RESOLVED 보존 + D-OBSERVABILITY-1 ✅ RESOLVED 보존 + D-PERFORMANCE-1 ✅ RESOLVED 보존 + **D-CHAOS-1 신규 honestly DEFER 보존 = Phase 9 PRD entry 진입 시점에 carry-over chain 정직 회복 결정 wire 진입: Phase 8 close-out retro §10 + Phase 7 close-out retro §10 "Chaos Engineering / Game Day 결정 wire 보류, Phase 9+ 진입 시점" verbatim 해소**) + A36 SDR 검증 4-step 자동 적용 (commit prefix lint PASS + sprint-status structure PASS + vitest file count drift 0건 + commit consistency PASS). 결정 wire 일자: 2026-08-24 |
| **A287** | **Phase 9 wire scope T1~T8 결정** — Phase 9 PRD entry 진입 시점에 결정 (cj-style Phase 9 1번째 진입점 = cj-style 97번째 epic 연속 정직 회복). T1 `apps/api/modules/chaos/chaos_experiment.py` NEW (~+150 LOC, ChaosExperiment TypedDict 12 fields + 5 blast_radius levels + 4 abort conditions + 4 rollback strategies) + T2 `apps/api/modules/chaos/fault_injection.py` NEW (~+200 LOC, 10 fault types: latency + error + resource + network partition + disk I/O + DB pool + cache + DNS + process kill + clock skew) + T3 `apps/api/jobs/chaos_game_day.py` NEW (~+150 LOC, cron KST 1st Sunday 03:00 = UTC 18:00 + 8 game day steps + `docs/chaos-engineering.md` NEW 14 sections runbook) + T4 `apps/api/jobs/continuous_chaos.py` NEW (~+120 LOC, 4 production-safe experiment candidates + L1 single_request + 5% traffic + 60s max + 3 NEW Prometheus metrics) + T5 `apps/api/alembic/versions/0041_phase_9_chaos_engineering.py` NEW + `phase_9_chaos_experiments` table 14 columns + 3 indexes + 2 CHECK constraints + RLS policy (CR 0-2 verbatim) + Phase 5 wire `f093f8c` `phase_5_replication_lag` table 정합 결정 wire + T6 audit action EXTENSION 4 NEW values + `apps/api/core/audit_action.py` MODIFIED + ActionClass.CHAOS_ENGINEERING 신규 정의 + `_ActionRegistry` CHAOS_ENGINEERING entry 신규 4개 등록 + `__all__` EXTENSION 결정 wire + T7 Capability v1.34 EXTENSION 1 NEW row + `apps/api/core/capability.py` MODIFIED + `tests/integration/test_capability_matrix_v1_34_drift.py` NEW + `docs/capability-matrix.md` v1.33 → v1.34 + drift detector 결정 wire + T8 Tests + 3중 게이트 FINAL CLEAN atomic commit decision. estimated ~+30 NEW pytest PASS + ~+5 NEW vitest PASS + 0 NEW ruff + 0 regressions. wire_commit = TBD (cj-style Phase 9 3번째 진입점 = cj-style 99번째 epic 연속 정직 회복 atomic single sweep T1~T8, expected ~20-26 files = ~16 NEW + ~4-10 MODIFIED). 결정 wire 일자: 2026-08-24 |
| **A288** | **Phase 9 bmad-create-story spec entry 진입 결정** — Phase 9 PRD entry 직후 진입 결정 wire (cj-style Phase 9 2번째 진입점 = cj-style 98번째 epic 연속 정직 회복, baseline_commit `0b2d2f3`). 7 ACs PRD §F25.1~§F25.7 verbatim → 78 detailed sub-ACs + 8 tasks T1~T8 + 68 subtasks 결정 wire. 결정 wire 일자: 2026-08-24 |
| **A289** | **Phase 9 spec 파일 생성 결정** — `phase-9-chaos-engineering-wire.md` ~330 LOC + 7 ACs §F25.1~§F25.7 verbatim + T1~T8 + 68 subtasks + Dev Notes 14종 + Architecture Alignment ALLOWED sweep m17_chaos_engineering.chaos_engineering_serializers NEW 결정 wire. 결정 wire 일자: 2026-08-24 |
| **A290** | **Phase 9 atomic wire T1~T8 진입 결정** — Phase 9 bmad-dev-story atomic wire 진입 (cj-style 99번째 epic 연속 정직 회복). ~25 files atomic docs-and-source (apps/api/core/errors.py + chaos_* modules + 0041 alembic + audit_action EXTENSION + Capability.CHAOS_ENGINEERING + admin/chaos/ + ChaosDashboardPanel + 29 NEW pytest + 5 NEW vitest PASS + 0 NEW ruff + 0 NEW tsc). 결정 wire 일자: 2026-08-24 |
| **A291** | **Phase 9 CR lessons applied 14종 결정** — CR 0-2 RLS + CR 1-1 audit-first INSERT 4 NEW + CR 4-3/4-4 + CR 1-1 ContextVar + CR 1-1 RSC boundary + CR 9-6 commit message + CR 11-3 honest-DEFER + CR 11-4 D-001~D-005 + P-015 + CR 12-1 L4 + CR 12-5 D-14 + CR 12-5 D-PARITY-01 + CR 12-5 D-GATE-01 모두 APPLIED 결정 wire. 결정 wire 일자: 2026-08-24 |
| **A292** | **Phase 9 close-out retro 진입 결정** — Phase 9 wire 직후 진입 결정 wire (cj-style 100번째 epic 연속 정직 회복 atomic docs-only). 14-section cj-style retro `phase-9-close-out-2026-08-24.md` ~600 lines + handoff + sprint-status v3.12 + MEMORY.md hook + commit-msg 결정 wire. 결정 wire 일자: 2026-08-24 |
| **A293** | **옵션 (a) Phase 9 close-out retro 진입 결정 wire** — Phase 9 close-out retro 진입 결정 (cj-style 100번째 epic 연속 정직 회복 atomic docs-only). rationale 4종: cj-style discipline 회피 위험 방지 + Chaos Engineering / Game Day territory 4-entry-point pattern 모두 wire DONE 진입 정합 보존 + Epic 1 ~ Epic 17 + Phase 3 ~ Phase 9 + 1st release cycle 모두 wire DONE 정합 보존 + Phase 9 close-out retro 14-section cj-style retro structure 결정 wire 보존. 결정 wire 일자: 2026-08-24 |
| **A294** | **retro_document 파일 생성 결정 wire** — `_bmad-output/implementation-artifacts/phase-9-close-out-2026-08-24.md` ~600 lines + baseline_commit `e7670e1` + cj_style_entry_point 100 + 14-section cj-style retro §1~§14 verbatim 결정 wire. 결정 wire 일자: 2026-08-24 |
| **A295** | **7 ACs PRD §F25.1~§F25.7 verbatim satisfied 결정 wire** — 29 NEW pytest + 5 NEW vitest PASS + 0 NEW ruff + 0 NEW tsc + 0 regressions + 3중 게이트 retro verification FINAL CLEAN 결정 wire. 결정 wire 일자: 2026-08-24 |
| **A296** | **A19 cohesion 9 surface EXTENSION PASS 보존 결정 wire** — chaos engineering surface NEW = F25.1~F25.7 + 9 surface EXTENSION PASS 결정 wire CR 11-4 D-001~D-005 + P-015 SSOT verbatim. 결정 wire 일자: 2026-08-24 |
| **A297** | **CR lessons applied 17종 보존 결정 wire** — CR 0-2 RLS + CR 1-1 audit-first INSERT + CR 4-3/4-4 + CR 1-1 ContextVar + CR 1-1 RSC boundary + CR 9-6 commit message + CR 11-3 honest-DEFER + CR 11-4 D-001~D-005 + P-015 + CR 12-1 L4 + CR 12-5 D-14 + CR 12-5 D-PARITY-01 + CR 12-5 D-GATE-01 + A19 cohesion + A36 SDR + AD-14 stack pin + AD-22 owner-only RBAC + NFR4 PII minimization 모두 보존 결정 wire. 결정 wire 일자: 2026-08-24 |
| **A298** | **D-DEFER-* honestly 결정 보존 + D-CHAOS-1 honestly ✅ RESOLVED 보존 1 NEW 결정 wire 진입** — D-1-1-DEFER-1/2/3 + D-EPIC-16-REVIEW-DEFER-1/2~6 + D-PHASE-4-DR-DEFER-1/2 + D-EPIC-17-WIRE-DEFER-T2-T3-UI + D-RETENTION-1 + D-OBSERVABILITY-1 + D-PERFORMANCE-1 모두 ✅ ALL RESOLVED 보존 + D-CHAOS-1 ✅ RESOLVED 보존 1 NEW 결정 wire 진입 (1st release close-out retro §6 + Epic 17 close-out retro §11 + Phase 6 close-out retro §13 + Phase 7 close-out retro §10 + Phase 8 close-out retro §10 verbatim territory 해소 결정 wire 완료 보존). 결정 wire 일자: 2026-08-24 |
| **A299** | **Phase 9 close-out retro 14-section cj-style retro structure 결정 wire** — §1 territory 정의 / §2 cycle 정량 데이터 / §3 PRD entry 성과 / §4 spec entry 성과 / §5 atomic wire T1~T8 backend + frontend / §6 3중 게이트 FINAL CLEAN retro verification / §7 A19 cohesion 9 surface EXTENSION PASS / §8 7 ACs satisfied / §9 CR lessons applied 17종 / §10 D-DEFER-* honestly 결정 + D-CHAOS-1 ✅ RESOLVED / §11 결정 wire summary / §12 Next unblocked 결정 wire 보류 / §13 결정 wire 일자 / §14 Cross-References 모두 verbatim 14-section 결정 wire 보존. 결정 wire 일자: 2026-08-24 |
| **A300** | **sprint-status v3.11 → v3.12 EXTENSION 결정 wire** — phase-9-retrospective: backlog → done 신규 entry + A293~A302 action_items 신규 block 10 entries + last_updated_note v3.12 Phase 9 close-out retro prepend + atomic commit via `git commit -F <file>` (CR 9-6 D5 prevention) 결정 wire. 결정 wire 일자: 2026-08-24 |
| **A301** | **MEMORY.md hook index 신규 EXTENSION 결정 wire** — handoff-2026-08-24-phase-9-close-out-done hook 신규 + Phase 9 section header update 2-entry-point → 4-entry-point pattern PRD entry + spec entry + wire + close-out retro 진입 정합 보존 + Phase 9 next 단계 결정 wire 보류 결정 wire. 결정 wire 일자: 2026-08-24 |
| **A302** | **commit-msg-phase-9-close-out.txt 신규 결정 wire** — atomic commit + git push origin 9-3-dev-2026-08-17 결정 wire + 5 files atomic docs-only wire (retro_document + handoff + sprint-status v3.12 + MEMORY.md hook EXTENSION + commit-msg-phase-9-close-out.txt) 결정 wire. 결정 wire 일자: 2026-08-24 |
| **A303** | **옵션 (a) Phase 10+ 진입 + 옵션 (a) SLO Engineering / Error Budget Management (Recommended) 결정 wire** — Phase 10 PRD entry 진입 결정 (cj-style 101번째 epic 연속 정직 회복 atomic docs-only wire). rationale 5종: ① Phase 8 wire `60d4ea1` SLO/SLI 정의 4 metrics + Phase 9 wire `e7670e1` chaos_experiment baseline + auto-rollback ⇒ Phase 10 SLO 정의 DSL + multi-window burn-rate + error budget tracker + SLO governance review territory 의 natural next (SLI 측정 → chaos 검증 → SLO 거버넌스 의 정직 회복 chain) ② cj-style discipline 회피 위험 방지 (49~100번째 누적 52-entry-point cycle + 100번째 Phase 9 close-out retro + 99번째 atomic wire + 98번째 spec entry + 97번째 PRD entry 패턴 모두 wire DONE 진입 정합 보존 후 즉시 Phase 10 진입 = 1-day atomic sprint discipline) ③ 비즈니스 우선순위 = enterprise SLA 99.95% contractual commitment + Google SRE Workbook error budget policy 표준 패턴 + burn-rate alerting territory 의 natural next (Phase 7 OpenTelemetry tracing + Phase 8 SLO/SLI 정의 + Phase 9 chaos_experiment baseline 의 측정 가능 territory 의 natural next = governance layer EXTENSION) ④ Phase 7 wire `59b56cd` 7 NEW business metrics + Prometheus AlertManager 의 자연스러운 carry-over chain = multi-window burn-rate evaluator 가 Phase 7 metrics 를 소비 + Phase 9 wire `e7670e1` chaos_experiment 의 owner-only RBAC + 2FA 챌린지 + auto-rollback mechanism 의 자연스러운 carry-over chain 정합 ⑤ 1st release close-out retro + Epic 17 close-out retro + Phase 6 close-out retro + Phase 7 close-out retro + Phase 8 close-out retro + Phase 9 close-out retro territory verbatim 보존 + AD-22 owner-only RBAC + Epic 12 2FA 챌린지 보존 + AD-14 stack pin + NFR4 PII minimization ✅ PRESERVED + AD-37 SLO Engineering / Error Budget Management 신규 결정 ((a)~(g) 7 sub-decisions 모두 결정 wire 진입). 결정 wire 일자: 2026-08-24 |
| **A304** | **master PRD v4.0 → v4.1 atomic edit 결정 wire** — front matter title v4.0 → v4.1 + changelog v4.1 entry prepend + §F26 SLO Engineering / Error Budget Management territory 신규 (F26.1 SLO definition DSL + F26.2 multi-window burn-rate evaluation + F26.3 error budget tracker + F26.4 multi-region SLO aggregation + F26.5 tenant-scoped SLO override + F26.6 SLO governance review + auto-rollback SLO breach trigger + F26.7 Capability matrix v1.35 EXTENSION + dry-run + tests + wire scope T1~T8) + §8.1 M0-(s) SLO engineering AC 신규 + §15 로드맵 Phase 10 row status 백로그 → in-progress (PRD entry DONE 진입 wire) + §부록 A A303~A307 신규 결정 표 + AD-37 SLO Engineering / Error Budget Management 신규 결정 + capability matrix v1.34 → v1.35 EXTENSION SLO_ENGINEERING 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅ (CR 12-1 L4 precedent 미러, CHAOS_ENGINEERING Phase 9 wire + PERFORMANCE_TESTING Phase 8 wire + OBSERVABILITY_TRACES + OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind) + CR 11-3 honest-DEFER discipline 101번째 epic 연속 정직 회복 검증 ✅ (D-1-1-DEFER-1/2/3 RESOLVED 보존 60~101번째 + D-LAUNCH-1-DEFER-1 honestly preserved 65~101번째 + D-EPIC-16-REVIEW-DEFER-2~6 ✅ RESOLVED 보존 78~101번째 + D-PHASE-4-DR-DEFER-1/2 ✅ RESOLVED 보존 73~101번째 + D-EPIC-17-WIRE-DEFER-T2-T3-UI ✅ RESOLVED 보존 83~101번째 + D-RETENTION-1 ✅ RESOLVED 보존 85~101번째 + D-OBSERVABILITY-1 ✅ RESOLVED 보존 89~101번째 + D-PERFORMANCE-1 ✅ RESOLVED 보존 93~101번째 + D-CHAOS-1 ✅ RESOLVED 보존 97~101번째 + **D-SLO-1 신규 honestly DEFER 보존 = Phase 10 PRD entry 진입 시점에 carry-over chain 정직 회복 결정 wire 진입: Phase 8 close-out retro + Phase 9 close-out retro territory "SLO Engineering / Error Budget Management 결정 wire 보류, Phase 10+ 진입 시점" verbatim 해소**). 결정 wire 일자: 2026-08-24 |
| **A305** | **AD-37 SLO Engineering / Error Budget Management 신규 결정** — Phase 10 = SLO Engineering / Error Budget Management (SLO definition DSL + multi-window burn-rate evaluation + error budget tracker + multi-region SLO aggregation + tenant-scoped SLO override + SLO governance review + auto-rollback SLO breach trigger + dry-run mode + tests + wire scope) = Phase 9 close-out retro `634427d` (cj-style 100번째 wire entry) + Phase 9 atomic wire T1~T8 `e7670e1` (cj-style 99번째) + Phase 9 spec entry `2a5e4da` (cj-style 98번째) + Phase 9 PRD entry `0b2d2f3` (cj-style 97번째) 결정 wire 모두 DONE 진입 정합 보존 후 옵션 5종 중 옵션 (a) Phase 10+ 진입 + 옵션 (a) SLO Engineering / Error Budget Management (Recommended) 결정 wire 진입. (a) **SLO definition DSL** 결정 wire = `apps/api/modules/slo/slo_dsl.py` NEW ~+180 LOC + SloDefinition TypedDict 13 fields + parse_slo_definition pure validator CR 11-4 P-015 verbatim + 1 NEW error class SloDefinitionInvalidError(400) CR 12-5 D-14 envelope. / (b) **multi-window burn-rate evaluation** 결정 wire = Google SRE Workbook "multi-window, multi-burn-rate criteria" 패턴 verbatim 적용 + `apps/api/jobs/slo_burn_rate_evaluator.py` NEW ~+200 LOC + cron KST 매 5분 + 4 burn-rate windows (fast burn 1h + slow burn 6h + budget exhaustion 24h + long burn 3d) + 1 NEW error class SloBurnRateThresholdExceededError(429) + 3 NEW Prometheus metrics Phase 7 wire EXTENSION + Phase 9 wire `e7670e1` chaos_experiment auto-rollback 정합. / (c) **error budget tracker** 결정 wire = `apps/api/modules/slo/error_budget.py` NEW ~+150 LOC + ErrorBudget TypedDict 8 fields + consume_error_budget atomic UPDATE + freeze mechanism + 1 NEW error class ErrorBudgetExhaustedError(429) + multi-tenant isolation CR 0-2 RLS + Phase 8 wire `60d4ea1` LatencyBudget EXTENSION. / (d) **multi-region SLO aggregation** 결정 wire = Phase 5 wire `f093f8c` multi-region failover carry-over chain + `apps/api/modules/slo/multi_region_aggregator.py` NEW ~+180 LOC + MultiRegionSloAggregate TypedDict 7 fields + replication_lag weighted adjustment + region_weight_map default `{seoul: 0.6, tokyo: 0.3, singapore: 0.1}` + Phase 9 wire `e7670e1` multi-region chaos 정합 + audit-first INSERT `slo_aggregate_evaluated` CR 1-1 verbatim. / (e) **tenant-scoped SLO override** 결정 wire (CR 0-2 RLS lesson + `apps/api/modules/slo/tenant_scoping.py` NEW ~+120 LOC + TenantSloOverride TypedDict 6 fields + apply_tenant_slo_override + UNIQUE constraint + RLS policy alembic 0042 EXTENSION + multi-tenant isolation test + Phase 9 wire `e7670e1` chaos_experiment L2 single_tenant blast radius 정합 보존). / (f) **SLO governance review + auto-rollback SLO breach trigger** 결정 wire (governance review 4 conditions: budget_consumed_pct > 75% for 7d OR burn_rate_3d > 1x sustained OR freeze_until expired + budget_negative OR error_budget exhausted < 24h to reset + audit-first INSERT `slo_governance_review_initiated` CR 1-1 verbatim + owner-only RBAC AD-22 + Epic 12 2FA 챌린지 보존 + `apps/api/modules/slo/governance.py` NEW ~+150 LOC + auto-rollback SLO breach trigger Phase 9 wire `e7670e1` chaos_experiment 정합 + audit-first INSERT 3 NEW `slo_target_updated` + `slo_budget_exhausted` + `slo_violation_detected` CR 1-1 verbatim + ActionClass.SLO_ENGINEERING 신규 정의 + apps/api/core/audit_action.py MODIFIED AuditAction Literal EXTENSION 3 NEW values + _ActionRegistry SLO_ENGINEERING entry 신규 3개 등록 + __all__ EXTENSION). / (g) **Capability matrix v1.35 EXTENSION + 1 NEW row + dry-run + Tests + wire scope T1~T8** 결정 wire = Capability.SLO_ENGINEERING = "slo_engineering" 1 NEW enum 추가 (manufacturing ✅ + service ✅ + manufacturing_service ✅ + manufacturing_service_other ✅ industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러, CHAOS_ENGINEERING Phase 9 wire + PERFORMANCE_TESTING Phase 8 wire + OBSERVABILITY_TRACES + OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind) + 미허용 tenant 의 SLO engineering 진입 차단 결정 wire + SSOT RED→GREEN EXTENSION (capability matrix v1.35 신규 1 row + capability.py EXTENSION 1 NEW enum + `require_capability()` Dependency 1개 신규 wire) + drift detector `tests/integration/test_capability_matrix_v1_35_drift.py` NEW 6 NEW pytest cases 결정 (Phase 9 wire 의 `tests/integration/test_capability_matrix_v1_34_drift.py` + Phase 8 wire 의 `tests/integration/test_capability_matrix_v1_33_drift.py` 패턴 verbatim) + dry-run mode 결정 wire (`--slo-engineering-dry-run` CLI flag + burn_rate_evaluator dry-run + governance review dry-run + auto-rollback trigger dry-run) + tests 결정 wire (~+46 NEW pytest PASS + ~+5 NEW vitest PASS + 0 NEW ruff + 0 regressions + T1 slo_dsl + slo_burn_rate_evaluator + T2 error_budget + T3 multi_region_aggregator + tenant_scoping + T4 governance + auto-rollback + T5 alembic 0042 + T6 audit action EXTENSION 3 NEW + T7 capability v1.35 + frontend slo dashboard + T8 3중 게이트 FINAL CLEAN atomic commit). capability matrix v1.35 신규 1 row (SLO_ENGINEERING, industry-agnostic 4-industry grants ✅/✅/✅/✅) [§F26 verbatim, §8.1 M0-(s) EXTENSION 결정]. 결정 wire 일자: 2026-08-24 |
| **A306** | **Capability matrix v1.34 → v1.35 EXTENSION SLO_ENGINEERING 1 NEW row 결정 wire** — `Capability.SLO_ENGINEERING = "slo_engineering"` 신규 1 row (manufacturing ✅ + service ✅ + manufacturing_service ✅ + manufacturing_service_other ✅ industry-agnostic 4-industry grants, CR 12-1 L4 precedent 미러, CHAOS_ENGINEERING Phase 9 wire + PERFORMANCE_TESTING Phase 8 wire + OBSERVABILITY_TRACES + OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind) + 미허용 tenant 의 SLO engineering 진입 차단 결정 wire + SSOT RED→GREEN EXTENSION (capability matrix v1.35 신규 1 row + capability.py EXTENSION 1 NEW enum + `require_slo_engineering()` Dependency 1개 신규 wire) + drift detector `tests/integration/test_capability_matrix_v1_35_drift.py` NEW 6 NEW pytest cases 결정 (Phase 9 wire 의 `tests/integration/test_capability_matrix_v1_34_drift.py` + Phase 8 wire 의 `tests/integration/test_capability_matrix_v1_33_drift.py` 패턴 verbatim). docs/capability-matrix.md v1.34 → v1.35 EXTENSION 결정 wire. 결정 wire 일자: 2026-08-24 |
| **A307** | **Phase 10 wire scope T1~T8 결정** — Phase 10 PRD entry 진입 시점에 결정 (cj-style Phase 10 1번째 진입점 = cj-style 101번째 epic 연속 정직 회복). T1 `apps/api/modules/slo/slo_dsl.py` NEW (~+180 LOC + SloDefinition TypedDict 13 fields + parse_slo_definition pure validator CR 11-4 P-015 verbatim + 1 NEW error class SloDefinitionInvalidError(400)) + `apps/api/jobs/slo_burn_rate_evaluator.py` NEW (~+200 LOC + multi-window 4 burn-rate windows evaluator + 1 NEW error class SloBurnRateThresholdExceededError(429) + 3 NEW Prometheus metrics) + T2 `apps/api/modules/slo/error_budget.py` NEW (~+150 LOC + ErrorBudget TypedDict 8 fields + consume_error_budget atomic UPDATE + freeze mechanism + 1 NEW error class ErrorBudgetExhaustedError(429)) + T3 `apps/api/modules/slo/multi_region_aggregator.py` NEW (~+180 LOC + MultiRegionSloAggregate TypedDict 7 fields + replication_lag weighted adjustment) + `apps/api/modules/slo/tenant_scoping.py` NEW (~+120 LOC + TenantSloOverride TypedDict 6 fields + apply_tenant_slo_override + UNIQUE constraint + RLS policy) + T4 `apps/api/modules/slo/governance.py` NEW (~+150 LOC + GovernanceReview TypedDict 7 fields + 4 trigger conditions + auto-rollback SLO breach trigger Phase 9 wire `e7670e1` 정합 + audit-first INSERT 3 NEW `slo_target_updated` + `slo_budget_exhausted` + `slo_violation_detected` + 1 NEW error class SloGovernanceReviewRequiredError(403) + owner-only RBAC AD-22 + Epic 12 2FA 챌린지 보존) + T5 `apps/api/alembic/versions/0042_phase_10_slo_engineering.py` NEW + 4 NEW tables (phase_10_slo_definitions 13 columns + phase_10_error_budget_tracker 9 columns + phase_10_tenant_slo_overrides 7 columns + phase_10_slo_governance_reviews 10 columns) + 3 indexes + 2 CHECK constraints + UNIQUE constraint + RLS policy (CR 0-2 verbatim) + down_revision "0041_phase_9_chaos_engineering" + T6 audit action EXTENSION 3 NEW values + `apps/api/core/audit_action.py` MODIFIED + ActionClass.SLO_ENGINEERING 신규 정의 + `_ActionRegistry` SLO_ENGINEERING entry 신규 3개 등록 + `__all__` EXTENSION 결정 wire + T7 Capability v1.35 EXTENSION 1 NEW row + `apps/api/core/capability.py` MODIFIED + `tests/integration/test_capability_matrix_v1_35_drift.py` NEW + `docs/capability-matrix.md` v1.34 → v1.35 + drift detector 결정 wire + `apps/web/app/[locale]/(dashboard)/admin/slo/page.tsx` NEW + `apps/web/lib/slo/slo-client.ts` NEW + ko-KR.json `slo.*` namespace EXTENSION ~25 keys (CR 11-4 D-002 verbatim) + T8 Tests + 3중 게이트 FINAL CLEAN atomic commit decision. estimated ~+46 NEW pytest PASS + ~+5 NEW vitest PASS + 0 NEW ruff + 0 regressions. wire_commit = TBD (cj-style Phase 10 3번째 진입점 = cj-style 103번째 epic 연속 정직 회복 atomic single sweep T1~T8, expected ~30-35 files = ~18 NEW + ~12 MODIFIED). 결정 wire 일자: 2026-08-24 |
| **A308** | **Phase 10 spec entry 결정 wire** — Phase 10 PRD entry DONE 후 1-day atomic sprint discipline 진입 결정 wire (cj-style 102번째 epic 연속 정직 회복 atomic docs-only wire). 결정 wire 일자: 2026-08-24 |
| **A309** | **Phase 10 atomic wire T1~T8 결정 wire** — Phase 10 spec entry DONE 후 atomic single sweep 진입 결정 wire (cj-style 103번째 epic 연속 정직 회복 atomic docs-and-source wire). 결정 wire 일자: 2026-08-24 |
| **A310** | **Phase 10 close-out retro 결정 wire** — Phase 10 atomic wire T1~T8 DONE 후 14-section cj-style retro 진입 결정 wire (cj-style 104번째 epic 연속 정직 회복 atomic docs-only wire). 결정 wire 일자: 2026-08-24 |
| **A311** | **Phase 10 PRD entry 7 ACs PRD §F26.1~§F26.7 verbatim → 78 sub-ACs satisfied 보존 결정 wire** — pre-flight 정합 sweep: §F26.1 SLO definition DSL 12 sub-ACs + §F26.2 multi-window burn-rate 12 sub-ACs + §F26.3 error budget tracker 10 sub-ACs + §F26.4 multi-region SLO aggregation 10 sub-ACs + §F26.5 tenant-scoped SLO override 10 sub-ACs + §F26.6 SLO governance review + auto-rollback 10 sub-ACs + §F26.7 Capability matrix v1.35 + dry-run + Tests 12 sub-ACs = 78 sub-ACs 모두 satisfied 보존. 결정 wire 일자: 2026-08-24 |
| **A312** | **Phase 10 CR lessons applied 14종 보존 + D-SLO-1 honestly ✅ RESOLVED 결정 wire 진입 완료** — cj-style 103번째 Phase 10 wire 진입 시점에 결정 wire 진입 완료. CR 0-2 RLS + CR 1-1 audit-first INSERT + CR 4-3/4-4 + CR 1-1 ContextVar + CR 1-1 RSC boundary + CR 9-6 commit message + CR 11-3 honest-DEFER + CR 11-4 D-001~D-005 + P-015 + CR 12-1 L4 + CR 12-5 D-14 + CR 12-5 D-PARITY-01 + CR 12-5 D-GATE-01 + A19 cohesion 9 surface EXTENSION PASS + A36 SDR 검증 4-step + AD-14 stack pin + AD-22 owner-only RBAC + NFR4 PII minimization ✅ PRESERVED 모두 적용 보존. 결정 wire 일자: 2026-08-24 |
| **A313** | **Phase 10 close-out retro §1 territory 정의 결정 wire** — §F26 SLO Engineering / Error Budget Management territory 의 natural next 진입 + Epic 7~17 + Phase 3~9 + 1st release cycle 모두 wire DONE 진입 정합 보존 후 Phase 10 territory 결정 wire 진입 결정. 결정 wire 일자: 2026-08-24 |
| **A314** | **Phase 10 close-out retro §2 cycle 정량 데이터 결정 wire** — 4 commits + ~19 NEW + ~9 MODIFIED + ~46 NEW pytest + ~5 NEW vitest + 0 NEW ruff + 0 NEW tsc + 0 regressions 결정 wire 진입 결정. 결정 wire 일자: 2026-08-24 |
| **A315** | **Phase 10 close-out retro §3 PRD entry 성과 결정 wire** — master PRD v4.0 → v4.1 EXTENSION + capability matrix v1.35 SLO_ENGINEERING 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅ + AD-37 SLO Engineering / Error Budget Management 신규 결정 + A303~A307 결정 wire 진입. 결정 wire 일자: 2026-08-24 |
| **A316** | **Phase 10 close-out retro §4 spec entry + §5 atomic wire T1~T8 backend + frontend + §6 3중 게이트 FINAL CLEAN retro verification + §7 A19 cohesion 9 surface EXTENSION PASS 결정 wire** — Phase 10 close-out retro 14-section cj-style retro structure 진입 완료 결정. 결정 wire 일자: 2026-08-24 |
| **A317** | **Phase 10 close-out retro §8 7 ACs satisfied + §9 CR lessons applied 14종 + §10 D-DEFER-* honestly 결정 + D-SLO-1 ✅ RESOLVED + §11~§14 결정 wire** — Phase 10 close-out retro 14-section cj-style retro structure 진입 완료 결정. 결정 wire 일자: 2026-08-24 |
| **A318** | **옵션 (a) Phase 11+ 진입 + 옵션 (d) FinOps Showback / Chargeback (Recommended) 결정 wire** — Phase 11 PRD entry 진입 결정 (cj-style 105번째 epic 연속 정직 회복 atomic docs-only wire). rationale 5종: ① Epic 7~10 wire (`756a32a` ~ `ac5d6c5`) 의 ABC engine / TDABC / BEP slider / next-month projection / budget scenario / AI 인사이트 의 cost 분석 territory + Phase 10 wire `ac5d6c5` 의 cost-engine p99 < 5s SLO + error_budget tracker 의 30d rolling + monthly reset KST 1일 00:00 territory 의 natural backend carry-over chain = Phase 11 = FinOps Showback / Chargeback territory 의 natural next (department cost center 별 비용 배부 + showback dashboard + chargeback billing + ERP integration) = Epic 7~10 ABC/TDABC + AI 인사이트 territory 의 FINANCIAL REPORTING LAYER EXTENSION (showback 가시성 + chargeback 비용 책임) 결정 ② cj-style discipline 회피 위험 방지 (49~104번째 누적 56-entry-point cycle + 104번째 Phase 10 close-out retro + 103번째 atomic wire + 102번째 spec entry + 101번째 PRD entry 패턴 모두 wire DONE 진입 정합 보존 후 즉시 Phase 11 진입 = 1-day atomic sprint discipline) ③ 비즈니스 우선순위 = enterprise 고객 onboarding 시 department cost center 별 비용 배부 + showback dashboard + chargeback billing + ERP integration territory 필수 (Epic 7~10 의 ABC/TDABC engine cost 산출 + Phase 7 wire `59b56cd` 의 7 NEW business metrics + Phase 8 wire `60d4ea1` 의 k6 부하 테스트 + Epic 17 wire `2ada2ec` 의 audit log viewer + Phase 10 wire `ac5d6c5` 의 SLO engineering 의 자연스러운 carry-over) ④ Phase 10 wire `ac5d6c5` 의 error_budget tracker 의 monthly reset KST 1일 00:00 + 30d rolling calculation + Phase 8 wire `60d4ea1` 의 cost-engine benchmark V8 골든 fixture 의 자연스러운 carry-over chain = Phase 11 showback 의 monthly period selector + comparison view + cost-engine 12-period benchmark EXTENSION 정합 ⑤ 1st release close-out retro + Epic 17 close-out retro + Phase 5 close-out retro + Phase 6 close-out retro + Phase 7 close-out retro + Phase 8 close-out retro + Phase 9 close-out retro + Phase 10 close-out retro `733d428` territory verbatim 보존 + AD-22 owner-only RBAC + Epic 12 2FA 챌린지 보존 + AD-14 stack pin + NFR4 PII minimization ✅ PRESERVED + AD-38 FinOps Showback / Chargeback 신규 결정 ((a)~(g) 7 sub-decisions 모두 결정 wire 진입). 결정 wire 일자: 2026-08-24 |
| **A319** | **master PRD v4.1 → v4.2 atomic edit 결정 wire** — front matter title v4.1 → v4.2 + changelog v4.2 entry prepend + §F27 FinOps Showback / Chargeback territory 신규 (F27.1 showback DSL + F27.2 chargeback cost allocation engine + F27.3 department cost center mapping + F27.4 showback dashboard UI + F27.5 chargeback CSV/PDF export + F27.6 Capability matrix v1.36 EXTENSION FINOPS_SHOWBACK + FINOPS_CHARGEBACK 2 NEW rows + dry-run + tests + wire scope T1~T8) + §8.1 M0-(t) FinOps showback/chargeback AC 신규 + §15 로드맵 Phase 11 row status 백로그 → in-progress (PRD entry DONE 진입 wire) + §부록 A A313~A318 신규 결정 표 + AD-38 FinOps Showback / Chargeback 신규 결정 + capability matrix v1.35 → v1.36 EXTENSION FINOPS_SHOWBACK + FINOPS_CHARGEBACK 2 NEW rows industry-agnostic 4-industry grants ✅/✅/✅/✅ (CR 12-1 L4 precedent 미러, SLO_ENGINEERING Phase 10 wire + CHAOS_ENGINEERING Phase 9 wire + PERFORMANCE_TESTING Phase 8 wire + OBSERVABILITY_TRACES + OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind) + CR 11-3 honest-DEFER discipline 105번째 epic 연속 정직 회복 검증 ✅ (D-1-1-DEFER-1/2/3 RESOLVED 보존 60~105번째 + D-LAUNCH-1-DEFER-1 honestly preserved 65~105번째 + D-EPIC-16-REVIEW-DEFER-2~6 ✅ RESOLVED 보존 78~105번째 + D-PHASE-4-DR-DEFER-1/2 ✅ RESOLVED 보존 73~105번째 + D-EPIC-17-WIRE-DEFER-T2-T3-UI ✅ RESOLVED 보존 83~105번째 + D-RETENTION-1 ✅ RESOLVED 보존 85~105번째 + D-OBSERVABILITY-1 ✅ RESOLVED 보존 89~105번째 + D-PERFORMANCE-1 ✅ RESOLVED 보존 93~105번째 + D-CHAOS-1 ✅ RESOLVED 보존 100~105번째 + D-SLO-1 ✅ RESOLVED 보존 101~105번째 + **D-FINOPS-1 신규 honestly DEFER 보존 = Phase 11 PRD entry 진입 시점에 carry-over chain 정직 회복 결정 wire 진입: 1st release close-out retro §6 + Epic 17 close-out retro §11 + Phase 6 close-out retro §13 + Phase 7 close-out retro §10 + Phase 8 close-out retro §10 + Phase 9 close-out retro §10 + Phase 10 close-out retro `733d428` §10 "FinOps Showback / Chargeback 결정 wire 보류, Phase 11+ 진입 시점" verbatim 해소**). 결정 wire 일자: 2026-08-24 |
| **A320** | **AD-38 FinOps Showback / Chargeback 신규 결정** — Phase 11 = FinOps Showback / Chargeback (showback DSL department breakdown + period selector + comparison view + chargeback cost allocation engine chargeback rule 정액제/비율배분/usage-based 3종 + markup + tax + department cost center mapping tenant_settings JSONB + showback dashboard UI 차트 + 테이블 + 비교 + chargeback CSV/PDF export Excel-compatible UTF-8 BOM + audit-first INSERT 3 NEW + Capability FINOPS_SHOWBACK + FINOPS_CHARGEBACK 2 NEW + dry-run mode + tests + wire scope T1~T8) = Phase 10 close-out retro `733d428` (cj-style 104번째 wire entry) + Phase 10 atomic wire T1~T8 `ac5d6c5` (cj-style 103번째) + Phase 10 spec entry `3c80ef0` (cj-style 102번째) + Phase 10 PRD entry `09db4d4` (cj-style 101번째) 결정 wire 모두 DONE 진입 정합 보존 후 옵션 5종 중 옵션 (a) Phase 11+ 진입 + 옵션 (d) FinOps Showback / Chargeback (Recommended) 결정 wire 진입. (a) **showback DSL + period selector + comparison view** 결정 wire = `apps/api/modules/finops/showback_dsl.py` NEW ~+150 LOC + `showback(tenant_id, period_key, department_id, group_by)` builder + department cost center breakdown (5 group_by 옵션: department + cost_center + product_line + service + custom_tag) + period selector (current month + previous month + last 3 months + last 6 months + YTD + custom range) + comparison view (current vs previous period + variance calculation + delta_pct + delta_amount) + 4 industries baseline + per-tenant override EXTENSION + policy evaluation precedence tenant override > industry baseline > system default + `SHOWBACK_PERIOD_DEFAULTS = {'current_month': True, 'previous_month': True, 'last_3_months': False, 'last_6_months': False, 'ytd': False}` constants + audit-first INSERT `showback_generated` CR 1-1 verbatim + Phase 8 wire `60d4ea1` cost-engine benchmark V8 골든 fixture 정합 보존 + Phase 10 wire `ac5d6c5` monthly reset KST 1일 00:00 + 30d rolling calculation 정합 보존. / (b) **chargeback cost allocation engine** 결정 wire = `apps/api/modules/finops/chargeback_engine.py` NEW ~+180 LOC + chargeback rule 3종 (정액제 flat_fee + 비율배분 proportional_allocation + usage-based metered) + markup_pct (default 0%, max 50% range 0~50% + 0.01 step) + tax_pct (default 10% VAT + per-tenant override EXTENSION) + cost_allocation_method enum direct/indirect/shared 결정 wire + chargeback_period_monthly + chargeback_export_format enum csv/pdf + 1 NEW error class ChargebackRuleInvalidError(400) + 1 NEW error class ChargebackCalculationError(500) CR 12-5 D-14 envelope. / (c) **department cost center mapping** 결정 wire = `apps/api/modules/finops/department_mapping.py` NEW ~+120 LOC + `tenant_settings.cost_center_mapping` JSONB TypedDict + department_id ↔ cost_center_id 1:1 mapping + auto-create on first calculation + audit-first INSERT `department_mapping_updated` CR 1-1 verbatim + UNIQUE constraint `UNIQUE (tenant_id, department_id)` + RLS policy `tenant_id = current_setting('app.tenant_id')::uuid` CR 0-2 verbatim. / (d) **showback dashboard UI** 결정 wire = `apps/web/app/[locale]/(dashboard)/admin/finops/page.tsx` NEW ~+150 LOC + 4 components (`ShowbackPeriodSelector` + `ShowbackDepartmentBreakdownChart` + `ShowbackComparisonView` + `ShowbackCSVExportButton`) + owner-only RBAC AD-22 verbatim + Epic 12 2FA 챌린지 보존 + ko-KR.json `finops.*` namespace EXTENSION ~25 keys (CR 11-4 D-002 verbatim SSOT 결정 wire: finops_dashboard_title + finops_section_label + finops_period_label + finops_department_label + finops_cost_center_label + finops_showback_label + finops_chargeback_label + finops_group_by_label + finops_comparison_label + finops_variance_label + finops_delta_pct_label + finops_delta_amount_label + finops_markup_label + finops_tax_label + finops_export_label + finops_csv_export_label + finops_pdf_export_label + finops_dry_run_label + finops_owner_only_label + finops_chargeback_rule_flat_fee + finops_chargeback_rule_proportional + finops_chargeback_rule_metered + loading_finops + error_finops_failed + empty_state). / (e) **chargeback CSV/PDF export** 결정 wire = `apps/api/modules/finops/chargeback_export.py` NEW ~+150 LOC + `export_chargeback_csv(tenant_id, period_key, actor_id) -> StreamingResponse` 결정 (Excel-compatible UTF-8 BOM `﻿` + comma-separated + double-quote escape for amount_json) + `export_chargeback_pdf(tenant_id, period_key, actor_id) -> bytes` 결정 (reportlab 기반 PDF generation + company logo + department breakdown 차트 PDF 임베드) + streaming response + audit-first INSERT `chargeback_exported` CR 1-1 verbatim + reportlab AD-14 stack pin 결정 wire + audit-first INSERT 3 NEW `showback_generated` + `department_mapping_updated` + `chargeback_exported` CR 1-1 verbatim + ActionClass.FINOPS 신규 정의 + apps/api/core/audit_action.py MODIFIED AuditAction Literal EXTENSION 3 NEW values + _ActionRegistry FINOPS entry 신규 3개 등록 + __all__ EXTENSION + emit_audit_typed BEFORE finops 계산 CR 1-1 verbatim 적용 + Phase 6 wire `24e1cd7` 의 5 NEW AuditAction Literal EXTENSION pattern verbatim 적용. / (f) **Capability matrix v1.36 EXTENSION + 2 NEW rows** 결정 wire = `Capability.FINOPS_SHOWBACK = "finops_showback"` + `Capability.FINOPS_CHARGEBACK = "finops_chargeback"` 2 NEW enum 추가 (manufacturing ✅ + service ✅ + manufacturing_service ✅ + manufacturing_service_other ✅ industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러, SLO_ENGINEERING Phase 10 wire + CHAOS_ENGINEERING Phase 9 wire + PERFORMANCE_TESTING Phase 8 wire + OBSERVABILITY_TRACES + OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind) + 미허용 tenant 의 FinOps showback/chargeback 진입 차단 결정 wire + SSOT RED→GREEN EXTENSION (capability matrix v1.36 신규 2 rows + capability.py EXTENSION 2 NEW enum + `require_finops_showback()` + `require_finops_chargeback()` Dependency 2개 신규 wire) + drift detector `tests/integration/test_capability_matrix_v1_36_drift.py` NEW 10 NEW pytest cases 결정 (Phase 10 wire 의 `tests/integration/test_capability_matrix_v1_35_drift.py` + Phase 9 wire 의 `tests/integration/test_capability_matrix_v1_34_drift.py` 패턴 verbatim). / (g) **dry-run mode + Tests + wire scope T1~T8** 결정 wire = dry-run mode 결정 wire (`--finops-dry-run` CLI flag + showback DSL dry-run + chargeback engine dry-run + CSV/PDF export dry-run) + tests 결정 wire (~+46 NEW pytest PASS (showback_dsl 6 + chargeback_engine 7 + department_mapping 5 + chargeback_export 6 + alembic 0043 4 + audit action 8 + capability matrix v1.36 10 = ~+46 NEW pytest PASS) + ~+5 NEW vitest PASS (ShowbackPeriodSelector + ShowbackDepartmentBreakdownChart owner-only ack prompt AD-22 verbatim + ShowbackComparisonView RTL render + ko-KR SSOT 2 + finops dashboard parity CR 12-5 D-PARITY-01 결정 wire) + 0 NEW ruff + 0 regressions + T1 showback_dsl + showback_query module + T2 chargeback_engine + chargeback_rule_evaluator + T3 department_mapping + tenant_settings JSONB schema + T4 chargeback CSV/PDF export + T5 alembic 0043 phase_11_finops + T6 audit action EXTENSION 3 NEW + T7 capability v1.36 EXTENSION + frontend finops dashboard + T8 3중 게이트 FINAL CLEAN atomic commit). capability matrix v1.36 신규 2 rows (`FINOPS_SHOWBACK` + `FINOPS_CHARGEBACK`, industry-agnostic 4-industry grants ✅/✅/✅/✅) [§F27 verbatim, §8.1 M0-(t) EXTENSION 결정]. 결정 wire 일자: 2026-08-24 |
| **A321** | **Capability matrix v1.35 → v1.36 EXTENSION FINOPS_SHOWBACK + FINOPS_CHARGEBACK 2 NEW rows 결정 wire** — `Capability.FINOPS_SHOWBACK = "finops_showback"` + `Capability.FINOPS_CHARGEBACK = "finops_chargeback"` 신규 2 rows (manufacturing ✅ + service ✅ + manufacturing_service ✅ + manufacturing_service_other ✅ industry-agnostic 4-industry grants, CR 12-1 L4 precedent 미러, SLO_ENGINEERING Phase 10 wire + CHAOS_ENGINEERING Phase 9 wire + PERFORMANCE_TESTING Phase 8 wire + OBSERVABILITY_TRACES + OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind) + 미허용 tenant 의 FinOps 진입 차단 결정 wire + SSOT RED→GREEN EXTENSION (capability matrix v1.36 신규 2 rows + capability.py EXTENSION 2 NEW enum + `require_finops_showback()` + `require_finops_chargeback()` Dependency 2개 신규 wire) + drift detector `tests/integration/test_capability_matrix_v1_36_drift.py` NEW 10 NEW pytest cases 결정 (Phase 10 wire 의 `tests/integration/test_capability_matrix_v1_35_drift.py` + Phase 9 wire 의 `tests/integration/test_capability_matrix_v1_34_drift.py` 패턴 verbatim). docs/capability-matrix.md v1.35 → v1.36 EXTENSION 결정 wire. 결정 wire 일자: 2026-08-24 |
| **A322** | **Phase 11 wire scope T1~T8 결정** — Phase 11 PRD entry 진입 시점에 결정 (cj-style Phase 11 1번째 진입점 = cj-style 105번째 epic 연속 정직 회복). T1 `apps/api/modules/finops/showback_dsl.py` NEW (~+150 LOC + `showback(tenant_id, period_key, department_id, group_by)` builder + 5 group_by 옵션 + period selector 6 모드 + comparison view + audit-first INSERT `showback_generated`) + `apps/api/modules/finops/showback_query.py` NEW (~+120 LOC + `query_showback_breakdown(tenant_id, period_key, group_by) -> list[DepartmentBreakdown]` + `query_showback_comparison(tenant_id, period_key, comparison_period_key) -> ComparisonView`) + T2 `apps/api/modules/finops/chargeback_engine.py` NEW (~+180 LOC + 3 chargeback rule + markup + tax + cost_allocation_method enum + 2 NEW error class `ChargebackRuleInvalidError(400)` + `ChargebackCalculationError(500)`) + `apps/api/modules/finops/chargeback_rule_evaluator.py` NEW (~+100 LOC + `evaluate_chargeback_rule(tenant_id, period_key, rule_type) -> ChargebackResult`) + T3 `apps/api/modules/finops/department_mapping.py` NEW (~+120 LOC + `tenant_settings.cost_center_mapping` JSONB TypedDict + auto-create on first calculation + audit-first INSERT `department_mapping_updated` + UNIQUE constraint + RLS policy) + T4 `apps/api/modules/finops/chargeback_export.py` NEW (~+150 LOC + `export_chargeback_csv` StreamingResponse + `export_chargeback_pdf` bytes + reportlab AD-14 stack pin + audit-first INSERT `chargeback_exported`) + T5 `apps/api/alembic/versions/0043_phase_11_finops.py` NEW + 3 NEW tables (phase_11_finops_showback 14 columns + phase_11_finops_chargeback 12 columns + phase_11_finops_department_mapping 9 columns) + 4 indexes + 2 CHECK constraints + UNIQUE constraint + RLS policy (CR 0-2 verbatim) + down_revision "0042_phase_10_slo_engineering" + T6 audit action EXTENSION 3 NEW values + `apps/api/core/audit_action.py` MODIFIED + ActionClass.FINOPS 신규 정의 + `_ActionRegistry` FINOPS entry 신규 3개 등록 + `__all__` EXTENSION 결정 wire + T7 Capability v1.36 EXTENSION 2 NEW rows + `apps/api/core/capability.py` MODIFIED + `tests/integration/test_capability_matrix_v1_36_drift.py` NEW + `docs/capability-matrix.md` v1.35 → v1.36 + drift detector 결정 wire + `apps/web/app/[locale]/(dashboard)/admin/finops/page.tsx` NEW + `apps/web/lib/finops/finops-client.ts` NEW + ko-KR.json `finops.*` namespace EXTENSION ~25 keys (CR 11-4 D-002 verbatim) + T8 Tests + 3중 게이트 FINAL CLEAN atomic commit decision. estimated ~+46 NEW pytest PASS + ~+5 NEW vitest PASS + 0 NEW ruff + 0 regressions. wire_commit = TBD (cj-style Phase 11 3번째 진입점 = cj-style 107번째 epic 연속 정직 회복 atomic single sweep T1~T8, expected ~30-35 files = ~18 NEW + ~12 MODIFIED). 결정 wire 일자: 2026-08-24 |
| **A323** | **Phase 11 close-out retro 진입 결정 wire** — Phase 11 PRD entry DONE 후 옵션 5종 중 옵션 (a) Phase 11+ (Recommended) 진입 결정 wire 진입 보류 (cj-style 108번째 wire 진입 시점에 결정). 결정 wire 일자: 2026-08-24 |

### Architectural Decisions (AD) — Epic 0~10 wire 정합 (2026-08-20 master PRD v2.0 edit)

> 본 절은 §3 회계 공리 헌장 (A1~A11) 외 **아키텍처 결정을 위한 AD (Architectural Decision)** 을 추가한다. AD-7·17·25 는 Epic 10 wire 진입 시점에 신규 bind 되었으며 master PRD v2.0 본체에 정식 등록한다.

| AD | 내용 | Story bind | Capability | 결정 wire |
|---|---|---|---|---|
| **AD-7 AI non-authoritative** | AI output → `input_drafts` only. `confirmed_inputs` 도달은 **AD-17 경로만**. AI commentary `source_kind='ai_reference'`. 자동 분석 `source_kind='auto_analysis'`. M10 attempts to write confirmed-input tables → denied + counted (target 0). System은 strict reject 외 value 도달 시 counter increment (F10.2-(b) 정합) | 10-1·10-3·10-4 | `AI_INSIGHT` (capability matrix v1.21, Epic 10 wire) | Epic 10 PRD entry 2026-08-17 |
| **AD-17 AI draft promotion port** | Only M2 may call `InputPromoter.promote(tenant_id, period_key, source_draft_id) -> MonthlyInput`. **Idempotent on `(tenant_id, period_key, source_draft_id)`**. Promotion retains draft with `state='promoted'`, records actor + draft hash in audit_logs, writes canonical confirmed-input shape. M10 never writes confirmed inputs | 10-1 + 10-4 | `AI_INSIGHT` | Epic 10 PRD entry 2026-08-17 |
| **AD-25 AI insight cache invalidation** | M10 cache key: `(tenant_id, period_key, calculation_result_hash)`. New AD-4 commit, AD-22 reversal insert, or M11 reopen emits one DB notification per `cache_invalidation_log` channel. M10 adapter consumes it and invalidates matching entries. Application polling + input-write-only invalidation forbidden. **EXTENSION (Epic 13 wire DONE 2026-08-20, A52)** — 4-channel publisher EXTENSION 결정 wire: `ai_cache` 외 `cost_engine_cache` / `fiscal_period_cache` / `closing_snapshot_cache` 3 channel 추가. PostgreSQL `LISTEN/NOTIFY` channel-specific eviction handler wire (alembic 0033 NOTIFY trigger + LISTEN daemon + 4-channel adapter dispatch). NOTIFY payload JSON 6-key alphabetical: `{channel, correction_group_id, invalidation_id, period_key, tenant_id, trace_id}`. capability matrix v1.22 신규 row `LISTEN_NOTIFY` (4-industry grants ✅/✅/✅/✅ industry-agnostic) [§F13 verbatim, §8.1 M10-(d)·§F10.1-(d) EXTENSION 결정]. **EXTENSION (Epic 14 PRD entry DONE 2026-08-20, A58)** — **5+ channels EXTENSION 결정 wire**: 4-channel 외 `cross_tenant_fanout` 1 channel 추가 (총 5+ channels). Cross-tenant invalidation fan-out channel = tenant-level subscription routing + multi-tenant isolation 검증 (CR 0-2 RLS lesson 적용 + AD-22 verbatim 보존). NOTIFY payload 7-key alphabetical EXTENSION: `{channel, correction_group_id, invalidation_id, period_key, source_tenant_id, target_tenant_ids, trace_id}`. **Multi-process coordination Option 1 결정**: PostgreSQL `LISTEN/NOTIFY` only via pg_notify fan-out leader/follower model (advisory lock + leader election + follower health check + leader takeover). Option 2 Redis pub/sub rejected (rationale: G2 인프라 최소화 정합). capability matrix v1.23 EXTENSION (`LISTEN_NOTIFY_TENANT_FANOUT` + `LISTEN_NOTIFY_MULTIPROCESS` 2 NEW rows, industry-agnostic 4-industry grants ✅/✅/✅/✅) [§F14 verbatim, §8.1 M10-(d)·§F10.1-(d) cross-tenant fan-out EXTENSION 결정] | 10-2 (+ 13-1 EXTENSION + 14-1 EXTENSION 진입 대기) | `AI_INSIGHT` (+ `LISTEN_NOTIFY` + `LISTEN_NOTIFY_TENANT_FANOUT` + `LISTEN_NOTIFY_MULTIPROCESS`) | Epic 10 PRD entry 2026-08-17 (Epic 13 EXTENSION 2026-08-20 + Epic 14 EXTENSION 결정 2026-08-20) |
| **AD-26 Auth Foundation** | **EXTENSION (Phase 3 PRD entry DONE 2026-08-20, A66)** — Phase 3 = 로그인/회원가입 UI + auth middleware (Epic 1 완성 territory) 신규 bind. (a) **Supabase SSR auth client** 결정 wire = `createServerClient` (Server Components, cookie-based) + `createBrowserClient` (Client Components, localStorage-based), single source of truth URL + anon key. (b) **`sb-access-token` cookie session** 결정 wire = `httpOnly` + `secure` + `sameSite=lax` + `path=/` + `maxAge=3600` (1시간, refresh token 으로 자동 �신). (c) **next-intl middleware EXTENSION** 결정 wire = `apps/web/middleware.ts` 의 next-intl middleware 에 Supabase session check + `(dashboard)` 보호 + `?redirect=` 쿼리 보존 + `(auth)` 공개 + `/api/v1/*` bypass + Edge Runtime 명시 추가. (d) **Auth route group `(auth)` 공개** 결정 wire (login + signup + forgot-password + 2fa + email-verification-pending). (e) **Dashboard route group `(dashboard)` 보호** 결정 wire (Supabase session 필수 + Epic 12 2FA 미설정 시 `/account/security?reason=2fa_required` redirect, Epic 12 M12-a 정합). (f) **CSRF 방어** 결정 wire = Supabase PKCE flow + sameSite=lax cookie 정합 (별도 CSRF token 미사용, Supabase 권장 정합). (g) **Email 존재 여부 노출 방지** 결정 wire = forgot-password 항상 200 반환 (보안 invariant). capability matrix v1.24 신규 5 rows (`LOGIN` + `SIGNUP` + `AUTH_MIDDLEWARE` + `FORGOT_PASSWORD` + `LOGOUT`, industry-agnostic 4-industry grants ✅/✅/✅/✅) [§F15 verbatim, §8.1 M0-(d)·M0-(e)·M0-(f) EXTENSION 결정] | Phase 3 T1~T8 (cj-style Phase 3 3번째 진입점 진입 대기) | `LOGIN` + `SIGNUP` + `AUTH_MIDDLEWARE` + `FORGOT_PASSWORD` + `LOGOUT` | Phase 3 PRD entry 2026-08-20 |
| **AD-28 Magic link + Social OAuth + SSO enterprise SAML** | **EXTENSION (Epic 15 PRD entry DONE 2026-08-22, A80)** — Epic 15 = Magic link + Social OAuth (Google/Naver/Kakao) + SSO enterprise SAML 통합 territory 신규 bind. (a) **Magic link** 결정 wire = Supabase `signInWithOtp({ email, options: { emailRedirectTo } })` wrapper + 5회 cool-down sessionStorage 30s (Phase 3-1 T2 wire 패턴 미러) + email 존재 여부 노출 방지 security invariant try/catch/finally (Phase 3-1 T6 forgot-password 정합) + audit-first INSERT `magic_link_sent` (CR 1-1 verbatim, action_class='AUTH' + action='magic_link_sent' + actor_id + target_email) + `sb-access-token` cookie session 자동 설정 (Phase 3-1 T1 wire 정합) + Epic 12 2FA 미설정 시 `/auth/2fa` redirect 결정. (b) **Social OAuth Google/Naver/Kakao** 결정 wire = Supabase `signInWithOAuth({ provider, options: { redirectTo } })` wrapper + provider whitelist `ALLOWED_SOCIAL_PROVIDERS = frozenset({'google', 'naver', 'kakao'})` strict reject + counter increment (AD-7 verbatim 정합) + 3회 cool-down sessionStorage 60s + audit-first INSERT `social_oauth_initiated` (CR 1-1 verbatim) + OAuth callback handler `/auth-callback` + `exchangeCodeForSession(code)` + Naver OAuth Option A Supabase 우선 / Option B custom Naver OAuth flow 결정 wire 보존. (c) **SSO enterprise SAML** 결정 wire = `python3-saml==1.16.0` AD-14 stack pin + SAML response validation (signature verification + `NotBefore`/`NotOnOrAfter` timestamp + `Audience` + `Destination` + `InResponseTo` CSRF 방어 + RelayState base64 encode) + 4 SSO routes (`/api/v1/auth/sso/{login,acs,metadata,sls}`) + JIT (Just-In-Time) user provisioning atomic 5-step flow (SAML → user + tenant_memberships + external_identities) + multi-tenant isolation CR 0-2 RLS lesson (`external_identities` RLS policy `tenant_id = (SELECT current_setting('app.tenant_id'))::uuid` 결정, alembic 0037) + tenant slug 별 IdP metadata routing (multi-tenant SSO) + audit-first INSERT `sso_identity_linked` (CR 1-1 verbatim, action_class='AUTH' + action='sso_identity_linked' + actor_id + provider + provider_user_id + tenant_id). capability matrix v1.26 신규 5 rows (`MAGIC_LINK` + `SOCIAL_OAUTH_GOOGLE` + `SOCIAL_OAUTH_NAVER` + `SOCIAL_OAUTH_KAKAO` + `SSO_ENTERPRISE`, industry-agnostic 4-industry grants ✅/✅/✅/✅) [§F17 verbatim, §8.1 M0-(h)·M0-(i)·M0-(j) EXTENSION 결정] | Epic 15 T1~T8 (cj-style Epic 15 3번째 진입점 진입 대기) | `MAGIC_LINK` + `SOCIAL_OAUTH_GOOGLE` + `SOCIAL_OAUTH_NAVER` + `SOCIAL_OAUTH_KAKAO` + `SSO_ENTERPRISE` | Epic 15 PRD entry 2026-08-22 |
| **AD-29 1st release launch** | **신규 결정 (1st release launch PRD entry DONE 2026-08-22, A85)** — 1st release launch = Epic 15 close-out retro §12 옵션 (d) 결정 진입 territory 신규 bind. (a) **Marketing landing page** 결정 wire = `/landing` public route (vercel.json public route EXTENSION) + `LandingHero` + `LandingFeatures` (6 feature cards: ABC engine + TDABC + AI insight + 4-industry grants + 2FA + LISTEN/NOTIFY) + `LandingPricing` (월 1만원 subscription + 14일 무료 체험 결정) + `LandingCTA` (signup CTA button + `/login` redirect) + ko-KR inline copy EXTENSION (`landing.*` namespace 8 keys) + `(public)` route group 신규 (D-001 route.tsx mount MUST actual mount) — `(auth)/landing` 결정 wire 진입. (b) **ToS + Privacy Policy** 결정 wire = `docs/terms-of-service.md` (~+150 LOC, 8 sections: 정의 + 서비스 이용 + 계약 변경 + 환불 정책 + 면책 + 분쟁 해결 + 準拠법 + 개정 이력) + `docs/privacy-policy.md` (~+200 LOC, 한국 PIPA + GDPR 정합, 10 sections: 수집 항목 + 이용 목적 + 보유 기간 + 제3자 제공 + 처리 위탁 + 정보주체 권리 + 안전성 확보 조치 + 쿠키 정책 + 분쟁 해결 + 개정 이력) + versioning (changelog + effective date 표기) + signup flow EXTENSION `(auth)/tos` + `(auth)/privacy` 결정 wire 진입 (Phase 3-1 wire `d3e7454` 정합). (c) **Onboarding user guide** 결정 wire = `docs/onboarding-guide.md` (~+200 LOC, 8 sections: 시작하기 + 첫 대시보드 + 데이터 입력 6종 + ABC/TDABC 분석 활용 + AI 인사이트 활용 + 보안/2FA 설정 + 자주 묻는 질문 + 지원팀 연락) + `OnboardingTooltip.tsx` (first-run wizard EXTENSION 결정, Epic 1 partial scaffold `d182d7d` 정합) + 4 tooltips (dashboard 첫 진입 + 데이터 입력 첫 진입 + 보고서 첫 진입 + 2FA 설정 첫 진입) + first-run wizard `(auth)/onboarding/page.tsx` (4-step wizard 결정). (d) **Customer support channels** 결정 wire = `docs/support.md` (~+150 LOC, 6 sections: 연락 채널 + FAQ + 응답 시간 + SLA + escalation 절차 + 외부 지원 링크) + `support@bizup.kr` email 결정 wire 진입 (Phase 4 deployment `934b35e` 환경 변수 EXTENSION) + in-app help widget `HelpWidget.tsx` (Phase 4 Sentry observability EXTENSION 결정) + `docs/faq.md` (~+100 LOC, 10 Q&A). (e) **Production launch verification** 결정 wire = smoke test RE-RUN 정직 결정 wire (`apps/api/scripts/smoke_test.py` RE-RUN, Walking Skeleton MVP `1e034c4` + Phase 3 close-out retro §6 honestly DEFER 해소, Epic 1 ~ Epic 15 모든 wire flow 정합 검증) + backup drill 0036 PITR quarterly EXTENSION (Phase 4 wire `71a033a` 정합) + Sentry alert wiring production 환경 (`apps/web/lib/observability/sentry-alerts.ts` + `apps/api/lib/observability/sentry-alerts.py`, Phase 4 deployment territory EXTENSION) + RPO 4h / RTO 24h SLA verification 결정 wire 진입 (Phase 4 backup strategy 정합) + launch checklist 6 conditions ALL PASS 진입 시점에 1st release official launch 결정 wire 보존. (f) **Public launch communications** 결정 wire = `docs/launch-announcement.md` (~+100 LOC, 4 sections: 출시 배경 + 핵심 기능 + 타겟 시장 + 향후 로드맵) + `docs/press-kit.md` (~+50 LOC, 회사 소개 + 제품 소개 + 로고 + 팩트시트 + 연락처 + 미디어 키트 결정) + social media assets `apps/web/public/og/` (og:image + og:description + twitter:card 결정, metadata 결정 wire 진입) + in-app announcement banner `(auth)/announcements/page.tsx` 결정 wire 진입. capability matrix v1.27 신규 4 rows (`LAUNCH_LANDING` + `LAUNCH_TOS` + `LAUNCH_SUPPORT` + `LAUNCH_MONITORING`, industry-agnostic 4-industry grants ✅/✅/✅/✅) [§F18 verbatim, §8.1 M0-(k) EXTENSION 결정] | 1st release T1~T8 (cj-style 1st release 3번째 진입점 진입 대기) | `LAUNCH_LANDING` + `LAUNCH_TOS` + `LAUNCH_SUPPORT` + `LAUNCH_MONITORING` | 1st release launch PRD entry 2026-08-22 |
| **AD-30 Tenant IdP admin management** | **신규 결정 (Epic 16 PRD entry DONE 2026-08-22, A94)** — Epic 16 = Tenant IdP admin management = Epic 15 close-out retro §12 옵션 (a) 결정 진입 territory 신규 bind (Epic 15 SSO enterprise SAML forward-reference `docs/sso-enterprise.md` §4.1 step 3 `Configure tenant_idps (TODO Epic 16)` verbatim 자연스러운 carry-over chain 결정 wire). (a) **`tenant_idps` table schema** 결정 wire = alembic `0038_epic_16_tenant_idps.py` NEW (13 columns: `id` UUID PK + `tenant_id` UUID FK `tenants(id)` + `idp_entity_id` TEXT NOT NULL + `idp_sso_url` TEXT NOT NULL + `idp_slo_url` TEXT nullable + `idp_x509_cert` TEXT NOT NULL (PEM 결정) + `acs_url` TEXT NOT NULL + `name_id_format` TEXT nullable + `enabled` BOOLEAN NOT NULL DEFAULT TRUE + `created_at` TIMESTAMPTZ NOT NULL DEFAULT NOW() + `updated_at` TIMESTAMPTZ NOT NULL DEFAULT NOW() + `created_by` UUID FK `users(id)` + `updated_by` UUID FK `users(id)`) + UNIQUE constraint `(tenant_id, idp_entity_id)` 결정 + RLS policy `tenant_id = current_setting('app.tenant_id')::uuid` 결정 (CR 0-2 RLS lesson 적용 + AD-22 verbatim 보존) + audit trigger `audit_log_trigger_row()` 자동 호출 결정. (b) **IdP metadata XML validation service** 결정 wire = `apps/api/modules/auth/sso/idp_metadata_validator.py` NEW ~120 LOC (Epic 15 `saml_validator.py` sibling module, 8 validation steps: XML well-formedness + EntityDescriptor root element 확인 + entityID 추출 + IDPSSODescriptor 존재 확인 + X509Certificate PEM wrap + SingleSignOnService Binding=HTTP-Redirect + Location `https://` scheme 검증 + SingleLogoutService 선택 + tenant slug `idp_entity_id` 매칭) + `IdPMetadataError` typed exception envelope `{code, message_ko, details, trace_id}` 결정 (CR 12-5 D-14 verbatim) + `IdPMetadata` TypedDict 6 fields (entity_id, sso_url, slo_url, x509_cert, acs_url, name_id_format). (c) **Tenant IdP CRUD API 5 routes** 결정 wire = `apps/api/modules/auth/sso/idp_admin_routes.py` NEW ~150 LOC (FastAPI Dependency Injection: `GET /api/v1/admin/tenant/{tenant_slug}/idp` (list) + `POST /api/v1/admin/tenant/{tenant_slug}/idp` (create + IdP metadata validation 자동 호출) + `PUT /api/v1/admin/tenant/{tenant_slug}/idp/{idp_id}` (update) + `DELETE /api/v1/admin/tenant/{tenant_slug}/idp/{idp_id}` (delete, audit-first INSERT) + `POST /api/v1/admin/tenant/{tenant_slug}/idp/{idp_id}/test` (test metadata, AuthnRequest 생성 + dry-run SAML flow) + owner/admin role required `require_role("owner", "admin")` FastAPI Dependency + capability gate `TENANT_IDP_MANAGEMENT` 자동 적용 + RLS 자동 적용 (CR 0-2 verbatim) + audit-first INSERT 4 NEW 결정: `tenant_idp_created` + `tenant_idp_updated` + `tenant_idp_deleted` + `tenant_idp_tested` (CR 1-1 verbatim 적용, action_class='AUTH' + action='tenant_idp_*' + actor_id + tenant_id + payload_json 정합). (d) **Tenant IdP admin UI** 결정 wire = `apps/web/app/[locale]/(dashboard)/settings/sso/page.tsx` NEW ~150 LOC + 4 components (`TenantIdPConfigForm` (metadata XML paste + file upload + entity ID auto-extract) + `TenantIdPStatusBadge` (enabled/disabled color coded) + `TenantIdPTestResultModal` (test metadata 결과 5 indicators) + `TenantIdPDeleteConfirmDialog` (확인 문구 + tenant slug typing verify)) + ko-KR.json `settings.sso.*` namespace EXTENSION 12 keys 결정 (CR 11-4 D-002 verbatim SSOT) + `(dashboard)` route group 보호 (Phase 3-1 T1 wire 정합) + `apps/web/lib/auth/admin-idp-client.ts` NEW (fetch wrapper + auth cookie 자동 첨부 + typed error envelope) + vitest RTL render discipline 결정 (CR 11-4 D-003 verbatim). (e) **Per-tenant IdP routing EXTENSION** 결정 wire = Epic 15 `apps/api/modules/auth/sso/saml_routes.py` MODIFIED (existing `acme` hardcoded tenant backward compatibility 보존: `acme` tenant 의 경우 `idp_sso_url` redirect → dynamic lookup `tenant_idps` table SELECT) + ACS `idp_x509_cert` 동적 로딩 결정 (per-tenant IdP cert SELECT, multi-tenant SSO scenario) + alembic 0038 데이터 migration `acme` tenant row 자동 seed 결정 (Epic 15 wire 의 backward compatibility 정합) + capability gate `TENANT_IDP_MANAGEMENT` per-tenant on/off 결정 (CR 12-5 D-GATE-01 inversion). (f) **Audit-first INSERT 4 NEW + multi-tenant isolation** 결정 wire (CR 1-1 verbatim + CR 0-2 RLS lesson 적용, 모든 CRUD API endpoint 의 audit_log INSERT 결정, action_class='AUTH' + action='tenant_idp_*' + actor_id + tenant_id + payload_json 정합 + per-tenant RLS 자동 적용, multi-tenant isolation test 결정). (g) **Capability matrix v1.28 EXTENSION + 1 NEW row** 결정 wire = `Capability.TENANT_IDP_MANAGEMENT = "tenant_idp_management"` 신규 1 row (manufacturing ✅ + service ✅ + manufacturing_service ✅ + manufacturing_service_other ✅ industry-agnostic 4-industry grants, CR 12-1 L4 precedent 미러, SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY 13-1 + LISTEN_NOTIFY_TENANT_FANOUT 14-1 + LISTEN_NOTIFY_MULTIPROCESS 14-1 + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire pattern verbatim bind) + 미허용 tenant 의 tenant IdP admin 진입 차단 결정 wire + SSOT RED→GREEN EXTENSION (capability matrix v1.28 신규 1 row + capability.py EXTENSION 1 NEW enum + `require_capability()` Dependency 1개 신규 wire) + drift detector `tests/integration/test_capability_matrix_v1_28_drift.py` 결정 (Epic 15 wire 의 `tests/integration/test_capability_matrix_v1_26_drift.py` 패턴 verbatim). capability matrix v1.28 신규 1 row (`TENANT_IDP_MANAGEMENT`, industry-agnostic 4-industry grants ✅/✅/✅/✅) [§F19 verbatim, §8.1 M0-(l) EXTENSION 결정] | Epic 16 T1~T8 (cj-style Epic 16 3번째 진입점 진입 대기) | `TENANT_IDP_MANAGEMENT` | Epic 16 PRD entry 2026-08-22 |
| **AD-31 Multi-Region Backup & Disaster Recovery** | **신규 결정 (Phase 5 PRD entry DONE 2026-08-22, A126)** — Phase 5 = Multi-Region Backup & Disaster Recovery = Epic 16 close-out retro §13 옵션 (a) 결정 진입 territory 신규 bind (Phase 4 close-out retro §6 disaster recovery "multi-region backup 결정 wire 보류, Phase 5+ 진입 시점" verbatim 자연스러운 carry-over chain 결정 wire, D-PHASE-4-DR-DEFER-1/2 honestly RESOLVE 진입 wire 결정, CR 11-3 honest-DEFER discipline 73번째 epic 연속 정직 회복 검증). (a) **Cross-region read replica + WAL archiving** 결정 wire = alembic `0039_phase_5_multi_region_backup.py` NEW `phase_5_replication_lag` table (BIGSERIAL id + replica_region TEXT enum seoul/tokyo/singapore/frankfurt/virginia/oregon/sao_paulo + primary_region TEXT enum + lag_bytes BIGINT + lag_seconds INTEGER + last_synced_lsn TEXT PG_LSN + last_synced_at TIMESTAMPTZ + replication_status TEXT enum syncing/replicating/lagged/disconnected/failed + created_at TIMESTAMPTZ DEFAULT NOW()) + 3 indexes (status + region+status+last_synced_at DESC + created_at) + 2 CHECK constraints (replication_status enum + replica_region enum) + audit-first INSERT `replica_status_changed` (CR 1-1 verbatim, action_class='INFRA' + action='replica_status_changed' + actor_id + region + previous_status + new_status + trace_id) 결정 wire 결정. WAL archiving 결정 wire = `postgresql.conf` `archive_mode = on` + `archive_command = 'pgbackrest --stanza=costmgr archive-push %p'` + `wal_level = replica` 결정 wire 보류 (Supabase managed 결정 wire). `docs/cross-region-replication.md` 결정 wire (cross-region replication setup + replica region 선택 Tokyo 1st choice latency Seoul-Tokyo ~50ms 결정 + replication lag monitoring lag_bytes threshold 100MB + lag_seconds threshold 30s alert 결정 + WAL archiving setup + Supabase pgbackrest 결정 wire 보류). (b) **Cross-region failover automation** 결정 wire = `apps/api/jobs/failover_orchestrator.py` NEW ~200 LOC (primary → secondary health probe 5-second interval + 3 consecutive failures trigger 결정 + automatic promotion 결정: secondary region 의 PostgreSQL promote decision via Supabase API `POST /v1/projects/{ref}/database/promote` 결정 wire 보류 + read-only mode 해제 + connection pool redirect 결정) + DNS update 결정 wire (failover 결정 wire 진입 시점에 Supabase project URL 의 custom domain redirect 결정 wire + Supabase custom domain 결정 wire 보류) + RTO 30-second target 결정 wire + failover trigger 3종 결정 (health probe 3 consecutive failures OR manual trigger via `POST /api/v1/admin/failover` owner-only AD-22 RBAC + 2FA 챌린지 Epic 12 정합 OR scheduled drill via `apps/api/jobs/dr_drill.py` cron 결정) + audit-first INSERT `failover_initiated` + `failover_completed` (CR 1-1 verbatim, action_class='INFRA' + action='failover_initiated' + actor_id + from_region + to_region + trace_id) + FastAPI lifespan hook startup/shutdown 결정 wire + GRACEFUL_SHUTDOWN_TIMEOUT=30s 결정 wire. (c) **DR drill + automated quarterly test** 결정 wire = `apps/api/jobs/dr_drill.py` NEW ~150 LOC (cron KST 1st Sunday 03:00 = UTC 18:00 결정 wire + actual failover drill test in staging 결정 wire production 환경 직접 failover 위험 회피 결정 + 6 drill steps: staging primary health check + staging secondary promote trigger + staging database connection write test + staging application health check + staging DNS update test + staging primary restore trigger + RPO/RTO measurement decision: drill 시작 시점 → drill 완료 시점 시간 측정 = RTO actual + drill 시작 전 마지막 transaction LSN → drill 후 secondary LSN 측정 = RPO actual + 결과 결정 wire `phase_5_dr_drill_results` table 신규 (BIGSERIAL id + drill_date DATE + rto_actual_seconds INTEGER + rpo_actual_bytes BIGINT + status TEXT enum pass/fail + notes TEXT + created_at TIMESTAMPTZ) + Q1/Q2/Q3/Q4 quarterly drill schedule 결정 wire (January + April + July + October 결정, docs/database-backup.md §9 quarterly drill pattern verbatim preserve) + audit-first INSERT `dr_drill_completed` (CR 1-1 verbatim, action_class='INFRA' + action='dr_drill_completed' + actor_id='system' + rto_actual_seconds + rpo_actual_bytes + status 결정) 결정. (d) **Cross-region backup strategy** 결정 wire = `docs/database-backup.md` EXTENSION 10 sections → 12 sections (purpose + PITR strategy + RPO/RTO + restore procedure + disaster recovery + monitoring + retention + quarterly drill testing + cross-region backup strategy 신규 + cross-region failover runbook 신규 + RPO 1h / RTO 4h SLA 결정: Phase 4 single-region RPO 5min/RTO 1h 의 honest-extreme risk = Seoul region disaster 시 backup restoration 불가 verbatim 해소 결정 wire) + Cross-region backup vs single-region 결정 wire (Phase 4 single-region 의 honest-extreme risk 의 multi-region 해소 결정) + 30일 hot (primary) + 90일 cold (secondary) + 365일 archive (regional) retention decision. (e) **Multi-region health observability** 결정 wire = `apps/api/core/health.py` EXTENSION `/api/v1/health/multi-region` endpoint 결정 wire (primary + secondary status array 결정 + CR 12-5 D-14 envelope `{status, primary: {region, status, lag_bytes, lag_seconds, last_synced_at}, secondary: {...}, timestamp}` 결정 + JWT verification probe 결정 Supabase Auth health probe per-region 결정) + `apps/api/core/observability.py` EXTENSION Sentry breadcrumb failover_initiated 시 Sentry breadcrumb + alert decision (`sentry_sdk.capture_message(f"Failover initiated from {from_region} to {to_region}", level="warning")` + Sentry alert routing decision) + Grafana multi-region dashboard EXTENSION decision (primary + secondary region metrics + replication lag graph decision + failover event log decision) + `apps/web/app/api/health/multi-region/route.ts` NEW decision wire (~+30 LOC, atomic, Next.js Edge Runtime + force-dynamic + Vercel region decision + NextResponse.json envelope decision `{status, primary, secondary, build, region, timestamp}` decision wire). (f) **Capability matrix v1.29 EXTENSION + 2 NEW rows** 결정 wire = `Capability.MULTI_REGION_BACKUP = "multi_region_backup"` 1 NEW row + `Capability.MULTI_REGION_FAILOVER = "multi_region_failover"` 1 NEW row (manufacturing ✅ + service ✅ + manufacturing_service ✅ + manufacturing_service_other ✅ industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러, DEPLOYMENT_PROD + DEPLOYMENT_DATABASE_BACKUP Phase 4 wire + SSO_ENTERPRISE Epic 15 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + LAUNCH_* 1st release wire + LISTEN_NOTIFY 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire pattern verbatim bind) + SSOT RED→GREEN EXTENSION (capability matrix v1.29 신규 2 rows + capability.py EXTENSION 2 NEW enum + `require_capability()` Dependency 2개 신규 wire) + drift detector `tests/integration/test_capability_matrix_v1_29_drift.py` NEW 결정 (Epic 16 wire 의 `tests/integration/test_capability_matrix_v1_28_drift.py` + Phase 4 wire 의 `tests/integration/test_capability_matrix_v1_25_drift.py` 패턴 verbatim). capability matrix v1.29 신규 2 rows (`MULTI_REGION_BACKUP` + `MULTI_REGION_FAILOVER`, industry-agnostic 4-industry grants ✅/✅/✅/✅) [§F20 verbatim, §8.1 M0-(m) EXTENSION 결정] | Phase 5 T1~T8 (cj-style Phase 5 3번째 진입점 진입 대기) | `MULTI_REGION_BACKUP` + `MULTI_REGION_FAILOVER` | Phase 5 PRD entry 2026-08-22 |
| **AD-32 Audit Log Viewer & Activity Stream** | **신규 결정 (Epic 17 PRD entry DONE 2026-08-22, A155)** — Epic 17 = Audit Log Viewer & Activity Stream = Sidebar/MenuProvider hot-fix `01a06e4` (cj-style 79번째) + D-EPIC-16-REVIEW-DEFER-2~6 RESOLVE sprint `512ed6a` (cj-style 78번째) 진입 직후 옵션 5종 중 옵션 (a) Epic 17 결정 진입 territory 신규 bind (모든 Epic 1~16 + Phase 3~5 의 audit-first INSERT (CR 1-1) 가 audit_log table 에 누적 → audit log viewer territory 의 natural next 진입 + Phase 5 multi-region wire 의 cross-region audit log visibility 자연스러운 carry-over). (a) **audit log query API** 결정 wire = `apps/api/modules/audit/audit_log_query.py` NEW ~180 LOC (4 functions: `query_audit_log(tenant_id, filters, page, page_size) -> AuditLogPage` + `count_audit_log(tenant_id, filters) -> int` + `get_audit_log_entry(tenant_id, entry_id) -> AuditLogEntry` + `query_activity_stream(tenant_id, window_days) -> list[ActivityStreamGroup]` + AuditLogQueryFilters + AuditLogEntry + AuditLogPage + ActivityStreamGroup TypedDict 결정 + RLS 자동 적용 CR 0-2 verbatim + owner/admin role required + capability gate AUDIT_LOG_VIEW). (b) **audit log viewer UI** 결정 wire = `apps/web/app/[locale]/(dashboard)/audit-log/page.tsx` NEW ~200 LOC + 5 components (`AuditLogFilterPanel` + `AuditLogTable` + `AuditLogPagination` + `AuditLogExportButton` + `AuditLogDetailModal`) + ko-KR.json `audit_log.*` namespace EXTENSION 14 keys 결정 (CR 11-4 D-002 verbatim SSOT) + `(dashboard)` route group 보호 (Phase 3-1 T4 wire 정합) + `apps/web/lib/audit/audit-log-client.ts` NEW 결정 + vitest RTL render discipline 결정 (CR 11-4 D-003 verbatim). (c) **activity stream UI** 결정 wire = `apps/web/app/[locale]/(dashboard)/activity/page.tsx` NEW ~150 LOC + 3 components (`ActivityStreamTimeline` + `ActivityStreamEntry` + `ActivityStreamWindowSelector`) + ko-KR.json `activity.*` namespace EXTENSION 8 keys 결정 + all tenant members 권한 (`require_role("owner", "admin", "member", "viewer")`) 결정 wire. (d) **cross-region audit log visibility** 결정 wire = Phase 5 wire `f093f8c` 의 `phase_5_replication_lag` table + Supabase multi-region primary Seoul + secondary Tokyo replica 결정 wire EXTENSION (audit log query 시 secondary region 의 read replica 에서 query 가능 + multi-region read replica 통한 cross-region audit visibility) + read-only routing 결정 wire + 읽기 일관성 lag_bytes ≤ 100MB + lag_seconds ≤ 30s threshold 정합 (Phase 5 wire 정합) — lag 초과 시 primary region 으로 fallback + Sentry breadcrumb 결정 wire. (e) **CSV export** 결정 wire = `apps/api/modules/audit/audit_log_export.py` NEW ~120 LOC + `export_audit_log_csv(tenant_id, filters, actor_id) -> StreamingResponse` 결정 (Excel-compatible UTF-8 BOM `﻿` + comma-separated + double-quote escape for payload_json) + streaming response 결정 + audit-first INSERT `audit_log_exported` (CR 1-1 verbatim, action_class='AUDIT' + action='audit_log_exported' + actor_id + tenant_id + filters_json + row_count + trace_id) — 누가 언제 어떤 filter 로 export 했는지 추적 + CR 12-5 D-14 error envelope 결정 (e.g. `AUDIT_LOG_EXPORT_FORBIDDEN_KO`, `AUDIT_LOG_EXPORT_TOO_LARGE_KO`). (f) **audit-first INSERT 1 NEW + RLS 자동 적용** 결정 wire (CR 1-1 verbatim + CR 0-2 RLS lesson 적용, F21.5 `audit_log_exported` 1 NEW audit log entry 결정, action_class='AUDIT' + action='audit_log_exported' + actor_id + tenant_id + filters_json + row_count + trace_id + per-tenant RLS 자동 적용, multi-tenant isolation test 결정). (g) **Capability matrix v1.30 EXTENSION + 1 NEW row** 결정 wire = `Capability.AUDIT_LOG_VIEW = "audit_log_view"` 신규 1 row (manufacturing ✅ + service ✅ + manufacturing_service ✅ + manufacturing_service_other ✅ industry-agnostic 4-industry grants, CR 12-1 L4 precedent 미러, MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind) + 미허용 tenant 의 audit log viewer 진입 차단 결정 wire + SSOT RED→GREEN EXTENSION (capability matrix v1.30 신규 1 row + capability.py EXTENSION 1 NEW enum + `require_capability()` Dependency 1개 신규 wire) + drift detector `tests/integration/test_capability_matrix_v1_30_drift.py` NEW 결정 (Phase 5 wire 의 `tests/integration/test_capability_matrix_v1_29_drift.py` + Epic 16 wire 의 `tests/integration/test_capability_matrix_v1_28_drift.py` 패턴 verbatim). capability matrix v1.30 신규 1 row (`AUDIT_LOG_VIEW`, industry-agnostic 4-industry grants ✅/✅/✅/✅) [§F21 verbatim, §8.1 M0-(n) EXTENSION 결정] | Epic 17 T1~T8 (cj-style Epic 17 3번째 진입점 진입 대기) | `AUDIT_LOG_VIEW` | Epic 17 PRD entry 2026-08-22 |
| **AD-33 Audit Log Retention Policy** | **신규 결정 (Phase 6 PRD entry DONE 2026-08-22, A195)** — Phase 6 = Audit Log Retention Policy = Epic 17 close-out retro `be8f3bd` (cj-style 84번째) + Epic 17 T2+T3 UI wire `bb92879` (cj-style 83번째) + Epic 17 bmad-dev-story atomic wire T1~T8 backend `2ada2ec` (cj-style 82번째) 진입 직후 옵션 5종 중 옵션 (a) Phase 6 진입 결정 wire + 옵션 (b) Audit Log Retention Policy = Phase 6 territory 결정 wire 진입 (AskUserQuestion 결정 verbatim bind, Epic 1 ~ Epic 17 + Phase 3 + Phase 4 + Phase 5 + 1st release cycle 모두 wire DONE 정합 보존 후 다음 territory). (a) **retention policy DSL** 결정 wire = `apps/api/modules/audit/audit_retention_policy.py` NEW ~+120 LOC, DSL syntax `retain(action_class, days, archive, mask_pii)` 결정 + 4 retention classes: AUDIT 2557일 7년 SOX hold + AUTH 30일 + INFRA 90일 + DATA 365일 + CONFIG 180일 + `DEFAULT_RETENTION_DAYS = {'AUDIT': 2557, 'AUTH': 30, 'INFRA': 90, 'DATA': 365, 'CONFIG': 180}` constants + 4 industries baseline + per-tenant override EXTENSION (`tenant_settings.audit_retention_overrides: dict[str, RetentionRule]`) + policy evaluation precedence tenant override > industry baseline > system default 결정 wire. (b) **automatic purge job** 결정 wire = `apps/api/jobs/audit_purge.py` NEW ~+150 LOC (cron KST 매일 03:00 UTC 18:00 + tenant 별 retention policy 평가 + dry-run mode `--dry-run` flag + purge algorithm 6 steps: phase_6_audit_purge_log INSERT + retention policy evaluate + dry-run + archive + mask_pii + delete + 5 NEW actions audit-first INSERT `audit_log_purged` + `audit_log_archived` + `audit_log_pii_masked` + `audit_log_cold_archived` + `audit_log_personal_data_erased` CR 1-1 verbatim 적용 결정) + `apps/api/jobs/audit_cold_archive.py` NEW ~+120 LOC (cron KST 1st Sunday 04:00 UTC 19:00 monthly cold archive + audit-first INSERT `audit_log_cold_archived` 결정) + `apps/api/alembic/versions/0040_phase_6_audit_retention.py` NEW + `audit_log.archived_at` column ADD + `phase_6_audit_purge_log` table + RLS 자동 적용 CR 0-2 verbatim 결정 wire. (c) **archive storage + 365일 cold archive** 결정 wire = `audit_log_archive` table alembic 0040 NEW + immutable append-only trigger `audit_log_archive_immutable_trg` BEFORE UPDATE OR DELETE raise exception + SHA-256 hash chain linkage `hash_chain` column + 이전 row hash + 현재 row payload SHA-256 = current.hash_chain 결정 + tamper detection C2 도입 + 4 NEW indexes `idx_audit_log_archive_tenant_id` + `idx_audit_log_archive_action_class` + `idx_audit_log_archive_archived_at` + `idx_audit_log_archive_original_id` + cross-region archive EXTENSION Phase 5 wire `f093f8c` `phase_5_replication_lag` table + Supabase multi-region primary Seoul + secondary Tokyo replica EXTENSION + 365일 cold archive + audit_log_archive 의 cross-region replication 결정 wire 결정. (d) **GDPR/NFR4 PII minimization + Article 17 erasure EXTENSION** 결정 wire = `email_mask(email) -> "[REDACTED:sha256(email)]"` + `ip_mask(ip) -> "[REDACTED:ip]/24"` 결정 wire + GDPR Article 17 erasure endpoint `DELETE /api/v1/audit/personal-data?email=<email>` (owner-only RBAC AD-22 verbatim 보존 + audit-first INSERT `audit_log_personal_data_erased` 5th NEW action + audit_log table 의 해당 email 의 PII 컬럼 전체 `[REDACTED]` UPDATE + audit_log_archive 에 archive_event INSERT + tenant owner notification 결정 wire) + NFR4 PII minimization 정합 보존 + 1st release close-out retro §6 + Epic 17 close-out retro §11 "audit log retention policy 결정 wire 보류, Phase 6+ 진입 시점" verbatim 해소 결정 wire. (e) **audit-first INSERT on deletion** 결정 wire (CR 1-1 verbatim + ActionClass.AUDIT 신규 정의, 5 NEW actions: `audit_log_purged` + `audit_log_archived` + `audit_log_pii_masked` + `audit_log_cold_archived` + `audit_log_personal_data_erased` action_class='AUDIT' + actor_id='system' or owner + tenant_id + row_count + retention_rule + trace_id + `apps/api/core/audit_action.py` MODIFIED AuditAction Literal 신규 5 values EXTENSION + _ActionRegistry AUDIT entry 신규 5개 등록 + __all__ EXTENSION + audit-first INSERT BEFORE actual mutation CR 1-1 verbatim discipline + immutable audit log table + 별도 archive table 동시 지원 territory 결정). (f) **Capability matrix v1.31 EXTENSION + 1 NEW row** 결정 wire = `Capability.AUDIT_LOG_RETENTION = "audit_log_retention"` 1 NEW row (manufacturing ✅ + service ✅ + manufacturing_service ✅ + manufacturing_service_other ✅ industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러, AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind) + `apps/api/dependencies/capability.py` EXTENSION `require_audit_log_retention` 1 NEW dep + __all__ EXTENSION 결정 wire + 미허용 tenant 의 audit log retention 진입 차단 결정 wire + SSOT RED→GREEN EXTENSION (capability matrix v1.31 신규 1 row + capability.py EXTENSION 1 NEW enum + `require_capability()` Dependency 1개 신규 wire) + drift detector `tests/integration/test_capability_matrix_v1_31_drift.py` NEW 8 NEW pytest cases 결정 (Epic 17 wire 의 `tests/integration/test_capability_matrix_v1_30_drift.py` + Phase 5 wire 의 `tests/integration/test_capability_matrix_v1_29_drift.py` 패턴 verbatim). (g) **dry-run mode UI** 결정 wire = `apps/web/app/[locale]/(dashboard)/audit-log/retention/page.tsx` NEW ~+100 LOC + 2 components (`RetentionPolicyDryRunButton` + `RetentionPurgeLogTable`) + ko-KR.json `audit_log_retention.*` namespace EXTENSION 8 keys 결정 (CR 11-4 D-002 verbatim SSOT) + vitest RTL render discipline 결정 wire (CR 11-4 D-003 verbatim) + (dashboard) route group 보호 결정 wire + `apps/web/lib/audit/retention-dry-run-client.ts` NEW 결정 wire 결정. capability matrix v1.31 신규 1 row (`AUDIT_LOG_RETENTION`, industry-agnostic 4-industry grants ✅/✅/✅/✅) [§F22 verbatim, §8.1 M0-(o) EXTENSION 결정] | Phase 6 T1~T8 (cj-style Phase 6 3번째 진입점 진입 대기) | `AUDIT_LOG_RETENTION` | Phase 6 PRD entry 2026-08-22 |
| **AD-34 Observability Stack 강화** | **신규 결정 (Phase 7 PRD entry DONE 2026-08-22, A225)** — Phase 7 = Observability Stack 강화 (OpenTelemetry distributed tracing + Prometheus custom metrics + Grafana dashboards + Alerting + Frontend performance tracing) = Phase 6 close-out retro `f9f006c` (cj-style 88번째 wire entry) + Phase 6 atomic wire T1~T8 `24e1cd7` (cj-style 87번째) + Phase 6 spec entry `f5c14c9` (cj-style 86번째) + Phase 6 PRD entry `e84a281` (cj-style 85번째) 결정 wire 모두 DONE 진입 정합 보존 후 옵션 5종 중 옵션 (a) Phase 7+ 진입 + 옵션 (a) Observability Stack 강화 (Recommended) 결정 wire 진입 (AskUserQuestion 결정 verbatim bind, Epic 1 ~ Epic 17 + Phase 3 + Phase 4 + Phase 5 + Phase 6 + 1st release cycle 모두 wire DONE 정합 보존 후 다음 territory). (a) **OpenTelemetry distributed tracing** 결정 wire = `apps/api/core/tracing.py` NEW ~+200 LOC (OTLP HTTP exporter 결정 wire + W3C Trace Context propagation `traceparent` + `tracestate` HTTP header 추출/주입 + FastAPI middleware `TraceContextMiddleware` 결정 wire + 비동기 trace context 보존 CR 1-1 ContextVar 정합 + span enrichment `tenant_id` + `user_id` + `trace_id` + `request_id` + `client_ip` 자동 span attribute 추가 결정 wire + auto-instrumentation `opentelemetry-instrumentation-fastapi` + `opentelemetry-instrumentation-sqlalchemy` + `opentelemetry-instrumentation-httpx` + `opentelemetry-instrumentation-asyncpg` 4 instrumentors 결정 wire + head_based sampler ratio 1.0 dev + 0.1 prod 결정 wire + AD-14 stack pin `opentelemetry-api==1.27.0` + `opentelemetry-sdk==1.27.0` + `opentelemetry-exporter-otlp-proto-http==1.27.0` 결정 wire + Phase 4 wire `71a033a` Sentry `tracesSampleRate=0.1` 정합 보존 + Phase 5 wire `f093f8c` multi-region observability carry-over chain 결정). (b) **Prometheus custom metrics** 결정 wire = `apps/api/core/metrics.py` NEW ~+180 LOC (Counter + Histogram + Summary + Gauge 4 metric types + 7 NEW business metrics: `business_signups_total{industry,plan}` Counter + `business_logins_total{method,outcome}` Counter + `business_calculations_total{engine,outcome}` Counter + `business_cost_engine_duration_seconds{engine,tenant_size_bucket}` Histogram + `business_audit_log_purge_total{action_class}` Counter + `business_active_tenants_gauge` Gauge + `business_ai_extraction_duration_seconds{model,outcome}` Histogram + `prometheus-client==0.20.0` AD-14 stack pin 결정 wire + `/metrics` endpoint Prometheus exposition format + `docs/grafana-dashboards.md` NEW 4 dashboards: business-signups + cost-engine-performance + auth-flow + audit-log-purge + Phase 5 wire `f093f8c` `phase_5_replication_lag` table 정합 보존 결정). (c) **Alerting system** 결정 wire = `apps/api/core/alerting.py` NEW ~+120 LOC + `config/alert_rules.yaml` NEW 5 NEW alert rules (`HighErrorRate` 5xx > 5% for 5m severity=critical + `SlowCalc` p99 > 5s for 10m severity=warning + `FailoverStuck` replication_lag_seconds > 30 for 5m Phase 5 wire 정합 severity=critical + `RetentionPurgeFailed` audit_log_purge_last_success_timestamp > 26h Phase 6 wire 정합 severity=warning + `MultiRegionDown` primary + secondary all down for 1m severity=critical) + Prometheus AlertManager integration + Sentry alert routing `apps/api/core/observability.py` EXTENSION alert webhook handler + Slack webhook integration `#bizup-alerts` channel + PagerDuty integration owner-only manual trigger AD-22 RBAC 결정 wire + audit-first INSERT `alert_fired` CR 1-1 verbatim action_class='OBSERVABILITY' + action='alert_fired' + actor_id='system' + alert_name + severity + tenant_id + trace_id 결정). (d) **Frontend performance tracing (Browser RUM)** 결정 wire = `apps/web/lib/tracing.ts` NEW ~+150 LOC + `@opentelemetry/sdk-trace-web` + `@opentelemetry/exporter-trace-otlp-http` AD-14 stack pin 결정 wire + W3C Trace Context propagation server → client through `traceparent` header (Phase 5 wire의 multi-region trace propagation EXTENSION 정합) + Web Vitals auto-collection LCP + FID + CLS + INP + TTFB 5 metrics + custom span attributes `user.tenant_id` + `user.role` + `user.industry` + `route.path` + `route.locale` + `apps/web/lib/api-fetch.ts` MODIFIED trace context propagation + `apps/web/instrumentation.ts` NEW Next.js instrumentation hook + NFR18 ko-KR 정합 + `web-vitals` AD-14 stack pin 결정). (e) **audit-first INSERT 2 NEW actions** 결정 wire (CR 1-1 verbatim + ActionClass.OBSERVABILITY 신규 정의 + 2 NEW AuditAction Literal values: `alert_fired` (severity + alert_name + tenant_id) + `trace_sampled` (decision + tenant_id + sampling_ratio) + apps/api/core/audit_action.py MODIFIED AuditAction Literal EXTENSION 2 NEW values + _ActionRegistry OBSERVABILITY entry 신규 2개 등록 + __all__ EXTENSION + emit_audit_typed BEFORE alerting trace CR 1-1 verbatim 적용 + Phase 6 wire `24e1cd7` 의 5 NEW AuditAction Literal EXTENSION pattern verbatim 적용). (f) **Capability matrix v1.32 EXTENSION + 2 NEW rows** 결정 wire = `Capability.OBSERVABILITY_TRACES = "observability_traces"` + `Capability.OBSERVABILITY_METRICS = "observability_metrics"` 2 NEW enum 추가 (manufacturing ✅ + service ✅ + manufacturing_service ✅ + manufacturing_service_other ✅ industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러, AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind) + 미허용 tenant 의 observability stack 진입 차단 결정 wire + SSOT RED→GREEN EXTENSION (capability matrix v1.32 신규 2 rows + capability.py EXTENSION 2 NEW enum + `require_capability()` Dependency 2개 신규 wire) + drift detector `tests/integration/test_capability_matrix_v1_32_drift.py` NEW 8 NEW pytest cases 결정 (Phase 6 wire 의 `tests/integration/test_capability_matrix_v1_31_drift.py` + Epic 17 wire 의 `tests/integration/test_capability_matrix_v1_30_drift.py` 패턴 verbatim). (g) **dry-run + Tests + wire scope T1~T8** 결정 wire = tracing dry-run mode (sampling disabled 시 trace propagation 검증) + tests backend 결정 wire (~+30 NEW pytest PASS tracing module 6 + metrics module 6 + alerting module 4 + grafana dashboards 2 + audit action 2 NEW + capability matrix v1.32 8 + integration e2e 2) + tests frontend 결정 wire (~+10 NEW vitest PASS browser RUM tracing 3 + API fetch trace propagation 2 + SSOT drift 2 + ko-KR SSOT 검증 3) + 0 NEW ruff + 0 regressions + T1 OpenTelemetry tracing module + T2 Prometheus metrics module + T3 Alerting module + T4 Frontend tracing module + T5 capability v1.32 EXTENSION 2 NEW rows + T6 audit action EXTENSION 2 NEW + T7 tests + T8 3중 게이트 FINAL CLEAN atomic commit 결정. capability matrix v1.32 신규 2 rows (`OBSERVABILITY_TRACES` + `OBSERVABILITY_METRICS`, industry-agnostic 4-industry grants ✅/✅/✅/✅) [§F23 verbatim, §8.1 M0-(p) EXTENSION 결정] | Phase 7 T1~T8 (cj-style Phase 7 3번째 진입점 진입 대기) | `OBSERVABILITY_TRACES` + `OBSERVABILITY_METRICS` | Phase 7 PRD entry 2026-08-22 |
| **AD-35 Performance / Load Testing** | **신규 결정 (Phase 8 PRD entry DONE 2026-08-24, A255)** — Phase 8 = Performance / Load Testing (k6 부하 테스트 5 scenarios + SLO/SLI 정의 4 metrics + p99 latency budget 5s cost-engine + Latency regression detector + Performance regression gate CI + Cost-engine benchmark V8 골든) = Phase 7 close-out retro `326fa9f` (cj-style 92번째 wire entry) + Phase 7 atomic wire T1~T8 `59b56cd` (cj-style 91번째) + Phase 7 spec entry (cj-style 90번째) + Phase 7 PRD entry `916a541` (cj-style 89번째) 결정 wire 모두 DONE 진입 정합 보존 후 옵션 5종 중 옵션 (a) Phase 8+ 진입 + 옵션 (a) Performance / Load Testing (Recommended) 결정 wire 진입 (AskUserQuestion 결정 verbatim bind, Epic 1 ~ Epic 17 + Phase 3 ~ Phase 7 + 1st release cycle 모두 wire DONE 정합 보존 후 다음 territory). (a) **k6 부하 테스트 5 scenarios** 결정 wire = `apps/api/tests/performance/k6/` directory + 5 NEW k6 scripts (`smoke-test-100.js` + `baseline-1000.js` + `stress-5000.js` + `soak-100-1h.js` + `spike-10000.js`) + `k6==0.45.0` AD-14 stack pin 결정 wire + `docs/load-testing.md` EXTENSION 12 sections runbook 결정. / (b) **SLO/SLI 정의 4 metrics** 결정 wire = `docs/slo-sli-definition.md` NEW 8 sections + 4 SLAs (cost engine p99 < 5s + signups success_rate > 99% + logins p99 < 1s + audit log purge success_rate > 99.9%) + 30d rolling window baseline + 1.5h/month error budget + Phase 7 wire `59b56cd` 의 7 NEW business metrics 의 자연스러운 carry-over chain 결정 wire. / (c) **p99 latency budget 5s cost-engine** 결정 wire = `apps/api/core/performance.py` NEW + `LatencyBudget` TypedDict + 4 industries baseline industry-agnostic + per-tenant override EXTENSION + CR 12-5 D-14 envelope 결정 wire. / (d) **Latency regression detector** 결정 wire = `apps/api/jobs/latency_regression.py` NEW ~+150 LOC + cron KST 매일 03:30 UTC 18:30 + 매 24시간 p99 latency sample 수집 + drift > 10% p99 vs golden baseline 자동 감지 + Sentry breadcrumb + Slack alert `#bizup-alerts` channel + audit-first INSERT `latency_regression_detected` (CR 1-1 verbatim, action_class='PERFORMANCE' + action='latency_regression_detected' + actor_id='system' + engine + drift_pct + baseline_p99 + current_p99 + trace_id) + Phase 7 wire `59b56cd` 의 SlowCalc alert rule 정합 결정 wire + Epic 11 V8 determinism 골든 fixture EXTENSION 결정 wire + ActionClass.PERFORMANCE_TEST 신규 정의 + PerformanceTestAction Literal 4 NEW values 결정 wire + `apps/api/core/audit_action.py` MODIFIED AuditAction Literal EXTENSION 4 NEW values + _ActionRegistry PERFORMANCE_TEST entry 신규 4개 등록 + __all__ EXTENSION + emit_audit_typed BEFORE alerting CR 1-1 verbatim 적용 + Phase 7 wire `59b56cd` 의 2 NEW AuditAction Literal EXTENSION pattern verbatim 적용. / (e) **Performance regression gate (CI)** 결정 wire = `.github/workflows/ci-performance.yml` NEW + k6 부하 테스트 자동 실행 + p99 budget 초과 시 CI 빌드 차단 + `BASELINE_THRESHOLD=1.10` (10% drift) 결정 wire + NFR18 ko-KR 정합 결정 wire. / (f) **Cost-engine benchmark V8 골든** 결정 wire = `tests/performance/test_cost_engine_benchmark.py` NEW ~+250 LOC + Epic 11 V8 determinism 골든 fixture EXTENSION + cost-engine 12-period benchmark 결정 wire + `BENCHMARK_GOLDEN_BASELINE_MS = {traditional: 800, abc: 1200, tdabc: 1500}` 결정 wire + `BENCHMARK_DRIFT_THRESHOLD = 1.10` 결정 wire + CR 12-5 D-14 envelope `{engine, period_count, total_ms, drift, trace_id}` 결정 (e.g. `COST_ENGINE_BENCHMARK_DRIFT_KO`) + tenant-scoped result_hash CR 4-3/4-4 verbatim 결정 wire. / (g) **Capability matrix v1.33 EXTENSION + 1 NEW row** 결정 wire = `Capability.PERFORMANCE_TESTING = "performance_testing"` 1 NEW enum 추가 (manufacturing ✅ + service ✅ + manufacturing_service ✅ + manufacturing_service_other ✅ industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러, OBSERVABILITY_TRACES + OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind) + 미허용 tenant 의 performance / load testing 진입 차단 결정 wire + SSOT RED→GREEN EXTENSION (capability matrix v1.33 신규 1 row + capability.py EXTENSION 1 NEW enum + `require_capability()` Dependency 1개 신규 wire) + drift detector `tests/integration/test_capability_matrix_v1_33_drift.py` NEW 6 NEW pytest cases 결정 (Phase 7 wire 의 `tests/integration/test_capability_matrix_v1_32_drift.py` + Phase 6 wire 의 `tests/integration/test_capability_matrix_v1_31_drift.py` 패턴 verbatim). capability matrix v1.33 신규 1 row (`PERFORMANCE_TESTING`, industry-agnostic 4-industry grants ✅/✅/✅/✅) [§F24 verbatim, §8.1 M0-(q) EXTENSION 결정]. 결정 wire 일자: 2026-08-24 |
| **AD-36 Chaos Engineering / Game Day** | **신규 결정 (Phase 9 PRD entry DONE 2026-08-24, A285)** — Phase 9 = Chaos Engineering / Game Day (chaos_experiment definition + fault injection types 10 categories + game day runbook + blast radius control 5 levels + continuous chaos vs scheduled game day + tenant-scoped + multi-region chaos + auto-rollback + safety mechanisms 6 layers) = Phase 8 close-out retro `ab495a8` (cj-style 96번째 wire entry) + Phase 8 atomic wire T1~T8 `60d4ea1` (cj-style 95번째) + Phase 8 spec entry (cj-style 94번째) + Phase 8 PRD entry `ced452f` (cj-style 93번째) 결정 wire 모두 DONE 진입 정합 보존 후 옵션 5종 중 옵션 (a) Phase 9+ 진입 + 옵션 (a) Chaos Engineering / Game Day (Recommended) 결정 wire 진입 (AskUserQuestion 결정 verbatim bind, Epic 1 ~ Epic 17 + Phase 3 ~ Phase 8 + 1st release cycle 모두 wire DONE 정합 보존 후 다음 territory). (a) **chaos experiment definition** 결정 wire = `apps/api/modules/chaos/chaos_experiment.py` NEW ~+150 LOC + `ChaosExperiment` TypedDict 12 fields + 5 blast_radius levels (L1 single_request / L2 single_tenant / L3 all_tenants / L4 single_region / L5 multi_region) + 4 abort conditions (steady_state_metric > 1.5x baseline / error_rate > 5% / duration max / manual abort) + 4 rollback strategies (automatic / manual (owner-only + 2FA 챌린지 Epic 12 정합) / hybrid / scheduled abort) + Phase 8 wire `60d4ea1` 의 `LatencyBudget` TypedDict EXTENSION chain 결정 wire. / (b) **fault injection types 10 categories** 결정 wire = `apps/api/modules/chaos/fault_injection.py` NEW ~+200 LOC + 10 fault types (latency injection + error injection + resource exhaustion + network partition + disk I/O stress + DB connection pool exhaustion + cache failure + DNS failure + process kill + clock skew) + Phase 7 wire `59b56cd` 의 alerting system EXTENSION 결정 wire + Phase 8 wire `60d4ea1` 의 k6 부하 테스트 EXTENSION 결정 wire. / (c) **game day runbook + blast radius control** 결정 wire = `docs/chaos-engineering.md` NEW 14 sections runbook + `apps/api/jobs/chaos_game_day.py` NEW ~+150 LOC + cron KST 1st Sunday 03:00 = UTC 18:00 + 8 game day steps (experiment selection + tenant scoping + blast radius confirmation + steady state baseline 측정 + fault injection + observation + auto-rollback + post-mortem report) + Phase 7 wire `59b56cd` 의 Prometheus custom metrics 자동 수집 + Phase 8 wire `60d4ea1` 의 k6 baseline 1000 VUs 5min scenario 자동 실행 정합. / (d) **continuous chaos vs scheduled game day** 결정 wire = `apps/api/jobs/continuous_chaos.py` NEW ~+120 LOC + 4 production-safe experiment candidates + L1 single_request blast radius only + 5% traffic + 60s max duration + 3 NEW Prometheus metrics (`business_chaos_experiments_total` + `business_chaos_auto_rollback_total` + `business_chaos_observations_seconds`) + Phase 8 wire `60d4ea1` 의 p99 latency budget EXTENSION + Phase 7 wire `59b56cd` 의 alerting system EXTENSION 결정 wire. / (e) **tenant-scoped + multi-region chaos** 결정 wire (CR 0-2 RLS lesson + L2 single_tenant + L4 single_region + L5 multi_region + `apps/api/alembic/versions/0041_phase_9_chaos_engineering.py` NEW `phase_9_chaos_experiments` table 14 columns + 3 indexes + 2 CHECK constraints + RLS policy `tenant_id = current_setting('app.tenant_id')::uuid` + Phase 5 wire `f093f8c` `phase_5_replication_lag` table 정합 결정 wire + Phase 5 wire 의 multi-region observability 정합 결정 wire). / (f) **auto-rollback + safety mechanisms 6 layers** 결정 wire (4 strategies + 6 safety layers: layer 1 abort conditions + layer 2 blast radius control + layer 3 owner-only RBAC AD-22 (Epic 12 2FA 챌린지 보존 결정 wire) + layer 4 dry-run mode default + layer 5 steady state verification + layer 6 circuit breaker + audit-first INSERT 4 NEW `chaos_experiment_started` + `chaos_experiment_completed` + `chaos_experiment_aborted` + `chaos_rollback_triggered` CR 1-1 verbatim + ActionClass.CHAOS_ENGINEERING 신규 정의 + apps/api/core/audit_action.py MODIFIED AuditAction Literal EXTENSION 4 NEW values + _ActionRegistry CHAOS_ENGINEERING entry 신규 4개 등록 + __all__ EXTENSION + emit_audit_typed BEFORE chaos_experiment 시작 CR 1-1 verbatim 적용 + Phase 7 wire `59b56cd` 의 2 NEW + Phase 8 wire `60d4ea1` 의 4 NEW AuditAction Literal EXTENSION pattern verbatim 적용). / (g) **Capability matrix v1.34 EXTENSION + 1 NEW row** 결정 wire = `Capability.CHAOS_ENGINEERING = "chaos_engineering"` 1 NEW enum 추가 (manufacturing ✅ + service ✅ + manufacturing_service ✅ + manufacturing_service_other ✅ industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러, PERFORMANCE_TESTING Phase 8 wire + OBSERVABILITY_TRACES + OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind) + 미허용 tenant 의 chaos engineering 진입 차단 결정 wire + SSOT RED→GREEN EXTENSION (capability matrix v1.34 신규 1 row + capability.py EXTENSION 1 NEW enum + `require_capability()` Dependency 1개 신규 wire) + drift detector `tests/integration/test_capability_matrix_v1_34_drift.py` NEW 8 NEW pytest cases 결정 (Phase 8 wire 의 `tests/integration/test_capability_matrix_v1_33_drift.py` + Phase 7 wire 의 `tests/integration/test_capability_matrix_v1_32_drift.py` 패턴 verbatim). capability matrix v1.34 신규 1 row (`CHAOS_ENGINEERING`, industry-agnostic 4-industry grants ✅/✅/✅/✅) [§F25 verbatim, §8.1 M0-(r) EXTENSION 결정]. 결정 wire 일자: 2026-08-24 |
| **AD-37 SLO Engineering / Error Budget Management** | **신규 결정 (Phase 10 PRD entry DONE 2026-08-24, A305)** — Phase 10 = SLO Engineering / Error Budget Management (SLO definition DSL + multi-window burn-rate evaluation + error budget tracker + multi-region SLO aggregation + tenant-scoped SLO override + SLO governance review + auto-rollback SLO breach trigger + dry-run mode + tests + wire scope) = Phase 9 close-out retro `634427d` (cj-style 100번째 wire entry) + Phase 9 atomic wire T1~T8 `e7670e1` (cj-style 99번째) + Phase 9 spec entry `2a5e4da` (cj-style 98번째) + Phase 9 PRD entry `0b2d2f3` (cj-style 97번째) 결정 wire 모두 DONE 진입 정합 보존 후 옵션 5종 중 옵션 (a) Phase 10+ 진입 + 옵션 (a) SLO Engineering / Error Budget Management (Recommended) 결정 wire 진입 (AskUserQuestion 결정 verbatim bind, Epic 1 ~ Epic 17 + Phase 3 ~ Phase 9 + 1st release cycle 모두 wire DONE 정합 보존 후 다음 territory). (a) **SLO definition DSL** 결정 wire = `apps/api/modules/slo/slo_dsl.py` NEW ~+180 LOC + SloDefinition TypedDict 13 fields (slo_id TEXT PK + tenant_id UUID + service TEXT enum cost_engine/signups/logins/audit_purge + slo_type TEXT enum availability/latency/throughput + target REAL CHECK >0 <=1 + window_seconds INTEGER default 2592000 (30d) + burn_rate_threshold_1h REAL default 14.4 + burn_rate_threshold_6h REAL default 6.0 + alerting_policy TEXT + owner_user_id UUID + created_at + updated_at + trace_id) + parse_slo_definition pure validator CR 11-4 P-015 verbatim + 1 NEW error class SloDefinitionInvalidError(400) CR 12-5 D-14 envelope + Phase 8 wire `60d4ea1` SLO/SLI 정의 4 metrics carry-over chain 정합. / (b) **multi-window burn-rate evaluation** 결정 wire = Google SRE Workbook "multi-window, multi-burn-rate criteria" 패턴 verbatim 적용 + `apps/api/jobs/slo_burn_rate_evaluator.py` NEW ~+200 LOC + cron KST 매 5분 UTC 매 5분 + 4 burn-rate windows (fast burn 1h + slow burn 6h + budget exhaustion 24h + long burn 3d) + 1 NEW error class SloBurnRateThresholdExceededError(429) + 3 NEW Prometheus metrics Phase 7 wire `59b56cd` EXTENSION + Phase 9 wire `e7670e1` chaos_experiment auto-rollback 정합 (burn_rate_exceeded ⇒ chaos_experiment_aborted 자동 fire). / (c) **error budget tracker** 결정 wire = `apps/api/modules/slo/error_budget.py` NEW ~+150 LOC + ErrorBudget TypedDict 8 fields (slo_id + budget_total_seconds + budget_consumed_seconds + budget_remaining_pct + reset_at + freeze_until nullable + governance_required + trace_id) + consume_error_budget atomic UPDATE AD-4 commit pattern verbatim + freeze mechanism governance 트리거 시 budget consumption 일시 정지 + 1 NEW error class ErrorBudgetExhaustedError(429) + multi-tenant isolation CR 0-2 RLS + Phase 8 wire `60d4ea1` LatencyBudget EXTENSION 결정 wire. / (d) **multi-region SLO aggregation** 결정 wire = Phase 5 wire `f093f8c` multi-region failover carry-over chain + `apps/api/modules/slo/multi_region_aggregator.py` NEW ~+180 LOC + MultiRegionSloAggregate TypedDict 7 fields (slo_id + region enum seoul/tokyo/singapore + aggregate_target + aggregate_actual + weighted_budget_remaining_pct + region_weight_map JSONB + trace_id) + aggregate_slo_across_regions + region_weight_map default `{seoul: 0.6, tokyo: 0.3, singapore: 0.1}` + cross-region replication lag weighted adjustment (replication_lag_seconds > 30s ⇒ seoul weight 0.5 / tokyo 0.3 / singapore 0.2) + Phase 9 wire `e7670e1` chaos_experiment multi-region 정합 + audit-first INSERT `slo_aggregate_evaluated` CR 1-1 verbatim + ActionClass.SLO_ENGINEERING EXTENSION + apps/api/core/audit_action.py MODIFIED AuditAction Literal EXTENSION 1 NEW value + _ActionRegistry SLO_ENGINEERING entry 신규 1개 등록 + __all__ EXTENSION. / (e) **tenant-scoped SLO override** 결정 wire (CR 0-2 RLS lesson + `apps/api/modules/slo/tenant_scoping.py` NEW ~+120 LOC + TenantSloOverride TypedDict 6 fields (override_id UUID PK + tenant_id + slo_id + target_override + window_seconds_override + trace_id) + apply_tenant_slo_override + UNIQUE constraint `(tenant_id, slo_id)` + RLS policy `tenant_id = current_setting('app.tenant_id')::uuid` alembic 0042 EXTENSION + multi-tenant isolation test + Phase 9 wire `e7670e1` chaos_experiment L2 single_tenant blast radius 정합 보존 + audit-first INSERT `tenant_slo_override_applied` CR 1-1 verbatim 결정 wire). / (f) **SLO governance review + auto-rollback SLO breach trigger** 결정 wire (governance review 4 conditions: budget_consumed_pct > 75% for 7d OR burn_rate_3d > 1x sustained OR freeze_until expired + budget_negative OR error_budget exhausted < 24h to reset + audit-first INSERT `slo_governance_review_initiated` CR 1-1 verbatim + owner-only RBAC AD-22 + Epic 12 2FA 챌린지 보존 + `apps/api/modules/slo/governance.py` NEW ~+150 LOC + GovernanceReview TypedDict 7 fields (review_id + slo_id + tenant_id + trigger_reason enum + initiated_by + initiated_at + status enum + trace_id) + auto-rollback SLO breach trigger (business_cost_engine_p99_breach OR business_signups_success_rate_breach 1m sustained ⇒ Phase 9 wire `e7670e1` chaos_experiment_aborted + chaos_rollback_triggered 자동 fire) + audit-first INSERT 3 NEW slo_target_updated + slo_budget_exhausted + slo_violation_detected CR 1-1 verbatim + ActionClass.SLO_ENGINEERING 신규 정의 + apps/api/core/audit_action.py MODIFIED AuditAction Literal EXTENSION 3 NEW values + _ActionRegistry SLO_ENGINEERING entry 신규 3개 등록 + __all__ EXTENSION + emit_audit_typed BEFORE slo evaluation CR 1-1 verbatim 적용 + Phase 9 wire `e7670e1` 의 4 NEW AuditAction Literal EXTENSION pattern verbatim 적용 + 1 NEW error class SloGovernanceReviewRequiredError(403) CR 12-5 D-14 envelope). / (g) **Capability matrix v1.35 EXTENSION + 1 NEW row + dry-run + Tests + wire scope T1~T8** 결정 wire = Capability.SLO_ENGINEERING = "slo_engineering" 1 NEW enum 추가 (manufacturing ✅ + service ✅ + manufacturing_service ✅ + manufacturing_service_other ✅ industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러, CHAOS_ENGINEERING Phase 9 wire + PERFORMANCE_TESTING Phase 8 wire + OBSERVABILITY_TRACES + OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind) + 미허용 tenant 의 SLO engineering 진입 차단 결정 wire + SSOT RED→GREEN EXTENSION (capability matrix v1.35 신규 1 row + capability.py EXTENSION 1 NEW enum + require_slo_engineering() Dependency 1개 신규 wire) + drift detector `tests/integration/test_capability_matrix_v1_35_drift.py` NEW 6 NEW pytest cases 결정 (Phase 9 wire 의 tests/integration/test_capability_matrix_v1_34_drift.py + Phase 8 wire 의 tests/integration/test_capability_matrix_v1_33_drift.py 패턴 verbatim) + dry-run mode 결정 wire (--slo-engineering-dry-run CLI flag + burn_rate_evaluator dry-run + governance review dry-run + auto-rollback trigger dry-run + dry_run=True default for prod deploys) + tests 결정 wire (~+46 NEW pytest PASS (slo_dsl 6 + slo_burn_rate_evaluator 6 + error_budget 5 + multi_region_aggregator 4 + tenant_scoping 4 + governance 4 + alembic 0042 3 + audit action 6 + capability matrix v1.35 6 + slo integration e2e 2 = ~+46 NEW pytest PASS) + ~+5 NEW vitest PASS (slo dashboard 3 + SSOT drift 2 = ~+5 NEW vitest PASS) + 0 NEW ruff + 0 regressions + T1 slo_dsl + slo_burn_rate_evaluator modules + T2 error_budget module + T3 multi_region_aggregator + tenant_scoping modules + T4 governance + auto-rollback SLO breach trigger + T5 alembic 0042 phase_10_slo_engineering + T6 audit action EXTENSION 3 NEW + T7 capability v1.35 + frontend slo dashboard + T8 3중 게이트 FINAL CLEAN atomic commit). capability matrix v1.35 신규 1 row (SLO_ENGINEERING, industry-agnostic 4-industry grants ✅/✅/✅/✅) [§F26 verbatim, §8.1 M0-(s) EXTENSION 결정]. 결정 wire 일자: 2026-08-24 |
| **AD-38 FinOps Showback / Chargeback** | **신규 결정 (Phase 11 PRD entry DONE 2026-08-24, A320)** — Phase 11 = FinOps Showback / Chargeback (showback DSL department breakdown + period selector + comparison view + chargeback cost allocation engine chargeback rule 정액제/비율배분/usage-based 3종 + markup + tax + department cost center mapping tenant_settings JSONB + showback dashboard UI 차트 + 테이블 + 비교 + chargeback CSV/PDF export Excel-compatible UTF-8 BOM + audit-first INSERT 3 NEW + Capability FINOPS_SHOWBACK + FINOPS_CHARGEBACK 2 NEW + dry-run mode + tests + wire scope T1~T8) = Phase 10 close-out retro `733d428` (cj-style 104번째 wire entry) + Phase 10 atomic wire T1~T8 `ac5d6c5` (cj-style 103번째) + Phase 10 spec entry `3c80ef0` (cj-style 102번째) + Phase 10 PRD entry `09db4d4` (cj-style 101번째) 결정 wire 모두 DONE 진입 정합 보존 후 옵션 5종 중 옵션 (a) Phase 11+ 진입 + 옵션 (d) FinOps Showback / Chargeback (Recommended) 결정 wire 진입 (AskUserQuestion 결정 verbatim bind, Epic 1 ~ Epic 17 + Phase 3 ~ Phase 10 + 1st release cycle 모두 wire DONE 정합 보존 후 다음 territory). (a) **showback DSL + period selector + comparison view** 결정 wire = `apps/api/modules/finops/showback_dsl.py` NEW ~+150 LOC + `showback(tenant_id, period_key, department_id, group_by)` builder + department cost center breakdown (5 group_by 옵션: department + cost_center + product_line + service + custom_tag) + period selector (current month + previous month + last 3 months + last 6 months + YTD + custom range) + comparison view (current vs previous period + variance calculation + delta_pct + delta_amount) + 4 industries baseline + per-tenant override EXTENSION + policy evaluation precedence tenant override > industry baseline > system default + `SHOWBACK_PERIOD_DEFAULTS = {'current_month': True, 'previous_month': True, 'last_3_months': False, 'last_6_months': False, 'ytd': False}` constants + audit-first INSERT `showback_generated` CR 1-1 verbatim + Phase 8 wire `60d4ea1` cost-engine benchmark V8 골든 fixture 정합 보존 + Phase 10 wire `ac5d6c5` monthly reset KST 1일 00:00 + 30d rolling calculation 정합 보존. / (b) **chargeback cost allocation engine** 결정 wire = `apps/api/modules/finops/chargeback_engine.py` NEW ~+180 LOC + chargeback rule 3종 (정액제 flat_fee + 비율배분 proportional_allocation + usage-based metered) + markup_pct (default 0%, max 50% range 0~50% + 0.01 step) + tax_pct (default 10% VAT + per-tenant override EXTENSION) + cost_allocation_method enum direct/indirect/shared 결정 wire + chargeback_period_monthly + chargeback_export_format enum csv/pdf + 1 NEW error class ChargebackRuleInvalidError(400) + 1 NEW error class ChargebackCalculationError(500) CR 12-5 D-14 envelope. / (c) **department cost center mapping** 결정 wire = `apps/api/modules/finops/department_mapping.py` NEW ~+120 LOC + `tenant_settings.cost_center_mapping` JSONB TypedDict + department_id ↔ cost_center_id 1:1 mapping + auto-create on first calculation + audit-first INSERT `department_mapping_updated` CR 1-1 verbatim + UNIQUE constraint `UNIQUE (tenant_id, department_id)` + RLS policy `tenant_id = current_setting('app.tenant_id')::uuid` CR 0-2 verbatim + `apps/api/alembic/versions/0043_phase_11_finops.py` NEW `phase_11_finops_department_mapping` table 9 columns + `phase_11_finops_showback` table 14 columns + `phase_11_finops_chargeback` table 12 columns + 4 indexes + 2 CHECK constraints + UNIQUE constraint + RLS policy (CR 0-2 verbatim) + down_revision "0042_phase_10_slo_engineering" 결정 wire. / (d) **showback dashboard UI + chargeback export** 결정 wire = `apps/web/app/[locale]/(dashboard)/admin/finops/page.tsx` NEW ~+150 LOC + 4 components (`ShowbackPeriodSelector` + `ShowbackDepartmentBreakdownChart` + `ShowbackComparisonView` + `ShowbackCSVExportButton`) + owner-only RBAC AD-22 verbatim + Epic 12 2FA 챌린지 보존 + ko-KR.json `finops.*` namespace EXTENSION ~25 keys (CR 11-4 D-002 verbatim SSOT 결정 wire: finops_dashboard_title + finops_section_label + finops_period_label + finops_department_label + finops_cost_center_label + finops_showback_label + finops_chargeback_label + finops_group_by_label + finops_comparison_label + finops_variance_label + finops_delta_pct_label + finops_delta_amount_label + finops_markup_label + finops_tax_label + finops_export_label + finops_csv_export_label + finops_pdf_export_label + finops_dry_run_label + finops_owner_only_label + finops_chargeback_rule_flat_fee + finops_chargeback_rule_proportional + finops_chargeback_rule_metered + loading_finops + error_finops_failed + empty_state) + `(dashboard)` route group 보호 (Phase 3-1 T1 wire 정합) + `apps/web/lib/finops/finops-client.ts` NEW (~+150 LOC + ShowbackRequest + DepartmentBreakdown + ComparisonView + ChargebackRule + ChargebackExport TypedDict CR 12-5 D-PARITY-01 verbatim + ChargebackApiError typed envelope CR 11-4 P-015) + vitest RTL render discipline 결정 wire (CR 11-4 D-003 verbatim) + `apps/api/modules/finops/chargeback_export.py` NEW ~+150 LOC + `export_chargeback_csv(tenant_id, period_key, actor_id) -> StreamingResponse` 결정 (Excel-compatible UTF-8 BOM `﻿` + comma-separated + double-quote escape for amount_json) + `export_chargeback_pdf(tenant_id, period_key, actor_id) -> bytes` 결정 (reportlab 기반 PDF generation + company logo + department breakdown 차트 PDF 임베드) + streaming response + reportlab AD-14 stack pin 결정 wire. / (e) **audit-first INSERT 3 NEW + RLS 자동 적용** 결정 wire (CR 1-1 verbatim + CR 0-2 RLS lesson 적용, F27.5 `showback_generated` + `department_mapping_updated` + `chargeback_exported` 3 NEW audit log entries 결정, action_class='FINOPS' + action='showback_*' / 'department_mapping_*' / 'chargeback_*' + actor_id + tenant_id + period_key + payload_json 정합 + per-tenant RLS 자동 적용, multi-tenant isolation test 결정 + ActionClass.FINOPS 신규 정의 + apps/api/core/audit_action.py MODIFIED AuditAction Literal EXTENSION 3 NEW values + _ActionRegistry FINOPS entry 신규 3개 등록 + __all__ EXTENSION + emit_audit_typed BEFORE finops 계산 CR 1-1 verbatim 적용 + Phase 6 wire `24e1cd7` 의 5 NEW AuditAction Literal EXTENSION pattern verbatim 적용). / (f) **Capability matrix v1.36 EXTENSION + 2 NEW rows** 결정 wire = `Capability.FINOPS_SHOWBACK = "finops_showback"` + `Capability.FINOPS_CHARGEBACK = "finops_chargeback"` 2 NEW enum 추가 (manufacturing ✅ + service ✅ + manufacturing_service ✅ + manufacturing_service_other ✅ industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러, SLO_ENGINEERING Phase 10 wire + CHAOS_ENGINEERING Phase 9 wire + PERFORMANCE_TESTING Phase 8 wire + OBSERVABILITY_TRACES + OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP + MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind) + 미허용 tenant 의 FinOps showback/chargeback 진입 차단 결정 wire + SSOT RED→GREEN EXTENSION (capability matrix v1.36 신규 2 rows + capability.py EXTENSION 2 NEW enum + `require_finops_showback()` + `require_finops_chargeback()` Dependency 2개 신규 wire) + drift detector `tests/integration/test_capability_matrix_v1_36_drift.py` NEW 10 NEW pytest cases 결정 (Phase 10 wire 의 `tests/integration/test_capability_matrix_v1_35_drift.py` + Phase 9 wire 의 `tests/integration/test_capability_matrix_v1_34_drift.py` 패턴 verbatim). / (g) **dry-run mode + Tests + wire scope T1~T8** 결정 wire = dry-run mode 결정 wire (`--finops-dry-run` CLI flag + showback DSL dry-run + chargeback engine dry-run + CSV/PDF export dry-run + `dry_run=True` default for prod deploys) + tests 결정 wire (~+46 NEW pytest PASS (showback_dsl 6 + chargeback_engine 7 + department_mapping 5 + chargeback_export 6 + alembic 0043 4 + audit action 8 + capability matrix v1.36 10 = ~+46 NEW pytest PASS) + ~+5 NEW vitest PASS (ShowbackPeriodSelector + ShowbackDepartmentBreakdownChart owner-only ack prompt AD-22 verbatim + ShowbackComparisonView RTL render + ko-KR SSOT 2 + finops dashboard parity CR 12-5 D-PARITY-01 결정 wire) + 0 NEW ruff + 0 regressions + T1 showback_dsl + showback_query module + T2 chargeback_engine + chargeback_rule_evaluator + T3 department_mapping + tenant_settings JSONB schema + T4 chargeback CSV/PDF export + T5 alembic 0043 phase_11_finops + T6 audit action EXTENSION 3 NEW + T7 capability v1.36 EXTENSION + frontend finops dashboard + T8 3중 게이트 FINAL CLEAN atomic commit). capability matrix v1.36 신규 2 rows (`FINOPS_SHOWBACK` + `FINOPS_CHARGEBACK`, industry-agnostic 4-industry grants ✅/✅/✅/✅) [§F27 verbatim, §8.1 M0-(t) EXTENSION 결정]. 결정 wire 일자: 2026-08-24 |
| **AD-39 FinOps Anomaly Detection & Budget Alerting** | **신규 결정 (Phase 12 PRD entry DONE 2026-08-24, A365)** — Phase 12 = FinOps Anomaly Detection & Budget Alerting (anomaly_detection_dsl + budget_definition_dsl + anomaly_detection_engine z-score 3.0 + budget_overrun_alerts Slack/PagerDuty/Email + forecast_accuracy_tracking MAE/MAPE/RMSE + dashboard UI Recharts + Capability FINOPS_ANOMALY_DETECTION+FINOPS_BUDGET_ALERT 2 NEW + audit-first INSERT 5 NEW + dry-run + tests + wire scope T1~T8) = Phase 11 close-out retro `80df15b` (cj-style 108번째) + Phase 11 atomic wire T1~T8 `e020ad0` (cj-style 107번째) + Phase 11 spec entry `82c93a8` (cj-style 106번째) + Phase 11 PRD entry `16d7698` (cj-style 105번째) 결정 wire 모두 DONE 진입 정합 보존 후 옵션 5종 중 옵션 (a) Phase 12+ 진입 + 옵션 (a) FinOps Anomaly Detection & Budget Alerting (Recommended) 결정 wire 진입 (AskUserQuestion 결정 verbatim bind). 4-industry grants ✅/✅/✅/✅ (CR 12-1 L4 precedent 미러, FINOPS_SHOWBACK+FINOPS_CHARGEBACK Phase 11 wire + SLO_ENGINEERING Phase 10 wire + CHAOS_ENGINEERING Phase 9 wire + PERFORMANCE_TESTING Phase 8 wire + OBSERVABILITY_TRACES+OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP+MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind). 결정 wire 일자: 2026-08-24 |
| **AD-40 FinOps Forecasting & Capacity Planning** | **신규 결정 (Phase 13 PRD entry DONE 2026-08-24, A389)** — Phase 13 = FinOps Forecasting & Capacity Planning (forecast_definition_dsl + cost_forecasting_engine 4 time series models ARIMA/Prophet/Exponential Smoothing/Linear Regression + capacity_headroom_analysis + budget_burn_rate_projection + forecast_accuracy_tracking MAE/MAPE/RMSE + model_retraining_trigger + dashboard UI Recharts + Capability FINOPS_FORECASTING_CAPACITY_PLANNING + audit-first INSERT 5 NEW + dry-run + tests + wire scope T1~T8) = Phase 12 close-out retro `3354e83` (cj-style 112번째) + Phase 12 atomic wire T1~T8 `f3c0e63` (cj-style 111번째) + Phase 12 spec entry `8c5f374` (cj-style 110번째) + Phase 12 PRD entry `344c7eb` (cj-style 109번째) 결정 wire 모두 DONE 진입 정합 보존 후 옵션 5종 중 옵션 (a) Phase 13+ 진입 + 옵션 (a) FinOps Forecasting & Capacity Planning (Recommended) 결정 wire 진입. AD-14 stack pin statsmodels==0.14.1 + prophet==1.1.5 + tensorflow==2.15.0 + Recharts 2.12.7 결정 wire. 4-industry grants ✅/✅/✅/✅ (CR 12-1 L4 precedent 미러, FINOPS_ANOMALY_DETECTION+FINOPS_BUDGET_ALERT Phase 12 wire + FINOPS_SHOWBACK+FINOPS_CHARGEBACK Phase 11 wire + SLO_ENGINEERING Phase 10 wire + CHAOS_ENGINEERING Phase 9 wire + PERFORMANCE_TESTING Phase 8 wire + OBSERVABILITY_TRACES+OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP+MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind). 결정 wire 일자: 2026-08-24 |
| **AD-41 FinOps Optimization & Rightsizing** | **신규 결정 (Phase 14 PRD entry DONE 2026-08-25, A415)** — Phase 14 = FinOps Optimization & Rightsizing (optimization_definition_dsl + rightsizing_engine 4 utilization methods MINIMUM_UTILIZATION_PCT=20.0 + estimated_savings_threshold_pct=5.0 + idle_resource_detector z-score 기반 30 consecutive days + commitment_recommender 3 service EC2/RDS/ElastiCache 1y/3y + payback_period_months + break_even_utilization_pct default 70% + MINIMUM_SAVINGS_PCT=10.0 + optimization_accuracy_tracker precision/recall/realized savings + dashboard UI 5 sub-components Recharts 2.12.7 + Capability FINOPS_OPTIMIZATION + audit-first INSERT 8 NEW + dry-run + tests + wire scope T1~T8) = Phase 13 close-out retro `850b4f8` (cj-style 116번째) + Phase 13 atomic wire T1~T8 `8b98030` (cj-style 115번째) + Phase 13 spec entry `77ed55f` (cj-style 114번째) + Phase 13 PRD entry `d31dfc8` (cj-style 113번째) 결정 wire 모두 DONE 진입 정합 보존 후 옵션 5종 중 옵션 (a) Phase 14+ 진입 + 옵션 (a) FinOps Optimization & Rightsizing (Recommended) 결정 wire 진입. AD-14 stack pin statsmodels==0.14.1 + prophet==1.1.5 + tensorflow==2.15.0 + Recharts 2.12.7 + slack-sdk==3.23.0 + pdpyras==5.2.0 + sendgrid==6.11.0 결정 wire. 4-industry grants ✅/✅/✅/✅ (CR 12-1 L4 precedent 미러, FINOPS_FORECASTING_CAPACITY_PLANNING Phase 13 wire + FINOPS_ANOMALY_DETECTION+FINOPS_BUDGET_ALERT Phase 12 wire + FINOPS_SHOWBACK+FINOPS_CHARGEBACK Phase 11 wire + SLO_ENGINEERING Phase 10 wire + CHAOS_ENGINEERING Phase 9 wire + PERFORMANCE_TESTING Phase 8 wire + OBSERVABILITY_TRACES+OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP+MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind). 결정 wire 일자: 2026-08-25 |
| **AD-42 FinOps Tag Governance & Cost Allocation** | **신규 결정 (Phase 15 PRD entry DONE 2026-08-25, A434)** — Phase 15 = FinOps Tag Governance & Cost Allocation (tag_policy_dsl 4 enforcement_level required/recommended/optional/blocked + untagged_resource_detector 6 resource_types EC2/RDS/S3/Lambda/EKS/VPC + allocation_rules_engine 5 rule_types tag_match/percentage_split/weighted/conditional/fallback + allocation_audit + compliance monthly/quarterly/annually cadence + chargeback_allocation_reconciliation hybrid_blended default + tag_governance dashboard UI 5 sub-components Recharts 2.12.7 + Capability FINOPS_TAG_GOVERNANCE + audit-first INSERT 10 NEW (5 d compliance + 5 e reconciliation) + dry-run + tests + wire scope T1~T8) = Phase 14 close-out retro `5b367d9` (cj-style 120번째) + Phase 14 atomic wire T1~T8 `e904485` (cj-style 119번째) + Phase 14 spec entry `30637f6` (cj-style 118번째) + Phase 14 PRD entry `0e3f8d9` (cj-style 117번째) 결정 wire 모두 DONE 진입 정합 보존 후 옵션 5종 중 옵션 (a) Phase 15+ 진입 + 옵션 (a) FinOps Tag Governance & Cost Allocation (Recommended) 결정 wire 진입 (AskUserQuestion 결정 verbatim bind, Epic 1 ~ Epic 17 + Phase 3 ~ Phase 14 + 1st release cycle 모두 wire DONE 진입 정합 보존 후 다음 territory). (a) **tag_policy DSL** 결정 wire = `apps/api/modules/finops/tag_policy_dsl.py` NEW ~+180 LOC + TagPolicy TypedDict 14 fields (policy_id TEXT PK + tenant_id UUID + tag_key TEXT enum environment/team/project/cost_center/owner + tag_value_pattern TEXT regex + enforcement_level TEXT enum required/recommended/optional/blocked + resource_types 6 EC2/RDS/S3/Lambda/EKS/VPC + owner_user_id UUID + alert_on_violation BOOLEAN + auto_remediation BOOLEAN + created_at + updated_at + effective_from + effective_until nullable + trace_id) + parse_tag_policy pure validator CR 11-4 P-015 verbatim + 1 NEW error class TagPolicyInvalidError(400) CR 12-5 D-14 envelope. / (b) **untagged resource detector** 결정 wire = `apps/api/modules/finops/untagged_resource_detector.py` NEW ~+200 LOC + UntaggedResource TypedDict 9 fields (resource_id + tenant_id + resource_type enum 6 + region + detected_at + tag_policy_id + missing_tags + enforcement_level + trace_id) + detect_untagged_resources(tenant_id, resource_type, enforcement_level) + 4 baseline windows (7d/30d/90d/all-time) + Phase 14 wire `e904485` idle_resource_detector EXTENSION chain. / (c) **allocation rules engine** 결정 wire = `apps/api/modules/finops/allocation_rules_engine.py` NEW ~+200 LOC + 5 rule_types: tag_match + percentage_split + weighted + conditional + fallback + AllocationRule TypedDict 11 fields + evaluate_allocation_rules + audit-first INSERT `allocation_rule_evaluated` + `allocation_rule_updated` CR 1-1 verbatim + Phase 11 wire `e020ad0` chargeback engine EXTENSION chain + Phase 14 wire `e904485` 5 resource_types EXTENSION 정합. / (d) **allocation audit + compliance** 결정 wire = `apps/api/modules/finops/allocation_audit.py` NEW ~+150 LOC + cron KST 매일 04:00 UTC 19:00 + compliance_report generator + 5 NEW audit actions `compliance_report_generated` + `compliance_alert_sent` + `compliance_remediation_initiated` + `tag_policy_updated` + `untagged_resource_detected` CR 1-1 verbatim + ActionClass.FINOPS_TAG_GOVERNANCE 신규 정의 + apps/api/core/audit_action.py MODIFIED AuditAction Literal EXTENSION 5 NEW values + _ActionRegistry FINOPS_TAG_GOVERNANCE entry 신규 5개 등록 + __all__ EXTENSION + emit_audit_typed BEFORE allocation CR 1-1 verbatim 적용. / (e) **chargeback allocation reconciliation** 결정 wire = `apps/api/modules/finops/chargeback_allocation_reconciliation.py` NEW ~+180 LOC + ReconciliationResult TypedDict 12 fields (reconciliation_id + tenant_id + period_key + tag_based_total + direct_invoice_total + variance_amount + variance_pct + reconciliation_status enum matched/variance_below_threshold/variance_above_threshold/investigating + reconciliation_method enum hybrid_blended/direct_tag_only/allocation_only + trace_id) + 5 EXTENSION audit actions `reconciliation_initiated` + `reconciliation_report_generated` + `reconciliation_investigation_triggered` + `reconciliation_approved` + `reconciliation_resolved` + reconciliation_method default `hybrid_blended` (Phase 11 wire chargeback_engine EXTENSION chain + Phase 14 wire optimization_accuracy_tracker EXTENSION chain + owner-only RBAC AD-22 verbatim + Epic 12 2FA 챌린지 보존) + variance_threshold default 5.0% (Phase 8 wire `60d4ea1` p99 latency budget EXTENSION pattern). / (f) **tag governance dashboard UI** 결정 wire = `apps/web/app/[locale]/(dashboard)/admin/finops/tag-governance/page.tsx` NEW ~+200 LOC + 5 sub-components (`TagPolicyList` + `UntaggedResourceTable` + `AllocationRuleEditor` + `ComplianceReportViewer` + `ReconciliationStatusBadge`) + ko-KR.json `finops_tag_governance.*` namespace EXTENSION ~30 keys (CR 11-4 D-002 verbatim SSOT) + `(dashboard)` route group 보호 + `apps/web/lib/finops-tag-governance/finops-tag-governance-client.ts` NEW (CR 12-5 D-PARITY-01 TS mirror verbatim) + vitest RTL render discipline 결정 wire (CR 11-4 D-003 verbatim) + Recharts 2.12.7 AD-14 stack pin 결정 wire. / (g) **Capability matrix v1.41 EXTENSION + 1 NEW row + dry-run + Tests + wire scope T1~T8** 결정 wire = Capability.FINOPS_TAG_GOVERNANCE = "finops_tag_governance" 1 NEW enum 추가 (manufacturing ✅ + service ✅ + manufacturing_service ✅ + manufacturing_service_other ✅ industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러, FINOPS_OPTIMIZATION Phase 14 wire + FINOPS_FORECASTING_CAPACITY_PLANNING Phase 13 wire + FINOPS_ANOMALY_DETECTION+FINOPS_BUDGET_ALERT Phase 12 wire + FINOPS_SHOWBACK+FINOPS_CHARGEBACK Phase 11 wire + SLO_ENGINEERING Phase 10 wire + CHAOS_ENGINEERING Phase 9 wire + PERFORMANCE_TESTING Phase 8 wire + OBSERVABILITY_TRACES+OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP+MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind) + 미허용 tenant 의 FinOps tag governance 진입 차단 결정 wire + SSOT RED→GREEN EXTENSION (capability matrix v1.41 신규 1 row + capability.py EXTENSION 1 NEW enum + `require_finops_tag_governance()` Dependency 1개 신규 wire) + drift detector `tests/integration/test_capability_matrix_v1_41_drift.py` NEW 8 NEW pytest cases 결정 (Phase 14 wire 의 `tests/integration/test_capability_matrix_v1_40_drift.py` + Phase 13 wire 의 `tests/integration/test_capability_matrix_v1_39_drift.py` 패턴 verbatim) + dry-run mode 결정 wire (`--finops-tag-governance-dry-run` CLI flag + tag_policy_dsl dry-run + untagged_resource_detector dry-run + allocation_rules_engine dry-run + reconciliation dry-run + `dry_run=True` default for prod deploys) + tests 결정 wire (~+56 NEW pytest PASS (tag_policy_dsl 6 + untagged_resource_detector 8 + allocation_rules_engine 8 + allocation_audit 6 + chargeback_allocation_reconciliation 7 + alembic 0047 5 + audit action 8 + capability matrix v1.41 8 = ~+56 NEW pytest PASS) + ~+7 NEW vitest PASS (TagPolicyList 2 + UntaggedResourceTable 2 + AllocationRuleEditor 1 + ComplianceReportViewer 1 + ReconciliationStatusBadge 1 = ~+7 NEW vitest PASS) + 0 NEW ruff + 0 regressions + T1 tag_policy_dsl + tag_policy_evaluator modules + T2 untagged_resource_detector + 6 resource_types 결정 wire + T3 allocation_rules_engine + 5 rule_types 결정 wire + T4 allocation_audit + compliance + chargeback_allocation_reconciliation modules + T5 alembic 0047 phase_15_tag_governance + T6 audit action EXTENSION 10 NEW (5 d + 5 e) + T7 capability v1.41 EXTENSION + frontend tag governance dashboard + T8 3중 게이트 FINAL CLEAN atomic commit). capability matrix v1.41 신규 1 row (`FINOPS_TAG_GOVERNANCE`, industry-agnostic 4-industry grants ✅/✅/✅/✅) [§F31 verbatim, §8.1 M0-(x) EXTENSION 결정]. 결정 wire 일자: 2026-08-25 |
| **AD-43 FinOps Reporting & Executive Dashboard** | **신규 결정 (Phase 16 PRD entry DONE 2026-08-25, A454)** — Phase 16 = FinOps Reporting & Executive Dashboard (executive_dashboard_aggregator 5-module cross-rollup (Phase 11 showback + Phase 12 anomaly + Phase 13 forecast + Phase 14 optimization + Phase 15 tag_governance) + cross-module KPI selector 8 NEW KPI (total_monthly_cost_krw + monthly_cost_growth_pct + cost_per_employee_krw + cost_anomaly_count_30d + forecast_deviation_pct + idle_cost_monthly_krw + tag_compliance_pct + optimization_realized_savings_krw) + executive report generation engine PDF + CSV + Excel monthly/quarterly/annual + scheduled dispatch KST cron weekly/Mon-09:00 + monthly/1st-09:00 + quarterly/1st-day-09:00 + annual/Jan-1-09:00 + tenant-scoped executive role RBAC owner-only + executive dashboard UI 5 sub-components Recharts 2.12.7 + ko-KR.json `finops_reporting.*` namespace EXTENSION ~30 keys + audit-first INSERT 8 NEW (`executive_report_generated` + `executive_dashboard_viewed` + `executive_kpi_refreshed` + `executive_report_exported` + `executive_report_dispatched` + `executive_scheduled_dispatch_evaluated` + `finops_reporting_dry_run_executed` + `cross_module_kpi_calculated`) + Capability FINOPS_REPORTING 1 NEW + dry-run mode 5 NEW CLI flags + tests + wire scope T1~T8) = Phase 15 close-out retro `102f370` (cj-style 124번째) + Phase 15 atomic wire T1~T8 `1b800d9` (cj-style 123번째) + Phase 15 spec entry `69c29df` (cj-style 122번째) + Phase 15 PRD entry `87393b4` (cj-style 121번째) 결정 wire 모두 DONE 진입 정합 보존 후 옵션 5종 중 옵션 (a) Phase 16+ 진입 + 옵션 (a) FinOps Reporting & Executive Dashboard (Recommended) 결정 wire 진입 (AskUserQuestion 결정 verbatim bind, Epic 1 ~ Epic 17 + Phase 3 ~ Phase 15 + 1st release cycle 모두 wire DONE 진입 정합 보존 후 다음 territory). (a) **executive_dashboard_aggregator** 결정 wire = `apps/api/modules/finops/executive_dashboard_aggregator.py` NEW ~+200 LOC + ExecutiveRollup TypedDict 16 fields (executive_rollup_id UUID PK + tenant_id UUID + period_key TEXT + scope enum tenant/department/cost_center/product_line + computed_at TIMESTAMPTZ + total_monthly_cost_krw NUMERIC(20,2) + monthly_cost_growth_pct NUMERIC(5,2) + cost_per_employee_krw NUMERIC(18,2) + cost_anomaly_count_30d INTEGER + forecast_deviation_pct NUMERIC(5,2) + idle_cost_monthly_krw NUMERIC(18,2) + tag_compliance_pct NUMERIC(5,2) + optimization_realized_savings_krw NUMERIC(18,2) + cost_per_revenue_krw NUMERIC(18,2) + trace_id TEXT + kpi_metadata JSONB 5-module source attribution) + `aggregate_executive_rollup(tenant_id, period_key, scope)` 진입점 + 5-module cross-join EXTENSION (Phase 11 wire `e020ad0` showback + Phase 12 wire `f3c0e63` anomaly + Phase 13 wire `8b98030` forecast + Phase 14 wire `e904485` optimization + Phase 15 wire `1b800d9` tag_governance) + 4 scope 옵션 (tenant + department + cost_center + product_line) + audit-first INSERT `cross_module_kpi_calculated` CR 1-1 verbatim. / (b) **cross-module KPI selector** 결정 wire = `apps/api/modules/finops/cross_module_kpi.py` NEW ~+150 LOC + 8 NEW KPI calculations: `total_monthly_cost_krw` (Phase 11 showback SUM) + `monthly_cost_growth_pct` ((current − previous) / previous × 100) + `cost_per_employee_krw` (total / active_employee_count) + `cost_anomaly_count_30d` (Phase 12 confirmed z_score > 3.0 last 30d count) + `forecast_deviation_pct` (Phase 13 predicted vs actual deviation) + `idle_cost_monthly_krw` (Phase 14 idle SUM current period) + `tag_compliance_pct` (Phase 15 latest compliance score last 30d) + `optimization_realized_savings_krw` (Phase 14 SUM realized_savings_krw current period) + 5-module index hints (idx_phase_11_showback_tenant_period + idx_phase_12_anomaly_tenant_period + idx_phase_13_forecast_accuracy + idx_phase_14_idle_resource + idx_phase_15_compliance_report tenant scope JOIN) + audit-first INSERT `cross_module_kpi_calculated` + `finops_reporting_dry_run_executed` CR 1-1 verbatim. / (c) **executive report generation engine** 결정 wire = `apps/api/modules/finops/executive_report_generator.py` NEW ~+220 LOC + 3 export_format (PDF: reportlab==4.0.7 + Korean font registration Pretendard-Regular.ttf + 8-section template executive_summary + cost_overview + anomaly_summary + forecast_summary + optimization_summary + tag_compliance_summary + kpi_breakdown + appendix) + CSV: pandas==2.1.4 + openpyxl==3.1.2 Excel: xlsxwriter==3.1.9 + 3 cadence (monthly default + quarterly + annual) + ExecutiveReport TypedDict 13 fields (report_id UUID PK + tenant_id UUID + period_key TEXT + report_type enum monthly/quarterly/annual/ad_hoc + format enum pdf/csv/xlsx + generated_at TIMESTAMPTZ + generated_by_user_id UUID + kpi_snapshot JSONB + report_path TEXT storage reference + file_size_bytes BIGINT + status enum generating/completed/failed + trace_id TEXT + delivery_status enum pending/delivered/failed not_started) + audit-first INSERT `executive_report_generated` + `executive_report_exported` 2 NEW CR 1-1 verbatim. / (d) **scheduled dispatch KST cron** 결정 wire = `apps/api/jobs/scheduled_executive_dispatch.py` NEW ~+180 LOC + 4 cron schedules (weekly Mon 09:00 KST = UTC 00:00 + monthly 1st-day 09:00 KST = UTC 00:00 + quarterly 1st-day 09:00 KST = UTC 00:00 + annual Jan-1 09:00 KST = UTC 00:00) + recipient resolver (Phase 11 chargeback report 의 recipient_config EXTENSION) + Slack channel / Email / S3 archive dispatch + ScheduledDispatch TypedDict 10 fields (dispatch_id UUID PK + tenant_id UUID + schedule_type enum weekly/monthly/quarterly/annual + next_run_at TIMESTAMPTZ + last_run_at TIMESTAMPTZ nullable + last_status enum success/failed/skipped + recipient_config JSONB + retry_count INTEGER default 0 + max_retries INTEGER default 3 + trace_id TEXT) + audit-first INSERT `executive_scheduled_dispatch_evaluated` + `executive_report_dispatched` 2 NEW CR 1-1 verbatim + owner-only RBAC AD-22 verbatim + Epic 12 2FA 챌린지 보존. / (e) **tenant-scoped executive role RBAC owner-only** 결정 wire = `Role.EXECUTIVE_VIEWER = "executive_viewer"` 1 NEW enum 추가 (AD-22 owner-only RBAC EXTENSION chain) + `require_executive_role()` Dependency 1 NEW wire (Phase 15 `require_finops_tag_governance` 패턴 verbatim) + 미허용 tenant 의 executive dashboard 진입 차단 결정 wire + tenant-level override EXTENSION (tenant_owner_id 의 delegated executive viewer 권한 OPTIONAL 설정) + Epic 12 2FA 챌린지 보존 (executive role 진입 시 TOTP 챌린지). / (f) **executive dashboard UI** 결정 wire = `apps/web/app/[locale]/(dashboard)/admin/finops/executive-dashboard/page.tsx` NEW ~+220 LOC + 5 sub-components (`ExecutiveDashboardAggregator` + `CrossModuleKPISelector` + `ExecutiveReportGeneratorPanel` + `ScheduledDispatchConfigPanel` + `ComplianceTrendMiniChart`) + ko-KR.json `finops_reporting.*` namespace EXTENSION ~30 keys (CR 11-4 D-002 verbatim SSOT, 5 sub-component label + 5 KPI label + report configuration label + dispatch configuration label + accessibility label) + `(dashboard)` route group 보호 + `apps/web/lib/finops-reporting/finops-reporting-client.ts` NEW (CR 12-5 D-PARITY-01 TS mirror verbatim) + vitest RTL render discipline 결정 wire (CR 11-4 D-003 verbatim) + Recharts 2.12.7 AD-14 stack pin 결정 wire + ARIA labels WCAG 2.1 AA 결정 wire. / (g) **Capability matrix v1.42 EXTENSION + 1 NEW row + dry-run + Tests + wire scope T1~T8** 결정 wire = Capability.FINOPS_REPORTING = "finops_reporting" 1 NEW enum 추가 (manufacturing ✅ + service ✅ + manufacturing_service ✅ + manufacturing_service_other ✅ industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러, FINOPS_TAG_GOVERNANCE Phase 15 wire + FINOPS_OPTIMIZATION Phase 14 wire + FINOPS_FORECASTING_CAPACITY_PLANNING Phase 13 wire + FINOPS_ANOMALY_DETECTION+FINOPS_BUDGET_ALERT Phase 12 wire + FINOPS_SHOWBACK+FINOPS_CHARGEBACK Phase 11 wire + SLO_ENGINEERING Phase 10 wire + CHAOS_ENGINEERING Phase 9 wire + PERFORMANCE_TESTING Phase 8 wire + OBSERVABILITY_TRACES+OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP+MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind) + 미허용 tenant 의 FinOps reporting 진입 차단 결정 wire + SSOT RED→GREEN EXTENSION (capability matrix v1.42 신규 1 row + capability.py EXTENSION 1 NEW enum + `require_finops_reporting()` Dependency 1개 신규 wire + Role.EXECUTIVE_VIEWER 신규 정의) + drift detector `tests/integration/test_capability_matrix_v1_42_drift.py` NEW 8 NEW pytest cases 결정 (Phase 15 wire 의 `tests/integration/test_capability_matrix_v1_41_drift.py` + Phase 14 wire 의 `tests/integration/test_capability_matrix_v1_40_drift.py` 패턴 verbatim) + dry-run mode 결정 wire (`--finops-reporting-dry-run` CLI flag 5 NEW: `--finops-reporting-dry-run` + `--finops-reporting-kpi-selector-dry-run` + `--finops-reporting-report-generator-dry-run` + `--finops-reporting-scheduled-dispatch-dry-run` + `--finops-reporting-dashboard-dry-run` + `dry_run=True` default for prod deploys) + tests 결정 wire (~+60 NEW pytest PASS (executive_dashboard_aggregator 8 + cross_module_kpi 10 + executive_report_generator 9 + scheduled_executive_dispatch 7 + alembic 0048 6 + audit action 8 + capability matrix v1.42 8 + executive_rbac 4 = ~+60 NEW pytest PASS) + ~+8 NEW vitest PASS (ExecutiveDashboardAggregator 2 + CrossModuleKPISelector 2 + ExecutiveReportGeneratorPanel 1 + ScheduledDispatchConfigPanel 1 + ComplianceTrendMiniChart 1 + ko-KR SSOT 1 = ~+8 NEW vitest PASS) + 0 NEW ruff + 0 regressions + T1 executive_dashboard_aggregator + cross_module_kpi modules + T2 executive_report_generator PDF/CSV/Excel + T3 scheduled_executive_dispatch KST cron + T4 alembic 0048 phase_16_finops_reporting + T5 audit action EXTENSION 8 NEW + T6 capability v1.42 EXTENSION + Role.EXECUTIVE_VIEWER + T7 frontend executive dashboard 5 NEW components + T8 3중 게이트 FINAL CLEAN atomic commit). capability matrix v1.42 신규 1 row (`FINOPS_REPORTING`, industry-agnostic 4-industry grants ✅/✅/✅/✅) [§F32 verbatim, §8.1 M0-(y) EXTENSION 결정]. 결정 wire 일자: 2026-08-25 |

| **AD-44 FinOps Sustainability & Carbon Reporting** | **신규 결정 (Phase 17 PRD entry DONE 2026-08-25, A474)** — Phase 17 = FinOps Sustainability & Carbon Reporting (carbon_emissions_aggregator 6-module cross-rollup (Phase 11 showback + Phase 12 anomaly + Phase 13 forecast + Phase 14 optimization + Phase 15 tag_governance + Phase 16 executive_reporting) + sustainability KPI selector 8 NEW KPI (total_carbon_emissions_kgco2e + scope1_emissions_kgco2e + scope2_emissions_kgco2e + scope3_emissions_kgco2e + carbon_intensity_kgco2e_per_krw + data_center_pue + renewable_energy_pct + carbon_offset_kgco2e) + sustainability report generation engine PDF + CSV + Excel monthly/quarterly/annual with EU CSRD ESRS E1 + IFRS S2 + KSSB compliance summary + scheduled dispatch KST cron weekly/Mon-09:00 + monthly/1st-09:00 + quarterly/1st-day-09:00 + annual/Jan-1-09:00 + tenant-scoped sustainability role RBAC owner-only + sustainability dashboard UI 5 sub-components Recharts 2.12.7 + ko-KR.json `finops_sustainability.*` namespace EXTENSION ~30 keys + audit-first INSERT 8 NEW (`sustainability_report_generated` + `sustainability_dashboard_viewed` + `sustainability_kpi_refreshed` + `sustainability_report_exported` + `sustainability_report_dispatched` + `sustainability_scheduled_dispatch_evaluated` + `finops_sustainability_dry_run_executed` + `cross_module_sustainability_kpi_calculated`) + Capability FINOPS_SUSTAINABILITY 1 NEW + dry-run mode 5 NEW CLI flags + tests + wire scope T1~T8) = Phase 16 close-out retro `26fd530` (cj-style 128번째) + Phase 16 atomic wire T1~T8 `81ae00a` (cj-style 127번째) + Phase 16 spec entry `69c29df` (cj-style 126번째) + Phase 16 PRD entry `4f11d03` (cj-style 125번째) 결정 wire 모두 DONE 진입 정합 보존 후 옵션 5종 중 옵션 (a) Phase 17+ 진입 + 옵션 (a) FinOps Sustainability & Carbon Reporting (Recommended) 결정 wire 진입 (AskUserQuestion 결정 verbatim bind, Epic 1 ~ Epic 17 + Phase 3 ~ Phase 16 + 1st release cycle 모두 wire DONE 진입 정합 보존 후 다음 territory). (a) **carbon_emissions_aggregator** 결정 wire = `apps/api/modules/finops/carbon_emissions_aggregator.py` NEW ~+200 LOC + CarbonEmissionsRollup TypedDict 17 fields (carbon_rollup_id UUID PK + tenant_id UUID + period_key TEXT + scope enum tenant/department/cost_center/product_line + computed_at TIMESTAMPTZ + total_carbon_emissions_kgco2e NUMERIC(20,4) + scope1_emissions_kgco2e NUMERIC(18,4) + scope2_emissions_kgco2e NUMERIC(18,4) + scope3_emissions_kgco2e NUMERIC(18,4) + carbon_intensity_kgco2e_per_krw NUMERIC(18,8) + data_center_pue NUMERIC(5,3) + renewable_energy_pct NUMERIC(5,2) + carbon_offset_kgco2e NUMERIC(18,4) + scope_breakdown JSONB + cloud_provider_breakdown JSONB + trace_id TEXT + carbon_metadata JSONB 6-module source attribution) + `aggregate_carbon_emissions(tenant_id, period_key, scope)` 진입점 + 6-module cross-join EXTENSION (Phase 11 wire `e020ad0` showback + Phase 12 wire `f3c0e63` anomaly + Phase 13 wire `8b98030` forecast + Phase 14 wire `e904485` optimization + Phase 15 wire `1b800d9` tag_governance + Phase 16 wire `81ae00a` executive_reporting) + 4 scope 옵션 (tenant + department + cost_center + product_line) + GHG Protocol scope1/2/3 breakdown + audit-first INSERT `sustainability_dashboard_viewed` CR 1-1 verbatim. / (b) **sustainability KPI selector** 결정 wire = `apps/api/modules/finops/sustainability_kpi_selector.py` NEW ~+180 LOC + 8 NEW sustainability KPI calculations: `total_carbon_emissions_kgco2e` (`scope1 + scope2 + scope3 − carbon_offset_kgco2e`) + `scope1_emissions_kgco2e` (direct emissions: data center fuel + on-prem cooling + on-prem servers, per-tenant override EXTENSION) + `scope2_emissions_kgco2e` (indirect emissions: cloud provider electricity × grid carbon intensity, AWS ap-northeast-2 0.4436 + GCP asia-northeast3 0.4521 + Azure koreacentral 0.4436 + on-prem 0.4715 kgCO2e/kWh, per-tenant override EXTENSION) + `scope3_emissions_kgco2e` (value chain emissions: cat1_purchased_goods + cat11_use_of_sold_products + cat15_investments, SaaS Slack + SendGrid + AWS S3 적용 EXTENSION) + `carbon_intensity_kgco2e_per_krw` (`total_carbon_emissions / total_monthly_cost_krw`, 4 industries baseline manufacturing ≤ 0.0008 + service ≤ 0.0004 + manufacturing_service ≤ 0.0006 + manufacturing_service_other ≤ 0.0007) + `data_center_pue` (`total_facility_power / it_equipment_power`, AWS Seoul avg 1.45 + Korea MSIT 2024 avg 1.5 EXTENSION) + `renewable_energy_pct` (cloud_provider renewable disclosure: AWS Seoul 100% + GCP 90% + Azure 70%, per-tenant override EXTENSION) + `carbon_offset_kgco2e` (voluntary offsets: VCU + CER + KCU registry, per-tenant override EXTENSION) + 6 mandatory KPI (total + scope1/2/3 + carbon_intensity + renewable_energy_pct) + 2 optional (data_center_pue + carbon_offset_kgco2e) + 6-module index hints (idx_phase_11_showback_carbon + idx_phase_12_anomaly_carbon + idx_phase_13_forecast_carbon + idx_phase_14_idle_carbon + idx_phase_15_tag_governance_carbon + idx_phase_16_executive_carbon tenant scope JOIN) + audit-first INSERT `cross_module_sustainability_kpi_calculated` + `finops_sustainability_dry_run_executed` CR 1-1 verbatim. / (c) **sustainability report generation engine** 결정 wire = `apps/api/modules/finops/sustainability_report_generator.py` NEW ~+220 LOC + 3 export_format (PDF: reportlab==4.0.7 + Korean font registration Pretendard-Regular.ttf + 7-page template cover + monthly trend + 6-module breakdown + 8 KPI bar + renewable trend + EU CSRD ESRS E1 disclosure + footer) + CSV: pandas==2.1.4 + openpyxl==3.1.2 Excel: xlsxwriter==3.1.9 + 3 cadence (monthly default + quarterly + annual) + SustainabilityReport TypedDict 14 fields (sustainability_report_id UUID PK + tenant_id UUID + period_key TEXT + report_type enum monthly/quarterly/annual/ad_hoc + format enum pdf/csv/xlsx + generated_at TIMESTAMPTZ + generated_by_user_id UUID + computed_carbon_rollup_id UUID FK + kpi_summary JSONB + csrd_compliance_summary JSONB + dispatch_ready BOOLEAN + report_path TEXT + file_size_bytes BIGINT + status enum generating/completed/failed + trace_id TEXT) + audit-first INSERT `sustainability_report_generated` + `sustainability_report_exported` 2 NEW CR 1-1 verbatim + EU CSRD ESRS E1 paragraph 12/13/14 (gross_scope1/2/3) + paragraph 16 (energy_consumption_mwh) + paragraph 18 (renewable_energy_pct) + paragraph 65 (financed_emissions) + IFRS S2 Industry-based + KSSB 2026 신설 climate disclosure EXTENSION 결정 wire. / (d) **scheduled dispatch KST cron** 결정 wire = `apps/api/jobs/scheduled_sustainability_dispatch.py` NEW ~+180 LOC + 4 cron schedules (weekly Mon 09:00 KST = UTC 00:00 + monthly 1st-day 09:00 KST = UTC 00:00 + quarterly 1st-day 09:00 KST = UTC 00:00 + annual Jan-1 09:00 KST = UTC 00:00) + recipient resolver (Phase 16 wire `81ae00a` scheduled_executive_dispatch EXTENSION chain) + Slack channel / Email / S3 archive dispatch + ScheduledSustainabilityDispatch TypedDict 12 fields (scheduled_sustainability_dispatch_id UUID PK + tenant_id UUID + scheduled_period enum monthly/quarterly/annual + scheduled_cron TEXT + recipient_emails JSONB List[str] max 50 + dispatch_format enum pdf/csv/excel/all_3 + dispatch_status enum active/paused/expired + last_dispatch_at TIMESTAMPTZ + last_dispatch_status enum success/failed/not_run + next_dispatch_at TIMESTAMPTZ + audit_first_insert BOOLEAN default true + trace_id TEXT) + audit-first INSERT `sustainability_scheduled_dispatch_evaluated` + `sustainability_report_dispatched` 2 NEW CR 1-1 verbatim + owner-only RBAC AD-22 verbatim + Epic 12 2FA 챌린지 보존 + EU CSRD annual reporting deadline (March 31) highlight 결정 wire. / (e) **tenant-scoped sustainability role RBAC owner-only** 결정 wire = `apps/api/core/capability.py` MODIFIED + `Capability.FINOPS_SUSTAINABILITY = "finops_sustainability"` 1 NEW enum + `apps/api/dependencies/capability.py` MODIFIED + `require_finops_sustainability()` Dependency 1 NEW wire (Phase 16 wire `81ae00a` `require_finops_reporting` 패턴 verbatim) + 4 industries grants ✅/✅/✅/✅ industry-agnostic CR 12-1 L4 precedent 미러 + 미허용 tenant 의 sustainability dashboard 진입 차단 + Epic 12 2FA 챌린지 보존 (sustainability role 진입 시 TOTP 챌린지) + AD-22 owner-only RBAC EXTENSION 결정 wire + 10 NEW typed exception classes (CarbonRollupCalculationError + SustainabilityKPIError + SustainabilityReportGenerationError + SustainabilityReportExportError + ScheduledSustainabilityDispatchInvalidCronError + ScheduledSustainabilityDispatchRecipientInvalidError + ScheduledSustainabilityDispatchExpiredError + SustainabilityDispatchEmailInvalidError + FinopsSustainabilityUnsupportedFormatError + FinopsSustainabilityCapabilityDeniedError) CR 12-5 D-14 envelope 결정 wire. / (f) **sustainability dashboard UI** 결정 wire = `apps/web/app/[locale]/(dashboard)/admin/finops/sustainability/page.tsx` NEW ~+180 LOC + 5 sub-components (`SustainabilityDashboardAggregator` + `SustainabilityKPISelector` + `SustainabilityReportGeneratorPanel` + `ScheduledSustainabilityDispatchConfigPanel` + `SustainabilityTrendMiniChart`) + ko-KR.json `finops_sustainability.*` namespace EXTENSION ~30 keys (CR 11-4 D-002 verbatim SSOT, 5 sub-component label + 8 KPI label + report configuration label + dispatch configuration label + accessibility label + ESG compliance label) + `(dashboard)` route group 보호 + `apps/web/lib/finops-sustainability/finops-sustainability-client.ts` NEW (CR 12-5 D-PARITY-01 TS mirror verbatim) + vitest RTL render discipline 결정 wire (CR 11-4 D-003 verbatim) + Recharts 2.12.7 + reportlab 4.0.7 + openpyxl 3.1.2 + apscheduler 3.10.4 + pytz 2024.1 AD-14 stack pin EXTENSION 결정 wire + ARIA labels WCAG 2.1 AA 결정 wire. / (g) **Capability matrix v1.43 EXTENSION + 1 NEW row + dry-run + Tests + wire scope T1~T8** 결정 wire = Capability.FINOPS_SUSTAINABILITY = "finops_sustainability" 1 NEW enum 추가 (manufacturing ✅ + service ✅ + manufacturing_service ✅ + manufacturing_service_other ✅ industry-agnostic 4-industry grants ✅/✅/✅/✅, CR 12-1 L4 precedent 미러, FINOPS_REPORTING Phase 16 wire + FINOPS_TAG_GOVERNANCE Phase 15 wire + FINOPS_OPTIMIZATION Phase 14 wire + FINOPS_FORECASTING_CAPACITY_PLANNING Phase 13 wire + FINOPS_ANOMALY_DETECTION+FINOPS_BUDGET_ALERT Phase 12 wire + FINOPS_SHOWBACK+FINOPS_CHARGEBACK Phase 11 wire + SLO_ENGINEERING Phase 10 wire + CHAOS_ENGINEERING Phase 9 wire + PERFORMANCE_TESTING Phase 8 wire + OBSERVABILITY_TRACES+OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP+MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind) + 미허용 tenant 의 sustainability dashboard 진입 차단 결정 wire + SSOT RED→GREEN EXTENSION (capability matrix v1.43 신규 1 row + capability.py EXTENSION 1 NEW enum + `require_finops_sustainability()` Dependency 1개 신규 wire + alembic 0049 phase_17_finops_sustainability.py NEW 6 NEW tables 결정 wire) + drift detector `tests/integration/test_capability_matrix_v1_43_drift.py` NEW 8 NEW pytest cases 결정 (Phase 16 wire 의 `tests/integration/test_capability_matrix_v1_42_drift.py` + Phase 15 wire 의 `tests/integration/test_capability_matrix_v1_41_drift.py` 패턴 verbatim) + dry-run mode 결정 wire (`--finops-sustainability-dry-run` CLI flag 5 NEW: `--finops-carbon-rollup-dry-run` + `--finops-sustainability-kpi-dry-run` + `--finops-sustainability-report-dry-run` + `--finops-scheduled-sustainability-dispatch-dry-run` + `--finops-sustainability-export-dry-run` + `dry_run=True` default for prod deploys) + tests 결정 wire (~+56 NEW pytest PASS (carbon_emissions_aggregator 8 + sustainability_kpi_selector 8 + sustainability_report_generator 7 + scheduled_sustainability_dispatch 6 + alembic 0049 5 + audit action 8 + capability matrix v1.43 8 + dry-run 6 = ~+56 NEW pytest PASS) + ~+7 NEW vitest PASS (SustainabilityDashboardAggregator 1 + SustainabilityKPISelector 1 + SustainabilityReportGeneratorPanel 1 + ScheduledSustainabilityDispatchConfigPanel 1 + SustainabilityTrendMiniChart 1 + ko-KR SSOT 2 = ~+7 NEW vitest PASS) + 0 NEW ruff + 0 regressions + T1 carbon_emissions_aggregator + sustainability_kpi_selector modules + T2 sustainability_report_generator PDF/CSV/Excel + T3 scheduled_sustainability_dispatch KST cron + T4 alembic 0049 phase_17_finops_sustainability 6 tables + RLS 6 tables + 2 preview tables + T5 audit action EXTENSION 8 NEW + 10 NEW typed exception classes + T6 capability v1.43 EXTENSION + frontend sustainability dashboard 5 NEW components + T7 scheduled_sustainability_dispatch_job wire + T8 3중 게이트 FINAL CLEAN atomic commit). capability matrix v1.43 신규 1 row (`FINOPS_SUSTAINABILITY`, industry-agnostic 4-industry grants ✅/✅/✅/✅) [§F33 verbatim, §8.1 M0-(z) EXTENSION 결정]. 결정 wire 일자: 2026-08-25. D-FINOPS-7 신규 honestly DEFER 보존 (Phase 17 territory 의 backend extension 진입 시점에 deferred 결정 wire = KSSB 2026 신설 표준 detail + ESRS Sector-specific 표준 detail + IFRS S2 Industry-based detail + asset-level carbon footprint detail + scope4 emissions future 정의 detail — 모두 단일 sprint `wire` 진입이 아닌 docs-only entry 에서 honestly defer 결정 wire 보존).

> **AD-22 reversal insert** = Epic 11 wire 결정 (master PRD §8.1 M11-(b) verbatim "입력·변경 시도는 역분개(A8)로만 허용"). 본 master PRD v2.0 edit 시점에 AD-22 cross-ref 보존.
>
> **AD-4 commit** = master PRD §8.1 M3-(a) verbatim "단일 트랜잭션으로 실행하고, 도중 실패 시 전체 롤백".
>
> **SM-3a** = §2.B 성공 지표 SM-3 inline 정의 "계산 결과 변경 시도 = 0건" 별도 추적 (§12 인사이트 큐레이션 원칙 검증).
>
> **§A11** = §3 회계 공리 헌장 A11 "오류의 가시화 (숨기지 않는 시스템)" — F10.2-(b) strict reject + counter increment 의 정당화 근거.

---

### 결정 entry — Phase 17 PRD entry 결정 entries (A474~A478, cj-style 129번째 epic 연속 정직 회복 = master PRD v4.7 → v4.8 atomic edit docs only 진입 정합 보존)

| 결정 ID | 내용 |
|---|---|
| **A474** | **Phase 17 PRD entry 진입 결정 wire** — Phase 16 close-out retro `26fd530` (cj-style 128번째 wire entry) 결정 wire DONE 진입 정합 보존 후 옵션 5종 중 옵션 (a) Phase 17+ 진입 + 옵션 (a) FinOps Sustainability & Carbon Reporting (Recommended) 결정 wire 진입 (AskUserQuestion 결정 verbatim bind, Epic 1 ~ Epic 17 + Phase 3 ~ Phase 16 + 1st release cycle 모두 wire DONE 진입 정합 보존 후 다음 territory). 결정 wire 진입 시점에 master PRD v4.7 → v4.8 atomic edit 결정 wire (front matter title v4.7 → v4.8 + changelog v4.8 entry prepend + §F33 신규 + §15 로드맵 Phase 17 row + §부록 A A474~A478 신규 결정 표 + AD-44 신규 결정 (a)~(g) 7 sub-decisions 모두 결정 wire 진입). D-FINOPS-7 신규 honestly DEFER 보존 (Phase 17 wire 진입 시점에 naturally defer 결정 wire = KSSB 2026 신설 표준 detail + ESRS Sector-specific 표준 detail + IFRS S2 Industry-based detail + asset-level carbon footprint detail + scope4 emissions future 정의 detail — 모두 단일 sprint `wire` 진입이 아닌 docs-only entry 에서 honestly defer 결정 wire 보존). 결정 wire 일자: 2026-08-25 |
| **A475** | **sustainability KPI 8 NEW 결정 wire** — Phase 17 = §F33.2 sustainability_kpi_selector 8 NEW KPI TypedDict 결정 wire = `total_carbon_emissions_kgco2e` + `scope1_emissions_kgco2e` + `scope2_emissions_kgco2e` + `scope3_emissions_kgco2e` + `carbon_intensity_kgco2e_per_krw` + `data_center_pue` + `renewable_energy_pct` + `carbon_offset_kgco2e` 결정 wire + 6 mandatory KPI (total + scope1/2/3 + carbon_intensity + renewable_energy_pct) + 2 optional (data_center_pue + carbon_offset_kgco2e) + 4 industries baseline (manufacturing ≤ 0.0008 + service ≤ 0.0004 + manufacturing_service ≤ 0.0006 + manufacturing_service_other ≤ 0.0007 kgCO2e/KRW) + per-tenant override EXTENSION + cache key = `(tenant_id, period_key, kpi_set_hash, kpi_values_hash)` 결정 wire + audit-first INSERT `cross_module_sustainability_kpi_calculated` + `finops_sustainability_dry_run_executed` CR 1-1 verbatim 결정 wire + NFR11 P95 ≤ 30s SLO 결정 wire. 결정 wire 일자: 2026-08-25 |
| **A476** | **sustainability report generation engine + EU CSRD ESRS E1 + IFRS S2 + KSSB compliance summary 결정 wire** — Phase 17 = §F33.3 sustainability_report_generator + 3 NEW format generator (PDF: reportlab 4.0.7 + Korean font Pretendard-Regular.ttf + 7-page template cover + monthly trend + 6-module breakdown + 8 KPI bar + renewable trend + EU CSRD ESRS E1 disclosure + footer) + CSV (Excel-compatible UTF-8 BOM + comma-separated + double-quote escape) + Excel (openpyxl 3.1.2) + 3 cadence (monthly default + quarterly + annual) + SustainabilityReport TypedDict 14 fields + EU CSRD ESRS E1 paragraph 12/13/14 (gross_scope1/2/3) + paragraph 16 (energy_consumption_mwh) + paragraph 18 (renewable_energy_pct) + paragraph 65 (financed_emissions) + IFRS S2 Industry-based + KSSB 2026 신설 climate disclosure EXTENSION 결정 wire + audit-first INSERT `sustainability_report_generated` + `sustainability_report_exported` 2 NEW CR 1-1 verbatim + cross-module sustainability KPI 6 module source attribution sequence 결정 wire. 결정 wire 일자: 2026-08-25 |
| **A477** | **scheduled sustainability dispatch KST cron 4 cadence 결정 wire** — Phase 17 = §F33.4 scheduled_sustainability_dispatch + APScheduler 3.10.4 AD-14 stack pin EXTENSION + 4 NEW cron schedules (weekly Mon 09:00 KST + monthly 1st-day 09:00 KST + quarterly 1st-day 09:00 KST + annual Jan-1 09:00 KST) 결정 wire + recipient_emails JSONB List[str] max 50 + STRICT email RFC 5322 validation + Slack channel + Email + S3 archive dispatch + ScheduledSustainabilityDispatch TypedDict 12 fields 결정 wire + retry 정책 exponential backoff 결정 wire (Phase 12 wire `f3c0e63` retry pattern EXTENSION) + audit-first INSERT `sustainability_scheduled_dispatch_evaluated` + `sustainability_report_dispatched` 2 NEW CR 1-1 verbatim + AD-22 owner-only RBAC EXTENSION + Epic 12 2FA 챌린지 보존 + EU CSRD annual reporting deadline (March 31) highlight 결정 wire. 결정 wire 일자: 2026-08-25 |
| **A478** | **sustainability dashboard UI 5 sub-components + Capability matrix v1.43 EXTENSION FINOPS_SUSTAINABILITY + CR lessons applied 18종 + D-FINOPS-7 honestly DEFER 결정 wire** — Phase 17 = §F33.5 + §F33.6 + §F33.7 + §F33.8 EXTENSION 결정 wire = §F33.5 tenant-scoped sustainability role RBAC owner-only (Capability.FINOPS_SUSTAINABILITY 1 NEW enum + 4 industries grants ✅/✅/✅/✅ industry-agnostic + Epic 12 2FA 챌린지 보존 + AD-22 owner-only RBAC + 10 NEW typed exception classes CR 12-5 D-14 envelope) / §F33.6 sustainability dashboard UI 5 sub-components (SustainabilityDashboardAggregator + SustainabilityKPISelector + SustainabilityReportGeneratorPanel + ScheduledSustainabilityDispatchConfigPanel + SustainabilityTrendMiniChart) + ko-KR.json `finops_sustainability.*` namespace EXTENSION ~30 keys (CR 11-4 D-002 verbatim SSOT) + `(dashboard)` route group 보호 + ARIA labels WCAG 2.1 AA 결정 wire / §F33.7 Capability matrix v1.42 → v1.43 EXTENSION FINOPS_SUSTAINABILITY 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅ (CR 12-1 L4 precedent 미러, FINOPS_REPORTING Phase 16 wire + FINOPS_TAG_GOVERNANCE Phase 15 wire + FINOPS_OPTIMIZATION Phase 14 wire + FINOPS_FORECASTING_CAPACITY_PLANNING Phase 13 wire + FINOPS_ANOMALY_DETECTION+FINOPS_BUDGET_ALERT Phase 12 wire + FINOPS_SHOWBACK+FINOPS_CHARGEBACK Phase 11 wire + SLO_ENGINEERING Phase 10 wire + CHAOS_ENGINEERING Phase 9 wire + PERFORMANCE_TESTING Phase 8 wire + OBSERVABILITY_TRACES+OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP+MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind) + drift detector `tests/integration/test_capability_matrix_v1_43_drift.py` NEW 8 NEW pytest cases EXTENSION / §F33.8 dry-run + Tests + wire scope T1~T8 (~+56 NEW pytest PASS + ~+7 NEW vitest PASS + 0 NEW ruff + 0 regressions + T1~T8 결정 wire + ~+33-38 files estimate) + CR lessons applied 18종 (CR 0-2 + CR 1-1 + CR 4-3/4-4 + CR 9-6 + CR 11-3 + CR 11-4 + CR 12-1 + CR 12-5 D-14 + CR 12-5 D-PARITY-01 + CR 12-5 D-GATE-01 + A19 + A36 + AD-14 + AD-22 + AD-44 + NFR4 + NFR18) + D-FINOPS-7 신규 honestly DEFER 보존 + 1-day atomic sprint discipline 결정 wire. 결정 wire 일자: 2026-08-25 |

### 결정 entry — Phase 18 PRD entry 결정 entries (A494~A498, cj-style 133번째 epic 연속 정직 회복 = master PRD v4.8 → v4.9 atomic edit docs only 진입 정합 보존)

| 결정 ID | 내용 |
|---|---|
| **A494** | **Phase 18 PRD entry 진입 결정 wire** — Phase 17 close-out retro `de009fe` (cj-style 132번째 wire entry) 결정 wire DONE 진입 정합 보존 후 옵션 5종 중 옵션 (a) Phase 18+ 진입 + 옵션 (a) FinOps Cloud Commitment Management (RIs/SPs/CUDs) (Recommended) 결정 wire 진입 (AskUserQuestion 결정 verbatim bind, Epic 1 ~ Epic 17 + Phase 3 ~ Phase 17 + 1st release cycle 모두 wire DONE 진입 정합 보존 후 다음 territory). 결정 wire 진입 시점에 master PRD v4.8 → v4.9 atomic edit 결정 wire (front matter title v4.8 → v4.9 + changelog v4.9 entry prepend + §F34 신규 + §15 로드맵 Phase 18 row + §부록 A A494~A498 신규 결정 표 + AD-45 신규 결정 (a)~(g) 7 sub-decisions 모두 결정 wire 진입). D-FINOPS-8 신규 honestly DEFER 보존 (Phase 18 wire 진입 시점에 naturally defer 결정 wire = AWS RI marketplace cross-account 2nd-hand RI 거래 detail + GCP CUD flexibile/fixed tier 최적화 detail + Naver Cloud / KT Cloud commitment API stability 검증 detail + commitment auto-renewal webhook integration detail + 5 cloud provider unified cost reconciliation detail — 모두 단일 sprint `wire` 진입이 아닌 docs-only entry 에서 honestly defer 결정 wire 보존). 결정 wire 일자: 2026-08-25 |
| **A495** | **commitment KPI 8 NEW 결정 wire** — Phase 18 = §F34.2 commitment_kpi_selector 8 NEW KPI TypedDict 결정 wire = `total_commitment_value_krw` (SUM committed_amount_krw of active commitments per period_key + KRW currency unit + industry baseline per-tenant override) + `coverage_pct` (commitment_covered_cost / total_tenant_cost * 100 + 4 industries baseline manufacturing ≥ 70% + service ≥ 80% + manufacturing_service ≥ 75% + manufacturing_service_other ≥ 65%) + `utilization_pct` (SUM utilized_amount_krw / SUM committed_amount_krw * 100 + Phase 14 optimization accuracy tracking EXTENSION + utilization_alert utilization < 70%) + `expiring_commitments_30d` (COUNT commitments with end_date - current_date ≤ 30 days + renewal_workflow trigger EXTENSION + Phase 13 forecast 12-month horizon EXTENSION + KST timezone('Asia/Seoul')) + `recommended_purchase_krw` (Phase 14 commitment_recommender 1y/3y break-even EXTENSION + coverage_gap * predicted_on_demand_cost * (1 - expected_discount_pct)) + `savings_realized_krw` (SUM on_demand_cost - commitment_cost + Phase 14 optimization realized_savings EXTENSION + savings_per_commitment_type aggregation) + `idle_commitment_krw` (SUM committed_amount_krw * (1 - utilization_pct / 100) for utilization < 70% commitments + Phase 14 idle resource detection EXTENSION + remediation recommendation) + `renewal_decision_score` (weighted score 0~100 = utilization_pct * 0.4 + coverage_pct * 0.3 + savings_realized_krw_normalized * 0.2 + expiring_urgency * 0.1 + decision: auto_renew score ≥ 80 / manual_review 40 ≤ score < 80 / do_not_renew score < 40) 결정 wire + 6 mandatory KPI (total_commitment_value_krw + coverage_pct + utilization_pct + expiring_commitments_30d + savings_realized_krw + renewal_decision_score) + 2 optional (recommended_purchase_krw + idle_commitment_krw) + 4 industries baseline EXTENSION + per-tenant override EXTENSION + cache key = `(tenant_id, period_key, kpi_set_hash, kpi_values_hash)` 결정 wire + audit-first INSERT `cross_module_commitment_kpi_calculated` + `finops_commitment_dry_run_executed` CR 1-1 verbatim 결정 wire + NFR11 P95 ≤ 30s SLO 결정 wire. 결정 wire 일자: 2026-08-25 |
| **A496** | **commitment report generation engine + 5-framework support 결정 wire** — Phase 18 = §F34.3 commitment_report_generator + §F34.4 scheduled_commitment_dispatch 결정 wire = §F34.3 3 NEW format generator (PDF: reportlab 4.0.7 + Korean font Pretendard-Regular.ttf + 8-section template executive summary + commitment inventory + coverage analysis + utilization analysis + expiration schedule + purchase recommendation + savings realized + renewal decisions) + CSV (Excel-compatible UTF-8 BOM + 14 columns commitment_id + cloud_provider + commitment_type + term + start_date + end_date + committed_amount_krw + utilized_amount_krw + utilization_pct + coverage_pct + savings_realized_krw + status + trace_id + period_key) + Excel (xlsxwriter 3.1.9 + 3 sheets Commitment Inventory + KPI Summary + Renewal Decisions + 차트 coverage trend + utilization trend + savings realized + conditional formatting utilization < 70% → red highlight) + 3 cadence (weekly default + monthly + quarterly + annual) + 5-framework support (FinOps Foundation Framework commitment optimization pillar + AWS Cost Optimization Pillar RI/SP best practices + Azure Cost Optimization reserved instances + GCP Cost Optimization CUDs + 한국 조달청 클라우드 commitment 가이드라인) + CommitmentReport TypedDict 14 fields + expiring_commitments_30d 강조 (report PDF 첫 페이지에 expiring commitments summary 박스 + renewal_decision_score 기반 색상 coding red < 40 / yellow 40~80 / green ≥ 80) + audit-first INSERT `commitment_report_generated` + `commitment_report_exported` 2 NEW CR 1-1 verbatim + cross-module commitment KPI 7 module source attribution sequence 결정 wire. / §F34.4 scheduled_commitment_dispatch + APScheduler 3.10.4 AD-14 stack pin EXTENSION + 4 NEW cron schedules (weekly Mon 09:00 KST = `0 9 * * 1` pytz timezone('Asia/Seoul') + monthly 1st-day 09:00 KST = `0 9 1 * *` + quarterly 1st-day 09:00 KST = `0 9 1 */3 *` + annual Jan-1 09:00 KST = `0 9 1 1 *`) 결정 wire + recipients JSONB (Slack webhook URL + Email SMTP + S3 archive commitment_report_{period_key}.pdf upload + MS Teams Adaptive Card 1.4) + role-based recipient (owner-only AD-22 verbatim + Epic 12 2FA 챌린지 보존) + idempotency_key (commitment_dispatch_{tenant_id}_{cadence}_{period_key}) + ScheduledCommitmentDispatch TypedDict 11 fields 결정 wire + last_status enum (success / failed / skipped / dry_run) + retry 3회 exponential backoff + alert routing (owner Slack DM) + audit-first INSERT `commitment_scheduled_dispatch_evaluated` + `commitment_report_dispatched` 2 NEW CR 1-1 verbatim + AD-22 owner-only RBAC EXTENSION + Epic 12 2FA 챌린지 보존 결정 wire. 결정 wire 일자: 2026-08-25 |
| **A497** | **commitment dashboard UI 5 sub-components + Capability matrix v1.44 EXTENSION FINOPS_COMMITMENT + CR lessons applied 18종 결정 wire** — Phase 18 = §F34.5 + §F34.6 + §F34.7 EXTENSION 결정 wire = §F34.5 tenant-scoped commitment role RBAC owner-only (Role.COMMITMENT_VIEWER 1 NEW enum + Capability.FINOPS_COMMITMENT 1 NEW enum + 4 industries grants ✅/✅/✅/✅ industry-agnostic + Epic 12 2FA 챌린지 보존 + AD-22 owner-only RBAC + require_commitment_role() Dependency 1 NEW wire + require_finops_commitment() Dependency 1 NEW wire + commitment_viewer 테이블 NEW + commitment viewer grant workflow EXTENSION) / §F34.6 commitment dashboard UI 5 sub-components (CommitmentInventoryAggregator + CommitmentKPISelector + CommitmentReportGeneratorPanel + ScheduledCommitmentDispatchConfigPanel + CommitmentTrendMiniChart) + apps/web/components/finops/FinopsCommitmentDashboardPanel.tsx NEW Client component 5-tab layout (Inventory / KPI / Reports / Dispatch / Trends) + ko-KR.json `finops_commitment.*` namespace EXTENSION ~30 keys (CR 11-4 D-002 verbatim SSOT) + apps/web/lib/finops/commitment-types.ts NEW TypeScript mirror (CR 12-5 D-PARITY-01 inversion) + `(dashboard)` route group 보호 + ARIA labels WCAG 2.1 AA 결정 wire / §F34.7 Capability matrix v1.43 → v1.44 EXTENSION FINOPS_COMMITMENT 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅ (CR 12-1 L4 precedent 미러, FINOPS_SUSTAINABILITY Phase 17 wire + FINOPS_REPORTING Phase 16 wire + FINOPS_TAG_GOVERNANCE Phase 15 wire + FINOPS_OPTIMIZATION Phase 14 wire + FINOPS_FORECASTING_CAPACITY_PLANNING Phase 13 wire + FINOPS_ANOMALY_DETECTION+FINOPS_BUDGET_ALERT Phase 12 wire + FINOPS_SHOWBACK+FINOPS_CHARGEBACK Phase 11 wire + SLO_ENGINEERING Phase 10 wire + CHAOS_ENGINEERING Phase 9 wire + PERFORMANCE_TESTING Phase 8 wire + OBSERVABILITY_TRACES+OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP+MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind) + apps/api/core/capability.py MODIFIED Capability.FINOPS_COMMITMENT + apps/api/dependencies/capability.py MODIFIED require_finops_commitment + apps/api/core/audit_action.py MODIFIED ActionClass.FINOPS_COMMITMENT + FinopsCommitmentAction 8 NEW Literal + apps/api/core/errors.py MODIFIED 16 NEW typed exception classes (CommitmentInventoryAggregationError + CommitmentInventoryScopeError + CommitmentInventoryPeriodError + CommitmentCrossModuleJoinError + CommitmentKPIError + CommitmentReportGenerationError + CommitmentReportExportError + CommitmentReportArchiveError + ScheduledCommitmentDispatchError + CommitmentCronExpressionInvalidError + CommitmentRecipientResolverError + CommitmentDispatchIdempotencyViolationError + CommitmentRolePermissionError + CommitmentTenantScopeViolationError + CommitmentCapabilityGateViolationError + CommitmentAccuracyDegradationError) CR 12-5 D-14 envelope + alembic 0050 phase_18_finops_commitment.py NEW 6 NEW tables + RLS 6 tables + 2 preview tables + drift detector `tests/integration/test_capability_matrix_v1_44_drift.py` NEW 8 NEW pytest cases EXTENSION + ALLOWED_SERVICE_SUBMODULES (CR 11-3 lesson) 즉시 sweep EXTENSION = m18_finops_commitment 신규 submodule 등록 + Phase 11~17 verbatim EXTENSION (Phase 17 m17_finops_sustainability 패턴 보존). 결정 wire 일자: 2026-08-25 |
| **A498** | **dry-run + Tests + wire scope T1~T8 + CR lessons applied 18종 + D-FINOPS-8 honestly DEFER 결정 wire** — Phase 18 = §F34.8 EXTENSION 결정 wire = dry-run mode 결정 wire (`--finops-commitment-inventory-dry-run` + `--finops-commitment-kpi-dry-run` + `--finops-commitment-report-dry-run` + `--finops-scheduled-commitment-dispatch-dry-run` + `--finops-commitment-export-dry-run` 5 NEW CLI flag + dry-run 시 actual commitment_dashboard_viewed + commitment_kpi_refreshed + commitment_report_generated + commitment_report_dispatched + commitment_scheduled_dispatch_evaluated audit-first INSERT skip) + dry-run preview 결과 결정 wire (phase_18_finops_commitment_inventory_preview + phase_18_finops_commitment_kpi_preview 2 table + preview_id UUID PK + tenant_id UUID + preview_type enum + period_key TEXT + preview_data JSONB + computed_at TIMESTAMPTZ DEFAULT NOW() + trace_id TEXT + RLS 자동 적용 CR 0-2 verbatim + audit-first INSERT finops_commitment_dry_run_executed) + tests 결정 wire (~+62 NEW pytest PASS (commitment_inventory_aggregator 8 + commitment_kpi_selector 8 + commitment_report_generator 8 + scheduled_commitment_dispatch 6 + alembic 0050 6 + audit action 8 + capability matrix v1.44 8 + dry-run 6 + 5 cloud provider integration 6 = ~+62 NEW pytest PASS) + ~+7 NEW vitest PASS (CommitmentInventoryAggregator 1 + CommitmentKPISelector 1 + CommitmentReportGeneratorPanel 1 + ScheduledCommitmentDispatchConfigPanel 1 + CommitmentTrendMiniChart 1 + ko-KR SSOT 2 = ~+7 NEW vitest PASS) + 0 NEW ruff + 0 regressions) + wire scope T1~T8 결정 wire (T1 commitment_inventory_aggregator + commitment_kpi_selector + T2 commitment_report_generator + 3 format generators + T3 scheduled_commitment_dispatch + APScheduler 4 cadence + T4 alembic 0050 phase_18_finops_commitment 6 tables + RLS 6 tables + 2 preview tables + T5 audit action EXTENSION 8 NEW + 16 NEW typed exception classes + T6 capability v1.44 EXTENSION + frontend finops commitment dashboard 5 NEW components + T7 scheduled_commitment_dispatch_job wire + T8 3중 게이트 FINAL CLEAN atomic commit) + estimated ~+28-33 files estimate = ~+21 NEW + ~+12 MODIFIED 결정 wire + Phase 17 wire `97cfe4e` 의 ~28 files pattern verbatim EXTENSION 결정 wire + Phase 11~17 wire 의 ~25-30 files pattern verbatim EXTENSION + A19 cohesion pattern 9 surface EXTENSION PASS 결정 wire (FinOps Cloud Commitment Management surface NEW = F34.1~F34.8 + Phase 17 sustainability + Phase 16 reporting + Phase 15 tag governance + Phase 14 optimization + Phase 13 forecasting + Phase 12 anomaly detection + Phase 11 showback carry-over chain 정합) + CR lessons applied 18종 (CR 0-2 + CR 1-1 + CR 4-3/4-4 + CR 9-6 + CR 11-3 + CR 11-4 + CR 12-1 + CR 12-5 D-14 + CR 12-5 D-PARITY-01 + CR 12-5 D-GATE-01 + A19 + A36 + AD-14 + AD-22 + AD-45 + NFR4 + NFR18) + D-FINOPS-8 신규 honestly DEFER 보존 (Phase 18 territory 의 backend extension 진입 시점에 deferred 결정 wire = AWS RI marketplace cross-account 2nd-hand RI 거래 detail + GCP CUD flexibile/fixed tier 최적화 detail + Naver Cloud / KT Cloud commitment API stability 검증 detail + commitment auto-renewal webhook integration detail + 5 cloud provider unified cost reconciliation detail — 모두 단일 sprint `wire` 진입이 아닌 docs-only entry 에서 honestly defer 결정 wire 보존) + 1-day atomic sprint discipline 결정 wire. 결정 wire 일자: 2026-08-25 |

### 결정 entry — Phase 19 PRD entry 결정 entries (A514~A518, cj-style 137번째 epic 연속 정직 회복 = master PRD v4.9 → v5.0 atomic edit docs only 진입 정합 보존)

| 결정 ID | 내용 |
|---|---|
| **A514** | **Phase 19 PRD entry 진입 결정 wire** — Phase 18 close-out retro `de72f50` (cj-style 136번째 wire entry) 결정 wire DONE 진입 정합 보존 후 옵션 5종 중 옵션 (a) Phase 19+ 진입 + 옵션 (a) FinOps Pricing, Rate Card & TCO Modeling (Recommended) 결정 wire 진입 (AskUserQuestion 결정 verbatim bind, Epic 1 ~ Epic 17 + Phase 3 ~ Phase 18 + 1st release cycle 모두 wire DONE 진입 정합 보존 후 다음 territory). 결정 wire 진입 시점에 master PRD v4.9 → v5.0 atomic edit 결정 wire (front matter title v4.9 → v5.0 + changelog v5.0 entry prepend + §F35 신규 + §15 로드맵 Phase 19 row + §부록 A A514~A518 신규 결정 표 + AD-46 신규 결정 (a)~(g) 7 sub-decisions 모두 결정 wire 진입). D-FINOPS-9 신규 honestly DEFER 보존 (Phase 19 wire 진입 시점에 naturally defer 결정 wire = AWS EDP 자동 negotiation bot detail + Azure EA consumption commit reconciliation detail + GCP CUD flexible/fixed tier break-even optimization detail + Naver Cloud / KT Cloud pricing API stability 검증 detail + 5 cloud provider unified cost reconciliation detail + blended vs unblended 실시간 차이 추적 detail + marketplace SaaS pricing 파편화 통합 detail — 모두 단일 sprint `wire` 진입이 아닌 docs-only entry 에서 honestly defer 결정 wire 보존). 결정 wire 일자: 2026-08-25 |
| **A515** | **pricing KPI 8 NEW 결정 wire** — Phase 19 = §F35.2 tco_modeling_selector 8 NEW KPI TypedDict 결정 wire = `total_blended_rate_krw_per_hour` (sum(effective_rate * usage_hours) / sum(usage_hours) — 가중 평균 + 4 industries baseline manufacturing 2500 + service 1800 + manufacturing_service 2200 + manufacturing_service_other 2700 KRW/hour) + `effective_discount_pct` ((1 - total_blended_rate / total_on_demand_rate) * 100 + range 0.00~100.00 + 4 industries baseline manufacturing ≥ 35% + service ≥ 45% + manufacturing_service ≥ 40% + manufacturing_service_other ≥ 30%) + `tco_1year_commitment_krw` (upfront_fee_1y + monthly_rate_1y * 12 + 4 industries baseline manufacturing ≤ 50M + service ≤ 30M + manufacturing_service ≤ 40M + manufacturing_service_other ≤ 60M KRW) + `tco_3year_commitment_krw` (upfront_fee_3y + monthly_rate_3y * 36 + 4 industries baseline manufacturing ≤ 130M + service ≤ 75M + manufacturing_service ≤ 100M + manufacturing_service_other ≤ 155M KRW) + `tco_on_demand_krw` (on_demand_rate * 8760 + 4 industries baseline manufacturing ≤ 80M + service ≤ 50M + manufacturing_service ≤ 65M + manufacturing_service_other ≤ 95M KRW) + `cost_per_user_krw` (total_cost / active_user_count + 4 industries baseline manufacturing 50000 + service 30000 + manufacturing_service 40000 + saas 15000 KRW) + `cost_per_transaction_krw` (total_cost / transaction_count + 4 industries baseline manufacturing 200 + service 100 + manufacturing_service 150 + saas 50 KRW) + `unit_economics_score` (revenue_per_unit / cost_per_unit * 100 + range 0.00~100.00 + 4 industries baseline manufacturing 75 + service 85 + manufacturing_service 80 + saas 90) 결정 wire + 6 mandatory KPI (total_blended_rate_krw_per_hour + effective_discount_pct + tco_1year_commitment_krw + tco_3year_commitment_krw + cost_per_user_krw + cost_per_transaction_krw) + 2 optional (tco_on_demand_krw + unit_economics_score) + 4 industries baseline EXTENSION + per-tenant override EXTENSION + cache key = `(tenant_id, period_key, kpi_set_hash, kpi_values_hash)` 결정 wire + audit-first INSERT `cross_module_pricing_kpi_calculated` + `finops_pricing_dry_run_executed` CR 1-1 verbatim 결정 wire + NFR11 P95 ≤ 30s SLO 결정 wire + break_even_months_1y = upfront_fee_1y / (on_demand_cost_1y - commitment_cost_1y) + break_even_months_3y = upfront_fee_3y / (on_demand_cost_3y - commitment_cost_3y) + recommendation = "1y" if break_even_months_1y < 8 else "3y" if break_even_months_3y < 18 else "on_demand" EXTENSION (Phase 14 commitment_recommender 패턴 verbatim EXTENSION). 결정 wire 일자: 2026-08-25 |
| **A516** | **pricing report generation engine + 5-framework support + scheduled dispatch KST cron 4 cadence 결정 wire** — Phase 19 = §F35.3 pricing_report_generator + §F35.4 scheduled_pricing_dispatch 결정 wire = §F35.3 3 NEW format generator (PDF: reportlab 4.0.7 + Korean font Pretendard-Regular.ttf + 6-page template cover + rate card summary table + TCO comparison chart (bar chart) + cost per unit economics dashboard + renewal recommendations + appendix (data sources + methodology)) + CSV (Excel-compatible UTF-8 BOM + 14 columns tenant_id + period_key + cloud_provider + service_code + region + on_demand_rate + negotiated_rate + effective_rate + volume_tier + commitment_discount_pct + tco_1y_krw + tco_3y_krw + tco_on_demand_krw + cost_per_unit_krw) + Excel (openpyxl 3.1.2 + xlsxwriter 3.1.9 + 5 sheets Summary (8 KPI) + Rate Cards (5 cloud provider) + TCO Comparison (1y vs 3y vs on_demand) + Unit Economics (cost per user/transaction) + Methodology (data sources + assumptions)) + 3 cadence (monthly default + quarterly + annual) + 5-framework support (FinOps Foundation Pricing Principles + AWS Pricing Models (On-Demand + Reserved + Savings Plans + Spot) + Azure Pricing Calculator + GCP Pricing Calculator + 한국 공공 조달 가격 가이드라인 (조달청 나라장터 클라우드 가격)) + PricingReport TypedDict 14 fields + audit-first INSERT `pricing_report_generated` + `pricing_report_exported` 2 NEW CR 1-1 verbatim + cross-module pricing KPI 9 module source attribution sequence 결정 wire. / §F35.4 scheduled_pricing_dispatch + APScheduler 3.10.4 AD-14 stack pin EXTENSION + 4 NEW cron schedules (weekly Mon 09:00 KST = `0 9 * * 1` pytz timezone('Asia/Seoul') + monthly 1st-day 09:00 KST = `0 9 1 * *` + quarterly 1st-day 09:00 KST = `0 9 1 */3 *` + annual Jan-1 09:00 KST = `0 9 1 1 *`) 결정 wire + recipients JSONB (Slack webhook URL + Email SMTP + S3 archive pricing_report_{period_key}.pdf upload + MS Teams Adaptive Card 1.4) + role-based recipient (owner-only AD-22 verbatim + Epic 12 2FA 챌린지 보존) + idempotency_key (pricing_dispatch_{tenant_id}_{cadence}_{period_key}) + ScheduledPricingDispatch TypedDict 11 fields 결정 wire + last_status enum (success / failed / skipped / dry_run) + retry 3회 exponential backoff + alert routing (owner Slack DM) + audit-first INSERT `pricing_scheduled_dispatch_evaluated` + `pricing_report_dispatched` 2 NEW CR 1-1 verbatim + AD-22 owner-only RBAC EXTENSION + Epic 12 2FA 챌린지 보존 결정 wire. 결정 wire 일자: 2026-08-25 |
| **A517** | **pricing dashboard UI 5 sub-components + Capability matrix v1.45 EXTENSION FINOPS_PRICING + CR lessons applied 18종 결정 wire** — Phase 19 = §F35.5 + §F35.6 + §F35.7 EXTENSION 결정 wire = §F35.5 tenant-scoped pricing role RBAC owner-only (Role.PRICING_VIEWER 1 NEW enum + Capability.FINOPS_PRICING 1 NEW enum + 4 industries grants ✅/✅/✅/✅ industry-agnostic + Epic 12 2FA 챌린지 보존 + AD-22 owner-only RBAC + require_pricing_role() Dependency 1 NEW wire + require_finops_pricing() Dependency 1 NEW wire + pricing_viewer 테이블 NEW + pricing viewer grant workflow EXTENSION) / §F35.6 pricing dashboard UI 5 sub-components (RateCardAggregatorPanel + TCOModelingSelectorPanel + PricingReportGeneratorPanel + ScheduledPricingDispatchConfigPanel + PricingTrendMiniChart) + apps/web/components/finops/FinopsPricingDashboardPanel.tsx NEW Client component 5-tab layout (Rate Cards / TCO / Reports / Dispatch / Trends) + ko-KR.json `finops_pricing.*` namespace EXTENSION ~30 keys (CR 11-4 D-002 verbatim SSOT) + apps/web/lib/finops/pricing-types.ts NEW TypeScript mirror (CR 12-5 D-PARITY-01 inversion) + apps/web/lib/finops/pricing-client.ts NEW TypeScript client (fetchRateCards + fetchTCOKPIs + generatePricingReport + schedulePricingDispatch 4 methods) + `(dashboard)` route group 보호 + ARIA labels WCAG 2.1 AA 결정 wire / §F35.7 Capability matrix v1.44 → v1.45 EXTENSION FINOPS_PRICING 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅ (CR 12-1 L4 precedent 미러, FINOPS_COMMITMENT Phase 18 wire + FINOPS_SUSTAINABILITY Phase 17 wire + FINOPS_REPORTING Phase 16 wire + FINOPS_TAG_GOVERNANCE Phase 15 wire + FINOPS_OPTIMIZATION Phase 14 wire + FINOPS_FORECASTING_CAPACITY_PLANNING Phase 13 wire + FINOPS_ANOMALY_DETECTION+FINOPS_BUDGET_ALERT Phase 12 wire + FINOPS_SHOWBACK+FINOPS_CHARGEBACK Phase 11 wire + SLO_ENGINEERING Phase 10 wire + CHAOS_ENGINEERING Phase 9 wire + PERFORMANCE_TESTING Phase 8 wire + OBSERVABILITY_TRACES+OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP+MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind) + apps/api/core/capability.py MODIFIED Capability.FINOPS_PRICING + apps/api/dependencies/capability.py MODIFIED require_finops_pricing + apps/api/core/audit_action.py MODIFIED ActionClass.FINOPS_PRICING + FinopsPricingAction 8 NEW Literal (pricing_dashboard_viewed + cross_module_pricing_kpi_calculated + pricing_report_generated + pricing_report_exported + pricing_report_dispatched + pricing_scheduled_dispatch_evaluated + finops_pricing_dry_run_executed + pricing_kpi_refreshed) + apps/api/core/errors.py MODIFIED 16 NEW typed exception classes (RateCardAggregationError + RateCardScopeError + RateCardPeriodError + RateCardProviderError + TCOModelingError + TCOScopeError + TCOPeriodError + TCOBaselineError + PricingReportGenerationError + PricingReportExportError + PricingReportArchiveError + ScheduledPricingDispatchError + PricingCronExpressionInvalidError + PricingRecipientResolverError + PricingDispatchIdempotencyViolationError + PricingAccuracyDegradationError) CR 12-5 D-14 envelope + alembic 0051 phase_19_finops_pricing.py NEW 6 NEW tables + RLS 6 tables + 2 preview tables + drift detector `tests/integration/test_capability_matrix_v1_45_drift.py` NEW 8 NEW pytest cases EXTENSION + ALLOWED_SERVICE_SUBMODULES (CR 11-3 lesson) 즉시 sweep EXTENSION = m19_finops_pricing 신규 submodule 등록 + Phase 11~18 verbatim EXTENSION (Phase 18 m18_finops_commitment 패턴 보존). 결정 wire 일자: 2026-08-25 |
| **A518** | **dry-run + Tests + wire scope T1~T8 + CR lessons applied 18종 + D-FINOPS-9 honestly DEFER 결정 wire** — Phase 19 = §F35.8 EXTENSION 결정 wire = dry-run mode 결정 wire (`--finops-pricing-rate-card-dry-run` + `--finops-pricing-tco-dry-run` + `--finops-pricing-report-dry-run` + `--finops-scheduled-pricing-dispatch-dry-run` + `--finops-pricing-export-dry-run` 5 NEW CLI flag + dry-run 시 actual pricing_dashboard_viewed + pricing_kpi_refreshed + pricing_report_generated + pricing_report_dispatched + pricing_scheduled_dispatch_evaluated audit-first INSERT skip) + dry-run preview 결과 결정 wire (phase_19_finops_pricing_rate_card_preview + phase_19_finops_pricing_tco_preview 2 table + preview_id UUID PK + tenant_id UUID + preview_type enum + period_key TEXT + preview_data JSONB + computed_at TIMESTAMPTZ DEFAULT NOW() + trace_id TEXT + RLS 자동 적용 CR 0-2 verbatim + audit-first INSERT finops_pricing_dry_run_executed) + tests 결정 wire (~+62 NEW pytest PASS (rate_card_aggregator 8 + tco_modeling_selector 8 + pricing_report_generator 8 + scheduled_pricing_dispatch 6 + alembic 0051 6 + audit action 8 + capability matrix v1.45 8 + dry-run 6 + 5 cloud provider integration 6 = ~+62 NEW pytest PASS) + ~+7 NEW vitest PASS (RateCardAggregatorPanel 1 + TCOModelingSelectorPanel 1 + PricingReportGeneratorPanel 1 + ScheduledPricingDispatchConfigPanel 1 + PricingTrendMiniChart 1 + ko-KR SSOT 2 = ~+7 NEW vitest PASS) + 0 NEW ruff + 0 regressions) + wire scope T1~T8 결정 wire (T1 rate_card_aggregator + tco_modeling_selector + T2 pricing_report_generator + 3 format generators + T3 scheduled_pricing_dispatch + APScheduler 4 cadence + T4 alembic 0051 phase_19_finops_pricing 6 tables + RLS 6 tables + 2 preview tables + T5 audit action EXTENSION 8 NEW + 16 NEW typed exception classes + T6 capability v1.45 EXTENSION + frontend finops pricing dashboard 5 NEW components + T7 scheduled_pricing_dispatch_job wire + T8 3중 게이트 FINAL CLEAN atomic commit) + estimated ~+28-33 files estimate = ~+21 NEW + ~+12 MODIFIED 결정 wire + Phase 18 wire `67059cf` 의 ~28 files pattern verbatim EXTENSION 결정 wire + Phase 11~18 wire 의 ~25-30 files pattern verbatim EXTENSION + A19 cohesion pattern 9 surface EXTENSION PASS 결정 wire (FinOps Pricing, Rate Card & TCO Modeling surface NEW = F35.1~F35.8 + Phase 18 commitment + Phase 17 sustainability + Phase 16 reporting + Phase 15 tag governance + Phase 14 optimization + Phase 13 forecasting + Phase 12 anomaly detection + Phase 11 showback carry-over chain 정합) + CR lessons applied 18종 (CR 0-2 + CR 1-1 + CR 4-3/4-4 + CR 9-6 + CR 11-3 + CR 11-4 + CR 12-1 + CR 12-5 D-14 + CR 12-5 D-PARITY-01 + CR 12-5 D-GATE-01 + A19 + A36 + AD-14 + AD-22 + AD-46 + NFR4 + NFR18) + D-FINOPS-9 신규 honestly DEFER 보존 (Phase 19 territory 의 backend extension 진입 시점에 deferred 결정 wire = AWS EDP 자동 negotiation bot detail + Azure EA consumption commit reconciliation detail + GCP CUD flexible/fixed tier break-even optimization detail + Naver Cloud / KT Cloud pricing API stability 검증 detail + 5 cloud provider unified cost reconciliation detail + blended vs unblended 실시간 차이 추적 detail + marketplace SaaS pricing 파편화 통합 detail — 모두 단일 sprint `wire` 진입이 아닌 docs-only entry 에서 honestly defer 결정 wire 보존) + 1-day atomic sprint discipline 결정 wire. 결정 wire 일자: 2026-08-25 |

### 결정 entry — Phase 20 PRD entry 결정 entries (A539~A543, cj-style 142번째 epic 연속 정직 회복 = master PRD v5.0 → v6.0 atomic edit docs only 진입 정합 보존)

| 결정 ID | 내용 |
|---|---|
| **A539** | **Phase 20 PRD entry 진입 결정 wire** — Phase 19.5 carry-over 결정 wire `b2fb1d8` (cj-style 141번째 wire entry) + Phase 19 close-out retro `18ca1ae` (cj-style 140번째) + Phase 19 atomic wire T1~T8 `8db3cfc` (cj-style 139번째) + Phase 19 spec entry `59d15fb` (cj-style 138번째) + Phase 19 PRD entry `ff8a797` (cj-style 137번째) 결정 wire 모두 DONE 진입 정합 보존 후 옵션 5종 중 옵션 (a) Phase 20+ 진입 + 옵션 (a) FinOps Multi-Cloud Cost Unified Reconciliation (Recommended) 결정 wire 진입 (AskUserQuestion 결정 verbatim bind, Epic 1 ~ Epic 17 + Phase 3 ~ Phase 19 + 1st release cycle 모두 wire DONE 진입 정합 보존 후 다음 territory). 결정 wire 진입 시점에 master PRD v5.0 → v6.0 atomic edit 결정 wire (front matter title v5.0 → v6.0 + changelog v6.0 entry prepend + §F36 신규 + §15 로드맵 Phase 20 row + §부록 A A539~A543 신규 결정 표 + AD-47 신규 결정 (a)~(g) 7 sub-decisions 모두 결정 wire 진입). D-FINOPS-9 honestly DEFER 보존 + Phase 19.5 carry-over 결정 wire `b2fb1d8` 의 AD-47 신규 (a)~(g) 7 sub-decisions 모두 결정 wire 진입 = Phase 20 PRD entry 진입 시점에 carry-over chain 정직 회복 결정 wire 진입 (5 cloud provider unified rate card reconciliation P0 + 5 cloud provider unified cost reconciliation P0 + AWS EDP 자동 negotiation bot P1 + Azure EA consumption commit reconciliation P1 + GCP CUD flexible/fixed tier break-even optimization P1 + Naver/KT public pricing API stability 검증 P2 + blended vs unblended 실시간 차이 추적 P2 + marketplace SaaS pricing 파편화 통합 P2 = 7 unique 항목 모두 Phase 20 territory 흡수). 결정 wire 일자: 2026-08-25 |
| **A540** | **multi_cloud_rate_card_reconciliation + cost_reconciliation + negotiation_bot 결정 wire** — Phase 20 = §F36.1 + §F36.2 + §F36.3 EXTENSION 결정 wire = §F36.1 multi_cloud_rate_card_reconciliation_aggregator 5 cloud provider cross-rollup EXTENSION (MultiCloudRateCardReconciliation TypedDict 18 fields + 9-module cross-join + 5 cloud provider rate card (AWS EDP + Azure EA + GCP CUD Pricing + Naver Cloud Volume Tier + KT Cloud Volume Tier) + 5-tier source priority chain (negotiation + contract + rate_card_api + manual + audit) + variance_pct > 5.0% alert + audit-first INSERT multi_cloud_rate_card_reconciled CR 1-1 verbatim) / §F36.2 multi_cloud_cost_reconciliation_aggregator unified source of truth (MultiCloudCostReconciliation TypedDict 19 fields + 9-module cross-join + 5 cloud provider cost (AWS Cost Explorer + Azure Cost Management + GCP Billing + Naver Cloud Billing + KT Cloud Billing) + unified source of truth 단일 view 모든 dashboard/KPI/report/reconciliation/drill-down 참조 + 5-tier cost source priority chain + cost_variance_pct > 3.0% alert + audit-first INSERT multi_cloud_cost_reconciled CR 1-1 verbatim) / §F36.3 negotiation_bot 3 cloud provider support (AWS EDP 자동 negotiation + Azure EA consumption commit reconciliation + GCP CUD flexible/fixed tier break-even optimization) + MINIMUM_SAVINGS_PCT=5.0 + MINIMUM_SAVINGS_KRW=1M + MAX_NEGOTIATIONS_PER_MONTH=3 + MAX_AUTO_TRIGGER_PER_DAY=1 + confidence_score (utilization_stability × 0.6 + historical_accuracy × 0.4) + risk_score (savings_pct × 0.4 + commitment_term × 0.3 + commitment_flexibility × 0.3) + auto-negotiate_ready / manual_review_required / low_confidence 3 status + idempotency_key + audit-first INSERT negotiation_bot_triggered CR 1-1 verbatim + AD-22 owner-only RBAC + Epic 12 2FA 챌린지 mandatory 결정 wire. 결정 wire 일자: 2026-08-25 |
| **A541** | **blended_unblended_tracker + Naver/KT public pricing API stability 검증 결정 wire** — Phase 20 = §F36.4 EXTENSION 결정 wire = blended_unblended_tracker 3 cloud provider support (AWS + Azure + GCP) + BlendedUnblendedDiff TypedDict 14 fields + 1-hour cron refresh + real_time drift 감지 (rate_diff_pct > 5% → alert) + audit-first INSERT blended_unblended_tracked CR 1-1 verbatim / Naver/KT public pricing API stability 검증 P2 (uptime ≥ 99.0% + P95 ≤ 2s + data_freshness ≤ 24h + rate_limited exponential backoff 60s → 120s → 240s + accuracy ≥ 95% match validation 4-week rolling sample + accuracy < 95% → alert + audit-first INSERT multi_cloud_rate_card_reconciled with accuracy_degraded flag) + 4-tier rate card format (tier_1 default + tier_2 5% discount + tier_3 10% discount + tier_4 custom) 결정 wire + Phase 19 wire `8db3cfc` 의 pricing_freshness tracking pattern verbatim EXTENSION + LISTEN/NOTIFY channel phase_20_blended_unblended_diff 신규 + LISTEN trigger after INSERT on phase_20_blended_unblended_diff table → broadcast to all tenant workers → all RSC page revalidate 결정 wire + CR 0-2 RLS + CR 1-1 audit-first INSERT verbatim EXTENSION + 4 industries baseline (manufacturing ≤ 100 + service ≤ 50 + manufacturing_service ≤ 80 + saas ≤ 30 KRW/hour) + per-tenant override EXTENSION 결정 wire. 결정 wire 일자: 2026-08-25 |
| **A542** | **marketplace_saas_pricing_integrator + multi_cloud dashboard UI 5 sub-components 결정 wire** — Phase 20 = §F36.5 + §F36.6 EXTENSION 결정 wire = §F36.5 marketplace_saas_pricing_integrator 5 marketplace source support (AWS Marketplace + Azure Marketplace + GCP Marketplace + Naver Marketplace + KT Marketplace) + MarketplaceSaaSPricingRollup TypedDict 16 fields + 5 marketplace adapter pattern (각 marketplace 별 parsing logic 격리) + unified SaaS pricing view (단일 schema + cheapest alternative suggestion across 5 marketplace + savings_pct guard 5~10%) + freshness tracking (staleness_threshold_hours=24 + 4-hour cron refresh + on-demand refresh) + audit-first INSERT marketplace_saas_pricing_integrated CR 1-1 verbatim / §F36.6 multi_cloud dashboard UI 5 sub-components (MultiCloudRateCardReconciliationPanel + MultiCloudCostReconciliationPanel + NegotiationBotConfigPanel + BlendedUnblendedTrackerPanel + MarketplaceSaaSPricingPanel) + apps/web/components/finops/FinopsMultiCloudDashboardPanel.tsx NEW Client component ~+250 LOC + 5-tab layout (Rate Cards / Costs / Negotiation / Tracking / Marketplace) + owner-only RBAC AD-22 verbatim + Epic 12 2FA 챌린지 mandatory + ko-KR.json `finops_multi_cloud.*` namespace EXTENSION ~30 keys (CR 11-4 D-002 verbatim SSOT) + apps/web/lib/finops/multi-cloud-types.ts NEW TypeScript mirror (CR 12-5 D-PARITY-01 inversion) + apps/web/lib/finops/multi-cloud-client.ts NEW TypeScript client (fetchRateCardReconciliations + fetchCostReconciliations + triggerNegotiationBot + trackBlendedUnblended + integrateMarketplacePricing 5 methods) + Recharts 2.12.7 AD-14 stack pin EXTENSION + ARIA labels WCAG 2.1 AA + dry-run mode UI (NegotiationBotConfigPanel 진입 시 dry-run toggle) + `(dashboard)` route group 보호 + scheduled dispatch KST cron weekly Mon 09:00 + monthly 1st-day 09:00 + quarterly 1st-day 09:00 + annual Jan-1 09:00 결정 wire + Phase 19 wire `8db3cfc` 의 pricing dashboard pattern verbatim EXTENSION 결정 wire. 결정 wire 일자: 2026-08-25 |
| **A543** | **Capability matrix v1.46 EXTENSION FINOPS_MULTI_CLOUD_UNIFIED_RECONCILIATION + dry-run + Tests + wire scope T1~T8 + CR lessons applied 18종 결정 wire** — Phase 20 = §F36.7 + §F36.8 EXTENSION 결정 wire = §F36.7 Capability matrix v1.45 → v1.46 EXTENSION FINOPS_MULTI_CLOUD_UNIFIED_RECONCILIATION 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅ (CR 12-1 L4 precedent 미러, FINOPS_PRICING Phase 19 wire + FINOPS_COMMITMENT Phase 18 wire + FINOPS_SUSTAINABILITY Phase 17 wire + FINOPS_REPORTING Phase 16 wire + FINOPS_TAG_GOVERNANCE Phase 15 wire + FINOPS_OPTIMIZATION Phase 14 wire + FINOPS_FORECASTING_CAPACITY_PLANNING Phase 13 wire + FINOPS_ANOMALY_DETECTION+FINOPS_BUDGET_ALERT Phase 12 wire + FINOPS_SHOWBACK+FINOPS_CHARGEBACK Phase 11 wire + SLO_ENGINEERING Phase 10 wire + CHAOS_ENGINEERING Phase 9 wire + PERFORMANCE_TESTING Phase 8 wire + OBSERVABILITY_TRACES+OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP+MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind) + apps/api/core/capability.py MODIFIED Capability.FINOPS_MULTI_CLOUD_UNIFIED_RECONCILIATION + apps/api/dependencies/capability.py MODIFIED require_finops_multi_cloud + apps/api/core/audit_action.py MODIFIED ActionClass.FINOPS_MULTI_CLOUD_UNIFIED_RECONCILIATION + FinopsMultiCloudAction 8 NEW Literal (multi_cloud_dashboard_viewed + multi_cloud_rate_card_reconciled + multi_cloud_cost_reconciled + negotiation_bot_triggered + marketplace_saas_pricing_integrated + multi_cloud_dry_run_executed + multi_cloud_kpi_refreshed + blended_unblended_tracked) + apps/api/core/errors.py MODIFIED 16 NEW typed exception classes (MultiCloudRateCardReconciliationError + MultiCloudRateCardScopeError + MultiCloudRateCardPeriodError + MultiCloudRateCardProviderError + MultiCloudCostReconciliationError + MultiCloudCostScopeError + MultiCloudCostPeriodError + MultiCloudCostProviderError + NegotiationBotError + NegotiationBotGuardError + NegotiationBotConfidenceError + NegotiationBotAutoTriggerError + BlendedUnblendedTrackerError + BlendedUnblendedDriftError + MarketplaceSaaSPricingIntegrationError + MarketplaceSaaSPricingFreshnessError) CR 12-5 D-14 envelope + alembic 0052 phase_20_multi_cloud_unified_reconciliation.py NEW 8 NEW tables + RLS 8 tables + 3 preview tables + drift detector `tests/integration/test_capability_matrix_v1_46_drift.py` NEW 8 NEW pytest cases EXTENSION + ALLOWED_SERVICE_SUBMODULES (CR 11-3 lesson) 즉시 sweep EXTENSION = m20_finops_multi_cloud 신규 submodule 등록 + Phase 11~19 verbatim EXTENSION (Phase 19 m19_finops_pricing 패턴 보존). / §F36.8 EXTENSION 결정 wire = dry-run mode 결정 wire (`--finops-multi-cloud-rate-card-dry-run` + `--finops-multi-cloud-cost-dry-run` + `--finops-multi-cloud-negotiation-bot-dry-run` + `--finops-multi-cloud-blended-unblended-dry-run` + `--finops-multi-cloud-marketplace-dry-run` 5 NEW CLI flag + dry-run 시 actual multi_cloud_dashboard_viewed + multi_cloud_rate_card_reconciled + multi_cloud_cost_reconciled + negotiation_bot_triggered + marketplace_saas_pricing_integrated + multi_cloud_dry_run_executed + multi_cloud_kpi_refreshed + blended_unblended_tracked audit-first INSERT skip) + dry-run preview 결과 결정 wire (phase_20_multi_cloud_rate_card_preview + phase_20_multi_cloud_cost_preview + phase_20_negotiation_bot_preview 3 table + preview_id UUID PK + tenant_id UUID + preview_type enum + period_key TEXT + preview_data JSONB + computed_at TIMESTAMPTZ DEFAULT NOW() + trace_id TEXT + RLS 자동 적용 CR 0-2 verbatim + audit-first INSERT multi_cloud_dry_run_executed) + tests 결정 wire (~+92 NEW pytest PASS (rate_card_reconciliation_aggregator 10 + cost_reconciliation_aggregator 10 + negotiation_bot 30 + blended_unblended_tracker 6 + marketplace_saas_pricing_integrator 8 + alembic 0052 6 + audit action 8 + capability matrix v1.46 8 + dry-run 6 + 5 cloud provider integration 8 = ~+92 NEW pytest PASS) + ~+7 NEW vitest PASS (MultiCloudRateCardReconciliationPanel 1 + MultiCloudCostReconciliationPanel 1 + NegotiationBotConfigPanel 1 + BlendedUnblendedTrackerPanel 1 + MarketplaceSaaSPricingPanel 1 + ko-KR SSOT 2 = ~+7 NEW vitest PASS) + 0 NEW ruff + 0 regressions) + wire scope T1~T8 결정 wire (T1 rate_card_reconciliation_aggregator + cost_reconciliation_aggregator + T2 negotiation_bot + 3 cloud provider + T3 blended_unblended_tracker + Naver/KT stability 검증 + marketplace_saas_pricing_integrator + T4 alembic 0052 phase_20_multi_cloud_unified_reconciliation 8 tables + RLS 8 tables + 3 preview tables + T5 audit action EXTENSION 8 NEW + 16 NEW typed exception classes + T6 capability v1.46 EXTENSION + frontend finops multi_cloud dashboard 5 NEW components + T7 scheduled_dispatch_job wire + T8 3중 게이트 FINAL CLEAN atomic commit) + estimated ~+30-35 files estimate = ~+22 NEW + ~+13 MODIFIED 결정 wire + Phase 19 wire `8db3cfc` 의 ~28 files pattern verbatim EXTENSION 결정 wire + Phase 11~19 wire 의 ~25-30 files pattern verbatim EXTENSION + A19 cohesion pattern 9 surface EXTENSION PASS 결정 wire (FinOps Multi-Cloud Cost Unified Reconciliation surface NEW = F36.1~F36.8 + Phase 19 pricing + Phase 18 commitment + Phase 17 sustainability + Phase 16 reporting + Phase 15 tag governance + Phase 14 optimization + Phase 13 forecasting + Phase 12 anomaly detection + Phase 11 showback carry-over chain 정합) + CR lessons applied 18종 (CR 0-2 + CR 1-1 + CR 4-3/4-4 + CR 9-6 + CR 11-3 + CR 11-4 + CR 12-1 + CR 12-5 D-14 + CR 12-5 D-PARITY-01 + CR 12-5 D-GATE-01 + A19 + A36 + AD-14 + AD-22 + AD-47 + NFR4 + NFR18) + D-FINOPS-9 honestly DEFER 보존 + Phase 19.5 carry-over 결정 wire `b2fb1d8` 의 AD-47 신규 (a)~(g) 7 sub-decisions 모두 결정 wire 진입 (Phase 20 territory 의 backend extension 진입 시점에 deferred 결정 wire = 5 cloud provider unified rate card reconciliation backend detail + AWS EDP 자동 negotiation bot backend detail + Azure EA consumption commit reconciliation backend detail + GCP CUD flexible/fixed tier break-even optimization backend detail + Naver/KT public pricing API stability 검증 backend detail + 5 cloud provider unified cost reconciliation backend detail + blended vs unblended 실시간 차이 추적 backend detail + marketplace SaaS pricing 파편화 통합 backend detail — 모두 단일 sprint `wire` 진입이 아닌 docs-only entry 에서 honestly defer 결정 wire 보존) + 1-day atomic sprint discipline 결정 wire. 결정 wire 일자: 2026-08-25 |

### 결정 entry — Phase 21 PRD entry 결정 entries (A574~A578, cj-style 149번째 epic 연속 정직 회복 = master PRD v6.0 → v7.0 atomic edit docs only 진입 정합 보존)

| 결정 ID | 내용 |
|---|---|
| **A574** | **Phase 21 PRD entry 진입 결정 wire** — Phase 20.5 close-out retro `8505d98` (cj-style 148번째) + Phase 20.5 atomic wire T1~T3 `46ddcc5` (cj-style 147번째) + Phase 20.5 spec entry `e23141d` (cj-style 146번째) + Phase 20 close-out retro `f361016` (cj-style 145번째) + Phase 20 atomic wire T1~T8 `52dad7f` (cj-style 144번째) + Phase 20 spec entry `bdc7997` (cj-style 143번째) + Phase 20 PRD entry `ff8a797` (cj-style 142번째) 결정 wire 모두 DONE 진입 정합 보존 후 옵션 (a) Phase 21+ 진입 + 옵션 (a) FinOps Reserved Capacity Planning (Recommended) 결정 wire 진입 (process-design 관점 최적 대안 분석 결과: reuse 최대화 + risk 최소화 + 비즈니스 가치 최고). 결정 wire 진입 시점에 master PRD v6.0 → v7.0 atomic edit 결정 wire (front matter title v6.0 → v7.0 + changelog v7.0 entry prepend + §F37 신규 + §15 로드맵 Phase 21 row + §부록 A A574~A578 신규 결정 표 + AD-49 신규 결정 (a)~(g) 7 sub-decisions 모두 결정 wire 진입). D-FINOPS-10 신규 honestly DEFER 보존 + Phase 21 carry-over 결정 wire 의 AD-49 신규 (a)~(g) 7 sub-decisions 모두 결정 wire 진입 = Phase 21 PRD entry 진입 시점에 carry-over chain 정직 회복 결정 wire 진입 (demand_forecast_aggregator 5-module cross-join P0 + capacity_planning_aggregator reserved_capacity_tier selection P0 + commitment_recommendation_engine confidence + risk + owner approval P1 + reserved_capacity_orchestrator composition_step_chain 5 step + 4 cadence schedule P1 = 4 unique 항목 모두 Phase 21 territory 흡수). 결정 wire 일자: 2026-08-26 |
| **A575** | **demand_forecast_aggregator + capacity_planning_aggregator 결정 wire** — Phase 21 = §F37.1 + §F37.2 EXTENSION 결정 wire = §F37.1 demand_forecast_aggregator 5 FinOps modules cross-join EXTENSION (ReservedCapacityDemandForecast TypedDict 16 fields + Phase 13 forecast + Phase 14 optimization + Phase 18 commitment + Phase 19 pricing + Phase 20 multi_cloud 5 module 의 가중 평균 = single forecasted_demand_krw + confidence interval calculation (low/high/width → volatile + growth + declining + stable) + seasonal_factor + growth_rate_pct 4 industries baseline EXTENSION + audit-first INSERT demand_forecast_calculated CR 1-1 verbatim) / §F37.2 capacity_planning_aggregator 6 reserved_capacity_tier enum (1_year_no_upfront + 1_year_partial_upfront + 1_year_all_upfront + 3_year_no_upfront + 3_year_partial_upfront + 3_year_all_upfront) + break_even_utilization_pct tier selection algorithm + capacity_headroom_pct target 10~20% + MINIMUM_SAVINGS_PCT=5.0 + MINIMUM_SAVINGS_KRW=1M + audit-first INSERT capacity_planning_recommended CR 1-1 verbatim + 4 industries baseline EXTENSION + per-tenant override EXTENSION + LISTEN/NOTIFY channel phase_21_demand_forecast_calculated + phase_21_capacity_planning_recommended 신규 + Phase 20 wire `52dad7f` 의 multi_cloud LISTEN/NOTIFY 패턴 verbatim EXTENSION + AD-22 owner-only RBAC + Epic 12 2FA 챌린지 mandatory 결정 wire. 결정 wire 일자: 2026-08-26 |
| **A576** | **commitment_recommendation_engine + reserved_capacity_orchestrator 결정 wire** — Phase 21 = §F37.3 + §F37.4 EXTENSION 결정 wire = §F37.3 commitment_recommendation_engine (CommitmentRecommendation TypedDict 17 fields + confidence_score (utilization_stability × 0.4 + historical_accuracy × 0.3 + demand_forecast_confidence_pct × 0.3) + risk_score (savings_pct × 0.4 + commitment_term × 0.3 + commitment_flexibility × 0.3) + execution_strategy 4 enum (auto_execute_ready + manual_review_required + owner_approval_required + low_confidence) + high-value (≥ 10M KRW/year savings) → Epic 12 2FA 챌린지 mandatory + owner approval flow (Slack DM + 2FA + approval_chain) + audit-first INSERT commitment_recommendation_generated CR 1-1 verbatim + 5-module composition pattern (Phase 13 + 14 + 18 + 19 + 20 + 21 가중 평균 = single recommendation_id)) / §F37.4 reserved_capacity_orchestrator composition_step_chain 5 step (demand_forecast → capacity_planning → commitment_recommendation → execute) + 4 cadence schedule (daily 02:00 KST + weekly Mon 03:00 KST + monthly 1st-day 04:00 KST + quarterly 1st-day 05:00 KST + pytz Asia/Seoul) + dry-run mode + 1 NEW CLI flag (`--finops-reserved-capacity-orchestrator-dry-run`) + audit-first INSERT reserved_capacity_orchestrator_triggered CR 1-1 verbatim + LISTEN/NOTIFY channel phase_21_reserved_capacity_orchestrated 신규 + Phase 20 wire `52dad7f` 의 multi_cloud LISTEN/NOTIFY 패턴 verbatim EXTENSION 결정 wire. 결정 wire 일자: 2026-08-26 |
| **A577** | **reserved_capacity dashboard UI 5 sub-components 결정 wire** — Phase 21 = §F37.5 EXTENSION 결정 wire = reserved_capacity dashboard UI 5 sub-components (DemandForecastPanel + CapacityPlanningPanel + CommitmentRecommendationPanel + ReservedCapacityOrchestratorPanel + ReservedCapacityTrendMiniChart) + apps/web/components/finops/FinopsReservedCapacityDashboardPanel.tsx NEW Client component ~+250 LOC + 5-tab layout (Demand Forecast / Capacity Planning / Commitment Recommendation / Orchestrator / Trend) + Recharts 2.12.7 AD-14 stack pin EXTENSION + owner-only RBAC AD-22 verbatim + Epic 12 2FA 챌린지 mandatory + ko-KR.json `finops_reserved_capacity.*` namespace EXTENSION ~30 keys (CR 11-4 D-002 verbatim SSOT) + apps/web/lib/finops/reserved-capacity-types.ts NEW TypeScript mirror (CR 12-5 D-PARITY-01 inversion) + apps/web/lib/finops/reserved-capacity-client.ts NEW TypeScript client (aggregateDemandForecast + planReservedCapacity + generateCommitmentRecommendation + orchestrateReservedCapacity 4 methods) + Recharts 5 NEW charts (AreaChart + BarChart + LineChart + GanttChart + PieChart) + ARIA labels WCAG 2.1 AA + dry-run mode UI (ReservedCapacityOrchestratorPanel 진입 시 dry-run toggle) + `(dashboard)` route group 보호 + scheduled dispatch KST cron weekly Mon 09:00 + monthly 1st-day 09:00 + quarterly 1st-day 09:00 + annual Jan-1 09:00 결정 wire + Phase 20 wire `52dad7f` 의 multi_cloud dashboard pattern verbatim EXTENSION 결정 wire. 결정 wire 일자: 2026-08-26 |
| **A578** | **Capability matrix v1.47 EXTENSION FINOPS_RESERVED_CAPACITY_PLANNING + dry-run + Tests + wire scope T1~T8 + CR lessons applied 18종 결정 wire** — Phase 21 = §F37.6 + §F37.7 + §F37.8 EXTENSION 결정 wire = §F37.6 Capability matrix v1.46 → v1.47 EXTENSION FINOPS_RESERVED_CAPACITY_PLANNING 1 NEW row industry-agnostic 4-industry grants ✅/✅/✅/✅ (CR 12-1 L4 precedent 미러, FINOPS_MULTI_CLOUD_UNIFIED_RECONCILIATION Phase 20 wire + FINOPS_PRICING Phase 19 wire + FINOPS_COMMITMENT Phase 18 wire + FINOPS_SUSTAINABILITY Phase 17 wire + FINOPS_REPORTING Phase 16 wire + FINOPS_TAG_GOVERNANCE Phase 15 wire + FINOPS_OPTIMIZATION Phase 14 wire + FINOPS_FORECASTING_CAPACITY_PLANNING Phase 13 wire + FINOPS_ANOMALY_DETECTION+FINOPS_BUDGET_ALERT Phase 12 wire + FINOPS_SHOWBACK+FINOPS_CHARGEBACK Phase 11 wire + SLO_ENGINEERING Phase 10 wire + CHAOS_ENGINEERING Phase 9 wire + PERFORMANCE_TESTING Phase 8 wire + OBSERVABILITY_TRACES+OBSERVABILITY_METRICS Phase 7 wire + AUDIT_LOG_RETENTION Phase 6 wire + AUDIT_LOG_VIEW Epic 17 wire + MULTI_REGION_BACKUP+MULTI_REGION_FAILOVER Phase 5 wire + TENANT_IDP_MANAGEMENT Epic 16 wire + SSO_ENTERPRISE Epic 15 wire + LISTEN_NOTIFY Epic 13/14 wire + AUTH_MIDDLEWARE Phase 3 wire + LAUNCH_* 1st release wire + DEPLOYMENT_* Phase 4 wire pattern verbatim bind) + apps/api/core/capability.py MODIFIED Capability.FINOPS_RESERVED_CAPACITY_PLANNING + apps/api/dependencies/capability.py MODIFIED require_finops_reserved_capacity + apps/api/core/audit_action.py MODIFIED ActionClass.FINOPS_RESERVED_CAPACITY_PLANNING + FinopsReservedCapacityAction 8 NEW Literal (reserved_capacity_dashboard_viewed + demand_forecast_calculated + capacity_planning_recommended + commitment_recommendation_generated + reserved_capacity_dry_run_executed + reserved_capacity_kpi_refreshed + reserved_capacity_commitment_executed + reserved_capacity_orchestrator_triggered) + apps/api/core/errors.py MODIFIED 16 NEW typed exception classes (ReservedCapacityDemandForecastError + ReservedCapacityDemandForecastScopeError + ReservedCapacityDemandForecastPeriodError + ReservedCapacityDemandForecastModuleError + ReservedCapacityPlanningError + ReservedCapacityPlanningScopeError + ReservedCapacityPlanningTierError + ReservedCapacityPlanningGuardError + ReservedCapacityRecommendationError + ReservedCapacityRecommendationConfidenceError + ReservedCapacityRecommendationApprovalError + ReservedCapacityRecommendationExecutionError + ReservedCapacityOrchestratorError + ReservedCapacityOrchestratorStepError + ReservedCapacityDryRunError + ReservedCapacityIdempotencyError) CR 12-5 D-14 envelope + alembic 0053 phase_21_reserved_capacity_planning.py NEW 8 NEW tables + RLS 8 tables + 1 preview table + drift detector `tests/integration/test_capability_matrix_v1_47_drift.py` NEW 8 NEW pytest cases EXTENSION + ALLOWED_SERVICE_SUBMODULES (CR 11-3 lesson) 즉시 sweep EXTENSION = m21_finops_reserved_capacity 신규 submodule 등록 + Phase 11~20 verbatim EXTENSION (Phase 20 m20_finops_multi_cloud 패턴 보존). / §F37.7 EXTENSION 결정 wire = audit action EXTENSION 8 NEW (reserved_capacity_dashboard_viewed + demand_forecast_calculated + capacity_planning_recommended + commitment_recommendation_generated + reserved_capacity_dry_run_executed + reserved_capacity_kpi_refreshed + reserved_capacity_commitment_executed + reserved_capacity_orchestrator_triggered) + 16 NEW typed exception classes (CR 12-5 D-14 envelope). / §F37.8 EXTENSION 결정 wire = dry-run mode 결정 wire (`--finops-reserved-capacity-orchestrator-dry-run` 1 NEW CLI flag + dry-run 시 actual reserved_capacity_orchestrator_triggered audit-first INSERT skip) + dry-run preview 결과 결정 wire (phase_21_orchestration_preview 1 table + preview_id UUID PK + tenant_id UUID + period_key TEXT + orchestration_data JSONB + computed_at TIMESTAMPTZ DEFAULT NOW() + trace_id TEXT + RLS 자동 적용 CR 0-2 verbatim + audit-first INSERT reserved_capacity_dry_run_executed) + tests 결정 wire (~+108 NEW pytest PASS (demand_forecast_aggregator 24 + capacity_planning_aggregator 30 + commitment_recommendation_engine 24 + reserved_capacity_orchestrator 30 = ~+108 NEW pytest PASS) + ~+7 NEW vitest PASS (DemandForecastPanel 1 + CapacityPlanningPanel 1 + CommitmentRecommendationPanel 1 + ReservedCapacityOrchestratorPanel 1 + ReservedCapacityTrendMiniChart 1 + ko-KR SSOT 2 + finops reserved_capacity dashboard parity CR 12-5 D-PARITY-01 = ~+7 NEW vitest PASS) + 0 NEW ruff + 0 regressions) + wire scope T1~T8 결정 wire (T1 4 backend aggregators + T2 dashboard 5 NEW sub-components + T3 alembic 0053 8 tables + RLS 8 tables + 1 preview table + T4 audit action EXTENSION 8 NEW + 16 NEW typed exception classes + T5 capability v1.47 EXTENSION + T6 scheduled_dispatch_job wire + T7 dry-run mode + 1 NEW CLI flag + T8 3중 게이트 FINAL CLEAN atomic commit) + estimated ~+25-30 files estimate = ~+18 NEW + ~+12 MODIFIED 결정 wire + Phase 20 wire `52dad7f` 의 ~30-35 files pattern verbatim EXTENSION 결정 wire + Phase 11~20 wire 의 ~25-30 files pattern verbatim EXTENSION + A19 cohesion pattern 9 surface EXTENSION PASS 결정 wire (FinOps Reserved Capacity Planning surface NEW = F37.1~F37.8 + Phase 20 multi_cloud + Phase 19 pricing + Phase 18 commitment + Phase 17 sustainability + Phase 16 reporting + Phase 15 tag governance + Phase 14 optimization + Phase 13 forecasting + Phase 12 anomaly detection + Phase 11 showback carry-over chain 정합) + CR lessons applied 18종 (CR 0-2 + CR 1-1 + CR 4-3/4-4 + CR 9-6 + CR 11-3 + CR 11-4 + CR 12-1 + CR 12-5 D-14 + CR 12-5 D-PARITY-01 + CR 12-5 D-GATE-01 + A19 + A36 + AD-14 + AD-22 + AD-49 + NFR4 + NFR18) + D-FINOPS-10 신규 honestly DEFER 보존 + Phase 21 carry-over 결정 wire 의 AD-49 신규 (a)~(g) 7 sub-decisions 모두 결정 wire 진입 (Phase 21 territory 의 composition layer 진입 시점에 deferred 결정 wire = demand_forecast_aggregator 의 5-module cross-join backend detail + capacity_planning_aggregator 의 reserved_capacity_tier selection algorithm detail + commitment_recommendation_engine 의 confidence + risk scoring detail + reserved_capacity_orchestrator 의 composition_step_chain 5 step detail — 모두 단일 sprint `wire` 진입이 아닌 docs-only entry 에서 honestly defer 결정 wire 보존) + 1-day atomic sprint discipline 결정 wire. 결정 wire 일자: 2026-08-26 |

---

## 부록 B. 보존 산식 — A×B×C×D 예산 편성 엔진 (2차 구현용)

```
예산금액 = A(편성단위수량) × B(기준비율%) × C(단위금액) × D(기간환산계수)
  산출기준: 인건비 / 평수 / 생산수량 / 매출 / 고정자산 / 차량대수 / 인원수 / 기타
  기간환산: 전통 엔진(djob exp) — 년12 / 반년6 / 분기3 / 월1 / 일30
           ABC 엔진(3 Indirect cost) — 년1 / 분기4 / 월12 / 일365
  ※ 두 파일의 D 계수 체계가 상이함(월 기준 환산 vs 연 기준 환산) — 통합 시 월 기준으로 정규화
```

## 부록 C. 용어

| 용어 | 정의 |
|------|------|
| ABC | Activity-Based Costing. 원가를 활동 기준으로 배부하여 제품·서비스 원가를 산출하는 방법론. §7 참조. |
| ABCost | 동일 저작자의 원본 ABC 전용 엑셀 파일명(13시트). §1.2·§부록 A Q-G 참조. |
| A×B×C×D 예산 편성 엔진 | 예산금액 = A(편성단위수량) × B(기준비율%) × C(단위금액) × D(기간환산계수)로 산출하는 원본 예산 체계. 2차 구현. §부록 B 참조. |
| append-only | 데이터가 삭제·수정 없이 추가만 되는 저장 정책 [A8]. 수불 원장·감사로그에 적용. |
| BEP | Break-Even Point. 손익분기점. 총수입 = 총비용이 되는 매출량. §6.1 (6). |
| BOM | Bill of Materials. 제품 1단위 생산에 필요한 자재·공정 명세. §6.1 (1), §7.3. |
| BOM 자기참조 1 | 유통품이 자기 자신을 BOM에 자식 품목으로 두는 원본 구조. 웹에서는 `merchandise` 유형으로 대체. |
| CCR | Capacity Cost Rate = 부서 원가 ÷ 실제적 조업능력. TDABC의 핵심 지표. §7.2. |
| CVP | Cost-Volume-Profit. 원가·조업도·이익 관계 분석. §6.1 (6). |
| FTE | Full-Time Equivalent. 일용직 등 비정규 근로자를 정직원 환산한 인원수. §6.1 (2). |
| RLS | Row-Level Security. PostgreSQL·Supabase가 제공하는 행 단위 접근 제어. 멀티테넌트 격리에 사용. |
| TDABC | Time-Driven ABC. 시간방정식 + CCR 기반의 간소화된 ABC. Kaplan 2004 이후 표준. §7.2. |
| 기계시간 | 배부기준 ③ 채택 시 노출되는 제품별 월 기계 가동 시간 입력값. §6.1 (3). |
| 관리 회계 뷰 | 재고평가·제조원가명세서와 분리된, 제품별 완전원가 손익을 보여주는 관리용 뷰 [A2]. |
| 매출원가 | 판매분에 대응하는 제조원가. 미판매 생산분은 재고자산으로 이월 [A4]. |
| 미사용능력 | 총작업가능시간 − 생산요구시간(전통) / 실제적 조업능력 − 사용시간(TDABC) — 금액화하여 별도 보고 [A9]. |
| 반제품 | BOM 매트릭스에서 자식 품목이 될 수 있는 중간재. §4.1·§6.1. |
| 배부기준 | 원가를 제품·활동·부문 등 원가대상에 분배하는 동인. 제조경비는 직접노무원가/직접노무시간/기계시간 3종 택1 [A5, Q-A]. |
| 부문귀속명세서 | 겸영 기업의 카브아웃 분할 근거를 공시하는 보고서. §7.3, §9 #21. |
| 사용자 부하 추정 | 동인 실적 미확보 시 사용되는 추정 입력. `is_estimated` 배지 표시. [A11, E7] |
| 세법 2기준 | 법인세법 시행규칙 제76조가 정한 공통비 분할 기준 — 매출액 비례 / 개별비용 비례 [A10]. |
| 실제적 조업능력 | 이론적 조업능력 × 80% (기본). TDABC의 분모. §7.2. |
| 역분개 | 마감된 데이터의 정정을 (삭제+재기록)이 아닌 (반대 entry + 신규 entry)로 처리하는 방식 [A8]. |
| 영업 손익 | 매출총이익 − 판매일반관리비. §6.1 (5). |
| 완전원가(full_cost) | 제조원가 + 관리인건비배부 + 판관비배부. 관리 뷰 전용 [A2]. |
| 원가경영관리 | 본 제품의 정식 명칭(부제). 단일 표기로 통일. |
| 원가풀(Resource Pool) | 간접비를 1차로 모으는 단계(ABC Step 1). 설비/매출/인원 3기준 비율. §7.1. |
| 제조원가(manufacturing_cost) | 직접재료비 + 직접노무비 + 제조간접비 3요소 합 [A2]. 재고 평가·제조원가명세서 기준. |
| 제품 재고 조정 | 생산기준 배부에서 생산·판매 수량차와 단가차로 생기는 재고 증감의 손익 표시 라인 (V4 자동 산출). |
| 전진법(prospective) | 설정·BOM 변경을 미래 기간에만 적용하고 과거 마감분은 불변으로 두는 원칙 [A7]. |
| 카브아웃 | 겸영 기업의 공통비를 세법 2기준으로 부문 분할하는 절차 [A10]. |
| 테넌트 | 멀티테넌트 SaaS에서 한 기업(한 가입 단위)의 데이터 격리 단위. §13.2 RLS. |
| 활동(Cost Activity) | 원가풀에서 원가대상으로 배부하기 위한 중간 단계. 주요활동 5~15개. §7.1. |
| 회사부담임률 | 생산직 노무비 합계 ÷ 생산요구시간 (원본 operation 주석(3)). |

*— 본 표는 §3–§15에서 사용된 도메인 어휘를 §부록 A·B·D와 동기화한 결과다. 신규 용어는 §부록 A의 결정 이력 방식(추가일·출처)으로 추후 보강한다.*

---

## 부록 D. 추론 인덱스 (Assumptions Index)

PRD 본문에서 단언되었으나 사용자(원가바이블 저작자 + 기획 세션 2026-07-22)에서 직접 확정을 받지 못한 추론성 룰. 각 항목은 본문 `[ASSUMPTION: ...]` 마커와 1:1 대응한다. 해소 시 본 표에서 제거 + §부록 A의 결정 이력으로 승격.

| ID | 위치 | 추론 내용 | 추론 근거 | 추론일 | 해소 owner |
|----|------|-----------|-----------|--------|-----------|
| AS-1 | A1 | 회계연도 시작월은 테넌트별 가변 | 동일 저작자 djob 시트가 "회계연도 시작월" 필드를 보유 | 2026-07-12 | PM (SaaS 다국어 정책 확정 시) |
| AS-2 | §7.2 | CCR 산출 단위 = 부서 (단일) | 부서 단위 산출이 1인 운영자 SaaS의 운영 부담을 낮춤; Kaplan TDABC 정론의 일반적 사례 | 2026-07-12 | PM (1차 파일럿 1곳에서 검증 후) |
| AS-3 | §6.1 (4) | 관리인건비배부 = 직접노무비 비례 | 원본 pl 시트의 "관리인건비 = 직접인건비의 29.3% 상당 배부" 패턴 계승 | 2026-07-12 | 운영자 (M5 완료 후 회귀 검증) |
| AS-4 | §6.1 (3) | 제조경비 배부기준 3종(직접노무원가/직접노무시간/기계시간) 택1 | Q-A 결정; 기계시간 신규 입력은 추론성 (원본에는 없음) | 2026-07-12 | 운영자 (M0 온보딩 시 사용자 선택 검증) |
| AS-5 | §8.1 M9 | 동인 토글(건수 / 비율 %) | 원본 ABCost의 "Yes/No" 토글 계승; UI 토글 노출 방식은 추론 | 2026-07-12 | UX designer |
| AS-6 | §9 #21 | 부문귀속명세서 카브아웃 분할 근거 공시 형식 | 법인세법 시행규칙 제76조 2기준의 시각화 방법(표/차트)은 추론 | 2026-07-12 | 운영자 + UX designer |

해소 절차: 각 항목의 owner가 결정 확정 시 본 표에서 삭제하고 §부록 A에 결정 이력(Q-?)으로 추가한다. 결정 사항은 memlog에 `event: "AS-N resolved → Q-? 결정"`으로 기록한다.

*— 문서 끝 —*
