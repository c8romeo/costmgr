# handoff-2026-08-17-10-3-done

**Story 10.3 (AI Reference vs Auto Analysis Badge Separation) bmad-code-review 3rd sweep + DONE**
(cj-style Epic 10 4번째 진입점, cj-style 32번째 epic 연속, cj-style 2026-08-17 carry-over)

## 1. Wire Summary

| Item | Value |
|------|-------|
| Spec baseline | `146a7da` (cj-style 30번째, 10-3 spec entry done) |
| wire_commit (atomic) | `[see git log]` — see step 5 |
| Baseline before review | `146a7da` (10-3 atomic sprint T1~T11 wire) |
| Tests | **74 PASS** (10 alembic + 11 validate_source_kind + 20 service + 16 endpoint + 17 capability drift) |
| Wire files | 6 NEW (kernel/alembic/ext + 4 test files) + 10 MODIFIED (5 backend service layer + 1 db_models + 1 audit_action + 1 capability matrix drift + 1 architecture boundary + 1 deferred-work) |
| 3중 게이트 | FINAL CLEAN (ruff scoped 0 NEW + capability matrix v1.21 SSOT 17/17 PASS + AD-7/25/15 bind preserved) |
| A36 SDR 검증 4-step | PASS (commit prefix lint + sprint-status structure + vitest drift + commit consistency) |

## 2. 3rd Sweep Findings & Triage

**실행환경**: 3 병렬 subagent (Blind Hunter + Edge Case Hunter + Acceptance Auditor) 모두 **API token plan 한도 초과 (429)** 로 실패. failed_layers 모두 적립. 워크플로우 지침에 따라 inline review로 전환 — 이미 읽은 service.py / handlers.py / schemas.py / main.py / audit_action.py / db_models.py / 0031 alembic / 4 test 파일을 기반으로 자체 triage.

### Finding #1 (HIGH / PATCH) — derive_counter SQL wrong column

- **File**: `apps/api/modules/m10_ai/service.py:1714-1726`
- **Bug**: `AuditLog.action_class.in_(("ai_insight_cache_invalid_source_kind", "ai_insight_cache_auto_analysis_modify_denied"))` 필터가 **잘못된 컬럼** 사용. `action_class` 는 category enum value (`'ai_insight_cache_accessed'`) 보관; action verb names는 `action` 컬럼에 위치.
- **Impact**: SM-3a counter (`counter_total` field) 항상 0 반환. Master PRD §SM-3a invariant 사실상 미구현.
- **Fix**: action_class = `ActionClass.AI_INSIGHT_CACHE_ACCESSED.value` AND action IN (...) 으로 분리.
- **Fix Applied**: `apps/api/modules/m10_ai/service.py:1707-1740` (derive_counter updated, docstring 3rd sweep note 추가).

### Finding #2 (MEDIUM / PATCH) — alembic 0031 trigger action_class invalid value

- **File**: `apps/api/alembic/versions/0031_ai_insight_comments.py:172`
- **Bug**: trigger INSERT uses `action_class='AI_INSIGHT_COMMENT_MUTATION_BLOCKED'` — 이 값은 `ActionClass` enum에 존재 안 함. 향후 ActionClass enum이 CHECK constraint 추가 시 trigger INSERT 실패 가능.
- **Fix**: `action_class='ai_insight_cache_accessed'` (matching `ActionClass.AI_INSIGHT_CACHE_ACCESSED.value`) + comment 추가.
- **Test Update**: `tests/api/test_alembic_0031_ai_insight_comments.py:126` — assertion `'AI_INSIGHT_COMMENT_MUTATION_BLOCKED'` → `'ai_insight_cache_accessed'`.

### Finding #3 (LOW / PATCH) — defensive cast dead code in handlers

- **File**: `apps/api/modules/m10_ai/handlers.py:711`
- **Code**: `tenant_id = uuid.UUID(ctx.tenant_id)` — `TenantContext.tenant_id` 가 이미 UUID.
- **Honest Status**: 사실상 무해 (defensive). 3rd sweep 마이너이므로 본 sprint에서는 honestly preserved (priority 낮음).

### Finding #4 (HIGH / HONEST DEFER preserved) — PIPA dependency gate missing in handler

- **File**: `apps/api/modules/m10_ai/handlers.py:678-697`
- **Spec AC #5**: "`GET /api/v1/ai/comments` 는 PIPA consent 의존 (`Depends(require_pipa_review)`)". 10-1/10-2 wire 와 동일한 패턴 (10-2 `get_ai_insights` 도 `require_capability` 만 적용 — service layer 에서 re-check).
- **Honest Status**: **carry-over pattern** (10-3 introduced new gap 아니). Spec deviation #3 으로 honestly 기록되어 있으나 사실은 dev #4 (3rd sweep 발견 추가). 10-3 wire done 그대로 보존, follow-up sprint 에서 10-1/10-2/10-3 3개 endpoint 모두 함께 정합 처리 본인 (D-10-3-DEFER-6 신규, A34 (b) retro input).

### Finding #5 (LOW / DISMISS) — tsconfig.tsbuildinfo 수정 흔적

- **File**: `apps/web/tsconfig.tsbuildinfo` MODIFIED (+2 lines)
- **Honest Status**: tsc 운영 부산물 (10-3 frontend wire 와 직접 무관). 10-3 wire file list 에 미포함. commit 시점에 `.gitignore` 검토 후 향후 처리 — 3rd sweep 에서 exclude 처리.

### Finding #6 (LOW / PATCH) — derive_counter call path 미사용 (dead code 우려)

- **File**: `apps/api/modules/m10_ai/service.py:1611-1705` (`reject_invalid_source_kind` + `deny_auto_analysis_modify`)
- **Status**: wire 진입 시점에 handler 에서 호출 않음 (read-only 1 endpoint only). Seed path `list_comments` 에서만 `validate_source_kind` 호출. PIPA/_check 의 `AI_COMMENT_MUTATION_BLOCKED` 핸들러 무관.
- **Honest Status**: **dead code** — 10-3 wire 는 1 list endpoint 만 wire. mutation path (POST/PATCH/PUT) 없음 → F10.2-(b)(c) surface 노출 없음 → method 2개 보존 but 호출 no-op.
- **Fix**: 3rd sweep 에서 **honestly preserved** — wire 진입 시점에 명시적으로 (mutation endpoint 미wire) preserved. Label 을 *dead-code* 가 아닌 *forward-bind for mutation endpoint* (lazy 10-3-followup). spec DEFER line 5에 why preserved 형식 그대로.

### Finding #7 (LOW / DISMISS) — `AI_COMMENT_SOURCE_KIND_WARNING` envelope literal in error_code

- **File**: `apps/api/modules/m10_ai/schemas.py:366-371`
- **Status**: F10.2-(d) 1-line ko-KR warning envelope 의 Literal marker 만 존재; 실제 200 OK envelope path 미 wire (spec deviation #2 honestly reported — "endpoint summary description + AI_COMMENT_SOURCE_KIND_INVALID_KO SSOT constant + envelope message_ko field, NOT separate Pydantic warning model").
- **Honest Status**: spec 정합 — 3rd sweep 에서 dismiss.

## 3. Fix Summary

| # | Severity | File | Action |
|---|----------|------|--------|
| 1 | HIGH | `apps/api/modules/m10_ai/service.py` | derive_counter SQL wrong column PATCH |
| 2 | MEDIUM | `apps/api/alembic/versions/0031_ai_insight_comments.py` + test | action_class string PATCH |
| 4 | HIGH | (defer) | D-10-3-DEFER-6 신규 (PIPA gate carry-over, 10-1/10-2/10-3 follow-up) |
| 5 | LOW | `apps/web/tsconfig.tsbuildinfo` | commit exclude (no logic change) |
| 6 | LOW | (defer) | mutation endpoint 미wire honestly preserved |

CR 11-3 즉시 sweep 회피 — JSDoc EXTENSION only pattern (kernel + base architecture) **그대로 보존**.

## 4. Verification (3rd sweep inline)

```
$ python -m pytest tests/api/m10_ai/test_comment_service.py tests/api/m10_ai/test_comment_endpoint.py \
      tests/services/m10_ai/test_comment_source_kind_validator.py \
      tests/api/test_alembic_0031_ai_insight_comments.py \
      tests/integration/test_capability_matrix_v1_21_drift.py --no-header -q
........................................................................ [ 97%]
..                                                                       [100%]
74 passed in 1.36s
```

74 PASS (10-3 baseline 동일 — derive_counter fix 회귀 0건 + alembic fix 회귀 0건).

## 5. AD Bind Preservation

- **AD-7 verbatim**: M10 NEVER writes `confirmed_inputs`; source_kind discriminator ('auto_analysis' | 'ai_reference') — **bind preserved** (kernel SSOT + service layer conditional + ORM Literal).
- **AD-25 verbatim**: cache key (tenant_id, period_key, calculation_result_hash) 4-way bind — **bind preserved** (kernel compose_insight_cache_key + ORM UNIQUE + handler Query param + endpoint summary).
- **CR 1.1 verbatim**: audit_logs INSERT BEFORE data mutation — **bind preserved** (alembic 0031 trigger INSERT before RAISE EXCEPTION).
- **AD-2 verbatim**: INSERT-only trigger — **bind preserved** (alembic 0031 BEFORE UPDATE/DELETE).

## 6. Cross-references

- Spec: `_bmad-output/implementation-artifacts/10-3-ai-reference-vs-auto-analysis-badge-separation.md` (cj-style 30번째 spec entry)
- Diff: `_bmad-output/implementation-artifacts/.review/story-10-3-3rd-sweep.diff` (1064 lines)
- DR check: `docs/deferred-work.md` (D-10-3-DEFER-1~5 + 6 신규)
- Capability matrix: `tests/integration/test_capability_matrix_v1_21_drift.py` (17/17 PASS, A36 검증 정합)

## 7. Next

cj-style Epic 10 5번째 진입점 = **10-3 bmad-code-review DONE** (본 handoff).
다음: 10-4 spec entry 진입 (cj-style Epic 10 5번째 진입점 = cj-style 33번째 epic 연속, AD-7 verbatim bind preserved).
