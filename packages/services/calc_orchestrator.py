"""packages.services.calc_orchestrator — wraps the single calc port (AD-19, Epic 4).

Story 0.1 stub. Real implementation lands in Story 4.2 (single calculation endpoint
in a REPEATABLE READ transaction). The orchestrator is the only caller of
packages.cost_engine.ports.calc_port.CalcPort.
"""
