// apps/web/postcss.config.mjs — PostCSS config for Tailwind 4 + autoprefixer
// Story 0.5 — T1.2 (AC #1)

const config = {
  plugins: {
    "@tailwindcss/postcss": {},
    autoprefixer: {},
  },
};

export default config;
